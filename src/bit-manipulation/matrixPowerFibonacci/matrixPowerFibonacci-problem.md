# 행렬 거듭제곱을 이용한 피보나치 (Matrix Power Fibonacci)

## 중요도 · 난이도

| 항목 | 값 |
|------|-----|
| 중요도 | ★★ 중 — 빈출 |
| 난이도 | 고급 |

## 함수 인터페이스

```ts
export function matrixPowerFibonacci(n: bigint): bigint;
```

## 제약 조건

- $0 \leq n \leq 10^{18}$
- 결과 값이 매우 커질 수 있으므로 `bigint` 를 사용한다.

## 문제 상세

피보나치 수열은 다음과 같이 정의된다:

$$F_0 = 0,\quad F_1 = 1,\quad F_n = F_{n-1} + F_{n-2}\ (n \geq 2)$$

주어진 인덱스 $n$ 에 대해 $n$ 번째 피보나치 수 $F_n$ 을 `bigint` 로 반환한다.

$n$ 이 최대 $10^{18}$ 까지 커질 수 있으므로, 단순 반복으로는 시간 안에 계산할 수 없다. $O(\log n)$ 수준의 시간 복잡도로 해결해야 한다.

## 예시

- $F_{10} = 55$
- $F_{50} = 12{,}586{,}269{,}025$
