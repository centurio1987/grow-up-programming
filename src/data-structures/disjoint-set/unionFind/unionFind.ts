/**
 * UnionFind — 원소 0 부터 n-1 까지를 서로소인 집합들로 나눠 두고, 두 집합을 합치고 · 원소가 속한 집합의 이름표(가장 작은 원소)를 묻고 · 두 원소가 같은 집합인지 묻는 일을 호출열 전체에서 원소 수의 역아커만 함수에 두는 분리 집합.
 *
 * **목적.** 생성 시점에 정한 원소 `0 … n-1` 을 **서로소인 집합들로 나눈 상태**(분할) 하나를 두고,
 * 두 집합을 합치는 일 · 원소가 속한 집합의 **이름표**를 묻는 일 · 두 원소가 같은 집합에 있는지 묻는
 * 일을 제공하되, 그 비용이 **호출열 전체에서** 호출 하나당 원소 수의 역아커만 함수 $\alpha(n)$ 에
 * 묶이는 것. **합친 것은 되돌릴 수 없다** — 이 계약의 비용은 그 포기와 맞바꾼 것이다(아래 「이웃 계약과의
 * 관계」).
 *
 * **원소 모형.** 원소는 **구조가 번호를 매긴다** — 생성자가 원소 수 `n` 을 받고 원소는 `0` 이상 `n` 미만의
 * 정수다. 구조가 사는 동안 원소가 늘거나 줄지 않는다. 번호 밖의 원소를 받는 연산은 던지고 상태를 바꾸지
 * 않는다(주입 정책). `tree/linkCutTree` 의 마디 번호와 같은 모형이다. 원소를 호출자의 임의 값으로 받지
 * 않는 이유는 그 값을 번호로 옮기는 일이 해시를 부르고, 해시는 한정자를 `expected` 로 바꿔 이 계약의
 * 계급을 다른 논증 위에 세우기 때문이다 — 그 옮기기는 호출자가 사전 계약(`hash/hashMapChaining`)으로 한다.
 *
 * **이름표를 그 집합의 가장 작은 원소로 정한 것이 이 계약의 선택이다.** 물려받은 문서는 `find` 를 「대표
 * 원소」로만 적고 어느 원소인지 정하지 않았다. 정하지 않으면 둘을 잃는다.
 *
 * 1. **답이 분할이 아니라 담는 모양에 기댄다.** 같은 분할에 대해 합친 순서만 다른 두 호출열이, 또는 거는
 *    방향만 다른 두 구현이 다른 수를 돌려준다. 이 계약의 상태는 분할이고(불변 사실 79 — 계약의 상태는 담긴
 *    것의 모음이지 담는 모양이 아니다) 상태가 같은데 답이 갈리면 그 갈림은 구현의 뿌리가 새어 나온 것이다.
 * 2. **축1이 그 자리를 못 잰다.** 참조 모델이 무엇을 기대할지 정해지지 않는다. 「정하지 않는다」는 자유를
 *    주는 것처럼 보이지만 검사에서 그 자리를 통째로 지운다 — `heap/leftistHeap` 이 합친 뒤 `other` 를
 *    「비운다」로 정한 것과 같은 처분이다(§「관측되지 않는 약속은 계약이 아니다」가 단일 스레드 계약에서 세
 *    번 걸렸다).
 *
 * 대가는 뿌리마다 가장 작은 원소를 하나 더 드는 일이고, 합칠 때 둘 중 작은 쪽을 고르는 상수 한 번이라
 * **상한의 계급을 바꾸지 않는다.** 나가는 것은 뿌리를 그대로 돌려주는 구현이고, 그 구현은 값 하나를 더 들면
 * 들어온다. 분할만으로 정해지는 고르기 중 원소 번호의 순서만 쓰는 것이 가장 작은 원소와 가장 큰 원소 둘이고
 * 그중 하나를 고른 것이다. **`hash/hashMapChaining` 이 열거 순서를 정하지 않고 스위트가 정렬해 견주게 한 것과
 * 처분이 갈리는 이유**는 그쪽은 순서를 정하는 순간 계급이 바뀌거나(정렬) 표현을 처방하기(칸 순서) 때문이고,
 * 여기서는 둘 다 일어나지 않기 때문이다.
 *
 * **이웃 계약과의 관계.**
 * - **`tree/linkCutTree` 와는 서로 담지 않는다**(불변 사실 54). 그쪽 계약을 지키는 구현에는 이름표를 묻는
 *   연산이 없고, 그쪽 상한은 `O(log n)` 상각이라 이쪽 상한 안에 들지 못한다 — **다만 이 둘째 갈림은 로그
 *   인수라 축3이 보지 못한다**(불변 사실 53). 반대로 이쪽 구현에는 간선을 빼는 연산이 없다. 그쪽 헤더가 적은
 *   「갈리는 자리는 `cut` 하나다」는 이 계약이 서기 전에 쓴 문장이고 이름표 연산이 둘째 자리다(그쪽 헤더는 고쳤다).
 * - **`disjoint-set/disjointSetRollback` 과는 연산 집합에서 그쪽이 되돌리기를 더 가진다.** 이쪽 구현에는
 *   되돌리는 연산이 없으므로 그쪽을 만족하지 못한다. 그쪽이 이쪽을 담는지는 **비용 열까지 대조해야 서고**
 *   (불변 사실 257) 그쪽 계약이 아직 없으므로 그 판정은 T3-05 가 한다.
 *
 * **이름표를 가장 작은 원소로 고정한 결정은 연산 선정과 따로 판정했다**(`docs/ORD-006-conventions.md` 「원칙 A」 판정표 ② —
 * KAN-027 `S27`). 목적의 「이름표」는 집합의 이름이라 **같은 분할이면 같은 값**이어야 한다(불변 사실 79). 대안 둘을 쟀다(스크래치
 * 탐침). ① **정하지 않고 성질로 판정**(이름표는 그 집합의 원소이고 같은 상태에서 원소마다 같다) — 껍데기가 `find` 마다 원소를 훑어
 * 가장 작은 원소로 바꿔 모델과 견주면 축1 에서 서지만(뿌리를 그대로 돌려주는 숲이 통과하고 자기 자신을 돌려주는 사본은 첫 경계
 * 케이스 둘째 걸음에서 걸린다), 껍데기 호출이 원소 수만큼 늘고(경계 케이스 · 3,000 걸음에서 2,815 회) vector 를 재생하는 쪽이 그
 * 훑기를 옮겨야 하며, 같은 분할에서 이름표가 호출 이력으로 갈리는 구현이 들어온다(`union(0, 1)` 뒤 0 · `union(1, 0)` 뒤 1) ·
 * 「합치기에 끼지 않은 집합의 이름표는 그대로」 · 합친 뒤 이름표를 두 옛 이름표의 작은 쪽으로 미리 아는 호출자의 쓰임이 사라진다.
 * ② **가장 큰 원소** — 관측 · vector · 비용이 전부 대칭이라 고를 근거가 없고, 바꾸면 vector 두 벌만 다시 뽑힌다. 고정의 비용은
 * 없다 — 뿌리를 돌려주는 숲과 정본의 축3 계측이 세 시나리오에서 같은 값이다(1.50 · 4.99 · 6.06 …). **그래서 고정이 배제하는 것은
 * 계급이 아니라 뿌리마다 값 하나를 덜 든 구현뿐이고**(불변 사실 59 — 잃는 것이 없다) 결정을 유지한다.
 *
 * **이 계약에 없는 것.**
 * - **가르기** — 간선 빼기(`tree/linkCutTree`)도 마지막 합치기 되돌리기(`disjoint-set/disjointSetRollback`)도.
 *   있으면 위 상한이 서지 않는 계약들이다.
 * - **집합의 크기 · 집합의 수 · 한 집합의 원소 열거.** 크기와 수는 원칙 A 의 A2 · A4 · A5′ 로 두지 않는다 — 원소마다
 *   `connected` 를 불러 조합되고, 수를 세어 두는 상수 보강으로 어느 설계든 지켜 떨어지는 계열이 없으며, **위 「목적」이 적은 일 셋
 *   (합치기 · 이름표 · 같은 집합인가) 어디에도 몇 개인지를 세는 일이 없다.**
 *   열거도 원소마다 `find` 를 불러 조합된다. 집합 크기에 묶는 상한을 따로 적으면 원소 목록을 들지 않는 설계가 떨어지지만
 *   목적의 비용 조건이 그 배제를 요구하지 않아 A4 에서 빠진다(판정표 ① 43 이 「A3」 으로 적은 줄 — 결과는 같다). 물려받은
 *   표면에도 없었다.
 * - **원소 더하기와 원소에 딸린 값.** 원소 수는 생성 시점에 정해진다.
 * - **공간.** 이 계약은 시간만 말한다(§규약1 「공간은 어느 계약에도 없다」 — 미결).
 *
 * **이름은 임시가 아니다.** 「합치기 · 찾기」는 이 계약의 두 연산 이름이고 구현 기법의 이름이 아니다(불변
 * 사실 66). 카테고리 `disjoint-set/` 도 계약 이름이다.
 *
 * **불변식.** 없다.
 *
 * 판별 절차(§규약1 「불변식 판별 절차」)를 돌린 결과다. 후보 넷이 서로 다른 자리에서 걸린다.
 *
 * - **「같은 집합인가」와 「이름표가 같은가」가 일치한다** — 관측 경로가 둘(`connected` · 두 `find` 의 비교)이라
 *   첫 물음은 지나고, **두 번째 물음에서 걸린다**: 아래 표의 `connected` 행이 그 정합을 적는다.
 * - **「이름표는 그 집합의 원소이고 이름표의 이름표는 자기 자신이다」** — 읽는 경로가 `find` 하나라 첫 물음에서
 *   걸린다. `find` 행의 의미이고 축1이 본다.
 * - **「한 번 같은 집합이 된 둘은 계속 같은 집합이다」 · 「합치기에 끼지 않은 집합의 이름표는 그대로다」** — 상태의
 *   성질이 아니라 **시간에 걸친 성질**이다(불변 사실 153). 축1의 경계 케이스가 짚는다.
 *
 * 담는 모양의 성질(나무의 높이 · 뿌리가 어느 원소인가 · 크기로 걸었는가)은 후보가 아니다. 적는 순간 그것이 표현을
 * 처방하고(불변 사실 36), 이 계약을 지키는 다른 구현(원소마다 번호표를 적는 구현)에는 그런 말이 없다(불변 사실 131).
 *
 * **연산 계약.** n 은 **생성자가 정한 원소 수**다. 구조가 사는 동안 바뀌지 않는다. 한 집합에 든 원소 수가 그보다
 * 훨씬 적어도 상한은 n 으로 적는다 — 호출자가 어느 집합을 만질지 계약이 알지 못한다(`tree/linkCutTree` 와 같다).
 * $\alpha$ 는 역아커만 함수다.
 *
 * | 연산 | 의미 | 상한 | 한정자 |
 * |---|---|---|---|
 * | `constructor(n)` | 원소 `0` 부터 `n-1` 까지를 각각 홀로 선 집합으로 둔다. `n` 은 0 이상의 정수여야 하고, 아니면 `RangeError` | O(n) | worst |
 * | `find(x)` | `x` 가 속한 집합의 **가장 작은 원소** — 그 집합의 이름표다 | O(alpha(n)) | amortized |
 * | `union(x, y)` | `x` 의 집합과 `y` 의 집합을 하나로 합친다. 이미 같은 집합이면(`x === y` 포함) **상태가 바뀌지 않는다** | O(alpha(n)) | amortized |
 * | `connected(x, y)` | `x`·`y` 가 같은 집합에 있으면 `true` — `find(x) === find(y)` 와 같다. `x === y` 면 `true` | O(alpha(n)) | amortized |
 *
 * **`amortized` 는 호출열 전체로 읽는다**(§「상각 행의 약속은 호출열 전체로 적는다」). 생성자로 연 뒤 부른 세 행의
 * 호출이 `m` 개면 **생성자를 포함한 총비용이 $n + m\,\alpha(n)$ 의 상수배 안**이다. 호출 하나가 원소 수의 로그만큼
 * 튀어도 계약을 어기지 않는다. 합치기와 찾기를 함께 이 값에 묶는 설계가 있다는 것은 문헌의 결과이고 이 계약은
 * 그 증명을 옮기지 않는다(확인 안 함) — 정본이 그 설계다.
 *
 * **축3은 $O(\alpha(n))$ 을 `O(1)` 로 판정한다**(`docs/ORD-006-conventions.md` 「`unionFind` 의 Bound 는 늘리지
 * 않는다」 — B19 확정). 크기 사다리 세 점에서 역아커만 함수가 같은 값이라 실측 비율이 `O(1)` 구간 한복판에 놓이고,
 * 판정 규격에 새 구간을 그어도 `O(1)` 과 겹쳐 아무것도 다르게 가르지 못한다. 그래서 스위트가 이 표에서 판정하는
 * 것은 **「원소 수의 어떤 거듭제곱보다도 느리게 자란다」**이다. 로그 인수만큼 어기는 구현 둘 — 길을 줄이지 않고
 * 높이로 거는 숲(`_contract/_fixtures/linkingPartition.ts` 의 `byRank`) · 작은 쪽 번호표를 다시 적는 구현
 * (`_contract/_fixtures/relabelingPartition.ts` 의 `smaller`) — 이 스위트 셋을 전부 통과하고, 짝지어 두 배씩
 * 합치는 입력에서는 각자 걸리는 행(찾기 · 합치기)의 호출 평균이 6 · 7 · 8 로 원소 수의 로그를 그대로 찍는데도
 * 비율 1.17 · 1.14 가 `O(1)` 구간 안이다. **그 통과는 계약을 지킨다는 뜻이 아니다**(불변 사실 62).
 *
 * **상한 $O(\alpha(n))$ 은 정본이 서는 계급으로 골랐다**(불변 사실 51).
 *
 * - **더 약하게 `O(log n)` 으로 적으면** 길을 줄이지 않는 구현이 전부 들어온다. 그러면 이 계약이 되돌리기를 포기하고
 *   얻는 것 — `tree/linkCutTree` 헤더가 적은 「합치기만 있는 계약은 상한을 상수 계급까지 내릴 수 있다」 — 이
 *   계약 어디에도 남지 않고, 되돌릴 수 있는 분리 집합과 비용으로 갈리지 않는다. **다만 그 갈림은 로그 인수라 축3이
 *   보지 못한다** — 경로를 줄이지 않는 구현을 배제하는 근거는 축이 아니라 가이드가 받는다(같은 절 · 불변 사실 45).
 * - **더 강하게 `O(1)` 로 적으면** 정본이 계약보다 강한 것을 약속하게 된다(`heap/vanEmdeBoasTree` 의 같은 자리).
 *   상수 상각으로 두는 구현이 있는지는 판정하지 않았다(문헌에 하한 결과가 있다 — 확인 안 함).
 *
 * **세 행이 `amortized` 인 것.** `worst` 로 적으면 길을 줄이는 계열이 나간다 — 그 계열에서는 줄이기 전의 긴 길을
 * 처음 오르는 호출 하나가 원소 수의 로그만큼 걷는다. 정본이 실제로 그렇다: 짝지어 두 배씩 합친 뒤 큰 번호부터 찾으면
 * 단일 호출 최대가 11 · 13 · 15 이고 같은 호출열의 호출 평균은 3.0 안팎이다. 그 계열을 배제할 이유가 목적에 없다.
 * **이 갈림도 로그 인수라 축3이 보지 못한다** — 그 입력을 `worst` 로 읽어도 비율 1.18 · 1.15 로 통과한다. `expected`
 * 는 `amortized` 와 비교할 수 없고 무작위를 실제로 뽑는 구현을 요구하게 되므로 적지 않는다. `constructor` 만 `worst`
 * 인 것은 상각이 한 인스턴스의 호출열에 대한 값인데 생성자가 그 호출열을 여는 연산이라 나눌 호출열 안에 없기
 * 때문이고, 축3도 이 행을 재지 않는다(§규약2 시나리오 규칙 2 — 면제).
 *
 * **물려받은 표면을 셋 고쳤다.** 연산 집합과 이름은 그대로 셋이다. 스텁에 계약 헤더가 없었고 물려받은 문서만 있었다.
 *
 * - **`find` 가 돌려주는 원소를 「대표 원소」에서 「가장 작은 원소」로 정했다**(위 목적 항목).
 * - **「$1 \le n$」 · 「$0 \le x, y < n$」 을 호출자의 전제에서 `RangeError` 로 옮겼다.** 전제를 두면 축1이 그 자리를
 *   못 잰다(불변 사실 82). `n = 0` 을 받는다 — 원소가 없는 분할이고, 그 뒤로 부를 수 있는 연산이 없다
 *   (`tree/linkCutTree` 와 같다).
 * - **물려받은 문서의 스토리 · 질의 수 제약 · 시간 제한과 벽시계 성능 시험을 뺐다.** 계약의 문장이 아니고(불변 사실
 *   38) 벽시계는 불변 사실 7 이다.
 *
 * `union` 은 물려받은 대로 값을 돌려주지 않는다. 약속이 관측되는가를 물으면(불변 사실 82) 「이미 같은 집합이면
 * 아무것도 안 바뀐다」는 앞뒤의 `connected`·`find` 가 관측한다 — `tree/linkCutTree` 의 `link` 가 `boolean` 을 돌려주는
 * 이유는 **사이클을 만들 호출을 거절한다**는 약속이 거기 있기 때문이고, 이 계약에는 거절이 없다. 물려받은 문서에는
 * 상한이 적혀 있지 않았다.
 *
 * **주입 정책.**
 * - 주입받는 것이 없다. 원소는 번호 하나이고 비교자도 해시도 들어올 자리가 없다. 이름표가 번호의 순서를 쓰는데
 *   그 순서는 정수의 순서로 고정이다.
 * - `n` 은 **0 이상의 정수**여야 하고, 아니면 `RangeError` 를 던진다.
 * - 원소 번호는 **`0` 이상 `n` 미만의 정수**여야 하고, 아니면 세 연산 모두 `RangeError` 를 던지고 상태가 바뀌지 않는다.
 *   **값으로 답하지 않는 이유는 반환 타입에 「그런 원소는 없다」를 담을 자리가 없어서다**(불변 사실 132). `find` 의 수는
 *   이름표라는 답이고 `connected` 의 `false` 는 「다른 집합이다」라는 답이며, `union` 은 돌려주는 값이 없다.
 *
 * **검증 등급.** `complexity`.
 * 판정 절차의 2번에서 걸린다 — 세 행을 **함께** 만족하는 자명한 구현이 없다. 배열 하나로 분할을 적는 길은 둘이고
 * 둘 다 한 행 이상에서 원소 수에 비례한다(원소 수를 사다리 1,024 · 4,096 · 16,384 에 올린 계약 스위트).
 *
 * - **원소마다 부모를 적고 인자 순서대로 걸기**(`_contract/_fixtures/linkingPartition.ts` 의 `underSecond` ·
 *   `underFirst`) — 큰 집합을 한쪽 인자로 넘기며 하나씩 붙이면 사슬이 서서, 합치기가 128.75 · 512.75 · 2,048.75
 *   ($r$ = 3.98 · 4.00) · 찾기·묻기가 396 · 1,547~1,565 · 6,167 안팎($r$ = 3.90~3.99)으로 걸린다. 두 방향이 서로의
 *   거울상이라 스위트는 한 호출열 안에서 두 방향을 함께 겨눈다.
 * - **원소마다 집합 번호표를 적고 한쪽을 전부 다시 적기**(`_contract/_fixtures/relabelingPartition.ts` 의 `first`) —
 *   묻기는 2.00 고정으로 **통과하고** 합치기가 129.25 · 513.25 · 2,049.25 로 걸린다. 자명한 구현이 전 행에서 걸리지 않는
 *   것(불변 사실 84)의 이 계약판이다.
 *
 * 길을 줄이지 않고 높이로 걸거나 작은 쪽만 다시 적으면 로그까지 내려오지만 그 둘은 이미 자명하지 않은 설계(높이로
 * 거는 규칙 · 두 배 논증)이고, 그래도 이 계약의 상한에 로그 인수만큼 못 미친다(위 「연산 계약」). 역아커만 함수까지
 * 내려오려면 **높이로 거는 규칙과 지난 길을 줄이는 일을 함께** 써야 하고, 둘을 함께 쓴 호출열의 상각 논증이 이 등급의
 * 내용이다.
 *
 * **필요충분조건.**
 * - 의미: 번호 매긴 원소의 분할 · 합치기 · 같은 집합 묻기 · **분할만으로 정해지는 이름표** · 되돌리지 않기. 간선 빼기를
 *   더하면 `tree/linkCutTree` 이고 마지막 합치기 되돌리기를 더하면 되돌릴 수 있는 분리 집합이며, 이름표를 버리면 집합마다
 *   원소를 모으는 일이 `connected` 를 쌍마다 부르는 일이 된다. 이름표를 뿌리로 두면 답이 분할이 아니라 담는 모양에 기댄다.
 * - 비용: 세 행의 **호출열 총비용**이 원소 수의 로그보다 느리게 자라야 한다. 합치기가 원소 수에 비례하면 번호표를 다시
 *   적는 목록이고, 찾기가 비례하면 부모 사슬이다. 로그에 머물면 되돌릴 수 있는 분리 집합이 같은 값에 하는 일을 되돌리기를
 *   포기하고 하는 셈이다 — **이 마지막 갈림은 로그 인수라 축3이 보지 못하고**, 판정 규격이 그 한계를 안고 있다는 사실을 위
 *   「연산 계약」이 적는다.
 */
export class UnionFind {
  constructor(n: number) {
    throw new Error("Not implemented");
  }

  find(x: number): number {
    throw new Error("Not implemented");
  }

  union(x: number, y: number): void {
    throw new Error("Not implemented");
  }

  connected(x: number, y: number): boolean {
    throw new Error("Not implemented");
  }
}
