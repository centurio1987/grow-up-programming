/**
 * DisjointSetRollback — 원소 0 부터 n-1 까지의 분할에 합치기 · 이름표(가장 작은 원소) 묻기 · 같은 집합 묻기 · 마지막 합치기 호출 되돌리기를 두고, 되돌리기가 섞인 호출열 전체에서 호출 하나당 원소 수의 로그에 두는 분리 집합.
 *
 * **목적.** 생성 시점에 정한 원소 `0 … n-1` 을 **서로소인 집합들로 나눈 상태**(분할) 하나를 두고,
 * 두 집합을 합치는 일 · 원소가 속한 집합의 이름표를 묻는 일 · 두 원소가 같은 집합에 있는지 묻는 일 ·
 * **아직 되돌리지 않은 합치기 호출 중 마지막 것을 되돌리는 일**을 제공하되, 그 비용이 되돌리기가 섞인
 * **호출열 전체에서** 호출 하나당 원소 수의 로그에 묶이는 것. 되돌리기를 얻는 대가가 그 로그다 — 되돌리지
 * 않는 분리 집합(`disjoint-set/unionFind`)은 같은 세 연산을 역아커만 함수에 둔다(아래 「이웃 계약과의 관계」).
 *
 * **원소 모형과 이름표는 `disjoint-set/unionFind` 의 것을 그대로 쓴다.** 생성자가 원소 수 `n` 을 받고 원소는
 * `0` 이상 `n` 미만의 정수이며, 번호 밖의 원소를 받는 연산은 던지고 상태를 바꾸지 않는다. `find` 는 그 집합의
 * **가장 작은 원소**다. 그대로 쓰는 근거는 둘이다.
 *
 * 1. **되돌리기가 두 선택 중 어느 것에도 기대지 않는다.** 되돌리기가 돌려놓는 것은 분할이고, 가장 작은 원소는
 *    분할만으로 정해지므로 되돌린 뒤의 이름표도 분할만으로 정해진다. 저쪽이 이름표를 정한 이유(답이 담는 모양에
 *    기대면 축1이 그 자리를 못 잰다 — 그쪽 헤더 「목적」)가 여기서도 그대로 선다. 원소 번호를 구조가 매기는 이유
 *    (호출자의 값을 번호로 옮기는 일은 사전 계약의 몫이다)도 되돌리기와 무관하다.
 * 2. **같은 이름의 세 연산이 두 계약에서 같은 뜻이면 스위트 항목을 두 계약이 함께 쓴다.** 저쪽 경계 케이스
 *    아홉을 이 계약의 스위트가 같은 객체로 쓴다(`./disjointSetRollback.contract.ts` 머리말). 저쪽 이름표 선택은
 *    결재 대상으로 넘어가 있고(`docs/ORD-006-runbook.md` 불변 사실 310), **결재가 바뀌면 이 계약의 `find` 행도
 *    함께 바뀐다.**
 *
 * **되돌리는 단위는 합치기 호출이다.**
 *
 * - `rollback()` 은 아직 되돌리지 않은 `union` 호출 중 **마지막 것 하나**를 되돌리고 `true` 를 돌려준다. 되돌린
 *   뒤의 분할은 그 호출 **직전**의 분할이다.
 * - **이미 같은 집합이라 아무것도 안 바꾼 `union` 호출도 되돌릴 호출 하나다.** 그 호출을 되돌려도 분할은 그대로다.
 *   합친 일만 세면 호출자가 호출마다 합쳤는지를 알아야 되돌리기의 짝을 맞출 수 있고, 아무것도 안 바꾼 호출 뒤의
 *   되돌리기가 그보다 앞선 합치기를 무른다. 호출로 세면 **호출자가 센 호출 수만큼 되돌리면 그 시점의 분할로
 *   돌아간다.**
 * - **던진 `union` 호출은 세지 않는다** — 상태를 바꾸지 않았고 되돌릴 것이 없다.
 * - 되돌릴 호출이 없으면 `false` 이고 상태가 바뀌지 않는다.
 *
 * **스냅숏·되살리기(`snapshot`·`restore`)는 이 계약에 없다 — 물려받은 표면에서 뺐다.** 없어서 목적을 진술하지
 * 못하게 되는 연산이 아니다(불변 사실 47). 호출로 세는 되돌리기 위에서는 「스냅숏」이 호출자가 센 합치기 호출
 * 수이고 「되살리기」가 그 수만큼 `rollback` 을 부르는 일이며, 비용도 같다(되돌리는 호출 수에 비례). 더하면 판번호의
 * 수명이라는 둘째 개념을 계약이 정해야 하는데, **물려받은 정의가 그 자리에서 이미 틀려 있었다** — 판번호가 「그때의
 * 기록 길이」라, 판을 뜬 뒤 그 앞 합치기를 되돌리고 다른 합치기를 하면 기록 길이가 같아져 되살리기가 아무것도 안
 * 한다. `union(0, 1)` · 판 뜨기 · `rollback()` · `union(2, 3)` · 그 판 되살리기 뒤에 `0`·`1` 은 떨어져 있고 `2`·`3` 은
 * 붙어 있다 — 판을 뜬 때의 분할이 아니다. 판번호에 수명을 주면 고쳐지지만, 고친 것은 호출자의 수 세기와 같은 일을
 * 거절 규칙 하나와 함께 계약에 들이는 것이다.
 *
 * **집합의 수(`groupCount`)도 뺐다.** `unionFind` 가 같은 이유로 두지 않았다 — 목적은 합치기 · 되돌리기 · 같은 집합
 * 묻기이고 집합의 수는 그중 어느 것도 진술하지 못하게 만들지 않는다.
 *
 * **이웃 계약과의 관계.**
 *
 * - **`disjoint-set/unionFind` 와는 서로 담지 않는다**(불변 사실 54). 반례가 양쪽에 하나씩 있다.
 *   - **저쪽 구현은 이쪽을 못 지킨다 — 되돌리기가 없고, 곧게 붙여도 서지 않는다.** 저쪽 정본에 「되돌릴 때 남은 합치기를
 *     처음부터 다시 부르기」를 붙인 구현(`_contract/_fixtures/replayingPartition.ts`)이 저쪽 스위트 셋을 정본 값 그대로
 *     통과하고(1.50 · 5.00 · 6.06~6.10) 이쪽의 되돌리기가 든 두 시나리오에서 원소 수에 비례해 걸린다(854.33 · 3,414.33 ·
 *     13,654.33 / 1,786.01 · 7,162 · 28,666). 저쪽 스위트를 통과하는 상각 설계 둘 — 인자 순서로 걸고 지난 길을 줄이는
 *     숲 · 작은 쪽 번호표를 다시 적는 구현 — 도 되돌리기를 붙이면 같다(2.00 · 5.50 · 7.47~9.17 / 2.00 · 2.00 · 4.68~4.77
 *     통과, 되돌린 뒤 거듭하기 343 · 1,367 · 5,463 / 342.33 · 1,366.33 · 5,462.33).
 *   - **이쪽 구현은 저쪽을 못 지킨다 — 로그 인수만큼.** 이쪽 정본은 지난 길을 줄이지 않아, 짝지어 두 배씩 합친 뒤
 *     모든 원소를 찾으면 호출 평균이 6 · 7 · 8 로 원소 수의 로그를 그대로 찍는다. 저쪽 상한은 역아커만 함수다.
 *     **이 갈림은 축3이 보지 못한다** — 비율 1.17 · 1.14 가 `O(1)` 구간과 `O(log n)` 구간에 함께 들고, 이쪽 정본이
 *     저쪽 스위트 셋을 전부 통과한다(불변 사실 53·62). 저쪽 결함 fixture 인 높이로 거는 숲(`byRank`)이 이쪽에서는
 *     정본과 같은 계열이다 — 결함 fixture 는 계약에 대해서만 결함이다(불변 사실 252).
 * - **`docs/ORD-006-conventions.md` B19 의 「경로를 압축하는 구현은 앞을 만족하고 뒤를 만족하지 못한다 — 압축이 이전
 *   상태를 지운다」는 이 계약의 문장으로 옮기지 않는다.** 줄인 칸을 기록해 두면 지운 것이 없고, 크기로 걸면서 줄이고
 *   기록하는 구현이 이 계약의 다섯 시나리오를 전부 통과한다(`_contract/_fixtures/loggingForest.ts` 의 `bySize`).
 *   계약의 문장으로 참인 것은 「되돌리기가 줄이기의 상각 이득을 무를 수 있다」이고, **거는 규칙이 높이를 묶지 않을
 *   때만** 그것이 비용으로 드러난다 — 같은 fixture 에서 인자 순서로 걸면 위 343 · 1,367 · 5,463 이다.
 * - **`tree/linkCutTree` 와는 서로 담지 않는다.** 그쪽 구현에는 이름표 연산이 없다. 이쪽 구현에는 아무 간선이나 빼는
 *   연산이 없고, 이쪽 합치기는 이미 같은 집합인 둘도 받아 되돌릴 호출로 쌓는데 그쪽 `link` 는 그 호출을 거절한다.
 *
 * **이 계약에 없는 것.**
 * - **스냅숏·되살리기 · 집합의 수** — 위 두 문단.
 * - **아무 합치기나 골라 무르기 · 되돌린 합치기 다시 하기.** 되돌리기는 나중 것부터이고, 다시 하려면 같은 `union` 을
 *   다시 부른다.
 * - **집합의 크기 · 한 집합의 원소 열거 · 원소 더하기.** `unionFind` 와 같다.
 * - **공간.** 이 계약은 시간만 말한다(§규약1 「공간은 어느 계약에도 없다」 — 미결). 되돌릴 호출의 기록이 호출열
 *   길이만큼 쌓여도 계약을 어기지 않는다.
 *
 * **이름은 임시가 아니다.** 「분리 집합 + 되돌리기」는 계약이 약속하는 연산의 이름이고 구현 기법의 이름이 아니다
 * (불변 사실 66). 카테고리 `disjoint-set/` 도 계약 이름이다.
 *
 * **불변식.** 없다.
 *
 * 판별 절차(§규약1 「불변식 판별 절차」)를 돌린 결과다. `unionFind` 의 후보를 다시 돌리고 되돌리기 몫 둘을 더했다.
 *
 * - **「같은 집합인가 ↔ 이름표가 같은가」** — `connected` 행이 그 정합을 적어 두 번째 물음에서 걸린다(`unionFind` 와 같다).
 * - **「이름표는 그 집합의 원소이고 자기 자신의 이름표다」** — 읽는 경로가 `find` 하나라 첫 물음에서 걸린다.
 * - **「한 번 같은 집합이 된 둘은 계속 같은 집합이다」는 이 계약에서 거짓이다** — 되돌리기가 가른다. 그 자리에 선
 *   「되돌린 뒤의 분할은 그 합치기 호출 직전의 분할과 같다」는 상태의 성질이 아니라 시간에 걸친 성질이다(불변 사실 153).
 *   축1의 경계 케이스가 짚는다.
 * - **「되돌릴 호출의 수는 아직 되돌리지 않은 `union` 호출의 수다」** — 그 수를 읽는 경로가 `rollback` 의 `true`/`false`
 *   하나라 첫 물음에서 걸리고, 그 읽기가 상태를 바꾼다(§「관측이 상태를 바꾸면 관측 경로로 세지 않는다」).
 *
 * 담는 모양의 성질(나무의 높이 · 기록의 모양)은 후보가 아니다. 적는 순간 표현을 처방한다(불변 사실 36).
 *
 * **연산 계약.** n 은 **생성자가 정한 원소 수**다. 구조가 사는 동안 바뀌지 않는다(`unionFind` 와 같다).
 *
 * | 연산 | 의미 | 상한 | 한정자 |
 * |---|---|---|---|
 * | `constructor(n)` | 원소 `0` 부터 `n-1` 까지를 각각 홀로 선 집합으로 두고 되돌릴 호출이 없다. `n` 은 0 이상의 정수여야 하고, 아니면 `RangeError` | O(n) | worst |
 * | `find(x)` | `x` 가 속한 집합의 **가장 작은 원소** | O(log n) | amortized |
 * | `union(x, y)` | `x` 의 집합과 `y` 의 집합을 하나로 합치고, 이 호출을 되돌릴 호출로 쌓는다. 이미 같은 집합이면(`x === y` 포함) 분할은 그대로이고 **호출은 쌓인다** | O(log n) | amortized |
 * | `connected(x, y)` | `x`·`y` 가 같은 집합에 있으면 `true` — `find(x) === find(y)` 와 같다 | O(log n) | amortized |
 * | `rollback()` | 아직 되돌리지 않은 `union` 호출 중 마지막 것을 되돌려 분할을 그 호출 직전으로 두고 `true`. 되돌릴 호출이 없으면 `false` 이고 상태가 바뀌지 않는다 | O(1) | amortized |
 *
 * **`amortized` 는 되돌리기가 섞인 호출열 전체로 읽는다**(§「상각 행의 약속은 호출열 전체로 적는다」). 생성자로 연 뒤
 * 부른 `find`·`union`·`connected` 호출이 모두 `m` 개, `rollback` 호출이 `k` 개면 **생성자를 포함한 총비용이
 * $n + m \log n + k$ 의 상수배 안**이다.
 *
 * **되돌리기가 상각을 깨뜨리는가 — 한정자는 깨지지 않고, 상각에 기댄 설계가 깨진다.** 되돌리기가 섞인 호출열도
 * 호출열이라 `amortized` 의 약속은 그대로 선다. 깨지는 것은 「비싼 호출이 뒤 호출을 싸게 만든다」에 기댄 논증이다 —
 * 되돌리기가 그 싸게 만든 것을 무르고 **같은 비싼 호출을 다시 부르게** 할 수 있다. 인자 순서로 걸고 지난 길을 줄이는
 * 숲(줄인 것이 되돌려져 같은 사슬을 거듭 훑는다)과 작은 쪽을 다시 적는 구현(되돌리기가 「다시 적힌 원소의 집합은 두 배가
 * 된다」를 무른다)이 그렇게 걸린다(위 수치). 스위트의 「되돌린 뒤 같은 합치기 거듭하기」가 이 판정의 시나리오이고,
 * `unionFind` 스위트와 같은 입력인 앞 시나리오 셋은 두 설계를 전부 통과시킨다.
 *
 * **상한 `O(log n)` 은 정본이 서는 계급으로 골랐다**(불변 사실 51).
 *
 * - **더 약하게 원소 수에 비례하게 적으면** 인자 순서로 거는 숲과 한쪽 번호표를 다시 적는 구현이 들어온다 — 둘 다
 *   자명한 구현이고(아래 「검증 등급」) 계약이 뜻을 잃는다.
 * - **더 강하게 `unionFind` 의 역아커만 함수로 적으면** 정본이 나간다. 되돌리기와 함께 그 값에 서는 구현은 이 유닛이
 *   찾지 못했고, 되돌리기가 있는 분리 집합의 하한을 다룬 문헌이 있는지는 확인 안 함이다. 그리고 **그 갈림은 로그
 *   인수라 축3이 보지 못한다** — 같은 이유로 `unionFind` 의 표는 역아커만 함수를 적고 `O(1)` 로 판정한다(B19 확정).
 *   두 계약의 비용 열이 갈리는 자리는 판정 규격 아래에 있고, 막는 것은 축이 아니라 가이드다(불변 사실 45).
 *
 * **`rollback` 의 `O(1)` 도 정본의 계급이다.** 원소 수에 비례하게 적으면 되돌릴 때 다시 합치는 구현이 들어와 되돌리기를
 * 계약에 둔 뜻이 사라진다. 그 사이의 `O(log n)` 은 축3이 `O(1)` 과 가르지 못한다(불변 사실 53) — 두 값이 같은 판정을
 * 받으므로 축3이 재는 행의 상한을 정본의 계급에 맞춘다(불변 사실 49).
 *
 * **축3이 이 표를 판정하는 방식.** 시나리오 다섯 중 되돌리기만 재는 하나가 `O(1)` 이고, 되돌리기를 합치기·묻기와 한
 * 호출열에 섞어 재는 하나는 셋 중 느슨한 `O(log n)` 으로 판정한다(§「표의 상한과 시나리오의 bound 가 다르면 헤더가 그
 * 판정을 적는다」). 판정 규격상 로그 인수만큼 어기는 구현(되돌리기 한 번에 로그만큼 되쓰는 구현 · 찾기에 로그의 제곱을
 * 쓰는 구현)은 통과한다 — 짓지는 않았다. **그 통과는 계약을 지킨다는 뜻이 아니다**(불변 사실 62).
 *
 * **네 행이 `amortized` 인 것.**
 *
 * - **`rollback`** — `worst` 로 적으면 줄인 칸을 기록해 되돌리는 계열이 나간다. 크기로 걸고 줄이며 기록하는 구현에서
 *   짝지어 두 배씩 합친 뒤 모든 원소를 찾고 되돌리면, 그 되돌리기 한 호출이 1,017 · 4,087 · 16,373 걸음인데 같은
 *   호출열의 호출 평균은 3.98 · 3.99 · 4.00 이다. 그 계열은 계약을 지키고 배제할 이유가 목적에 없다.
 * - **`find`·`union`·`connected`** — 정본은 호출마다 로그 안이다(같은 입력의 단일 호출 최대 11 · 13 · 15). `worst` 로
 *   적어 추가로 나가는 계열을 이 유닛이 짓지 못했다. 그러나 **배제해도 잃는 것이 없다는 것도 보이지 못했으므로**
 *   (불변 사실 59 — 강한 쪽을 적을 조건) 약한 쪽을 적는다. 되돌리기가 있어도 상각이 뜻을 갖는다는 것은 위
 *   `rollback` 행이 보인다.
 * - `expected` 는 무작위를 실제로 뽑는 구현을 요구하게 되므로 적지 않는다. `constructor` 만 `worst` 인 이유와 축3이
 *   그 행을 재지 않는 것은 `unionFind` 와 같다(§규약2 시나리오 규칙 2 — 면제).
 *
 * **물려받은 표면을 여섯 고쳤다.** 물려받은 상한 주장(찾기·합치기 `O(log n)` · 되돌리기 `O(1)` · 스냅숏 `O(1)`)은 판정의
 * 입력으로 쓰지 않았다(불변 사실 65).
 *
 * - **`same` → `connected`.** `unionFind` 와 같은 뜻의 연산을 같은 이름으로 둔다.
 * - **`union` 의 `boolean` 반환을 뺐다**(`unionFind` 와 같다). 되돌리기가 호출 단위라 합쳤는지를 몰라도 짝이 맞고,
 *   「이미 같은 집합이면 분할이 그대로」는 앞뒤의 `connected` 가 관측한다(불변 사실 82 — 거절하는 약속이 없다).
 * - **`rollback` 이 `boolean` 을 돌려준다.** 되돌릴 호출이 없는 자리를 전면화했다 — `void` 에는 「되돌릴 것이 없다」를
 *   담을 자리가 없다(불변 사실 82·132).
 * - **`find` 가 뿌리가 아니라 가장 작은 원소를 돌려준다**(위 「목적」).
 * - **`snapshot`·`restore`·`groupCount` 를 뺐다**(위 「목적」).
 * - **내부 필드 다섯과 「경로 압축을 쓰지 않는다 · 랭크로 건다」는 처방, 스토리 · 원소 수 제약 · 벽시계 성능 시험을
 *   뺐다**(불변 사실 4·7·20·38). `n = 0` 을 받는다.
 *
 * **주입 정책.**
 * - 주입받는 것이 없다. `unionFind` 와 같다.
 * - `n` 은 **0 이상의 정수**여야 하고, 아니면 `RangeError` 를 던진다.
 * - 원소 번호는 **`0` 이상 `n` 미만의 정수**여야 하고, 아니면 `find`·`union`·`connected` 가 `RangeError` 를 던지고 상태가
 *   바뀌지 않는다 — 던진 `union` 은 되돌릴 호출로 쌓이지 않는다. 값으로 답하지 않는 이유는 `unionFind` 와 같다(불변 사실
 *   132).
 * - `rollback` 은 인자가 없고 던지지 않는다. 되돌릴 것이 없다는 답이 반환 타입 안에 있다(`false`).
 *
 * **검증 등급.** `complexity`.
 * 판정 절차의 2번에서 걸린다 — 네 행을 **함께** 만족하는 자명한 구현이 없다. 배열로 분할을 적고 바꾼 칸을 쌓아 되돌리는
 * 길이 둘이고 둘 다 원소 수를 사다리 1,024 · 4,096 · 16,384 에 올린 계약 스위트에서 걸린다.
 *
 * - **원소마다 부모를 적고 인자 순서대로 걸기**(`_contract/_fixtures/loggingForest.ts` 의 `underSecond`·`underFirst` ·
 *   줄이기 끔) — 합치기 128.75 · 512.75 · 2,048.75, 찾기·묻기 396 · 1,547~1,565 · 6,167 안팎, 되돌린 뒤 거듭하기 173 ·
 *   685 · 2,733. 되돌리기만 재는 시나리오는 3.99 · 4.00 · 4.00 으로 통과한다.
 * - **원소마다 번호표를 적고 한쪽을 전부 다시 적기**(`_contract/_fixtures/relabelingUndoPartition.ts` 의 `first`) — 합치기와
 *   되돌리기가 둘 다 129.25 · 513.25 · 2,049.25 로 걸리고 묻기는 2.00 으로 통과한다.
 *
 * 줄이기로 사슬을 펴거나 작은 쪽만 다시 적으면 되돌리기가 없는 시나리오 셋은 통과하지만, 그 둘은 이미 자명하지 않은
 * 설계이고 되돌린 뒤 거듭하기에서 걸린다(위 「이웃 계약과의 관계」). 네 행을 함께 로그 안에 두려면 **나무의 높이를 거는
 * 규칙으로 묶어 찾기가 호출마다 싸고, 합치기 하나가 바꾸는 칸이 상수라 되돌리기가 그것만 되쓰는** 설계가 들어와야 한다.
 *
 * **필요충분조건.**
 * - 의미: 번호 매긴 원소의 분할 · 합치기 · 같은 집합 묻기 · 분할만으로 정해지는 이름표 · **합치기 호출을 나중 것부터
 *   되돌리기**. 되돌리기를 버리면 `disjoint-set/unionFind` 이고, 아무 간선이나 빼되 숲을 지키면 `tree/linkCutTree` 쪽이며,
 *   되돌리기가 합친 일만 세면 호출자가 호출마다 합쳤는지를 알아야 되돌리기의 짝을 맞춘다.
 * - 비용: 되돌리기가 섞인 **호출열 총비용**이 호출 하나당 원소 수의 로그에 묶여야 한다. 되돌리기가 원소 수에 비례하면
 *   되돌릴 때 다시 합치는 구현이고, 되돌린 뒤 같은 호출이 거듭 원소 수에 비례하면 되돌리지 않는 분리 집합의 상각 설계를
 *   그대로 옮긴 것이다. 로그 아래로 내리면 `unionFind` 가 같은 세 연산에 쓰는 값이 되는데, **그 마지막 갈림은 로그
 *   인수라 축3이 보지 못한다.**
 */
export class DisjointSetRollback {
  constructor(n: number) {
    throw new Error("Not implemented");
  }

  find(x: number): number {
    throw new Error("Not implemented");
  }

  union(x: number, y: number): void {
    throw new Error("Not implemented");
  }

  connected(x: number, y: number): boolean {
    throw new Error("Not implemented");
  }

  rollback(): boolean {
    throw new Error("Not implemented");
  }
}
