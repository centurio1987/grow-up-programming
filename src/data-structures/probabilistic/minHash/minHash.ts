/**
 * MinHash — 원소를 따로 넣은 두 인스턴스가 받은 두 집합이 얼마나 닮았는지(자카드 닮음) 묻는 추정기. 추정은 정해 둔 확률로
 * 참 닮음의 정해 둔 거리 안에 든다.
 *
 * **목적.** 문자열 원소를 인스턴스마다 거듭 넣고, **두 인스턴스에 들어온 두 집합의 자카드 닮음** J(A, B) = |A ∩ B| / |A ∪ B|
 * 를 묻는 일을 **넣은 원소의 수와 무관한 비용**에 제공하되, 답이 틀리는 것을 허용하는 것. 추정이 J 에서 **ε 넘게** 벗어날
 * 확률이 δ 이하다 — **닮음 오차 ε** 와 **실패 확률 δ** 는 생성 때 받는다. 두 집합이 같으면(둘 다 비었을 때 포함) J = 1 로
 * 둔다. 같은 원소를 몇 번 넣든, 어느 순서로 넣든, 어느 쪽에서 묻든 **답이 두 집합만으로 정해지고 두 집합이 같으면 1** 이라는
 * 것은 확률 없이 선다.
 *
 * **표면을 고쳤다 — 물려받은 표면으로는 상태가 쌓이지 않았다.** 물려받은 `update(set)` 은 집합 하나를 받아 서명을 **덮어썼다**.
 * 호출 사이에 남는 것이 마지막 집합의 서명뿐이라 집합 하나를 받아 서명을 내는 함수와 같았다(`docs/ORD-006-conventions.md`
 * 「A군 17종 판정」 ⑥ 끝). 원소 하나씩 넣는 `add` 로 바꿔 넣은 원소가 쌓이게 했다. 집합을 한꺼번에 넣는 연산은 `add` 를 거듭
 * 부르는 것과 같은 계급이라 배제하는 구현이 없어 따로 두지 않았다(불변 사실 210). 물려받은 정적 `jaccard(a, b)` 는 인스턴스의
 * `similarity(other)` 로 옮겼다 — 이름은 기법이 아니라 묻는 양을 따르고, 두 쪽의 매개변수를 한 인스턴스가 견준다.
 *
 * **두 매개변수가 계약이고, 물려받은 매개변수와 `signature()` 는 표현이었다.** 물려받은 생성자는 `numHashes` — 해시 함수의
 * 수 — 를 받았고 `signature()` 가 그 수만큼의 가장 작은 해시 값을 돌려줬다. 둘 다 한 기법의 눈금과 그 기법의 요약이라 오차를
 * 그것으로 적으면 그 표현을 처방한다(「A군 17종 판정」 ④). 물려받은 정적 `exact(a, b)` 는 두 언어 집합의 정확한 닮음을 내는
 * 함수다 — 상태가 없고 누구든 두 집합 크기에 비례해 답하므로 배제하는 것이 없고, 스위트에서는 참조 모델의 몫이다. 셋 다
 * 표면에서 뺐다. 물려받은 문서의 「표준 편차 ≈ 1/√numHashes」 · 보편 해시 식은 그 표현에서만 참이라 옮기지 않았다(불변 사실 38).
 *
 * **닮음 묻기(`similarity`)가 구현을 배제한다.** 넣기만이면 들어온 원소를 빠짐없이 담는 정확한 집합이 이 계약을 지킨다. 닮음의
 * 비용을 **들어온 원소 수와 무관하게** 적으면 그 집합이 나간다 — 교집합을 세려면 원소를 훑어야 하기 때문이다(실행으로 확인 —
 * `src/data-structures/_contract/_fixtures/exactSetSimilarity.ts`). 목적의 「넣은 원소의 수와 무관한 비용」이 그 배제를 요구한다.
 * `probabilistic/hyperLogLog` 의 합치기와 같은 자리이고, **저장량을 적은 것은 아니다**(§규약1 「공간은 어느 계약에도 없다」).
 *
 * 이 계약에 없는 것은 넷이다.
 *
 * - **공간.** 위 문단 끝과 같다.
 * - **지우기 · 합치기 · 원소 목록 · 서명 읽기.** 물려받은 표면에 없거나(앞 셋) 표현이고(서명), 없으면 목적을 진술하지 못하는
 *   연산이 아니다(불변 사실 47).
 * - **다른 실행에서 세운 인스턴스와 견주기.** 표면에 인스턴스를 바이트로 옮기는 연산이 없다. 결정적인 쪽이 「같은 실행」에서만
 *   서는 까닭이 이것이다(아래 「오차 보장」).
 * - **매개변수가 다른 인스턴스와 견주기.** 답이 어느 (ε, δ) 의 보장을 받는지 정할 수 없어 `RangeError` 로 거절한다.
 *
 * **이름은 임시다.** 「민해시」는 해시 함수마다 가장 작은 값을 모아 견주는 **기법의 이름**이다. 계약이 말하는 것은 「근사 자카드
 * 닮음」이고 계약 이름은 KAN-031 이 정한다(불변 사실 198).
 *
 * **불변식.** 없다.
 *
 * 판별 절차(§규약1 「불변식 판별 절차」)를 돌린 결과다. 상태를 읽는 공개 연산이 `similarity` 하나다. 「자기 자신과의 닮음은 1」 ·
 * 「부르는 방향을 바꿔도 같다」는 둘 다 `similarity` 행의 의미이고 축1이 본다. 서명 칸의 성질은 표현이라 후보가 아니다(불변 사실 36).
 *
 * **연산 계약.** L 은 인자 문자열의 길이(UTF-16 코드 단위 수), ε 는 닮음 오차, δ 는 실패 확률이다. 인스턴스에 **들어온 원소**는
 * 그 인스턴스에 `add` 로 넣은 원소의 집합이다.
 *
 * | 연산 | 의미 | 상한 | 한정자 |
 * |---|---|---|---|
 * | `constructor(epsilon, delta)` | 닮음 오차 ε · 실패 확률 δ 인, 들어온 원소가 없는 추정기를 연다 | O(1/(ε² · δ)) | worst |
 * | `add(item)` | `item` 이 들어온 원소가 된다. 반환이 없다. 이미 들어온 원소면 이 뒤의 모든 관측이 부르지 않았을 때와 같다 | O(L/(ε² · δ)) | expected |
 * | `similarity(other)` | `this` 와 `other` 에 들어온 두 집합의 자카드 닮음 추정을 0 이상 1 이하로 돌려준다. **같은 실행 안에서 같은 (ε, δ) 로 세운 두 인스턴스에서, 값은 두 쪽에 들어온 집합의 짝만으로 정해지고(부르는 방향과 무관하다) 두 집합이 같으면 1 이다.** 추정이 벗어날 확률을 아래 「오차 보장」이 누른다. 상태를 바꾸지 않는다. `other` 는 이 구현으로 같은 (ε, δ) 로 세운 인스턴스여야 하고 매개변수가 다르면 `RangeError` 다. `other` 가 `this` 여도 된다 | O(1/(ε² · δ)) | expected |
 *
 * **결정적인 쪽과 확률적인 쪽.** `similarity` 행의 굵은 문장이 결정적인 쪽 전부다. 따라 나오는 것 셋을 이름으로 적는다 — 같은
 * 원소를 다시 넣어도 닮음이 안 바뀐다(`add` 행) · 넣은 순서와 무관하다 · 자기 자신과의 닮음은 1 이다. 두 집합이 같지 않으면
 * 어떤 값도 결정적으로 약속하지 않는다 — 교집합이 없는 두 집합의 닮음이 0 이라는 것조차 확률 문장으로만 누른다(서로 다른 두
 * 원소를 해시가 겹치게 보낼 수 있다).
 *
 * **오차 보장.** 확률 문장 하나와 그것을 판정하는 문장 하나다.
 *
 * - **확률 문장.** 같은 실행에서 같은 (ε, δ) 로 세운 어느 두 인스턴스에서든, `similarity` 가 두 집합의 자카드 닮음에서 ε 넘게
 *   벗어날 확률이 δ 이하다.
 * - **확률의 출처는 구현이 뽑는 무작위다 — 입력의 분포가 아니다. 그 무작위를 같은 실행의 인스턴스가 함께 쓴다.** 결정적인 쪽이
 *   따로 세운 두 인스턴스에 같은 집합이 들어가면 1 을 요구하므로, 원소를 어떤 값으로 보내는지를 인스턴스마다 따로 뽑을 수 없다
 *   (실행으로 확인 — `src/data-structures/_contract/_fixtures/perInstanceSeedMinHash.ts`). 그래서 확률 문장은 **실행이 무작위를
 *   뽑기 전에** 정해진 두 집합에 대해 선다. 같은 실행의 다른 인스턴스가 낸 답을 보고 원소를 고르는 호출자는 덮지 않는다. 입력
 *   분포로 읽지 않는 근거는 `probabilistic/bloomFilter` 헤더의 셋과 같고, **해시를 고정한 구현은 이 계약을 어긴다** — 그 해시에
 *   대해 서명이 몰리는 두 집합이 실재하고 그 짝을 미리 고른 호출자에게 확률이 1 이다. 스위트는 구현을 읽지 않고는 그 짝을 짓지
 *   못하므로(불변 사실 44) 그 위반은 스위트를 통과한다(블룸 필터에서 실행으로 보인 모양 — 불변 사실 298, 이 계약에서는 다시 돌리지
 *   않았다). 무작위를 실행이 뽑는 것은 `probabilistic/hyperLogLog` 와 같은 자리이고 사람 결정 대기다(불변 사실 366).
 * - **판정 문장과 여유.** 스위트는 확률 문장을 축1 안에서 이렇게 판정한다(2026-09-15 유저 결정 — 축을 늘리지 않는다). 새 인스턴스
 *   짝을 ⌈16/δ⌉ 개 세우고, 저마다 고정 seed 로 정한 두 집합(합집합 크기를 1 부터 max(256, ⌈4/(ε² · δ)⌉) 까지 로그 고르게, 교집합
 *   크기를 0 부터 합집합 크기까지 고르게, 쪽마다 둘에 한 번 방금 넣은 원소를 다시 넣는다)을 넣어 **닮음이 참 닮음에서 ε 넘게 벗어난
 *   짝의 수가 3 · 16 = 48 이하인가**를 관측값으로 돌려준다. **여유는 3 이다.** 짝마다 따로 확률 δ 로 벗어나는 구현 — 확률 문장을
 *   경계에서 지키는 구현 — 이 한 판정에서 떨어질 확률이 체르노프 부등식으로 e^{−16} (약 1 × 10^−7) 이하다. 모으는 단위가 원소가
 *   아니라 인스턴스 짝이라 `probabilistic/hyperLogLog` 의 수를 그대로 쓴다(실측은 `./minHash.contract.ts` 머리말).
 * - **판정 문장은 확률 문장과 같지 않다 — 틈 하나를 적어 둔다.** 확률 문장은 짝 하나씩에 대한 것이라, **실행마다 동전을 한 번
 *   던져 확률 δ 로 모든 짝을 벗어나게 하는 구현**도 지킨다. 그 구현은 판정에서 확률 δ 로 떨어진다. 블룸 필터 헤더가 적은 틈과
 *   같은 모양이고, 무작위를 실행이 함께 쓰는 이 계약에서는 그 몰림이 **구현의 기본 모양**에 더 가깝다 — 같은 실행의 인스턴스가 같은
 *   해시를 쓰기 때문이다.
 *
 * **`add` · `similarity` 가 `expected` 인 이유.** 「기대」의 뜻은 `hash/hashSet` 헤더가 정의한 것과 같다 — 구현이 뽑는 무작위에
 * 대한, 호출 **시퀀스** 총비용의 기댓값이다. 이 계약이 배제하려는 것은 들어온 원소 수에 비례하는 구현이고, 그것을 배제하는 가장
 * 약한 한정자가 이 뜻의 `expected` 다(불변 사실 21 · 59). 이름의 기법은 호출마다 같은 비용이라 `worst` 로도 지키지만, `worst` 를
 * 적으면 원소가 요약 크기보다 적은 동안 원소를 따로 들었다가 몰아 요약으로 옮기는 구현이 나간다. 그 배제를 목적이 요구하지
 * 않는다 — 논증이고, 그 구현을 짓지는 않았다.
 *
 * **상한은 고른 값이다(불변 사실 48 · 51).** 둘 다 들어온 원소 수를 적지 않는 것이 목적에서 온 부분이다. ε · δ 의 함수로서는
 * **닮음 추정의 흩어짐만으로 δ 를 맞추는 구현** — 체비쇼프 부등식으로 요약 하나의 크기를 1/(ε² · δ) 에 비례하게 잡는 것,
 * `probabilistic/hyperLogLog` 가 같은 이유로 고른 계급 — 이 서는 계급으로 정했다.
 *
 * - **`similarity` 의 `O(1/(ε² · δ))`.** 더 강하게 `O(log(1/δ)/ε²)` 로 적으면 그 구현이 나간다. 해시 함수마다 같을 사건이 서로
 *   독립이면 호에프딩 부등식으로 그 크기에 닿는 것이 이름의 기법의 곧은 길이지만(정본이 그렇게 고른다), 체비쇼프 크기를 배제할
 *   근거가 목적에 없다. `O(1)` 로 적으면 요약 전체를 견주는 이름의 기법 그대로가 나간다.
 * - **`add` 의 `O(L/(ε² · δ))`.** 이름의 기법은 넣을 때마다 서명 칸 전부를 고치므로 요약 크기가 넣기에 곱해진다 — 넣기가 자리
 *   하나를 고치는 `probabilistic/hyperLogLog` 와 갈리는 자리다. 더 강하게 `O(L + 1/(ε² · δ))` 로 적으면 해시 함수마다 문자열을 다시
 *   읽는 구현이 나가고, `O(L · log(1/δ)/ε²)` 로 적으면 위의 체비쇼프 크기가 나간다 — 둘 다 배제할 근거가 목적에 없다. 원소 하나에
 *   요약 칸 하나만 고치는 변형(순열 하나를 칸으로 나누는 기법)은 더 빠르지만, 그만큼 강하게 적으면 이름의 기법이 나간다.
 *
 * **축3은 L · ε · δ 몫을 재지 않는다** — 사다리가 들어온 원소 수만 키우고 셋을 상수로 누른다(§규약2 시나리오 규칙 3). 생성자는 n 에
 * 대해 반복 호출되지 않아 시나리오 면제다(시나리오 규칙 2).
 *
 * **주입 정책.**
 * - **주입받는 것이 없다.** 원소 타입은 문자열로 고정하고, 원소의 동등성은 문자열 값이 같은가(`===`)다. 원소를 정수로 펴는 함수도
 *   해시 함수도 씨앗도 받지 않는다 — 까닭은 `probabilistic/bloomFilter` 헤더와 같다. 묻는 양이 집합의 닮음이라서 달라지는 것은
 *   없다 — 집합은 인스턴스가 들고, 주입자가 매달릴 자리는 여전히 원소 하나다.
 * - `epsilon` · `delta` 는 둘 다 0 보다 크고 1 보다 작은 수여야 하고 아니면(`NaN` 포함) 생성자가 `RangeError` 를 던진다. ε ≥ 1 은
 *   무엇이든 돌려주는 구현을 정당화해 뜻이 없고(닮음은 0 ~ 1), ε = 0 은 정확한 닮음을 요구한다. δ ≥ 1 은 확률 문장을 비우고,
 *   δ = 0 은 결정적 한계를 요구해 상한의 1/δ 가 서지 않는다.
 * - `similarity(other)` 의 `other` 는 이 구현의 인스턴스다. 매개변수(`===` 로 견준 ε · δ)가 다르면 `RangeError` 를 던지고 두 쪽이
 *   그대로다.
 * - `add` 는 던지지 않는다.
 *
 * **검증 등급.** `basic`.
 * 상한이 자명한 구현으로 달성된다 — 해시 함수 1/(ε² · δ) 개에 원소마다 그 함수를 한 번씩 돌려 칸마다 가장 작은 값을 남기고, 닮음은
 * 칸을 한 번씩 견주는 구현은 **그 시간**에 확률 논증이 들지 않는다. 오차 보장에는 확률 논증이 들지만 등급을 올리지 않는다 — 판정
 * 절차 2번은 시간 상한만 읽는다(불변 사실 203). 등급이 정하는 것은 축3 엄격도이고 오차는 축1이 판정한다. 불변식 절이 비었다.
 *
 * **필요충분조건.**
 * - 의미: 닮음이 참 닮음에서 ε 넘게 벗어날 확률이 **δ 이하**이고, 같은 실행 · 같은 매개변수에서 **닮음이 두 집합의 짝만으로 정해지며
 *   두 집합이 같으면 1** 이어야 한다. 앞을 버리면 **늘 1 을 돌려주는 구현**이 계약을 지킨다 — 상수는 어느 짝의 함수이기도 하고 같은
 *   집합에서 요구하는 값이 바로 그 상수라 뒤를 공짜로 지킨다. 뒤를 버리면 부를 때마다 오차 안에서 값을 새로 고르는 구현과, 인스턴스마다
 *   해시를 따로 뽑아 같은 집합을 따로 넣은 두 인스턴스가 1 을 못 내는 구현이 들어온다. 그러면 「같은 두 집합에는 같은 답」 · 「같은
 *   집합이면 같다고 답한다」가 호출자에게 관측되지 않는다.
 * - 비용: `similarity` 가 들어온 원소 수에 비례하면 원소를 전부 담아 교집합을 세는 집합이고, `add` 가 비례하면 들어온 원소를 늘어놓고
 *   겹침을 훑는 목록이다 — 어느 쪽이든 본 원소 수와 무관하게 견준다는 값이 남지 않는다.
 */
export class MinHash {
  constructor(epsilon: number, delta: number) {
    throw new Error("Not implemented");
  }

  add(item: string): void {
    throw new Error("Not implemented");
  }

  similarity(other: MinHash): number {
    throw new Error("Not implemented");
  }
}
