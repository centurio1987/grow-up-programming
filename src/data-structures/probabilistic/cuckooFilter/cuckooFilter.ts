/**
 * CuckooFilter — 넣고 지울 수 있는 근사 소속 판정. 넣은 사본이 남은 원소에는 참으로 답하고, 사본이 없는 원소에는 정해 둔 몫 이하로만 참으로 잘못 답하며, 용량이 차기 전에는 새 원소를 거절하지 않는다.
 *
 * **목적.** 문자열의 **사본**을 넣고 빼며 「사본이 남아 있는가」를 묻는 일을 담긴 수와 무관한 비용에 제공하되, 답이
 * 한쪽으로만 틀리는 것을 허용하는 것. `probabilistic/bloomFilter` 와 같은 두 매개변수 — **용량 n** 과 **목표 거짓 양성률
 * ε** — 를 받고, 그 계약에 **지우기**와 **가득 참**을 더한 자리에 선다. 두 매개변수가 계약이고 물려받은 `(capacity,
 * fingerprintSize)` 의 지문 폭이 표현이라는 판정은 그쪽 헤더와 같다(`docs/ORD-006-conventions.md` 「A군 17종 판정」 ④).
 *
 * **사본 수는 반환값으로 정의한다.** 원소 x 의 사본 수 = `add(x)` 가 참을 돌려준 수 − `delete(x)` 가 참을 돌려준 수. **담긴
 * 사본** = 모든 원소의 사본 수의 합. 둘 다 호출자가 자기 호출의 반환값으로 센다 — 구조가 무엇을 담는지가 아니라 호출자가
 * 관측한 것으로 적는다. 같은 원소를 두 번 받아들이면 사본이 둘이고, 지우기 한 번 뒤에도 남는다(다중집합).
 *
 * 이 계약에 없는 것은 넷이다.
 *
 * - **공간.** 넣은 사본을 빠짐없이 담는 정확한 다중집합도 이 계약을 지킨다(§규약1 「공간은 어느 계약에도 없다」).
 * - **담긴 수 · 채워진 몫.** 물려받은 `size()` 는 뺐다 — 위 정의대로 호출자가 반환값으로 세는 조합이고(원칙 A 의 A2) 떨어지는 계열이 없는 편의 연산이며(A4)
 *   반례가 기대지 않는다(A5 — `docs/ORD-006-conventions.md` 「원칙 A — 연산 선정」 판정표 ① 의 27 번째 행). 물려받은 `loadFactor()` 는 칸 수라는 표현을 읽는 값이라 뺐다(A1). 가득 참은 `add` 의 거짓이 관측한다.
 * - **사본 수 묻기 · 필터끼리 합치기.** 물려받은 표면에 없고 없으면 목적을 진술하지 못하는 연산이 아니다(불변 사실 47).
 * - **넣지 않은 원소를 지운 뒤의 결정적 보장.** 아래 「오차 보장」의 범위 문장이다.
 *
 * **`probabilistic/bloomFilter` 와 서로 담지 않는다(불변 사실 54).** 반례가 양쪽에 하나씩이고 셋 다 실행했다
 * (`src/data-structures/_contract/runContract.cuckooFilter.test.ts`). ① 칸을 함께 쓰는 비트 배열 구현은 블룸 계약을 지키고 이
 * 계약을 못 지킨다 — 지우는 척 참만 돌려주면 다 지운 필터가 지운 원소에 참으로 답한다. ② 용량이 차면 넣기를 거절하는 이
 * 계약의 정본은 블룸 계약의 「용량을 넘겨 넣어도 넣은 원소는 참」을 어긴다. ③ 칸마다 수를 세는 블룸 필터는 두 스위트를 다
 * 통과한다.
 *
 * **이름은 임시다.** 「뻐꾸기」는 지문을 두 자리 중 하나에 두고 밀어내는 **기법의 이름**이다. 계약이 말하는 것은 「지울 수
 * 있는 근사 소속 다중집합」이고 계약 이름은 KAN-031 이 정한다(불변 사실 198).
 *
 * **불변식.** 없다.
 *
 * 판별 절차(§규약1 「불변식 판별 절차」)를 돌린 결과다. 사본이 남았는지를 상태를 바꾸지 않고 읽는 연산이 `has` 하나라 첫
 * 물음에서 걸린다. `delete` 의 반환값도 사본의 유무를 알려 주지만 상태를 바꾸므로 관측 경로로 세지 않는다
 * (`docs/ORD-006-conventions.md` 「관측이 상태를 바꾸면 관측 경로로 세지 않는다」). 담긴 사본 수는 표면에 없다.
 *
 * **연산 계약.** n 은 생성자가 받은 **용량**, L 은 인자 문자열의 길이(UTF-16 코드 단위 수), ε 은 목표 거짓 양성률이다.
 *
 * | 연산 | 의미 | 상한 | 한정자 |
 * |---|---|---|---|
 * | `constructor(capacity, falsePositiveRate)` | 용량 n · 목표 ε 인 빈 필터를 연다 | O(n · log(1/ε)) | worst |
 * | `add(item)` | 참이면 `item` 의 사본 하나가 늘고, 거짓이면 상태가 그대로다. **담긴 사본이 n 개 미만이고 이 호출 직전의 `has(item)` 이 거짓이면 반드시 참이다.** 그 밖 — 담긴 사본이 n 개 이상이거나, `has(item)` 이 이미 참(사본이 있거나 헷갈렸다) — 에는 참이든 거짓이든 된다 | O(L · log(1/ε)) | expected |
 * | `has(item)` | 사본이 남은 원소면 **반드시 참**. 사본이 없는 원소면 참이거나 거짓이고, 참일 확률을 아래 「오차 보장」이 누른다. 상태를 바꾸지 않는다 | O(L · log(1/ε)) | expected |
 * | `delete(item)` | 사본이 남은 원소면 **그 원소의 사본 하나를 빼고 참**. 사본이 없는 원소면 거짓이고 상태가 그대로이거나, 참이고 **어느 원소의 것인지 정하지 않은 사본 하나**가 빠진다(헷갈린 지우기). 참일 확률을 아래 「오차 보장」이 누른다 | O(L · log(1/ε)) | expected |
 *
 * **오차 보장.** `probabilistic/bloomFilter` 헤더의 「오차 보장」을 그대로 따르고, 이 계약에서 달라지는 것만 적는다.
 *
 * - **확률 문장 둘.** 담긴 사본이 n 개 이하인 동안, 사본이 없는 원소 x 에 대해 `has(x)` 가 참일 확률과 `delete(x)` 가 참일
 *   확률이 각각 ε 이하다. **확률의 출처는 구현이 뽑는 무작위다** — 원소 묶음이 필터가 무작위를 뽑기 전에 정해져 있으면
 *   무엇이든 된다. 출처를 고른 근거 셋과 따라 나오는 것(고정 해시는 계약을 어기는데 스위트를 통과한다)은 블룸 필터 헤더다.
 * - **결정적 문장의 범위 — 전제조건을 전면화한 모양이다(불변 사실 82).** 물려받은 문서는 「넣지 않은 원소를 지우면 다른
 *   원소의 거짓 음성이 생긴다」를 호출자가 피할 전제조건으로 두었다(불변 사실 207). 전제조건이면 축1이 그 자리를 못 재므로
 *   `delete` 를 **모든 입력에 정의했다** — 사본이 없는 원소도 답이 있다(위 표). 대신 그 답이 참이면 빠진 사본이 누구의
 *   것인지 계약이 정하지 않으므로, **「사본이 남은 원소는 `has` 참 · `delete` 참」은 헷갈린 지우기가 참을 돌려준 적이 없는
 *   동안 선다.** 호출자는 그 일을 관측한다 — 자기가 사본을 넣지 않은 원소의 `delete` 가 참이었는지를 반환값으로 안다.
 *   「사본이 없는 원소의 `delete` 는 늘 거짓」으로 적지 않은 것은 그것이 소속을 정확히 알 것을 요구해 **필터를 전부
 *   배제하기** 때문이다. `add` 의 결정적 문장(용량 미만 · `has` 거짓이면 참)과 확률 문장 둘은 헷갈린 지우기 뒤에도 선다.
 * - **판정 문장과 여유는 블룸 필터와 같다** — 고정 seed 로 채우고 사본이 없는 원소 N 개(N · ε = 64)를 물어 참이 2 · ε · N
 *   이하, 여유 2. 이 계약의 스위트는 그것을 여섯 판정으로 나눠 본다(`./cuckooFilter.contract.ts` 머리말). **답을 한데 몰아
 *   내는 구현을 판정이 확률 ε 로 떨어뜨리는 틈도 같다.**
 *
 * **`add` 의 거절을 결정적 문장으로 적었다 — S1 이 넘긴 「`worst` 인가 `expected` 인가」는 한정자가 아니라 거절의 뜻에서
 * 닫혔다.** 물려받은 문서는 「밀어내기가 최대 횟수를 넘으면 가득 찬 것으로 보고 거짓」이었다. 그 거절을 계약이 어떻게
 * 허용하느냐가 갈림이다.
 *
 * - **아무 때나 허용하면** 늘 거짓으로 답하는 구현이 계약을 지킨다 — 아무것도 담지 않아 거짓 음성도 거짓 양성도 없다.
 * - **확률로 허용하면**(용량 미만에서 거절될 확률 ≤ δ) 거짓이 「찼다」인지 「운이 없었다」인지 호출자가 가르지 못해 가득
 *   참의 관측이 흐려진다. 판정도 서지 않는다 — 필터 하나에 한 번 나는 사건이라 원소마다 모아 셀 수 없고, 한 번의 거절로
 *   떨어뜨리면 경계의 올바른 구현이 확률 δ 로 떨어진다.
 * - **그래서 결정적으로 적었다** — 용량 미만이고 헷갈리지 않은(`has` 거짓) 원소는 거절하지 않는다. 헷갈린 원소 · 사본이 있는
 *   원소의 거절은 허용한다 — 같은 지문의 사본을 담을 자리가 유한한 기법을 배제할 근거가 목적에 없고, 그 거절은 호출자가
 *   `has` 로 미리 알 수 있다.
 * - **따라 나오는 것** — 밀어내기에 상한을 두고 거기서 거절하는 흔한 뻐꾸기 필터는 칸이 절반 차 있어도 드물게 거절하므로 이
 *   계약을 **어긴다.** 받을 자리를 하나 더 두면(곁 목록) 지킨다 — 정본이 그렇게 한다(`./_reference/cuckooFilter.ts` 머리말).
 *   곁 목록이 드물게만 차므로 한정자는 그대로 `expected` 다.
 *
 * **`add` · `has` · `delete` 가 `expected` 인 이유는 블룸 필터 헤더와 같다** — 「기대」는 `hash/hashSet` 이 정의한 뜻(구현이
 * 뽑는 무작위에 대한 호출 시퀀스 총비용)이고, 담긴 수에 비례하는 구현을 배제하는 가장 약한 한정자다. 밀어내기 · 곁 목록
 * 훑기가 무작위에 기댄 기대 상수이므로 `worst` 로 적으면 이름의 기법이 나간다. **상한의 log(1/ε)** 도 같은 판정이다(두
 * 물음은 그쪽 헤더). 축3은 L · ε 를 상수로 누르고 용량 n 만 키운다(§규약2 시나리오 규칙 3). 생성자는 시나리오 면제다.
 *
 * **이 계약은 시간과 오차만 말한다.** 공간을 계약에 넣는 규격 결정이 나면 목적 문단을 다시 연다(불변 사실 58).
 *
 * **주입 정책.**
 * - **`probabilistic/bloomFilter` 와 같다.** 주입받는 것이 없다 — 원소는 문자열로 고정하고 동등성은 `===` 다. 원소를 정수로
 *   펴는 함수도, 해시 씨앗도 받지 않는다(근거는 그쪽 헤더 — 펴기를 받으면 확률 문장이 주입자에 매달린다).
 * - `capacity` 는 0 이상의 정수, `falsePositiveRate` 는 0 보다 크고 1 보다 작은 수다. 아니면(`NaN` 포함) 생성자가
 *   `RangeError` 를 던진다. **용량 0 은 정당하다** — 모든 `add` 가 「담긴 사본이 n 개 이상」의 자리라 참이든 거짓이든 된다.
 * - `add` · `has` · `delete` 는 던지지 않는다. 사본이 없는 원소를 묻거나 지우는 것은 오류가 아니라 답이 있는 물음이다(위 표).
 *
 * **검증 등급.** `basic`.
 * 상한이 자명한 구현으로 달성된다 — **칸마다 수를 세는 블룸 필터**(원소마다 칸 k = ⌈log₂(1/ε)⌉ 개의 수를 올리고 내린다)는
 * 세 행을 호출마다 L 과 k 에 비례해 끝내고 용량 미만에서 거절하지 않으며, 그 시간에 확률 논증이 들지 않는다. 스위트에 넣어
 * 네 축 전부를 확인했다(`src/data-structures/_contract/_fixtures/countingBloomFilter.ts`). S1 이 「`add` 행 한정자가 등급을
 * 가른다」고 예상한 것은 이름의 기법 하나만 본 판정이었다 — 등급은 달성하는 자명한 구현이 하나라도 있는가를 묻는다(불변
 * 사실 55). 오차 보장의 확률 논증은 등급을 올리지 않는다(불변 사실 203). 불변식 절이 비었다.
 *
 * **필요충분조건.**
 * - 의미: 사본이 남은 원소에 거짓 음성이 **없고**(헷갈린 지우기 전까지), 지우기가 **사본을 빼며**, 용량 미만의 헷갈리지 않은
 *   새 원소를 **거절하지 않고**, 사본이 없는 원소의 `has` · `delete` 가 참일 확률이 **ε 이하**여야 한다. 지우기가 사본을 빼지
 *   않으면 블룸 필터이고, 새 원소를 거절해도 되면 늘 거절하는 구현이 지키며, 확률 문장을 버리면 늘 참으로 답하는 구현이
 *   지킨다.
 * - 비용: 세 행이 담긴 수나 용량에 비례하면 사본을 늘어놓고 훑는 목록이다.
 */
export class CuckooFilter {
  constructor(capacity: number, falsePositiveRate: number) {
    throw new Error("Not implemented");
  }

  add(item: string): boolean {
    throw new Error("Not implemented");
  }

  has(item: string): boolean {
    throw new Error("Not implemented");
  }

  delete(item: string): boolean {
    throw new Error("Not implemented");
  }
}
