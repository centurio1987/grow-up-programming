/**
 * HyperLogLog — 넣은 원소 중 서로 다른 것이 몇인지 묻는 추정기. 추정은 정해 둔 확률로 실제 수의 정해 둔 비율 안에 들고,
 * 따로 센 둘을 합칠 수 있다.
 *
 * **목적.** 문자열 원소를 거듭 넣고 「서로 다른 원소가 몇인가」를 묻는 일, 그리고 **따로 센 두 추정기를 합쳐 합집합의 수를
 * 묻는 일**을 넣은 원소의 수와 무관한 비용에 제공하되, 답이 틀리는 것을 허용하는 것. 들어온 서로 다른 원소 수를 n 이라 할 때
 * 추정이 n 에서 n 의 ε 배 넘게 벗어날 확률이 δ 이하다 — **상대 오차 ε** 와 **실패 확률 δ** 는 생성 때 받는다. 같은 원소를 몇
 * 번 넣든, 어느 순서로 넣든, 어떻게 나눠 넣고 합치든 **답이 들어온 원소의 집합만으로 정해진다**는 것은 확률 없이 선다.
 *
 * **두 매개변수가 계약이고, 물려받은 매개변수와 `error()` 는 표현이었다.** 물려받은 생성자는 `precision` — 자리 수 m = 2^p 의
 * 로그 — 을 받았고 `error()` 가 1.04/√m 을 돌려줬다. 둘 다 한 표현의 눈금과 그 표현의 상수라 오차를 그것으로 적으면 그 표현을
 * 처방한다(`docs/ORD-006-conventions.md` 「A군 17종 판정」 ④). `error()` 는 표면에서 뺐다 — 돌려줄 값이 구현마다 다르고, 호출자가
 * 알아야 할 오차는 자기가 준 ε · δ 다. 물려받은 문서의 조화 평균 공식 · 보정 구간 · 「95% 신뢰 구간」은 그 표현에서만 참이라
 * 옮기지 않았다(불변 사실 38).
 *
 * **합치기(`merge`)를 계약에 남긴 까닭 — 고유한 변경이고 목적이 그 사용을 적는다.** 넣기와 추정으로는 따로 센 둘의 합집합 추정을
 * 짓지 못해 조합이 아니고(원칙 A 의 A2), 목적의 둘째 문장(따로 센 둘을 합친다)이 그 사용을 적는다(A3). 판정 줄은
 * `docs/ORD-006-conventions.md` 「원칙 A — 연산 선정」 판정표 ① 의 33 번째 행이다. **상한은 따로 고른 값이다.** 합치기의 비용을
 * **들어온 원소 수와 무관하게** 적으면 들어온 원소를 빠짐없이 담는 정확한 집합이 나간다 — 합칠 때 두 쪽 원소를 옮겨야 하기
 * 때문이다(실행으로 확인 — `src/data-structures/_contract/_fixtures/exactSetSketch.ts`). 목적의 둘째 문장(본 원소 수와 무관하게)이 그
 * 배제를 요구한다. 이 배제는 상한의 근거이지 연산을 남긴 근거가 아니다. **저장량을 적은 것은 아니다** — 합치기의 시간을 적었고,
 * 그 시간 안에 두 쪽 원소를 옮길 수 없다는 것이 따라 나올 뿐이다(§규약1 「공간은 어느 계약에도 없다」).
 *
 * 이 계약에 없는 것은 넷이다.
 *
 * - **공간.** 위 문단 끝과 같다.
 * - **지우기 · 교집합 · 원소 목록.** 물려받은 표면에 없고, 없으면 목적을 진술하지 못하는 연산이 아니다(불변 사실 47).
 * - **다른 실행에서 센 추정기와 합치기.** 표면에 추정기를 바이트로 옮기는 연산이 없다. 결정적인 쪽이 「같은 실행」에서만 서는
 *   까닭이 이것이다(아래 「오차 보장」).
 * - **매개변수가 다른 추정기끼리 합치기.** 합친 결과가 어느 (ε, δ) 의 보장을 받는지 정할 수 없어 `RangeError` 로 거절한다.
 *
 * **이름은 임시다.** 「하이퍼로그로그」는 자리마다 해시의 앞쪽 0 비트 수를 적고 조화 평균으로 추정하는 **기법의 이름**이다.
 * 계약이 말하는 것은 「합칠 수 있는 근사 서로 다른 원소 수」이고 계약 이름은 KAN-031 이 정한다(불변 사실 198).
 *
 * **불변식.** 없다.
 *
 * 판별 절차(§규약1 「불변식 판별 절차」)를 돌린 결과다. 추정을 읽는 공개 연산은 `count` 하나다. `merge` 가 돌려준 인스턴스의
 * `count` 가 둘째 경로처럼 보이지만 그 정합(「합집합을 한 인스턴스에 넣은 추정과 같다」)을 `merge` 행이 이미 적고 있으므로 두 번째
 * 물음에서 걸린다 — `heap/leftistHeap` 의 합치기와 같은 자리다. 자리 값의 성질은 표현이라 후보가 아니다(불변 사실 36).
 *
 * **연산 계약.** L 은 인자 문자열의 길이(UTF-16 코드 단위 수), ε 는 상대 오차, δ 는 실패 확률이다. 인스턴스에 **들어온 원소**는
 * 그 인스턴스에 `add` 로 넣은 원소와, 그 인스턴스를 돌려준 `merge` 의 두 쪽에 들어온 원소의 합집합이다. n 은 들어온 원소 중 서로
 * 다른 것의 수다.
 *
 * | 연산 | 의미 | 상한 | 한정자 |
 * |---|---|---|---|
 * | `constructor(epsilon, delta)` | 상대 오차 ε · 실패 확률 δ 인, 들어온 원소가 없는 추정기를 연다 | O(1/(ε² · δ)) | worst |
 * | `add(item)` | `item` 이 들어온 원소가 된다. 반환이 없다. 이미 들어온 원소면 이 뒤의 모든 관측이 부르지 않았을 때와 같다 | O(L · log(1/δ)) | expected |
 * | `count()` | n 의 추정을 돌려준다. **같은 실행 안에서 같은 (ε, δ) 로 세운 인스턴스에 들어온 원소 집합이 같으면 같은 값이다.** 추정이 벗어날 확률을 아래 「오차 보장」이 누른다 | O(1/(ε² · δ)) | expected |
 * | `merge(other)` | 들어온 원소가 두 쪽의 합집합인 **새** 인스턴스를 돌려준다. `this` · `other` 는 그대로다. `other` 는 이 구현으로 같은 (ε, δ) 로 세운 인스턴스여야 하고 매개변수가 다르면 `RangeError` 다. `other` 가 `this` 여도 된다 | O(1/(ε² · δ)) | expected |
 *
 * **결정적인 쪽과 확률적인 쪽.** `count` 행의 굵은 문장 하나가 결정적인 쪽 전부다. 따라 나오는 것 둘을 이름으로 적는다 — 같은
 * 원소를 다시 넣어도 추정이 안 바뀐다(`add` 행) · 합친 인스턴스는 두 쪽 원소를 한 인스턴스에 넣은 것과 같은 추정을 낸다(넣은 순서 ·
 * 나눈 방식과 무관하다). 물려받은 문서의 「merge 는 원본을 변경하지 않는다」도 여기 들었다.
 *
 * **오차 보장.** 확률 문장 하나와 그것을 판정하는 문장 하나다.
 *
 * - **확률 문장.** 어느 인스턴스에서든, `count()` 가 n 에서 ε · n 넘게 벗어날 확률이 δ 이하다(n = 0 이면 추정이 0 이 아닐 확률이
 *   δ 이하다).
 * - **확률의 출처는 구현이 뽑는 무작위다 — 입력의 분포가 아니다. 그 무작위를 같은 실행의 인스턴스가 함께 쓴다.** 결정적인 쪽이
 *   따로 세운 두 인스턴스에 같은 집합이 들어가면 같은 답을 요구하므로, 원소를 어디로 보내는지를 인스턴스마다 따로 뽑을 수 없다.
 *   그래서 확률 문장은 **실행이 무작위를 뽑기 전에** 정해진 원소 집합에 대해 선다. 같은 실행의 다른 인스턴스가 낸 답을 보고 원소를
 *   고르는 호출자는 덮지 않는다. 입력 분포로 읽지 않는 근거는 `probabilistic/bloomFilter` 헤더의 셋과 같고, **해시를 고정한 구현은
 *   이 계약을 어긴다** — 그 해시에 대해 자리 하나로 몰리는 원소 집합이 실재하고 그 집합을 미리 고른 호출자에게 확률이 1 이다.
 *   스위트는 구현을 읽지 않고는 그 집합을 짓지 못하므로(불변 사실 44) 그 위반은 스위트를 통과한다(블룸 필터에서 실행으로 보인 모양 —
 *   불변 사실 298, 이 계약에서는 다시 돌리지 않았다).
 * - **판정 — 독립 시행.** 스위트는 확률 문장을 축1 통계 판정으로 본다(2026-09-15 유저 결정 — 축을 늘리지 않는다 · 2026-09-16 결재 —
 *   러너 `src/data-structures/_contract/runTrials.ts`). 공유 범위가 실행이므로 **시행 하나 = 새 실행(워커) 하나**이고, 그 안에서 새
 *   인스턴스 넷에 서로 다른 원소 n 개씩(1 ~ max(4,096, ⌈16/(ε² · δ)⌉) 의 로그 눈금 네 칸에 하나씩)을 넣는다. 원소 목록은 입력 seed ·
 *   시행 번호 · 모양만으로 **워커를 띄우기 전에** 정한다. 시행 t 의 몫 Z_t 는 추정이 ε · n 넘게 벗어난 인스턴스의 비율이고, 모양마다
 *   T 시행의 합 S 가 한계 k 를 넘으면 떨어진다 — (ε, δ) = (0.3, 0.3) 은 T 16 · k 15, (0.1, 0.1) 은 T 56 · k 21.
 * - **[보장]** 부등식: Hoeffding(1963) 정리 1, P(S > k) ≤ exp(−T · D(k/T ‖ δ)). 전제: ① 입력이 이 계약의 무작위와 독립으로 정해진다
 *   (위 문장 — 확률 문장의 「실행이 무작위를 뽑기 전에」) ② 시행끼리 독립 — 이 계약의 공유 범위가 실행이고 시행마다 새 실행이다(워커
 *   사이의 `Math.random` 이 독립이라는 것은 실행 환경의 전제로 둔다) ③ 시행당 E[Z_t] ≤ δ — 확률 문장과 기댓값의 선형성(인스턴스
 *   사이 독립은 요구하지 않는다). 값: 판정마다 4.23 × 10^−7 · 3.08 × 10^−7, 스위트 한 번 7.31 × 10^−7 이하. **한 실행의 인스턴스가
 *   함께 틀리는 구현**(해시를 실행이 함께 쓰는 이 계약의 기본 모양)도 이 상한을 그대로 받는다 — 앞 판정(인스턴스를 한 실행에서
 *   ⌈16/δ⌉ 개 모아 셈)이 그 구현을 확률 δ 로 떨어뜨리던 틈이 여기서 닫혔다.
 * - **[경험]** 판정이 잡는 것과 놓치는 것은 fixture 실측이다(`docs/ORD-006-conventions.md` 의 `S24` 절 「fixture 수치」 —
 *   반복 수와 결과 범위). **검사 못 하는 의무**: 벗어날 확률이 δ 보다 크되 k/T 근처(0.1 모양에서 0.375) 아래인 구현은 판정의
 *   해상도 밖이고, 해시를 고정한 구현은 위 「확률의 출처」 문단 그대로 통과한다. 보완 작업은 `docs/ORD-006-conventions.md` 의
 *   `S24` 절 「검사 못 하는 의무」 표.
 *
 * **`add` · `count` · `merge` 가 `expected` 인 이유.** 「기대」의 뜻은 `hash/hashSet` 헤더가 정의한 것과 같다 — 구현이 뽑는 무작위에
 * 대한, 호출 **시퀀스** 총비용의 기댓값이다. 이 계약이 배제하려는 것은 들어온 원소 수에 비례하는 구현이고, 그것을 배제하는 가장
 * 약한 한정자가 이 뜻의 `expected` 다(불변 사실 21 · 59). 정본은 셋 다 호출마다 같은 비용이라 `worst` 로도 지키지만, `worst` 를 적으면
 * 원소가 적은 동안 원소를 따로 들었다가 몰아 옮기는 구현(작은 구간을 성기게 적어 두는 변형)이 나간다. 그 배제를 목적이 요구하지 않는다 —
 * 논증이고, 그 구현을 짓지는 않았다.
 *
 * **상한은 고른 값이다(불변 사실 48 · 51).**
 *
 * - **`add` 의 `O(L · log(1/δ))`.** 더 강하게 `O(L)` 로 적으면 δ 를 맞추려고 추정기 log(1/δ) 벌의 가운데 값을 쓰는 구현이 나간다 —
 *   배제할 근거가 목적에 없다. 더 약하게 `O(L/(ε² · δ))` 로 적으면 넣을 때마다 요약 전체를 훑는 구현이 들어오고, ε 를 1/√n 가까이
 *   낮추면 넣기 하나가 들어온 원소 수에 비례하는 셈이 된다.
 * - **`count` · `merge` 의 `O(1/(ε² · δ))`.** 들어온 원소 수를 적지 않는 것이 목적에서 온 부분이다(위 「합치기를 남긴 까닭」).
 *   ε · δ 의 함수로서는 **추정의 흩어짐만으로 δ 를 맞추는 구현** — 체비쇼프 부등식으로 요약 하나의 크기를 1/(ε² · δ) 에 비례하게
 *   잡는 것, 이름의 기법을 확률 논증 하나로 세우는 곧은 길 — 이 서는 계급으로 정했다. 더 강하게 `O(log(1/δ)/ε²)` 로 적으면 그 구현이
 *   나가고, `O(1)` 로 적으면 요약 전체를 훑어 추정하는 이름의 기법 그대로가 나간다.
 *
 * **축3은 L · ε · δ 몫을 재지 않는다** — 사다리가 들어온 원소 수만 키우고 셋을 상수로 누른다(§규약2 시나리오 규칙 3). 생성자는 n 에
 * 대해 반복 호출되지 않아 시나리오 면제다(시나리오 규칙 2).
 *
 * **주입 정책.**
 * - **주입받는 것이 없다.** 원소 타입은 문자열로 고정하고, 원소의 동등성은 문자열 값이 같은가(`===`)다. 원소를 정수로 펴는 함수도
 *   해시 함수도 씨앗도 받지 않는다 — 까닭은 `probabilistic/bloomFilter` 헤더와 같다.
 * - `epsilon` · `delta` 는 둘 다 0 보다 크고 1 보다 작은 수여야 하고 아니면(`NaN` 포함) 생성자가 `RangeError` 를 던진다. ε ≥ 1 은 늘
 *   0 을 돌려주는 구현을 정당화해 뜻이 없고(|0 − n| ≤ n), ε = 0 은 정확한 수를 요구한다. δ ≥ 1 은 확률 문장을 비우고, δ = 0 은
 *   결정적 한계를 요구해 상한의 1/δ 가 서지 않는다.
 * - `merge(other)` 의 `other` 는 이 구현의 인스턴스다. 매개변수(`===` 로 견준 ε · δ)가 다르면 `RangeError` 를 던지고 두 쪽이 그대로다.
 * - `add` · `count` 는 던지지 않는다.
 *
 * **검증 등급.** `basic`.
 * 상한이 자명한 구현으로 달성된다 — 자리 1/(ε² · δ) 개에 원소마다 해시 한 번으로 자리 하나를 고치고, 추정 · 합치기에서 자리를 한 번씩
 * 훑는 구현은 **그 시간**에 확률 논증이 들지 않는다. 오차 보장에는 확률 논증이 들지만 등급을 올리지 않는다 — 판정 절차 2번은 시간
 * 상한만 읽는다(불변 사실 203). 등급이 정하는 것은 축3 엄격도이고 오차는 축1이 판정한다. 불변식 절이 비었다.
 *
 * **필요충분조건.**
 * - 의미: 추정이 ε · n 넘게 벗어날 확률이 **δ 이하**이고, 같은 실행 · 같은 매개변수에서 **추정이 들어온 원소 집합만으로 정해져야** 한다.
 *   앞을 버리면 **늘 0 을 돌려주는 구현**이 계약을 지킨다 — 상수는 어느 집합의 함수이기도 해서 뒤를 공짜로 지킨다. 뒤를 버리면 부를
 *   때마다 오차 안에서 값을 새로 고르는 구현이 들어오고, 그러면 같은 원소를 다시 넣었을 때 · 나눠 넣고 합쳤을 때의 답을 한 곳에 넣은
 *   답과 견줄 수 없어 「서로 다른」 원소를 센다는 뜻이 호출자에게 관측되지 않는다. 합치기를 빼면 정확한 집합보다 적게 약속하는
 *   계약이다.
 * - 비용: `add` 가 들어온 원소 수에 비례하면 원소를 늘어놓고 겹침을 훑는 목록이고, `merge` 가 비례하면 원소를 전부 담아 옮기는
 *   집합이다 — 어느 쪽이든 본 원소 수와 무관하게 세고 합친다는 값이 남지 않는다.
 */
export class HyperLogLog {
  constructor(epsilon: number, delta: number) {
    throw new Error("Not implemented");
  }

  add(item: string): void {
    throw new Error("Not implemented");
  }

  count(): number {
    throw new Error("Not implemented");
  }

  merge(other: HyperLogLog): HyperLogLog {
    throw new Error("Not implemented");
  }
}
