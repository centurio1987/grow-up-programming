/**
 * SkipList — 전순서를 유지하는 집합. 어느 입력에도 **기대 비용**이 로그 안에 든다.
 *
 * **이 계약은 `tree/treap` 의 계약과 같다 — 두 계약이 말하는 범위 안에서**(아래 「「같다」의 범위」). 아래 연산 계약 표의 여덟 행이
 * 그 파일의 여덟 행과 글자까지 같고, 스위트도 그 파일의 `ContractSpec` 을 그대로 돌린다(`./skipList.contract.ts`). 그 사실을 감추지
 * 않고 여기 맨 앞에 적는다 — 이름이 다르다고 계약이 다른 것이 아니다(불변 사실 43 · 230). 판정의 입력은 WBS 가 적어 둔
 * 「둘 다 정렬 집합 expected 다」(`docs/ORD-006-wbs.md` §2 「트랙을 넘는 의존 하나」)가 아니라 아래 여섯 시도다 — 물려받은
 * 상한 · 한정자 주장은 판정의 입력이 아니다(불변 사실 65).
 *
 * **「같다」의 범위.** 불변 사실 43 의 「만족하는 구현 집합이 같다」는 **지금 두 헤더의 문장**으로 잰 것이고, 그 문장이 다루는 것은 셋이다.
 *
 * 1. **연산 집합** — 표의 여덟 행과 그 의미, 불변식 넷. 원소 핸들 · 차례 따라가기 · 순위 같은 행이 더해지면 그 표면에서 판정을 다시 낸다.
 * 2. **비용 모형** — 행마다의 **시간** 상한과 한정자(`expected` 여섯 · `worst` 셋), 단일 스레드 호출열 위에서.
 * 3. **주입 모형** — 비교자를 주입받고 무작위성은 주입받지 않는다(호출자가 그것을 알 수 없어야 한다).
 *
 * **이 판정이 말하지 않는 것.** 두 계약이 다루지 않는 영역이라 「같다」가 거기까지 가지 않는다.
 *
 * - **공간** — 두 계약 모두 시간만 말한다(§규약1 「공간은 어느 계약에도 없다」 — 미결). 공간 조항이 들어오면 이 판정은 다시 열린다(불변
 *   사실 61). 저장량을 무작위와 무관한 상한으로 묶으면 층의 윗끝이 없는 줄은 층 수가 동전에 달려 그 조항에서 갈릴 수 있다(산술 — 재지 않았다).
 * - **동시 접근** — 선형화 · 진행 보장은 `probabilistic/concurrentSkipList` 가 따로 세운 계약이다(불변 사실 237). 이 판정은 그 계약과
 *   `tree/treap` 의 관계를 말하지 않는다.
 * - **무작위를 담는 모양과 상수** — 층 분포 · 우선순위 분포, 절대 비용(아래 두 정본 표에서 시나리오마다 1.6 ~ 1.7 배씩 반대로 갈린다),
 *   기대값 밖의 분포(분산 · 꼬리 확률). 계약의 말이 아니어서 판정의 입력이 되지 못했다(불변 사실 76).
 * - **`expected` 의 읽기를 새로 정하지 않는다** — 두 헤더가 표를 같은 객체로 쓰므로 함께 움직이고, 읽기는 `tree/treap` 헤더 끝이 호출별
 *   기대로 두었다(아래 「`expected` 의 확률이 어디서 오는가」).
 *
 * 두 이름을 가르려고 여섯을 시도했고 여섯 다 계약의 문장이 되지 못했다.
 *
 * - **정수 키만 받는다.** 물려받은 표면은 `number` 키였다. 어느 행도 키를 더하거나 쪼개지 않고 견주기만 하므로, 좁혀도
 *   목적이 얻는 것이 없고 넓혀도 나가는 구현이 없다(§「주입 정책도 상한과 같은 두 물음으로 정한다」). 좁히면 오히려 정수의
 *   비트를 읽는 구조(`heap/vanEmdeBoasTree` 계열)가 들어오는데 그것은 이 이름이 약속해 온 것이 아니다. 비교자를 주입받는
 *   `tree/treap` 의 표면으로 맞췄다. B1 이 `probabilistic/concurrentSkipList` 와 이 구조를 견줄 때 「실제 차이는 제네릭이었다」로
 *   적은 자리가 이것이다(`docs/ORD-006-conventions.md` 「`probabilistic/concurrentSkipList` — Rust 전용으로 존치」).
 * - **최대 층 16 · 승격 확률 0.5.** 물려받은 문서가 처방한 두 상수다. 층은 내부 표현이라 적는 순간 처방이다(금지 1). 게다가
 *   **층의 윗끝을 고정하는 설계는 이 계약을 어긴다** — 담긴 수가 $2^{\text{윗끝}}$ 을 넘으면 맨 윗줄에 마디가 담긴 수에 비례해
 *   남는다(아래 「스위트가 보는 것과 못 보는 것」). 승격 확률은 윗끝이 없으면 계약 안에서 움직인다 — 어느 상수 확률이든 기대
 *   층 수가 담긴 수의 로그에 비례한다. 1/2 · 1/4 · 1/8 로 바꾼 사본을 40 회씩 돌리면 40 · 39 · 37 회 통과하고, 걸린 네 번은
 *   전부 오름차순 조회에서 이웃 비율이 허용 상단(1.56)을 넘은 흔들림이다(최대 1.77) — 확률을 낮출수록 윗줄이 성겨 한 번 지은
 *   모양의 분산이 커진다. 계급이 갈린 것이 아니므로 계약의 문장이 되지 못한다(불변 사실 235).
 * - **무작위의 출처 — 층을 뽑는가, 우선순위를 뽑는가.** 두 기법이 가르는 자리가 이것인데 계약은 출처를 말하지 않는다. `tree/treap`
 *   헤더가 적은 대로 **계약이 요구하는 것은 무작위성의 출처가 아니라 호출자가 그것을 알 수 없다는 것**이고, 층을 동전으로 정하는
 *   구현도 우선순위를 난수로 정하는 구현도 그 요구를 같은 방식으로 채운다. 반대로 층을 호출자가 되짚을 수 있는 규칙(넣은 차례 ·
 *   씨앗을 고정한 의사 난수)으로 정하면 그 구현은 계약을 어긴다(불변 사실 232) — 기법이 아니라 요구가 가른다.
 * - **동시에 여러 스레드가 쓰기 좋다.** 물려받은 스토리의 근거였다. 그 약속은 `probabilistic/concurrentSkipList` 가 선형화 ·
 *   진행 보장으로 **계약에 넣었고**(B21) 그쪽은 등급(`concurrency`)과 연산 집합이 갈렸다 — `size`·`toArray` 를 뺐다
 *   (`docs/ORD-006-conventions.md:1634-1635`). 뺀 근거가 「여러 원소를 한 시점의 것으로 함께 보는 관측은 선형화 대상으로 넣으면
 *   모든 변경이 그 관측과 원자적으로 묶인다」였으므로 **단일 스레드 계약에는 그 근거가 없다.** 이 계약은 둘을 둔다(불변 사실 237).
 *   동시 접근을 약속하지 않는 이 계약은 그쪽과 다른 계약이고, 같은 계약은 `tree/treap` 이다.
 * - **맨 아래 줄을 따라 차례로 읽기.** 구간 읽기와 전체 열거가 줄을 따라가면 되는 것이 이 기법의 장점으로 꼽히는데, 두 연산은
 *   `tree/treap` 계약에 이미 같은 상한으로 있다(`range` $O(\log n + k)$ · `toArray` $O(n)$). 트리도 중위 순회로 같은 계급을 낸다.
 *   원칙 A 로는 A2(두 연산으로 조합) · A4(같은 상한이라 떨어지는 계열 없음) · A5(반례가 기대지 않음)로 두지 않는 행이다
 *   (`docs/ORD-006-conventions.md` 「원칙 A」 판정표 ① 55).
 * - **공간.** 마디당 기대 링크 수(승격 확률 1/2 에서 2)는 특정 구현의 진술이고 이 계약은 시간만 말한다(§규약1 「공간은 어느 계약에도
 *   없다」 — 미결).
 *
 * **그래서 위 범위 안에서 두 이름은 계약이 아니라 무작위를 담는 모양으로 갈린다.** 계약이 구현을 처방하지 않기로 한 이상 그 갈림은 계약에 들어올
 * 수 없고, 자리는 가이드다(불변 사실 45 · 61). 지우지 않는 이유는 판정이 *"계약이 담지 못한다"* 이지 *"쓸모가 없다"* 가 아니기
 * 때문이다. **이름은 임시다** — `tree/treap` 도 `probabilistic/skipList` 도 구현 이름이고, 계약 이름과 카테고리(`probabilistic/`)는
 * KAN-031 로 넘긴다(불변 사실 64).
 *
 * **두 정본의 판정이 갈리지 않는다는 것을 수치로 확인했다**(불변 사실 234). 같은 스위트를 20 회씩 돌려 둘 다 20 회 전부 통과했고, 절대
 * 비용은 시나리오마다 **반대 방향으로** 갈린다(사다리 끝 n = 2^14 의 20 회 범위).
 *
 * | 시나리오 | 우선순위를 뽑는 트리(`tree/treap`) | 층을 뽑는 줄(이 정본) |
 * |---|---|---|
 * | 오름차순 넣기 | 16.85 ~ 19.49 | 27.20 ~ 30.04 |
 * | 무작위 넣기 | 45.79 ~ 48.27 | 25.71 ~ 27.99 |
 * | 오름차순 조회(`has`·`min`·`max`) | 35.20 ~ 43.19 | 56.01 ~ 68.63 |
 * | 오름차순 지우기 | 18.43 ~ 20.93 | 13.88 ~ 17.37 |
 * | 좁은 구간 읽기 | 18.24 ~ 19.19 | 27.71 ~ 30.27 |
 *
 * 무작위 넣기는 트리가 1.7 배 비싸고 오름차순 조회는 줄이 1.6 배 비싼데, 이웃 크기 사이의 비는 두 정본 모두 로그 구간 안이다.
 * **「어느 기법이 낫다」가 입력에 달렸다는 것이 곧 계약이 그 기법을 고를 수 없다는 직접 증거다**(§「성격 전환의 실행 규격 보강」).
 *
 * **목적.** 같은 원소를 두 벌 담지 않으면서 전순서를 유지하고, 담기 · 지우기 · 찾기 · 양 끝 읽기 · 구간 읽기를 원소 수의 **기대**
 * 로그 비용으로 제공하는 것. **기댓값은 구현이 뽑는 무작위성에 대한 것이지 입력에 대한 것이 아니다** — 호출자가 어떤 입력을 주든,
 * 오름차순으로 넣어도 넣은 순서대로 찾아도 기대가 로그여야 한다. 「입력 분포에 대한 기댓값」으로 읽으면 층을 두지 않은 정렬된
 * 연결 목록조차 무작위 입력의 **순위**가 고르다는 이유로 들어올 자리가 생기고, 계약이 아무것도 배제하지 못한다. 근거 전문은
 * `tree/treap` 헤더의 같은 항목에 있다.
 *
 * 같은 값을 여러 벌 담는 일은 이 계약에 없다(`tree/multiset`). 위치로 읽는 일(k 번째 원소 · 순위)도 없다
 * (`tree/orderStatisticTree`). 동시 접근도 없다(`probabilistic/concurrentSkipList`).
 *
 * **불변식.** 넷이다. 넷 다 **관측 경로가 둘**이라 성립한다 — 구현이 두 경로를 따로 유지할 수 있고, 따로 유지하는 순간 갈린다.
 * 어떤 연산 뒤에도 성립해야 한다.
 * 1. `toArray().length === size()`. 원소 수를 읽는 길이 둘이다.
 * 2. 임의의 `x` 에 대해 `has(x)` 는 `toArray()` 안에 `x` 와 동등한(비교자가 0) 원소가 있는가와 같다. 담김 여부를 읽는 길이 둘이다.
 * 3. 비어 있지 않으면 `min()` 은 `toArray()` 의 첫 원소와, `max()` 는 마지막 원소와 같다. 양 끝을 읽는 길이 둘이다.
 * 4. 임의의 `low ≤ high` 에 대해 `range(low, high)` 는 `toArray()` 에서 그 구간에 드는 원소만 남긴 것과 같다. 구간을 읽는 길이 둘이다.
 *
 * 넷이 `tree/treap` 의 넷과 같은 것은 베낀 것이 아니라 계약이 같고 판별 절차가 같은 답을 내기 때문이다(§규약1 「불변식 판별
 * 절차」). 줄마다 키가 오름차순이라는 것, 윗줄이 아랫줄의 부분이라는 것은 불변식이 **아니다** — 층은 계약의 말이 아니고 적는 순간
 * 처방이다(불변 사실 36). 줄이 치우친 구현은 축3의 성장률이 잡는다.
 *
 * **연산 계약.** n 은 담긴 원소 수, k 는 `range` 가 돌려주는 원소 수다.
 *
 * | 연산 | 의미 | 상한 | 한정자 |
 * |---|---|---|---|
 * | `constructor(comparator?)` | 비교자를 고정한다. 이후 교체할 수 없다 | O(1) | worst |
 * | `insert(item)` | `item` 을 담는다. 동등한 원소가 이미 있으면 **상태가 바뀌지 않는다** | O(log n) | expected |
 * | `delete(item)` | 동등한 원소를 지우고 `true`. 없으면 `false`, 상태는 불변 | O(log n) | expected |
 * | `has(item)` | 동등한 원소가 담겨 있으면 `true` | O(log n) | expected |
 * | `min()` / `max()` | 비교자 기준 최소·최대. 비어 있으면 `null` | O(log n) | expected |
 * | `range(low, high)` | `low ≤ v ≤ high` 인 원소를 비내림차순 배열로. 사본이다 | O(log n + k) | expected |
 * | `size()` | 담긴 원소 수 | O(1) | worst |
 * | `toArray()` | 비내림차순 배열의 사본. 반환값을 고쳐도 원본은 바뀌지 않는다 | O(n) | worst |
 *
 * 한정자의 근거(`size`·`toArray`·생성자만 `worst` 인 이유, `min`·`max`·`range` 를 넣은 근거)는 `tree/treap` 헤더의 같은 문단이
 * 적은 그대로다 — 계약이 같으므로 근거도 같다. 이 기법에서 새로 확인한 것만 적는다.
 *
 * **`expected` 의 확률이 어디서 오는가 — 구현 안의 동전이다**(불변 사실 236). 이 기법은 새 마디의 층을 입력과 무관하게 뽑으므로,
 * 호출열을 고정해 두어도 각 호출의 비용이 그 동전들에 대한 확률 변수다. 계약이 말하는 기댓값이 그것이다 — **호출별 기대로 두고**
 * 검사에 맞춰 낮추지 않는다(`docs/ORD-006-conventions.md` 「원칙 B」 B7 · `tree/treap` 헤더 끝). 무작위 출처를 셋으로 가르면(B2) 스위트의
 * seed 는 입력 seed 이고, 층 동전은 구현 무작위이며, 주입 무작위는 받지 않는다(아래 「주입 정책」). **앞 여덟 시나리오는 호출별 기대를
 * 재지 못한다** — `expected` 통계가 seed 다섯의 시퀀스 평균 중앙값이라 결정론적인 `_contract/_fixtures/splayingSearchTree.ts` 가 그
 * 여덟을 통과한다. `S28` 이 `tree/treap` 스위트 끝에 더한 **입력 고정 탐침**(채우기와 조회 자리를 고정하고 조회 열여섯만 잰다 — B1)이
 * 그 구현을 원소 수에 비례해 잡고, 이 정본은 2,000 회 돌려 0 회 떨어졌다([경험] — B6 때문에 [보장] 이 서지 않는다). 탐침도 못 보는 의무
 * (나머지 다섯 행의 호출별 기대 · 드문 큰 비용 · 되짚어야 지어지는 입력 · 시행 독립)와 보완 작업(`KAN-041`)은 `tree/treap` 헤더 끝
 * 「검사 못 하는 의무」 표가 이 계약의 것으로 적는다 — 성격 전환이라 따로 적지 않는다.
 *
 * **스위트가 보는 것과 못 보는 것 — 이 이름의 흔한 결함 다섯**(`_contract/_fixtures/levelRuleSkipList.ts` · 불변 사실 231–233).
 *
 * | 결함 | 스위트 | 수치 |
 * |---|---|---|
 * | 층을 1~16 에서 고르게 뽑는다 | 걸린다 — 넣기 둘 · 조회 · 구간(40 회 전부). `S22` 무작위 차례 지우기 · `S28` 입력 고정 탐침에서도 걸린다(40 회 전부) | 오름차순 넣기 55.8~70.5 · 149.9~160.7 · 530.9~557.1(10 회 범위) |
 * | 층의 윗끝 4 | 걸린다 — 같은 넷에 같은 둘을 더해 여섯(40 회 전부) | 오름차순 넣기 66.4~77.3 · 254.5~279.7 · 1,022.4~1,053.6(10 회 범위) |
 * | **층의 윗끝 16(물려받은 설계)** | **통과한다**(아홉 전부 · 40 회 전부) | 사다리 끝 $2^{14}$ 가 문턱 $2^{16}$ 아래다. 윗끝 4 사본이 같은 결함을 사다리 안으로 끌어온 것이다 |
 * | **씨앗을 고정한 의사 난수로 층을 정한다** | **통과한다**(`S28` 입력 고정 탐침 포함 아홉 전부) | 층의 수열을 되짚어 한 층짜리 마디를 오른쪽에 몰아 넣으면 그 구간을 찾는 호출 평균이 157.5 · 534.3 · 2,062.3(같은 입력에서 이 정본 16.7~26.2 · 22.2~29.9 · 25.5~33.8, 10 회 범위). 그 입력은 구현을 읽어야 지어져 시나리오가 될 수 없다(불변 사실 44) |
 * | **지우기가 층마다 머리부터 다시 훑는다** | 걸린다 — 무작위 차례 지우기 하나(`S22` 가 더한 여덟째, 40 회 전부) | 오름차순 지우기는 늘 최솟값을 지워 훑을 거리가 0 이라 통과한다. 무작위 차례로 전부 지우면 호출 평균 347.4~366.5 · 1,355.4~1,386.9 · 5,490.5~5,544.2(이 정본 16.9~22.0 · 21.5~25.8 · 25.4~28.4, 10 회 범위) |
 *
 * 둘째 · 셋째 줄이 윗끝 결함 계열의 **판정 해상도**를 보인다 — 상수 하나(윗끝)가 계급 문턱이 되는 설계는 문턱이 사다리 안에 들어올
 * 때만 걸린다. 셋째 · 넷째 줄은 계약 위반이 숨은 통과다(불변 사실 62). **다섯째는 T5-04 가 찾은 스위트의 구멍이었다** — 이 유닛은
 * 그 스위트가 `tree/treap` 의 것이라 고치지 않았고(`docs/ORD-006-wbs.md` §4), `S22` 가 그 스위트 끝에 무작위 차례 지우기 시나리오를
 * 더해 막았다(불변 사실 242). 성격 전환이라 이 파일의 스위트는 같은 객체로 따라 움직였다.
 *
 * **주입 정책.**
 * - `comparator(a, b)` 는 음수·0·양수를 돌려주는 전순서여야 한다. 0 은 「동등」을 뜻하고, **동등한 두 원소는 이 집합에 함께 담기지
 *   않는다.** 나중에 넣은 쪽이 버려지는지 앞의 것을 덮는지는 계약이 약속하지 않는다.
 * - 미제공 시 기본 비교자는 `<`·`>` 를 쓴다. 따라서 `number`·`string` 밖의 `T` 에는 비교자 주입이 **필수다.**
 * - 빈 집합의 `min`·`max` 는 예외가 아니라 `null` 이다. 그 대가로 `T` 에 `null` 이 섞이면 「비어 있음」과 「값이 `null` 인 원소」가
 *   구분되지 않는다.
 * - `range(low, high)` 에서 `low > high` 면 빈 배열이다. 예외가 아니다.
 * - **무작위성은 주입받지 않는다.** 물려받은 문서의 승격 확률 · 최대 층도 받지 않는다. 씨앗을 인자로 받으면 씨앗을 아는 호출자에게
 *   그 구현이 결정론적인 것과 같아져 위 넷째 줄의 입력이 그대로 걸린다. 확률 · 윗끝은 표현의 상수라 계약의 인자가 될 수 없다.
 * - 물려받은 `delete(key): void` 를 `boolean` 으로 바꿨다 — 「없는 키 지우기는 무시」가 반환값 없이는 관측되지 않는다(불변 사실 82 와
 *   같은 전면화). 물려받은 `search` 는 `has` 로 이름을 맞췄다.
 *
 * **검증 등급.** `complexity`.
 * 계약이 `tree/treap` 과 같으므로 등급도 같다 — 등급은 계약에서 기계적으로 따라 나온다(불변 사실 56). 판정 근거(배열은 가운데
 * 넣기가, 해시는 순서 연산이 상한을 넘고, 기댓값을 구현의 무작위성에 걸어 층 없는 정렬 목록이 자명한 구현으로 들어오지 못한다)도
 * 그 헤더와 같다.
 *
 * **필요충분조건.**
 * - 의미: 순서와 유일성 중 하나라도 버리면 이 구조가 아니다. 순서를 버리면 해시 집합이고(`hash/hashSet`), 유일성을 버리면
 *   다중집합이다(`tree/multiset`).
 * - 비용: 둘 다 지키더라도 `insert`·`delete` 의 **기댓값**이 n 에 비례하면 정렬 배열이나 층 없는 정렬 목록이다. 그리고 **어느 입력에도
 *   치우치지 않는다는 것이 이 계약의 전부다** — 층을 호출자가 되짚을 수 있게 정하는 순간 그 입력에서 목록으로 무너진다. 최악까지
 *   로그면 `tree/redBlackTree` 계약을 **함께** 지키는 것이고, 시퀀스 평균만 로그면 `tree/splayTree` 이며 그쪽과는 서로 담지 않는다.
 */
export class SkipList<T> {
  constructor(comparator?: (a: T, b: T) => number) {
    throw new Error("Not implemented");
  }

  insert(item: T): void {
    throw new Error("Not implemented");
  }

  delete(item: T): boolean {
    throw new Error("Not implemented");
  }

  has(item: T): boolean {
    throw new Error("Not implemented");
  }

  min(): T | null {
    throw new Error("Not implemented");
  }

  max(): T | null {
    throw new Error("Not implemented");
  }

  range(low: T, high: T): T[] {
    throw new Error("Not implemented");
  }

  size(): number {
    throw new Error("Not implemented");
  }

  toArray(): T[] {
    throw new Error("Not implemented");
  }
}
