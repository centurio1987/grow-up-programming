/**
 * PairingHeap — 넣기와 합치기를 호출 하나하나 상수에 두고, 빼기는 여러 호출에 걸쳐 원소 수의 로그로 갚는 우선순위 큐.
 *
 * **목적.** 합칠 수 있는 우선순위 큐(`heap/leftistHeap`)와 **같은 여섯** — 담기·최우선 원소
 * 꺼내기·두 큐를 하나로 접기·보기·크기·빔 — 을 제공하되, 비용을 다르게 나눠 약속하는 것.
 * 넣기와 합치기는 호출 하나하나가 상수이고(`worst`), 빼기는 **호출 하나가 아니라 n 회의
 * 평균**이 로그다(`amortized`). 그래서 넣기·합치기가 많고 빼기가 적은 호출열에서 약속하는
 * 총비용의 상한이 갈린다 — 넣기와 합치기를 합쳐 m 회, 빼기를 k 회 하면 이 계약은
 * $O(m + k\log n)$ 을, 합칠 수 있는 우선순위 큐는 $O((m + k)\log n)$ 을 약속한다.
 *
 * **대가는 빼기 한 호출이다.** 넣기만 n 번 한 뒤의 첫 빼기 하나가 담긴 수에 비례해도 이
 * 계약 안이다. 그 한 호출까지 로그로 묶는 계약이 `heap/leftistHeap` 이고, **두 계약은 서로
 * 담지 않는다**(불변 사실 54). 반례가 양쪽에 하나씩 실재한다 — 오른쪽 길로 합치는 구현은
 * 저쪽을 만족하고 이쪽을 만족하지 못하며(합치기가 로그다), 이 계약의 정본은 이쪽을 만족하고
 * 저쪽을 만족하지 못한다(빼기 한 호출이 담긴 수에 비례하는 상태가 있다). **축3은 그 갈림을
 * 한쪽에서만 본다** — 정본을 저쪽 스위트에 넣으면 빼기 두 시나리오에서 걸리고, 저쪽 정본은
 * 이쪽 스위트를 전부 통과한다. 상수와 로그의 차이가 축3의 해상도 아래이기 때문이다(불변
 * 사실 53). 하네스 자기시험이 두 방향을 다 고정한다.
 *
 * **합치기가 「넘겨받은 큐를 비운다」인 것은 `heap/leftistHeap` 과 같은 선택이고 이유도
 * 같다.** 두 큐를 다 살려 두는 쪽으로 적으면 넘겨받은 마디를 그대로 가져다 쓰는 계열이
 * 통째로 나가고, 「정하지 않는다」로 적으면 축1이 그 자리를 재지 못한다(관측되지 않는 약속은
 * 계약이 아니다).
 *
 * **이 계약에 없는 것 셋.**
 * - **담긴 원소의 우선순위 낮추기.** 원소를 가리키는 이름이 이 계약에 없다. 그것을 더한
 *   계약이 따로 있다(`heap/fibonacciHeap`).
 * - **빼기 한 호출의 상한.** 위 「대가」 문단이 그 자리다.
 * - **같은 우선순위 원소들 사이의 순서.** 비교자가 0 을 돌려주는 두 원소 중 어느 쪽이 먼저
 *   나오는지 약속하지 않는다. 합친 두 큐 사이에서도 마찬가지다.
 *
 * **이 계약은 시간만 말한다.** 저장 공간에 대한 조건이 없으므로, 원소 수보다 훨씬 많은
 * 자리를 쓰는 구현도 이 계약을 지킨다(§규약1 「공간은 어느 계약에도 없다」 — 미결).
 *
 * **정본 이름은 임시다**(불변 사실 64·198). `pairingHeap` 은 「형제를 짝지어 잇는다」는
 * **기법의 이름**이다. 이 계약이 말하는 것은 「넣기·합치기를 상수에 두는 우선순위 큐」이고
 * 그 이름의 디렉터리가 없다. 이름이 하나뿐이어도 구현 이름이면 임시로 적고 KAN-031(계약
 * 이름 재편)로 넘긴다.
 *
 * **불변식.** 없다.
 *
 * **판별 절차의 입력이 `heap/leftistHeap` 과 같아서 결과도 같다**(§규약1 「불변식 판별 절차」).
 * 두 계약의 연산 표는 의미 열이 글자 하나 다르지 않고, 불변식은 상태의 성질이라 비용 열이
 * 갈린 것이 판별에 들어오지 않는다. 후보 셋이 각각 다른 물음에서 걸린다.
 *
 * - **`peek()` 은 다음 `dequeue()` 가 돌려줄 원소와 같다** — 상태를 바꾸지 않고 최우선 원소를
 *   읽는 공개 연산이 `peek` 하나뿐이라 **첫 번째 물음에서 걸린다**(불변 사실 124).
 * - **`isEmpty()` 는 `size() === 0` 과 같다** — 경로가 둘이라 첫 물음은 지나고, 아래 표의
 *   `isEmpty` 행이 그 정합을 적고 있어 **두 번째 물음에서 걸린다.**
 * - **합친 뒤 이 큐의 크기는 두 크기의 합이고 넘겨받은 큐는 빈다** — `merge` 행이 그 정합을
 *   적고 있어 두 번째 물음에서 걸린다.
 *
 * 담는 모양의 성질(뿌리가 자식보다 앞선다, 형제가 목록으로 이어진다)은 후보조차 아니다 —
 * 적는 순간 표현을 처방하고(불변 사실 36), 이 계약을 지키는 다른 구현에는 그런 말이 없다
 * (불변 사실 131). **상각 보장도 상태의 성질로 옮겨지지 않는다**(불변 사실 123). 「다음 빼기가
 * 싸게 끝날 만큼 모양이 고르다」는 매 연산 뒤 성립하지 않는다 — 넣기만 n 번 한 뒤의 상태가
 * 이 계약 안에서 그 반례다.
 *
 * **연산 계약.** n 은 담긴 원소 수다. **`merge` 행에서만 「합친 뒤의 원소 수」로 읽는다** —
 * 그 행의 상한이 상수라 값이 달라지지는 않지만, 축3의 사다리가 무엇을 오르는지를
 * `heap/leftistHeap` 과 같게 정해 두어야 두 계약의 정본을 서로의 스위트에 넣어 볼 수 있다.
 * **`amortized` 는 한 큐가 받는 호출열 전체에 대한 약속이다** — 빈 큐에서 시작한 어떤 호출열에서도
 * 호출 전부의 총비용이 행마다의 상한을 그 행의 호출 수만큼 더한 것의 상수배 안이고, `worst` 행은 그에 더해 호출마다 제 상한 안이다.
 *
 * | 연산 | 의미 | 상한 | 한정자 |
 * |---|---|---|---|
 * | `constructor(comparator)` | 비교자를 고정한다. 이후 교체할 수 없다 | O(1) | worst |
 * | `enqueue(item)` | `item` 을 담는다. 같은 원소를 여러 벌 담을 수 있고 넣은 수만큼 나온다 | O(1) | worst |
 * | `dequeue()` | 비교자가 가장 앞세우는 원소를 지우고 돌려준다. 비어 있으면 `null` 이고 상태가 바뀌지 않는다 | O(log n) | amortized |
 * | `merge(other)` | `other` 에 담긴 원소를 전부 이 큐로 옮기고 `other` 를 비운다. `other` 가 이 큐 자신이면 `TypeError` | O(1) | worst |
 * | `peek()` | 최우선 원소를 지우지 않고 돌려준다. 비어 있으면 `null` | O(1) | worst |
 * | `size()` | 담긴 원소 수 | O(1) | worst |
 * | `isEmpty()` | `size() === 0` 과 같다 | O(1) | worst |
 *
 * **물려받은 표면을 넷 고쳤다.** 연산 집합은 그대로 여섯이다.
 *
 * - `insert`/`extractMin` 을 `enqueue`/`dequeue` 로 맞췄다. `extractMin` 은 비교자가 정하는
 *   방향을 이름에 박아 둔 것이고, 같은 이름을 쓰는 이웃 계약 둘(`heap/priorityQueue`·
 *   `heap/leftistHeap`)과 표면을 맞춰야 정본을 서로의 스위트에 넣어 관계를 잴 수 있다.
 * - `merge` 의 반환을 없앴다. 물려받은 표면은 합친 새 큐를 돌려줘 두 원본 큐의 상태를
 *   말하지 않았다 — `heap/leftistHeap` 이 같은 자리를 같은 이유로 고쳤다.
 * - **스텁이 내보내던 마디 클래스(`PairingNode` — 첫 자식·다음 형제·부모)와 private 필드
 *   셋을 뺐다.** 담는 모양을 적은 것이라 계약이 아니고, 스텁 본문에 두면 산문보다 강한
 *   처방이 된다(불변 사실 20).
 * - 물려받은 문서가 「실제 벤치마크에서 피보나치 힙을 능가하는 경우가 많다」로 적은 비교를
 *   뺐다. 상수 배수와 기계에 대한 진술이라 이 계약을 지키는 모든 구현에 대해 참인 문장이
 *   아니다(불변 사실 38).
 *
 * **물려받은 상한은 이 표와 같지만 입력으로 쓰지 않았다**(불변 사실 65). 행마다 근거가 다르다.
 *
 * - **`dequeue` 의 `O(log n)` 은 하한이 지목한다.** 비교로만 순서를 아는 구현에서 n 개를 넣고
 *   n 번 빼면 나오는 것이 정렬 수열이고 그 하한이 $\Omega(n\log n)$ 이다. 넣기를 상수로
 *   적었으므로 넣기 n 회가 $O(n)$ 이고, 남는 $\Omega(n\log n)$ 을 빼기 n 회가 치러야 한다 —
 *   **빼기의 상각 상한을 로그보다 작게 적을 수 없다.** 하한이 시퀀스 전체의 값이고 한정자도
 *   `amortized` 라 호출 하나로 옮기는 단계가 없다(§규약1 「하한이 지목하는 상한은 두 걸음으로
 *   적는다」). 둘째 걸음 — **그 값을 내는 구현이 있다** — 을 정본이 축3에서 짚는다.
 * - **`enqueue`·`merge` 의 `O(1)` 은 목적이 지목한다.** $O(\log n)$ 으로 적으면
 *   `heap/leftistHeap` 을 만족하는 구현이 **전부** 이 계약도 만족하게 되어(그쪽은 같은 행을
 *   `worst O(log n)` 으로 지키고 `worst` 는 `amortized` 를 함의한다) 이 계약이 저쪽의 약한
 *   판이 된다 — 「넣기·합치기를 상수에」가 계약에서 사라지고 계약이 뜻을 잃는다. 더 강하게
 *   적을 자리는 없다.
 * - **`peek`·`size`·`isEmpty` 의 `O(1)`.** 목적이 지목하고 강하게 적는 대가가 없다 — 넣기와
 *   합치기가 이미 최우선 자리를 지나가므로 그 자리와 원소 수를 함께 들고 있을 수 있다.
 *
 * **한정자.** 셋은 줄 세워지지 않으므로(불변 사실 59) 행마다 정했고, 강한 쪽을 적은 행은
 * **그것이 추가로 배제하는 계열이 약한 쪽을 실제로 통과하는지 쟀다**(§규약1 「강한 한정자의
 * 근거는 추가로 배제되는 계열을 수치로 낸다」).
 *
 * - **`dequeue` 가 `amortized` 인 이유.** `worst` 로 적으면 미뤄 두었다 빼기 때 짝지어 접는
 *   계열이 나간다. 이 계약의 정본이 그 계열이고, 크기를 n 으로 유지하며 번갈아 부르는 같은
 *   입력에서 단일 호출 최대 비용이 1,445 · 3,018 · 15,129 로 걸리고 호출 평균은 19.36 ·
 *   22.47 · 25.62 로 통과한다. 그 계열을 배제하면 넣기·합치기를 상수에 두는 가장 단순한
 *   길이 나가는데, 목적은 호출열의 총비용과 넣기·합치기의 호출 자리를 말하지 빼기 한 호출을
 *   말하지 않는다. 빼기 한 호출까지 약속하는 구현이 문헌에 있다는 것은 감추지 않는다(Brodal 의
 *   최악 효율 우선순위 큐) — 그런 구현도 이 계약을 지킨다(이 카드는 짓거나 재지 않았다).
 *   `expected` 는 `amortized` 와 서로를 함의하지 않는 다른 계약이고 무작위를 뽑는 구현을
 *   요구하게 되므로 적지 않는다.
 * - **`enqueue`·`merge` 가 `worst` 인 이유.** `amortized` 로 적으면 **쌓아 두었다 이따금
 *   한꺼번에 잇는 계열**이 들어온다(`_contract/_fixtures/bufferedLinkingHeap.ts`). 오름차순
 *   넣기에서 단일 호출 최대가 1,025 · 4,097 · 16,385 로 걸리고, 같은 입력의 호출 평균은 3.00
 *   고정으로 통과한다 — `worst` 가 추가로 배제하는 것이 이 계열이다. **배제해도 계약이 잃는
 *   것이 없다**(불변 사실 194 — 그 계열이 계약에 필요하지 않다). 그 계열이 이따금 하는 일을
 *   빼기로 미룬 구현이 실재하고(정본) 나머지 행을 그대로 지키므로, 그 계열을 살려야만 서는
 *   행이 없다. 그리고 목적이 「호출 자리에서 싸다」를 말하므로 `amortized` 는 그 문장을 지운다.
 *   같은 fixture 가 합치기에서도 쌓인 줄을 먼저 이어 단일 호출이 520 · 2,056 · 8,200 으로
 *   걸린다. **합치기 쪽의 상각 대조는 없다** — 합치기 시나리오가 호출 셋만 재는데 상각은 n 회
 *   측정을 요구한다(§규약2 시나리오 규칙 4).
 * - **`peek`·`size`·`isEmpty` 가 `worst` 인 이유.** `worst` 가 배제하는 것은 첫 호출에 값을
 *   만들어 두는 계열인데, 그 값을 처음부터 들고 있는 데 드는 대가가 없다.
 * - `constructor` 만 축3에서 면제된다. n 에 대해 반복 호출되는 연산이 아니다(§규약2 축3 면제).
 *
 * **주입 정책.**
 * - `comparator(a, b)` 는 음수·0·양수를 돌려주는 전순서여야 한다. 음수는 「`a` 가 먼저
 *   나온다」이고 0 은 「순서를 정하지 않는다」다. **기본 비교자를 두지 않는다** — 기본값을
 *   두면 그 방향이 계약의 일부가 된다. 비교자를 주지 않으면 `TypeError` 다.
 * - **`merge(other)` 의 `other` 는 같은 구현으로 만든, 같은 비교자를 든 큐다.** 다른 구현의
 *   큐를 합치는 일을 약속하면 어떤 구현이든 상대의 담는 모양을 읽어야 하고 그 순간 계약이
 *   표현을 정한다. 둘 다 주입자의 의무이고 이 구조가 검사하지 않는다.
 * - **`other` 가 이 큐 자신이면 `TypeError` 다.** 「`other` 를 비운다」와 「`other` 의 원소를
 *   이 큐에 담는다」가 같은 객체에서 서로를 부정해 값으로 답할 자리가 없다(불변 사실 132).
 * - 같은 원소를 여러 벌 담을 수 있다. `enqueue` 도 `merge` 도 중복을 버리지 않는다.
 * - 빈 큐의 `peek`·`dequeue` 는 예외가 아니라 `null` 이다. 그 대가로 `T` 에 `null` 이 섞이면
 *   「비어 있음」과 「값이 `null` 인 원소」가 구분되지 않는다.
 * - 비교자가 호출마다 다른 답을 내면 계약이 성립하지 않는다. 그것도 주입자의 의무다.
 *
 * **검증 등급.** `complexity`.
 * 일곱 행을 **함께** 만족하는 자명한 구현이 없다. 원소를 순서 없이 이어 두고 최우선 자리를
 * 따로 들면 넣기·합치기·보기가 상수로 서지만, 빼기가 다음 최우선을 찾으려 남은 원소를 전부
 * 훑고 그 훑기가 **호출마다** 일어나 평균에서도 줄지 않는다. 늘 정렬해 두면 넣기가 원소 수에
 * 비례한다. 일곱이 함께 서려면 빼기가 밀린 일을 갚으면서 **다음 빼기들이 쓸 부분 순서를
 * 남겨 두는** 상각 설계가 들어와야 한다.
 *
 * **필요충분조건.**
 * - 의미: `heap/leftistHeap` 과 같다 — 기본 우선순위 큐의 다섯에 **두 큐를 하나로 접는 일**을
 *   더한 것. 접기를 버리면 `heap/priorityQueue` 이고, 담긴 원소의 우선순위를 낮추는 일을
 *   더하면 `heap/fibonacciHeap` 이다.
 * - 비용: **넣기와 합치기를 호출마다 상수에 두면서 빼기를 상각 로그에 두는 것.** 넣기나
 *   합치기가 로그를 써도 되면 합칠 수 있는 우선순위 큐의 약한 판이 되어 그쪽 구현이 전부
 *   들어오고, 빼기를 상각으로도 로그 안에 못 두면 순서 없이 이어 둔 목록과 같은 자리다.
 *   반대로 빼기 한 호출까지 로그로 묶으면 미뤄 두었다 접는 계열이 나가는 **더 좁은** 계약이다.
 */
export class PairingHeap<T> {
  constructor(comparator: (a: T, b: T) => number) {
    throw new Error("Not implemented");
  }

  enqueue(item: T): void {
    throw new Error("Not implemented");
  }

  dequeue(): T | null {
    throw new Error("Not implemented");
  }

  merge(other: PairingHeap<T>): void {
    throw new Error("Not implemented");
  }

  peek(): T | null {
    throw new Error("Not implemented");
  }

  size(): number {
    throw new Error("Not implemented");
  }

  isEmpty(): boolean {
    throw new Error("Not implemented");
  }
}
