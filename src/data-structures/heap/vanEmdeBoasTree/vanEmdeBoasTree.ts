/**
 * VanEmdeBoasTree — 0 이상 u 미만의 정수만 담는 집합에서, 넣기·지우기·찾기·이웃 찾기를 담긴 원소 수와 무관하게 우주 크기 u 의 로그의 로그에 두는 정렬 집합.
 *
 * **목적.** 생성자가 정한 우주 $[0, u)$ 의 정수를 한 벌씩 담고, 담기·지우기·찾기·양 끝 읽기와
 * **이웃 찾기**(주어진 수 바로 다음 키 · 바로 앞 키)를 제공하되, 그 비용이 **담긴 원소 수 n 에
 * 기대지 않고 우주 크기 u 에만 기대며 그 기댐이 $\log\log u$ 인** 것. 원소를 비교로만 견주는 정렬
 * 집합(`tree/redBlackTree`)은 이웃 찾기에 원소 수의 로그를 쓴다 — 비교 기반 탐색의 하한이다. 키가
 * 정수이고 범위가 미리 정해져 있으면 그 하한이 적용되지 않고, 이 계약의 자리가 거기다.
 *
 * **우주 크기 u 의 모형.**
 * - 생성자가 u 를 받고 구조가 사는 동안 바뀌지 않는다. u 는 1 이상의 안전한 정수
 *   (`Number.isSafeInteger`)여야 한다.
 * - 키는 0 이상 u 미만의 정수다. 우주 밖의 키를 받는 연산은 던지고 상태를 바꾸지 않는다(주입 정책).
 * - **u 가 2의 거듭제곱일 필요는 없다.** 요구하면 비트를 반씩 나누는 구현의 편의가 호출자의 의무가
 *   된다 — 표현의 처방이다(불변 사실 4). 위로 올려 담는 일은 구현이 한다.
 * - **상한은 u 를 끝없이 키울 수 있는 파라미터로 읽는다.** 안전한 정수의 끝($2^{53}-1$)은 수의
 *   표현이 정한 것이고 상한의 상수로 접지 않는다 — 원소 수에 배열 길이의 끝이 있어도 $O(n)$ 을
 *   $O(1)$ 로 접지 않는 것과 같다. 접으면 u 에 비례하는 구현도 「상수」가 되어 계약이 뜻을 잃는다.
 *
 * **이웃 계약과의 관계 — 셋 다 서로 담지 않는다**(불변 사실 54). 반례가 양쪽에 하나씩 있다.
 * - **정렬 집합(`tree/redBlackTree` — 전 연산 `worst O(log n)`).** 그쪽은 비교자로 아무 원소나
 *   담고 구간 읽기·크기·전체 열거를 가진다. 이쪽 정본은 정수만 받고 그 연산이 없다. 반대로 그쪽
 *   계약을 지키는 균형 트리를 $[0, u)$ 의 정수에 쓰면, 원소 수가 u 에 가까울 때 이웃 찾기가
 *   $\log u$ 에 비례해 이쪽 상한 안에 들지 못한다. **그 차이는 로그 인수라 지금 사다리의 축3이 보지 못한다**
 *   (불변 사실 53) — 크기 순 배열에 이분 탐색하는 실측용 사본이 이쪽 조회 시나리오를 33 · 39 · 45
 *   로 통과했다.
 * - **우선순위 큐(`heap/priorityQueue` 부터 `heap/fibonacciHeap` 까지의 계약 넷).** 그쪽은 같은
 *   우선순위의 원소를 여러 벌 담고 넣은 수만큼 내놓는다. 이쪽은 같은 키를 한 벌만 담으므로 같은
 *   키를 두 번 넣는 호출열에서 이쪽 정본은 그쪽을 못 지킨다. 반대로 그쪽 정본에는 이웃 찾기와
 *   임의 키 지우기가 없다. **`min()` 뒤 `delete` 로 최소를 꺼낼 수 있다는 것은 포섭의 근거가
 *   아니다** — 연산이 유도된다는 것은 판정의 입력이 아니다(불변 사실 43).
 * - **해시 집합(`hash/hashSet`).** 그쪽은 아무 원소나 담고 순서를 약속하지 않는다. 이쪽 정본은
 *   정수 밖의 원소를 못 받고, 그쪽 정본은 이웃 찾기가 없다.
 *
 * **이 계약에 없는 것 넷.**
 * - **원소 수 읽기·전체 열거·구간 읽기.** 원칙 A 로 빠진다(`docs/ORD-006-conventions.md` 「원칙 A」 판정표 ① 49) — 셋 다
 *   `min` 과 `successor` 를 거듭 불러 조합되고(A2), 원소 수는 세어 두는 상수 보강으로 어느 설계든 지켜 떨어지는 계열이 없으며,
 *   열거 · 구간을 조합보다 싼 상한으로 따로 적으면 떨어지는 계열이 생기지만 목적(이웃 찾기)의 비용 조건이 그 배제를 요구하지
 *   않는다(A4). **위 「목적」이 적은 일(담기 · 지우기 · 찾기 · 양 끝 읽기 · 이웃 찾기) 어디에도 이 셋이 없다**(A5′).
 * - **같은 키 여러 벌과 키에 딸린 값.** 집합이다.
 * - **비교자.** 키는 수 자체로 쓰인다. 비교자를 받으면 비교로만 순서를 아는 구현의 하한이 돌아와
 *   목적이 선 자리가 사라진다.
 * - **공간.** 이 계약은 시간만 말한다. 우주 크기만큼 자리를 잡는 구현도, 담긴 수에 비례하게
 *   잡는 구현도 이 계약을 지킨다(§규약1 「공간은 어느 계약에도 없다」 — 미결).
 *
 * **정본 이름은 임시다**(불변 사실 64). `vanEmdeBoasTree` 는 이 계급을 처음 낸 설계의 **이름**이다.
 * 이 계약이 말하는 것은 「정수 우주 위의 정렬 집합」이고 그 이름의 디렉터리가 없다. **카테고리
 * `heap/` 도 이 계약과 맞지 않는다** — 이 계약은 우선순위 큐가 아니다. 둘 다 KAN-031(계약 이름
 * 재편)로 넘긴다.
 *
 * **불변식.** 넷이다. 넷 다 **관측 경로가 둘**이라 성립한다 — 우주 전체를 `has` 로 훑어 모은
 * 키의 수열이 한 경로이고, 각 항목의 연산이 다른 경로다. 구현이 두 경로를 따로 유지할 수 있고
 * 따로 유지하는 순간 갈린다. 어떤 연산 뒤에도 성립해야 한다.
 * 1. `min()` 은 `has(x)` 가 참인 가장 작은 `x` 이고, 그런 `x` 가 없으면 `null` 이다.
 * 2. `max()` 는 `has(x)` 가 참인 가장 큰 `x` 이고, 그런 `x` 가 없으면 `null` 이다.
 * 3. 우주 안의 모든 `x` 에 대해 `successor(x)` 는 `has(y)` 가 참이고 `y > x` 인 가장 작은 `y`
 *    이며, 없으면 `null` 이다.
 * 4. 우주 안의 모든 `x` 에 대해 `predecessor(x)` 는 `has(y)` 가 참이고 `y < x` 인 가장 큰 `y`
 *    이며, 없으면 `null` 이다.
 *
 * 유일함은 불변식이 아니다 — 담긴 벌 수를 읽는 연산이 없어 대조할 상대가 없고, 그 조건은 축1이
 * 참조 모델과 하는 일이다(§규약1 「불변식 판별 절차」 ①). 우주를 나누는 방식·요약의 정합 같은 담는
 * 모양의 성질은 후보조차 아니다 — 적는 순간 표현을 처방하고(불변 사실 36), 이 계약을 지키는 다른
 * 구현에는 그런 말이 없다(불변 사실 131). 넷은 `tree/redBlackTree` 의 불변식 셋째(양 끝을 읽는 길이
 * 둘)와 같은 판별에서 나왔다.
 *
 * **연산 계약.** u 는 생성자가 정한 우주 크기다. **상한에 담긴 원소 수 n 이 없다** — 그것이 이
 * 계약의 내용이다. $O(\log\log u)$ 는 u 의 함수이고 그 상수는 u 에 기대지 않는다. `worst` 는 호출
 * 하나하나가 제 상한 안이라는 뜻이다.
 *
 * | 연산 | 의미 | 상한 | 한정자 |
 * |---|---|---|---|
 * | `constructor(universe)` | 우주 크기를 `universe` 로 정하고 빈 집합으로 연다. `universe` 는 1 이상의 안전한 정수여야 하고, 아니면 `RangeError` | O(u) | worst |
 * | `insert(x)` | 키 `x` 를 담는다. 이미 담겨 있으면 **상태가 바뀌지 않는다** | O(log log u) | worst |
 * | `delete(x)` | 키 `x` 를 지우고 `true`. 담겨 있지 않으면 `false` 이고 상태가 바뀌지 않는다 | O(log log u) | worst |
 * | `has(x)` | 키 `x` 가 담겨 있으면 `true` | O(log log u) | worst |
 * | `min()` | 담긴 가장 작은 키. 비어 있으면 `null` | O(1) | worst |
 * | `max()` | 담긴 가장 큰 키. 비어 있으면 `null` | O(1) | worst |
 * | `successor(x)` | `x` 보다 큰 담긴 키 중 가장 작은 것. 없으면 `null`. `x` 자신이 담겨 있는지와 무관하다 | O(log log u) | worst |
 * | `predecessor(x)` | `x` 보다 작은 담긴 키 중 가장 큰 것. 없으면 `null`. `x` 자신이 담겨 있는지와 무관하다 | O(log log u) | worst |
 *
 * **축3은 $O(\log\log u)$ 를 `O(1)` 로 판정한다**(`docs/ORD-006-conventions.md` 「상한이 우주의 로그
 * 로그인 계약은 Bound 를 늘리지 않는다」). 크기가 4배씩 오르는 사다리에서 $\log\log u$ 의 비율이
 * 1.06~1.08 이라 `O(1)` 의 허용 구간 안이고, 판정 규격에 새 상한을 그어도 그 구간이 `O(1)` 과 겹쳐
 * 아무것도 다르게 가르지 못한다. 그래서 **4 배 간격 사다리**가 이 표에서 판정하는 것은 「n 에 기대지
 * 않는다」와 「u 의 어떤 거듭제곱보다도 느리게 자란다」 둘이고, $O(\log u)$ 로 어기는 구현(비트 하나씩
 * 내려가는 트라이 — `_contract/_fixtures/bitTrieIntegerSet.ts`)은 그 다섯을 전부 통과한다 — **그 통과는
 * 계약을 지킨다는 뜻이 아니다**(불변 사실 62). 로그 인수는 사다리를 **비트 수**로 읽는 시나리오 둘이
 * 따로 잡는다. 닫힌 것과 남은 것은 아래 「검증 등급」의 「검사 공백」.
 *
 * **물려받은 표면을 넷 고쳤다.** 연산 집합은 그대로 일곱이다.
 *
 * - **스텁이 들고 있던 private 필드 다섯(우주 크기·최소·최대·요약·묶음 배열)을 뺐다.** 재귀로 우주를
 *   나누는 담는 모양을 적은 것이라 계약이 아니고, 스텁 본문에 두면 산문보다 강한 처방이 된다(불변
 *   사실 20). 헤더가 적던 재귀 구조 설명도 같은 이유로 뺐다.
 * - `delete(x): void` 를 `delete(x): boolean` 으로 바꿨다. 「없는 값의 삭제는 무시한다」가 물려받은
 *   문제 문서에만 있었다 — 반환값으로 적어야 축1이 그 자리를 직접 잰다. `tree/redBlackTree` 의 같은
 *   행과 표면이 같아진다.
 * - 생성자 인자의 「2의 거듭제곱 권장」을 뺐고 **우주 밖 키의 처분을 적었다.** 물려받은 표면은 둘 다
 *   말하지 않았다.
 * - 물려받은 문서가 적은 쓰이는 자리(주소 조회·최단 경로)와 「$u = 2^{32}$ 이면 $\log\log u \approx 5$
 *   라 매우 빠르다」를 뺐다. 쓰이는 자리와 상수에 대한 진술이라 이 계약을 지키는 모든 구현에 대해 참인
 *   문장이 아니다(불변 사실 38).
 *
 * **물려받은 상한은 생성자를 뺀 일곱 행에서 이 표와 같지만 입력으로 쓰지 않았다**(불변 사실 65).
 * 생성자 행은 물려받은 문서에 없었다. 상한은 고르는 값이고(불변 사실 48·51) 행마다 근거가 다르다.
 *
 * - **넣기·지우기·이웃 찾기의 $O(\log\log u)$ 는 정본이 서는 계급으로 골랐다.** 더 약하게 $O(\log u)$
 *   로 적으면 원소 수가 u 를 넘지 못하므로 비교 기반 균형 트리가 **전부** 들어온다($\log n \le \log u$)
 *   — 이 계약이 「정수만 받는 정렬 집합」이 되고 목적 첫 문단의 자리가 사라진다. 더 강하게 상수로
 *   적을 수 있는지는 이 카드가 판정하지 않았다(동적 이웃 찾기의 하한 결과가 문헌에 있다 — 확인 안
 *   함). 그래서 남은 후보 중 하나를 정본이 짚는다(불변 사실 51).
 * - **`has` 의 $O(\log\log u)$ 는 더 강하게 적지 않았다.** 우주만큼의 칸을 따로 들면 상수가 되지만,
 *   찾기를 상수에 두는 것을 목적이 말하지 않으므로 그 칸을 들지 않는 계열을 배제할 이유가 없다.
 * - **`min`·`max` 의 $O(1)$.** 넣기와 지우기가 양 끝을 고치는 자리를 이미 지나가므로 그 값을 함께 들
 *   수 있고, 지우기가 다음 양 끝을 찾는 일은 이웃 찾기 한 번이라 그 행의 상한 안이다. 강하게 적는
 *   대가가 없다.
 * - **생성자의 $O(u)$.** 이 계약의 정본은 우주를 담는 자리를 생성자에서 전부 세운다. 상수로 적으면
 *   그 계열이 나가고, 남는 계열은 자리를 필요할 때 잡는 일을 넣기 한 호출이 떠안거나(`worst` 를 못
 *   지킨다) 해시에 기대야 한다. 그 계열을 배제할 이유가 목적에 없다. n 에 대해 반복 호출되는 연산이
 *   아니라 축3에서 면제된다(§규약2 축3 면제).
 *
 * **한정자.** 셋은 줄 세워지지 않으므로(불변 사실 59) 강한 쪽(`worst`)을 적으려면 그것이 추가로
 * 배제하는 계열을 배제해도 계약이 잃는 것이 없어야 하고, 그 계열을 수치로 낸다(§규약1 「강한
 * 한정자의 근거는 추가로 배제되는 계열을 수치로 낸다」).
 *
 * - **`amortized` 에 견주어 추가로 배제되는 계열 — 넣기를 쌓아 두었다 다른 연산이 올 때 한꺼번에
 *   넣는 구현**(`_contract/_fixtures/flushingIntegerSet.ts`). 넣기 n 번 뒤 이웃 찾기 n 번을 전부 재는
 *   입력에서 단일 호출 최대가 18,832 · 75,368 · 301,520 으로 걸리고, 같은 실행의 호출 평균은
 *   15.06 · 15.08 · 15.08 로 통과한다(하네스 자기시험). **배제해도 계약이 잃는 것이 없다** — 이 계약의
 *   정본이 일곱 행을 전부 호출마다 지키므로 그 계열을 살려야만 서는 행이 없다.
 * - **`expected` 에 견주어 추가로 배제되는 계열 — 무작위 해시에 기대는 구현.** 그 계열의 이득은
 *   자리를 담긴 수에 비례하게 줄이는 것인데 이 계약은 공간을 말하지 않는다. 이 카드는 짓거나 재지
 *   않았다.
 * - `min`·`max` 는 상태를 바꾸지 않고 값을 읽는 연산이고, `worst` 가 배제하는 것은 읽을 때 값을 만들어
 *   두는 계열인데 그 값을 처음부터 들고 있는 대가가 없다(위 상한 근거).
 * - `constructor` 는 n 에 대해 반복 호출되지 않아 `worst` 와 `amortized` 가 다르게 배제하는 것이 없다.
 *
 * **주입 정책.**
 * - **키를 받는 다섯 연산(`insert`·`delete`·`has`·`successor`·`predecessor`)의 `x` 는 0 이상 u 미만의
 *   정수여야 한다.** 아니면(음수·u 이상·정수가 아닌 수) `RangeError` 이고 상태가 바뀌지 않는다.
 *   값으로 답하지 않는 것은 **반환 타입에 「우주 밖」을 담을 자리가 없어서다**(불변 사실 82·132).
 *   `has`·`delete` 의 `false` 는 「담겨 있지 않다」라는 답이고, `successor`·`predecessor` 의 `null` 은
 *   「그런 키가 없다」라는 답이다 — 거기에 「우주 밖」을 겹치면 한 값이 두 뜻을 진다.
 * - 생성자의 `universe` 가 1 이상의 안전한 정수가 아니면 `RangeError` 다.
 * - `successor(x)`·`predecessor(x)` 의 `x` 는 담겨 있지 않아도 된다.
 * - 빈 집합의 `min`·`max`·이웃 찾기는 예외가 아니라 `null` 이다. 키가 수이므로 `null` 이 키와 섞일
 *   자리가 없다.
 *
 * **검증 등급.** `complexity`.
 * 일곱 행을 **함께** 만족하는 자명한 구현이 없다. 우주만큼의 칸에 있고 없음을 적으면 넣기·찾기가
 * 상수이지만 이웃 찾기와 양 끝 지우기가 칸을 훑어 u 에 비례한다(`_contract/_fixtures/scanningBitSet.ts`).
 * 키마다 다음 키를 적은 표를 들면 이웃 찾기가 상수이지만 넣기·지우기가 틈의 칸을 전부 고쳐 u 에
 * 비례한다. 담긴 키를 크기 순 배열에 두면 이웃 찾기를 이분 탐색으로 해도 넣기·지우기가 원소 수에
 * 비례한다(`_contract/_fixtures/sortedArrayIntegerSet.ts` 는 자리까지 훑기도 한다). 객체·`Map` 같은
 * 해시는 순서를 잃어 이웃 찾기를 못 한다. 넷 어느 것도 이웃 찾기와 갱신을 **함께** 상한 안에 두지
 * 못한다 — 갱신이 고치는 범위와 이웃 찾기가 훑는 범위를 함께 u 의 로그 로그로 줄이는 설계가 들어와야
 * 하고, 그 설계가 자명하지 않다는 것이 이 등급의 내용이다.
 *
 * **검사 공백 — 로그 인수 계열 둘은 닫혔고, 로그보다 느린 계열은 남는다**(`S24` 가 열고 `KAN-042` 가 닫았다 · 검토 반려 2026-09-15).
 * 축3 이 판정하는 것은 여전히 `O(1)` 계급이고 계약은 $O(\log\log u)$ 라 **둘은 같은 보장이 아니다.** 4 배 간격 사다리에서 로그 인수는 비율을
 * 1.2 밖에 안 바꿔 `O(1)` 구간(0.70~1.30) 한복판에 앉기 때문이다(불변 사실 53 · 62). **막는 것은 판정 규격이 아니라 시나리오가 사다리를 읽는
 * 방식이다** — 파라미터를 **비트 수가 두 배씩** 오르게 읽으면 같은 규격 위에서 로그가 비율 2.0 으로 보인다. 그 읽기로 시나리오 둘을 스위트 끝에
 * 붙였고, 하네스(`_contract/runContract.ts` · `_contract/judge.ts`)는 고치지 않았다.
 *
 * **닫힌 것 — 로그 인수 계열 둘.** 지나가던 두 계열이 각각 제 사다리에서 걸린다. 둘은 서로의 사다리를 통과하므로 사다리가 둘이어야 한다.
 *
 * | 공백 | 지나가던 구현 | 4 배 사다리 다섯 | 닫은 시나리오 |
 * |---|---|---|---|
 * | u 쪽 — 이웃 찾기 · 갱신이 $O(\log u)$ | 비트 하나씩 내려가는 트라이(`_contract/_fixtures/bitTrieIntegerSet.ts`) | 전부 통과 — 우주를 키우는 둘이 73 · 87 · 101($r$ = 1.19 · 1.16) · 24 · 28 · 32(1.17 · 1.14) | **(가) 우주 비트 사다리** — 38 · 73 · 143(1.92 · 1.96) 실패, 정본 17 · 19 · 21(1.12 · 1.11) 통과 |
 * | n 쪽 — 모든 연산이 $O(\log n)$(비교로만 견주는 균형 탐색 트리) | 키의 곱셈 해시를 우선순위로 쓰는 결정론 트립(`_contract/_fixtures/hashPriorityTreapIntegerSet.ts` — 축1 · 축2 를 통과한다) | 전부 통과 — 넣기 8 · 9 · 11 · 지우기 14 · 16 · 20 · 조회 63 · 66 · 81 | **(나) 담긴 수 비트 사다리** — 15 · 39 · 66(2.60 · 1.69) 실패, 정본 30 · 30 · 30 통과 |
 *
 * **(가)의 bound 가 표의 $O(\log\log u)$ 와 다른 `O(log n)` 인 것은 표기의 어긋남이 아니라 사다리가 바뀐 결과다.** 그 시나리오에서 사다리를
 * 오르는 파라미터는 우주 크기가 아니라 **우주의 비트 수** $b$ 이고($u = 2^b$ · $b$ = 5 · 10 · 20), 계약의 $\log\log u$ 를 그 파라미터로 다시
 * 적으면 $\log b$ 다. 그래서 그 자리에서 계약이 요구하는 계급의 이름이 `O(log n)` 이 된다 — 같은 상한을 사다리의 말로 옮긴 것이다. 실제로
 * $\log\log u$ 의 기대 비율이 그 사다리에서 1.43 · 1.30 이라 `O(1)` 구간을 넘고, `O(log n)` 구간(0.84~1.56 · 0.82~1.52)이 그것을 담으면서
 * 로그(2.0)는 뱉는다. 판정이 서는 문장은 「u 의 로그보다 느리게 자란다」다(§규약2 「표의 상한과 시나리오의 bound 가 다르면 헤더가 그 판정을 적는다」).
 *
 * **남은 것 — 로그보다 느리고 상수보다 빠른 계열.** $\sqrt{\log u}$ 로 자라는 구현은 (가)의 사다리에서 비율이 $\sqrt 2$ = 1.41 이고 그것은
 * `O(log n)` 구간 안이라 **통과한다**(산술 — 짓거나 재지 않았다). 같은 자리에 $\log\log u \cdot \log\log\log u$ 처럼 로그 로그를 조금 웃도는
 * 계열도 있다. 이 공백을 닫으려면 사다리를 한 번 더 굽혀야 하고(비트 수의 비트 수), 그 사다리에서는 정본과 후보가 한두 걸음 차이라 ±30% 안에
 * 겹친다 — **이 계약이 닫은 것은 로그 인수 계열이지 로그 로그 위의 모든 계열이 아니다.** 4 배 사다리 다섯만으로는 로그 인수도 여전히 안 보이고,
 * 다섯의 통과를 「계약을 지킨다」로 읽지 않는 근거가 그대로 남는다(불변 사실 62).
 *
 * **버린 보완 후보 둘**(`S24` 가 쟀다). **(다) 허용치를 좁히기**(이 계약만 `O(1)` ±10%) — 4 배 사다리에서 트라이(1.19)와 트립(1.26)이 걸리지만
 * `_contract/judge.ts` 의 허용치를 계약마다 가르는 하네스 규격 변경이고, 로그 계열에 상수 몫이 크면 비율이 1 에 붙어 통과한다(산술).
 * **(라) `O(log log n)` 계급 신설** — 4 배 사다리에서는 `O(1)` 구간과 겹쳐 가르는 것이 없고(「연산 계약」 아래 문단), (가)의 사다리 위에서도
 * 새 구간의 위쪽 끝(1.86 · 1.69)이 `O(log n)` 구간(1.56 · 1.52)보다 넓어 더 거르는 것이 없다(산술).
 *
 * **필요충분조건.**
 * - 의미: 정수 키 · 생성자가 정한 우주 · 한 벌씩 담기 · 이웃 찾기. 이웃 찾기(순서)를 버리면 해시
 *   집합이고(`hash/hashSet`), 한 벌씩 담기를 버리면 같은 키를 여러 벌 담는 우선순위 큐 쪽이며, 정수
 *   우주를 버리고 비교자를 받으면 정렬 집합이다(`tree/redBlackTree`).
 * - 비용: **이웃 찾기와 갱신이 함께** 원소 수에 기대지 않고 u 의 로그 로그 안이어야 한다. 이웃 찾기만
 *   빠르면 다음 키 표이고, 갱신만 빠르면 칸 배열이다. 둘 다 원소 수의 로그를 쓰면 비교 기반 정렬
 *   집합이다 — **이 마지막 갈림은 로그 인수라 4 배 사다리의 축3이 보지 못하고**, 사다리를 비트 수로
 *   읽는 시나리오가 그 자리를 잡는다. 닫힌 것과 남은 것은 위 「검증 등급」의 「검사 공백」이 적는다.
 */
export class VanEmdeBoasTree {
  constructor(universe: number) {
    throw new Error("Not implemented");
  }

  insert(x: number): void {
    throw new Error("Not implemented");
  }

  delete(x: number): boolean {
    throw new Error("Not implemented");
  }

  has(x: number): boolean {
    throw new Error("Not implemented");
  }

  min(): number | null {
    throw new Error("Not implemented");
  }

  max(): number | null {
    throw new Error("Not implemented");
  }

  successor(x: number): number | null {
    throw new Error("Not implemented");
  }

  predecessor(x: number): number | null {
    throw new Error("Not implemented");
  }
}
