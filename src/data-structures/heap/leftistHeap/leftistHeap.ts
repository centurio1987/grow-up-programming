/**
 * LeftistHeap — 두 큐를 하나로 접는 일까지 원소 수의 로그 안에 두는 우선순위 큐.
 *
 * **목적.** 기본 우선순위 큐(`heap/priorityQueue`)가 약속하는 다섯 — 담기·최우선 원소
 * 꺼내기·보기·크기·빔 — 에 **두 큐를 하나로 접는 일**을 더하고, 그 접기까지 원소 수의 로그
 * 안에 두는 것. 갱신 셋은 호출 하나하나가 로그 안이고(`worst`) 조회 셋은 상수다. 더한 것이
 * 연산 하나뿐인데도 계약이 갈리는 이유는 **그 하나가 배제하는 구현 집합이 크기 때문**이다 —
 * 원소를 배열 한 줄에 접어 담는 계열은 다섯 행을 전부 지키면서 이 행 하나에서 나간다
 * (배열 둘을 합치면 원소 수에 비례한다).
 *
 * **합치기가 「넘겨받은 큐를 비운다」인 것이 두 번째 내용이다.** 두 큐를 다 살려 두는 쪽으로
 * 적을 수도 있었다. 그렇게 적으면 마디를 함께 가리키거나 길을 복사하는 계열만 남고, 넘겨받은
 * 마디를 그대로 가져다 쓰는 계열이 통째로 나간다 — 배제할 이유가 목적에 없다. 반대로 이렇게
 * 적으면 다 살려 두는 구현도 들어온다(합친 뒤 상대를 비우는 데 드는 대가가 없다). **약한
 * 쪽이 이쪽이다.**
 *
 * **정하지 않는 쪽은 고를 수 없었다.** 「합친 뒤 `other` 의 상태는 정하지 않는다」로 적으면
 * 그 자리를 축1이 재지 못한다 — 참조 모델이 무엇을 기대해야 할지 정해지지 않으므로 어떤
 * 구현도 대조받지 않는다. 관측되지 않는 약속은 계약이 아니다(불변 사실 69 ③이 동시성에서
 * 낸 문장이 여기서도 그대로다).
 *
 * **이 계약에 없는 것 셋.**
 * - **담긴 원소의 우선순위 낮추기.** 이미 담긴 원소를 가리켜 그 우선순위를 올리는 연산은
 *   「그 원소가 지금 어디 있는가」를 계약이 말할 수 있어야 성립하는데, 이 계약에는 원소를
 *   가리키는 이름이 없다. 그것은 다른 계약이다(`heap/fibonacciHeap`).
 * - **넣기·합치기를 상수에 두는 것.** 그것을 요구하는 계약이 따로 있고(`heap/pairingHeap`),
 *   그 계약은 대신 빼기를 상각으로 내린다. 아래 「한정자」가 그 갈림을 적는다.
 * - **같은 우선순위 원소들 사이의 순서.** 비교자가 0 을 돌려주는 두 원소 중 어느 쪽이 먼저
 *   나오는지 약속하지 않는다. 합친 두 큐 사이에서도 마찬가지다 — 약속하면 어느 큐에서 왔는지
 *   또는 넣은 순서를 함께 들고 있어야 하고, 그 자리를 두지 않는 구현이 전부 나간다.
 *
 * **이 계약은 시간만 말한다.** 저장 공간에 대한 조건이 없으므로, 원소 수보다 훨씬 많은
 * 자리를 쓰는 구현도 이 계약을 지킨다(§규약1 「공간은 어느 계약에도 없다」 — 미결).
 *
 * **정본 이름은 임시다**(불변 사실 64). 이 계약의 ADT 이름은 「합칠 수 있는 우선순위 큐」인데
 * 그 이름의 디렉터리가 없고, `leftistHeap` 도 `heap/binomialHeap` 도 담는 모양을 가리키는
 * **구현 이름**이다. 물려받은 표면이 이 계약과 어긋난 자리가 적은 쪽을 골라 임시로 세웠고,
 * 이름 자체는 KAN-031(계약 이름 재편)이 정한다. `heap/priorityQueue` 가 임시가 아닌 것과
 * 다른 자리다 — 그 덩어리에는 우선순위 큐라는 ADT 이름이 있었다(불변 사실 66).
 *
 * **이름이 계약을 정하지 않는다.** `heap/binomialHeap` 이 이 계약과 **같은 계약**을 적고 같은
 * 스위트를 돈다. 오른쪽 길을 짧게 유지하는 것과 나무를 차수별로 세워 두는 것은 담는 모양의
 * 차이이고, 계약은 담는 모양을 관측하지 못한다(§규약1 「힙 여덟의 판정」).
 *
 * **불변식.** 없다.
 *
 * **연산이 여섯인데도 비는 것은 판별 절차를 돌린 결과다**(§규약1 「불변식 판별 절차」).
 * 후보 셋이 각각 다른 물음에서 걸린다.
 *
 * - **`peek()` 은 다음 `dequeue()` 가 돌려줄 원소와 같다** — 「최우선 원소가 무엇인가」를
 *   **상태를 바꾸지 않고** 읽는 공개 연산이 `peek` 하나뿐이므로 **첫 번째 물음에서 걸린다.**
 *   `dequeue` 도 그 값을 내놓지만 그것은 관측이 아니라 상태를 바꾸는 연산이다(불변 사실 124).
 * - **`isEmpty()` 는 `size() === 0` 과 같다** — 원소 수를 읽는 경로가 둘이라 첫 물음은
 *   지난다. **두 번째 물음에서 걸린다**: 아래 표의 `isEmpty` 행이 그 정합을 이미 적고 있다.
 * - **합친 뒤 이 큐의 크기는 두 크기의 합이고 넘겨받은 큐는 빈다** — 관측 경로가 둘인 것을
 *   넘어 **객체가 둘**이지만, 그 정합을 `merge` 행이 이미 적고 있으므로 두 번째 물음에서
 *   걸린다. 그 조건은 `merge` 의 의미이고 축1이 호출 자리에서 대조한다.
 *
 * 담는 모양의 성질(오른쪽 길이 짧다, 자리마다 나무가 하나 이하다)은 후보조차 아니다.
 * 계약에 적어야 검사할 수 있는데 **적는 순간 그것이 표현을 처방한다**(불변 사실 36).
 * 그리고 이 계약에서는 그보다 앞선 자리에서 걸린다 — **후보를 계약의 말로 적을 수가 없다.**
 * 「오른쪽 길」은 정본의 담는 모양에만 있는 말이고 나무를 차수별로 세우는 구현
 * (`heap/binomialHeap`)에는 그런 것이 아예 없다. 불변 사실 123 이 「그 성질이 매 연산 뒤
 * 성립하기는 하는가를 먼저 보라」고 한 물음이 여기서는 **어느 구현에 대해 묻는가**로
 * 되돌아온다. 담는 모양이 어긋난 구현은 답이 아니라 걸음 수가 틀리므로 축3이 잡는다.
 *
 * **연산 계약.** n 은 담긴 원소 수다. **`merge` 행에서만 「합친 뒤의 원소 수」로 읽는다** —
 * 두 큐의 크기가 갈리는데 상한 값은 갈리지 않으므로, 계약이 어느 쪽 n 인지 정해 두지 않으면
 * 같은 `O(log n)` 이 두 가지를 뜻하고 축3의 사다리가 무엇을 오르는지 정해지지 않는다
 * (불변 사실 185 와 같은 물음이다).
 *
 * | 연산 | 의미 | 상한 | 한정자 |
 * |---|---|---|---|
 * | `constructor(comparator)` | 비교자를 고정한다. 이후 교체할 수 없다 | O(1) | worst |
 * | `enqueue(item)` | `item` 을 담는다. 같은 원소를 여러 벌 담을 수 있고 넣은 수만큼 나온다 | O(log n) | worst |
 * | `dequeue()` | 비교자가 가장 앞세우는 원소를 지우고 돌려준다. 비어 있으면 `null` 이고 상태가 바뀌지 않는다 | O(log n) | worst |
 * | `merge(other)` | `other` 에 담긴 원소를 전부 이 큐로 옮기고 `other` 를 비운다. `other` 가 이 큐 자신이면 `TypeError` | O(log n) | worst |
 * | `peek()` | 최우선 원소를 지우지 않고 돌려준다. 비어 있으면 `null` | O(1) | worst |
 * | `size()` | 담긴 원소 수 | O(1) | worst |
 * | `isEmpty()` | `size() === 0` 과 같다 | O(1) | worst |
 *
 * **물려받은 표면을 셋 고쳤다.** 연산 집합은 그대로 여섯이다(뺄 것도 더할 것도 없다 —
 * 필요충분조건의 비용 조건이 *"못 지키면 무엇이 되는가"* 로 끝난다).
 *
 * - `insert`/`extractMin` 을 `enqueue`/`dequeue` 로 맞췄다. `extractMin` 은 **비교자가 정하는
 *   방향을 이름에 박아 둔 것**이고, 뒤집어 주입하면 그 연산이 최대를 내놓는다 —
 *   `minHeap`·`maxHeap` 이 한 계약인 것과 같은 자리다. 담는 쪽 계약(`heap/priorityQueue`)과
 *   같은 이름을 쓰므로 이 계약의 정본을 저쪽 스위트에 그대로 넣어 포섭을 잴 수 있다.
 * - `merge` 의 반환을 없앴다. 물려받은 표면은 **합친 새 큐를 돌려준다**로 적고 제약에
 *   *"파괴적 병합도 허용한다"* 를 함께 적었는데, 그러면 두 원본 큐의 상태를 계약이 말하지
 *   않으므로 어느 구현도 그 자리에서 대조받지 않는다.
 * - `heap/binomialHeap` 이 적어 둔 `peek` 의 $O(\log n)$ 을 `O(1)` 로 내렸다. 그 값은 뿌리
 *   목록을 훑는 구현을 그대로 옮겨 적은 것이고, 최우선 자리를 따로 드는 데 드는 대가가
 *   갱신의 상한을 바꾸지 않는다 — **`O(1)` 로 적어도 배제되는 구현 계열이 없다**(불변 사실 65).
 *
 * **상한을 `O(log n)` 으로 적은 근거는 행마다 다르다.**
 *
 * - **`enqueue`·`dequeue`.** 담는 쪽 계약이 그 값을 하한에서 얻었고 여기서도 그대로다 —
 *   비교로만 순서를 아는 구현에서 n 개를 넣고 n 번 빼면 나오는 것이 정렬 수열이고, 비교 기반
 *   정렬의 하한이 $\Omega(n\log n)$ 이라 **갱신 둘을 함께 상수로 적을 수 없다.** 하한이
 *   아래를 막고 정본이 그 자리를 짚는다(불변 사실 120).
 * - **`merge`.** 이 행에는 그런 하한이 없다 — 합치기를 **상수**에 하는 구현이 실재하고
 *   (`heap/pairingHeap`), 그것이 C 계약의 내용이다. 그래서 상한을 고르는 일이 불변 사실 51 의
 *   모양이 된다. 두 물음의 답이 이렇다. ① **더 약하게 $O(n)$ 으로 적으면** 배열 한 줄에 접어
 *   담는 계열이 들어온다 — 두 줄을 이어 붙인 뒤 한 번에 다시 쌓으면 원소 수에 비례한다.
 *   그러면 담는 쪽 계약을 만족하는 구현이 **전부** 이 계약도 만족하게 되어 배제되는 것이
 *   없어지고, 이 계약을 따로 둘 이유를 계약 자신이 말하지 못한다 — 계약이 뜻을 잃는다.
 *   ② **더 강하게 $O(1)$ 로 적으면** 나무를 합치는 계열이 통째로 나간다. 그 계열을 배제할
 *   이유가 목적에 없고, 상수 합치기를 요구하는 계약은 따로 있다. 그러면 남는 후보 중
 *   **정본이 서는 계급**으로 정한다.
 * - **`peek`·`size`·`isEmpty` 의 `O(1)`.** 목적이 지목한다. $O(\log n)$ 으로 적으면 정렬
 *   집합(`tree/redBlackTree`)이 이 계약의 조회 셋을 만족하게 되고, 그러면 「보기를 상수에」가
 *   계약에서 사라진다. 강하게 적는 대가는 없다 — 갱신 셋이 이미 로그를 쓰며 최우선 자리를
 *   지나가므로 그 자리를 들고 있는 데 드는 비용이 상한을 바꾸지 않는다.
 *
 * **한정자.** 여섯 행이 전부 `worst` 다.
 *
 * - **`enqueue`·`dequeue`·`merge` 가 `worst` 인 이유.** `amortized` 로 적으면 들어오는 계열이
 *   있다 — 넣기와 합치기를 미뤄 두었다가 **빼기 때 한꺼번에 짝지어 접는** 설계다. 담는 쪽
 *   계약(`heap/priorityQueue`)은 그 계열을 일부러 안에 두려고 `amortized` 를 적었고, 이
 *   계약은 배제한다. **배제해도 잃는 것이 없는 이유**가 목적에 있다: 그 계열은 넣기와
 *   합치기를 상수에 하므로 이 계약의 갱신 셋 중 둘에서 상한보다 빠르고, **그 빠름을 약속하는
 *   계약이 따로 있다.** 즉 배제되는 계열은 이 계약이 담아 줘야만 자기 자리를 갖는 계열이
 *   아니다. 반대로 `amortized` 로 적으면 **합친 직후의 첫 빼기 하나가 합친 크기에 비례해도**
 *   이 계약 안이고, 그러면 「합치기가 싸다」가 호출 자리에서 말하는 바가 없어진다 — 싸게
 *   합쳐 놓고 그 값을 다음 빼기에서 치르기 때문이다. 이 계약이 `worst` 로 사는 자리가 거기다.
 * - **`peek`·`size`·`isEmpty` 가 `worst` 인 이유.** 강한 쪽을 적으려면 그것이 배제하는 계열을
 *   배제해도 계약이 잃는 것이 없어야 한다(§규약1 「가장 약한 한정자는 셋을 줄 세우지 않는다」).
 *   `worst` 가 배제하는 것은 **첫 호출에 값을 만들어 두는 계열**인데, 그 값을 처음부터 들고
 *   있는 데 드는 대가가 없다 — 갱신 셋이 이미 로그를 쓰고 있고 그 안에서 최우선 자리와 원소
 *   수를 함께 고칠 수 있다.
 * - `constructor` 만 축3에서 면제된다. n 에 대해 반복 호출되는 연산이 아니라 성장률을 잴
 *   대상이 아니다(§규약2 축3 면제).
 *
 * **주입 정책.**
 * - `comparator(a, b)` 는 음수·0·양수를 돌려주는 전순서여야 한다. 음수는 「`a` 가 먼저
 *   나온다」이고 0 은 「순서를 정하지 않는다」다. **기본 비교자를 두지 않는다** — 기본값을
 *   두면 그 방향이 계약의 일부가 되고, 「최소부터」와 「최대부터」가 서로 다른 계약이 된다.
 *   비교자를 주지 않으면 `TypeError` 다.
 * - **`merge(other)` 의 `other` 는 같은 구현으로 만든, 같은 비교자를 든 큐다.** 다른 구현의
 *   큐를 합치는 일을 계약이 약속하지 않는다 — 약속하면 어떤 구현이든 상대의 담는 모양을 읽을
 *   수 있어야 하고, 그 순간 계약이 표현을 정한다. 두 큐의 비교자가 다르면 「최우선」이 두
 *   가지를 뜻하므로 계약이 성립하지 않는다. 둘 다 주입자의 의무이고 이 구조가 검사하지 않는다.
 * - **`other` 가 이 큐 자신이면 `TypeError` 다.** 값으로 답하지 않는 이유는 답이 없기
 *   때문이다 — 「`other` 를 비운다」와 「`other` 의 원소를 이 큐에 담는다」가 같은 객체에서
 *   서로를 부정한다. 반환 타입에 「없음」을 담을 자리가 있는 연산(`peek`·`dequeue`)에서
 *   값으로 답한 것과 반대쪽 선택이다(불변 사실 82).
 * - 같은 원소를 여러 벌 담을 수 있다. 집합이 아니므로 `enqueue` 도 `merge` 도 중복을 버리지
 *   않고, 담은 수만큼 `dequeue` 로 나온다.
 * - 빈 큐의 `peek`·`dequeue` 는 예외가 아니라 `null` 이다. 그 대가로 `T` 에 `null` 이 섞이면
 *   「비어 있음」과 「값이 `null` 인 원소」가 구분되지 않는다.
 * - 비교자가 호출마다 다른 답을 내면 계약이 성립하지 않는다. 그것도 주입자의 의무다.
 *
 * **검증 등급.** `complexity`.
 * 일곱 행을 **함께** 만족하는 자명한 구현이 없다. 담는 쪽 계약에서 이미 그랬고
 * (순서 없이 쌓으면 빼기가, 늘 정렬해 두면 넣기가 원소 수에 비례한다) `merge` 행이 그 위에
 * 하나를 더한다 — **부분적으로만 정렬해 두는 설계를 넣어 갱신 둘을 로그에 맞춰도, 그 부분
 * 순서를 배열 한 줄에 접어 담으면 합치기가 원소 수에 비례한다.** 자리를 마디로 나눠 들고
 * 두 모음의 경계만 이어 붙이는 설계가 들어와야 일곱이 함께 선다.
 *
 * **필요충분조건.**
 * - 의미: 담는 쪽 계약의 다섯에 **두 큐를 하나로 접는 일**이 더해진 것. 접기를 버리면 기본
 *   우선순위 큐이고(`heap/priorityQueue`), 담긴 원소의 우선순위를 낮추는 일을 더하면
 *   `heap/fibonacciHeap` 이다. 접기가 「넘겨받은 큐를 비운다」를 잃으면 두 큐가 같은 원소를
 *   함께 세는 상태가 계약 안에 들어와, 합친 뒤의 크기가 무엇인지 계약이 말하지 못한다.
 * - 비용: **합치기가 원소 수에 비례하면 배열 한 줄이다** — 그 구현이 나머지 여섯을 전부
 *   지키므로 이 계약의 자리를 정하는 것은 합치기 행 하나다. 그리고 **일곱을 다 지키더라도
 *   갱신 셋을 상각으로만 지키면** 미뤄 두었다 접는 계열이고, 그쪽은 넣기·합치기를 상수에 두는
 *   대신 빼기 한 호출이 담긴 수에 비례할 수 있다(`heap/pairingHeap`).
 */
export class LeftistHeap<T> {
  constructor(comparator: (a: T, b: T) => number) {
    throw new Error("Not implemented");
  }

  enqueue(item: T): void {
    throw new Error("Not implemented");
  }

  dequeue(): T | null {
    throw new Error("Not implemented");
  }

  merge(other: LeftistHeap<T>): void {
    throw new Error("Not implemented");
  }

  peek(): T | null {
    throw new Error("Not implemented");
  }

  size(): number {
    throw new Error("Not implemented");
  }

  isEmpty(): boolean {
    throw new Error("Not implemented");
  }
}
