/**
 * PriorityQueue — 주입된 비교자가 앞세우는 원소 하나만 꺼낼 수 있는 모음.
 *
 * **목적.** 원소를 담는 일·비교자가 가장 앞세우는 원소를 꺼내는 일을 원소 수의 **상각**
 * 로그 비용으로, 그 원소를 꺼내지 않고 보는 일과 담긴 수를 읽는 일을 **최악** 상수 비용으로
 * 함께 제공하는 것. 꺼낼 수 있는 것이 **맨 앞 하나뿐**이라는 것은 부족함이 아니라 계약의
 * 내용이다 — 임의 원소를 찾거나 지우는 일을 약속하지 않기 때문에 전순서를 통째로 들고
 * 있지 않아도 되고, 그 자유가 이 계약이 정렬 집합(`tree/redBlackTree`)과 갈리는 자리다.
 *
 * **보기가 상수라는 것이 두 번째 내용이다.** 정렬 집합도 최솟값을 돌려주지만 그 상한은
 * 로그이고, 이 계약은 상수를 요구한다. 그래서 **정렬 집합 계약을 만족하면서 이 계약을
 * 만족하지 못하는 구현이 있다** — 최솟값 자리를 따로 들지 않고 그때그때 왼쪽 끝까지
 * 내려가는 균형 트리가 그것이다. 반대 방향의 반례도 있으므로(순서 없이 담는 힙은 `has` 를
 * 상한 안에 못 한다) 두 계약은 **서로를 담지 않는다**(불변 사실 54).
 *
 * **「균형 탐색 트리는 이 계약을 못 지킨다」로 적지 않는다.** 가장 왼쪽 마디를 따로 들고
 * 있는 균형 트리는 네 행을 전부 지킨다 — 구현의 종류를 지목하는 문장은 그 계약을
 * 만족하는 모든 구현에 대해 참이 아니고, 그러면 계약의 문장이 아니다(불변 사실 38).
 * B14 외부 검토가 잡았던 자리와 같은 모양이고, T2-01 의 외부 검토가 여기서 잡았다.
 *
 * **이 계약에 없는 것 넷.**
 * - **합치기.** 두 큐를 하나로 접는 연산을 넣으면 계약이 배제하는 구현 집합이 통째로
 *   달라진다 — 배열 한 줄에 접어 담는 구현이 그 자리에서 나간다(배열 둘을 합치면 원소
 *   수에 비례한다). 그것은 다른 계약이다(`heap/leftistHeap`).
 * - **담긴 원소의 우선순위 낮추기.** 이미 담긴 원소를 가리켜 그 우선순위를 올리는 연산은
 *   「그 원소가 지금 어디 있는가」를 계약이 말할 수 있어야 성립하는데, 이 계약에는 원소를
 *   가리키는 이름이 없다. 그것도 다른 계약이다(`heap/fibonacciHeap`).
 * - **같은 우선순위 원소들 사이의 순서.** 비교자가 0 을 돌려주는 두 원소 중 어느 쪽이 먼저
 *   나오는지 계약이 약속하지 않는다. 약속하면 넣은 순서를 함께 들고 있어야 하고, 그 자리를
 *   두지 않는 구현이 전부 나간다.
 * - **비었는지 묻기.** 물려받은 표면의 `isEmpty()` 를 뺐다.
 *
 * **뺀 행의 판정은 목적 항목 밖에 적는다.** `isEmpty()` 는 원칙 A 판정표 ③ 의 판정으로 뺐다 —
 * 판정 줄과 목적 인용은 `docs/ORD-006-conventions.md` 「판정표 ③ 의 103 행을 묶어 판정했다」
 * 절에, 행 단위 대장은 `tools/_baseline/principle-a-verdicts.tsv` 에 있다. 판정의 결론을 목적
 * 문단 안에 적으면 다음 판정이 자기 메아리를 근거로 읽는다(§원칙 A A5′-2).
 *
 * **이 계약은 시간만 말한다.** 저장 공간에 대한 조건이 없으므로, 원소 수보다 훨씬 많은
 * 자리를 쓰는 구현도 이 계약을 지킨다(§규약1 「공간은 어느 계약에도 없다」 — 미결).
 *
 * **이름이 계약을 정하지 않는다.** `heap/minHeap` · `heap/maxHeap` · `heap/daryHeap` 이
 * 이 계약과 **같은 계약**을 적고 같은 스위트를 돈다. 「최소」와 「최대」는 비교자를 뒤집은
 * 같은 문장이고, 갈래 수는 담는 모양의 성질이라 계약이 관측하지 못한다(§규약1 「힙 여덟의
 * 판정」). 이 자리에 우선순위 큐가 정본으로 선 것은 넷 중 **유일한 ADT 이름**이기
 * 때문이고, 그래서 이 이름은 임시가 아니다(불변 사실 66). 다만 카테고리 이름 `heap/` 은
 * 여전히 구현 이름이고 그것은 KAN-031 의 대상이다.
 *
 * **불변식.** 없다.
 *
 * **연산이 넷인데도 비는 것은 판별 절차를 돌린 결과다**(§규약1 「불변식 판별 절차」).
 * 판별 절차를 **한 행을 뺀 표면에 다시 돌렸고** 결과는 그대로 「없다」다. 후보가 둘에서
 * 하나로 줄었고, 남은 하나가 첫 번째 물음에서 걸린다.
 *
 * - **`peek()` 은 다음 `dequeue()` 가 돌려줄 원소와 같다** — 「최우선 원소가 무엇인가」를
 *   **상태를 바꾸지 않고** 읽는 공개 연산이 `peek` 하나뿐이므로 **첫 번째 물음에서
 *   걸린다.** `dequeue` 도 그 값을 내놓지만 그것은 관측이 아니라 상태를 바꾸는 연산이고,
 *   그 반환값에 대한 조건은 그 연산의 의미이므로 축1이 참조 모델과 대조하며 본다
 *   (불변 사실 35).
 * - **사라진 후보는 「`isEmpty()` 는 `size() === 0` 과 같다」였다.** 원소 수를 읽는 경로가
 *   둘이라 첫 물음을 지나고 둘째 물음에서 걸리던 자리인데, 그 행이 표면에서 빠져 후보 자체가
 *   서지 않는다. 비었는지는 `size()` 하나로만 읽힌다.
 *
 * 담는 모양의 성질(부모가 자식보다 앞서는가, 자리가 앞쪽에 빈틈없이 들어찼는가)은 후보조차
 * 아니다. 계약에 적어야 검사할 수 있는데 **적는 순간 그것이 표현을 처방한다**(불변 사실 36).
 * 그런 성질이 어긋난 구현은 답이 틀리므로 축1이 값에서 잡는다.
 *
 * **연산 계약.** n 은 담긴 원소 수다.
 *
 * | 연산 | 의미 | 상한 | 한정자 |
 * |---|---|---|---|
 * | `constructor(comparator)` | 비교자를 고정한다. 이후 교체할 수 없다 | O(1) | worst |
 * | `enqueue(item)` | `item` 을 담는다. 같은 원소를 여러 벌 담을 수 있고 넣은 수만큼 나온다 | O(log n) | amortized |
 * | `dequeue()` | 비교자가 가장 앞세우는 원소를 지우고 돌려준다. 비어 있으면 `null` 이고 상태가 바뀌지 않는다 | O(log n) | amortized |
 * | `peek()` | 같은 원소를 지우지 않고 돌려준다. 비어 있으면 `null` | O(1) | worst |
 * | `size()` | 담긴 원소 수 | O(1) | worst |
 *
 * **물려받은 표면에서 뺀 것이 한 행이다.** 남은 네 행에 더할 것이 없다 — 필요충분조건의
 * 비용 조건이 *"못 지키면 무엇이 되는가"* 로 끝나므로 연산 집합이 모자라지 않고
 * (§규약1 「연산 집합은 계약의 결과가 아니라 계약의 일부다」), 표면에 내부 표현을 내보이는
 * 이름도 없다. **같은 계약을 적은 나머지 셋에서도 같은 행을 뺐다** — `push`/`pop` 은 이름만
 * 다른 같은 연산이라 이쪽 이름으로 맞췄고, `daryHeap` 생성자의 갈래 수는 담는 모양이라 뺐다.
 *
 * **상한을 `O(log n)` 으로 적은 근거는 하한이다.** 비교로만 순서를 아는 구현에서 원소 n 개를
 * 넣고 n 번 빼면 나오는 것이 정렬 수열이고, 비교 기반 정렬의 하한이 $\Omega(n\log n)$ 이다.
 * 이 하한은 **호출 하나가 아니라 시퀀스 전체**에 대한 값인데, 계약이 적는 것도 상각이라
 * 그대로 맞물린다 — 갱신 둘을 다 상각 상수로 적으면 그 시퀀스의 총비용이 $O(n)$ 이어야
 * 하고 하한과 어긋난다. 그러므로 **둘을 함께 상수로 적을 수 없다.** 남는 선택 셋을 이렇게
 * 갈랐다.
 *
 * - **한쪽만 $O(1)$ 로 적는 것**은 가능하다(넣기를 상수로 두는 계열이 실재한다). 그러나
 *   그렇게 적으면 배열 한 줄에 접어 담는 계열이 통째로 나가고, 배제할 이유가 목적에 없다.
 *   상수 시간 넣기를 요구하는 계약은 따로 있다(`heap/pairingHeap`).
 * - **$O(n)$ 은 계약이 뜻을 잃는다.** 순서 없이 쌓아 두고 꺼낼 때 훑는 구현이 그 상한을
 *   내고, 그것이 상한이면 이 구조를 따로 둘 이유를 계약이 말하지 못한다.
 * - **$O(\sqrt n)$ 은 두 자명한 구현을 배제하기는 하지만 하한에서 멀다.** 하한이 있는
 *   자리에서는 그 하한이 고를 값을 지목한다(§규약1 「약한 쪽 규칙은 상한으로 확장되지
 *   않는다」의 `unrolledLinkedList` 자리와 같다). **다만 하한만으로는 상한이 정해지지
 *   않는다** — 그 값을 실제로 달성하는 구현이 있어야 하고, `_reference/priorityQueue.ts`
 *   가 그것이다. 하한이 아래를 막고 정본이 그 자리를 짚어 둘이 만나는 값을 적었다.
 *
 * **이 하한은 증명이 아니라 근거다.** 비교로만 순서를 아는 구현에 대해서만 성립하고,
 * 원소가 유계 정수라 자리를 나눠 담을 수 있으면 아래로 내려간다(`heap/vanEmdeBoasTree`
 * 계열). 그 계열까지 담으려면 계약이 원소 타입을 제한해야 하는데 이 계약은 제한하지 않는다.
 *
 * **`peek` 를 `O(1)` 로 적은 것은 목적이 지목한다.** $O(\log n)$ 으로 적으면 정렬 집합이
 * 이 계약을 만족하게 되고, 그러면 「보기를 상수에」가 계약에서 사라진다. 반대로 **강하게
 * 적는 대가가 없다** — 최우선 원소가 어디 있는지를 넣기·빼기가 이미 로그를 쓰며 유지하므로,
 * 그 자리를 따로 들고 있는 데 드는 비용이 상한을 바꾸지 않는다.
 *
 * **한정자.** 갱신 둘이 `amortized` 이고 조회 둘이 `worst` 다. 셋은 줄 세워지지 않으므로
 * (§규약1 「가장 약한 한정자는 셋을 줄 세우지 않는다」, 불변 사실 59) 행마다 따로 정했다.
 *
 * - **`enqueue`·`dequeue` 가 `amortized` 인 이유.** `worst` 로 적으면 나가는 계열이
 *   실재하고 **수치로 확인했다.** 자식들을 미뤄 두었다 빼기 때 한꺼번에 짝지어 접는
 *   구현(`heap/pairingHeap` 계열)은 오름차순으로 n 개를 넣은 뒤 **첫 빼기 하나**가
 *   1,023 → 4,095 → 16,383 걸음이다($r = 4.00$, 그다음 빼기는 2). 네 행을 상각으로는
 *   전부 지키므로 이 계약 안에 있고, `worst` 로 적는 순간 나간다. 배제할 이유가 목적에
 *   없다 — 목적은 갱신을 로그 안에 두는 것이지 호출 하나의 응답 시간을 약속하는 것이
 *   아니다. 자리를 배로 늘려 가며 이어 담는 구현도 같은 자리인데 이쪽은 **계측으로
 *   보이지 않는다**(런타임의 재할당은 `__cost` 가 세지 않는다 — 불변 사실 60).
 *   `expected` 는 `amortized` 와 서로를 함의하지 않는 다른 계약이고, 무작위를 실제로
 *   뽑는 구현을 요구하게 되므로 적지 않는다.
 * - **`peek`·`size` 가 `worst` 인 이유.** 강한 쪽을 적으려면 그것이 배제하는
 *   계열을 배제해도 계약이 잃는 것이 없어야 한다. `worst` 가 배제하는 것은 **첫 호출에
 *   값을 만들어 두는 계열**인데, 그 값을 처음부터 들고 있는 데 드는 대가가 없다 — 갱신 둘이
 *   이미 로그를 쓰고 있고 그 안에서 최우선 자리와 원소 수를 함께 고칠 수 있다. 그래서
 *   배제해도 계약이 잃는 것이 없다.
 * - `constructor` 만 축3에서 면제된다. 상각이 **한 인스턴스에 대한 호출 시퀀스**의 값인데
 *   생성자는 그 시퀀스를 여는 연산이라 나눌 시퀀스 안에 없다(§규약2 축3 면제).
 *
 * **주입 정책.**
 * - `comparator(a, b)` 는 음수·0·양수를 돌려주는 전순서여야 한다. 음수는 「`a` 가 먼저
 *   나온다」이고 0 은 「순서를 정하지 않는다」다. **기본 비교자를 두지 않는다** — 기본값을
 *   두면 그 방향이 계약의 일부가 되고, 「최소부터」와 「최대부터」가 서로 다른 계약이 된다.
 *   방향은 주입자가 정한다. 비교자를 주지 않으면 `TypeError` 다.
 * - 같은 원소를 여러 벌 담을 수 있다. 집합이 아니므로 `enqueue` 는 중복을 버리지 않고,
 *   담은 수만큼 `dequeue` 로 나온다.
 * - 빈 큐의 `peek`·`dequeue` 는 예외가 아니라 `null` 이다. 그 대가로 `T` 에 `null` 이
 *   섞이면 「비어 있음」과 「값이 `null` 인 원소」가 구분되지 않는다. 반환 타입에 「없음」을
 *   담을 자리가 있으므로 값으로 답하는 쪽을 골랐다(`tree/linkCutTree` 의 `RangeError` 와
 *   반대쪽 선택이다 — 그쪽은 `boolean` 에 그 자리가 없다).
 * - 비교자가 호출마다 다른 답을 내면 계약이 성립하지 않는다. 그 조건을 지키는 것은
 *   주입자의 의무이고 이 구조가 검사하지 않는다.
 *
 * **검증 등급.** `complexity`.
 * 네 행을 **함께** 만족하는 자명한 구현이 없다. 언어가 내주는 배열 하나로 담는 길은 둘뿐이고
 * 둘 다 한쪽을 놓는다.
 * - **순서 없이 쌓아 두면** 넣기가 상수인 대신 최우선 원소를 읽는 일과 빼는 일이 원소 수에
 *   비례한다. 최우선 자리를 따로 캐시해 두면 `peek` 는 상수가 되지만 **빼기가 그대로다** —
 *   지운 뒤 다음 최우선을 찾으려면 다시 훑어야 한다.
 * - **늘 정렬해 두면** 빼기와 보기가 상수인 대신 넣기가 자리를 찾아 뒤를 밀므로 원소 수에
 *   비례한다.
 * - 둘을 함께 들어도 각자의 약점이 그대로 남는다. 넷을 함께 상한 안에 두려면 **원소를
 *   부분적으로만 정렬해 두고 그 부분 순서를 갱신마다 고쳐 쓰는** 설계가 들어와야 하고,
 *   그것은 배열·객체의 기본 연산에서 나오지 않는다.
 *
 * **필요충분조건.**
 * - 의미: 꺼낼 수 있는 것이 **비교자 기준 맨 앞 하나**라는 것과 그 하나를 **지우지 않고 볼
 *   수 있다**는 것 중 하나라도 버리면 이 구조가 아니다. 임의 원소를 찾고 지우는 일을 더하면
 *   정렬 집합이고(`tree/redBlackTree`), 두 모음을 하나로 접는 일을 더하면 합칠 수 있는
 *   우선순위 큐이며(`heap/leftistHeap`), 담긴 원소의 우선순위를 낮추는 일을 더하면
 *   `heap/fibonacciHeap` 이다. 반대로 보기를 버리면 넣은 것을 정렬해 한꺼번에 돌려주는
 *   일이라 정렬 함수로 충분하다.
 * - 비용: 둘 다 지키더라도 **넣기와 빼기 중 하나가 원소 수에 비례하면 배열 하나다** —
 *   정렬해 두면 넣기가, 안 하면 빼기가 그렇다. 그리고 **셋을 다 지키더라도 보기가 로그면
 *   정렬 집합이다.** 이 계약이 요구하는 것은 갱신 둘을 로그 안에 두면서 보기를 **상수**에
 *   두는 것이다.
 */
export class PriorityQueue<T> {
  constructor(comparator: (a: T, b: T) => number) {
    throw new Error("Not implemented");
  }

  enqueue(item: T): void {
    throw new Error("Not implemented");
  }

  dequeue(): T | null {
    throw new Error("Not implemented");
  }

  peek(): T | null {
    throw new Error("Not implemented");
  }

  size(): number {
    throw new Error("Not implemented");
  }
}
