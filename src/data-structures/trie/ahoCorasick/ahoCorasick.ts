/**
 * AhoCorasick — 생성 시점에 고정한 패턴 집합이 텍스트의 어디에 나타나는지를, 검색 한 번의 비용이 패턴 집합의 크기에 기대지 않게 답하는 불변 구조.
 *
 * **목적.** 문자열 패턴 목록을 받아 **한 번 짓고**, 그 뒤로 **텍스트를 줄 때마다 모든 패턴의 모든 출현**을 돌려주는 것. 검색 한 번의
 * 비용이 **텍스트 길이와 답의 수**로만 정해지고 패턴이 몇 개인지 · 얼마나 긴지에 기대지 않는다 — 패턴 열 개를 담든 십만 개를 담든
 * 답이 같은 텍스트에 드는 비용이 같다. 짓는 일은 패턴 목록의 크기에 비례한다. 패턴 집합은 **생성 시점에 정해지고 그 뒤로 바뀌지
 * 않는다** — 이 계약에는 상태를 바꾸는 연산이 하나도 없다(불변 구조 — §규약1 「불변 구조」).
 *
 * **자료구조인지를 먼저 물었다**(`docs/ORD-006-conventions.md` 「「자료구조인가」는 물음 둘이다」 — T5-06). ① 호출 사이에 남는 상태가
 * **없다** — 생성자 뒤의 `search` 가 읽기뿐이다. ② 그래도 **답이 구성 입력과 질의 인자만으로 하나로 정해진다** — 패턴마다 나타나는
 * 시작 자리의 집합은 (패턴 목록, 텍스트)의 함수이고, 자리를 오름차순으로 정해 배열까지 하나다. 지어 둔 상태는 그 답을 싸게 내는
 * 색인이다. `tree/huffmanTree`(지어 둔 상태가 알고리즘이 고른 답)와 갈리는 자리이고 `range-query/sparseTable` 과 같은 쪽이다.
 *
 * **`trie/suffixTree` 와는 거울상이고 서로 담지 않는다**(불변 사실 54). 저쪽은 텍스트를 고정하고 패턴을 질의로 받아 질의 비용이 텍스트
 * 길이에 기대지 않고, 이쪽은 패턴을 고정하고 텍스트를 질의로 받아 검색 비용이 패턴 집합에 기대지 않는다.
 *
 * - **저쪽 구현으로 이쪽 표면을 지으면 검색이 패턴 수에 기댄다** — 검색마다 텍스트로 접미사 트리를 짓고 패턴마다 자리를 모으면, 텍스트를
 *   고정하고 패턴 수를 키우는 시나리오에서 3,690 · 6,762 · 19,050 으로 걸린다(저쪽 정본으로 지은 구현 —
 *   `src/data-structures/_contract/runContract.ahoCorasick.test.ts` 셋째 묶음).
 * - **이쪽 구현으로 저쪽 표면을 지으면 질의가 텍스트 길이에 기댄다** — 패턴 하나를 물을 때마다 그 패턴으로 짓고 텍스트를 다시 훑어야
 *   한다(산술 — 짓지 않았다).
 *
 * **`trie/ternarySearchTree`(문자열 집합)와는 연산 집합으로 갈린다.** 저쪽은 넣기 · 지우기와 낱말 · 접두사 질의를 갖고 이쪽은 텍스트를
 * 훑는 검색 하나만 갖는다. 저쪽 계약을 지키는 트라이를 그대로 두고 텍스트의 자리마다 뿌리부터 내려가는 구현
 * (`_contract/_fixtures/restartingTrieMatcher.ts`)은 이쪽 검색 행을 어긴다 — 아래 「검증 등급」의 둘째 수치.
 *
 * **이 계약에 없는 것.**
 * - **패턴 넣기 · 빼기.** 들어오면 호출 사이에 상태가 남아 불변 구조가 아니게 되고, 짓기와 검색의 약속을 다시 적어야 한다.
 * - **텍스트를 조각으로 나눠 이어 주기.** 조각 사이에 읽던 자리를 들고 있어야 해 역시 호출 사이에 상태가 생긴다.
 * - **겹치지 않게 고르기 · 가장 긴 것만 · 가장 왼쪽만.** **위 「목적」이 적은 일은 「모든 패턴의 모든 출현」을 돌려주는 것**이라 그중
 *   일부를 고르는 일은 사용으로 적혀 있지 않고, 돌려받은 목록에서 호출자가 고른다(A2 · A4 · A5′ — 판정표 ① 72).
 * - **공간.** 이 계약은 시간만 말한다(§규약1 「공간은 어느 계약에도 없다」 — 미결).
 *
 * **이 이름은 계약을 정당화하지 않는다.** 계약의 어느 문장도 실패 쪽 상태나 오토마톤을 말하지 않는다 — 문자 가짓수를 상수로 접는 모형
 * 위에서는 다음 상태를 표로 전부 적어 두는 구현도 두 행을 지킨다. 이름이 가리키는 기법은 이 계약이 고른 계급의 대표일 뿐이고, 계약
 * 이름을 다시 붙이는 일은 KAN-031 의 몫이다.
 *
 * **불변식.** 없다.
 *
 * 판별 절차(§규약1 「불변식 판별 절차」)를 돌린 결과이고, 후보가 셋이었다.
 *
 * - **「같은 텍스트에 대한 답은 늘 같다」** — 그 값을 읽는 공개 연산이 `search` 하나라 첫 물음에서 걸린다. 상태를 바꾸는 연산이 없어
 *   「두 상태에 걸친 성질」이 생기지도 않는다.
 * - **「돌려준 자리에서 그 패턴이 실제로 시작한다」** — 역시 `search` 하나가 말하는 것이라 그 연산의 의미이고 축1이 본다.
 * - **담는 모양의 성질**(실패 쪽 상태가 가장 긴 진접미사를 가리킨다 등)은 후보가 아니다. 적는 순간 표현을 처방한다(불변 사실 36).
 *
 * **그리고 불변 구조라 판별 결과와 무관하게 축2가 축1보다 더 잡는 것이 없다**(불변 사실 52 ①) — 축2가 볼 수 있는 상태가 생성 직후
 * 하나뿐이고, 그 하나에서 관측값들이 맞는지는 축1이 매 연산 대조한다. 절이 빈 이유는 이 문장이 아니라 위 판별이다.
 *
 * **연산 계약.** m 은 생성자가 받은 **패턴 목록의 크기**(패턴마다 길이 + 1 을 더한 값 — 같은 문자열이 여러 번 있으면 여러 번 센다), ℓ 은
 * `search` 가 받은 텍스트의 길이, k 는 그 검색이 돌려준 **자리의 수**(배열 길이의 합)다. 길이는 코드 단위로 센다.
 *
 * | 연산 | 의미 | 상한 | 한정자 |
 * |---|---|---|---|
 * | `constructor(patterns)` | `patterns` 의 문자열 집합을 패턴으로 고정한다(같은 문자열은 하나). **돌아온 뒤의 검색은 아래 상한을 지킨다.** 돌아온 뒤 호출자가 `patterns` 를 고쳐도 답은 바뀌지 않는다 | O(m) | worst |
 * | `search(text)` | 패턴 p 마다 `0 <= i <= ℓ - (p 의 길이)` 중 `text` 의 `i` 부터가 p 와 같은 모든 `i` 를 **오름차순** 배열로 담은 `Map`. **한 번이라도 나타난 패턴만 키로 둔다.** 키의 차례는 정하지 않는다. 돌려준 `Map` 과 배열은 호출자의 것이다 | O(ℓ + k) | worst |
 *
 * **나타나지 않은 패턴을 키로 두지 않는 것이 이 계약의 중심 조항이다.** 물려받은 문서는 「결과 Map 은 모든 패턴을 키로 포함(매칭 없으면
 * 빈 배열)」이었는데, 그러면 검색 한 번이 패턴 수만큼 키를 만들어 비용이 패턴 수에 묶이고 목적이 사라진다 — 짧은 텍스트 하나를 많은
 * 패턴에 대어 보는 쓰임에서 검색 비용을 패턴 수가 정하게 된다(산술 — 그 표면의 구현을 짓지는 않았다).
 *
 * **빈 패턴은 자리 `0` 부터 `ℓ` 까지 전부에서 나타난다** — 위 조건에 길이 0 을 넣은 그대로이고 `trie/suffixTree` 의 빈 패턴과 같은 읽기다.
 *
 * **자리 배열을 오름차순으로 정한 것은 계급도 표현도 바꾸지 않는다**(§「분할을 담는 계약은 대표를 분할만으로 정해지는 값으로 정한다」의
 * 두 물음). 패턴 하나의 출현은 끝나는 자리의 차례가 곧 시작하는 자리의 차례라, 텍스트를 한 번 훑는 어느 구현에서도 따로 드는 일이 없다.
 * **키의 차례는 정하지 않는다** — 처음 나타난 차례든 패턴 목록의 차례든 정하면 그 차례를 만드는 방법까지 계약이 정한다.
 *
 * **구성 시점을 계약이 정한다**(불변 사실 52 ③). 표의 「돌아온 뒤의 검색은 아래 상한을 지킨다」가 그 줄이다. 적지 않으면 생성자는 목록을
 * 베끼기만 하고 첫 검색이 색인을 짓는 구현(`_contract/_fixtures/deferredAhoCorasick.ts`)이 두 상한을 함께 만족하는 것처럼 읽힌다 — 실측으로
 * 그 구현의 생성자는 두 구성 시나리오를 통과하고, 짓자마자 재는 첫 검색이 20,435 · 81,103 · 323,780 으로 검색 상한을 넘는다.
 *
 * **구성과 묶어 적을 질의가 없다**(불변 사실 52 ②). `search` 는 인자가 있고 인자 공간(텍스트)이 끝이 없어 미리 계산할 수 없다 — §규약1
 * 「불변 구조」 표의 첫째 줄이라 상한을 따로 적는다.
 *
 * **축3은 두 상한을 시나리오 다섯으로 판정한다**(§「표의 상한과 시나리오의 bound 가 다르면 헤더가 그 판정을 적는다」).
 *
 * - **생성자 `O(m)` 은 `O(n)` 으로 두 번** — 무작위 패턴 목록, 그리고 짧은 패턴 `a` … `a^c` 가 긴 패턴 `a…a` 의 모든 자리에서 끝나는
 *   목록(n 은 목록 크기).
 * - **검색 `O(ℓ + k)` 는 파라미터 끝마다.** ① ℓ(64) · k(상수)를 고정하고 **패턴 수를 n 으로 키워 `O(1)`** — 상한에 없는 파라미터가 비용을
 *   안 바꾼다는 이 계약의 중심이다(§「상한에 없는 파라미터도 시나리오가 키운다」) ② **ℓ = n · k = 0 에서 `O(n)`** ③ **ℓ = n · k = Θ(n) 에서
 *   `O(n)`** — `spatial/kdTree` 의 「답의 수는 양 끝에 고정한다」와 같은 처분이다. ②③은 패턴 길이를 √n 으로 함께 올린다 — 패턴 길이도
 *   상한에 없고 한 사다리에 싣는다(§「상한에 없는 파라미터를 사다리 하나에 싣는 조건」).
 * - 그래서 **로그 인수만큼 어기는 구현은 통과한다**(불변 사실 53). 텍스트마다 접미사 트리를 짓는 위 구현이 ② · ③ 에서 60,484 · 282,756 ·
 *   1,294,596(비율 4.67 · 4.58)으로 `O(n)` 구간 안이다.
 *
 * **두 상한은 하한이 지목한 값이다**(§「하한이 지목하는 상한은 두 걸음으로 적는다」).
 *
 * - **검색 `O(ℓ + k)`.** 텍스트의 문자 하나를 안 읽으면 그 자리에서 시작하는 한 글자 패턴을 가를 수 없고, 돌려주는 자리마다 적는 일이
 *   든다 — 아래가 ℓ + k 로 잘린다. 정본이 그 계급을 낸다. 패턴 집합의 크기를 조금이라도 싣는 상한은 「할 수 있는데 안 하겠다」가 아니라
 *   목적을 버리는 것이다.
 * - **생성자 `O(m)`.** 패턴의 문자 하나를 안 읽으면 그 패턴이 나타나는지 가를 수 없어 아래가 m 으로 잘리는데, **이 하한은 구성 시점 조항과
 *   배열 조항에 기대어 선다**(§「하한이 지목한 상한도 다른 계약 조항에 기대어 선다」) — 둘이 없으면 배열 참조만 들고 첫 검색에서 짓는
 *   생성자가 상수다. 정본이 그 계급을 낸다. 더 약하게(목록 크기의 1.5 제곱) 적으면 상태마다 끝나는 패턴 목록을 복사해 합치는 설계
 *   (물려받은 문서의 3단계를 배열 복사로 옮긴 것 — `_contract/_fixtures/outputRuleAutomaton.ts` 의 `copied`)가 들어오는데, 그 설계는 두 번째
 *   구성 시나리오에서 6,970 · 50,652 · 385,556(비율 7.27 · 7.61)으로 걸리고 정본은 같은 목록을 1,451 · 5,379 · 20,764 에 짓는다 — 배제해도
 *   잃는 것이 없다.
 *
 * **두 행이 `worst` 인 것.** §「`worst` 만 있는 계약의 한정자 근거는 「배제될 계열이 없다」로 적는다」(T1-06)가 든 두 계열 — 비용을 뒤
 * 호출로 미루는 계열(상태를 바꾸는 연산이 있어야 선다)과 무작위성으로 상한을 얻는 계열(답이 입력의 함수로 유일하다) — 은 이 계약에 없다.
 * **셋째 계열이 있다 — 구성을 첫 질의로 미루는 계열**(§「불변 구조에도 `worst` 가 배제하는 셋째 계열이 있다」 — `range-query/sparseTable`
 * 선례, 불변 사실 328). 짓자마자 검색 n 번을 전부 재면 위 구현의 단일 호출 최대가 20,435 · 81,103 · 323,780 으로 걸리고 호출 평균은
 * 200.78 · 200.76 · 200.75 로 통과한다. 정본은 두 통계 모두 181 이라 **그 계열을 배제해도 잃는 것이 없고**(불변 사실 59) 축3의 단일 호출
 * 최대가 그 강함을 잰다(§「한정자의 강한 읽기는 축이 그 강함을 잴 때만 적는다」). 생성자 행은 n 에 대해 반복 호출되지 않아 강한 한정자를
 * 적는 대가가 없다.
 *
 * **문자 가짓수는 상한에 없다.** `trie/ternarySearchTree` 헤더와 같은 모형이다 — 코드 단위 가짓수를 상수로 접은 모형 위의 값이고, 자식을
 * 문자로 찾는 한 번을 한 걸음으로 친다. 그 「상수」가 $2^{16}$ 까지 간다는 것을 감추지 않고 여기 적는다.
 *
 * **주입 정책.** 없다. 입력이 문자열 목록과 문자열이고 문자의 동등성은 언어가 정한 코드 단위 비교를 쓴다. 이 계약은 패턴을 **견주지 않고**
 * 같은지만 본다.
 *
 * - 빈 목록 · 빈 패턴 · 빈 텍스트 · 텍스트보다 긴 패턴은 전부 정당하고 **던지지 않는다.** 나타나는 패턴이 없으면 빈 `Map` 이다.
 * - **생성자는 `patterns` 를 읽기만 한다** — 표의 생성자 행 그대로다.
 *
 * **물려받은 표면을 넷 고쳤다.** 물려받은 상한 주장(생성자 「총 패턴 길이 합」 · 검색 「텍스트 길이 + 총 매칭 수」)은 판정 입력으로 쓰지
 * 않았다(불변 사실 65) — 앞의 것은 패턴 수를 빠뜨렸고(빈 패턴만 여럿이면 길이 합이 0 이다) 뒤의 것은 같은 문서의 「모든 패턴을 키로」와
 * 함께 설 수 없었다.
 *
 * - **결과 `Map` 의 키를 나타난 패턴으로 좁혔다**(위 중심 조항).
 * - **빈 패턴의 답을 정했다**(자리 `0` … `ℓ`). 물려받은 문서는 말하지 않았다.
 * - **오름차순 · 같은 패턴은 하나를 조건에서 의미 열로 옮겼다.**
 * - **내부 필드(`root` · `patterns`) · 마디 타입 · 「Goto · Failure · Output 세 함수」 처방 · 스토리 · 크기 제약 · 벽시계 성능 시험을 뺐다**
 *   (불변 사실 7 · 20 · 38).
 *
 * **검증 등급.** `complexity`.
 * 두 행을 **함께** 만족하는 자명한 구현이 없다.
 *
 * - **패턴마다 텍스트를 훑으면**(`_contract/_fixtures/patternwiseScanMatcher.ts`) 짓기는 목록 크기인데 검색이 패턴 수 × 텍스트 길이다 —
 *   패턴 수 시나리오에서 103,143 · 410,343 · 1,639,143.
 * - **트라이 하나에 담고 자리마다 뿌리부터 내려가면** 검색이 패턴 수에는 안 기대는 대신 텍스트 길이 × 패턴 길이다 — ℓ = n · k = 0 에서
 *   34,289 · 268,257 · 2,121,665.
 * - 텍스트를 **한 번** 훑으며 읽던 자리를 이어 가려면 실패 쪽 상태 같은 설계가 들어오고, 그 훑기가 텍스트 길이에 선다는 것은 물러서는 걸음의
 *   총합을 세는 논증이라 코드를 국소적으로 읽어서는 안 나온다(§「한 호출 안쪽의 총합 논증을 상각이라 부르지 않는다」). 그 설계에서도 끝나는
 *   패턴을 실패 쪽 사슬 끝까지 따라가 찾으면 ℓ = n · k = 0 에서 35,313 · 272,353 · 2,138,049 로 걸린다(`outputRuleAutomaton` 의 `chain`).
 *
 * **필요충분조건.**
 * - 의미: 한 번 고정한 패턴 집합의 **모든 출현**(겹친 것 · 서로 포함하는 것 포함)을 텍스트마다 돌려준다. 패턴을 넣고 빼는 일이 들어오면
 *   문자열 집합 계열이고, 텍스트를 고정하고 패턴을 질의로 받으면 `trie/suffixTree` 다.
 * - 비용: 둘 다 지키더라도 **검색이 패턴 수에 비례하면** 패턴마다 훑기이고, **텍스트 길이 × 패턴 길이면** 자리마다 트라이를 내려가는
 *   것이며, **답을 적는 일이 답의 수의 제곱이면** 결과를 늘릴 때마다 복사하는 것이고(k = Θ(ℓ) 시나리오 1,020,337 · 16,533,345 ·
 *   266,411,713 — `outputRuleAutomaton` 의 `append: "copy"`), **짓기를 첫 검색으로 미루면** 구성 시점을 어긴 것이다 — 넷 다 이 구조가
 *   아니다. 짓기나 검색이 로그 인수만큼 무거운 구현도 이 계약이 아니지만 그 갈림은 축3이 못 본다.
 */
export class AhoCorasick {
  constructor(patterns: string[]) {
    throw new Error("Not implemented");
  }

  search(text: string): Map<string, number[]> {
    throw new Error("Not implemented");
  }
}
