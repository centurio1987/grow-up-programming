/**
 * LRUCache — 담을 수 있는 키의 수가 생성 시점에 정해진 사전. 가득 찬 뒤 새 키를 넣으면 가장 오래 안 쓴 키가 밀려나고, 찾기와 넣기가 그 용량과 무관한 비용이다.
 *
 * **목적.** 키 하나에 값 하나를 대응시켜 담되 **담는 키의 수에 생성 시점에 정한 경계**를 두고,
 * 경계에 닿은 뒤 새 키가 오면 **가장 오래 안 쓴 키를 밀어내는** 사전을 제공하는 것. 키로 값을
 * 찾는 일과 넣는 일이 둘 다 **용량과 무관한 비용**이고, 그 무관함이 `hash/hashMapChaining` 과 같은
 * 뜻의 **기대**로 붙는다.
 *
 * **「쓴다」가 무엇인지를 이 계약이 정한다.** 밀려날 키를 고르는 기준이 곧 이 계약의 내용이라 셋을
 * 못 박는다.
 *
 * - **`put` 은 쓰기다** — 새 키를 넣는 것도, 담긴 키의 값을 덮어쓰는 것도. 덮어쓰기를 쓰기로 치지
 *   않으면 계속 고쳐 쓰는 키가 처음 넣은 시점에 묶여 밀려난다.
 * - **맞은 `get` 도 쓰기다.** 치지 않으면 무엇이 밀려나는지가 마지막으로 **넣거나 덮어쓴** 순서로만
 *   정해져, 계속 읽히는 키가 오래전에 쓰였다는 이유로 밀려난다. 그것은 다른 정책이다(아래 필요충분조건).
 * - **빗나간 `get` 은 쓰기가 아니다.** 담기지 않은 키에는 올릴 자리가 없고, 상태가 바뀌지 않는다.
 *
 * 「가장 오래 안 쓴 키」는 담긴 키 가운데 **마지막으로 쓴 시점이 가장 이른 키**다. 호출은 하나씩
 * 일어나므로 그런 키는 늘 하나로 정해진다.
 *
 * **「기대」가 무엇에 대한 기댓값인지는 `hash/hashMapChaining` 헤더가 정의한 것과 같다.** ① 구현이
 * 뽑는 무작위성에 대한 기댓값이지 입력에 대한 것이 아니고 — **호출자가 어떤 키를 주든** 성립해야
 * 한다 — ② 호출 하나가 아니라 같은 인스턴스에 부른 호출 시퀀스의 기대 총비용이 호출 수 × 상한
 * 안이라는 뜻이다. 이 문장이 한정자 셋 중 하나가 아니라는 사실과 그 처분(뜻이 가장 가까운
 * `expected` 를 적고 헤더가 정의한다), 그리고 축3의 `expected` 통계가 기댓값이 아니라 seed 다섯의
 * 시퀀스 평균 중 가운데 값이라는 틈은 §규약1 「한정자 셋으로 못 적는 계약이 있다」와 「`expected`
 * 통계는 기댓값이 아니라 중앙값이다」에 있고 여기서 다시 세우지 않는다.
 *
 * **용량은 저장량 조건이 아니라 관측되는 경계라서 계약에 들어온다**(불변 사실 67 ·
 * `linear/circularBuffer` 와 같은 자리). §규약1 「공간 제약이 계약에 들어오는 자리」의 세 걸음을
 * 돌렸다.
 *
 * 1. 계약의 용어로 — **담긴 키는 용량을 넘지 않고, 넘길 넣기는 가장 오래 안 쓴 키를 먼저 뺀다.**
 * 2. 그 문장이 사는 행 — `put` 행의 밀어내기와, 밀려난 키를 묻는 `get` 행의 `null`. 두 행이 함께
 *    말한다(§규약1 「존재 이유가 두 행의 조합에서 계약의 문장이 된다」).
 * 3. 그 제약이 사는 축 — 값의 **의미**이므로 축1이다. 용량을 받아 두고 아무것도 밀어내지 않는 구현
 *    (`_contract/_fixtures/unboundedCache.ts`)이 축3 네 시나리오를 전부 1.00 고정으로 통과하고, 축1 경계
 *    케이스 「가득 찬 뒤 새 키를 넣으면 …」의 여섯째 걸음 `get(1)` 에서 관측 10 / 모델 `null` 로 갈린다.
 *
 * 그러므로 **이 계약은 저장량을 상한으로 적지 않으면서도 용량을 적는다.** 용량의 열 배로 자리를
 * 잡는 구현도 두 행을 지킨다 — 저장 공간에 대한 조건은 여전히 없다(§규약1 「공간은 어느 계약에도
 * 없다」 — 미결).
 *
 * **이웃 계약과의 관계.**
 * - **사전(`hash/hashMapChaining`) — 두 계약을 동시에 만족하는 구현이 없다.** 불변 사실 54 의
 *   「서로 담지 않는다」보다 강한 관계이고 `linear/circularBuffer` ↔ `linear/queue` 와 같은 모양이다
 *   (불변 사실 181). 논증은 두 계약의 문장만 쓴다. 이름을 맞춰 놓고(`put`↔`set` · `get`↔`get`) 용량
 *   `c` 로 연 이 계약의 구현이 사전 계약도 만족한다고 하자. 빈 상태에서 서로 다른 키 `c + 1` 개를
 *   넣은 뒤 첫 키를 물으면, 사전의 `get` 행은 대응된 값을 요구하고 이 계약의 `put` 행은 그 키가 이미
 *   밀려났으므로 `null` 을 요구한다. 같은 호출에 두 값을 요구하므로 그런 구현은 없다. 상한도 한정자도
 *   쓰지 않는 논증이다.
 * - **집합(`hash/hashSet`)과는 서로 담지 않는다.** 그쪽은 값이 없고 집합 연산을 요구하며, 이쪽은
 *   값을 돌려주고 경계에서 밀어낸다. 표면부터 갈린다.
 * - **고정 용량 수열(`linear/circularBuffer`)과는 밀어낼 것을 고르는 기준이 갈린다.** 그쪽은 쓴
 *   순서로 가장 오래된 **원소**를 밀어내고 읽기가 원소를 꺼낸다. 이쪽은 키로 찾고, 읽기가 꺼내지 않고
 *   순서를 올린다. 넣거나 덮어쓴 순서로만 밀어내는 캐시(`_contract/_fixtures/readBlindCache.ts`)는 이
 *   계약의 축1 경계 케이스 「맞은 get 은 …」의 일곱째 걸음 `get(2)` 에서 관측 20 / 모델 `null` 로 갈린다.
 *
 * **이 계약에 없는 것.**
 * - **담긴 수 · 전체 열거 · 지우기 · 용량 읽기 · 사용 순서 읽기.** 목적은 경계 안에서 키로 찾고
 *   넣는 것이고, 다섯 중 어느 것도 없어서 목적을 진술하지 못하게 되는 연산이 아니다(불변 사실 47).
 *   물려받은 표면에도 없었다. 원칙 A 로 읽으면 용량 읽기는 호출자가 넘긴 생성 인자라 조합(A2)이고 나머지 넷은
 *   목적이 사용을 적지 않은 고유 연산(A3)이다 — 결과는 같다(`docs/ORD-006-conventions.md` 「원칙 A」 판정표 ① 44).
 * - **밀려난 키를 알려주는 반환값.** 약속이 관측되는가를 물으면(불변 사실 82) 답이 이미 있다 —
 *   밀려난 키는 그 뒤 `get` 이 `null` 로 관측한다. `linear/circularBuffer` 의 `write` 가 값을 돌려주지
 *   않는 것과 같은 자리다(판정표 ① 45).
 * - **공간.** 위에 적었다.
 *
 * **이름은 임시가 아니다.** 「가장 오래 안 쓴 것을 밀어낸다(least recently used)」가 이 계약의 밀어내기
 * 정책 자체이고 구현 기법의 이름이 아니다(불변 사실 66). 카테고리 `hash/` 가 기법의 이름인 것은
 * 별건이고 KAN-031 이 정한다.
 *
 * **불변식.** 없다.
 *
 * 판별 절차(§규약1 「불변식 판별 절차」)를 돌린 결과이고, 이 계약에서는 절차의 **첫 물음 앞에서**
 * 걸린다 — 담긴 것을 읽는 공개 연산이 `get` 하나인데 **그 호출이 맞으면 사용 순서를 고친다.** 관측이
 * 상태를 바꾸면 관측 경로로 세지 않으므로(불변 사실 124) 상태를 바꾸지 않고 읽는 경로가 하나도 없다.
 * 축2의 검사가 `get` 을 부르면 그 검사가 곧 연산이 된다. 후보 셋이 각각 어디서 받아지는지 적는다.
 *
 * - **「담긴 키가 용량을 넘지 않는다」** — 담긴 수를 읽는 연산이 없다. 그 약속은 `put` 행의 밀어내기가
 *   적고 있고, 어기면 밀려났어야 할 키의 `get` 이 값을 돌려주므로 축1이 본다(위 목적 항목의 셋째 걸음).
 * - **「`put(k, v)` 뒤의 `get(k)` 는 다른 넣기에 밀려나지 않은 한 `v` 다」** — `get` 행의 의미다.
 * - **「사용 순서가 쓰기의 역사와 맞는다」** — 순서를 읽는 연산이 없고, 그 순서를 드러내는 것은 다음
 *   밀어내기의 결과뿐이라 `put`·`get` 두 행의 의미다. 축1이 참조 모델과 매 호출 대조한다.
 *
 * 담는 모양의 성질(칸이 몇이고 줄이 어떻게 이어졌는가)은 후보가 아니다. 적는 순간 그것이 표현을
 * 처방하고(불변 사실 36), 이 계약을 지키는 다른 구현에는 그런 말이 없다(불변 사실 131).
 *
 * **연산 계약.** n 은 **생성자가 정한 용량**이다. 담긴 키의 수가 아니라 미리 정해진 경계이고 캐시가
 * 사는 동안 바뀌지 않는다. 담긴 수는 언제나 n 이하이므로 두 값을 나눠 적어도 상한 값이 갈리지 않는다
 * — 그럴 때는 계약이 고정한 쪽을 적는다(불변 사실 185).
 *
 * | 연산 | 의미 | 상한 | 한정자 |
 * |---|---|---|---|
 * | `constructor(capacity, spread)` | 용량을 `capacity` 로 정하고 키를 정수로 펴는 함수를 고정한 뒤 빈 캐시로 연다. 둘 다 이후 바꿀 수 없다. `capacity` 는 1 이상의 정수여야 하고, 아니면 `RangeError` | O(n) | worst |
 * | `get(key)` | `key` 가 담겨 있으면 그 값을 돌려주고 **`key` 를 가장 최근에 쓴 키로 올린다.** 담겨 있지 않으면 `null` 이고 상태가 바뀌지 않는다 | O(1) | expected |
 * | `put(key, value)` | `key` 가 담겨 있으면 값을 `value` 로 바꾸고 `key` 를 가장 최근에 쓴 키로 올린다 — **아무것도 밀려나지 않는다.** 담겨 있지 않으면, 담긴 수가 용량과 같을 때 **가장 오래 안 쓴 키를 먼저 빼고**(그 키와 값은 다시 관측되지 않는다) `key` 를 `value` 와 함께 가장 최근에 쓴 키로 담는다 | O(1) | expected |
 *
 * **두 행의 `O(1)` 은 목적이 지목한 값이다.** 목적이 「용량과 무관한 비용」이고 축3 사다리가 아는
 * 상한 중 상수 아래는 없다. 한 칸 위 `O(log n)` 으로 적으면 쓴 시각 순으로 정렬해 두는 계열이 두
 * 행을 만족하고, 그때 이 구조는 「정렬 사전에서 가장 이른 시각을 빼는 것」과 갈리지 않아 용량을
 * 크게 잡을수록 호출 하나가 비싸진다 — 목적이 말하는 무관함이 사라진다. **다만 그 갈림은 로그
 * 인수라 축3이 보지 못한다**(불변 사실 53) — 정렬해 두는 계열이 이 스위트를 통과하더라도 그것은
 * 계약을 지킨다는 뜻이 아니다(불변 사실 62). 그 계열의 사본은 짓지 않았다.
 *
 * **두 행이 `expected` 인 것은 키를 흩는 일에서 온다.** 키가 임의의 타입이고 펴기를 주입받으므로
 * 키로 찾는 일은 구현이 뽑는 무작위성 위에 선다(아래 주입 정책). 사용 순서를 고치는 일 자체는 호출마다
 * 상수로 할 수 있지만 찾기 뒤에 붙으므로 행 전체가 찾기의 한정자를 따른다. `worst` 로 적으면
 * 무작위를 뽑는 구현이 나가는데 그것이 이 계약이 요구하는 바로 그 일이다.
 *
 * **호출별 기댓값으로 좁히지 않았다 — 용량을 미리 아는데도.** 좁히면 담긴 수가 늘 때 표를 한꺼번에
 * 늘리는 계열이 나가고, 용량이 생성자 인자라 그 계열이 선 전제(「얼마나 필요한지 모른다」)가 여기서는
 * 서지 않으므로 잃는 것도 없다 — `linear/circularBuffer` 가 같은 전제로 `worst` 를 적은 자리다. 그래도
 * 좁히지 않은 이유는 **목적이 호출별 기대를 요구하지 않기 때문이다** — 불변 사실 59 의 기본값은 배제하려는
 * 것(호출이 담긴 수에 비례하는 구현)을 배제하는 가장 약한 쪽이고, 강한 쪽은 잃는 것이 없을 때 **적을 수
 * 있을** 뿐이다. 이전 판(T3-03)은 「두 읽기가 이름도 통계도 같아 축이 그 강함을 못 잰다」를 근거로 들었는데,
 * 검사가 재는지는 읽기를 고르는 근거가 아니다(`docs/ORD-006-conventions.md` 「원칙 B」 B7). 결과는 같다.
 *
 * **생성자가 `O(n)` 인 것은 용량만큼 자리를 미리 잡는 구현을 배제하지 않으려는 것이다.** `O(1)` 로
 * 적으면 그 계열이 나가는데, 미리 잡는 일은 저장량과 한꺼번에 늘리는 일 사이의 교환이고 그것을 고를
 * 근거가 목적에 없다. 이 행은 n 에 대해 반복 호출되지 않아 축3이 재지 않는다(§규약2 시나리오 규칙 2
 * — 면제). 그래서 정본이 이 행보다 빠른 것(칸 16 개로 연다)이 판정에 걸리지 않는다 — 상한과 정본의
 * 계급이 같아야 한다는 요구(불변 사실 49)는 **축3이 재는 행**에 대한 것이다. 한 번만 불리므로 `worst`
 * 와 나머지 둘이 다르게 배제하는 것이 없다.
 *
 * **물려받은 표면을 넷 고쳤다.** 연산 집합은 그대로 둘이다.
 *
 * - **키·값을 수에서 임의 타입으로 넓히고 펴기를 주입받는다.** 수 키만 받으면 칸을 수 자체로 정할 수
 *   있어 이 계약이 사전 계약들과 다른 주입 정책을 갖게 되는데, 그 차이를 요구하는 문장이 목적에 없다.
 *   T3 의 이웃 둘(`hash/hashMapChaining` · `hash/hashSet`)과 같은 정책이다.
 * - **빗나간 `get` 의 답을 `-1` 에서 `null` 로 바꿨다.** 값이 수가 아니게 된 이상 `-1` 은 담을 수 있는
 *   값과 겹친다. 반환 타입에 「없음」을 담을 자리를 따로 둔다(불변 사실 132).
 * - **「용량은 1 이상이 보장된다」를 호출자의 전제에서 `RangeError` 로 옮겼다.** 전제를 두면 축1이 그
 *   자리를 못 잰다(불변 사실 82). 생성자는 돌려줄 값이 없으므로 던지는 것이 전면화다.
 * - **물려받은 문서의 내부 처방(해시 표 + 이중 연결 리스트 · 더미 머리와 꼬리 · 마디를 맨 앞으로 옮기는
 *   절차)과 쓰이는 자리 이야기, 벽시계 성능 시험을 뺐다.** 앞은 표현의 처방이고(불변 사실 4) 가운데는
 *   모든 구현에 대해 참인 문장이 아니며(불변 사실 38) 뒤는 불변 사실 7 이다.
 *
 * 물려받은 상한은 두 행 `O(1)` 로 이 표와 같지만 입력으로 쓰지 않았다(불변 사실 65). 한정자는
 * 물려받은 문서가 「평균」으로만 적었다.
 *
 * **주입 정책.**
 * - 주입받는 것은 **키를 정수로 펴는 함수 하나**이고 생성자가 고정한다. 선택이 아니라 필수다 —
 *   기본값을 두면 그 기본값이 고정 함수라 아래 조건을 계약이 스스로 어긴다.
 * - 주입자가 지킬 것은 `hash/hashMapChaining` 헤더의 셋과 같다. ① **순수** — 같은 키에 언제나 같은
 *   정수. ② **키의 동등성과 맞을 것** — `Object.is` 로 같은 두 키는 같은 정수. ③ **키의 차이를 지우지
 *   않을 것.** ③이 없으면 어떤 구현도 상한을 지킬 수 없으므로 두 행은 조건부다 — 같은 정수로 가는 키가
 *   담긴 것 중 `c` 개면 그 키들에 드는 비용이 `c` 에 비례한다. **③은 「잘 섞어라」가 아니다** — 흩는
 *   일은 구현의 몫이다. 돌려주는 정수는 32비트로 읽고 완전한 단사를 요구하지 않는다.
 * - 키의 동등성은 `Object.is` 다. 값에는 제약이 없다.
 * - **값으로 `null` 을 담으면 `get` 의 답이 「담겨 있지 않다」와 구별되지 않고, 이 계약에는 가르는 연산이
 *   없다.** 그래도 의미는 갈린다 — 맞은 `get` 은 그 키를 올리고 빗나간 `get` 은 아무것도 안 바꾸므로,
 *   다음 밀어내기가 그 차이를 드러낸다. 가려야 하는 호출자는 값을 감싸 담는다.
 * - `capacity` 는 **1 이상의 정수**여야 하고, 아니면 `RangeError` 다. 0 을 받지 않는 이유는 그때 넣은
 *   키가 곧바로 밀려나 「넣은 것을 읽는다」가 어떤 순서로도 성립하지 않기 때문이다 — 아무것도 담지
 *   못하는 캐시는 이 계약의 대상이 아니다. **용량의 위쪽 끝은 두지 않는다** — 수의 표현이 정한 끝을
 *   계약의 상수로 접지 않는다(`heap/vanEmdeBoasTree` 의 우주 크기와 같은 자리).
 * - 던지는 자리는 생성자 하나다. 담기지 않은 키를 묻는 것은 오류가 아니라 답이 있는 물음이다.
 *
 * **검증 등급.** `complexity`.
 * 판정 절차의 2번에서 걸린다 — 두 행을 **함께** 만족하는 자명한 구현이 없다. 자명한 길 셋을 지어
 * 쟀고(용량을 사다리 1,024 · 4,096 · 16,384 에 올린 계약 스위트) 셋 다 한 행 이상에서 막힌다.
 *
 * - **사용 순서를 배열 한 줄에 두기**(`_contract/_fixtures/recencyArrayCache.ts`) — 맞은 `get` 이 옮길
 *   키를 찾고 당기느라 513 · 2,049 · 8,193, 밀어내는 `put` 이 맨 앞을 지우고 당기느라 1,025 · 4,097 ·
 *   16,385 다($r$ = 3.99 · 4.00). 무작위 키와 적대적 키를 가리지 않는다.
 * - **키마다 쓴 시각을 적고 밀어낼 때 훑기**(`_contract/_fixtures/stampScanningCache.ts`) — `get` 은
 *   1.00 고정으로 **통과하고** 밀어내는 `put` 하나에서 1,025 · 4,097 · 16,385 로 걸린다. 자명한 구현이
 *   전 행에서 걸리지 않는 것(불변 사실 84)의 이 계약판이다.
 * - **펴기 값을 그대로 칸으로 쓰기**(`_contract/_fixtures/remainderSlotCache.ts` — 순서의 줄은 정본과
 *   같다) — 무작위 키에서는 두 행 다 통과하고(`put` 7.00 · `get` 3.00 고정), `i * 65536` 키 묶음에서
 *   `put` 1,541.5 · 6,149.5 · 24,581.5 · `get` 770.5 · 3,074.5 · 12,290.5 로 걸린다. 그 키 묶음은 위 주입
 *   정책을 전부 지킨다.
 *
 * **언어의 `Map` 에 기대는 길은 이 판정에서 자명한 구현으로 세지 않는다.** `Map` 은 넣은 순서를
 * 기억하므로 쓸 때마다 지우고 다시 넣으면 맨 앞이 가장 오래 안 쓴 키가 되고, 이 계약의 축1 참조
 * 모델이 정확히 그 모양이다. 그러나 그것은 상한을 배열·객체의 기본 연산에서 **얻는** 것이 아니라 확률
 * 논증을 런타임 안으로 옮기는 것이고(`hash/hashMapChaining` 이 같은 근거로 뺐다), 지운 자리를 건너뛰며
 * 맨 앞을 찾는 비용이 호출마다 상수인지는 런타임의 사정이다(확인 안 함). **등급 규격이 이 자리를
 * 명시하지 않는다는 것을 함께 적는다**(불변 사실 146 — 미결).
 *
 * **필요충분조건.**
 * - 의미: 키로 값을 찾는 사전 · 생성 시점에 정한 용량 · 넘칠 때 **가장 오래 안 쓴 키**를 밀어내기 ·
 *   **맞은 `get` 과 `put` 이 둘 다 쓰기**라는 것. 용량을 버리면 두 행만 남은 사전이고(위 논증), 넘칠 때
 *   새 키를 거절하면 먼저 온 것을 지키는 다른 정책이며, 맞은 `get` 을 쓰기로 치지 않으면 넣은 순서로
 *   밀어내는 캐시이고, 쓴 횟수로 고르면 또 다른 정책이다. 넷 중 용량을 버린 구현과 맞은 `get` 을
 *   치지 않는 구현은 축1이 경계 케이스에서 가르는 것을 fixture 로 확인했다(위 목적 항목). 나머지 둘은
 *   짓지 않았다.
 * - 비용: 맞은 `get` 과 밀어내는 `put` 이 **둘 다** 용량과 무관한 비용이어야 한다. `get` 만 빠르면 쓴
 *   시각을 훑는 캐시이고, 둘 다 용량에 비례하면 사용 순서를 배열에 둔 목록이다 — 어느 쪽이든 용량을
 *   키울수록 호출 하나가 비싸져 경계를 크게 잡는 뜻이 사라진다. 그리고 그 비용이 **호출자가 고른 키에
 *   대해** 성립해야 한다 — 칸을 고정 함수로 정하면 그것을 역산한 키 묶음에서 총비용이 용량의 제곱이 된다.
 */
export class LRUCache<K, V> {
  constructor(capacity: number, spread: (key: K) => number) {
    throw new Error("Not implemented");
  }

  get(key: K): V | null {
    throw new Error("Not implemented");
  }

  put(key: K, value: V): void {
    throw new Error("Not implemented");
  }
}
