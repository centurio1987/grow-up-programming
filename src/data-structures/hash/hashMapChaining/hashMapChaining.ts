/**
 * HashMapChaining — 키 하나에 값 하나를 대응시키고, 그 대응을 키로 찾는 사전. 네 연산이
 * 담긴 수와 **무관한** 비용이고, 그 무관함이 **기대**로 붙는다.
 *
 * **목적.** 키와 값을 짝지어 담고, 키를 주면 그 값을 찾고·바꾸고·지우는 일을 담긴 수와
 * 무관한 비용에 제공하는 것. 여기에 담긴 것 전부를 훑는 일이 담긴 수에 비례하는 비용으로
 * 붙는다. 「키로 찾는다」가 목적의 전부이고, **어떤 키가 어떤 키보다 앞선다는 말은 이 계약에
 * 없다** — 순서를 약속하는 순간 정렬 사전이 되고 상한이 로그로 올라간다.
 *
 * **「기대」가 무엇에 대한 기댓값인지가 이 계약의 거의 전부다.** 둘을 정한다.
 *
 * 1. **구현이 뽑는 무작위성에 대한 기댓값이지 입력에 대한 것이 아니다.** 「입력 분포에 대한
 *    기댓값」으로 읽으면 담을 자리를 **고정된 함수로만** 정하는 구현이 이 계약을 만족한다 —
 *    키가 고르게 들어오면 그런 구현의 기대 비용도 상수이기 때문이다. 그러면 계약이 아무것도
 *    배제하지 못한다. 그래서 이렇게 적는다: **호출자가 어떤 키를 주든**, 구현 자신이 만들어
 *    내는 무작위성에 대해 기댓값이 상한 안이어야 한다.
 * 2. **호출 하나가 아니라 호출 시퀀스에 대한 기댓값이다.** 같은 인스턴스에 n 번 부른
 *    **총비용의 기댓값이 n × 상한 안**이라는 뜻이고, 호출 하나가 담긴 수에 비례해 튀는 것은
 *    계약을 어기지 않는다.
 *
 * **두 번째가 한정자 규칙을 양쪽에서 조인 결과다.** 규칙은 「계약이 배제하려는 것을 배제하는
 * 것 중 가장 약한 것을 적는다」인데(§규약1 「가장 약한 한정자는 셋을 줄 세우지 않는다」),
 * 여기서는 `amortized` 도 호출별 `expected` 도 각자 혼자서는 배제하면 안 되는 것을 배제한다.
 *
 * - `amortized` 는 「**어떤** 입력에도 총비용이 n × 상한 안」이라는 결정론적 약속이라
 *   **무작위를 뽑는 구현을 배제한다** — 뽑은 것이 나쁠 수 있기 때문이다. 그런데 무작위를
 *   뽑는 것이 이 계약이 요구하는 바로 그 일이다.
 * - 호출별 `expected` 는 담을 자리를 **한꺼번에** 다시 정하는 구현을 배제한다 — 그 한 호출의
 *   기대 비용이 담긴 수에 비례한다. 자리를 조금씩 옮기는 구현과 한꺼번에 옮기는 구현 중
 *   하나를 고를 이유가 목적에 없다.
 *
 * 위에 적은 「시퀀스의 기대 총비용」은 **둘 다에 함의된다.** 결정론적으로 총비용이 묶이면
 * 기댓값도 그 값에 묶이고, 호출마다 기댓값이 묶이면 그 합도 묶인다. 그래서 셋 중 가장 약한
 * 것이 이것이고 규칙이 지목하는 것도 이것이다.
 *
 * **다만 이 문장은 한정자 셋 중 하나가 아니다.** 규격이 쓸 수 있는 이름은 `worst`·
 * `amortized`·`expected` 셋뿐이고 「기대 상각」이라 부를 넷째 이름이 없다. 그래서 표에는 뜻이
 * 가장 가까운 `expected` 를 적고 **그 이름이 이 계약에서 무엇을 뜻하는지를 이 항목이 정의한다.**
 * 규격에 이름이 하나 모자란 자리라는 것을 감추지 않고 여기 적어 둔다.
 *
 * **판정 규격이 재는 값도 이 문장과 정확히 같지는 않다.** 축3의 `expected` 통계는 seed 다섯의
 * **시퀀스 평균 다섯의 중앙값**이다(`_contract/judge.ts` 의 `statistic`). 시퀀스로 묶는 부분은
 * 맞지만 중앙값은 기댓값이 아니다 — 표본이 다섯이고 중앙값이 꼬리를 보지 않으므로, 드물게
 * 비용이 아주 큰 무작위를 뽑는 구현이 이 통계를 통과할 수 있다. 계약이 약속하는 것과 축3이
 * 재는 것 사이의 이 틈은 판정 규격의 한계이고, 메우려면 통계를 바꿔야 하는데 그것은 69종
 * 전체에 걸리는 결정이라 이 카드가 정하지 않는다(불변 사실 45 의 셋 중 「가이드」 자리).
 *
 * **이 이름은 계약을 정당화하지 않는다.** 계약의 어느 문장도 사슬이나 탐사를 말하지 않고,
 * 말할 수 없다 — 명세는 내부 표현을 처방하지 않는다. 같은 계약에 이름이 둘 붙어 있고
 * (`hash/hashMapOpenAddressing`) 둘 다 충돌 처리 기법의 이름이다. 계약을 적어 둘 자리로
 * 이쪽을 골랐지만 그 이름도 **임시다** — 계약 이름으로 옮기는 것은 KAN-031 의 일이다
 * (불변 사실 64). 갈라 보려 시도한 것들이 왜 계약의 문장이 못 되는지는 저쪽 파일 맨 앞에
 * 적혀 있다.
 *
 * **`hash/hashSet` 과는 서로 담지 않는다.** 그쪽은 합집합·교집합·차집합을 요구하므로 이
 * 계약의 구현이 그대로 만족하지 못하고, 이쪽은 `get(key)` 로 값을 돌려주므로 값을 담을 자리가
 * 없는 구현이 만족하지 못한다. 어느 쪽도 상대를 담지 않으므로 다른 구조다(불변 사실 54).
 *
 * **이 계약은 시간만 말한다.** 저장 공간에 대한 조건이 없으므로, 담긴 수보다 훨씬 많은 자리를
 * 쓰는 구현도 이 계약을 지킨다(§규약1 「공간은 어느 계약에도 없다」 — 미결). 아래 `keys` 행이
 * 자리 수를 제약하는 것처럼 보이지만 그렇지 않다 — 담긴 것을 따로 이어 두고 열거하는 구현은
 * 자리를 얼마나 쓰든 그 행을 지킨다.
 *
 * **불변식.** 셋이다. 셋 다 **관측 경로가 둘**이라 성립한다 — 구현이 두 경로를 따로 유지할
 * 수 있고, 따로 유지하는 순간 갈린다. 어떤 연산 뒤에도 성립해야 한다.
 * 1. `keys().length === size()`. 담긴 수를 읽는 길이 둘이다.
 * 2. 임의의 키 `k` 에 대해 `has(k)` 는 `keys()` 안에 `k` 가 있는가와 같다. 담김 여부를 읽는
 *    길이 둘이다.
 * 3. 임의의 `i` 에 대해 `values()[i]` 는 `get(keys()[i])` 와 같다. 키와 값의 짝을 읽는 길이
 *    둘이다 — 열거 둘이 서로 어긋난 순서로 나오면 여기서 갈린다.
 *
 * 3번이 불변식인 것은 **열거 순서를 계약이 정하지 않기 때문이다.** 순서를 정하면 그것이
 * 표현을 처방하므로 정하지 않았고, 정하지 않은 이상 축1은 두 열거를 정렬해서 견줄 수밖에
 * 없다. 정렬이 지우는 정보가 정확히 「짝」이고, 그 자리를 축2가 받는다.
 *
 * 담는 모양의 성질(자리가 몇이고 한 자리에 몇이 매달렸는가, 적재율이 얼마인가)은 후보가
 * 아니다. 계약에 적어야 검사할 수 있는데 **적는 순간 그것이 표현을 처방한다**(불변 사실 36).
 *
 * **연산 계약.** n 은 담긴 대응의 수다.
 *
 * | 연산 | 의미 | 상한 | 한정자 |
 * |---|---|---|---|
 * | `constructor(spread)` | 키를 정수로 펴는 함수를 고정한다. 이후 교체할 수 없다 | O(1) | worst |
 * | `set(key, value)` | `key` 에 `value` 를 대응시킨다. 이미 대응이 있으면 **값만 바뀌고 담긴 수는 그대로다** | O(1) | expected |
 * | `get(key)` | 대응된 값. 대응이 없으면 `null` | O(1) | expected |
 * | `has(key)` | 대응이 있으면 `true` | O(1) | expected |
 * | `delete(key)` | 대응을 지우고 `true`. 없으면 `false` 이고 상태가 바뀌지 않는다 | O(1) | expected |
 * | `size()` | 담긴 대응의 수 | O(1) | worst |
 * | `keys()` | 담긴 키 전부의 배열. **순서는 계약이 정하지 않는다.** 사본이다 | O(n) | worst |
 * | `values()` | 담긴 값 전부의 배열. 사본이다 | O(n) | worst |
 *
 * **상한을 `O(1)` 로 적은 것은 목적이 하나로 지목한 값이다.** 고를 여지가 있는 계약도 있지만
 * (§규약1 「상한이 목적에서 유일하게 따라 나오지 않는 계약이 있다」) 여기는 아니다. 축3 사다리가
 * 아는 상한 중 상수 아래는 없고, 한 칸 위인 `O(log n)` 을 적으면 키를 정렬해 두는 구현이 네 행을
 * 만족한다 — 그 순간 이 계약이 정렬 사전과 갈리지 않고, 순서를 약속하지 **않는데** 로그를 무는
 * 구조가 되어 계약이 뜻을 잃는다.
 *
 * **`keys`·`values` 가 `worst` 인 것은 세 한정자가 아무것도 다르게 배제하지 않기 때문이다.**
 * 담긴 것 n 개를 배열에 적는 일은 어느 구현에서도 호출 하나가 n 에 비례하므로 상각할 여지도
 * 무작위성이 개입할 자리도 없다. 그럴 때는 더 말이 되는 쪽을 적는다(§규약1 B9 단서). 이 행이
 * 실제로 배제하는 것은 하나다 — **담긴 수와 무관하게 커진 자리를 훑어야만 열거되는 구현.**
 * 담긴 것을 따로 이어 두면 자리를 얼마나 쓰든 이 행을 지키므로, 이것은 공간 제약이 아니라
 * 열거 비용에 대한 요구다.
 *
 * **`size` 가 `worst O(1)` 인 것은 강하게 적는 쪽을 고른 자리다.** 나가는 계열이 있다 — 담긴
 * 수를 세어 두지 않고 그때그때 훑는 구현. 배제가 정당한 이유는 「아무것도 안 나간다」가 아니라
 * **나가는 계열이 이 계약의 목적과 무관하다**는 것이다. 담긴 수를 상수에 답하는 일은 키를 어떻게
 * 흩는가와 아무 관계가 없다.
 *
 * **`constructor` 가 `worst` 인 것은 근거가 다르다.** 「한 번만 부르니 기댓값이 뜻이 없다」가
 * 아니다 — 한 번 도는 무작위 절차에도 기대 비용은 정의된다. 근거는 둘이다: 이 생성자가 하는
 * 일이 함수 하나를 붙들어 두는 것뿐이고, 축3이 이 행을 재지 않는다(§규약2 축3 면제 — n 에 대해
 * 반복 호출되는 연산이 아니다).
 *
 * **주입 정책.**
 * - 주입받는 것은 **키를 정수로 펴는 함수 하나**이고, 생성자가 고정한다. 선택이 아니라 필수다 —
 *   기본값을 두면 그 기본값이 고정 함수이므로 아래 조건을 계약이 스스로 어기게 된다.
 * - 주입자가 지켜야 할 것 셋. ① **순수해야 한다** — 같은 키에 언제나 같은 정수. ② **키의
 *   동등성과 맞아야 한다** — `Object.is` 로 같은 두 키는 같은 정수여야 한다. ③ **키의 차이를
 *   지우지 말아야 한다.**
 * - **③ 이 없으면 상한이 성립하지 않는다.** 모든 키를 0 으로 보내는 함수는 ①·②를 지키는
 *   정당한 함수인데, 그것을 받은 구현에게는 담긴 키를 가를 수단이 없다 — **어떤 구현도** 그
 *   조건에서 상한을 지킬 수 없다. 그래서 위 표의 네 행은 조건부다: 같은 정수로 가는 키가 담긴
 *   것 중 `c` 개면 그 키들에 드는 비용이 `c` 에 비례한다.
 * - **③ 은 「잘 섞어라」가 아니다.** 흩는 일은 구현의 몫이고, 주입자에게 요구하는 것은 정보를
 *   남기라는 것뿐이다. 둘을 합쳐 주입자에게 좋은 흩기를 요구하면 그 순간 계약이 무너진다 —
 *   주입자가 고른 것은 **고정 함수**이므로 그것을 역산한 키 묶음이 존재하고, 「호출자가 어떤
 *   키를 주든」이 다시 거짓이 된다. 갈라 두는 것이 `expected` 를 성립시키는 자리다.
 * - 돌려주는 정수는 32비트로 읽는다. **완전한 단사를 요구하지 않는다** — 키 공간이 무한이면
 *   32비트로 단사가 불가능하다. 요구는 담긴 키들 사이에서 지켜지면 되고, 안 지켜지는 몫은 위
 *   조건부 문장이 답한다.
 * - 키의 동등성은 `Object.is` 다. 값에는 제약이 없다. **다만 값으로 `null` 을 담으면 `get` 이
 *   대응 없음과 구별되지 않는다** — 담김 여부의 정본은 `has` 다.
 * - 던지는 자리가 없다. 담기지 않은 키를 묻거나 지우는 것은 오류가 아니라 답이 있는 물음이다.
 *
 * **검증 등급.** `complexity`.
 * 판정 절차의 2번에서 걸린다 — **상한이 자명한 구현으로 달성되지 않는다.** 자명한 구현은
 * 「배열·객체의 기본 연산만으로, 재균형·상각 설계·**확률 논증 없이** 상한이 나오는 구현」인데,
 * 이 계약의 상한은 확률 논증에서만 나온다. 무작위를 뽑지 않는 구현은 담을 자리를 키의 함수로만
 * 정하고, 그 함수를 역산한 키 묶음에서 네 행이 담긴 수에 비례한다. 자명한 길 셋을 실제로 지어
 * 재 봤고 셋 다 막힌다.
 *
 * - **차례로 담고 훑기** — 네 행 전부가 선형이다. 담기·찾기가 연산당 512.5 · 2,048.5 · 8,192.5
 *   이고($r$ = 4.00) 있는지 묻기는 768.0 · 3,072.0 · 12,288.0 이다($r$ = 4.00 — 못 찾는 물음이
 *   절반이라 상수 배수만 다르다).
 * - **키를 정렬해 두고 이분 탐색** — 갱신 둘이 선형이다(무작위 순서로 담기 265.2 · 1,036.6 ·
 *   4,124.3, $r$ = 3.91 · 3.98). 조회 둘은 로그이고, 로그는 이 계약의 `O(1)` 을 어기는데
 *   **축3이 그 위반을 못 본다**(§규약2 「계약 위반을 축3이 놓치는 자리」). 그리고 담기는
 *   **오름차순 키에서 통과한다**(10.0 · 12.0 · 14.0) — 끼워 넣을 자리가 늘 맨 뒤라 미는 일이
 *   없기 때문이고, 적대적인 쪽이 늘 더 많이 잡는 것이 아니라는 실물이다.
 * - **펴기 값을 그대로 자리로 쓰기** — 무작위 키에서는 통과하고, `i * 65536` 같은 키 묶음에서
 *   네 행이 선형이 된다(518.5 · 2,054.5 · 8,198.5, $r$ = 3.96 · 3.99). 그 키 묶음은 위 주입
 *   정책이 요구하는 것을 전부 지킨다.
 *
 * **언어가 내주는 사전에 위임하는 길은 이 판정에서 자명한 구현으로 세지 않는다.** 그것은 상한을
 * 배열·객체의 기본 연산에서 **얻는** 것이 아니라 같은 계약을 이미 만족하는 다른 구현을 부르는
 * 것이고, 등급이 묻는 것은 그 상한이 어디서 나오는가다. 위임은 확률 논증을 없애지 않고 런타임
 * 안으로 옮긴다. **다만 등급 규격이 이 자리를 명시하지 않는다는 것을 함께 적어 둔다** — 「위임이
 * 자명한가」는 아직 규격이 말하지 않은 자리이고, 여기서는 위 문장을 근거로 판정했다.
 *
 * **필요충분조건.**
 * - 의미: 키 하나에 값 하나를 대응시키는 일과 그 대응을 **키로** 찾는 일 중 하나라도 버리면 이
 *   구조가 아니다. 값을 버리면 집합이고(`hash/hashSet`), 키의 순서까지 약속하면 정렬 사전이며,
 *   위치로 찾으면 그냥 배열이다.
 * - 비용: 네 행의 **시퀀스 기대 총비용**이 호출 수에 비례하지 않으면 이 계약이 아니다. 담긴
 *   수의 로그에 기대면 정렬 사전이 하는 일을 순서를 약속하지 않고 하는 셈이라 이 구조를 따로
 *   둘 이유가 사라지고, 담긴 수에 비례하면 그냥 목록이다. 그리고 그 총비용이 **호출자가 고른
 *   키에 대해** 성립해야 한다 — 자리를 고정 함수로 정하면 그것을 역산한 키 묶음에서 총비용이
 *   담긴 수의 제곱이 되고, 그때 이것은 사전이 아니라 목록이다.
 */
export class HashMapChaining<K, V> {
  constructor(spread: (key: K) => number) {
    throw new Error("Not implemented");
  }

  set(key: K, value: V): void {
    throw new Error("Not implemented");
  }

  get(key: K): V | null {
    throw new Error("Not implemented");
  }

  has(key: K): boolean {
    throw new Error("Not implemented");
  }

  delete(key: K): boolean {
    throw new Error("Not implemented");
  }

  size(): number {
    throw new Error("Not implemented");
  }

  keys(): K[] {
    throw new Error("Not implemented");
  }

  values(): V[] {
    throw new Error("Not implemented");
  }
}
