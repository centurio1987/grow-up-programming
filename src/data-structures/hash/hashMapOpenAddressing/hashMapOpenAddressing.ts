/**
 * HashMapOpenAddressing — 키 하나에 값 하나를 대응시키고, 그 대응을 키로 찾는 사전. 네 연산이
 * 담긴 수와 **무관한** 비용이고, 그 무관함이 **기대**로 붙는다.
 *
 * **이 계약은 `hash/hashMapChaining` 의 계약과 같다.** 비슷한 것이 아니라 같은 것이다 —
 * 아래 연산 계약 표의 여덟 행이 그 파일의 여덟 행과 글자까지 같고, 스위트도 그 파일의
 * `ContractSpec` 을 그대로 돌린다(`./hashMapOpenAddressing.contract.ts`). 그 사실을 감추지
 * 않고 여기 맨 앞에 적는다 — 이름이 다르다고 계약이 다른 것이 아니다(불변 사실 43).
 *
 * 두 이름을 가르려고 다섯을 시도했고 다섯 다 계약의 문장이 되지 못했다.
 *
 * - **한 자리에 여럿을 매달기 대 옆자리로 밀려나기.** 이것이 두 이름의 실제 차이인데,
 *   **계약에 「자리」라는 말이 없다.** 계약이 보는 것은 호출 하나의 비용이고 두 기법 모두 그
 *   비용이 담긴 수와 무관하다. 자리를 어떻게 쓰는지를 상한으로 적으려면 먼저 자리를 계약이
 *   말해야 하는데, 자리는 내부 표현이라 적는 순간 그것이 처방이다(불변 사실 36).
 * - **적재율.** 사슬로 잇는 쪽은 담긴 수가 자리 수를 넘어도 돌고 밀려나는 쪽은 넘을 수
 *   없으니 진짜 차이다. 그런데 그것은 **공간**이고 이 저장소의 어느 계약도 공간을 말하지
 *   않는다(§규약1 「공간은 어느 계약에도 없다」 — 미결). 공간에서 온 제약이 계약에 들어오는
 *   길이 하나 있는데(**그 제약을 관측하는 연산이 계약에 있을 때** — 불변 사실 67), 이 계약에는
 *   그런 연산이 없다. `linear/circularBuffer` 의 `isFull()` 이 있는 자리가 여기는 비어 있다.
 * - **생성자가 받던 처음 자리 수.** 물려받은 표면 양쪽에 `initialCapacity` 가 있었고 뺐다.
 *   자리 수는 내부 표현이라 계약의 인자가 될 수 없다.
 * - **지운 자리의 뒤처리.** 밀려나는 쪽은 지운 자리를 「지나갈 수는 있는 상태」로 남겨야 하고
 *   사슬 쪽은 그럴 것이 없다. 이것도 담는 모양의 이야기이고, 계약이 보는 `delete` 한 번의
 *   비용은 양쪽 다 상한 안이다.
 * - **캐시 지역성.** 물려받은 문서가 밀려나는 쪽의 장점으로 적어 둔 것이다. **특정 구현을
 *   지목해야만 참인 진술이라 계약이 아니고**(§규약1 「계약의 문장은 구현 전체에 대해 참이어야
 *   한다」), 이 저장소는 벽시계를 쓰지 않으므로 근거를 낼 수단도 없다(불변 사실 7). 참인지
 *   거짓인지 여기서 판정하지 않는다 — 재지 않았다.
 *
 * **그래서 이 구조는 계약이 아니라 충돌 처리 기법으로 갈린다.** 계약이 구현을 처방하지 않기로
 * 한 이상 그 갈림은 계약에 들어올 수 없고, 자리는 가이드다(불변 사실 45·61). 지우지 않는
 * 이유는 판정이 *"계약이 담지 못한다"* 이지 *"쓸모가 없다"* 가 아니기 때문이다.
 *
 * **갈리지 않는다는 것을 수치로 확인했다.** 두 정본이 같은 스위트 열한 시나리오를 **둘 다
 * 전부 통과**하고, 절대 비용은 최대 1.6 배 갈린다(무작위 키 지우기 9.10 대 5.83, 열거
 * 3,072 대 2,048). 그리고 같은 파일에서 적재율 문턱 **하나만** 바꾼 사본 여덟(사슬 넷 ·
 * 탐사 넷)도 전부 통과한다 — 밀어내기 안에서만 봐도 넣기가 8.32(문턱 0.25)에서 15.24(0.9)로
 * 1.8 배이고, 여덟을 통틀면 5.89 에서 15.24 로 2.6 배다. 성장률 $r$ 은 여덟 벌 전부 허용
 * 구간 안이다(불변 사실 80).
 *
 * **계약 이름은 아직 없다.** 같은 계약에 이름이 둘이고 둘 다 구현 이름이다. 계약을 적어 둘
 * 자리로 `hash/hashMapChaining` 을 골랐지만 그 이름도 **임시다** — 계약 이름으로 옮기는 것은
 * KAN-031 의 일이다(불변 사실 64).
 *
 * **목적.** 키와 값을 짝지어 담고, 키를 주면 그 값을 찾고·바꾸고·지우는 일을 담긴 수와 무관한
 * 비용에 제공하는 것. 담긴 것 전부를 훑는 일이 담긴 수에 비례하는 비용으로 붙는다.
 * **어떤 키가 어떤 키보다 앞선다는 말은 이 계약에 없다** — 순서를 약속하면 정렬 사전이 되고
 * 상한이 로그로 올라간다.
 *
 * **「기대」가 무엇에 대한 기댓값인지가 이 계약의 거의 전부다.** 둘을 정한다.
 *
 * 1. **구현이 뽑는 무작위성에 대한 기댓값이지 입력에 대한 것이 아니다.** 「입력 분포에 대한
 *    기댓값」으로 읽으면 담을 자리를 고정된 함수로만 정하는 구현이 이 계약을 만족한다.
 *    그래서 이렇게 적는다: **호출자가 어떤 키를 주든**, 구현 자신이 만들어 내는 무작위성에
 *    대해 기댓값이 상한 안이어야 한다.
 * 2. **호출 하나가 아니라 호출 시퀀스에 대한 기댓값이다.** 같은 인스턴스에 n 번 부른
 *    **총비용의 기댓값이 n × 상한 안**이라는 뜻이다.
 *
 * 두 번째가 한정자 규칙을 양쪽에서 조인 결과다. `amortized` 는 **어떤** 입력에도 총비용이
 * 묶인다는 결정론적 약속이라 무작위를 뽑는 구현을 배제하고, 호출별 `expected` 는 담을 자리를
 * 한꺼번에 다시 정하는 구현을 배제한다. 둘 다 배제할 이유가 목적에 없다. 「시퀀스의 기대
 * 총비용」은 둘 다에 함의되므로 셋 중 가장 약하고, 그것이 규칙이 지목하는 값이다
 * (§규약1 「가장 약한 한정자는 셋을 줄 세우지 않는다」).
 *
 * **그 문장은 한정자 셋 중 하나가 아니고, 판정 규격이 재는 값과도 정확히 같지 않다.** 규격에
 * 「기대 상각」이라 부를 넷째 이름이 없어 표에는 `expected` 를 적고, 축3의 통계는 기댓값이
 * 아니라 반복 단위 열의 시퀀스 평균에 대한 **중앙값**이다. 두 자리의 근거 전문은
 * `hash/hashMapChaining` 헤더의 같은 항목에 있다.
 *
 * **이 기법에서 그 「시퀀스」가 더 두꺼운 뜻을 가진다.** 사슬 쪽에서 한꺼번에 다시 정하는 일은
 * 자리가 모자랄 때만 일어나는데, 밀려나는 쪽은 **지운 자리가 쌓여도** 다시 정해야 한다. 지운
 * 자리를 지나갈 수 있게 남겨 두지 않으면 그 자리를 지나야 닿는 키를 잃어버리고, 남겨 두면
 * 그것이 표를 채운다. 그래서 다시 짓는 일이 넣기뿐 아니라 **넣고 지우기를 되풀이하는 것만으로**
 * 일어나고, 그 몫까지 시퀀스가 흡수한다.
 *
 * **`hash/hashSet` 과는 서로 담지 않는다.** 그쪽은 합집합·교집합·차집합을 요구하므로 이
 * 계약의 구현이 그대로 만족하지 못하고, 이쪽은 `get(key)` 로 값을 돌려주므로 값을 담을 자리가
 * 없는 구현이 만족하지 못한다(불변 사실 54).
 *
 * **이 계약은 시간만 말한다.** 저장 공간에 대한 조건이 없다(§규약1 「공간은 어느 계약에도
 * 없다」 — 미결). 위에서 적재율이 계약의 문장이 되지 못한 것과 같은 자리다.
 *
 * **불변식.** 셋이다. 셋 다 **관측 경로가 둘**이라 성립한다 — 구현이 두 경로를 따로 유지할
 * 수 있고, 따로 유지하는 순간 갈린다. 어떤 연산 뒤에도 성립해야 한다.
 * 1. `keys().length === size()`. 담긴 수를 읽는 길이 둘이다.
 * 2. 임의의 키 `k` 에 대해 `has(k)` 는 `keys()` 안에 `k` 가 있는가와 같다. 담김 여부를 읽는
 *    길이 둘이다.
 * 3. 임의의 `i` 에 대해 `values()[i]` 는 `get(keys()[i])` 와 같다. 키와 값의 짝을 읽는 길이
 *    둘이다.
 *
 * 셋이 사슬로 잇는 계약의 셋과 같은 것은 베낀 것이 아니라 **판별 절차가 같은 답을 내기
 * 때문이다**(§규약1 「불변식 판별 절차」). 불변식은 상태의 성질이고 충돌 처리는 담는 모양의
 * 이야기라 서로를 건드리지 않는다. **이 기법에서 2번이 특히 잘 깨진다** — 지운 자리를
 * 「비었다」로 표시하면 그 자리를 지나야 닿는 키가 `has` 에서만 사라지고 `keys()` 에는 남는다.
 * 그래도 그것이 이 계약의 불변식인 근거는 기법이 아니라 판별 절차다.
 *
 * 자리가 몇이고 적재율이 얼마인가는 후보가 아니다. 계약에 적어야 검사할 수 있는데 **적는 순간
 * 그것이 표현을 처방한다**(불변 사실 36).
 *
 * **연산 계약.** n 은 담긴 대응의 수다.
 *
 * | 연산 | 의미 | 상한 | 한정자 |
 * |---|---|---|---|
 * | `constructor(spread)` | 키를 정수로 펴는 함수를 고정한다. 이후 교체할 수 없다 | O(1) | worst |
 * | `set(key, value)` | `key` 에 `value` 를 대응시킨다. 이미 대응이 있으면 **값만 바뀌고 담긴 수는 그대로다** | O(1) | expected |
 * | `get(key)` | 대응된 값. 대응이 없으면 `null` | O(1) | expected |
 * | `has(key)` | 대응이 있으면 `true` | O(1) | expected |
 * | `delete(key)` | 대응을 지우고 `true`. 없으면 `false` 이고 상태가 바뀌지 않는다 | O(1) | expected |
 * | `size()` | 담긴 대응의 수 | O(1) | worst |
 * | `keys()` | 담긴 키 전부의 배열. **순서는 계약이 정하지 않는다.** 사본이다 | O(n) | worst |
 * | `values()` | 담긴 값 전부의 배열. 사본이다 | O(n) | worst |
 *
 * **상한 `O(1)` 은 목적이 하나로 지목한 값이다.** 상수 아래는 축3 사다리에 없고, 한 칸 위인
 * `O(log n)` 을 적으면 키를 정렬해 두는 구현이 네 행을 만족한다 — 그 순간 순서를 약속하지
 * **않는데** 로그를 무는 구조가 되어 계약이 뜻을 잃는다.
 *
 * **`keys`·`values` 가 `worst` 인 것은 세 한정자가 아무것도 다르게 배제하지 않기 때문이다.**
 * n 개를 배열에 적는 일은 어느 구현에서도 호출 하나가 n 에 비례한다(§규약1 B9 단서). 이 행이
 * 배제하는 것은 **담긴 수와 무관하게 커진 자리를 훑어야만 열거되는 구현**이고, 담긴 것을 따로
 * 이어 두는 구현은 자리를 얼마나 쓰든 이 행을 지킨다 — 그래서 공간 제약이 아니다.
 *
 * **`size` 가 `worst O(1)` 인 것은 강하게 적는 쪽을 고른 자리다.** 담긴 수를 세어 두지 않고
 * 훑는 구현이 나가는데, 그 계열은 이 계약의 목적과 무관하다. **밀려나는 쪽에서는 이 행이 한
 * 걸음 더 요구한다** — 살아 있는 수와 지운 자리를 합친 수가 다르므로 둘 중 어느 것을 세고
 * 있는지가 답을 가른다. 그것도 계약의 문장이 아니라 구현의 일이다.
 *
 * **`constructor` 가 `worst` 인 것은 근거가 다르다.** 이 생성자가 하는 일이 함수 하나를 붙들어
 * 두는 것뿐이고, 축3이 이 행을 재지 않는다(§규약2 축3 면제).
 *
 * **주입 정책.**
 * - 주입받는 것은 **키를 정수로 펴는 함수 하나**이고, 생성자가 고정한다. 선택이 아니라 필수다 —
 *   기본값을 두면 그 기본값이 고정 함수이므로 아래 조건을 계약이 스스로 어기게 된다.
 * - 주입자가 지켜야 할 것 셋. ① **순수해야 한다** ② **키의 동등성과 맞아야 한다**(`Object.is`
 *   로 같은 두 키는 같은 정수) ③ **키의 차이를 지우지 말아야 한다.**
 * - **③ 이 없으면 상한이 성립하지 않는다.** 모든 키를 0 으로 보내는 함수는 ①·②를 지키는
 *   정당한 함수인데, 그것을 받은 구현에게는 담긴 키를 가를 수단이 없다. 그래서 위 표의 네
 *   행은 조건부다: 같은 정수로 가는 키가 담긴 것 중 `c` 개면 그 키들에 드는 비용이 `c` 에
 *   비례한다.
 * - **③ 은 「잘 섞어라」가 아니다.** 흩는 일은 구현의 몫이다. 둘을 합쳐 주입자에게 좋은 흩기를
 *   요구하면 그 순간 계약이 무너진다 — 주입자가 고른 것은 고정 함수이므로 그것을 역산한 키
 *   묶음이 존재하고, 「호출자가 어떤 키를 주든」이 다시 거짓이 된다.
 * - 돌려주는 정수는 32비트로 읽는다. **완전한 단사를 요구하지 않는다** — 키 공간이 무한이면
 *   32비트로 단사가 불가능하다. 안 지켜지는 몫은 위 조건부 문장이 답한다.
 * - 키의 동등성은 `Object.is` 다. 값에는 제약이 없다. **다만 값으로 `null` 을 담으면 `get` 이
 *   대응 없음과 구별되지 않는다** — 담김 여부의 정본은 `has` 다.
 * - 던지는 자리가 없다. 담기지 않은 키를 묻거나 지우는 것은 오류가 아니다.
 *
 * **검증 등급.** `complexity`. 계약이 같으므로 등급도 같다 — 등급은 계약에서 기계적으로 따라
 * 나온다(불변 사실 56).
 * 판정 절차의 2번에서 걸린다 — 상한이 자명한 구현으로 달성되지 않는다. 자명한 구현은
 * 「배열·객체의 기본 연산만으로, 재균형·상각 설계·**확률 논증 없이** 상한이 나오는 구현」인데,
 * 이 계약의 상한은 확률 논증에서만 나온다. 자명한 길 셋을 지어 재 봤고 셋 다 막힌다 — 차례로
 * 담고 훑으면 네 행이 선형이고($r$ = 4.00), 키를 정렬해 두면 갱신 둘이 선형이며($r$ = 3.91 ·
 * 3.98), 펴기 값을 그대로 자리로 쓰면 `i * 65536` 같은 키 묶음에서 네 행이 선형이 된다
 * ($r$ = 3.96 · 3.99). 근거 전문과 「언어가 내주는 사전에 위임하는 길」에 대한 판정은
 * `hash/hashMapChaining` 헤더의 같은 항목에 있다.
 *
 * **필요충분조건.**
 * - 의미: 키 하나에 값 하나를 대응시키는 일과 그 대응을 **키로** 찾는 일 중 하나라도 버리면 이
 *   구조가 아니다. 값을 버리면 집합이고(`hash/hashSet`), 키의 순서까지 약속하면 정렬 사전이며,
 *   위치로 찾으면 그냥 배열이다.
 * - 비용: 네 행의 **시퀀스 기대 총비용**이 호출 수에 비례하지 않으면 이 계약이 아니다. 담긴
 *   수의 로그에 기대면 정렬 사전이 하는 일을 순서를 약속하지 않고 하는 셈이고, 담긴 수에
 *   비례하면 그냥 목록이다. 그리고 그 총비용이 **호출자가 고른 키에 대해** 성립해야 한다.
 */
export class HashMapOpenAddressing<K, V> {
  constructor(spread: (key: K) => number) {
    throw new Error("Not implemented");
  }

  set(key: K, value: V): void {
    throw new Error("Not implemented");
  }

  get(key: K): V | null {
    throw new Error("Not implemented");
  }

  has(key: K): boolean {
    throw new Error("Not implemented");
  }

  delete(key: K): boolean {
    throw new Error("Not implemented");
  }

  size(): number {
    throw new Error("Not implemented");
  }

  keys(): K[] {
    throw new Error("Not implemented");
  }

  values(): V[] {
    throw new Error("Not implemented");
  }
}
