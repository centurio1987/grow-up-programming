/**
 * PieceTable — 자리를 인자로 받아 여러 원소를 한 번에 넣고 지우는 수열. 편집 한 번의 값이 담긴 수가 아니라 지금까지의 편집 수에 묶인다.
 *
 * **목적.** 원소를 순서대로 늘어놓고, **임의 자리에 원소 여럿을 한 번에 끼워 넣기**와 **임의 구간을 한 번에 지우기**를 제공하되
 * 편집 한 번의 값이 **담긴 원소 수 n 에 기대지 않고 지금까지 부른 편집의 수 m 에 묶이게** 하는 것. 긴 수열을 한 번에 넣고 몇 군데만
 * 고치는 쓰임에서 고치는 값이 수열 길이와 무관하다 — 이 계약의 자리가 그것이다. 수열 전체를 읽는 일은 담긴 수에 편집 수를 더한 값이다.
 *
 * 한 원소를 자리로 읽는 연산(`at(i)`)은 이 계약에 없다. 값으로 찾기 · 정렬 · 비교도 없다 — 이 계약은 원소를 비교하지 않는다.
 * 담긴 원소 수를 읽는 일도 없다.
 *
 * **뺀 행의 판정은 목적 항목 밖에 적는다.** `length()` 는 원칙 A 판정표 ③ 의 판정으로 뺐다 — 판정 줄과 목적 인용은
 * `docs/ORD-006-conventions.md` 「판정표 ③ 의 103 행을 묶어 판정했다」 절에, 행 단위 대장은
 * `tools/_baseline/principle-a-verdicts.tsv` 에 있다. 판정의 결론을 목적 문단 안에 적으면 다음 판정이 자기 메아리를
 * 근거로 읽는다(§원칙 A A5′-2).
 * 원칙 A 로는 `at` 이 `toArray` 로 조합되고, 목적의 비용 조건이 그보다 싼 상한을 요구하지 않으며(A2 · A4), **위 「목적」이 적은 일
 * 셋(끼워 넣기 · 지우기 · 수열 전체 읽기) 어디에도 한 자리만 읽는 일이 없다**(A5′). 값 찾기 · 정렬은 목적이 사용을 적지 않은 고유
 * 연산이다(A3 — `docs/ORD-006-conventions.md` 「원칙 A」 판정표 ① 53).
 *
 * **`linear/gapBuffer` 와 같은 계약인가 — 이 유닛의 첫 물음이었고, 답은 「같지 않다」이다**(불변 사실 90). 두 이름 모두 「공간이 존재
 * 이유」 표시를 달고 왔고(`docs/ORD-006-p4-triage.md`), 그 표시를 물음으로 바꾸지 않고 존재 이유를 계약의 말로 먼저 다시 적었다(불변
 * 사실 188).
 *
 * - **원본을 고치지 않는다 · 넣은 원소는 덧붙이기만 한다 · 조각 목록** — 셋 다 계약의 문장이 아니다. 조각은 내부 표현이라 적는 순간
 *   처방이고(금지 1 · 불변 사실 36), 앞의 둘은 공간에서 온 제약인데 **그것을 관측하는 연산이 이 계약에 없다**(불변 사실 67). 절차의
 *   셋째 걸음을 실제로 돌렸다 — 담긴 원소를 제자리에서 옮기고 덮어 쓰는 구현
 *   (`src/data-structures/_contract/_fixtures/cursorGapSequence.ts`)이 축1 경계 케이스 일곱과 무작위 500 회를 값 한 자리 안 틀리고
 *   통과한다. 이 자리만 보면 B15 처분의 모양이다(불변 사실 61).
 * - **그런데 계약은 선다 — 존재 이유가 비용의 문장으로 적힌다.** 「편집 한 번이 수열 길이가 아니라 편집 이력에 묶인다」는 구현을
 *   지목하지 않고 쓰이고, 그 제약이 사는 축은 축3이다(불변 사실 187). 위 fixture 가 축3 에서 걸린다 — 긴 수열의 앞과 끝을 번갈아 한
 *   개씩 넣으면 호출 하나가 2,050 · 8,194 이고 이 계약의 정본은 73 · 73 이다.
 *
 * **두 계약은 서로 담지 않는다**(불변 사실 54). 두 설계를 한 표면에서 같은 두 입력에 넣었다
 * (`src/data-structures/_contract/runContract.pieceTable.test.ts`).
 *
 * | 입력 | 조각 목록(이 계약의 정본) | 빈 구간 옮기기(`gapBuffer` 의 설계) |
 * |---|---|---|
 * | 긴 수열의 앞 · 끝 · 가운데 · 무작위 자리를 돌며 한 개씩 32 번 — 호출 하나의 최대 | 73 · 73 | **2,050 · 8,194** |
 * | 긴 수열의 한 자리에 한 개씩 n 번 — 호출 평균 | **516.5 · 2,052.5** | 4.5 · 4.5 |
 *
 * 첫 줄이 이 계약의 약속이고(편집이 적으면 먼 편집도 싸다) 둘째 줄이 `gapBuffer` 의 약속이다(한 자리 언저리의 편집은 거듭해도 싸다 —
 * 그 계약의 `insert` 행 `amortized O(1)`). **각 설계가 한쪽을 지키고 다른 쪽을 어긴다.** 조각 목록이 둘째 줄에서 비싼 것은 한 자리에
 * 거듭 넣으면 조각이 편집 수만큼 늘기 때문이고, 그것은 이 계약의 상한 $O(m)$ 안이다. 두 약속을 **함께** 지키는 구현은 있을 것으로 본다
 * — 조각을 이어 두고 마지막 편집 자리의 조각을 기억하는 구현 — 그러나 **짓지 않았다**(확인 안 함). 그래서 관계를 불변 사실 181(동시에
 * 만족하는 구현이 없다)로도, 199(있다)로도 적지 않는다.
 *
 * **「같지 않다」의 범위.** 위 두 입력은 **지금 두 헤더의 연산 · 시간 상한과 한정자 · 주입 모형 안에서** 낸 반례다 — 이쪽 편집 행
 * `worst O(m + k)` · `O(m)` 과 저쪽 커서 자리 편집 `amortized O(1)` · 커서 옮기기 `O(d)` 의 약속을 자리를 인자로 받는 한 표면의 두 입력으로
 * 옮겨 두 설계를 넣었고(`docs/ORD-006-conventions.md` 「공간 표시가 붙은 두 이웃은 같은 표면의 두 입력으로 가른다」), 주입받는 것은 양쪽 모두
 * 없다. 그러므로 판정은 이 범위에서 선 것이고, 범위 밖 조항이 두 계약에 더해지면 **반례로 쓴 두 설계가 새 조항까지 지키는지부터** 다시
 * 본다 — 지키지 못하면 그 방향의 반례가 사라진다.
 *
 * - **공간** — 두 이름이 달고 온 「공간이 존재 이유」 표시의 본래 내용(원본을 복사하지 않는다 · 넣은 원소를 모아 두는 저장량)은 두 계약
 *   모두에 없다(아래 「이 계약은 시간만 말한다」). 두 설계가 공간에서 어떻게 갈리는지 이 판정은 말하지 않는다.
 * - **되돌리기 · 옛 수열 묻기** — 둘 다 없다. 넣으면 원본 보존이 관측되어 공간 제약이 계약에 들어오므로(불변 사실 98) 관계를 그 표면에서 다시
 *   낸다.
 * - **동시 접근 · 캐시 지역성** — 두 계약 모두 단일 스레드 호출열의 시간만 말하고, 지역성은 구현 하나를 지목하는 진술이라 계약이 아니다(불변
 *   사실 38).
 * - **연산 집합이 바뀌면** — 이쪽에 커서를 들이거나 저쪽에 자리 인자 편집을 들이면 두 표면의 관계를 다시 낸다. 두 약속을 함께 지키는 구현이
 *   있는가는 지금 범위 안에서도 확인하지 않았다(위 문단).
 *
 * **`PieceTable` 은 물려받은 구현 이름이고 이 계약의 이름이 아니다**(불변 사실 64 · 198). 계약이 말하는 것을 그대로 부르면 「편집
 * 수에 묶인 자리 편집 수열」이다. 게다가 **이 이름이 보통 가리키는 설계가 이 계약의 한 행을 어긴다** — 원본 하나와 추가 버퍼 하나를
 * 두고 그 버퍼를 한 줄로 두 배씩 늘리면, 늘리는 호출 하나가 지금까지 넣은 원소 전부를 옮긴다(아래 「연산 계약」 · 불변 사실 92 ·
 * §「이름이 가리키는 설계가 그 이름의 계약을 어길 수 있다」). 이름 재편은 KAN-031 이다.
 *
 * **이 계약은 시간만 말한다.** 원본을 복사하지 않는다는 이득도, 넣은 원소를 모아 두는 저장량도 계약에 없다(§규약1 「공간은 어느
 * 계약에도 없다」 — 미결). **되돌리기(undo)도 이 계약에 없다.** 넣으면 옛 수열을 끝까지 물을 수 있다는 약속이 생기고, 그것이 이
 * 계약에 없는 원본 보존을 관측하게 해 공간 제약이 계약에 들어온다(`range-query/persistentSegmentTree` 의 「잊지 말라」 모양). 목적이 그
 * 연산을 요구하지 않아 넣지 않았다(불변 사실 47 · 98).
 *
 * **되돌리기 부재는 연산 선정과 따로 판정했다**(`docs/ORD-006-conventions.md` 「원칙 A」 판정표 ② — KAN-027 `S27`). ① 되돌리기는
 * 다른 공개 연산으로 조합되지 않는 **고유 변경**이라 원칙 A 로는 A3 하나가 남고, 목적 문장(편집 수에 묶인 자리 편집)이 그 사용을 적지
 * 않는다 — 이름의 설계가 흔히 되돌리기와 함께 쓰인다는 것은 근거가 아니다(불변 사실 64 · A6 ①). ② 넣으면 되돌리기를 거듭해 첫 수열까지
 * 돌아갈 수 있어야 해 **지난 편집을 버리지 말라**가 그 연산의 뜻으로 관측되고(`disjoint-set/disjointSetRollback` 의 되돌리기와 같은 모양),
 * 조각을 합쳐 줄이거나 제자리에서 덮어 쓰는 구현이 전부 나가 편집 행의 비용 논증도 기록을 들고 다시 서야 한다 — 다른 계약이다.
 * ③ 그 계약과 `linear/gapBuffer` 는 지금의 반례(위 표 첫 줄 — 먼 편집)로 그대로 갈리고 되돌리기가 새 반례를 더하지 않는다(산술 — 짓지 않았다). 비용은
 * 편집 한 번이 바꾼 조각을 기록하는 몫이다 — 넣기는 상수 개, 지우기는 걷어낸 조각 수라 편집 행 상한 $O(m)$ 안이다(산술). 목적이 바뀌지 않는 한 부재를 유지하고, 불변 사실 98 이 사람 결정으로 넘긴
 * 자리는 이 판정으로 닫는다.
 *
 * **불변식.** 없다.
 *
 * 판별 절차(§규약1 「불변식 판별 절차」)를 **한 행을 뺀 표면에 다시 돌린 결과다.** 하나 있던 불변식 「`length()` 가 말하는 원소
 * 수와 `toArray()` 가 내놓는 배열의 길이가 같다」는 세어 둔 쪽이 표면에서 사라져 **경로가 `toArray()` 하나**로 줄었고, 첫 번째
 * 물음에서 걸린다. 조각마다 원소 수를 따로 드는 구현이 두 수를 따로 들다 갈리는 자리는 이제 늘어놓기와 참조 모델의 대조에
 * 나타나므로 축1이 본다. 남은 표면에서 관측 경로가 둘인 성질은 새로 서지 않는다.
 *
 * 떨어진 후보 둘을 함께 적는다.
 *
 * - **「`toArray()` 가 내놓는 순서가 편집의 역사와 맞는다」** — 순서를 읽는 공개 연산이 `toArray()` 하나라 첫 물음에서 걸린다. 수열
 *   전체는 축1이 참조 모델과 대조한다.
 * - **「조각 수가 편집 수의 두 배를 넘지 않는다」** — 조각은 계약의 말이 아니다(불변 사실 36). 편집이 조각을 몇 개 만드는지는
 *   정본 머리말이 적는다.
 *
 * **연산 계약.** n 은 담긴 원소 수, **m 은 이 구조에 지금까지 부른 `insert`·`delete` 호출의 수**(아무것도 안 바꾼 호출은 세고, 던진
 * 호출은 세지 않는다), **k 는 그 `insert` 가 받은 원소 수**다. m 은 호출자가 스스로 셀 수 있는 값이라 계약의 말이다 —
 * `disjoint-set/disjointSetRollback` 이 되돌리는 단위를 호출로 센 것과 같은 자리다.
 *
 * | 연산 | 의미 | 상한 | 한정자 |
 * |---|---|---|---|
 * | `constructor()` | 빈 수열로 연다 | O(1) | worst |
 * | `insert(offset, items)` | `items` 의 원소를 그 순서대로 `offset` 자리에 끼워 넣고 그 뒤를 `items.length` 칸씩 민다. 직후 `toArray()` 의 `offset` 째부터가 `items` 다. `offset` 은 0 이상 담긴 원소 수 이하의 정수여야 하고, 아니면 `RangeError` 를 던지며 상태는 불변. 빈 배열이면 아무것도 바꾸지 않는다 | O(m + k) | worst |
 * | `delete(offset, count)` | `offset` 째부터 `count` 개를 지우고 그 뒤를 당긴다. `offset`·`count` 가 0 이상의 정수이고 `offset + count` 가 담긴 원소 수 이하여야 하며, 아니면 `RangeError` 를 던지고 상태는 불변. `count` 가 0 이면 아무것도 바꾸지 않는다. 지운 원소를 돌려주지 않는다 | O(m) | worst |
 * | `toArray()` | 앞에서부터 순서대로 담은 배열의 사본. 고쳐도 이 구조는 바뀌지 않는다 | O(n + m) | worst |
 *
 * **`delete` 의 상한에 지우는 수가 없는 것이 이 계약의 둘째 내용이다.** 긴 구간을 지우는 한 번이 그 길이에 기대지 않는다. 원소를
 * 하나씩 지우는 구현(`src/data-structures/_contract/_fixtures/itemwisePieceTable.ts`)이 그 행에서 걸리고, **지운 원소를 돌려주지 않는
 * 것**도 같은 이유다 — 돌려주면 그 배열을 채우는 일이 지우는 수에 비례한다.
 *
 * **편집 행이 `worst` 인 근거는 둘이고, 둘째가 이 계약에서 새로 나왔다**(불변 사실 91).
 *
 * 1. **`amortized` 로 적으면 목적이 반증되지 않는다.** 상각은 호출열 전체의 약속이다(§「상각 행의 약속은 호출열 전체로 적는다」). 긴
 *    수열을 한 번에 넣은 호출이 k = n 만큼의 몫을 쌓아 두므로, 그 뒤 **상수 번의** 편집이 각각 n 에 비례해도 호출열 전체로는 상한
 *    안이다. 「긴 수열에서 몇 군데만 고치면 싸다」가 목적인데 바로 그 쓰임에서 약속이 비어 버린다.
 * 2. **`amortized` 로 적으면 축3이 그 끝을 잴 수 없다.** 상각 시나리오는 n 회를 재야 하고(§규약2 시나리오 규칙 4) 편집을 n 번 하면 m
 *    이 n 이 되어 「편집 적게」 끝이 사라진다. 상한의 파라미터가 **호출의 역사**라서 생긴 자리다(§「상한의 파라미터가 호출 이력이면
 *    `amortized` 는 그 파라미터의 작은 끝을 잴 수 없다」).
 *
 * **`worst` 가 추가로 배제하는 계열과, 배제해도 잃는 것이 없다는 근거**(불변 사실 59 · 183). 배제되는 것은 **넣은 원소를 한 줄에 이어
 * 쓰고 모자라면 두 배로 늘리는 설계**다(`src/data-structures/_contract/_fixtures/doublingBufferPieceTable.ts`). 같은 입력(n 개 한 번에 +
 * 한 개씩 32 번)을 호출마다 m + k 에 대한 비로 재면, 호출열 전체로는 1.69 · 1.89 · 1.97 로 상한 안이고 한 호출로는 342.67 · 1,366.67 ·
 * 5,462.67 로 밖이다. **잃는 것이 없다** — 넣기마다 받은 원소를 제 배열 하나에 두면 넣기 한 번의 일이 k 에 묶이고, 정본이 그렇게 해
 * 두 읽기가 1.06 · 1.02 · 1.01 / 2.19 로 둘 다 안이다. 그리고 축3의 단일 호출 최대가 그 강함을 잰다(§「한정자의 강한 읽기는 축이 그
 * 강함을 잴 때만 적는다」). **런타임이 배열을 늘리는 일은 계측이 세지 않으므로**(§규약2 계측 단위) 추가 버퍼를 언어 배열의 `push` 로
 * 쓰면 이 차이가 안 보인다 — fixture 는 칸을 손으로 옮겨 세는 언어의 모양을 옮겼다.
 *
 * **상한을 $O(m)$ 으로 고른 근거**(불변 사실 48 · 51). 약하게 적으면 — 담긴 수 n 이 상한에 들어오면 — 수열을 한 줄로 들고 뒤를 미는
 * 배열이 들어와 목적이 사라진다. 강하게 적으면 — $O(\log m)$ — 조각을 차례로 훑는 구현이 전부 나가는데, 조각을 균형 트리에 두라는
 * 처방이 될 뿐 그 배제를 요구하는 것이 목적에 없다. **정본이 서는 계급으로 정했다.** 담긴 수의 로그만 드는 구현(균형 트리에 원소를
 * 담는 구현)은 편집이 상수 번일 때 이 행을 **어긴다** — 계약의 문장이 「담긴 수에 기대지 않는다」이기 때문이다. 그 차이는 로그 인수라
 * 축3이 못 보고(불변 사실 53 · 62) 그 구현을 짓지도 않았다.
 *
 * **축3이 이 표를 무엇으로 판정하는가**(§「표의 상한과 시나리오의 bound 가 다르면 헤더가 그 판정을 적는다」). 파라미터마다 끝을
 * 고정한다(§「상한의 파라미터가 n 이 아니면 시나리오가 그 파라미터를 양 끝에 고정한다」).
 *
 * - `insert` — m 이 64 이하 · k = 1 인 끝을 `O(1)`(정본 73 · 73), k = n 인 끝을 `O(n)`(1,037 · 4,109)으로.
 * - `delete` — m 이 64 이하 · 지우는 수 = n 인 끝을 `O(1)`(6 · 6)로.
 * - `length` — 끝에 하나씩 붙여 m = n 인 끝을 `O(1)`(1 · 1)로. 편집이 적으면 조각도 적어 **길이를 조각마다 더해 세는 구현**이 상수로
 *   통과하므로 이 끝에서만 잰다.
 * - `toArray` — m = 1 인 끝과, 앞에서 하나씩 n 번 지워 m = n 인 끝을 둘 다 `O(n)`(1,026 · 4,098)으로. **편집을 적어 두었다 열거할 때
 *   되풀이하는 구현**이 뒤의 끝에서만 걸린다(1,576,449 · 25,180,161).
 * - **편집 행의 m = n 끝은 재지 않는다.** 그 끝에서만 걸리는 결함 계열을 찾지 못했고(불변 사실 57), 걸리는 것은 조각을 균형 트리에 두는
 *   구현처럼 계약보다 빠른 것뿐이다(불변 사실 49). 그래서 **정본이 편집 행에서 편집 수에 비례한다는 것을 축3이 재지 않는다** — 위 둘째
 *   입력 표(한 자리에 n 번 516.5 · 2,052.5)가 그 수치다.
 *
 * **주입 정책.**
 * - 주입받는 것이 없다. `T` 는 임의 타입이고 비교 · 해시 · 동등성을 쓰지 않는다.
 * - **생성자가 인자를 받지 않는다.** 물려받은 표면은 원본 텍스트를 받았는데, 받은 원본으로 여는 것은 빈 수열에 `insert(0, items)` 를
 *   한 번 부른 것과 같은 상태이고 그 호출의 상한 $O(k)$ 와 같은 구현을 배제한다 — 따로 두어 계약이 더 말하는 것이 없다(원칙 A 의
 *   A2 · A4, **위 「목적」이 「원본을 들고 열기」를 사용으로 적지 않아** A5′ — 판정표 ① 51). 원본을
 *   **복사하지 않고** 붙들어 상수에 여는 길은 호출자에게 「넘긴 배열을 다시 고치지 말라」는 의무를 거는 일이라 두지 않았다.
 * - **넘긴 배열을 붙들지 않는다.** `insert` 뒤에 호출자가 `items` 를 고쳐도 담긴 수열은 바뀌지 않고, `toArray()` 가 돌려준 배열을 고쳐도
 *   마찬가지다. 사본을 뜨는 값이 k 이고 그것이 `insert` 상한에 k 가 있는 이유 하나다.
 * - `offset`·`count` 의 범위 밖은 `RangeError` 다. **잘라 맞추지(clamp) 않는다** — 물려받은 문서는 넣을 자리를 「0 이상 담긴 원소 수 이하」로 자르고
 *   지울 구간을 끝까지로 잘랐는데, 그러면 「그 자리에 넣었다」와 「끝에 붙었다」가 같은 관측이 되어 호출자가 자기 인자가 유효했는지 알 수
 *   없다(`linear/gapBuffer` 의 `moveCursor` 와 같은 처분). 위쪽 끝이 담긴 원소 수 자신인 것은 넣을 자리가 원소가 아니라 원소 사이이기
 *   때문이다.
 * - 원소를 문자로 좁히지 않는다. 물려받은 표면은 문자열을 받고 돌려줬는데(`getText()`), 어느 행도 원소가 문자라는 것을 쓰지 않는다.
 *   전체를 읽는 연산의 이름을 `toArray()` 로 바꾼 것도 같은 이유다.
 *
 * **검증 등급.** `invariant` **〔`S3` 표시 — 이 배치가 불변식 절을 비웠다. 등급 재판정과 그에 딸린 네 자리는 `S5` 가
 * 맞춘다. 그때까지 아래 마지막 문장은 낡은 채로 선다.〕**
 * 상한이 자명한 구현으로 달성된다 — **넣은 원소를 넣기마다 배열 하나에 두고, 수열을 「어느 배열의 몇째부터 몇 개」인 쌍의 배열로 드는
 * 구현**이 네 행을 전부 상한 안에 한다. 넣기가 그 쌍 배열을 앞에서부터 훑어 자리를 찾고 한 쌍을 둘로 가른 뒤 끼우는 일이고, 지우기가
 * 두 끝에서 가른 뒤 사이의 쌍을 빼는 일이다. 쌍의 수가 편집 수의 두 배를 넘지 않는다. 재균형도 상각 설계도 확률 논증도 없다 — 정본이
 * 그 구현이다. 불변식 절이 비어 있지 않으므로 `basic` 도 아니다. 등급이 `linear/gapBuffer` 와 같은 것은 판정의 입력이 아니다(불변 사실
 * 56 의 역은 성립하지 않는다 · 불변 사실 95).
 *
 * **필요충분조건.**
 * - 의미: 자리를 **인자로** 받아 원소 **여럿을 한 번에** 넣고 구간을 한 번에 지워야 한다. 편집 자리가 구조 안에 이름을 갖고 살아 있으면
 *   커서를 가진 수열이고(`linear/gapBuffer`), 원소를 하나씩만 넣으면 긴 수열을 만드는 일 자체가 편집 n 번이라 m 이 n 보다 작아지는 쓰임이
 *   없어진다 — 그러면 편집 수에 묶인다는 약속이 담긴 수에 묶인다는 약속과 같아진다.
 * - 비용: 편집 한 번이 담긴 수에 비례하면 수열을 한 줄로 드는 배열이다. 그리고 긴 구간을 지우는 한 번이 **지우는 수**에 비례하면, 긴
 *   수열을 한 번에 넣을 수는 있어도 한 번에 치울 수는 없는 비대칭이 남는다 — 두 편집 행이 함께 편집 수에만 묶여야 이 계약이다.
 */
export class PieceTable<T> {
  insert(offset: number, items: readonly T[]): void {
    throw new Error("Not implemented");
  }

  delete(offset: number, count: number): void {
    throw new Error("Not implemented");
  }

  toArray(): T[] {
    throw new Error("Not implemented");
  }
}
