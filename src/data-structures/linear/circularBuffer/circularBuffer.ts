/**
 * CircularBuffer — 담을 수 있는 수가 정해진 수열. 다 찬 뒤의 쓰기는 가장 오래된 것을 밀어낸다.
 *
 * **목적.** 원소를 **도착한 순서 그대로** 내보내되, 담을 수 있는 수가 **생성 시점에 정해져
 * 있고 그 경계가 관측되는** 수열을 제공하는 것. 경계에 닿았는지는 `isFull()` 이 답하고,
 * 닿은 뒤의 쓰기가 무엇을 하는지는 아래 `write` 행이 정한다 — 새것을 거절하는 것이 아니라
 * **가장 오래된 것을 밀어낸다.** 다섯 행이 모두 호출 하나당 담긴 수와 무관한 비용이다.
 *
 * 임의 위치 접근·탐색·순회는 이 계약에 없다. 뒤 끝을 보는 일, 앞 끝에 넣는 일도 없다.
 * 비었는지 묻는 일도 없다 — 그 답은 `size()` 가 돌려준 수로 호출자가 짓는다.
 *
 * **뺀 행의 판정은 목적 항목 밖에 적는다.** `isEmpty()` 는 원칙 A 판정표 ③ 의 판정으로 뺐다 —
 * 판정 줄과 목적 인용은 `docs/ORD-006-conventions.md` 「판정표 ③ 의 103 행을 묶어 판정했다」
 * 절에, 행 단위 대장은 `tools/_baseline/principle-a-verdicts.tsv` 에 있다. 판정의 결론을 목적
 * 문단 안에 적으면 다음 판정이 자기 메아리를 근거로 읽는다(§원칙 A A5′-2).
 * **용량을 나중에 바꾸는 연산도 없다** — 있으면 「생성 시점에 정해져 있다」가 거짓이 되고,
 * 그 순간 아래 반례가 서지 않는다.
 *
 * **고정 용량은 저장량 조건이 아니라 관측되는 성질이라서 계약에 들어온다.** 여기서 두
 * 문장이 함께 참이므로 갈라 적는다.
 *
 * - **이 계약은 저장량을 상한으로 적지 않는다.** 용량의 열 배로 칸을 잡는 구현도 여섯 행을
 *   전부 지킨다. 적을 수 **없어서**가 아니다 — `O(n)` 공간 같은 문장은 구현을 지목하지 않고도
 *   쓸 수 있다. 적지 않는 이유는 그것이 69종 전체에 걸리는 규격 결정이고 아직 열려 있다는
 *   것이며(§규약1 「공간은 어느 계약에도 없다」 — 미결), 넣더라도 지금은 검사할 축이 없다는
 *   것이다(축3은 `__cost` 로 시간만 잰다).
 * - **그런데 용량 제약 자체는 계약 안에 있다.** 저장량을 거치지 않고 들어온다 — 적는 것이
 *   저장량이 아니라 **경계에서 연산이 무엇을 하는가**이기 때문이다. 그 경계를 관측하는 것은
 *   셋이다: `isFull()` 이 닿았는지 답하고, `size()` 행이 「용량을 넘지 않는다」를 적고,
 *   `write` 행이 닿은 뒤의 쓰기가 무엇을 밀어내는지 적는다. 그래서 **담을 수 있는 수를
 *   무시하고 계속 담는 구현은 이 계약을 만족하지 못한다** — 공간을 더 쓰기 때문이 아니라
 *   용량을 넘긴 뒤의 `size()`·`isFull()`·`read()` 가 계약이 정한 값과 다르기 때문이다.
 *
 * **`linear/queue` 와 시간 상한을 하나도 쓰지 않고 갈린다.** 큐 계약의 세 행은 전부
 * `O(1)` 이고 여기 다섯 행도 전부 `O(1)` 이다. 상한 값이 하나도 다르지 않은데 **두 계약을
 * 동시에 만족하는 구현은 하나도 없다.** 불변 사실 54 의 「서로 담지 않는다」보다 강한 관계다.
 *
 * 논증은 계약의 문장만 쓰고 **어느 정본도 보지 않는다**(불변 사실 43 이 요구하는 형태다).
 * 이름을 맞춰 놓고(`write`↔`enqueue` · `read`↔`dequeue` · `peek`↔`front`) 용량이 `c` 인
 * 이 계약을 만족하면서 큐 계약도 만족하는 구현이 있다고 하자. 빈 상태에서 `c + 1` 회 넣고
 * `c + 1` 회 뺀다. 큐의 `enqueue` 행은 *"이 원소는 지금 담겨 있는 것들이 전부 나간 뒤에
 * 나온다"* 고 적으므로(`src/data-structures/linear/queue/queue.ts:34`) 넣은 `c + 1` 개가 전부
 * 순서대로 나와야 하고, 마지막 빼기도 값을 돌려준다. 그런데 이 계약의 `write` 행은 꽉 찬 뒤의
 * 쓰기가 앞 끝 원소를 밀어내고 **다시 관측되지 않는다**고 적고 `size` 행이 「용량을 넘지
 * 않는다」라, 담긴 것이 `c` 개이므로 `c + 1` 번째 빼기는 `null` 이다. 두 요구가 같은 호출에
 * 다른 값을 요구하므로 그런 구현은 없다.
 *
 * **한정자도 갈리지만 그것은 두 번째 이유다.** 큐의 넣기·꺼내기는 `amortized` 이고 여기는
 * `worst` 다. 그 갈림도 결국 고정 용량에서 온다(아래 한정자 문단). 다만 **한정자를 큐와
 * 똑같이 맞춰 놓아도 위 논증은 그대로 선다** — 논증이 상한도 한정자도 쓰지 않기 때문이다.
 *
 * **불변식.** 없다.
 *
 * 판별 절차(§규약1 「불변식 판별 절차」)를 **한 행을 뺀 표면에 다시 돌린 결과다.** 결과는
 * 그대로 「없다」다. 남은 후보 셋이 다 **두 번째 물음에서** 걸린다 — 관측 경로는 둘 이상인데
 * 그 정합을 아래 계약 표가 이미 적고 있다. 넷째 후보였던 「`isEmpty()` 와 `size()` 가 맞는다」는
 * `isEmpty()` 를 빼면서 **후보 자체가 사라졌다.**
 *
 * - `isFull()` 과 `size()` — 표가 `isFull()` 의 의미를 「`size()` 가 용량과 같다」로 적는다.
 * - `peek()` 과 `read()` — 표가 `peek()` 행에 「직후 `read()` 가 같은 값을 돌려준다」를 적는다.
 * - 「`size()` 가 용량을 넘지 않는다」 — `write` 행이 꽉 찬 뒤의 쓰기를 「밀어낸다」로 적고
 *   있으므로 그 행의 의미에서 따라 나온다.
 *
 * 「담긴 것이 도착 순서로 늘어서 있다」도 후보인데 같은 자리에서 걸린다 — 경로는 `peek()`
 * 과 되풀이하는 `read()` 로 둘이지만, 그 정합을 `peek` 행이 이미 적고 있다(세 번째 줄과
 * 같은 자리다). 되풀이한 `read()` 가 내놓는 수열 전체는 축1이 참조 모델과 대조한다.
 *
 * **담는 모양의 성질은 후보가 아니다.** 「가장 오래된 것을 가리키는 번호가 용량 미만이다」
 * 같은 조건은 자료구조 교과서의 클래스 불변식이지만 **이 계약의 불변식이 아니다** — 그런
 * 번호를 드는 구현을 계약이 요구하지 않고, 요구하지 않는 것의 성질은 적을 수 없다. 적는
 * 순간 그것이 표현을 처방한다(불변 사실 36). 여기서 「불변식」이 뜻하는 것은 **공개 연산으로
 * 관측 가능하면서 각 연산이 국소적으로 옳아도 깨질 수 있는 상태 성질**뿐이다.
 *
 * **연산 계약.** n 은 **생성자가 정한 용량**이다. 담긴 원소 수가 아니라 미리 정해진 자리의
 * 수이고, 구조가 사는 동안 바뀌지 않는다. 담긴 수는 언제나 n 이하이므로 두 값을 나눠 적어도
 * 상한 값이 갈리지 않는다 — 그럴 때는 계약이 고정한 쪽을 적는다.
 *
 * | 연산 | 의미 | 상한 | 한정자 |
 * |---|---|---|---|
 * | `constructor(capacity)` | 담을 수 있는 수를 `capacity` 로 정하고 빈 상태로 연다. `capacity` 는 1 이상의 정수여야 한다 | O(n) | worst |
 * | `write(item)` | `item` 을 뒤 끝에 놓는다. 놓기 전 `isFull()` 이 참이면 **앞 끝 원소가 밀려 나가고 다시 관측되지 않으며** `size()` 는 그대로다. 거짓이면 `size()` 가 하나 는다 | O(1) | worst |
 * | `read()` | 앞 끝 원소를 제거하고 돌려준다. 비어 있으면 `null`, 상태는 불변 | O(1) | worst |
 * | `peek()` | 앞 끝 원소를 제거하지 않고 돌려준다. 비어 있으면 `null`. 직후 `read()` 가 같은 값을 돌려준다 | O(1) | worst |
 * | `isFull()` | `size()` 가 생성자가 받은 용량과 같다 | O(1) | worst |
 * | `size()` | 현재 원소 수. 용량을 넘지 않는다 | O(1) | worst |
 *
 * **`write` 가 값을 돌려주지 않는 것은 정당하다.** 물을 것은 「그 약속이 관측되는가」이고
 * (불변 사실 82), 밀어냈는지는 쓰기 직전 `isFull()` 이 이미 답하며 밀려난 결과는 `size()`
 * ·`peek()`·`read()` 가 전부 관측한다. 관측되지 않는 약속이 없으므로 반환값을 붙일 이유가
 * 없다.
 *
 * **`isFull()` 의 근거는 「없으면 큐가 된다」가 아니다 — 그 문장은 거짓이다.** 외부 검토가
 * 이 자리를 잡았고, 초고에 적혀 있던 그 근거를 여기서 바로잡는다. `isFull()` 을 지워도
 * `size` 행의 「용량을 넘지 않는다」와 `write` 행의 뒷문장이 남아 위 논증이 그대로 서므로,
 * 남는 다섯 행을 큐가 만족하지 못한다. 옳은 근거는 **구조를 건네받은 쪽의 관측**이다 —
 * 용량을 묻는 연산이 계약에 없으므로, 생성자를 부르지 않은 쪽은 `size()` 가 돌려준 수가
 * 경계인지 판정할 수단이 없다. `isFull()` 이 그 판정을 계약 안으로 들여놓는 유일한 행이고,
 * 그것이 없으면 「경계에 닿았는가」가 계약 밖의 지식(생성자에 넘긴 값)으로만 답해진다.
 * **다만 이것은 고른 것이다** — `capacity()` 를 두어도 `size()` 와 함께 같은 일을 하고
 * 오히려 더 많이 말한다. 물려받은 표면이 `isFull()` 이고 이 계약이 필요로 하는 것이 경계
 * 판정 하나뿐이라 더 적게 말하는 쪽을 남겼다(§규약1 「상한은 고르는 값이다」와 같은 자리다).
 *
 * **다섯 행이 모두 `worst` 인 것은 고정 용량에서 따라 나온다.** 계약이 배제하려는 것은
 * **원소를 옮기는 구현**이다 — 앞을 비우려고 뒤를 당기거나 그 반대로 미는 계열. 그 계열은
 * 호출마다 옮기므로 n 회 총비용이 이차가 되고, 따라서 **`amortized O(1)` 이 이미 그것을
 * 배제한다**(실측: 옮기는 fixture 의 상각 평균이 1,025 → 4,097 로 $r = 4.00$ 이다).
 * 그러므로 `amortized` 가 「배제하려는 것을 배제하는 것 중 가장 약한 것」이고, 더 강한
 * `worst` 를 적으려면 그것이 **추가로** 배제하는 계열을 배제해도 계약이 잃는 것이 없어야
 * 한다(§규약1 「가장 약한 한정자는 셋을 줄 세우지 않는다」). 그 계열은 **자리를 나중에 늘리는
 * 구현**이다 — 옮기는 계열과 달리 늘리는 호출만 담긴 수에 비례하고 나머지는 상수라, 상각
 * 평균이 상수로 남아 `amortized` 를 통과한다. 배제해도 되는 이유는 그 계열이 서 있는 전제가
 * 여기서 성립하지 않는다는 것이다: 그 계열은 「얼마나 필요한지 미리 모른다」를 전제로 하는데
 * **이 계약은 미리 안다.** 용량이 생성자 인자이고, 그만큼의 자리를 잡는 대가를 `constructor`
 * 행이 `O(n)` 으로 이미 지고 있다. 큐가 `amortized` 를 적어야 했던 것은 담길 수에 상한이
 * 없어 그 계열을 살려야 했기 때문이고, 여기서는 살릴 계열이 없다.
 *
 * **이 판정은 공간이 계약에 들어오면 다시 열린다.** 늘리는 계열이 아끼는 것은 저장량인데
 * 저장량이 계약에 없어서 「잃는 것이 없다」가 성립했다. 저장량을 상한으로 적는 규격 결정이
 * 나면 그 계열이 계약이 지키려는 것을 하나 갖게 되므로, 그때 이 행의 한정자를 다시 본다
 * (불변 사실 61 이 `trie/ternarySearchTree` 의 처분에 붙인 단서와 같은 자리다).
 *
 * **주입 정책.**
 * - 주입받는 것이 없다. `T` 는 임의 타입이고 비교·해시·동등성을 쓰지 않으므로 주입할 것이
 *   없다.
 * - `capacity` 는 **1 이상의 정수**여야 하고, 아니면 `RangeError` 를 던진다. 0 을 허용하지
 *   않는 이유는 그때 `isFull()` 이 언제나 참이면서 `size()` 도 언제나 0 이라 두 관측이 서로를
 *   구분하지 못하고, `write` 직후에도 `peek()` 이 `null` 이라 「쓴 것을 읽는다」가 어느
 *   순서로도 성립하지 않기 때문이다. 담을 수 없는 수열은 이 계약의 대상이 아니다.
 * - 빈 버퍼에서 `read`·`peek` 은 예외를 던지지 않고 `null` 을 돌려준다 — 이 선택은 계약의
 *   일부이며, 그 대가로 `T` 에 `null` 이 섞이면 "비어 있음"과 "값이 `null` 인 원소"가
 *   구분되지 않는다. 반환 타입에 「없음」을 담을 자리가 있으므로 예외로 답하지 않는다.
 *
 * **검증 등급.** `basic`.
 * 상한이 자명한 구현으로 달성된다 — **용량만큼의 칸을 잡아 두고 「가장 오래된 것이 놓인
 * 칸」과 「몇 개 있는가」를 정수 둘로 기억하면** 여섯 행이 전부 상한 안이다. 재균형도 상각
 * 설계도 확률 논증도 없다. 자명한 구현 중에 상한을 넘는 것이 있지만(담긴 것을 배열 앞부터
 * 순서대로 들고 앞을 실제로 지우면 `read` 와 꽉 찬 뒤의 `write` 가 담긴 수에 비례한다)
 * 그것은 등급을 바꾸지 않는다 — 판정이 묻는 것은 *달성하는 자명한 구현이 있는가*이지
 * *전부 달성하는가*가 아니다(불변 사실 55). 불변식 절이 비어 있으므로 `invariant` 도 아니다.
 *
 * **`linear/queue` 와 등급이 같은 것은 판정의 입력이 아니다.** 등급이 다르면 계약이 같을 수
 * 없다는 것의 **역은 성립하지 않는다**(불변 사실 56). 두 계약을 가르는 것은 등급이 아니라
 * 위 목적 항목이 든 반례 둘이다.
 *
 * **필요충분조건.**
 * - 의미: 담을 수 있는 수가 **정해져 있고 그 경계가 어느 행에서든 관측되어야** 한다. 경계가
 *   없으면 큐다. 경계가 있는데 **어느 행도 그것을 말하지 않으면** — `size` 행이 상한을 적지
 *   않고, `write` 행이 다 찬 뒤의 일을 적지 않고, 닿았는지 물을 연산도 없으면 — 그 경계는
 *   계약의 문장이 아니라 **구현의 사정**이고, 남는 계약은 다시 큐다. 세 자리 중 **하나만
 *   남아도 큐가 아니다**(위 논증이 `size` 행 하나로 선다). 그리고 다 찬 뒤의 처분이
 *   「밀어낸다」가 아니라 「거절한다」면 그것도 다른 계약이다 — 오래된 것이 새것보다 값지다고
 *   정한 것이라 무엇이 담기는지가 갈린다.
 * - 비용: `write` 나 `read` 가 담긴 수에 비례하면 자리를 돌려 쓰지 않고 원소를 옮기고 있는
 *   것이다. 그러면 **고정 용량이 주는 유일한 값이 사라진다** — 담긴 수가 용량까지 자라도
 *   호출 하나의 비용이 그대로라는 것이 이 계약의 비용 쪽 내용이고, 옮기는 구현에서는 용량을
 *   키울수록 호출 하나의 비용도 함께 커진다.
 */
export class CircularBuffer<T> {
  constructor(capacity: number) {
    throw new Error("Not implemented");
  }

  write(item: T): void {
    throw new Error("Not implemented");
  }

  read(): T | null {
    throw new Error("Not implemented");
  }

  peek(): T | null {
    throw new Error("Not implemented");
  }

  isFull(): boolean {
    throw new Error("Not implemented");
  }

  size(): number {
    throw new Error("Not implemented");
  }
}
