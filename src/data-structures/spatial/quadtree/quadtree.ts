/**
 * Quadtree — 평면의 점을 여러 벌 담고, 축에 나란한 사각형 범위 질의를 담긴 점 수의 제곱근과 답의 수에 두는 점 색인.
 *
 * **목적.** 평면(2차원)의 점을 담고 **자리에 대한 물음 하나**에 답한다 — 「**이 닫힌 사각형 안에 담긴 점은 무엇인가**」(범위 질의).
 * **가까움은 묻지 않는다.** 이 계약이 좌표에 하는 일은 축마다 견주는 것뿐이고 두 점 사이의 거리를 셈하는 자리가 없다. 그래서 좌표는
 * **유한한 수면 무엇이든** 되고(아래 「주입 정책」 — 정의역이 이 물음에서 따라 나온다), 답은 어느 셈법으로도 같다.
 *
 * 비용으로는, 범위 질의가 답이 없어도 담긴 점 전부를 지나지 않아야 하고(담긴 수의 제곱근 + 답의 수) 넣기는 담긴 수의 로그 제곱 안에
 * 상각된다. 점을 배열에 붙이고 물을 때마다 훑는 구현은 넣기가 상수이지만 가는 띠 하나를 묻는 데 점 전부를 지난다 — 이 계약의 자리가
 * 그 차이다.
 *
 * **차원 · 좌표 폭 · 답의 수 — 상한에 무엇이 들어오는가.** `spatial/kdTree` 헤더의 같은 절과 판정이 같다.
 * - **차원은 2 로 고정한다.** 차원 d 를 열면 같은 계열의 범위 질의가 $O(n^{1-1/d} + k)$ 이고, 판정 규격(`judge.ts` 의 `Bound`)에 그
 *   계급이 없으며 기존 구간으로는 d = 4 부터 선형 구현과 갈리지 않아 열지 않았다(`spatial/kdTree` 헤더). 네 갈래라는 이름도 2차원의 것이다.
 * - **좌표 폭 W 는 상한에 없다.** 상한은 담긴 점 수 n 과 답의 수 k 만 말하고 W 와 점 사이의 간격은 끝없이 키울 수 있는 것으로 읽는다.
 *   **이 절이 이 계약에서 가장 무겁다** — 칸의 가운데로 네 등분하는 설계(아래 「정본 이름은 임시다」)는 깊이가 담긴 수가 아니라
 *   간격과 폭에 기대고, 그 차이가 범위 행을 어기는 입력으로 드러난다. 스위트는 W 를 n 과 같은 배율로 사다리에 싣는다(채우기가 0 이상
 *   4n 미만의 정수 좌표). W 에만 기대는 계열은 짓지 않았다(확인 안 함).
 * - **답의 수 k 는 범위 질의 상한에 들어온다.** 스위트가 k 를 양 끝에 고정한다 — 답이 없는 가는 띠(k = 0)와 전부 담는 사각형(k = n).
 *
 * **`spatial/kdTree` 와는 서로 담지 않는다**(B19 확정 「다르다」 · 불변 사실 54). 그쪽은 이쪽 두 연산을 같은 이름 · 같은 뜻 · 같은
 * 상한 · 같은 한정자로 갖고 최근접을 하나 더 가지며, 점의 정의역을 절댓값 $2^{25}$ 이하의 정수로 좁힌다(거리를 정확히 견주려고).
 * - **같은 호출에 다른 답** — `insert([0.5, 2])` 뒤 `rangeSearch([0, 0], [1, 3])`. 이쪽은 `[[0.5, 2]]` 이고 그쪽은 넣기가 `RangeError`
 *   라 `[]` 다. 그쪽 정본이 이쪽 스위트의 축1 에서 정수가 아닌 점을 넣는 자리(경계 케이스 하나 · 무작위 시퀀스 3번째 연산인 넣기)에서 갈리고
 *   시나리오 셋은 통과한다.
 * - **이쪽 정본에는 최근접이 없다** — B19 표의 반례(「범위만 하는 공간 색인은 뒤를 만족하고 앞을 만족하지 못한다」).
 * - **한쪽이 상한을 어기는 입력은 없다.** 겹치는 두 행의 비용 열이 같다. 그리고 그쪽 최근접 행(`worst O(n)`)은 느린 쪽으로 아무 구현도
 *   배제하지 않는다 — 이쪽 정본에 좌표 검사와 훑는 최근접을 붙인 구현이 그쪽 스위트를 전부 통과한다
 *   (`src/data-structures/_contract/runContract.quadtree.test.ts`). 두 계약이 갈리는 자리는 **목적이 묻는 물음의 수**다 — 그쪽 목적은
 *   가까움까지 묻고 이쪽은 묻지 않으며, 정의역이 그 뒤를 따른다.
 *
 * **이 계약에 없는 것 다섯.**
 * - **경계 사각형과 칸 용량.** 물려받은 생성자는 경계(`boundary`)와 칸에 담을 점 수(`capacity`)를 받고 경계 밖 점의 넣기를 `false`
 *   로 거절했다. 용량은 담는 모양의 값이다(불변 사실 4 — 원칙 A 의 A1). 경계는 넣기의 뜻(거절)을 바꾸므로 표현이 아니라 A2 로 가고,
 *   **호출자가 넣기 전에 사각형과 견주면 같은 거절을 얻는다**(A2 조합). 경계 검사는 어느 구현이든 상수에 하므로 떨어지는 계열이
 *   없고(A4), **위 「목적」의 물음이 「담을 수 있는 자리인가」를 묻지 않는다**(A5′ — `docs/ORD-006-conventions.md` 「원칙 A」 판정표 ①
 *   64). 경계가 필요했던 것은 뿌리 칸을 미리 정하는 설계였다. 그래서 생성자는 인자를 받지 않고 `insert` 는 `void` 다.
 * - **담긴 점 수 읽기.** 원칙 A 의 A2 · A4 · A5′ 로 빠진다 — 넣은 수는 호출자가 알고, 세어 두는 상수 보강으로 떨어지는 계열이 없으며,
 *   **위 「목적」의 물음이 담긴 수를 묻지 않는다**(판정표 ① 66). 물려받은 `size()` 를 뺐다.
 * - **지우기 · 최근접.** 물려받은 표면에 없다. 지우기는 목적이 그 사용을 적지 않아 두지 않는다(A3 · 불변 사실 47). 최근접도 같은
 *   줄이다 — **위 「목적」이 「가까움은 묻지 않는다」로 그 물음을 밖에 둔다**(A5′ — 판정표 ① 67). 범위 질의로 조합되는 편의 연산이라
 *   비용으로 더할 이유도 없다. 더하면 목적이 묻는 물음이 하나 늘어 **다른 계약**이 되고 정의역도 거리 셈에 맞춰 좁아진다.
 * - **범위 질의 답의 순서.** 정하면 답을 늘어놓는 일이 k 에 대해 선형보다 무거워질 수 있다(삽입 정렬로 늘어놓으면 전부 담는 사각형에서
 *   걸린다 — `spatial/kdTree` 헤더의 수치). 축1 은 답을 (x, y) 순으로 정규화해 견준다.
 * - **공간.** 이 계약은 시간만 말한다(§규약1 「공간은 어느 계약에도 없다」 — 미결).
 *
 * **정본 이름은 임시다 — 그리고 이 이름이 가리키는 설계가 이 계약을 어긴다**(불변 사실 64). 「쿼드트리」는 보통 칸을 **칸의 가운데로**
 * 네 등분하는 설계를 부르는데, 그 설계는 칸의 깊이가 점 사이 간격에 기대 가는 띠 양옆에 점이 몰리면 띠를 가로지르는 칸이 담긴 수에
 * 비례한다 — `src/data-structures/_contract/_fixtures/regionQuadtree.ts` 가 이 스위트의 가는 띠에서 246 · 728 · 2,632 로 걸린다. 이 계약의
 * 정본은 네 갈래로 나누되 **점의 순위로** 나눈다(`_reference/quadtree.ts`). 계약 이름과 카테고리 `spatial/` 은 KAN-031(계약 이름 재편)로
 * 넘긴다.
 *
 * **불변식.** 없다. 담긴 점을 읽는 공개 연산이 범위 질의 **하나**라 대조할 상대가 없고(§규약1 「불변식 판별 절차」 ①), 답에 대한 조건
 * (사각형 안의 점을 빠짐없이 · 넣은 벌 수만큼)은 그 연산의 의미라 축1 이 본다. 칸 · 균형 같은 담는 모양의 성질은 후보가 아니다(불변
 * 사실 36). `spatial/kdTree` 가 불변식 하나를 갖는 것은 최근접이 담긴 점을 읽는 둘째 길이기 때문이다.
 *
 * **연산 계약.** n 은 담긴 점의 수(넣은 벌 수), k 는 그 범위 질의가 돌려주는 점의 수다. 좌표 폭은 상한에 없다(위 「목적」).
 *
 * | 연산 | 의미 | 상한 | 한정자 |
 * |---|---|---|---|
 * | `constructor()` | 빈 색인으로 연다 | O(1) | worst |
 * | `insert(point)` | 점 `point` 를 한 벌 더 담는다. 같은 점이 이미 있어도 한 벌 더다. 좌표가 유한한 수가 아니면 `RangeError` 이고 상태가 바뀌지 않는다 | O(log² n) | amortized |
 * | `rangeSearch(min, max)` | `min[0] ≤ x ≤ max[0]` 이고 `min[1] ≤ y ≤ max[1]` 인 담긴 점을 **벌마다 하나씩** 새 배열로 돌려준다. 순서는 정하지 않는다. 모서리가 뒤집히면 든 점이 없어 빈 배열이다 | O(sqrt n + k) | worst |
 *
 * **축3이 이 표를 무엇으로 판정하는가**(§「표의 상한과 시나리오의 bound 가 다르면 헤더가 그 판정을 적는다」). 시나리오 셋이
 * `spatial/kdTree` 스위트의 것과 **같은 객체**다.
 * - **넣기 $O(\log^2 n)$ → `O(log n)`**(대각선 넣기 n 번 · 호출 평균). 정본 113.44 · 155.03 · 202.63(비율 1.37 · 1.31). 로그 인수 하나는
 *   해상도 아래라 로그 한 번과 로그 제곱이 이 행에서 갈리지 않는다(불변 사실 53).
 * - **범위 질의 $O(\sqrt n + k)$ → 시나리오 둘.** 점 절반을 줄 여덟의 양옆에 몰아 채우고 답이 없는 가는 띠를 `O(sqrt n)` 으로(정본 113 ·
 *   205 · 365), 전부 담는 사각형을 `O(n)` 으로(1,365 · 5,461 · 21,845) 판정한다. 제곱근에 로그 인수가 붙는 구현은 통과한다.
 *
 * **상한 근거**(불변 사실 48 · 51 · 65). `spatial/kdTree` 헤더의 같은 두 행과 근거가 같다.
 * - **범위 질의 $O(\sqrt n + k)$ 는 정본이 서는 계급이다.** 목적이 자르는 것은 「답이 없어도 담긴 수에 비례하는 구현」이고, 더 강하게
 *   로그 거듭제곱 + k 로 적으면 영역을 나누는 구현 계열이 전부 나가는데 배제할 이유가 목적에 없다(그 계열의 흔한 대가는 공간이다). 그런
 *   구현은 가는 띠에서 계급 아래로 걸린다(불변 사실 49).
 * - **넣기 $O(\log^2 n)$ 도 정본의 계급이다.** $O(n)$ 으로 적으면 동적 색인이라는 목적이 사라지지만 $O(\sqrt n)$ 쯤으로 적어도 목적은
 *   산다 — 이 행을 로그 제곱으로 적는 이유는 상한이 정본의 계급과 같아야 한다는 것 하나다.
 * - 물려받은 상한(넣기 「O(log n) 평균 — capacity 와 공간 분포에 따라 다름」, 질의 「O(log n + k) 평균」)은 입력으로 쓰지 않았다(불변
 *   사실 65). 둘 다 점의 분포에 대한 말이라 호출자가 어떤 점을 주든 참인 문장이 아니고(불변 사실 104), 질의의 「log n + k」는 칸의
 *   가운데로 나누는 설계가 띠 양옆에 몰린 점에서 지키지 못한다(위 fixture).
 *
 * **한정자.** 셋은 줄 세워지지 않으므로(불변 사실 59) 강한 쪽을 적으려면 추가로 배제하는 계열이 있고 배제해도 잃는 것이 없어야 한다.
 * - **넣기는 `amortized` 다 — `worst` 로 적으면 정본이 나간다**(층을 모두 합치는 한 호출이 담긴 수의 로그 배다).
 * - **범위 질의 `worst` 가 추가로 배제하는 계열은 넣기를 쌓아 두었다 질의 때 한꺼번에 넣는 구현이다** — 수치는 같은 fixture 를 잰
 *   `spatial/kdTree` 헤더 「한정자」에 있다. 이 정본은 채운 뒤 가는 띠 n 번을 전부 재도 호출마다 125 · 241 · 489 라 배제해도 잃는 것이
 *   없다.
 * - `constructor` 는 n 에 대해 반복 호출되지 않아 두 한정자가 다르게 배제하는 것이 없다.
 *
 * **물려받은 표면을 다섯 고쳤다.**
 * - 스텁의 private 필드 아홉(경계 · 용량 · 점 목록 · 나뉨 표시 · 네 자식 · 크기)과 헤더 · 문제 문서의 「용량을 넘으면 NW/NE/SW/SE 로
 *   4분할한다」 · 알고리즘 절을 뺐다 — 담는 모양의 처방이다(불변 사실 4 · 20).
 * - 생성자 인자 둘과 `size()` 를 뺐고 `insert` 의 `boolean` 반환을 `void` 로 바꿨다(위 「이 계약에 없는 것」).
 * - **`query(range: Rect)` 를 `rangeSearch(min, max)` 로 바꿨다.** `{ x, y, width, height }` 는 오른쪽 끝이 `x + width` 라는 **덧셈의
 *   결과**여서, 좌표가 유한한 수 전부인 이 계약에서 반올림이 경계를 정하고 두 구현이 같은 사각형을 다르게 읽는다(`x + width` 를 셈하는
 *   구현과 `px − x ≤ width` 로 견주는 구현). 모서리 둘로 받으면 셈이 없다. 이름은 같은 연산을 가진 `spatial/kdTree` 와 맞췄다 — 한
 *   구현을 두 스위트에 껍데기 없이 넣을 수 있다.
 * - **점의 정의역을 정했다**(유한한 수). 물려받은 제약 「−10^6 ≤ x, y ≤ 10^6」 은 경계 사각형이 대신하던 것이라 경계와 함께 뺐다.
 * - 「`width > 0`, `height > 0` 보장」 전제가 경계와 함께 빠졌고, 뒤집힌 사각형은 빈 배열로 전면화했다(불변 사실 82).
 *
 * **주입 정책.**
 * - **점의 두 좌표는 유한한 수여야 한다.** NaN · 무한이면 `RangeError` 이고 상태가 바뀌지 않는다. 정수가 아니어도 · 아주 커도 된다.
 *   **정의역을 좁히지 않는 이유**(§「주입 정책도 상한과 같은 두 물음으로 정한다」): 이 계약은 좌표를 견주기만 하므로 넓혀도 나가는 구현이
 *   없고, 좁히면 호출자만 나간다. `spatial/kdTree` 가 정수로 좁힌 이유(거리의 반올림)가 여기에는 없다.
 * - **`rangeSearch` 의 모서리는 NaN 이 아닌 수면 된다**(무한 포함). NaN 이면 `RangeError` 다 — 빈 배열은 「든 점이 없다」라는 답이라
 *   「사각형이 정해지지 않았다」를 겹칠 자리가 아니다.
 * - 구조는 호출자가 넘긴 배열을 붙들지 않고, 돌려준 배열을 고쳐도 담긴 점은 그대로다.
 * - 비교 · 좌표 읽기를 주입받지 않는다.
 *
 * **검증 등급.** `complexity`.
 * 넣기와 범위 질의 두 행을 **함께** 만족하는 자명한 구현이 없다. 배열에 붙이고 훑으면 가는 띠가 담긴 수에 비례하고
 * (`src/data-structures/_contract/_fixtures/scanningPointSet.ts`), 한 좌표 순으로 늘어놓은 배열은 다른 좌표로 좁은 띠에서 담긴 수에
 * 비례하며(`src/data-structures/_contract/_fixtures/xSortedRunsPointSet.ts`), 넣을 때마다 견주며 매다는 나무는 대각선으로 넣으면 사슬이
 * 된다(`src/data-structures/_contract/_fixtures/unbalancedPointTree.ts`) — 세 fixture 가 이 스위트와 같은 시나리오 객체에서 걸린다. 칸의
 * 가운데로 나누는 설계는 자명하지 않은데도 간격에 기대 걸린다. 두 좌표를 **함께** 점의 순위로 줄여 나누는 균형을 넣기 사이에 지키는
 * 설계가 들어와야 하고, 그것이 자명하지 않다는 것이 이 등급의 내용이다.
 *
 * **필요충분조건.**
 * - 의미: 평면의 유한한 좌표 점 · 여러 벌 담기 · 닫힌 사각형 범위 질의. **가까움까지 묻기 시작하면**(그리고 정의역을 정수로 좁히면)
 *   `spatial/kdTree` 이고, 한 좌표만 남기면 정렬 집합의 구간 읽기다.
 * - 비용: **넣기가 로그 제곱에 상각되면서 범위 질의가 답이 없을 때 담긴 수의 제곱근 안**이어야 한다. 넣기만 빠르면 훑는 배열이고, 질의만
 *   빠르면 넣을 때마다 다시 짓는 정적 색인이며, 점 사이 간격에 기대면 가운데로 나누는 칸이다.
 */
export type Point2D = [number, number];

export class Quadtree {
  constructor() {
    throw new Error("Not implemented");
  }

  insert(point: Point2D): void {
    throw new Error("Not implemented");
  }

  rangeSearch(min: Point2D, max: Point2D): Point2D[] {
    throw new Error("Not implemented");
  }
}
