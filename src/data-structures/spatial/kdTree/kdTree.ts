/**
 * KDTree — 평면의 정수 점을 여러 벌 담고, 축에 나란한 사각형 범위 질의를 담긴 점 수의 제곱근과 답의 수에, 최근접 질의를 담긴 점 수에 두는 점 색인.
 *
 * **목적.** 평면(2차원)의 점을 넣고, **닫힌 사각형 안의 점 전부**(범위 질의)와 **질의점에서 가장 가까운 점**(최근접)을
 * 묻는다. 범위 질의는 답이 없어도 담긴 점 전부를 지나지 않아야 하고(담긴 수의 제곱근 + 답의 수), 넣기는 담긴 수의 로그 제곱
 * 안에 상각된다. 점을 배열에 붙이고 물을 때마다 훑는 구현은 넣기가 상수이지만 가는 띠 하나를 묻는 데 점 전부를 지난다 — 이 계약의
 * 자리가 그 차이다. **최근접은 담긴 수에 비례하는 것까지만 약속한다**(아래 「상한 근거」).
 *
 * **차원 · 좌표 폭 · 답의 수 — 상한에 무엇이 들어오는가.**
 * - **차원은 2 로 고정한다**(물려받은 표면 그대로). 차원 d 를 열면 같은 계열의 범위 질의가 $O(n^{1-1/d} + k)$ 가 되고 기대 비율이
 *   2.52(d = 3) · 2.83(d = 4)이다. 판정 규격(`judge.ts` 의 `Bound`)에 그 계급이 없고, 기존 구간으로 재면 **d = 4 부터 `O(n)` 구간
 *   (2.80~5.20)이 선형 구현과 함께 통과시켜** 범위 행의 목적을 판정하지 못한다 — 열려면 규격을 바꿔야 해서 열지 않았다(산술이고 그 차원의
 *   구현은 짓지 않았다). d = 1 이면 정렬 집합의 구간 읽기라 이 계약의 자리가 아니다.
 * - **좌표 폭 W(담긴 좌표의 최댓값 − 최솟값)는 상한에 없다.** 상한은 담긴 점 수 n 과 답의 수 k 만 말하고 W 를 끝없이 키울 수 있는
 *   것으로 읽는다. 좌표 한도 $2^{25}$(「주입 정책」)는 거리를 정확히 견주려고 정의역에 둔 조건이지 상한의 상수로 접는 값이 아니다 —
 *   접으면 폭의 로그만큼 내려가는 구현이 「상수」가 되어 계약이 뜻을 잃는다(`heap/vanEmdeBoasTree` 의 안전한 정수 끝과 같은 읽기).
 *   스위트는 **W 를 n 과 같은 배율로** 사다리에 싣는다(무작위 채우기가 0 이상 4n 미만 — §「상한에 없는 파라미터를 사다리 하나에 싣는
 *   조건」). W 에만 기대는 계열은 **짓지 않았다**(확인 안 함).
 * - **답의 수 k 는 범위 질의 상한에 들어온다.** 스위트가 k 를 양 끝에 고정한다 — 답이 없는 가는 띠(k = 0)와 전부 담는 사각형
 *   (k = n)(§규약2 시나리오 규칙 3 · T5-02).
 *
 * **`spatial/quadtree` 와는 서로 담지 않는다**(B19 확정 「다르다」 · 불변 사실 54). 그쪽은 이쪽에서 최근접을 뺀 두 연산을 같은
 * 이름 · 같은 뜻으로 갖고 점의 정의역이 유한한 수 전부다.
 * - **같은 호출에 다른 답** — `insert([0.5, 2])` 뒤 `rangeSearch([0, 0], [1, 3])`. 그쪽은 `[[0.5, 2]]` 이고, 이쪽은 넣기가
 *   `RangeError` 라 `[]` 다. 이쪽 정본을 그쪽 스위트에 넣으면 축1 이 정수가 아닌 점을 넣는 자리(경계 케이스 하나 · 무작위 시퀀스 3번째
 *   연산인 넣기)에서 갈리고 시나리오 셋은 통과한다(`src/data-structures/_contract/runContract.quadtree.test.ts`).
 * - **연산이 하나 더 있다** — 그쪽 정본에는 최근접이 없다. B19 표의 반례(「범위만 하는 공간 색인은 뒤를 만족하고 앞을 만족하지
 *   못한다」)가 이것이다.
 * - **한쪽이 상한을 어기는 입력은 없다.** 겹치는 두 행의 상한 · 한정자가 같다. 그리고 **최근접 행은 비용으로 아무 구현도 배제하지
 *   않는다** — 범위 질의 상한을 지키는 어느 색인이든 전부 담는 사각형 한 번(`O(n)`)으로 최근접을 낼 수 있다. 그쪽 정본에 좌표 검사와
 *   그 훑는 최근접을 붙인 구현이 이쪽 스위트를 축1 · 축2 · 시나리오 넷 전부 통과한다(최근접 1,365 · 5,461 · 21,845). 두 계약이 갈리는
 *   자리는 최근접이라는 **연산의 뜻**과 정의역이고 비용 열이 아니다.
 *
 * **최근접 행을 두는 근거는 원칙 A 의 A5 다**(`docs/ORD-006-conventions.md` 「원칙 A」 판정표 ① 62). 범위 질의 한 번으로 조합되고(A2)
 * 그 상한 때문에 떨어지는 계열도 없어(A4 — 위 문단) 편의 연산이지만, **빼면 이 계약과 `spatial/quadtree` 를 가르는 반례가 사라진다** —
 * 위 둘째 항목(B19 표의 반례)이 이 질의에 기대고, 남는 것은 정의역 하나다. 그래서 「비용으로 배제하는 구현이 없다」를 적은 채로 둔다.
 * 이 행을 두고 뺄지를 사람 결정으로 넘겼던 `docs/ORD-006-conventions.md` 「비용으로 아무 구현도 배제하지 않는 연산 행과 「다르다」 판정」
 * 은 이 줄로 닫힌다.
 *
 * **이 계약에 없는 것 여섯.**
 * - **지우기.** 목적(범위 · 최근접)은 넣기만으로 진술되고, 지우기가 없어서 목적을 말하지 못하게 되는 자리가 없다(불변 사실 47 — 원칙 A 의 A3).
 * - **담긴 점 수 읽기.** 원칙 A 의 A2 · A4 · A5 로 빠진다 — 넣은 수는 호출자가 이미 알고, 수 하나를 세어 두는 상수 보강으로 어느
 *   설계든 지켜 떨어지는 계열이 없으며, 반례가 이 수에 기대지 않는다(판정표 ① 60). 물려받은 `size()` 를 뺐다.
 * - **점 목록을 받는 생성자.** 넣기 n 번과 같은 상태를 지어 조합되고(A2), 넣기 n 번의 합으로 적으면 떨어지는 계열이 없고, 그보다
 *   싸게 적으면 넣기를 거듭하는 구현이 로그 인수 하나로 떨어지지만 목적의 비용 조건이 그 배제를 요구하지 않는다(A4). 반례도 기대지
 *   않는다(A5 — 판정표 ① 61). 물려받은 `constructor(points?)` 의 인자를 뺐다.
 * - **범위 질의 답의 순서.** 정하면 답을 늘어놓는 일이 k 에 대해 선형보다 무거워질 수 있다 — 삽입 정렬로 늘어놓는 구현
 *   (`src/data-structures/_contract/_fixtures/sortedOutputPointIndex.ts`)이 전부 담는 사각형에서 436,824 · 6,991,268 · 111,871,656
 *   으로 걸린다. 축1 은 답을 (x, y) 순으로 정규화해 견준다.
 * - **k 번째까지의 최근접 · 거리를 주입받는 최근접.** 물려받은 표면에 없다.
 * - **공간.** 이 계약은 시간만 말한다(§규약1 「공간은 어느 계약에도 없다」 — 미결).
 *
 * **정본 이름은 임시다**(불변 사실 64). `kdTree` 는 축을 번갈아 가르는 설계의 이름이고, 이 계약을 지키는 구현은 그 설계에 묶이지
 * 않는다. 계약 이름과 카테고리 `spatial/` 은 KAN-031(계약 이름 재편)로 넘긴다.
 *
 * **불변식.** 하나다. 담긴 점을 읽는 공개 연산이 **둘**(범위 질의 · 최근접)이고 둘의 정합을 어느 행의 의미 열도 적지 않는다 — 구현이
 * 두 질의를 따로 유지하는 순간 갈린다(§규약1 「불변식 판별 절차」 ②). 어떤 연산 뒤에도 성립해야 한다.
 * 1. 정의역 안의 모든 `q` 에 대해 `nearestNeighbor(q)` 는 `rangeSearch([-2^25, -2^25], [2^25, 2^25])` 가 돌려주는 점 중 `q` 에 가장
 *    가까운 점(같은 거리면 x 가 작은 점, 그다음 y 가 작은 점)이고, 그 답이 비었으면 `null` 이다.
 *
 * 한 질의의 답에 대한 조건(사각형 안의 점을 빠짐없이 · 넣은 벌 수만큼)은 그 연산의 의미라 축1 이 본다(불변 사실 35). 영역을
 * 나누는 모양 · 균형 같은 담는 모양의 성질은 후보가 아니다(불변 사실 36).
 *
 * **연산 계약.** n 은 담긴 점의 수(넣은 벌 수), k 는 그 범위 질의가 돌려주는 점의 수다. 좌표 폭은 상한에 없다(위 「목적」).
 *
 * | 연산 | 의미 | 상한 | 한정자 |
 * |---|---|---|---|
 * | `constructor()` | 빈 색인으로 연다 | O(1) | worst |
 * | `insert(point)` | 점 `point` 를 한 벌 더 담는다. 같은 점이 이미 있어도 한 벌 더다. 정의역 밖이면 `RangeError` 이고 상태가 바뀌지 않는다 | O(log² n) | amortized |
 * | `rangeSearch(min, max)` | `min[0] ≤ x ≤ max[0]` 이고 `min[1] ≤ y ≤ max[1]` 인 담긴 점을 **벌마다 하나씩** 새 배열로 돌려준다. 순서는 정하지 않는다. 모서리가 뒤집히면 든 점이 없어 빈 배열이다 | O(sqrt n + k) | worst |
 * | `nearestNeighbor(query)` | 담긴 점 중 `query` 까지의 유클리드 거리가 가장 작은 점. 같은 거리면 x 가 작은 점, x 도 같으면 y 가 작은 점. 비었으면 `null` | O(n) | worst |
 *
 * **축3이 이 표를 무엇으로 판정하는가**(§「표의 상한과 시나리오의 bound 가 다르면 헤더가 그 판정을 적는다」).
 * - **넣기 $O(\log^2 n)$ → `O(log n)`.** 로그 인수 하나는 해상도 아래다(불변 사실 53). 정본의 호출 평균이 118.44 · 160.03 · 207.62
 *   (비율 1.35 · 1.30)로 `O(log n)` 구간(0.84~1.56) 안이다. 그래서 **로그 한 번으로 상각되는 구현과 로그 제곱인 구현이 이 행에서 갈리지
 *   않는다**(로그 세제곱은 기대 비율 1.73 이라 걸린다).
 * - **범위 질의 $O(\sqrt n + k)$ → 시나리오 둘.** k = 0 인 가는 띠를 `O(sqrt n)` 으로(정본 108 · 189 · 337), k = n 인 전부 담는 사각형을
 *   `O(n)` 으로(1,024 · 4,096 · 16,384) 판정한다. 제곱근에 로그 인수가 붙는 구현은 통과한다. 가는 띠의 채우기는 점 절반을 띠 양옆 두
 *   칸에 몰아 둔다 — **칸의 가운데로 네 등분하는 구현**(`src/data-structures/_contract/_fixtures/regionQuadtree.ts`, 물려받은
 *   `spatial/quadtree` 문제 문서의 설계)이 거기서 246 · 728 · 2,632 로 걸리고, 무작위로만 채우면 169 · 320 · 597 로 통과한다. 그 구현의
 *   깊이가 담긴 수가 아니라 점 사이 간격과 좌표 폭에 기대기 때문이다.
 * - **최근접 $O(n)$ → `O(n)`, 원 위의 점 입력으로만.** 무작위로 채우면 정본이 37 · 37 · 47 로 로그 쪽이라 이 행을 **계급 아래로**
 *   통과하지 못한다(불변 사실 113). 모든 점이 질의점에서 거의 같은 거리인 원 위의 입력에서 정본이 1,024 · 4,096 · 16,384 다.
 *   **선형보다 빠른 최근접을 가진 구현은 계약을 지키면서 이 행에서 걸린다**(불변 사실 49 · 111 — 상한을 약하게 적은 대가).
 *
 * **상한 근거**(불변 사실 48 · 51 — 고르는 값이고, 물려받은 값은 입력이 아니다 · 불변 사실 65).
 * - **범위 질의 $O(\sqrt n + k)$ 는 정본이 서는 계급으로 골랐다.** 목적이 자르는 것은 「답이 없어도 담긴 수에 비례하는 구현」이다.
 *   더 약하게 적으면(예: $n^{0.9} + k$) 목적은 살지만 가장 약한 값이 없고(불변 사실 48), 상한이 정본의 계급과 같아야 축3이 정본을
 *   떨어뜨리지 않는다(불변 사실 49). 더 강하게 로그 거듭제곱 + k 로 적으면 영역을 나누는 구현 계열이 전부 나가는데 그 계열을 배제할
 *   이유가 목적에 없다 — 로그 계열이 흔히 치르는 대가는 공간이고 이 계약은 공간을 말하지 않는다. 그런 구현은 가는 띠에서 **계급
 *   아래로** 걸린다.
 * - **넣기 $O(\log^2 n)$ 도 정본의 계급이다.** 더 약하게 $O(n)$ 으로 적으면 넣을 때마다 정렬된 배열을 미는 구현이 들어와 동적 색인이라는
 *   목적이 사라진다. 그런데 $O(\sqrt n)$ 쯤으로 적어도 목적은 살아 있다 — **이 행은 불변 사실 51 의 첫 물음에 「뜻을 잃는다」로 답하지
 *   못한다.** 로그 제곱으로 적는 이유는 정본의 계급과 같아야 한다는 것 하나다(제곱근으로 판정하면 정본이 계급 아래로 실패한다). 더
 *   강하게 로그 한 번으로 적을 수 있는지는 판정하지 않았고 축3도 그 갈림을 못 본다.
 * - **최근접 $O(n)$ 은 이 카드가 지은 계열이 실제로 내는 계급이다.** 영역으로 가지를 치는 구현은 원 위의 입력에서 전부를 지난다.
 *   담긴 수보다 느리게 자라는 최악 최근접을 주는 설계(평면을 가장 가까운 점의 영역으로 나누고 점 위치를 찾는 계열)는 문헌에 있다고
 *   알고 있으나 **이 카드는 짓지도 확인하지도 않았다**(불변 사실 165). 그래서 더 강하게 적지 않았다 — 적으면 이 정본이 나가고 그
 *   자리에 설 정본이 없다.
 *
 * **한정자.** 셋은 줄 세워지지 않으므로(불변 사실 59) 강한 쪽을 적으려면 추가로 배제하는 계열을 배제해도 잃는 것이 없어야 하고, 그
 * 계열을 수치로 낸다.
 * - **넣기는 `amortized` 다 — `worst` 로 적으면 정본이 나간다.** 층을 모두 합치는 넣기 한 호출이 34,933 · 164,249 · 755,229 다. 최악
 *   로그 제곱의 넣기를 가진 설계가 이 행을 지키는지는 판정하지 않았다. `expected` 와는 서로 함의하지 않고, 무작위를 뽑는 계열을 들일
 *   근거가 정본에 없어 정본이 선 쪽을 적었다.
 * - **범위 질의 `worst` 가 추가로 배제하는 계열 — 넣기를 쌓아 두었다 질의 때 한꺼번에 넣는 구현**
 *   (`src/data-structures/_contract/_fixtures/bufferedPointIndex.ts`). 채운 뒤 가는 띠 n 번을 전부 재면 단일 호출 최대 125,557 · 695,742 ·
 *   3,689,685 로 걸리고 호출 평균 193.78 · 314.08 · 515.33 으로 통과한다. 정본은 호출마다 89 · 177 · 361 이라 배제해도 잃는 것이 없다.
 * - **최근접 `worst` 는 같은 계열을 경계에서만 가른다.** 원 위의 점 n 개를 쌓아 두었다 첫 최근접이 넣으면 단일 호출 최대 103,247 ·
 *   552,459 · 2,849,595(비율 5.35 · 5.16)로 첫 비율만 `O(n)` 구간(2.80~5.20) 밖이다 — 한꺼번에 넣는 비용이 선형보다 로그 제곱만큼
 *   무겁기 때문이다(불변 사실 53). 정본은 호출마다 이 행을 지켜(1,024 · 4,096 · 16,384) 잃는 것이 없다.
 * - `constructor` 는 n 에 대해 반복 호출되지 않아 두 한정자가 다르게 배제하는 것이 없다.
 *
 * **물려받은 표면을 여섯 고쳤다.** 연산 이름 셋(`insert` · `rangeSearch` · `nearestNeighbor`)은 그대로다.
 * - 스텁의 private 필드 둘과 마디 인터페이스 `KDNode`, 헤더의 「x · y 를 번갈아 중앙값으로 가른다」와 문제 문서의 알고리즘 절을 뺐다 —
 *   담는 모양의 처방이다(불변 사실 4 · 20).
 * - `size()` 와 `constructor(points?)` 의 인자를 뺐다(위 「이 계약에 없는 것」).
 * - **점의 정의역을 정했다** — 물려받은 제약은 「−10^6 ≤ x, y ≤ 10^6」 뿐이라 정수인지 말하지 않았다(「주입 정책」).
 * - **최근접의 동률 규칙을 정했다.** 정하지 않으면 같은 거리의 점 중 무엇을 돌려줄지가 담는 모양에 기대고 축1 이 기대할 값이 없다.
 *   정하는 대가는 가지를 칠 때 같은 거리를 치지 않는 것뿐이라 계급이 안 바뀐다(§「분할을 담는 계약은 대표를 분할만으로 정해지는
 *   값으로 정한다」 표의 두 물음이 둘 다 「아니다」).
 * - 「올바른 범위 보장(min ≤ max)」 전제를 뺐다 — 뒤집힌 사각형은 든 점이 없다는 뜻이 이미 있어 빈 배열로 전면화했다(불변 사실 82).
 * - 물려받은 상한(넣기 · 최근접 「O(log n) 평균」, 범위 「O(√n + k) 평균」)을 입력으로 쓰지 않았다(불변 사실 65). 「평균」은 입력
 *   분포에 대한 말이라 호출자가 어떤 점을 주든 참인 문장이 아니고(불변 사실 104), 넣기의 「O(n) 최악」은 다시 짓지 않는 나무의 값이다.
 *
 * **주입 정책.**
 * - **점(`insert` 의 `point` · `nearestNeighbor` 의 `query`)의 두 좌표는 절댓값이 $2^{25}$ 이하인 정수여야 한다.** 아니면(정수 아닌
 *   수 · 한도 밖 · NaN) `RangeError` 이고 상태가 바뀌지 않는다. 이 한도에서 두 점의 제곱 거리가 $2^{53}$ 을 넘지 않아 부동소수로 셈해도
 *   정확하다. **정의역을 넓히지 않는 이유**(§「주입 정책도 상한과 같은 두 물음으로 정한다」): 정수가 아닌 좌표를 받으면 제곱 거리가
 *   반올림되어, 부동소수로 거리를 셈하는 구현 계열이 동률 근처에서 다른 점을 돌려준다 — 그 계열이 전부 나가거나 답이 셈법에 기댄다.
 *   좁혀서 나가는 호출자는 좌표를 정수로 옮겨 쓸 수 있다. **`spatial/quadtree` 는 거리를 셈하지 않아 이 제약이 없다** — 두 계약의
 *   정의역이 갈리는 이유가 최근접 연산 하나다(§「값의 타입이 계약의 조건이 되는 자리가 있다」).
 * - **`rangeSearch` 의 모서리는 NaN 이 아닌 수면 된다**(정수가 아니어도 · 한도 밖이어도 · 무한이어도). 견주기만 하므로 넓혀도 나가는
 *   구현이 없다. NaN 이면 `RangeError` 다 — 사각형이 정해지지 않고, 빈 배열은 「든 점이 없다」라는 답이라 겹칠 자리가 아니다.
 * - 구조는 호출자가 넘긴 배열을 붙들지 않는다 — `insert` 뒤에 그 배열을 고쳐도 담긴 점은 그대로이고, `rangeSearch` · `nearestNeighbor`
 *   가 돌려준 배열을 고쳐도 담긴 점은 그대로다.
 * - 거리 · 비교를 주입받지 않는다. 거리는 유클리드로 고정이다.
 *
 * **검증 등급.** `complexity`.
 * 넣기와 범위 질의 두 행을 **함께** 만족하는 자명한 구현이 없다. 배열에 붙이고 훑으면 넣기가 상수이지만 가는 띠가 담긴 수에 비례하고
 * (`src/data-structures/_contract/_fixtures/scanningPointSet.ts` — 1,024 · 4,096 · 16,384), 한 좌표 순으로 늘어놓은 배열은 그 좌표로
 * 좁은 띠에만 빠르고 다른 좌표로 좁은 띠에서 담긴 수에 비례하며(`src/data-structures/_contract/_fixtures/xSortedRunsPointSet.ts` — 가로 띠
 * 1,035 · 4,109 · 16,399), 넣을 때마다 좌표로 견주며 매다는 나무는 대각선으로 넣으면 사슬이 된다
 * (`src/data-structures/_contract/_fixtures/unbalancedPointTree.ts` — 511.5 · 2,047.5 · 8,191.5). 두 좌표를 **함께** 반씩 줄여 가며 나누는
 * 균형을 넣기 사이에 지키는 설계가 들어와야 하고, 그것이 자명하지 않다는 것이 이 등급의 내용이다. 최근접 행은 이 판정에 들어오지
 * 않는다(훑기가 그 상한이다).
 *
 * **필요충분조건.**
 * - 의미: 평면의 정수 점 · 여러 벌 담기 · 닫힌 사각형 범위 질의 · 동률 규칙이 있는 유클리드 최근접. 최근접을 버리면 `spatial/quadtree`
 *   쪽이고, 한 좌표만 남기면 정렬 집합의 구간 읽기다.
 * - 비용: **넣기가 로그 제곱에 상각되면서 범위 질의가 답이 없을 때 담긴 수의 제곱근 안**이어야 한다. 넣기만 빠르면 훑는 배열이고,
 *   질의만 빠르면 넣을 때마다 다시 짓는 정적 색인이다. 최근접의 선형 상한은 이 조건에 들어오지 않는다 — 어느 구현이든 지키므로 못
 *   지켜서 되는 다른 무엇이 없다.
 */
export type Point2D = [number, number];

export class KDTree {
  constructor() {
    throw new Error("Not implemented");
  }

  insert(point: Point2D): void {
    throw new Error("Not implemented");
  }

  rangeSearch(min: Point2D, max: Point2D): Point2D[] {
    throw new Error("Not implemented");
  }

  nearestNeighbor(query: Point2D): Point2D | null {
    throw new Error("Not implemented");
  }
}
