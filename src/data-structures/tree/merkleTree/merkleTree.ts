/**
 * MerkleTree — 블록 수열 하나를 짧은 뿌리 하나에 묶고, 어느 자리의 블록이든 수열 없이 확인할 수 있는 짧은 증명을 내며, 블록 하나를 바꾸면 뿌리를 로그 비용에 다시 묶는 구조.
 *
 * **목적.** 블록(문자열) 수열을 받아 **뿌리** 하나를 내고, 자리마다 **증명**을 내며, 뿌리와 증명만 든 쪽이 「이 블록이 그 뿌리가 가리키는
 * 수열의 그 자리 블록인가」를 **수열을 갖지 않고** 확인하게 하는 것. 뿌리는 **수열 하나를 가리킨다**(같은 수열이면 같은 뿌리, 다른 수열이면
 * 다른 뿌리 — 주입 해시의 조건 아래). 증명은 블록 수의 로그 크기이고, 블록 하나를 바꾸는 일도 로그 비용이다.
 *
 * **자료구조인지를 먼저 물었다**(`docs/ORD-006-conventions.md` 「「자료구조인가」는 물음 둘이다」 — T5-06). ① 호출 사이에 남는 상태가
 * **있다** — `update` 가 수열을 바꾸고 뒤의 `rootHash` · `getProof` 가 그 수열을 읽는다. 그 절의 규칙대로 여기서 판정이 끝난다 — 자료구조다.
 * ② 을 따로 물으면, 뿌리와 증명의 **값**은 구현이 고르는 토큰이라 입력만으로 정해지지 않지만 계약이 정하는 것은 **토큰 사이의 관계**이고
 * 그 관계는 하나로 정해진다(아래 「토큰 모형」). `tree/huffmanTree`(지어 둔 상태 자체가 알고리즘이 고른 답)와 갈리는 자리가 이것이다.
 *
 * **불변 구조가 아니다.** WBS 가 이 유닛에 적은 「증명 검증을 가진 불변 구조」는 물려받은 표면(`update` 가 있다)과 맞지 않고, 물려받은 주장은
 * 판정 입력으로 쓰지 않는다(불변 사실 65). `update` 를 뺄 근거는 서지 않았다 — 블록 하나를 바꾼 뒤 뿌리를 **다시 짓지 않고** 로그 비용에
 * 다시 묶는 것이 이 구조가 「통째로 다시 해시하기」와 갈리는 자리다(불변 사실 47). 그래서 **불변 구조의 네 원칙(불변 사실 52)은 적용하지
 * 않는다.** `range-query/sparseTable` 선례에서 가져오는 것은 `worst` 근거의 계열 구분뿐이다(아래 「모든 행이 `worst` 인 것」 — 불변 사실 328).
 *
 * **`range-query/segmentTree` 와는 연산 집합으로 갈린다.** 뿌리는 수열 전체를 한 번 접은 값처럼 보이지만, 해시로 두 토큰을 잇는 일은
 * 결합법칙을 지키지 않아 저쪽 주입자 의무를 어긴다 — 저쪽 구현에 해시를 주입해 뿌리를 흉내 내는 길이 서지 않는다(산술). 저쪽은 임의 구간을
 * 접고 이쪽은 증명을 내고 확인한다.
 *
 * **이 계약에 없는 것.**
 * - **블록 읽기.** 수열을 가진 쪽은 블록을 이미 들고 있고, 검증하는 쪽은 수열을 갖지 않는다 — 목적이 요구하지 않는다(불변 사실 47).
 * - **블록 넣기 · 빼기(길이 바꾸기).** 뿌리가 블록 수를 포함해 수열을 가리키므로 넣고 빼는 일은 다른 약속이 된다.
 * - **토큰의 모양 · 길이 · 다른 구현과의 호환.** 계약은 토큰 사이의 관계만 정한다 — 특정 표준(자리 채우기 · 문자열 짜임)을 정하면 그것이 표현을
 *   처방한다(불변 사실 4).
 * - **공간.** 이 계약은 시간만 말한다(§규약1 「공간은 어느 계약에도 없다」 — 미결).
 *
 * **이 이름은 계약을 정당화하지 않는다.** 계약의 어느 문장도 나무를 말하지 않는다. 이름이 가리키는 설계(물려받은 문서의 「마지막 리프를 복제해
 * 짝수로」)는 오히려 이 계약의 결속과 검증을 어긴다(아래 수치). 계약 이름을 다시 붙이는 일은 KAN-031 의 몫이다.
 *
 * **불변식.** 없다.
 *
 * 판별 절차(§규약1 「불변식 판별 절차」)를 돌린 결과이고, 후보가 셋이었다.
 *
 * - **「지금 뿌리 · 지금 증명 · 지금 블록으로 한 검증은 참이다」** — 관측 경로가 셋(`rootHash` · `getProof` · `verify`)이지만 그 정합을 `verify` 행의
 *   의미 열이 이미 적고 있어 두 번째 물음에서 그 연산의 의미가 된다 — 축1이 본다.
 * - **「같은 수열이면 같은 뿌리」** — 두 때의 뿌리를 읽는 경로가 `rootHash` 하나라 첫 물음에서 걸리고, `rootHash` 행의 의미다.
 * - **「증명의 길이는 블록 수의 로그다」** — 출력 크기의 성질이라 상태의 성질이 아니고, `getProof` 행의 상한이 받는다.
 *
 * **연산 계약.** n 은 담긴 블록 수, t 는 `verify` 가 받은 `proof` 의 토큰 수다. 상한이 세는 것은 **주입된 해시를 부른 횟수와 지나간 칸 수**다 —
 * 해시 한 번이 받는 문자열의 길이는 세지 않는다(해시의 비용은 주입자의 몫이고, 해시 한 번을 한 걸음으로 친다).
 *
 * | 연산 | 의미 | 상한 | 한정자 |
 * |---|---|---|---|
 * | `constructor(blocks, hash)` | 블록 수열 `blocks` 를 담고, 뿌리 · 증명에 `hash` 를 쓴다. 빈 수열도 정당하다. 돌아온 뒤 호출자가 `blocks` 를 고쳐도 담긴 수열은 바뀌지 않는다 | O(n) | worst |
 * | `rootHash()` | 지금 수열의 뿌리. **같은 `hash` 를 받은 이 구현의 어느 인스턴스 · 어느 때든 수열이 같으면 같은 뿌리이고 다르면 다른 뿌리다** | O(1) | worst |
 * | `getProof(index)` | 지금 수열에서 자리 `index` 의 증명(문자열 배열). 범위 밖 · 정수 아닌 자리는 `RangeError` | O(log n) | worst |
 * | `verify(rootHash, index, block, proof)` | `rootHash` 가 이 구현이 수열 X 에 대해 돌려준 뿌리일 때 — `index` 가 X 의 자리가 아니거나 `block` 이 X 의 그 자리 블록이 아니면 **거짓**, 둘 다 맞고 `proof` 가 수열 X 에서 `getProof(index)` 로 받은 증명이면 **참**. 그 밖은 정하지 않는다. **답이 이 인스턴스의 지금 수열에 기대지 않는다.** 던지지 않는다 | O(t) | worst |
 * | `update(index, block)` | 자리 `index` 의 블록을 `block` 으로 바꾼다. 범위 밖 · 정수 아닌 자리는 `RangeError` | O(log n) | worst |
 *
 * **토큰 모형.** 뿌리와 증명은 구현이 고르는 문자열이고 계약은 그 내용을 정하지 않는다 — `heap/fibonacciHeap` 의 핸들이 「어느 원소를 가리키는가」만
 * 정한 것(§「원소를 가리키는 연산이 들어오면 계약이 핸들 모형을 정한다」)과 같은 자리다. 정하는 것은 셋이다.
 *
 * 1. **결속** — 뿌리 하나가 수열 하나를 가리킨다(`rootHash` 행). 끝 블록을 한 번 더 붙인 수열, 블록 수만 다른 수열도 다른 뿌리다.
 * 2. **완전성** — 수열 X 의 뿌리 · 자리 · 블록 · 그 자리의 증명으로 한 검증은 참이다.
 * 3. **건전성** — 뿌리가 가리키는 수열에서 그 자리 블록이 아닌 것은 어떤 증명으로도 거짓이다.
 *
 * **정하지 않는 자리가 둘이다.** ① 이 구현이 돌려준 적 없는 문자열을 뿌리로 준 검증 ② 블록은 맞는데 증명이 그 뿌리 · 그 자리의 증명이 아닌 검증.
 * ②를 거짓으로 정하면 증명에 뜻 없는 토큰을 더 붙여도 참으로 받는 구현(건전성은 지킨다)이 나가는데, 그 배제의 이유가 목적에 없다.
 *
 * **`verify` 가 뿌리를 인자로 받는 것이 이 계약의 중심 조항이다.** 물려받은 표면은 `verify(index, block, proof)` 로 인스턴스의 지금 뿌리에 대해
 * 물었는데, 그러면 **담긴 블록과 견주는 구현**(`_contract/_fixtures/blockCheckingMerkleTree.ts`)이 정본과 같은 답을 내어 「수열을 갖지 않고 확인한다」가
 * 관측되지 않는다(불변 사실 69 ③ — 관측되지 않는 약속은 계약이 아니다). 뿌리를 인자로 받으면 고치기 전 · 다시 짓기 전의 뿌리를 묻는 순간 갈린다 —
 * 경계 케이스 「검증은 담긴 블록을 읽지 않는다」 다섯째 걸음에서 그 구현이 거짓 / 모델 참.
 *
 * **모든 행이 `worst` 인 것 — 배제하는 계열을 셋으로 갈라 쟀다**(§「`worst` 만 있는 계약의 한정자 근거는 「배제될 계열이 없다」로 적는다」 · T1-06 · 불변 사실 328).
 *
 * - **비용을 뒤 호출로 미루는 계열(T1-06 첫째)이 이 계약에는 실재한다** — `update` 가 상태를 바꾸기 때문이다. 고친 자리만 적어 두고 다음 읽기가
 *   한꺼번에 다시 짓는 구현(`_contract/_fixtures/recomputeRuleMerkleTree.ts` 의 `dirtyPaths`)은 흩어진 자리 n 곳을 고친 뒤 뿌리 n 번을 전부 재면 단일
 *   호출 최대 3,071 · 12,287 · 49,151 로 걸리고 호출 평균 4 · 4 · 4 로 통과한다. 쌓아 두다 블록 칸의 1/64 이 차면 통째로 다시 짓는 구현(`flushWhenFull`)은
 *   고치기 n 번에서 단일 호출 최대 1,057 · 4,225 · 16,897 실패 / 호출 평균 67 · 67 · 67 통과.
 * - **짓기를 첫 읽기로 미루는 계열(불변 사실 328 의 셋째)도 있다** — `deferredBuild` 가 짓자마자 읽는 뿌리에서 3,074 · 12,290 · 49,154 로 걸린다.
 * - **무작위로 상한을 얻는 계열(T1-06 둘째)은 얻는 것이 없다** — 결속이 뿌리를 수열의 함수로 묶는다.
 *
 * 정본은 뿌리 1 · 1 · 1, 고치기 22 · 26 · 30 을 두 통계 모두에서 내므로 **세 계열을 배제해도 잃는 것이 없고**(불변 사실 59) 축3의 단일 호출 최대가
 * 그 강함을 잰다(§「한정자의 강한 읽기는 축이 그 강함을 잴 때만 적는다」). 생성자 행은 n 에 대해 반복 호출되지 않아 강한 한정자를 적는 대가가 없다.
 *
 * **상한 넷은 이렇게 골랐다.**
 *
 * - **생성자 `O(n)` · 검증 `O(t)` 는 하한이 지목한 값이다**(§「하한이 지목하는 상한은 두 걸음으로 적는다」). 블록 하나를 안 읽으면 결속이 설 수
 *   없고(배열 조항에 기대어 선다 — §「하한이 지목한 상한도 다른 계약 조항에 기대어 선다」), 증명 토큰 하나를 안 읽으면 건전성이 설 수 없다. 정본이
 *   두 계급을 낸다.
 * - **뿌리 `O(1)` · 증명과 고치기 `O(log n)` 은 정본이 서는 계급으로 골랐다**(불변 사실 51). 약하게 `O(n)` 으로 적으면 블록 토큰만 들고 읽을 때마다
 *   다시 짓는 구현 · 고칠 때마다 다시 짓는 구현이 들어와 「짧은 증명 · 싼 다시 묶기」라는 목적이 사라진다(아래 등급의 두 수치). 그 사이의 `O(sqrt n)`
 *   계열(블록을 묶음으로 나눈 두 층)은 짓지 않았다(확인 안 함). 강하게 — 증명 · 고치기를 로그 아래로 — 적을 수 있는 구현은 **해시 하나만 주입받는
 *   자리에서 찾지 못했다**(불변 사실 165 — 「없다」가 아니라 「못 찾았다」).
 *
 * **축3은 이렇게 판정한다** — 검증 행의 `O(t)` 는 옳은 증명의 길이가 로그라 `O(log n)` 으로, 나머지는 표의 상한 그대로다(§「표의 상한과 시나리오의
 * bound 가 다르면 헤더가 그 판정을 적는다」). 로그 인수만큼 어기는 구현은 통과한다(불변 사실 53).
 *
 * **주입 정책.**
 *
 * - **해시 `hash(data: string): string` 를 생성자로 받는다. 선택이 아니라 필수다.** 세 물음(§「주입 정책도 상한과 같은 두 물음으로 정한다」)으로 정했다.
 *   **닫으면**(SHA-256 고정) 다른 해시를 쓰는 호출자가 목적과 무관하게 나가고, 스위트가 결속 · 건전성을 결정론으로 볼 해시를 쓸 수 없다. **좁혀
 *   열면**(고정 길이 출력) 끝없는 정의역에서 단사가 불가능해 같은 문제가 남는다. **열면** 아래 의무를 주입자가 진다.
 * - **주입자의 의무 둘.** ① **순수** — 같은 문자열에 언제나 같은 값. ② **단사** — 서로 다른 문자열에 서로 다른 값. 끝없는 정의역을 고정 길이로 받는
 *   실제 해시는 ②를 **「충돌을 찾을 수 없다」는 뜻으로만** 지키고, 결속 · 건전성 조항은 그 읽기 아래에서 선다. **계약이 ②를 검사하지 않는다.**
 * - **②를 어기면 답이 구현마다 갈린다** — 길이만 돌려주는 해시(`s => String(s.length)`)로 `["ab", "cd", "ef", "gh"]` 를 짓고 자리 1 의 증명으로 틀린
 *   블록 `"zz"` 를 물으면 정본은 참, 담긴 블록과 견주는 구현은 거짓이다(자기시험) — 그래서 ②가 계약의 문장이다.
 * - **단사인 해시를 받아도 결속은 구현의 몫이다.** 해시에 넘기는 문자열을 갈래 표시 · 경계 없이 이어 붙이면, 출력끼리 앞뒤가 겹칠 수 있는 단사
 *   해시(`s => s + "!"`)에서 `["x!", "y"]` 와 `["x", "!y"]` 가 같은 뿌리를 받는다(`_contract/_fixtures/unseparatedMerkleTree.ts` — 둘 다 `2:x!!y!!!`,
 *   정본은 갈린다). **스위트는 이 위반을 결속 열거로 잡는다** — 의무 둘만 지키는 그 해시로, `x` · `!` 로 짠 길이 2 이하 블록 일곱의 길이 0 … 4 수열
 *   2,801 개 전부에 짓기와 고치기 두 길로 뿌리를 받아 서로 다른 수열이 같은 뿌리를 받으면 떨어뜨린다(`.test.ts` 주입 정책 시험). 수열은 이 행의
 *   결속 문장과 의무 ②에서 고른 것이라 구현이 토큰을 잇는 방식을 읽지 않는다(불변 사실 44). 겹친 몫(뿌리마다 받은 수열 수 − 1 의 합)이 그 구현
 *   1,355 · 끝 복제 설계 1,577 · 정본 0 이다. 열거는 유한하다 — 더 긴 수열이나 다른 글자에서만 겹치는 구현은 통과한다(그런 구현을 짓지는 않았다).
 * - 블록은 임의 문자열이고 빈 블록 · 빈 수열도 정당하다. 던지는 자리는 `getProof` · `update` 의 범위 밖 자리뿐이다 — 반환 타입에 「그런 자리는
 *   없다」를 담을 자리가 없다(불변 사실 82). `verify` 는 `boolean` 에 거짓을 담을 수 있어 던지지 않는다.
 *
 * **물려받은 표면을 다섯 고쳤다.** 물려받은 상한 주장(생성자 `O(n)` · 뿌리 `O(1)` · 증명 · 검증 · 고치기 `O(log n)`)은 판정 입력으로 쓰지
 * 않았다(불변 사실 65) — 판정은 위 문단에서 다시 냈고 결과가 검증 행(`O(t)`)에서만 갈렸다.
 *
 * - **해시를 주입받는다**(SHA-256 · 64자 16진수 고정을 뺐다).
 * - **`verify` 가 뿌리를 인자로 받는다**(위 중심 조항).
 * - **빈 수열을 받는다** — 물려받은 「빈 배열은 에러」를 뺐다. 빈 수열에도 뿌리가 정해지고 거절할 근거가 목적에 없다.
 * - **범위 밖 자리를 `RangeError` 로 정했다** — 물려받은 것은 「에러를 던진다」뿐이었고 `update` 는 말하지 않았다.
 * - **「홀수 리프는 마지막 리프를 복제」 · 「증명 길이 ceil(log2 n)」 · 「부모 = SHA256(왼쪽 + 오른쪽)」 처방, 내부 필드(`leaves` · `tree` ·
 *   `leafCount`) · 스토리 · 크기 제약 · 벽시계 성능 시험을 뺐다**(불변 사실 7 · 20 · 38). 앞의 셋은 처방이기도 하지만 **그대로 따르면 계약을 어긴다** —
 *   그 설계(`_contract/_fixtures/duplicatingLastMerkleTree.ts`)는 빈 수열과 빈 블록 하나짜리 수열 · `[a, b, c]` 와 `[a, b, c, c]` 에 같은 뿌리를 주고,
 *   `[a, b, c]` 의 자리 2 증명을 없는 자리 3 에 대어도 참을 낸다.
 *
 * **검증 등급.** `complexity`.
 * 네 행을 **함께** 만족하는 자명한 구현이 없다 — `range-query/segmentTree` 의 판정과 같은 모양이다.
 *
 * - **블록 토큰만 들고 읽을 때마다 위층을 다시 지으면**(`recomputeRuleMerkleTree` 의 `leafOnly`) 고치기가 상수인 대신 뿌리 1,025 · 4,097 · 16,385 ·
 *   증명 1,034 · 4,108 · 16,398 이다.
 * - **고칠 때마다 위층 전부를 다시 지으면**(`eagerRebuild`) 읽기가 싼 대신 고치기가 1,025 · 4,097 · 16,385 다.
 *
 * 둘을 함께 두려면 부분 수열의 토큰을 층마다 들고 고친 자리의 길만 다시 짓는 설계가 들어와야 하고, 결속을 지키려면 해시에 넘기는 문자열의 짜임까지
 * 설계해야 한다 — 배열의 기본 연산에서 나오지 않는다.
 *
 * **필요충분조건.**
 * - 의미: 뿌리 하나가 수열 하나를 가리키고(결속), 뿌리 · 증명만으로 블록의 자리를 확인하되 틀린 블록은 거절하고(완전성 · 건전성), 블록 하나를
 *   바꿀 수 있다. 결속을 버리면 뿌리가 수열을 대신하지 못하고, 검증이 수열을 읽으면 그냥 배열과 견주기이며, 고치기를 버리면 불변 구조가 된다.
 * - 비용: 셋을 다 지키더라도 **고치기가 블록 수에 비례하면** 통째로 다시 해시하기이고, **뿌리 · 증명이 블록 수에 비례하면** 블록 토큰만 든 목록이며,
 *   **고친 뒤 첫 읽기나 이따금의 고치기 한 번이 블록 수에 비례하면** 비용을 미루는 설계다 — 셋 다 이 구조가 아니다.
 */
export class MerkleTree {
  constructor(blocks: string[], hash: (data: string) => string) {
    throw new Error("Not implemented");
  }

  rootHash(): string {
    throw new Error("Not implemented");
  }

  getProof(index: number): string[] {
    throw new Error("Not implemented");
  }

  verify(
    rootHash: string,
    index: number,
    block: string,
    proof: string[],
  ): boolean {
    throw new Error("Not implemented");
  }

  update(index: number, block: string): void {
    throw new Error("Not implemented");
  }
}
