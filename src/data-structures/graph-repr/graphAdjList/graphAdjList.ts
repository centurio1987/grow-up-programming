/**
 * GraphAdjList — 정점을 하나씩 늘리며 간선을 넣고 빼는 그래프. 한 정점의 이웃을 그 정점의 차수에 비례하는 비용으로 열거한다.
 *
 * **목적.** 정점 수를 미리 정하지 않는 그래프 하나를 두고, 정점 늘리기 · 간선 넣기 · 간선
 * 빼기 · 한 정점의 이웃 열거를 **인자로 받은 정점의 차수**에 비례하는 비용으로 제공하는 것.
 * 차수가 작은 정점을 다루는 데 그래프 전체의 크기(정점 수 · 간선 수)가 들지 않는다는 것이 이
 * 계약의 비용 쪽 내용이다. 방향 그래프인지는 생성 때 정해지고 바뀌지 않는다.
 *
 * 이 계약에 없는 것은 다섯이다.
 *
 * - **두 정점 사이에 간선이 있는지를 상수 비용에 묻는 일.** `hasEdge`·`weight` 는
 *   `graph-repr/graphAdjMatrix` 의 계약이다. 두 계약의 관계는 아래 반례 문단에 있다.
 * - **탐색.** 물려받은 표면의 `bfs`·`dfs`·`hasPath` 를 뺐다. 셋 다 `neighbors` 를 거듭 불러 짓는 알고리즘이고(원칙 A 의
 *   A2 — 조합), `neighbors` 행을 지키는 구현이면 누구든 정점 수 + 간선 수에 비례해 답하므로 그 행들 때문에 떨어지는 계열이
 *   없는 편의 연산이며(A4), 아래 반례 문단이 이 질의들에 기대지 않는다(A5). 판정 줄은 `docs/ORD-006-conventions.md` 「원칙 A —
 *   연산 선정」 판정표 ① 의 15 · 16 번째 행이다. 앞의 둘에는 보조 근거가 하나 더 있다 — 방문 순서가 이웃을 꺼내는 차례에
 *   달려, 그 차례를 행에 적으면 표현을 처방하고(§규약1 금지 1·2) 적지 않으면 반환값이 계약으로 정해지지 않아 축1이 대조할
 *   것이 없다(불변 사실 69 ③). 그래프를 도는 일은 알고리즘 트랙이 받는다
 *   (`src/algorithms/graph/dfsTraversal/dfsTraversal.ts:1`).
 * - **정점 지우기.** 아래 정점 번호 문단의 약속(번호가 `[0, vertexCount())` 를 빈틈 없이
 *   채운다)과 부딪힌다. 지우면 뒤 번호를 옮기거나 구멍을 남겨야 하고, 어느 쪽이든 호출자가 들고
 *   있는 번호의 뜻이 바뀐다.
 * - **사이클을 만드는 간선의 거부.** `graph-repr/dag` 의 계약이다. 방향 그래프에서 `addEdge(u, v)`
 *   뒤에 `addEdge(v, u)` 를 부르면 이 계약은 간선 둘을 요구하고 그 계약은 둘째의 거부를 요구하므로
 *   두 계약을 동시에 만족하는 구현이 없다(불변 사실 208).
 * - **같은 두 정점 사이의 간선 여럿(다중 간선).** 같은 쌍에 다시 넣으면 무게만 바뀐다.
 *   다중 간선을 받으면 `removeEdge` 가 여럿 중 무엇을 빼는지를 따로 정해야 하고, 같은 호출에
 *   `graph-repr/graphAdjMatrix` 계약과 다른 값을 요구하게 된다(아래 반례 문단 넷째 줄).
 *
 * **이 계약은 시간만 말한다.** 「희소 그래프에서 공간이 적다」는 물려받은 존재 이유는 저장량
 * 문장이라 계약에 들어오지 못하고(§규약1 「공간은 어느 계약에도 없다」 — 미결), 계약의 용어로
 * 다시 적었을 때 남는 것이 위의 「차수에 비례」다(불변 사실 188).
 *
 * **정점 번호는 구조가 매긴다.** `addVertex()` 가 새 정점의 번호를 돌려주고 그 값은 호출 직전의
 * `vertexCount()` 다. 그래서 유효한 번호는 언제나 `[0, vertexCount())` 이고 빈틈이 없다. 호출자가
 * 번호(임의 정수 · 문자열)를 고르게 두지 않은 이유는 그러면 번호에서 정점을 찾는 사전이 필요해지고,
 * **언어가 내주는 사전을 자명한 구현에 넣을지가 미결이라**(`docs/ORD-006-conventions.md:3002-3008`)
 * 검증 등급이 그 미결에 매달리기 때문이다. 번호가 조밀하면 배열로 끝난다. 자기 이름으로 정점을
 * 부르려는 호출자는 이름과 번호를 잇는 사전을 밖에서 든다 — 그 비용은 이 계약 밖이다. 이 결정의
 * 전문은 `docs/ORD-006-conventions.md` 「정점 번호는 구조가 매긴다」 절이다.
 *
 * **`graph-repr/graphAdjMatrix` 와 서로 담지 않는다(불변 사실 54).** 반례를 계약의 문장으로
 * 적는다. 어느 정본도 보지 않는다(불변 사실 43).
 *
 * - **이쪽을 지키고 저쪽을 못 지키는 구현** — 정점마다 이웃 항목을 배열로 드는 구현. 이 계약의
 *   일곱 행을 전부 지키지만(아래 검증 등급), 차수가 d 인 정점에서 행렬 계약의 `hasEdge` 가 그
 *   배열을 훑느라 d 에 비례한다. 행렬 계약은 그 행을 정점 수 · 차수와 무관한 상수로 적는다.
 * - **저쪽을 지키고 이쪽을 못 지키는 구현** — 정점 수 곱하기 정점 수의 칸에 무게를 적는 구현.
 *   행렬 계약을 지키지만 이 계약의 `neighbors` 가 차수 1 인 정점에서도 행 하나를 끝까지 훑어 정점
 *   수에 비례하고, `addVertex` 가 칸을 한 줄 · 한 열씩 늘리느라 정점 수에 비례한다.
 * - **둘 다 지키는 구현은 정점 수가 고정된 동안에만 보였다.** 칸마다 「그 간선 항목이 이웃 배열의
 *   몇 번째에 있는가」를 적고 이웃 배열을 함께 들면, 행렬 계약의 행 전부와 이 계약의 `addEdge` ·
 *   `removeEdge` · `neighbors` 행을 함께 지킨다. **`addVertex` 까지 상각 상수로 함께 지키는 구현은
 *   찾지 못했다** — 칸 몫을 늘리는 일이 정점 수를 따라간다. 「없다」가 아니라 「못 찾았다」다
 *   (`docs/ORD-006-conventions.md:2798`).
 * - **같은 호출에 다른 값을 요구하는 자리는 없다** — 불변 사실 181 의 모양이 아니다. 두 계약이
 *   공통 행(`addEdge` · `removeEdge` · `vertexCount`, `neighbors` 의 정점 쪽)에 같은 의미를 적는다.
 *
 * **불변식.**
 * 1. 모든 정점 `u` 에 대해 `neighbors(u)` 의 항목을 세어 더한 수가 `edgeCount()` 와 같다. 단 무방향
 *    그래프에서는 `vertex >= u` 인 항목만 센다.
 * 2. 무방향 그래프에서 `neighbors(u)` 에 `{ vertex: v, weight: w }` 가 있으면 `neighbors(v)` 에
 *    `{ vertex: u, weight: w }` 가 있다.
 *
 * 판별 절차(§규약1 「불변식 판별 절차」)를 돌린 결과다. 둘 다 관측 경로가 둘이고 어느 계약 줄도 그
 * 정합을 적지 않는다. 구현이 두 경로를 따로 유지할 수 있고, 따로 유지하는 순간 갈린다.
 *
 * - 1번은 **세어 둔 수와 늘어놓은 수**다(`hash/hashSet` 의 1번과 같은 모양). 간선 수를 따로 세는
 *   구현이 무게만 바꾸는 `addEdge` 에서 수를 늘리면 갈린다. 무방향에서 `vertex >= u` 만 세는 것은
 *   간선 하나가 두 끝에서 한 번씩 보이고 제자리 간선은 한 번만 보이기 때문이다.
 * - 2번은 **같은 간선을 두 끝에서 읽는 두 경로**다. 무방향 간선을 두 끝에 하나씩 적어 두는 구현이
 *   `removeEdge` 나 무게 바꾸기에서 한쪽만 고치면 갈린다.
 *
 * 후보였다가 빠진 것이 둘이다. 「`neighbors(u)` 의 항목이 가리키는 정점은 `vertexCount()` 미만이다」는
 * 서로 다른 두 양의 순서 관계라 불변식이 아니다(불변 사실 193). 「`neighbors(u)` 에 같은 정점이 두 번
 * 나오지 않는다」는 읽는 경로가 `neighbors(u)` 하나라 첫 물음에서 걸리고 축1이 본다. 담는 모양의
 * 성질(이웃 배열이 몇 개이고 어떻게 늘어서 있는가)은 후보가 아니다(불변 사실 36).
 *
 * **연산 계약.** n 은 `vertexCount()`, `d(x)` 는 정점 x 의 **차수** — `neighbors(x)` 가 돌려주는 항목
 * 수다(방향 그래프에서는 나가는 간선 수). d 로 적힌 행은 n 이 아니라 인자로 받은 정점의 차수를
 * 따라간다(불변 사실 190).
 *
 * | 연산 | 의미 | 상한 | 한정자 |
 * |---|---|---|---|
 * | `constructor(directed = false)` | 정점도 간선도 없는 그래프를 연다. `directed` 가 참이면 방향 그래프, 거짓이면 무방향 그래프이고 바뀌지 않는다 | O(1) | worst |
 * | `addVertex()` | 이웃이 없는 정점 하나를 더하고 그 번호를 돌려준다. 번호는 호출 직전의 `vertexCount()` 다 | O(1) | amortized |
 * | `addEdge(u, v, weight = 1)` | 방향 그래프면 u→v, 무방향이면 u–v 간선을 무게 `weight` 로 둔다. 같은 간선이 이미 있으면 **무게만 바뀌고** `edgeCount()` 는 그대로다. `u === v` 인 제자리 간선도 받는다 | O(d(u) + d(v)) | amortized |
 * | `removeEdge(u, v)` | 그 간선이 있으면 지운다. 없으면 상태가 바뀌지 않는다 | O(d(u) + d(v)) | amortized |
 * | `neighbors(u)` | 방향 그래프면 u 에서 나가는 간선마다, 무방향이면 u 에 닿은 간선마다 `{ vertex: 반대쪽 정점, weight }` 하나. 제자리 간선은 한 번 나온다. **순서를 정하지 않는다.** 돌려준 배열과 그 항목을 고쳐도 그래프는 바뀌지 않는다 | O(d(u)) | worst |
 * | `vertexCount()` | 정점 수 | O(1) | worst |
 * | `edgeCount()` | 간선 수. 무방향 간선 하나는 하나로 센다 | O(1) | worst |
 *
 * **상한을 차수로 적은 것은 고른 것이다.** 약하게 적으면 뜻을 잃는다 — 정점 수로 적으면 칸을 훑는
 * 행렬이, 간선 수로 적으면 간선을 한 줄로 늘어놓은 목록이 들어오고, 둘 다 작은 차수의 정점을 다루는
 * 데 그래프 전체를 치른다. 강하게 적으면 나가는 계열에 이유가 없다 — `addEdge`·`removeEdge` 를
 * 상수로 적으면 같은 간선을 상수에 찾아야 해서 정점마다 이웃을 사전으로 드는 구현만 남는데, 목적이
 * 그것을 요구하지 않고 그 사전이 자명한 구현인지가 미결이다. `neighbors` 의 `O(d(u))` 는 고를
 * 여지가 없다 — 항목 d 개를 새 배열로 내놓으므로 어느 구현도 그 아래로 못 간다.
 *
 * **`addEdge` 에 d 가 드는 이유는 같은 간선을 찾는 일이다.** 있으면 무게만 바꾸고 없으면 새로
 * 두어야 하므로 넣기 전에 있는지 알아야 한다. 무방향이면 반대쪽 끝의 항목도 함께 고친다.
 *
 * **한정자.** 정점이나 간선을 늘리는 두 행(`addVertex`·`addEdge`)은 `amortized` 다 — 넣을 자리가
 * 없으면 어느 구현이든 어디선가 자리를 더 잡으므로 그 계열을 살려야 한다(불변 사실 194).
 * `removeEdge` 는 늘리지 않지만 `amortized` 로 둔다. `worst` 로 올리려면 그것이 **추가로** 배제하는
 * 계열(지운 자리를 표시만 하고 이따금 한꺼번에 치우는 구현)이 약한 쪽을 통과함을 재서 보여야 하는데
 * (불변 사실 183) 그 계열을 짓지 않았다. 읽기 셋은 `worst` 다. `neighbors` 는 호출마다 항목 d 개를
 * 내놓아 어느 구현에서도 d 에 비례하므로 두 한정자가 배제하는 것이 같고(§규약1 「복잡도 한정자」
 * B9 조건), 수 둘은 상태를 바꾸지 않고 값을 읽는 연산이라 그 값을 처음부터 들고 있는 대가가 없다
 * (§규약1 「가장 약한 한정자는 셋을 줄 세우지 않는다」).
 *
 * **주입 정책.**
 * - 주입받는 것이 없다. 정점은 번호이고 번호끼리의 동등성은 수의 비교다. 무게는 아무 `number` 나
 *   받아 그대로 돌려주고 검사하지 않는다 — 이 계약 안에서 무게로 계산하는 연산이 없다.
 * - 정점 번호는 `[0, vertexCount())` 안의 정수여야 한다. 벗어나면 `addEdge` · `removeEdge` ·
 *   `neighbors` 가 `RangeError` 를 던지고 상태는 바뀌지 않는다. `false` 나 빈 배열로 답하지 않는
 *   이유는 그 값이 「간선이 없다」·「이웃이 없다」로 읽혀 잘못된 번호가 조용히 지나가기 때문이다
 *   (`tree/linkCutTree` 의 범위 밖 마디와 같은 선택이다).
 * - `weight` 를 생략하면 1 이다.
 *
 * **검증 등급.** `invariant`.
 * 상한이 자명한 구현으로 달성된다. 정점마다 이웃 항목(반대쪽 정점 · 무게)을 담는 배열 하나와 간선
 * 수를 세는 정수 하나를 두면 일곱 행이 전부 상한 안이다 — 같은 간선은 u 의 배열(무방향이면 v 의
 * 배열까지)을 훑어 찾고, 지운 자리는 마지막 항목으로 메운다. 번호가 조밀하므로 번호에서 배열을 찾는
 * 데 사전이 들지 않는다. 배열 대신 이웃 항목을 마디로 이어 두어도 같다 — **연결 마디는 자명한
 * 구현이다**(`docs/ORD-006-conventions.md:4784`, 2026-09-15 유저 결정). `addVertex`·`addEdge` 가
 * 기대는 배열 늘리기는 언어 배열의 것이고, 계약이 그 행에 적은 한정자가 `amortized` 라 계약 안이다
 * (불변 사실 197). 재균형도 상각 설계도 확률 논증도 없으므로 `complexity` 가 아니고, 불변식 절에
 * 내용이 있으므로 `basic` 도 아니다.
 *
 * **필요충분조건.**
 * - 의미: 정점을 **나중에 늘릴 수 있어야** 하고, 한 쌍에 간선이 하나여야 하며, 방향 여부가 생성 때
 *   정해져야 한다. 정점을 늘리지 못하면 정점 수가 고정된 그래프, 곧 `graph-repr/graphAdjMatrix` 가
 *   서는 자리가 되고, 사이클을 만드는 간선을 거부하면 `graph-repr/dag` 가 된다.
 * - 비용: `neighbors(u)` 가 u 의 차수가 아니라 정점 수에 비례하면 칸을 훑는 행렬이고, 간선 수에
 *   비례하면 간선을 한 줄로 늘어놓은 목록이다. 둘 다 차수가 작은 정점을 다루는 데 그래프 전체를
 *   치르므로 이 구조를 따로 둘 이유가 사라진다. `addVertex` 가 정점 수에 비례하면 칸을 늘리는 행렬이다.
 */
export class GraphAdjList {
  constructor(directed = false) {
    throw new Error("Not implemented");
  }

  addVertex(): number {
    throw new Error("Not implemented");
  }

  addEdge(u: number, v: number, weight = 1): void {
    throw new Error("Not implemented");
  }

  removeEdge(u: number, v: number): void {
    throw new Error("Not implemented");
  }

  neighbors(u: number): Array<{ vertex: number; weight: number }> {
    throw new Error("Not implemented");
  }

  vertexCount(): number {
    throw new Error("Not implemented");
  }

  edgeCount(): number {
    throw new Error("Not implemented");
  }
}
