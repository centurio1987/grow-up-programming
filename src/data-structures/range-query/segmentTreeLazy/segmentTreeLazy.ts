/**
 * SegmentTreeLazy — 구간의 자리마다 주입받은 갱신을 적용하는 일과 임의 구간을 주입받은 결합으로 접는 일을 함께 로그 비용에 두는 수열.
 *
 * **목적.** 자리 수를 생성 시점에 고정한 수열 하나를 두고, **구간 하나의 자리마다 같은 갱신을 적용하는 일**과
 * **임의 구간의 값들을 접어 읽는 일**을 둘 다 자리 수의 로그 비용으로 제공하는 것. 갱신이 덮는 폭과 무관하게
 * 그 비용에 두는 것, 그리고 접는 결합과 적용하는 갱신이 **호출자가 고르는 대수**라는 것 둘이 계약의 내용이다.
 *
 * **`range-query/segmentTree` 와는 서로 담지 않는다**(불변 사실 54). 갈리는 자리는 연산 집합이다.
 *
 * - **저쪽 구현은 이쪽을 못 지킨다 — 구간 갱신이 없고, 곧게 붙이면 폭에 비례한다.** 저쪽 정본에 구간 갱신을 「덮는
 *   자리마다 그 자리 값을 묻고 새 값으로 바꾸기」로 붙인 구현(`_contract/_fixtures/pointwiseLazyRangeFold.ts`)이 이쪽
 *   질의 시나리오를 저쪽 정본 값으로 통과하고(37 · 45 · 53) 갱신 시나리오에서 27,652 · 126,982 · 573,448 로 걸린다.
 *   `docs/ORD-006-conventions.md` B19 표의 「단일 갱신만 하는 구현은 구간 갱신이 $O(k\log n)$ 이다」가 이 실측이다.
 * - **이쪽 구현은 저쪽을 못 지킨다 — 자리 하나를 주어진 값으로 놓는 연산이 없다.** 주입한 갱신이 「덮기」를 담으면
 *   폭 하나짜리 구간 갱신으로 흉내 낼 수 있지만 계약은 그런 갱신을 요구하지 않는다(더하기만 담는 대수에서는 자리를
 *   정해진 값으로 놓을 길이 없다). 대수에 기대는 흉내는 포섭이 아니다.
 *
 * **구간 갱신은 「자리마다 적용한 것」으로 정의하고, 한꺼번에 적용하는 방법을 정하지 않는다.** 표의 `apply` 행이
 * 자리 하나하나의 새 값을 적는다. 여러 자리를 접은 값에 갱신을 한 번에 적용하는 구현, 쌓인 갱신 여럿을 하나로
 * 합성해 두는 구현, 자리마다 적용하는 구현이 모두 **같은 답**을 내야 하고, 그것이 주입 정책의 세 법칙이 필요한
 * 이유다(아래 「주입 정책」). **갱신을 표시해 두었다 나중에 내려보내는 기법을 계약이 적지 않는다** — 이름이 가리키는
 * 기법은 이 계약이 배제하지 않기로 한 계열의 대표일 뿐이고 계약 이름을 다시 붙이는 일은 KAN-031 의 몫이다.
 *
 * **이 계약에 없는 것.**
 * - **자리 하나를 값으로 놓기** — 위 둘째 반례.
 * - **자리를 넣고 빼기 · 지난 상태 묻기.** `n` 은 미리 정해진 자리의 수다. 지난 상태를 되묻는 것은
 *   `range-query/persistentSegmentTree` 의 계약이다.
 * - **공간.** 이 계약은 시간만 말한다(§규약1 「공간은 어느 계약에도 없다」 — 미결).
 *
 * **불변식.** 없다.
 *
 * 판별 절차(§규약1 「불변식 판별 절차」)를 돌린 결과이고, 후보가 셋이었다.
 *
 * - **「구간을 둘로 쪼개 접으면 통째 구간과 같다」** — 그 값을 읽는 공개 연산이 `query` 하나라 첫 물음에서 걸린다
 *   (`range-query/segmentTree` 와 같다).
 * - **「갱신 뒤 구간의 답은 갱신 전 자리마다 갱신을 적용하고 접은 값이다」** — 두 상태에 걸친 성질이고 `apply`·`query`
 *   두 행의 의미에서 그대로 따라 나온다(불변 사실 153·154). 축1이 매 연산 대조한다.
 * - **담는 모양의 성질**(어느 마디에 어떤 갱신이 쌓여 있는가)은 후보가 아니다. 적는 순간 표현을 처방한다(불변 사실 36).
 *
 * **연산 계약.** n 은 생성자가 고정한 **자리 수**(`values.length`)다. **두 행의 상한은 주입 정책의 세 법칙이 설 때
 * 선다**(불변 사실 142 — 상한이 주입자의 의무에 기대면 조건부로 적는다).
 *
 * | 연산 | 의미 | 상한 | 한정자 |
 * |---|---|---|---|
 * | `constructor(values, combine, identity, act, compose)` | 자리 `0` 부터 `n-1` 까지를 `values` 의 값으로 둔다. 넷은 아래 「주입 정책」 | O(n) | worst |
 * | `apply(from, to, update)` | 자리 `from` 이상 `to` 미만의 **자리마다** 그 자리 값 `v` 를 `act(update, v, 1)` 로 바꾼다. `from === to` 면 아무것도 안 바뀐다 | O(log n) | worst |
 * | `query(from, to)` | 자리 `from` 이상 `to` 미만의 값을 **왼쪽에서 오른쪽으로** `combine` 으로 접은 값. `from === to` 면 `identity` | O(log n) | worst |
 *
 * **구간은 반열린 것으로 적는다**(`range-query/segmentTree` 와 같은 이유 — 빈 구간을 값으로 적어야 항등원 요구가
 * 관측된다). 물려받은 표면은 닫힌 구간이었다.
 *
 * **상한이 세는 것은 주입된 세 함수를 부른 횟수다**(`segmentTree` 와 같다). 한 번이 얼마나 비싼지는 주입자가 정한다.
 *
 * **상한 `O(log n)` 은 정본이 서는 계급으로 골랐다**(불변 사실 51).
 *
 * - **`O(n)` 은 계약이 뜻을 잃는다.** 값을 그대로 적어 두고 덮는 자리를 하나씩 고치고 물을 때 훑으면 그 상한이
 *   나온다(`_contract/_fixtures/scanningLazyRangeFold.ts` — 갱신 2,046 · 8,190 · 32,766, 질의 2,044 · 8,188 · 32,764).
 * - **`O(sqrt n)` 은 남는 후보이고 하한이 아니다.** 자리를 묶음으로 나눠 묶음마다 접은 값과 쌓인 갱신을 드는 구현이
 *   그 상한 안에 들 것이고 값 배열은 여전히 배제된다 — `segmentTree` 헤더가 같은 자리를 실측으로 적었고 여기서는 짓지
 *   않았다. 목적이 이 계열을 자르지 못하므로 정본이 서는 계급을 적는다.
 * - **`O(1)` 은 만족하는 구현을 찾지 못했고, 없다는 것을 보이지도 않았다**(불변 사실 165).
 *
 * **두 행이 `worst` 인 것.** 강한 쪽을 적는 조건 둘(불변 사실 59 · §「한정자의 강한 읽기는 축이 그 강함을 잴 때만
 * 적는다」)을 둘 다 채운다.
 *
 * - **추가로 배제되는 계열은 갱신을 쌓아 두었다 질의가 올 때 한꺼번에 적용하는 계열이다**
 *   (`_contract/_fixtures/bufferedLazyRangeFold.ts`). 좁은 갱신 n 번 뒤 질의 n 번을 전부 재면 단일 호출 최대가 50,650 ·
 *   230,490 · 1,016,992 로 걸리고 호출 평균은 53.70 · 63.13 · 72.03 으로 통과한다. **정본이 두 행을 호출마다 지키므로
 *   그 계열을 배제해도 잃는 것이 없다**(`heap/vanEmdeBoasTree` 의 같은 근거).
 * - 축3의 `worst` 통계(단일 호출 최대)가 그 강함을 잰다 — 스위트의 질의 시나리오에서 그 계열이 53,853 · 234,460 ·
 *   1,021,717 로 걸린다.
 * - `constructor` 는 n 에 대해 반복 호출되지 않아 강한 한정자를 적는 대가가 없고 축3도 재지 않는다(§규약2 시나리오
 *   규칙 2 — 면제).
 *
 * **주입 정책.**
 *
 * - **넷을 생성자로 받는다 — 결합 `combine`, 그 항등원 `identity`, 갱신 적용 `act(update, value, count)`, 갱신 합성
 *   `compose(later, earlier)`. 여는 것이 이 계약의 내용이다.** `range-query/segmentTree` 가 결합과 항등원을 연 근거
 *   (§규약1 「주입 정책도 상한과 같은 두 물음으로 정한다」)를 갱신 쪽에 그대로 물었다.
 *   - **갱신을 닫으면**(물려받은 표면처럼 「더하기」 하나로) 구간을 덮는 호출자 · 곱하는 호출자가 통째로 나가고, 더하기와
 *     합에만 서는 기법(앞구간 차로 답하는 계열 — 짓지 않았다)이 다시 들어온다 — 결합을 닫았을 때 `segmentTree` 에서 일어나는 일과 같다.
 *   - **`act` 가 덮는 자리 수 `count` 를 받는 이유** — 값이 수 하나라서, 폭에 기대는 갱신(구간 더하기 뒤 합은
 *     `value + update × count`)을 적을 자리가 그 인자뿐이다. 빼면 물려받은 문제의 쓰임(구간 더하기 · 구간 합)이 나간다.
 *     값이 수 하나라 (합, 자리 수) 같은 쌍을 값으로 쓰는 대수는 여전히 적지 못한다 — 자리 수 인자가 그중 가장 흔한 쓰임을
 *     받는다.
 *   - **`compose` 를 받는 이유** — 같은 자리 묶음에 쌓인 갱신 여럿을 하나로 줄이는 길이 주입된 함수 밖에 없다. 받지 않고
 *     로그 상한을 지키는 구현을 이 유닛이 찾지 못했다(없다고 보이지는 않았다).
 *   - **「아무것도 안 하는 갱신」은 받지 않는다.** 구현이 「쌓인 갱신 없음」을 스스로 표시할 수 있어, 받아도 새로 들어오는
 *     구현이 없고 주입자의 의무만 는다.
 * - **세 법칙이 주입자의 의무다.** 계약이 **자리를 묶는 방법도, 갱신을 한꺼번에 적용하는지도, 쌓인 갱신을 합성하는지도**
 *   정하지 않으므로, 답이 구현마다 하나로 정해지려면 셋이 서야 한다.
 *   1. **결합법칙과 항등원** — `combine(combine(a, b), c) === combine(a, combine(b, c))`, `combine(identity, x) ===
 *      combine(x, identity) === x`. `segmentTree` 와 같다.
 *   2. **분배** — 이어지는 자리 `m` 개를 접은 `a` 와 그 뒤 자리 `k` 개를 접은 `b` 에 대해 `act(u, combine(a, b), m + k)
 *      === combine(act(u, a, m), act(u, b, k))`, 그리고 `act(u, identity, 0) === identity`. 여러 자리를 접은 값에 갱신을
 *      한 번에 적용한 것이 자리마다 적용한 뒤 접은 것과 같다는 뜻이다.
 *   3. **합성** — `act(compose(later, earlier), v, k) === act(later, act(earlier, v, k), k)`. 쌓인 갱신을 합성해 한 번
 *      적용한 것이 차례대로 적용한 것과 같다는 뜻이다.
 * - **교환법칙은 결합에도 합성에도 요구하지 않는다.** 계약이 자리 순서(왼쪽에서 오른쪽)와 합성 순서(나중 갱신이 바깥)를
 *   고정한다. 이 두 줄은 실제로 계열을 배제한다 — 정본에서 질의의 두 통을 하나로 합친 변이와 합성 인자를 뒤집은 변이가
 *   교환적이지 않은 대수에서 각자 경계 케이스 하나로 갈린다(`_contract/_fixtures/misorderedLazyRangeFold.ts`).
 * - **세 법칙을 계약이 검사하지 않는다** — 값과 갱신의 정의역을 계약이 모른다. **어기면 답이 구현마다 갈린다**: 합 결합에
 *   「자리마다 제곱」 갱신을 주면 정본은 `[1, 3]` 의 두 자리를 덮는 마디에 한 번 적용해 16 을 내고 값 배열은 10 을 낸다.
 * - **자리 번호와 구간 경계가 범위 밖이거나 `from > to` 면 두 연산 모두 `RangeError` 를 던지고 상태가 바뀌지 않는다.**
 *   `segmentTree` 와 같은 판별이다 — `query` 의 반환 타입에 「그런 구간은 없다」를 담을 자리가 없고, `apply` 는 돌려주는
 *   값이 없다.
 * - **`values` 가 빈 배열인 것은 정당하다.** 부를 수 있는 것은 빈 구간 `[0, 0)` 뿐이다.
 *
 * **물려받은 표면을 넷 고쳤다.** 스텁에 계약 헤더가 없었고 물려받은 문서만 있었다. 물려받은 상한 주장은 없었다.
 *
 * - **주입을 열었다** — `rangeAdd`·`rangeSum` 의 더하기 · 합을 넷의 주입으로(위 「주입 정책」).
 * - **`constructor(n)` → `constructor(values, …)`.** 모든 자리를 0 으로 시작하는 것은 합의 항등원에만 뜻이 있고, 자리 하나를
 *   값으로 놓는 연산이 없는 계약이라 초기 값을 받지 않으면 호출자가 임의의 수열에서 시작할 길이 없다.
 * - **닫힌 구간 → 반열린 구간, `rangeAdd`·`rangeSum` → `apply`·`query`.** 이름이 더하기·합을 가리키지 않게 했고, `query` 는
 *   `segmentTree` 와 같은 뜻의 연산을 같은 이름으로 둔다.
 * - **스토리 · 크기 제약 · 벽시계 성능 시험을 뺐다**(불변 사실 7·38). 범위 밖 인자를 `RangeError` 로 적었다(불변 사실 82).
 *
 * **검증 등급.** `complexity`.
 * 두 행을 **함께** 만족하는 자명한 구현이 없다. 배열로 적는 길이 둘이고 둘 다 한쪽을 놓는다.
 *
 * - **값을 그대로 적어 두면** 갱신과 질의가 둘 다 구간 폭에 비례한다(위 수치).
 * - **모든 구간의 답을 미리 접어 두면** 질의가 상수인 대신 갱신 한 번이 그 갱신과 겹치는 구간 전부를 다시 접어야 하고,
 *   그 수가 자리 수의 제곱에 비례한다.
 *
 * 두 행을 함께 로그 안에 두려면 서로 다른 길이의 구간을 미리 접어 두고 **그 접은 값에 갱신을 한 번에 적용하며 아래로는
 * 미뤄 두는** 설계가 들어와야 하고, 그것은 배열의 기본 연산에서 나오지 않는다. `segmentTree` 의 자리 하나 바꾸기로 푸는
 * 길은 자명하지 않은데도 갱신이 폭에 비례한다(위 첫째 반례).
 *
 * **필요충분조건.**
 * - 의미: 구간의 자리마다 갱신을 적용하는 일과 임의 구간을 접어 읽는 일 둘을 **세 법칙을 지키는 주입 대수** 위에서 제공한다.
 *   구간 갱신을 버리면 한 번 짓고 묻기만 하는 표이거나(`segmentTree` 의 자리 하나 바꾸기를 더하면 그 계약이다), 구간 읽기를
 *   버리면 그냥 배열이다. **분배를 버리면** 접어 둔 값 하나로 갱신 뒤의 답이 정해지지 않는다 — 합 결합에서 `[1, 3]` 과
 *   `[2, 2]` 는 합 4 · 자리 수 2 가 같은데 자리마다 제곱한 뒤의 합이 10 과 8 이라, 주입된 함수로만 값을 다루는 구현은 덮은
 *   자리를 하나씩 건드려야 한다. **합성을 버리면** 쌓인 갱신 여럿을 하나로 줄이지 못한다. 셋이 서면 로그 구현이 있다(정본).
 * - 비용: 둘 다 지키더라도 **갱신이 덮는 폭에 비례하면** 이 구조가 아니다 — 값 배열이거나, 자리 하나 바꾸기로 푼
 *   `segmentTree` 다. 질의가 호출열 평균으로만 로그이고 한 호출이 쌓인 갱신 수에 비례하면 쌓아 두는 계열이고 그것도 이
 *   계약이 아니다. 이 계약이 요구하는 것은 **둘을 호출마다** 로그 안에 두는 것이다.
 */
export class SegmentTreeLazy {
  constructor(
    values: number[],
    combine: (a: number, b: number) => number,
    identity: number,
    act: (update: number, value: number, count: number) => number,
    compose: (later: number, earlier: number) => number,
  ) {
    throw new Error("Not implemented");
  }

  apply(from: number, to: number, update: number): void {
    throw new Error("Not implemented");
  }

  query(from: number, to: number): number {
    throw new Error("Not implemented");
  }
}
