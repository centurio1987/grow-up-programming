/**
 * SparseTable — 생성 시점에 고정된 수열의 임의 구간을 주입받은 멱등 결합으로 접는 일을, 한 번 짓고 나면 상수 비용에 두는 불변 구조.
 *
 * **목적.** 수열 하나를 받아 **한 번 짓고**, 그 뒤로 **임의 구간의 값들을 접어 읽는 일**을 자리 수와 무관한 상수 비용으로
 * 제공하는 것. 짓는 일은 자리 수 × 로그 안이다. 수열은 **생성 시점에 정해지고 그 뒤로 바뀌지 않는다** — 이 계약에는 상태를
 * 바꾸는 연산이 하나도 없다(불변 구조 — §규약1 「불변 구조」). 짓고 난 뒤의 질의를 상수에 두는 것, 그리고 접는 결합이
 * **호출자가 고르는 멱등 대수**라는 것 둘이 계약의 내용이다.
 *
 * **`range-query/segmentTree` 와는 서로 담지 않는다**(불변 사실 54). 한쪽 반례는 축3이 못 본다.
 *
 * - **저쪽 구현은 이쪽을 못 지킨다 — 질의가 로그다.** 저쪽 정본은 이쪽 표면(생성자 · `query`)을 그대로 갖고 답도 옳은데
 *   질의가 `O(log n)` 이다. **축3은 이 위반을 통과시킨다** — 질의 37 · 45 · 53(비율 1.22 · 1.18)이 `O(1)` 구간 안이고 구성
 *   3,070 · 12,286 · 49,150(비율 4.00)이 `O(n log n)` 구간 안이다(불변 사실 53 · 62 — `src/data-structures/_contract/runContract.sparseTable.test.ts`
 *   첫째 묶음이 이 통과를 이름으로 고정한다).
 * - **이쪽 구현은 저쪽을 못 지킨다 — 자리 하나를 고치는 연산이 없고, 결합을 멱등으로 좁혔다.**
 *
 * **이 계약에 없는 것.**
 * - **자리 고치기 · 넣기 · 빼기.** 고치는 일이 들어오면 `range-query/segmentTree` 계열이다.
 * - **멱등이 아닌 결합(합 · 곱 · 이어 붙이기).** 아래 「주입 정책」과 「필요충분조건」 — 상수 질의에 멱등이 필요해서 뺀 것이
 *   아니다.
 * - **공간.** 이 계약은 시간만 말한다(§규약1 「공간은 어느 계약에도 없다」 — 미결).
 *
 * **이 이름은 계약을 정당화하지 않는다.** 계약의 어느 문장도 표의 모양을 말하지 않는다 — 구간을 겹치지 않게 접어 두는 구현도
 * 두 행을 전부 지킨다(`_contract/_fixtures/nonOverlappingFoldTable.ts`). 이름이 가리키는 기법은 이 계약이 배제하지 않기로 한
 * 계열의 대표일 뿐이고, 계약 이름을 다시 붙이는 일은 KAN-031 의 몫이다.
 *
 * **불변식.** 없다.
 *
 * 판별 절차(§규약1 「불변식 판별 절차」)를 돌린 결과이고, 후보가 셋이었다.
 *
 * - **「구간을 둘로 쪼개 접으면 통째 구간과 같다」** — 그 값을 읽는 공개 연산이 `query` 하나라 첫 물음에서 걸린다
 *   (`range-query/segmentTree` 와 같다).
 * - **「지은 뒤 같은 구간의 답은 늘 같다」** — 역시 읽는 경로가 `query` 하나라 첫 물음에서 걸리고, 상태를 바꾸는 연산이 없어
 *   「두 상태에 걸친 성질」이 생기지도 않는다.
 * - **담는 모양의 성질**(어느 칸이 어느 구간을 접어 들고 있는가)은 후보가 아니다. 적는 순간 표현을 처방한다(불변 사실 36).
 *
 * **그리고 불변 구조라 판별 결과와 무관하게 축2가 축1보다 더 잡는 것이 없다**(불변 사실 52 ①) — 축2가 볼 수 있는 상태가 생성
 * 직후 하나뿐이고, 그 하나에서 관측값들이 맞는지는 축1이 매 연산 대조한다. 절이 빈 이유는 이 문장이 아니라 위 판별이다.
 *
 * **연산 계약.** n 은 생성자가 받은 **자리 수**(`values.length`)다.
 *
 * | 연산 | 의미 | 상한 | 한정자 |
 * |---|---|---|---|
 * | `constructor(values, combine, identity)` | 자리 `0` 부터 `n-1` 까지가 `values` 의 값인 수열을 짓는다. **돌아온 뒤의 질의는 아래 상한을 지킨다.** 돌아온 뒤 호출자가 `values` 를 고쳐도 답은 바뀌지 않는다 | O(n log n) | worst |
 * | `query(from, to)` | 자리 `from` 이상 `to` 미만의 값을 **왼쪽에서 오른쪽으로** `combine` 으로 접은 값. `from === to` 면 `identity` | O(1) | worst |
 *
 * **구성 시점을 계약이 정한다**(불변 사실 52 ③). 표의 「돌아온 뒤의 질의는 아래 상한을 지킨다」가 그 줄이다. 적지 않으면
 * 생성자는 베끼기만 하고 첫 질의가 표를 짓는 구현이 두 상한을 함께 만족하는 것처럼 읽힌다 — 실측으로 그 구현
 * (`_contract/_fixtures/deferredSparseTable.ts`)의 생성자가 1,024 · 4,096 · 16,384 로 구성 상한 안이고, 짓자마자 묻는 첫 질의가
 * 17,435 · 86,047 · 409,635 로 질의 상한을 넘는다.
 *
 * **구성과 묶어 적을 질의가 없다**(불변 사실 52 ②). `query` 는 인자가 있어 반복 호출되고, 인자 공간은 유한하지만(구간 쌍) 호출
 * 때 폭만큼 훑으면 이 계약의 목적이 사라진다 — §규약1 「불변 구조」 표의 셋째 줄(`at`·`rankOf`)이라 상한을 따로 적는다.
 *
 * **구간은 반열린 것으로 적는다**(`range-query/segmentTree` 와 같은 이유 — 빈 구간을 값으로 적어야 항등원 요구가 관측된다,
 * 불변 사실 170). 물려받은 표면은 닫힌 구간이었다.
 *
 * **상한이 세는 것은 주입된 결합을 부른 횟수와 지나간 칸 수다**(`segmentTree` 와 같다). 폭에서 층을 고르는 비트 연산 한 번은
 * 칸을 지나지 않는다.
 *
 * **두 상한은 정본이 서는 계급으로 골랐다**(불변 사실 51).
 *
 * - **질의를 `O(log n)` 으로 적으면 고치기를 버리고 얻는 것이 없어진다** — `range-query/segmentTree` 정본이 고치기까지 함께 두면서
 *   그 질의 상한을 낸다. `O(sqrt n)` 이상이면 묶음 표가 들어오고, 값 배열(질의가 폭 — 2,044 · 8,188 · 32,764)은 어느 쪽이든
 *   배제된다. 목적이 상수를 요구한다 — 다만 `O(log n)` 과의 차이는 축3이 못 본다(위 첫째 반례).
 * - **구성 `O(n)` 은 만족하는 구현을 찾지 못했고, 없다는 것을 보이지도 않았다**(불변 사실 165). 상수 질의를 주는 두 구현(정본 ·
 *   겹치지 않게 접기)이 둘 다 자리 수 × 층 수를 짓는다. **구성 `O(n^2)` 은 계약이 뜻을 잃는다** — 모든 구간의 답을 미리 접어 두는
 *   구현이 들어온다. 칸마다 그 구간을 처음부터 훑어 짓는 구현(`_contract/_fixtures/rescanningSparseTable.ts`)이 1,399,124 ·
 *   22,373,716 · 357,930,324(비율 15.99)로 그 계급에서 걸린다.
 *
 * **두 행이 `worst` 인 것.** §「`worst` 만 있는 계약의 한정자 근거는 「배제될 계열이 없다」로 적는다」(T1-06)가 든 두 계열 —
 * 비용을 뒤 호출로 미루는 계열(상태를 바꾸는 연산이 있어야 선다)과 무작위성으로 상한을 얻는 계열(답이 입력의 함수로 유일하다)
 * — 은 이 계약에 없다. **셋째 계열이 하나 있다 — 구성을 첫 질의로 미루는 계열.** 공개 연산이 상태를 바꾸지 않아도 구현이 안에서
 * 짓기를 미룰 수 있다. 짓자마자 질의 n 번을 전부 재면 그 계열의 단일 호출 최대가 17,435 · 86,047 · 409,635 로 걸리고 호출 평균은
 * 20.02 · 24.01 · 28 로 통과한다(생성자 몫을 질의 n 번에 나눈 평균이라 로그를 따라 자라고 축3 해상도 아래다). 정본은 두 통계
 * 모두 3 이라 **그 계열을 배제해도 잃는 것이 없고**(불변 사실 59) 축3의 단일 호출 최대가 그 강함을 잰다(§「한정자의 강한 읽기는
 * 축이 그 강함을 잴 때만 적는다」). 구성 행은 n 에 대해 반복 호출되지 않아 강한 한정자를 적는 대가가 없다.
 *
 * **주입 정책.**
 *
 * - **결합과 그 항등원을 생성자로 받는다.** `range-query/segmentTree` 가 연 근거(닫으면 · 모노이드보다 넓게 열면 — §「주입
 *   정책도 상한과 같은 두 물음으로 정한다」)를 그대로 물었고 답도 같다.
 * - **세 법칙이 주입자의 의무다.**
 *   1. **결합법칙** — `combine(combine(a, b), c) === combine(a, combine(b, c))`. 계약이 접는 차례를 정하지 않으므로 답이 하나로
 *      정해지려면 서야 한다(`segmentTree` 와 같다).
 *   2. **항등원** — `combine(identity, x) === combine(x, identity) === x`.
 *   3. **멱등** — `combine(x, x) === x`. **이 줄이 `segmentTree` 와 갈리는 자리다.** 계약이 구간을 덮는 조각끼리 **겹쳐도 되는지**를
 *      정하지 않으므로, 겹쳐 덮는 구현과 겹치지 않게 덮는 구현이 같은 답을 내려면 두 번 덮인 조각이 한 번으로 줄어야 한다.
 * - **멱등을 어기면 답이 구현마다 갈린다** — 합 결합에서 `[1, 2, 3]` 전체를 정본(겹치는 두 칸)은 8 로, 값 배열과 겹치지 않게 접는
 *   구현은 6 으로 답한다. **계약이 멱등을 검사하지 않는다** — 값의 정의역을 계약이 모른다.
 * - **멱등을 요구하는 것은 고른 것이다 — 두 물음으로 정했다.**
 *   - **모노이드로 넓게 열면**(멱등을 빼면) 합 · 곱을 쓰는 호출자가 들어오는 대신 **겹쳐 덮는 구현 계열이 전부 나간다.** 그 계열은
 *     겹치지 않게 덮는 구현과 같은 계급이라(구성 17,432 · 86,044 · 409,632 대 21,504 · 102,400 · 475,136 / 질의 3 대 4) 배제할
 *     이유가 목적에 없다 — 넓게 여는 것이 곧 「겹치지 않게 덮으라」는 처방이 된다.
 *   - **교환적인 멱등(최솟값 · 최댓값 · 최대공약수)으로 좁히면** 조각을 아무 차례로나 접는 구현이 들어오는 대신 「왼쪽에서 처음
 *     만나는 것」 같은 호출자가 나간다. 차례를 지키는 일은 어느 구현에서도 계급을 바꾸지 않으므로(두 조각을 이어 붙이는 순서 하나)
 *     좁힐 이유가 없다 — **교환법칙은 요구하지 않는다.** 이 줄은 실제로 계열을 배제한다: 정본에서 두 칸의 차례를 뒤집은 사본이
 *     경계 케이스 「왼쪽부터 접는다」 둘째 걸음에서 관측 7 / 모델 2 로 갈린다.
 *   - `range-query/fenwickTree` 가 결합을 닫아 앞구간 차로 답하는 구현을 지킨 것(T4-01)과 같은 쪽의 선택이다 — 호출자를 덜 받고
 *     구현 계열을 지킨다.
 * - **구간 경계가 범위 밖이거나 정수가 아니거나 `from > to` 면 `RangeError` 를 던진다.** `segmentTree` 와 같은 판별이다(불변 사실
 *   82) — 반환 타입에 「그런 구간은 없다」를 담을 자리가 없다.
 * - **`values` 가 빈 배열인 것은 정당하다.** 부를 수 있는 것은 빈 구간 `[0, 0)` 뿐이다.
 * - **생성자는 `values` 를 읽기만 한다** — 표의 생성자 행 그대로다.
 *
 * **물려받은 표면을 넷 고쳤다.** 물려받은 상한 주장(빌드 `O(n log n)` · 질의 `O(1)` · 공간 `O(n log n)`)은 판정 입력으로 쓰지
 * 않았다(불변 사실 65) — 판정은 위 두 상한 문단에서 다시 냈고 공간 줄은 계약에 없다.
 *
 * - **항등원을 받는다** — `constructor(arr, merge)` → `constructor(values, combine, identity)`. 빈 구간의 답이 그것이다.
 * - **닫힌 구간 → 반열린 구간**(`query(l, r)` → `query(from, to)`).
 * - **멱등을 주석의 「주의」에서 주입자의 의무로 옮기고 근거를 바꿨다.** 물려받은 문서는 「멱등이 아니면 O(1) 질의가 불가능하다」로
 *   적었는데 **거짓이다** — 겹치지 않게 접는 구현이 합에서 옳은 답을 질의 4 · 4 · 4 로 낸다(아래 「필요충분조건」).
 * - **내부 필드(`_n`·`_log`·`_table`·`_merge`)와 「2의 거듭제곱 구간 표」 처방 · 스토리 · 크기 제약 · 벽시계 성능 시험을 뺐다**
 *   (불변 사실 7·20·38). 범위 밖 인자를 `RangeError` 로 적었다.
 *
 * **검증 등급.** `complexity`.
 * 두 행을 **함께** 만족하는 자명한 구현이 없다. 배열로 적는 길이 셋이고 전부 한쪽을 놓는다.
 *
 * - **값을 그대로 적어 두면** 구성이 자리 수인 대신 질의가 구간 폭에 비례한다(위 수치).
 * - **모든 구간의 답을 미리 접어 두면** 질의가 상수인 대신 구성이 자리 수의 제곱이다.
 * - **앞구간을 미리 접어 두는 길은 후보가 아니다** — 멱등 결합에서 되돌리는 값을 가진 값은 항등원뿐이라(`x` 에 되돌리는 값을
 *   붙이면 `combine(x, x) === x` 에서 `x === identity`) 앞구간 둘에서 구간을 얻지 못한다(`segmentTree` 와 같은 자리).
 *
 * 두 행을 함께 두려면 **길이가 서로 다른 구간을 미리 접어 두고 질의가 그중 몇 개를 고르는** 설계가 들어와야 하고, 그것은 배열의
 * 기본 연산에서 나오지 않는다.
 *
 * **필요충분조건.**
 * - 의미: 한 번 지은 수열의 임의 구간을 **세 법칙(결합법칙 · 항등원 · 멱등)을 지키는 주입 결합**으로 왼쪽부터 접어 읽는다. 구간
 *   읽기를 버리면 그냥 배열이고, 고치기를 더하면 `range-query/segmentTree` 계열이다. **멱등은 상수 질의의 필요조건이 아니고
 *   충분조건이다** — 충분한 것은 정본(겹치는 두 칸을 접는다)이 보이고, 필요하지 않은 것은 결합법칙만 쓰는 구현이 멱등이 아닌 합에서
 *   옳은 답을 같은 계급(구성 21,504 · 102,400 · 475,136 / 질의 4 · 4 · 4)으로 내는 것이 보인다
 *   (`src/data-structures/_contract/runContract.sparseTable.test.ts` 셋째 묶음). 그래서 멱등은 **이 계약이 겹쳐 덮는 구현 계열을
 *   들이려고 주입자에게 거는 의무**이고, 빼면 계약이 그 계열을 내보내는 다른 계약이 된다.
 * - 비용: 둘 다 지키더라도 **질의가 구간 폭에 비례하면** 값 배열이고, **구성이 자리 수의 제곱이면** 모든 구간을 미리 접은 표이며,
 *   **짓기를 첫 질의로 미뤄 그 한 호출이 표 전부를 지으면** 구성 시점을 어긴 것이다 — 셋 다 이 구조가 아니다. 질의가 로그인 구현도
 *   이 계약이 아니지만 그 갈림은 축3이 못 본다.
 */
export class SparseTable {
  constructor(
    values: number[],
    combine: (a: number, b: number) => number,
    identity: number,
  ) {
    throw new Error("Not implemented");
  }

  query(from: number, to: number): number {
    throw new Error("Not implemented");
  }
}
