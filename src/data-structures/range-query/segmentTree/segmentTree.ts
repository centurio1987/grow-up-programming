/**
 * SegmentTree — 자리 하나를 고치는 일과 임의 구간을 주입받은 결합으로 접는 일을 함께 로그 비용에 두는 수열.
 *
 * **목적.** 자리 수를 생성 시점에 고정한 수열 하나를 두고, **자리 하나의 값을 바꾸는 일**과
 * **임의 구간의 값들을 주입받은 결합으로 접어 읽는 일**을 둘 다 자리 수의 로그 비용으로
 * 제공하는 것. 두 일을 **함께** 그 비용에 두는 것, 그리고 접는 연산이 **호출자가 고르는
 * 것**이라는 것 둘이 계약의 내용이다.
 *
 * **`range-query/fenwickTree` 와 갈리는 자리는 주입 정책 한 줄이다.** 저쪽 계약은 접는 연산을
 * 열지 않고 **덧셈** 하나만 요구하며, 이쪽은 결합과 항등원을 주입받는다. 반례는 계약의 용어
 * 만으로 적힌다 — **구간의 답을 두 앞구간의 차로 내는 구현**은 저쪽 네 행을 전부 지키고
 * 이쪽은 지키지 못한다. 되돌리는 값이 없는 결합에서 「전체에서 앞을 뺀다」가 성립하지 않기
 * 때문이다. **실행해 확인했다**(`src/data-structures/_contract/_fixtures/prefixDifferenceRangeFold.ts`) —
 * 같은 구현이 결합이 덧셈일 때 400 회 호출을 정본과 한 자리도 다르지 않게 답하고, 최솟값을
 * 주입하면 두 번째 호출 `query(6, 8)` 에서 8 을 낸다(정본은 0 이다). 반대 방향은 담긴다:
 * 임의의 결합을 접을 수 있으면 덧셈도 접을 수 있으므로 이 계약을 만족하는 구현은 저쪽 계약도
 * 만족한다. **포섭은 「같다」가 아니라 「다르다」의 증거다**(불변 사실 43).
 *
 * **표면이 저쪽과 둘 갈리고, 그중 둘 다 그 한 줄에서 따라 나온다.**
 *
 * - **갱신이 「더한다」가 아니라 「바꾼다」다.** 되돌리는 값이 없는 결합에서 「자리에 더한다」가
 *   정의되지 않는다. 자리의 값을 새 값으로 놓는 것만이 임의의 결합에 대해 뜻을 갖는다.
 * - **앞구간을 따로 묻는 연산이 없다.** 앞구간이 특별한 자리가 되는 것은 구간을 앞구간 둘에서
 *   얻을 수 있을 때뿐이고, 그 길을 닫은 것이 이 계약이다. `query(0, i)` 가 앞구간을 이미
 *   적는다.
 *
 * **생성자가 초기 수열을 받는 것은 위 한 줄에서 따라 나오지 않는다.** 항등원을 주입받으므로
 * 「자리 수만 받고 전부 항등원으로 시작한다」도 적을 수 있는 계약이다. 물려받은 표면을 그대로
 * 둔 이유는 하나다 — 초기 수열을 안 받으면 호출자가 `update` 를 n 번 불러 채워야 하고, 그때
 * 짓는 일의 상한이 계약에서 사라진다. 아래 표의 첫 행이 그 사실을 적을 자리를 지킨다.
 *
 * **구간을 한 번에 고치는 일은 이 계약에 없다.** 여기서 고치는 것은 자리 하나이고, `k` 개
 * 자리를 고치려면 호출이 `k` 번이다. 구간을 통째로 고치는 연산을 넣으면 배제하는 구현 집합이
 * 통째로 달라진다 — 고치는 일을 표시만 해 두었다가 나중에 내려보내는 설계가 새로 요구되고,
 * 자리 하나씩만 고칠 줄 아는 구현이 전부 나간다. 그것은 `range-query/segmentTreeLazy` 가 받는
 * 계약이다.
 *
 * **자리를 넣고 빼는 일도, 지난 상태를 다시 묻는 일도 이 계약에 없다.** 자리 수는 생성자가
 * 정하고 그 뒤로 바뀌지 않으므로 `n` 은 담긴 원소 수가 아니라 **미리 정해진 자리의 수**이고,
 * 값이 항등원인 자리도 자리다. 지난 상태를 되묻는 것은 `range-query/persistentSegmentTree` 의
 * 계약이다.
 *
 * **이 계약은 시간만 말한다.** 저장 공간에 대한 조건이 없으므로 자리 수보다 훨씬 많은 자리를
 * 쓰는 구현도 이 계약을 지킨다(§규약1 「공간은 어느 계약에도 없다」 — 미결).
 *
 * **이 이름은 계약을 정당화하지 않는다.** 계약의 어느 문장도 나무·마디·잎을 말하지 않고,
 * 말할 수 없다 — 명세는 내부 표현을 처방하지 않는다. 자리를 $\sqrt n$ 개씩 묶고 묶음을 다시
 * 묶어 층을 쌓는 구현도 세 행을 전부 지킨다. 이름이 가리키는 기법은 **이 계약이 배제하지
 * 않기로 한 계열**의 대표일 뿐이고, 계약 이름을 다시 붙이는 일은 KAN-031 의 몫이다.
 *
 * **불변식.** 없다.
 *
 * 판별 절차(§규약1 「불변식 판별 절차」)를 돌린 결과이고, 후보가 셋이었다.
 *
 * - **「구간을 둘로 쪼개 접으면 통째 구간과 같다」** — 그 값을 읽는 공개 연산이 `query`
 *   **하나**라 첫 번째 물음에서 걸린다. 대조할 상대가 없고, 그 조건은 `query` 행의 의미이므로
 *   축1(참조 모델 대조)의 몫이다. **`range-query/fenwickTree` 가 정확히 여기서 갈린다** —
 *   저쪽은 앞구간을 읽는 경로가 `prefixSum` 과 `rangeSum` 둘이라 같은 후보가 불변식으로
 *   남았다. 관측 경로의 수는 **연산 집합이 정하고**, 이 계약의 연산 집합은 앞구간을 따로 두지
 *   않는다. 불변식 절이 비는 것이 계약이 약속을 덜 한다는 뜻이 아니다.
 * - **「고친 자리 하나만 답이 달라진다」** — 갱신 전후 두 상태에 걸친 성질이라 상태의 성질이
 *   아니고, `update` 행과 `query` 행의 의미에서 그대로 따라 나온다. 축1의 몫이다.
 * - **담는 모양의 성질**(어느 마디가 어느 구간을 접어 들고 있는가)은 후보가 아니다. 계약에
 *   적어야 검사할 수 있는데 **적는 순간 그것이 표현을 처방한다**(불변 사실 36).
 *
 * **연산 계약.** n 은 생성자가 고정한 **자리 수**(`values.length`)다. 담긴 원소 수가 아니라
 * 미리 정해진 자리의 수이고, 구조가 사는 동안 바뀌지 않는다.
 *
 * | 연산 | 의미 | 상한 | 한정자 |
 * |---|---|---|---|
 * | `constructor(values, combine, identity)` | 자리 `0`부터 `n-1`까지를 `values` 의 값으로 둔다. `combine` 은 접는 연산이고 `identity` 는 그 항등원이다 | O(n) | worst |
 * | `update(i, value)` | 자리 `i` 의 값을 `value` 로 바꾼다 | O(log n) | worst |
 * | `query(from, to)` | 자리 `from` 이상 `to` 미만의 값을 **왼쪽에서 오른쪽으로** `combine` 으로 접은 값. `from === to` 면 `identity` | O(log n) | worst |
 *
 * **구간을 반열린 것으로 적은 것은 빈 구간을 값으로 적기 위해서다.** 물려받은 표면은 닫힌
 * 구간 `[l, r]` 이었는데 그것으로는 「자리가 하나도 없다」를 쓸 수 없고, 쓸 수 없으면 항등원을
 * 함께 받는 요구가 계약 안에서 **관측되지 않는다.** 관측되지 않는 약속은 계약이 아니다
 * (불변 사실 69 ③이 동시성에서 낸 문장이 여기서도 그대로 선다). 같은 눈금이라 `query(0, i)`
 * 가 앞구간을, `query(0, n)` 이 전부를 묻는다.
 *
 * **상한을 `O(log n)` 으로 적은 것은 고른 것이다.** 목적에서 유일하게 따라 나오지 않는다
 * (§규약1 「상한이 목적에서 유일하게 따라 나오지 않는 계약이 있다」). 자리를 `r` 개씩 묶어
 * 층을 쌓는 모델에서 갱신과 질의를 함께 가장 작게 누르는 값이 `r = 2` 라는 셈은
 * `range-query/fenwickTree` 헤더가 이미 폈고(불변 사실 164) 여기서도 그대로 선다.
 * **다른 것은 질의 쪽이다** — 앞구간은 경계가 하나이고 임의 구간은 둘이라, 묶음 모델의 질의가
 * 조각을 양쪽에서 떼야 한다. 실측이 그 차이를 그대로 낸다: 묶음 구현의 질의가 오른쪽 끝을
 * `n` 으로 눌러 둔 시나리오에서 124 → 252 → 508 이고 두 경계가 다 어긋난 구간에서
 * 184 → 376 → 760 이다(1.5 배). 두 자리 모두 $r \approx 2.0$ 이라 `O(log n)` 의 허용 상단
 * 1.56 밖이고, **그것이 이 상한 선택이 실제로 배제하는 계열**이다.
 *
 * - **`O(n)` 은 계약이 뜻을 잃는다.** 값을 그대로 적어 두고 물을 때마다 훑으면 그 상한이
 *   나오고, 그것이 상한이면 배열 하나와 달라지는 것이 없다.
 * - **`O(1)` 은 만족하는 구현을 못 찾았다.** 고치기와 임의 구간 접기를 **둘 다** 상수에 두는
 *   구현을 이 카드가 찾지 못했고, **없다는 것을 여기서 보이지도 않았다.** 상한을 그렇게 적지
 *   않는 이유는 그것과 별개로 선다 — 계약의 상한은 정본이 서는 계급이어야 한다
 *   (불변 사실 165).
 * - **`O(sqrt n)` 은 남는 후보이고 하한이 아니다.** 위 묶음 구현이 그 상한 안에 들어오고, 그
 *   상한을 적어도 값 배열은 여전히 배제된다. **목적이 이 계열을 자르지 못한다.** 그러므로
 *   여기서는 정본이 서는 계급을 적는다(§규약2 「축3은 상한이 아니라 성장 계급을 판정한다」가
 *   그것을 요구한다 — 계약의 상한이 정본의 계급과 어긋나면 정본이 자기 스위트에서 걸린다).
 *
 * **상한이 세는 것은 주입된 결합을 부른 횟수다.** 결합 한 번이 얼마나 비싼지는 주입자가
 * 정하고 계약이 알지 못한다. 결합이 자리 수에 비례하는 일을 하면 호출자가 보는 비용이 그만큼
 * 곱해진다 — 계약이 막지 않는 이유는 막으려면 주입된 함수의 안을 보아야 하기 때문이다.
 *
 * **한정자가 셋 다 `worst` 인 것은 배제될 계열이 없기 때문이다.** `worst` 는 셋 중 가장
 * 강하므로(불변 사실 59) 적으려면 그것이 배제하는 계열을 배제해도 계약이 잃는 것이 없어야
 * 한다. 자리 수가 생성자에 고정되고 자리의 순서도 처음부터 정해져 있어, 호출이 바꾸는 것은
 * 값뿐이고 담는 모양은 그대로다 — 비용을 뒤 호출로 미루는 계열(`amortized`)이 미룬 것을 갚을
 * 자리가 없고, 무작위로 나쁜 모양을 흩는 계열(`expected`)이 흩을 모양이 없다. `constructor`
 * 는 넷째 이유로 `worst` 다 — n 에 대해 반복 호출되지 않으므로 강한 한정자를 적는 대가가 없고,
 * 그래서 축3도 이 행을 재지 않는다(§규약2 축3 면제).
 *
 * **주입 정책.**
 *
 * - **접는 연산과 그 항등원을 생성자로 받는다. 여는 것이 이 계약의 내용이다.** 세 물음으로
 *   정했다(§규약1 「주입 정책도 상한과 같은 두 물음으로 정한다」).
 *   - **닫으면**(덧셈 하나로 고정) 되돌리는 값이 있는 결합만 남고, 그렇게 적은 계약은 이미
 *     `range-query/fenwickTree` 의 것이다. 최솟값·최댓값·최대공약수를 묻는 호출자가 통째로
 *     나간다.
 *   - **가환군으로 좁혀 열면**(더하기·되돌리기·항등원을 함께 주입받는 형태) 위와 같은 계열이
 *     그대로 나간다. 되돌리는 값을 요구하는 순간 **구간을 두 앞구간의 차로 답하는 구현이 다시
 *     들어오고**, 이 계약이 존재하는 이유가 사라진다.
 *   - **모노이드보다 넓게 열면**(항등원 없이 결합만) 빈 구간의 답이 정해지지 않는다. 반환
 *     타입에 「없음」을 담을 자리가 없으므로 빈 구간을 `RangeError` 로 막는 길이 남는데, 그러면
 *     `query(i, i)` 가 값으로 관측되지 않고 경계가 하나 는다. 항등원 없는 반군을 든 호출자는
 *     자기 값 바깥의 표지 하나를 붙여 항등원을 만들 수 있으므로, 여기서 나가는 것은 계열이
 *     아니라 편의다.
 * - **결합법칙은 주입자의 의무다.** 이 계약의 어느 줄도 **접는 차례**를 정하지 않는다 — 어느
 *   자리들을 먼저 묶는지가 곧 담는 모양이라, 정하는 순간 그것이 처방이다. 차례를 정하지 않고도
 *   답이 하나로 정해지려면 `combine` 이 결합적이어야 한다.
 * - **그래서 `range-query/fenwickTree` 의 값 타입 제한이 여기에는 없다.** 저쪽은 같은 이유로
 *   (더하는 차례를 정하지 않는다) 값을 **정수로 제한한다** — 부동소수 덧셈이 결합적이지 않기
 *   때문이다(불변 사실 163). 이쪽은 결합법칙이 주입자의 의무이므로 계약이 값 타입을 제한할
 *   자리가 없다. 실수 덧셈을 주입하면 그것은 주입자가 결합적이지 않은 것을 준 것이다.
 *   **조건이 사라진 것이 아니라 의무의 주인이 바뀌었다.**
 * - **교환법칙은 요구하지 않는다.** 자리의 순서는 계약이 고정한다 — `query(from, to)` 의 답은
 *   `values[from]` 부터 `values[to - 1]` 까지를 **왼쪽에서 오른쪽으로** 접은 값이고, 괄호만
 *   자유다. 이 줄은 실제로 계열 하나를 배제한다: 덮는 조각을 **만나는 차례대로** 접는 구현이다.
 *   실측으로 확인했다(`src/data-structures/_contract/_fixtures/unorderedRangeFold.ts`) — 교환적인
 *   결합에서는 정본과 답이 한 자리도 다르지 않고, 교환적이지 않은 결합에서 `query(1, 7)` 에 9 를
 *   낸다(정본은 5 다).
 * - **`identity` 는 실제로 항등원이어야 한다** — 값의 정의역 안의 모든 `x` 에 대해
 *   `combine(identity, x)` 와 `combine(x, identity)` 가 `x` 여야 한다. 계약이 검사하지 않는
 *   이유는 검사하려면 값의 정의역을 전부 알아야 하는데 그것을 계약이 알지 못하기 때문이다.
 *   어기면 빈 구간의 답부터 갈린다.
 * - **자리 번호와 구간 경계가 범위 밖이면 두 연산 모두 `RangeError` 를 던진다.** 값으로 답하지
 *   않는 이유는 **반환 타입에 「그런 자리는 없다」를 담을 자리가 없기 때문이다** — 항등원을
 *   돌려주면 「그 구간의 답이 항등원이다」로 읽혀 잘못된 색인이 조용히 지나간다.
 *   `query(from, to)` 의 `from > to` 도 같다(불변 사실 82 의 판별. 반환 타입에 자리가 있는
 *   계약은 다르게 고른다 — `trie/suffixArray` 의 범위 밖 접근은 `null` 이다).
 * - **`values` 가 빈 배열인 것은 정당하다.** 자리가 없는 수열이고, 그 뒤로 부를 수 있는 것은
 *   `query(0, 0)` 하나뿐이며 답은 `identity` 다.
 *
 * **검증 등급.** `complexity`.
 * 세 행을 **함께** 만족하는 자명한 구현이 없다. 언어가 내주는 배열·객체로 적는 길이 둘이고
 * 둘 다 한쪽을 놓는다.
 *
 * - **값을 그대로 적어 두면** 고치기가 상수인 대신 질의가 구간 폭에 비례한다(실측 2,048 →
 *   8,192 → 32,768, $r = 4.00$).
 * - **모든 구간의 답을 미리 접어 두면** 질의가 상수인 대신 자리 하나를 고칠 때 그 자리를 담은
 *   구간 전부를 다시 접어야 하고, 그 수가 자리 수의 제곱에 비례한다.
 *
 * **`range-query/fenwickTree` 의 자명한 구현 셋 중 하나가 여기서는 후보조차 아니다** —
 * 「앞구간을 미리 접어 두기」다. 되돌리는 값이 없으면 앞구간 둘에서 구간을 얻지 못하므로, 그
 * 표를 들고 있어도 임의 구간에 답하지 못한다. 등급이 같은 두 계약에서 **자명한 구현의 목록이
 * 다르다.**
 *
 * 세 행을 함께 로그 안에 두려면 서로 다른 길이의 구간을 미리 접어 두고 그것을 겹쳐 쓰는 설계가
 * 들어와야 하고, 그것은 배열의 기본 연산에서 나오지 않는다.
 *
 * **필요충분조건.**
 * - 의미: 자리 하나를 바꾸는 일과 임의 구간을 접은 값을 읽는 일 중 하나라도 버리면 이 구조가
 *   아니다. 고치기를 버리면 한 번 짓고 마는 표이고, 구간 읽기를 버리면 그냥 배열이다. 그리고
 *   답이 **주입받은 임의의 결합**에 대해 나오는 것까지가 이 계약이다 — 덧셈 하나에만 답하면
 *   되면 `range-query/fenwickTree` 이고, 되돌리기를 함께 요구하면 같은 자리로 돌아간다.
 * - 비용: 셋을 다 지키더라도 **한쪽이 자리 수에 비례하면** 이 구조가 아니다. 고치기를 상수로
 *   두고 질의를 선형에 두면 값 배열이고, 둘 다 자리 수의 제곱근이면 묶음 표이며 그것도 이
 *   계약이 아니다. 이 계약이 요구하는 것은 **둘을 함께** 로그 안에 두는 것이다.
 */
export class SegmentTree {
  constructor(
    values: number[],
    combine: (a: number, b: number) => number,
    identity: number,
  ) {
    throw new Error("Not implemented");
  }

  update(i: number, value: number): void {
    throw new Error("Not implemented");
  }

  query(from: number, to: number): number {
    throw new Error("Not implemented");
  }
}
