/**
 * 폴라드 로 알고리즘 (Pollard's Rho)
 *
 * 큰 합성수 $n$의 비자명한 약수(즉, $1$도 $n$도 아닌 약수)를 확률적으로 찾는다.
 *
 * 아이디어는 의사 난수 수열 $x_{i+1} = (x_i^2 + c) \bmod n$ 을 따라 가면서
 * 두 시점의 차이의 최대공약수를 취해 약수를 추출하는 것이다:
 *
 * $$d = \gcd(|x_i - x_j|,\ n)$$
 *
 * 위에서 $1 < d < n$ 이면 $d$가 비자명한 약수가 된다.
 *
 * 평균 시간 복잡도는 $O(n^{1/4})$로 알려져 있다 (생일 역설 기반).
 *
 * 입력 $n$이 소수면 비자명한 약수가 존재하지 않으므로, 호출 전에 Miller-Rabin 등으로
 * 합성수임을 확인하거나, 구현 내부에서 그러한 경우를 별도 처리해야 한다.
 * 본 함수에서는 $n$이 짝수인 경우 즉시 2를 반환하고, $n$이 소수인 경우 $n$ 자체를 반환할 수 있다.
 *
 * @param n - 인수분해 대상 정수 (bigint, $n \geq 2$, 합성수 권장)
 * @returns $n$의 비자명한 약수 ($1 < d \leq n$). 소수인 경우 $n$.
 */
export function pollardRho(n: bigint): bigint {
  throw new Error("Not implemented");
}
