=== ChatGPT (codex CLI) — 폐쇄형 독해 시험 ===
[Q1] 답변: 근거 없음  
[Q1] 인용: "여러 후보 중 최솟값을 구하는 문제니, 가장 정직한 방법은 등록된 직선을 전부 순회하며 최솟값을 취하는 겁니다." / "n = q = 10^5 이면  nq = 10^10   →  1초 안에 절대 불가능"  
[Q1] 판정: FAIL(근거 없음)

[Q2] 답변: 어떤 입력에서도 최솟값이 되지 못하는 직선은 버리고, 남은 직선들의 담당 구간 경계가 정렬되므로 빠르게 찾을 수 있다.  
[Q2] 인용: "핵심 관찰은 "어떤 $x$에서도 절대 최솟값을 담당하지 못하는 직선은 미리 버려도 안전하다"는 것이고, 살아남는 직선들만 남기면 그 집합이 하한 볼록 껍질(lower convex hull)을 이루어 이진 탐색이 통합니다."  
[Q2] 판정: PASS

[Q3] 답변: 처음에는 교차점이 없어 불변식이 참이고, 새 직선을 넣을 때는 스택 길이와 `bad()` 결과에 따라 빠짐없이 세 경우로 나뉜다. 그냥 추가해도 교차점 수열이 유지되거나, 담당 구간을 잃은 직선을 제거한 뒤 이미 불변식을 만족하던 더 짧은 스택에서 같은 검사를 다시 하므로, 모든 추가 뒤에도 불변식이 유지된다.  
[Q3] 인용: "**기저**: 직선이 0개 또는 1개면 교차점 수열이 아예 없으므로 불변식은 공허하게(vacuously) 참입니다." / "`addLine`이 하는 일은 다음 세 경우 중 정확히 하나이고, 이 세 경우가 전부입니다(스택 길이가 2 미만인지, 2 이상이면 `bad()`가 참인지 거짓인지로 완전히 나뉘므로 빠지는 경우가 없어요)." / "스택 길이가 2 이상이고 `bad(stack[-2], stack[-1], new)`가 거짓 — 상단 직선의 담당 구간이 여전히 비어 있지 않다는 뜻이므로 그대로 추가합니다. 새로 생기는 교차점은 직전 마지막 교차점보다 오른쪽에 생기도록 `bad()`가 보장하므로(다음 문단에서 계산으로 확인) 순증가가 유지됩니다." / "`bad(...)`가 참 — 상단 직선의 담당 구간이 사라졌다는 뜻이므로 팝하고 같은 검사를 반복합니다. 팝한 직후의 스택은 팝하기 직전보다 원소가 하나 적은 상태에서 이미 불변식을 만족하던 스택이므로(귀납 가정 적용), 다시 경우 1~3 중 하나로 재귀적으로 떨어집니다." / "세 경우 모두에서 불변식이 유지되므로, `addLine`을 몇 번 호출해도 스택은 항상 불변식을 만족합니다."  
[Q3] 판정: PASS

[Q4] 답변: `bad()`의 부등호 방향이 깨지면 제거해야 할 직선이 남고 필요한 직선이 삭제되어, `query(0)`이 정답 `-1` 대신 `0`을 반환한다.  
[Q4] 인용: "`bad()` 부등호 방향을 반대로 쓰기 쉽습니다." / "방향을 반대로 쓰면 지워야 할 직선을 놔두고 엉뚱한 직선을 지우는 사고가 납니다." / "그런데 부등호를 반대로 쓴 `bad()`로 같은 시퀀스를 넣으면, 원래 지워져야 할 $(-1,5)$는 살아남고 대신 진짜 필요한 $(0,-1)$이 지워져서 최종 스택이 `[(-2,0), (-1,5), (2,0)]`이 되고 `query(0)`이 `0`을 반환합니다 — 조용히 틀린 값이에요."  
[Q4] 판정: PASS

[Q5] 답변: 본문 코드의 `bad()` 조건 방향을 반대로 바꾸면 같은 입력 `(-2,0), (-1,5), (0,-1), (2,0)`에서 최종 스택이 잘못되고 `query(0)`이 `0`을 반환한다.  
[Q5] 인용: "그런데 부등호를 반대로 쓴 `bad()`로 같은 시퀀스를 넣으면, 원래 지워져야 할 $(-1,5)$는 살아남고 대신 진짜 필요한 $(0,-1)$이 지워져서 최종 스택이 `[(-2,0), (-1,5), (2,0)]`이 되고 `query(0)`이 `0`을 반환합니다 — 조용히 틀린 값이에요."  
[Q5] 판정: PASS

Q1: 절차 층위에서 같은 목표를 노리는 더 단순하거나 먼저 떠오르는 다른 절차·설계를 실명으로 제시하고, 같은 입력에서 수치로 대조하는 근거가 본문에 없다.  
VERDICT: FAIL

[codex] 이해 게이트 판정: FAIL

=== Gemini (antigravity CLI) — 폐쇄형 독해 시험 ===
[Q1] 답변: (a) 개념 층위: CHT가 없을 때 사용하는 가장 단순한 방법은 등록된 모든 직선을 순회하며 일일이 최솟값을 계산하는 완전 탐색(`queryNaive`)이다. 이 방법은 직선과 질의가 각각 $n = q = 10^5$개일 때 연산 횟수가 $nq = 10^{10}$에 달하여 1초 시간 제한 내에 처리가 불가능하다. (b) 절차 층위: 근거 없음. 본문은 스택 기반 CHT 외에 Li Chao Tree 등의 대안 기법을 이름만 언급했을 뿐, 동일한 입력 조건에서 어떤 절차적 차이와 수치적 열세가 발생하는지 구체적으로 대조하여 설명하지 않았다.
[Q1] 인용: "(a) 직선 n개 등록, 질의 q개\n질의마다:  n개 직선을 전부 평가해서 min   →  질의당 O(n)\n전체:      O(n) × q개 질의                →  O(nq)\n\nn = q = 10^5 이면  nq = 10^10   →  1초 안에 절대 불가능 (b) 근거 없음"
[Q1] 판정: FAIL(근거 없음)

[Q2] 답변: 어떤 입력에서도 절대 최솟값을 담당하지 못하는 쓸모없는 직선을 미리 걸러내고 살아남은 직선들의 담당 구간 경계를 순서대로 유지하면 이진 탐색으로 최적의 직선을 빠르게 찾을 수 있다.
[Q2] 인용: "핵심 관찰은 "어떤 $x$에서도 절대 최솟값을 담당하지 못하는 직선은 미리 버려도 안전하다"는 것이고, 살아남는 직선들만 남기면 그 집합이 하한 볼록 껍질(lower convex hull)을 이루어 이진 탐색이 통합니다."
[Q2] 판정: PASS

[Q3] 답변: 본문은 스택의 불변식("스택은 기울기 오름차순으로 정렬돼 있고, 인접한 두 직선의 교차점 $x$좌표 수열은 순증가한다")을 수학적 귀납법으로 증명하여 정당성을 보인다.
1. 기저 단계: 직선이 0개 또는 1개일 때는 교차점 수열이 없으므로 불변식이 공허하게 참이다.
2. 귀납 단계: 불변식을 만족하는 스택에 새 직선 $\ell_{new}$를 추가할 때:
   - 스택 크기 < 2: 교차점 비교가 불필요하므로 즉시 추가되어 불변식이 성립한다.
   - 스택 크기 $\ge$ 2 및 `bad()` 거짓: 상단 직선의 담당 구간이 비어 있지 않으며, 새 교차점이 직전 교차점보다 오른쪽에 위치하므로 순증가가 유지된다.
   - `bad()` 참: 상단 직선의 담당 구간이 사라졌으므로 pop한 뒤 동일 검사를 반복하며, pop 직후 스택은 귀납 가정에 의해 이미 불변식을 만족하던 상태이므로 1~3의 경우 중 하나로 환원되어 불변식이 유지된다.
3. 조회 단계: 교차점 수열이 순증가하므로 각 직선의 최솟값 담당 구간이 왼쪽에서 오른쪽으로 정렬되어 있어 이진 탐색을 통한 최솟값 선정이 항상 옳다.
[Q3] 인용: "스택은 기울기 오름차순으로 정렬돼 있고, 인접한 두 직선의 교차점 $x$좌표 수열 $x_1^{*} < x_2^{*} < \cdots$은 순증가한다. ... 기저: 직선이 0개 또는 1개면 교차점 수열이 아예 없으므로 불변식은 공허하게(vacuously) 참입니다. ... 귀납 단계: 불변식이 성립하는 스택에 새 직선 $\ell_{new}$를 추가한다고 합시다. `addLine`이 하는 일은 다음 세 경우 중 정확히 하나이고, 이 세 경우가 전부입니다(스택 길이가 2 미만인지, 2 이상이면 `bad()`가 참인지 거짓인지로 완전히 나뉘므로 빠지는 경우가 없어요). 1. 스택 길이가 2 미만 — 교차점 비교 자체가 불가능하니 그냥 추가합니다. ... 2. 스택 길이가 2 이상이고 `bad(stack[-2], stack[-1], new)`가 거짓 — 상단 직선의 담당 구간이 여전히 비어 있지 않다는 뜻이므로 그대로 추가합니다. ... 3. `bad(...)`가 참 — 상단 직선의 담당 구간이 사라졌다는 뜻이므로 팝하고 같은 검사를 반복합니다. 팝한 직후의 스택은 팝하기 직전보다 원소가 하나 적은 상태에서 이미 불변식을 만족하던 스택이므로(귀납 가정 적용), 다시 경우 1~3 중 하나로 재귀적으로 떨어집니다."
[Q3] 판정: PASS

[Q4] 답변: `bad()` 판정식의 부등호 방향 제약이 깨져 반대로 판정하면, 제거되어야 할 직선이 남고 실제 필요한 유효 직선이 제거되어 직선 `(-2,0), (-1,5), (0,-1), (2,0)` 추가 시 올바른 최솟값인 `-1` 대신 잘못된 값인 `0`이 반환된다.
[Q4] 인용: "실제로 확인해 보죠. $\ell_0=(-2,0), \ell_1=(-1,5), \ell_2=(0,-1), \ell_3=(2,0)$을 이 순서로 추가하면 정답은 `query(0) = -1`이어야 합니다(직선 $(0,-1)$이 $x=0$에서 최소). 그런데 부등호를 반대로 쓴 `bad()`로 같은 시퀀스를 넣으면, 원래 지워져야 할 $(-1,5)$는 살아남고 대신 진짜 필요한 $(0,-1)$이 지워져서 최종 스택이 `[(-2,0), (-1,5), (2,0)]`이 되고 `query(0)`이 `0`을 반환합니다 — 조용히 틀린 값이에요."
[Q4] 판정: PASS

[Q5] 답변: `bad()` 함수의 판정 부등호 조건을 반대로 수정하면 직선 `(-2,0), (-1,5), (0,-1), (2,0)`을 순서대로 등록했을 때 스택이 `[(-2,0), (-1,5), (2,0)]`으로 잘못 구축되어 `query(0)` 호출 시 정답 `-1`이 아닌 `0`이 반환된다.
[Q5] 인용: "부등호를 반대로 쓴 `bad()`로 같은 시퀀스를 넣으면, 원래 지워져야 할 $(-1,5)$는 살아남고 대신 진짜 필요한 $(0,-1)$이 지워져서 최종 스택이 `[(-2,0), (-1,5), (2,0)]`이 되고 `query(0)`이 `0`을 반환합니다 — 조용히 틀린 값이에요."
[Q5] 판정: PASS

---

### 보완 지시사항
- **[Q1-(b)]**: 동일한 목표를 해결하는 다른 대안 절차·설계(예: Li Chao Tree, 동적 세그먼트 트리 등)를 실명으로 제시하고, 왜 이 스택 기반 절차를 선택하는지 동일한 입력 조건에서의 구체적 수치 대조(연산량, 시간/공간 복잡도 등)를 본문에 명시해야 합니다.

VERDICT: FAIL


[agy] 이해 게이트 판정: FAIL

----------------------------------------
이해 게이트: FAIL — 위 "근거 없음 / 결론만 있음" 항목이 곧 보완 지시입니다.
해당 내용을 본문에 채워 넣은 뒤 다시 실행하세요. 기준을 낮춰 통과시키지 마세요.
