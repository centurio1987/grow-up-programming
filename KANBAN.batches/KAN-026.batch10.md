---
card: KAN-026
batch: 10
created: 2026-09-14
branch: KAN-026
status: 계획
steps: S15, S16
---

# KAN-026 배치10 — 스케치 둘

카드: [KAN-026.md](../KANBAN.cards/KAN-026.md) · 범위 `S15` · `S16`
선행: [배치9](KAN-026.batch9.md)

> **이 문서는 착수 전 계획이다.** 수행 내역은 카드 실행 문서의 「수행 내역」에 있다.
> 수행 방식: 단일 에이전트(2026-09-14 유저 결재). 착수 전 `python3 <skill>/scripts/kanban.py resume . --card KAN-026` 로 다음 work 를 확인한다.

## 1. 작업 패키지

### WP1 · `S15` `probabilistic/countMinSketch` 계약 전환

걸린 것: JSDoc 없음. 오차 보장이 계약, 재는 축이 없다(`p4-triage.md:47`).

**완료 기준**: 공통 완료 기준은 카드 문서 「실행 계획 · 단계별 완료 기준」의 구조 하나 항목이다 — 헤더 여섯 항목 · `.contract.ts` + `.test.ts` + `_reference/<name>.ts`(`__cost` · `#region guide:core`) · vector 등록(알파벳 순) · `check-links refs` 뒤 `-problem.md` 제거 · `bun run tools/ci.ts all` · `check-contract` · `tsc` · biome 경고 0 · 종료 산출(불변 사실은 카드 대역).

### WP2 · `S16` `probabilistic/hyperLogLog` 계약 전환

걸린 것: 공간이 존재 이유이고 오차가 계약(`p4-triage.md:48`). 메모리 직접 사용 목록 편입 판정 대상.

**완료 기준**: 공통 완료 기준은 카드 문서 「실행 계획 · 단계별 완료 기준」의 구조 하나 항목이다 — 헤더 여섯 항목 · `.contract.ts` + `.test.ts` + `_reference/<name>.ts`(`__cost` · `#region guide:core`) · vector 등록(알파벳 순) · `check-links refs` 뒤 `-problem.md` 제거 · `bun run tools/ci.ts all` · `check-contract` · `tsc` · biome 경고 0 · 종료 산출(불변 사실은 카드 대역).

## 2. 의존과 순서

- **결재 반영**: 오차 보장 판정은 배치9 가 세운 방식을 그대로 쓴다(결재 2026-09-15).
- 서로 독립. `S14` 의 축밖 처분 문형을 선례로 쓴다.
- 불변 사실 번호는 이 카드 대역(200–229)만 쓴다 — `KAN-027` 가 다른 워크트리에서 같은 런북을 고친다.

## 3. 리스크

- `S13`·`S14` 와 같은 메모리 판정 기준을 쓴다 — 구조마다 기준을 새로 세우지 않는다.
- 세션 절반을 넘길 기미가 보이면 이 배치의 마지막 work 를 다음 배치 앞으로 미루고 「4. 착수 시점 판단」에 적는다.

## 4. 착수 시점 판단
<!-- 착수할 때 채운다 — 마지막 work 를 다음 배치로 미룰지 여기서 정한다. -->

**`S15` → `S16` 을 둘 다 했다. 미룬 work 는 없다.** 앞선 에이전트가 `S15` doing 만 남기고 중단해 claim 을 `--steal` 로 다시 잡았다(같은 세션 id 라
빼앗은 것으로 기록되지 않았다). 코드 변경은 이어받은 것이 없었다.

- **`-problem.md` 두 편을 지우지 못했다.** `rm` 을 Claude Code auto mode 분류기가 막았고(되돌릴 수 없는 로컬 삭제로 분류), 우회하지 않았다.
  `check-links refs` 는 둘 다 0 이다. 사람이 `src/data-structures/probabilistic/countMinSketch/countMinSketch-problem.md` ·
  `src/data-structures/probabilistic/hyperLogLog/hyperLogLog-problem.md` 를 지우면 끝난다 — `docs/ORD-006-inventory.tsv` 두 행은 지운 뒤의 값
  (problem_lines 0)으로 먼저 적었다. → 메인 세션이 `git rm` 으로 지웠다(2026-09-15, 커밋 전 `check-links refs` 재확인).
- **`S12`(`hash/rollingHash`)는 KAN-039 로 넘어가 건드리지 않았다.**
- **위 「2. 의존과 순서」의 결재 반영(배치9 판정 방식)은 실행으로 닫혔다** — 두 스위트 모두 하네스 무수정(불변 사실 360). 모으는 단위가 하이퍼로그로그에서
  인스턴스로 바뀌어 여유를 다시 쟀다(361).
- **불변 사실은 360–370 을 썼다**(셋째 대역 350–379 안 — 넷째 대역을 열 일이 없었다). 「2.」의 「200–229 만」은 계획 시점 문장이다.
- **위 「1. WP2」의 「메모리 직접 사용 목록 편입 판정 대상」** — S1 이 「넣지 않는다」로 닫았고(불변 사실 206) 카드 기준도 같다. 정본은 `Float64Array` ·
  `Uint8Array` 에 상태를 둔다.
- **사람 결정으로 올린 것 하나** — 하이퍼로그로그의 무작위를 실행이 한 번 뽑는다(불변 사실 366, conventions 「A군 스케치 둘」).
- **위 「3. 리스크」 첫 줄(`S13`·`S14` 와 같은 메모리 판정 기준)** — 그 기준대로 새 판정을 세우지 않았다.
