---
card: KAN-026
batch: 3
created: 2026-09-14
branch: KAN-026
status: 계획
steps: S20, S4, S6
---

# KAN-026 배치3 — basic 등급 둘 — 단방향 리스트와 동적 배열

카드: [KAN-026.md](../KANBAN.cards/KAN-026.md) · 범위 `S20` · `S4` · `S6`
선행: [배치2](KAN-026.batch2.md)

> **이 문서는 착수 전 계획이다.** 수행 내역은 카드 실행 문서의 「수행 내역」에 있다.
> 수행 방식: 단일 에이전트(2026-09-14 유저 결재). 착수 전 `python3 <skill>/scripts/kanban.py resume . --card KAN-026` 로 다음 work 를 확인한다.

## 1. 작업 패키지

### WP0 · `S20` ternarySearchTree 스위트에 「곁에 긴 낱말」 시나리오 둘 — 먼저 한다

배치2(`S3`)가 찾은 구멍이다. 복사 쪼개기 radixTree 가 계약의 `O(m)` 을 어기는데 정본 스위트를 통과한다
(낱말 길이 8 고정). 시나리오 추가뿐이고 계약 표·헤더·정본은 안 바꾼다.

**완료 기준**: 카드 문서 「실행 계획 · `S20`」.

### WP1 · `S4` `linear/singlyLinkedList` 계약 전환

걸린 것: 자명한 배열이 전 행을 더 빠르게 한다(`docs/ORD-006-p4-triage.md:41`) — 연산 집합 판정은 `S1` 결과.

**완료 기준**: 공통 완료 기준은 카드 문서 「실행 계획 · 단계별 완료 기준」의 구조 하나 항목이다 — 헤더 여섯 항목 · `.contract.ts` + `.test.ts` + `_reference/<name>.ts`(`__cost` · `#region guide:core`) · vector 등록(알파벳 순) · `check-links refs` 뒤 `-problem.md` 제거 · `bun run tools/ci.ts all` · `check-contract` · `tsc` · biome 경고 0 · 종료 산출(불변 사실은 카드 대역).

### WP2 · `S6` `linear/dynamicArray` 계약 전환

걸린 것: 언어 배열이 이미 이 계약이다(`p4-triage.md:40`).

**완료 기준**: 공통 완료 기준은 카드 문서 「실행 계획 · 단계별 완료 기준」의 구조 하나 항목이다 — 헤더 여섯 항목 · `.contract.ts` + `.test.ts` + `_reference/<name>.ts`(`__cost` · `#region guide:core`) · vector 등록(알파벳 순) · `check-links refs` 뒤 `-problem.md` 제거 · `bun run tools/ci.ts all` · `check-contract` · `tsc` · biome 경고 0 · 종료 산출(불변 사실은 카드 대역).

## 2. 의존과 순서

- **결재 반영**: 연결 마디는 자명한 구현(결재 2026-09-15) — `S4` 는 `complexity` 가 아니다.
- 둘은 서로 독립. `S1` 이 둘을 같은 계약으로 접었으면 뒤쪽이 성격 전환이다.
- 불변 사실 번호는 이 카드 대역(200–229)만 쓴다 — `KAN-027` 가 다른 워크트리에서 같은 런북을 고친다.

## 3. 리스크

- basic 등급은 자명한 구현이 전 행을 통과한다 — 스위트가 아무것도 배제하지 못하는 것이 결함이 아니라 등급의 뜻인지 conventions 규약1 로 확인한다.
- 세션 절반을 넘길 기미가 보이면 이 배치의 마지막 work 를 다음 배치 앞으로 미루고 「4. 착수 시점 판단」에 적는다.

## 4. 착수 시점 판단
<!-- 착수할 때 채운다 — 마지막 work 를 다음 배치로 미룰지 여기서 정한다. -->
