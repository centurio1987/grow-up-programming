---
card: KAN-034.9-WHH53W
batch: 3
created: 2026-09-10
branch: KAN-034-KSD7XR
status: 진행
steps: S4, S8, S9
---

# KAN-034.9-WHH53W 배치3 — 검사를 새로 세우는 셋

카드: [KAN-034.9-WHH53W.md](../KANBAN.cards/KAN-034.9-WHH53W.md) · 범위 `S4` `S8` `S9`
선행: [배치2](./KAN-034.9-WHH53W.batch2.md)

> **이 문서는 착수 전 계획이다.** 수행 내역은 카드 실행 문서의 「수행 내역」에 있다.

**폭 3.** `check-v2.ts` · `check-metaphor.ts` · 새 도구로 갈린다.

## 1. 작업 패키지

### WP1 · `S4` `viz` 걸음 ↔ `.sim.ts` 프레임 대조 + 생성 관행 규약 (`B3`·`C1`)

**`B3`**: `P3` 이 「T# 수 ≥ 프레임 수」만 보고 **어떤 걸음을 골랐는지는 안 본다.**
배치7 `maxBipartiteMatching` 이 11걸음 대 12프레임이었다.

**`C1`**: `viz` 를 손으로 그리면 `B1`·`B3` 이 함께 생긴다. **해법이 이미 실측됐다** —
`.sim.ts` 에서 생성해 붙인다. 배치10 이 표 생성을 했고 배치11 이 두 발 더 갔다:
`countInversions` 는 사이드카가 **프레임↔실행을 대조해 던지고**, `externalMergeSort` 는 원고를
**멱등하게 재주입**하며 양방향 대조를 찍는다.

**둘이 한 work 인 이유**: `C1` 의 관행이 서면 `B3` 의 대상이 줄고, `B3` 의 검사가 서면 `C1` 을
안 지킨 편이 드러난다. 규약과 검사가 한 쌍이다.

**완료 기준**(공통 기준에 더해): `P3` 이 걸음 **자리**를 대조한다(수만 세지 않는다) ·
`maxBipartiteMatching` 이 그 검사에 걸린다 · `SPEC.md` 에 「`viz` 는 `.sim.ts` 에서 생성한다」가
규약으로 서고 **배치11 의 멱등 재주입이 본보기로 인용된다**

### WP2 · `S8` 은유 검사가 사이드카를 본다 (`B4`) — `tools/check-metaphor.ts`

`check-metaphor` 가 **`.md` 만 본다.** 사이드카 `.ts` 의 주석·문자열에 남은 은유가 생성 블록을
타고 원고에 들어와서야 `P2` 가 잡는다 — **들어오지 않는 것은 영영 안 잡힌다.**

실측: 배치10 `sparseTableRangeMin` 사이드카에 **14자리**, 그중 **9자리는 `.md` 에 안 실린다.**

**완료 기준**(공통 기준에 더해): 대상 집합에 사이드카 5~6종이 든다 ·
`sparseTableRangeMin` 사이드카의 14자리가 잡힌다 · **코드 식별자와 문자열 리터럴을 가른다**
(변수명 `cost` 는 은유가 아니다 — 오탐 시험으로 박는다) · 111편 사이드카 전수 판정 목록

### WP3 · `S9` 이웃 편 겹침 스캐너 (`C4`) — 새 도구

**겹침을 재는 스캐너가 없다.** 그리고 겹침은 문장만이 아니라 **값으로도 온다** — 모범 편의
수치가 그대로 딸려 온 자리가 실측됐다.

**잣대가 이미 실측됐다.** 새로 정하지 않는다.

| 잣대 | 무엇 |
| --- | --- |
| 완전 일치 줄 | 두 편에 글자 그대로 같은 줄 |
| 18자 한글 shingle | 18자 창을 밀며 겹치는 구간 |
| 최장 공통 구간 | 가장 긴 연속 겹침 |
| **2편 빈도** (배치11) | `grep -rlF` 로 그 문자열을 쓰는 **편 수가 2 면 리프트**, **60 이상이면 관례** |

**완료 기준**(공통 기준에 더해): 도구가 잣대 넷을 낸다 · 111편에서 「2편 빈도」 목록이 나온다 ·
**리프트와 관례를 화면이 가른다**(사람이 다시 세지 않게) · 도구 자기시험(겹치는 쌍·안 겹치는 쌍)

## 2. 의존과 순서

**셋이 서로 독립이다.** 다만 `S9` 는 다른 둘과 성격이 다르다 — 새 도구라 기존 판정에 안 걸리고,
**결과가 원고 부채의 목록이지 규칙 위반이 아니다.** 그 목록을 이 카드에서 다 고칠지 별건으로
낼지는 수를 보고 정한다(배치 종료 문서에 남긴다).

`S4` 는 배치2 `S3` 이 `P15` 를 넓힌 뒤라야 값이 온전하다 — 손으로 그린 `viz` 가 `S3` 의 대상이고
`S4` 의 규약이 그것을 없애는 쪽이다. **배치 순서가 그 의존을 이미 만족한다.**

## 3. 리스크

- **`S9` 가 큰 목록을 낼 수 있다.** 111편 × 111편 쌍이라 「2편 빈도」 잣대가 아니면 사람이 못 읽는
  분량이 나온다. **먼저 잣대를 걸고 그다음 목록을 낸다** — 순서가 반대면 목록에 묻힌다.
- **`S8` 의 오탐이 코드 식별자에서 온다.** `cost`·`weight`·`cheap` 같은 변수명은 은유가 아니다.
  **막을 것을 명사가 아니라 어미로 적는다**가 `FEEDBACK.md` §4 규칙인데, 영문 식별자에는 어미가
  없다 — 문자열 리터럴과 주석만 보는 것이 처방이다.
- **`S4` 가 원고 재생성을 부른다.** 걸음 자리를 대조하면 어긋난 편의 `viz` 를 다시 만들어야 하고,
  그것은 생성 블록이라 사이드카부터 고쳐야 한다(`B7` 이 닫힌 방식의 2번 그대로).

## 4. 착수 시점 판단 (2026-09-10)

**셋을 계획대로 폭 3 으로 띄운다.** 미루는 work 없음.

`S4` 의 의존(「배치2 `S3` 이 `P15` 를 넓힌 뒤라야 값이 온전하다」)은 **이미 만족됐다** —
배치2 가 손 펜스와 2행 덩어리까지 넓혔고, 손으로 그린 `viz` 가 그 대상에 들어와 있다.

### `S4` 에 하나를 붙인다 — 배치2 가 낸 사각지대

`S7` 이 「이 검사가 못 보는 자리」로 낸 둘 중 **머리줄에 세로줄이 든 표**는 손상이 가장 크다.
GFM 이 머리줄 칸 수와 `| --- |` 구분줄 칸 수가 안 맞는 것을 보고 **표를 아예 안 세운다**(실측:
파싱 결과 표 0개). 세 줄이 통째로 한 문단으로 흐르므로 mdast 에도 hast 에도 `table` 이 없고,
`S7` 의 칸 수 대조는 0 대 0 으로 일치라 **원리적으로 못 잡는다.**

잡는 길은 「`| --- |` 구분줄 모양인데 그 자리가 표가 아니다」를 보는 것이고, 그것은
`check-v2` 쪽이라 **`S4` 워커가 같은 파일에서 함께 처리한다.** 오케스트레이터가 111편을 훑어
**지금 0건**임을 확인했으니 교정할 원고는 없고, 세우는 것은 강제 지점뿐이다.
