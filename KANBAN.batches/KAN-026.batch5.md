---
card: KAN-026
batch: 5
created: 2026-09-14
branch: KAN-026
status: 계획
steps: S7, S8
---

# KAN-026 배치5 — 단조 둘 — 스택과 큐, 알고리즘 가이드 링크 교체

카드: [KAN-026.md](../KANBAN.cards/KAN-026.md) · 범위 `S7` · `S8`
선행: [배치4](KAN-026.batch4.md)

> **이 문서는 착수 전 계획이다.** 수행 내역은 카드 실행 문서의 「수행 내역」에 있다.
> 수행 방식: 단일 에이전트(2026-09-14 유저 결재). 착수 전 `python3 <skill>/scripts/kanban.py resume . --card KAN-026` 로 다음 work 를 확인한다.

## 1. 작업 패키지

### WP1 · `S7` `linear/monotonicStack` 계약 전환

걸린 것: 지우기 전 `check-links refs` — `largestRectangleInHistogram-guide.md:143,753` · `nextGreaterElement-guide.md:74,645` 를 계약 헤더로.

**완료 기준**: 공통 완료 기준은 카드 문서 「실행 계획 · 단계별 완료 기준」의 구조 하나 항목이다 — 헤더 여섯 항목 · `.contract.ts` + `.test.ts` + `_reference/<name>.ts`(`__cost` · `#region guide:core`) · vector 등록(알파벳 순) · `check-links refs` 뒤 `-problem.md` 제거 · `bun run tools/ci.ts all` · `check-contract` · `tsc` · biome 경고 0 · 종료 산출(불변 사실은 카드 대역).

### WP2 · `S8` `linear/monotonicQueue` 계약 전환

걸린 것: `slidingWindowMaximum-guide.md:100` 를 계약 헤더로. 창 최댓값이 계약에 들어오면 스택과 갈린다(`p4-triage.md:45`).

**완료 기준**: 공통 완료 기준은 카드 문서 「실행 계획 · 단계별 완료 기준」의 구조 하나 항목이다 — 헤더 여섯 항목 · `.contract.ts` + `.test.ts` + `_reference/<name>.ts`(`__cost` · `#region guide:core`) · vector 등록(알파벳 순) · `check-links refs` 뒤 `-problem.md` 제거 · `bun run tools/ci.ts all` · `check-contract` · `tsc` · biome 경고 0 · 종료 산출(불변 사실은 카드 대역).

## 2. 의존과 순서

- **결재 반영**: 두 구조는 상태 있는 계약으로 연산 집합을 새로 짓는다(결재 2026-09-15). 큐는 「뒤에 넣고 앞에서 빼며 최댓값을 묻는 큐」, 최솟값은 비교자 주입으로 접는다. 링크 교체 대상 파일(`src/algorithms/`)은 카드 scope 에 들어 있다.
- `S7` → `S8`. 링크 교체는 각자 지우기 직전에.
- 불변 사실 번호는 이 카드 대역(200–229)만 쓴다 — `KAN-027` 가 다른 워크트리에서 같은 런북을 고친다.

## 3. 리스크

- **알고리즘 트랙 파일을 고친다** — `src/algorithms/` 가이드는 이 카드 scope 밖이다. 링크 한 줄만 바꾸고 `bun run tools/check-links.ts check` 0 을 확인한다. scope 에 그 경로를 더하고 `dep-check` 를 다시 돌린다.
- 세션 절반을 넘길 기미가 보이면 이 배치의 마지막 work 를 다음 배치 앞으로 미루고 「4. 착수 시점 판단」에 적는다.

## 4. 착수 시점 판단
<!-- 착수할 때 채운다 — 마지막 work 를 다음 배치로 미룰지 여기서 정한다. -->

**`S7` · `S8` 둘 다 이 배치에서 끝냈다 — 미룬 work 없음.** (2026-09-15, 단일 에이전트. 도중에 API 세션 한도로 한 번 끊겼고, 재개 시 워킹트리 상태로 이어 받았다)

- 착수 시 워킹트리 깨끗 · `resume` 의 다음 work `S7` · 배치 5 확인.
- **불변 사실 대역.** §2 의 「200–229 만」은 계획 당시 값이다. 그 대역은 S6 에서 소진됐고 배치4 가 둘째 대역을 열었으므로 276–282 를 썼다(남은 것 283–299).
- **§1 WP1 의 링크 줄 목록이 실제와 달랐다.** `largestRectangleInHistogram-guide.md:753` · `nextGreaterElement-guide.md:645` 는 스택이 아니라 큐 문서 링크였다(`check-links refs` 실측). 스택 둘(`:143` · `:74`)은 S7, 큐 셋(`slidingWindowMaximum-guide.md:100` 포함)은 S8 에서 바꿨다. 링크 대상만 바꿨고 `check-v2` 세 편 판정이 교체 전후 같다. `slidingWindowMaximum-guide.md:100` 은 문장이 「덱」을 가리켜 새 계약과 뜻이 어긋나 보고로 넘겼다(불변 사실 282).
- **§3 리스크 「`src/algorithms/` 가 scope 밖」** — 카드 scope 에 세 경로가 이미 들어 있었다(착수 시 `resume` 출력). 링크 한 줄씩만 바꿨다.
- **스택 연산 집합은 규약으로 닫히지 않았다.** 「최댓값을 묻는 스택」(배제가 분명한 표준 ADT)을 골랐고 다른 후보는 탐침 수치와 함께 사람 결정으로 올렸다(`docs/ORD-006-conventions.md` 「A군 단조 둘」).
- 「창 최댓값이 계약에 들어오면 스택과 갈린다」(§1 WP2)는 등급으로 섰다 — 스택 `basic` · 큐 `complexity`(불변 사실 277).
- `_fixtures/` 는 새 파일 여섯만 더했고 기존 파일은 안 건드렸다. `runContract.test.ts` 에 줄을 넣지 않았다(자기시험은 구조마다 새 파일) — 불변 사실 106 의 전수 대조가 필요 없다.
