---
card: KAN-034.5-0RGSQM
batch: 2
created: 2026-08-29
branch: KAN-034-KSD7XR
status: 계획
steps: S8, S2, S14, S9
---

# KAN-034.5-0RGSQM 배치2 — `etc` 개방 + 첫 후속 셋

카드: [KAN-034.5-0RGSQM.md](../KANBAN.cards/KAN-034.5-0RGSQM.md) · 범위 `S8` · `S2` · `S14` · `S9`
선행: [배치1](KAN-034.5-0RGSQM.batch1.md)

> **이 문서는 착수 전 계획이다.** 수행 내역은 카드 실행 문서의 「수행 내역」에 있다.

## 1. 작업 패키지

**공통 완료 기준은 카드 실행 문서 「실행 계획」의 24편 공통 블록이고, 명령 전문은 그 문서
「검증」 절이다.** 여기에는 편마다 다른 것만 적는다.

### WP1 · `S8` maxCounters (etc · 뷰 array+keyValue)

**`etc` 카테고리 첫 편.** 카테고리에 이 편 하나뿐이라 뒤를 막지 않는다. 뷰 선례를 `S4` 가 지므로 배치1 다음에 온다 — 그 대기가 이 배치 경계의 이유다

**완료 기준**: `src/algorithms/etc/maxCounters/maxCounters-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `bun test src/algorithms/etc/maxCounters/maxCounters-guide.test.ts` 통과

### WP2 · `S2` parametricBinarySearch (binary-search · 뷰 array)

`S1` 이 연 카테고리의 후속

**완료 기준**: `src/algorithms/binary-search/parametricBinarySearch/parametricBinarySearch-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `bun test src/algorithms/binary-search/parametricBinarySearch/parametricBinarySearch-guide.test.ts` 통과

### WP3 · `S14` connectedComponents (graph · 뷰 graph+keyValue)

`S13` 이 연 카테고리의 후속

**완료 기준**: `src/algorithms/graph/connectedComponents/connectedComponents-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `bun test src/algorithms/graph/connectedComponents/connectedComponents-guide.test.ts` 통과

### WP4 · `S9` sortArray (sorting · 뷰 array)

`sorting` 은 파일럿 `quicksort` 가 이미 열었다 — 선례 대기가 없다

**완료 기준**: `src/algorithms/sorting/sortArray/sortArray-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `bun test src/algorithms/sorting/sortArray/sortArray-guide.test.ts` 통과

## 2. 의존과 순서

`S8` 이 `S4`(배치1) 를 기다린 것이 이 배치 경계의 이유다. 나머지 셋은 배치1 의
카테고리 선례가 닫히면서 함께 풀린다 — `S2`←`S1`, `S14`←`S13`. `S9` 는 파일럿이 이미
`sorting` 을 열어 둬서 처음부터 자유롭다.

배치 안에서 넷이 서로를 기다리는 자리는 없다.

### 공유 파일 넷은 워커가 안 건드린다

편별 종료 산출 일곱 중 **넷이 배치 전체가 공유하는 파일**이다 —
`문제_가이드_목록.md` · `tools/_baseline/guide-rhythm.tsv` ·
`sandbox/algo-guide-v2/FEEDBACK.md` §5 · `KANBAN.md`.
넷을 워커가 각자 고치면 같은 줄에서 부딪히고, 부딪힌 결과가 조용히 반쯤 반영된다.

**워커는 자기 편 디렉터리만 쓴다.**
넷은 배치를 닫을 때 **한 자리에서 일괄 반영**한다. 그 일괄 반영도 전진 한 단위라 커밋한다.

## 3. 리스크

- **골격 결함이 있으면 이 배치의 4편이 함께 틀린다.** 폭을 도구가 내는 6 이 아니라 4 로
  낮춘 이유가 그것이다. 배치를 닫기 전에 `bun run tools/ci.ts all` 이 초록이어야 한다.
- **`tools/check-guide-rhythm.ts` 를 v2 편에 부르지 않는다.** 구 헤딩 접두와 어긋나 조용히
  공전하고 통과 표시가 거짓이 된다.
- 되돌리기는 편 단위다 — 편 하나가 커밋 하나이고 태그 하나를 받는다. 공유 파일 넷은 줄 단위
  추가·삭제라 편별 되돌리기가 서로를 안 깬다.

## 4. 착수 시점 판단
<!-- 착수할 때 채운다 — 마지막 work 를 다음 배치로 미룰지 여기서 정한다. -->
