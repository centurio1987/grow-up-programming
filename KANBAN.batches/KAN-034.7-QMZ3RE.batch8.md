---
card: KAN-034.7-QMZ3RE
batch: 8
created: 2026-09-04
branch: KAN-034-KSD7XR
status: 계획
steps: S33, S35, S39, S5
---

# KAN-034.7-QMZ3RE 배치8 — `geometry` 여섯째와 `dp`·`advanced`

카드: [KAN-034.7-QMZ3RE.md](../KANBAN.cards/KAN-034.7-QMZ3RE.md) · 범위 `S33` · `S35` · `S39` · `S5`
선행: [배치7](KAN-034.7-QMZ3RE.batch7.md)

> **이 문서는 착수 전 계획이다.** 수행 내역은 카드 실행 문서의 「수행 내역」에 있다.

## 1. 작업 패키지

**공통 완료 기준은 카드 실행 문서 「실행 계획」의 42편 공통 블록이고, 명령 전문은 그 문서
「검증」 절이다.** 여기에는 편마다 다른 것만 적는다.

### WP1 · `S33` bentleyOttmann (geometry · 뷰 keyValue)

**`segmentsIntersect`(`S29`·배치2)를 3곳에서 건다** — 그 링크를 `.md` 로 고쳐 이 편 커밋에 넣는다

**자료구조 트랙도 가리킨다** — `priorityQueue`·`avlTree` 가이드다. 그 트랙은 ORD-006 이 바꾸는 중이라 대상이 움직일 수 있으므로 잡을 때 `check-links.ts refs` 로 현재 자리를 확인한다

**완료 기준**: `src/algorithms/geometry/bentleyOttmann/bentleyOttmann-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `build-html` exit 0 · `bun test src/algorithms/geometry/bentleyOttmann/bentleyOttmann-guide.test.ts` 통과

### WP2 · `S35` tspBitmask (dp · 뷰 keyValue+matrix)

**완료 기준**: `src/algorithms/dp/tspBitmask/tspBitmask-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `build-html` exit 0 · `bun test src/algorithms/dp/tspBitmask/tspBitmask-guide.test.ts` 통과

### WP3 · `S39` meetInTheMiddleSubsetSum (advanced · 뷰 keyValue)

**완료 기준**: `src/algorithms/advanced/meetInTheMiddleSubsetSum/meetInTheMiddleSubsetSum-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `build-html` exit 0 · `bun test src/algorithms/advanced/meetInTheMiddleSubsetSum/meetInTheMiddleSubsetSum-guide.test.ts` 통과

### WP4 · `S5` aStarSearch (shortest-path · 뷰 graph+priorityQueue)

**완료 기준**: `src/algorithms/shortest-path/aStarSearch/aStarSearch-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `build-html` exit 0 · `bun test src/algorithms/shortest-path/aStarSearch/aStarSearch-guide.test.ts` 통과

## 2. 의존과 순서

넷 다 카테고리가 다르다(`geometry` · `dp` · `advanced` · `shortest-path`). 배치 안 상호 대기 없음.

**배치 밖 의존이 하나 있다** — `S33` bentleyOttmann 이 `S29` segmentsIntersect(배치2)를
3곳에서 건다. 42편 상호 링크 세 쌍 중 가장 잦은 자리다.

### 공유 파일 넷은 워커가 안 건드린다

`문제_가이드_목록.md` · `tools/_baseline/guide-rhythm.tsv` ·
`sandbox/algo-guide-v2/FEEDBACK.md` §5 · `KANBAN.md` 넷은 배치 전체가 공유한다.
**워커는 자기 편 디렉터리만 쓴다.** 넷은 배치를 닫을 때 한 자리에서 일괄 반영하고,
그 일괄 반영도 전진 한 단위라 커밋한다. `tools/ord004-manifest.json` 은 `KAN-034.8` 몫이다.

**워커는 `git add` 도 하지 않는다** — 한 워크트리의 인덱스를 공유해서 스테이징이 남의
커밋에 딸려 들어간다(W1 배치6 에서 실제로 났다).

**목록 링크 교체는 언제나 `src/algorithms/` 접두사로 좁힌다** — `:81`·`:82` 는 같은 줄에
자료구조 트랙 링크를 들고 있어서 이름으로 치환하면 그 트랙까지 `.md` 가 된다.

## 3. 리스크

- **골격 결함이 있으면 이 배치의 편들이 함께 틀린다.** 배치를 닫기 전에 `bun run tools/ci.ts all` 이 초록이어야 한다.
- **`tools/check-guide-rhythm.ts` 를 v2 편에 부르지 않는다** — 구 헤딩 접두와 어긋나 조용히 공전하고 통과 표시가 거짓이 된다.
- **스캐너 넷이 0 이어도 `FEEDBACK.md` §3 대조를 밟는다** — W2 배치6~11 의 대조 건수가 65 · 94 · 116 · 166 · 79 · 63 이고 줄어드는 추세가 없다.
- **최악 입력이라고 손으로 적은 수의 성질을 실행으로 확인한다** — W2 에서 `isPrimeTrial` 의 999,999,000,001 이 소수였다.
- **경쟁 설계를 배정밀도로 재지 않는다** — `alt.ts` 의 `measure()` 가 매 실행마다 정본과 답을 대조한다(W2 `digitDp` 실패에서 굳은 규칙).
- 되돌리기는 편 단위다 — 편 하나가 커밋 하나이고 태그 하나(`kan/KAN-034.7-QMZ3RE/S<n>`)를 받는다.
- **`bentleyOttmann` 은 자료구조 트랙을 가리킨다** — ORD-006 이 그 트랙을 바꾸는 중이라 잡을 때 `refs` 로 현재 자리를 확인한다.

## 4. 착수 시점 판단

**넷 다 이 배치에서 한다 — 미룰 work 없음**(2026-09-07 착수 실측).

`bun run tools/algo-wbs.ts` 의 W3 claim 후보 11건에 넷이 전부 들어 있다
(`bentleyOttmann` · `tspBitmask` · `meetInTheMiddleSubsetSum` · `aStarSearch`).
현재 W3 은 30/44 이고 **막힌 편이 7 에서 3 으로 줄었다** — 사유는 여전히 전부 `category` 다.
후보 11건은 그대로이므로 **이 배치까지는 세션 예산이 상한**이고, 배치9 부터 카테고리 잔량이
폭을 내린다(2항이 예고한 자리).

착수 직전 `bun run tools/ci.ts all` 이 **exit 0** 이다. 파이프 없이 종료코드를 봤다.

### 2항이 적은 「배치 밖 의존」은 이미 해소돼 있다

`S33` bentleyOttmann 이 `S29` segmentsIntersect 를 3곳에서 거는데, **v1 원고가 이미
`segmentsIntersect-guide.md` 를 가리킨다**(`.mdx` 가 아니다 — 배치2 가 그 편을 닫을 때 함께
고쳤다). 새 원고는 처음부터 `.md` 로 적으면 되고 **편 커밋에 딸려 넣을 링크 교체가 없다.**

**자료구조 트랙으로 나가는 링크 넷은 살아 있다** — `avlTree-guide.mdx` 2곳 ·
`priorityQueue-guide.mdx` 2곳, 대상 파일 둘 다 실재한다. 카드 「검증」이 「그 편을 잡을 때
`refs` 로 현재 자리를 확인한다」고 적은 자리이고, **ORD-006 이 아직 그 둘을 안 옮겼다.**
새 원고도 `.mdx` 그대로 가리킨다 — 고칠 쪽은 이 편의 링크가 아니라 그 트랙이다.

### 인바운드는 넷 다 두 자리뿐이다

| 편 | 인바운드 | 누구 몫인가 |
| --- | --- | --- |
| `bentleyOttmann` | `ord004-manifest.json:631` · `문제_가이드_목록.md:108` | 앞은 `KAN-034.8` · 뒤는 배치 종료 일괄 반영 |
| `tspBitmask` | `ord004-manifest.json:603` · `문제_가이드_목록.md:113` | 〃 |
| `meetInTheMiddleSubsetSum` | `ord004-manifest.json:69` · `문제_가이드_목록.md:116` | 〃 |
| `aStarSearch` | `ord004-manifest.json:1108` · `문제_가이드_목록.md:64` | 〃 |

목록 네 줄 다 알고리즘 링크가 하나뿐이라 다른 편·다른 트랙과 줄을 안 나눈다.

### 배치7 이 인계한 것 — 판정 명령을 바꾼다

**`bun run tools/check-v2.ts <파일>` 은 `P16` 을 안 돈다.** 단일 대상 분기가 `.ref.ts` 를
안 실으면서 「P1~P16 통과」로 출력한다(`tools/check-v2.ts:1602-1612`). **카드 「검증」 절의
편별 명령이 정확히 그 경로다.**

**이 배치부터 편별 재검증은 `bun run tools/check-v2.ts --all` 로 한다.** 워커에게도 편별
명령 뒤에 `--all` 을 한 번 더 돌리게 지시한다. 도구 수정은 `KAN-034.8` 몫이라 웨이브 중에
손대지 않는다.

배치6·7 이 낸 나머지 구멍 여덟(표 칸의 `|` · `P15` 의 손 펜스 사각 · viz 걸음과 `.sim.ts`
프레임 불일치 · 값에서 만든 문장의 조사 · 활용형 누락 둘 · `P12`·`check-proof` 오탐 둘)도
워커 지시에 그대로 싣는다.

### `L37` 은 배치7 종료 뒤에 닫혔다

유저가 aside(Playwright) 연결을 열어 주어 실제 창 폭 1440 에서 확인했다 — 레일이 우측 고정
(`x=1120` `w=208`) · 항목 12 · 끊어진 앵커 0 · 앵커 이동과 현재 절 표시(`is-here`)가 따라온다.
**배치4~7 연속 열려 있던 항목이 닫혔으므로 이 배치는 그 부담 없이 돈다.**

다만 그 화면이 **스캐너가 못 잡는 은유 하나**를 본문에서 보여 줬다(살아 있는 원고 10자리 ·
6편, `FEEDBACK.md` §4 「은유 계보 여덟째」). 이 배치의 워커는 그 부류를 **손으로 피한다.**

### 이 편이 특히 위험한 자리 셋

- **`S33` bentleyOttmann** — 상태 자료구조 둘(사건 큐·스위프 상태)이 얽혀 기호가 가장 많다.
  그리고 `closestPairOfPoints`(배치6)와 **같은 카테고리·같은 뷰**라 문장 중복 위험이 크다.
- **`S35` tspBitmask** — 비트마스크 상태를 다루는 첫 W3 편이다. 상태 수 `2^n·n` 이 제약
  상한에서 실제로 몇인지, 손으로 적은 상수가 그 성질을 갖는지 실행으로 본다.
- **`S39` meetInTheMiddleSubsetSum** — `fftMultiply`(배치7)·`babyStepGiantStep`(파일럿)과
  **「중간에서 만나기」라는 같은 이름의 개념**을 공유한다. `related` 가 그 이름이면 이미 쓴
  편과 겹치므로 다른 층의 이름을 고르거나 겹침을 절 안에 밝힌다.

### 띄우는 폭은 2 다

배치4 가 넷을 한꺼번에 띄웠다가 사용량 한도로 워커 넷이 전부 죽었고, 배치5·6·7 이 폭 2 로
끝냈다. 한도로 죽은 워커는 그 뒤로 0 이다.

### 끝난 뒤 — 착수 판단이 맞았는가
<!-- 배치를 닫을 때 채운다. -->
