---
card: KAN-034.5-0RGSQM
batch: 6
created: 2026-08-29
branch: KAN-034-KSD7XR
status: 계획
steps: S12, S20, S21, S23
---

# KAN-034.5-0RGSQM 배치6 — 마감 4편 — `sorting`·`array` 잔여로 W1 을 닫는다

카드: [KAN-034.5-0RGSQM.md](../KANBAN.cards/KAN-034.5-0RGSQM.md) · 범위 `S12` · `S20` · `S21` · `S23`
선행: [배치5](KAN-034.5-0RGSQM.batch5.md)

> **이 문서는 착수 전 계획이다.** 수행 내역은 카드 실행 문서의 「수행 내역」에 있다.

## 1. 작업 패키지

**공통 완료 기준은 카드 실행 문서 「실행 계획」의 24편 공통 블록이고, 명령 전문은 그 문서
「검증」 절이다.** 여기에는 편마다 다른 것만 적는다.

### WP1 · `S12` radixSort (sorting · 뷰 array)

이 편으로 `sorting` 이 마감된다

**완료 기준**: `src/algorithms/sorting/radixSort/radixSort-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `bun test src/algorithms/sorting/radixSort/radixSort-guide.test.ts` 통과

### WP2 · `S20` maximumProductSubarray (array · 뷰 array+keyValue)

**완료 기준**: `src/algorithms/array/maximumProductSubarray/maximumProductSubarray-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `bun test src/algorithms/array/maximumProductSubarray/maximumProductSubarray-guide.test.ts` 통과

### WP3 · `S21` houseRobber (array · 뷰 array+keyValue)

`tools/_baseline/guide-rhythm.tsv` 에 이 편의 행이 **없다**

**완료 기준**: `src/algorithms/array/houseRobber/houseRobber-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `bun test src/algorithms/array/houseRobber/houseRobber-guide.test.ts` 통과

### WP4 · `S23` bestTimeToBuyAndSellStock (array · 뷰 array+keyValue)

이 편으로 `array` 와 W1 이 함께 마감된다

**완료 기준**: `src/algorithms/array/bestTimeToBuyAndSellStock/bestTimeToBuyAndSellStock-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `bun test src/algorithms/array/bestTimeToBuyAndSellStock/bestTimeToBuyAndSellStock-guide.test.ts` 통과

## 2. 의존과 순서

넷 다 카테고리가 열려 있다. 배치 안 상호 대기 없음.

**이 배치가 닫히면 W1 이 빈다** — 그때 `tools/algo-wbs.ts` 가 W2 의 claim 후보를 내기
시작한다(웨이브 배리어). 그 전까지는 안 낸다.

### 공유 파일 넷은 워커가 안 건드린다

편별 종료 산출 일곱 중 **넷이 배치 전체가 공유하는 파일**이다 —
`문제_가이드_목록.md` · `tools/_baseline/guide-rhythm.tsv` ·
`sandbox/algo-guide-v2/FEEDBACK.md` §5 · `KANBAN.md`.
넷을 워커가 각자 고치면 같은 줄에서 부딪히고, 부딪힌 결과가 조용히 반쯤 반영된다.

**워커는 자기 편 디렉터리만 쓴다.**
넷은 배치를 닫을 때 **한 자리에서 일괄 반영**한다. 그 일괄 반영도 전진 한 단위라 커밋한다.

## 3. 리스크

- **골격 결함이 있으면 이 배치의 4편이 함께 틀린다.** 폭을 도구가 내는 6 이 아니라 4 로
  낮춘 이유가 그것이다. 배치를 닫기 전에 `bun run tools/ci.ts all` 이 초록이어야 한다.
- **`tools/check-guide-rhythm.ts` 를 v2 편에 부르지 않는다.** 구 헤딩 접두와 어긋나 조용히
  공전하고 통과 표시가 거짓이 된다.
- 되돌리기는 편 단위다 — 편 하나가 커밋 하나이고 태그 하나를 받는다. 공유 파일 넷은 줄 단위
  추가·삭제라 편별 되돌리기가 서로를 안 깬다.
- 이 배치의 4편 중 3편이 `array` 다. 공유 파일 반영이 한 배치 안에서 가장 몰리는 자리다.

## 4. 착수 시점 판단

착수함(2026-08-30). **§2 의 「넷 다 카테고리가 열려 있다」는 틀렸다** — 도구가 맞다
(카드 「실행 계획」의 「이 목록과 도구가 어긋나면 도구가 맞다」). `algo-wbs` 는 카테고리별
한 편만 후보로 내므로(`tools/algo-wbs.ts:206`) 착수 시점 후보가 **2건**이다.

```
W1 claim 후보 2건 (카테고리별 한 편)
  sorting          radixSort                      array
  array            maximumProductSubarray         array+keyValue
```

`S20`·`S21`·`S23` 이 전부 `array` 라 셋은 배치 안에서 순차다. **폭 2 로 시작해
`array` 가 비는 대로 `S21` → `S23` 을 잇는다** — 배치5 에서 `S19` 를 「배치 안에서 후속」으로
돌린 것과 같은 모양이다.

**인바운드 실측**(`check-links.ts refs`, 넷 다 참조됨):

| 편 | 상호 링크(그 편 커밋 몫) | 인덱스(배치 종료 일괄) | manifest |
| --- | --- | --- | --- |
| `radixSort` | `countingSort-guide.md:620` | `문제_가이드_목록.md:17` | `:1277` |
| `maximumProductSubarray` | 없음 | `:21` | `:265` |
| `houseRobber` | 없음 | `:22` | `:181` |
| `bestTimeToBuyAndSellStock` | 없음 | `:22` | `:111` |

`houseRobber` 와 `bestTimeToBuyAndSellStock` 이 **인덱스 같은 줄(22)** 에 있다 — 배치 종료
일괄 반영에서 한 줄을 두 번 고치는 자리다. `tools/ord004-manifest.json` 넷은 기존 부채라
`KAN-034.8`(v1 잔여 처분) 몫이고 이 배치에서 안 건드린다.

**마지막 work 를 다음 배치로 미루지 않는다.** 넷이 다 닫혀야 W1 이 비고, 그래야
웨이브 배리어가 풀려 W2 후보가 열린다(`§2`). 셋만 닫으면 배리어가 그대로다.
