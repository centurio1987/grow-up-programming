---
card: KAN-034.6-CF6ZHE
batch: 7
created: 2026-08-30
branch: KAN-034-KSD7XR
status: 계획
steps: S17, S26, S22, S38
---

# KAN-034.6-CF6ZHE 배치7 — `sorting`·`graph` 마감

카드: [KAN-034.6-CF6ZHE.md](../KANBAN.cards/KAN-034.6-CF6ZHE.md) · 범위 `S17` · `S26` · `S22` · `S38`
선행: [배치6](KAN-034.6-CF6ZHE.batch6.md)

> **이 문서는 착수 전 계획이다.** 수행 내역은 카드 실행 문서의 「수행 내역」에 있다.

## 1. 작업 패키지

**공통 완료 기준은 카드 실행 문서 「실행 계획」의 41편 공통 블록이고, 명령 전문은 그 문서
「검증」 절이다.** 여기에는 편마다 다른 것만 적는다.

### WP1 · `S17` kthSmallest (sorting · 뷰 array)

이 편으로 `sorting` 가 마감된다

**완료 기준**: `src/algorithms/sorting/kthSmallest/kthSmallest-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `build-html` exit 0 · `bun test src/algorithms/sorting/kthSmallest/kthSmallest-guide.test.ts` 통과

### WP2 · `S26` undirectedCycleDetection (graph · 뷰 graph+keyValue)

이 편으로 `graph` 가 마감된다

`directedCycleDetection`(`S25`, 배치6 — 앞 배치)와 상호 링크가 잦은 짝이다. `check-links.ts refs` 가 내는 자리를 그 편 커밋에 함께 넣는다

**완료 기준**: `src/algorithms/graph/undirectedCycleDetection/undirectedCycleDetection-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `build-html` exit 0 · `bun test src/algorithms/graph/undirectedCycleDetection/undirectedCycleDetection-guide.test.ts` 통과

### WP3 · `S22` nextGreaterElement (array · 뷰 array+keyValue)


**완료 기준**: `src/algorithms/array/nextGreaterElement/nextGreaterElement-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `build-html` exit 0 · `bun test src/algorithms/array/nextGreaterElement/nextGreaterElement-guide.test.ts` 통과

### WP4 · `S38` extendedEuclidean (number-theory · 뷰 keyValue)

`gcd`(`S37`, 배치4 — 앞 배치)와 상호 링크가 잦은 짝이다. `check-links.ts refs` 가 내는 자리를 그 편 커밋에 함께 넣는다

**완료 기준**: `src/algorithms/number-theory/extendedEuclidean/extendedEuclidean-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `build-html` exit 0 · `bun test src/algorithms/number-theory/extendedEuclidean/extendedEuclidean-guide.test.ts` 통과

## 2. 의존과 순서

넷 다 카테고리가 다르다(`sorting` · `graph` · `array` · `number-theory`) — 도구가 카테고리마다 한 편만 후보로 내므로
이것이 병렬 폭의 상한이다. 배치 안 상호 대기 없음.

### 공유 파일 넷은 워커가 안 건드린다

`문제_가이드_목록.md` · `tools/_baseline/guide-rhythm.tsv` ·
`sandbox/algo-guide-v2/FEEDBACK.md` §5 · `KANBAN.md` 넷은 배치 전체가 공유한다.
**워커는 자기 편 디렉터리만 쓴다.** 넷은 배치를 닫을 때 한 자리에서 일괄 반영하고,
그 일괄 반영도 전진 한 단위라 커밋한다. `tools/ord004-manifest.json` 은 `KAN-034.8` 몫이다.

**워커는 `git add` 도 하지 않는다** — 한 워크트리의 인덱스를 공유해서 스테이징이 남의
커밋에 딸려 들어간다(W1 배치6 에서 실제로 났다).

## 3. 리스크

- **골격 결함이 있으면 이 배치의 편들이 함께 틀린다.** 배치를 닫기 전에 `bun run tools/ci.ts all` 이 초록이어야 한다.
- **`tools/check-guide-rhythm.ts` 를 v2 편에 부르지 않는다** — 구 헤딩 접두와 어긋나 조용히 공전하고 통과 표시가 거짓이 된다.
- **스캐너 넷이 0 이어도 `FEEDBACK.md` §3 대조를 밟는다** — W1 배치6 에서 네 편 전부 그 뒤에 결함이 나왔다(3 · 3 · 7건).
- 되돌리기는 편 단위다 — 편 하나가 커밋 하나이고 태그 하나를 받는다.

## 4. 착수 시점 판단

**2026-09-03 착수. 네 편을 그대로 간다 — `S38` 을 배치8 로 미루지 않는다.**

근거 넷을 착수 직전에 실측했다.

- **골격 기준선이 초록이다** — 배치6 을 닫으면서 `bun run tools/ci.ts all` 이 16 단계 통과했고
  `bun run tools/check-links.ts check` 가 448 건 전부 실재를 냈다(리스크 1 이 닫혔다).
- **넷 다 지금 잡을 수 있다** — `bun run tools/algo-wbs.ts` 의 W2 claim 후보 8 건에 넷이
  전부 들어 있다(`sorting` kthSmallest · `graph` undirectedCycleDetection · `array`
  nextGreaterElement · `number-theory` extendedEuclidean). **배치6 이 닫히면서 앞 셋이
  열렸다** — 도구가 카테고리마다 한 편만 내므로 두 배치가 순차인 것이 이것으로 확인됐다.
- **편 커밋에 함께 넣을 링크 교체가 둘 있다.** WP2 가 예고한
  `directedCycleDetection`↔`undirectedCycleDetection` 짝은 **이번에는 실재한다** —
  배치6 에서 선 `directedCycleDetection-guide.md:784` 가 이 편의 `.mdx` 를 가리킨다.
  WP4 가 예고한 `gcd`↔`extendedEuclidean` 은 그 방향이 아니었다 — `gcd` 는 이 편을
  안 부르고, 대신 **아직 `.mdx` 인 `crt-guide.mdx:136`** 이 이 편을 가리킨다(`crt` 는
  이 웨이브 밖이라 그 편을 재집필하지 않고 링크 한 줄만 고친다). `kthSmallest` 와
  `nextGreaterElement` 의 인바운드는 `tools/ord004-manifest.json` 과
  `문제_가이드_목록.md` 두 자리뿐이라 교체가 없다.
- **배치 크기를 바꿀 이유가 없다** — 배치1~6 이 전부 work 4 개로 닫혔다(24/41).

**남기는 경계 둘.**

- **이 배치로 `sorting` 과 `graph` 가 마감된다**(WP1 · WP2). 카테고리가 닫히면 그 자리의
  claim 후보가 빠지므로 다음 배치의 후보 목록이 좁아진다 — 배치8 착수 때 다시 잰다.
- 스캐너 넷이 0 이어도 `FEEDBACK.md` §3 대조를 편마다 밟는다(리스크 3). 배치6 이 65 건을
  냈고 그중 무거운 것 둘이 **참조 편에서 옮겨 와 이 편에서는 거짓이 된 문장**과
  **낫표 안이라 스캐너를 빠져나간 표현**이었다. 둘 다 대조 없이는 안 잡힌다.
