---
card: KAN-026
batch: 7
created: 2026-09-14
branch: KAN-026
status: 계획
steps: S11, S12
---

# KAN-026 배치7 — 「자료구조인가」가 걸린 둘

카드: [KAN-026.md](../KANBAN.cards/KAN-026.md) · 범위 `S11` · `S12`
선행: [배치6](KAN-026.batch6.md)

> **이 문서는 착수 전 계획이다.** 수행 내역은 카드 실행 문서의 「수행 내역」에 있다.
> 수행 방식: 단일 에이전트(2026-09-14 유저 결재). 착수 전 `python3 <skill>/scripts/kanban.py resume . --card KAN-026` 로 다음 work 를 확인한다.

## 1. 작업 패키지

### WP1 · `S11` `graph-repr/dag` 계약 전환

걸린 것: 비순환이 불변식. 위상 순서는 알고리즘이라 계약에 들어오는지 — `S1` 결론.

**완료 기준**: 공통 완료 기준은 카드 문서 「실행 계획 · 단계별 완료 기준」의 구조 하나 항목이다 — 헤더 여섯 항목 · `.contract.ts` + `.test.ts` + `_reference/<name>.ts`(`__cost` · `#region guide:core`) · vector 등록(알파벳 순) · `check-links refs` 뒤 `-problem.md` 제거 · `bun run tools/ci.ts all` · `check-contract` · `tsc` · biome 경고 0 · 종료 산출(불변 사실은 카드 대역).

### WP2 · `S12` `hash/rollingHash` 계약 전환

걸린 것: `S1` 이 자료구조가 아니라고 판정했으면 처분(이관·제거)을 실행하고 inventory 에 사유.

**완료 기준**: 공통 완료 기준은 카드 문서 「실행 계획 · 단계별 완료 기준」의 구조 하나 항목이다 — 헤더 여섯 항목 · `.contract.ts` + `.test.ts` + `_reference/<name>.ts`(`__cost` · `#region guide:core`) · vector 등록(알파벳 순) · `check-links refs` 뒤 `-problem.md` 제거 · `bun run tools/ci.ts all` · `check-contract` · `tsc` · biome 경고 0 · 종료 산출(불변 사실은 카드 대역).

## 2. 의존과 순서

- **결재 반영**: `rollingHash` 는 알고리즘 트랙 이관(결재 2026-09-15). 옮긴 편의 v2 가이드 집필은 이 카드 밖 — 알고리즘 트랙 게이트가 가이드를 요구하면 멈추고 보고한다.
- 서로 독립.
- 불변 사실 번호는 이 카드 대역(200–229)만 쓴다 — `KAN-027` 가 다른 워크트리에서 같은 런북을 고친다.

## 3. 리스크

- 처분이 「제거·이관」이면 `check-links refs` 로 참조 전수를 먼저 받는다 — 알고리즘 가이드가 링크하고 있을 수 있다.
- 세션 절반을 넘길 기미가 보이면 이 배치의 마지막 work 를 다음 배치 앞으로 미루고 「4. 착수 시점 판단」에 적는다.

## 4. 착수 시점 판단
<!-- 착수할 때 채운다 — 마지막 work 를 다음 배치로 미룰지 여기서 정한다. -->

**`S11` 을 먼저 닫고, `S12` 는 조사 뒤 조건부로 실행한다(2026-09-15 착수 지시).** 두 work 는 서로 독립이다. 앞 배치
결정 넷이 둘 다의 입력을 이미 닫았다 — `dag` 는 자료구조(S1)이고 정점 번호 모형은 배치6 이 정했으며, `rollingHash`
처분은 이관으로 결재됐다.

- **불변 사실 번호는 둘째 대역 283–299 를 쓴다.** 위 「2. 의존과 순서」의 「200–229 만」은 계획 시점 문장이다 — 그 대역은
  S6 에서 소진됐고 270–282 는 배치5 까지 썼다.
- **`S11` — 완료.** `addEdge` 상한을 무엇의 크기로 적을지가 규약 절차로 닫히지 않아 「닿는 부분」을 고르고 사람 결정으로
  올렸다(`docs/ORD-006-conventions.md` 「A군 비순환 방향 그래프」). 그래프 크기로 되돌리는 길은 시나리오 하나 · 헤더 세
  자리를 고치는 것으로 준비돼 있다.
- **`S12` — 멈춤, todo 로 둔다.** 실행 조건 셋 중 ①(가이드 없이 게이트 통과)과 ②(옛 가이드를 보존할 자리)가 깨끗이 서지
  않았다 — `tools/algo-wbs.test.ts` 가 목록의 알고리즘 편을 111 로 고정하고 가이드 없는 편을 셀 자리가 없으며, 알고리즘
  트랙은 살아 있는 `.mdx` 도 `_deprecated/` 도 두지 않는다. 파일은 옮기지 않았다. 조사 · 선택지는 같은 문서
  「A군 알고리즘 트랙 이관 조사 — rollingHash」, 불변 사실 288.
- 예산은 절반을 넘지 않았다. 미룬 work 는 없다 — `S12` 는 미룬 것이 아니라 조건이 안 서서 멈춘 것이다.
