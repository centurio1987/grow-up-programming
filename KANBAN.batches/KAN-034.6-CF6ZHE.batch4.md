---
card: KAN-034.6-CF6ZHE
batch: 4
created: 2026-08-30
branch: KAN-034-KSD7XR
status: 계획
steps: S8, S31, S7, S37
---

# KAN-034.6-CF6ZHE 배치4 — `graph-flow`·`tree` 첫 편

카드: [KAN-034.6-CF6ZHE.md](../KANBAN.cards/KAN-034.6-CF6ZHE.md) · 범위 `S8` · `S31` · `S7` · `S37`
선행: [배치3](KAN-034.6-CF6ZHE.batch3.md)

> **이 문서는 착수 전 계획이다.** 수행 내역은 카드 실행 문서의 「수행 내역」에 있다.

## 1. 작업 패키지

**공통 완료 기준은 카드 실행 문서 「실행 계획」의 41편 공통 블록이고, 명령 전문은 그 문서
「검증」 절이다.** 여기에는 편마다 다른 것만 적는다.

### WP1 · `S8` kruskalMst (graph-flow · 뷰 graph+keyValue)

**`graph-flow` 카테고리 첫 편이다** — 이 편이 서야 같은 카테고리의 후속이 열린다

`primMst`(`S9`, 배치5 — 뒤 배치)와 상호 링크가 잦은 짝이다. `check-links.ts refs` 가 내는 자리를 그 편 커밋에 함께 넣는다

**완료 기준**: `src/algorithms/graph-flow/kruskalMst/kruskalMst-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `build-html` exit 0 · `bun test src/algorithms/graph-flow/kruskalMst/kruskalMst-guide.test.ts` 통과

### WP2 · `S31` treeDiameter (tree · 뷰 keyValue+tree)

**`tree` 카테고리 첫 편이다** — 이 편이 서야 같은 카테고리의 후속이 열린다

이 편으로 `tree` 가 마감된다

**완료 기준**: `src/algorithms/tree/treeDiameter/treeDiameter-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `build-html` exit 0 · `bun test src/algorithms/tree/treeDiameter/treeDiameter-guide.test.ts` 통과

### WP3 · `S7` longestIncreasingSubsequence (array · 뷰 array+keyValue)


**완료 기준**: `src/algorithms/array/longestIncreasingSubsequence/longestIncreasingSubsequence-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `build-html` exit 0 · `bun test src/algorithms/array/longestIncreasingSubsequence/longestIncreasingSubsequence-guide.test.ts` 통과

### WP4 · `S37` gcd (number-theory · 뷰 keyValue)

`extendedEuclidean`(`S38`, 배치7 — 뒤 배치)와 상호 링크가 잦은 짝이다. `check-links.ts refs` 가 내는 자리를 그 편 커밋에 함께 넣는다

**완료 기준**: `src/algorithms/number-theory/gcd/gcd-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `build-html` exit 0 · `bun test src/algorithms/number-theory/gcd/gcd-guide.test.ts` 통과

## 2. 의존과 순서

넷 다 카테고리가 다르다(`graph-flow` · `tree` · `array` · `number-theory`) — 도구가 카테고리마다 한 편만 후보로 내므로
이것이 병렬 폭의 상한이다. 배치 안 상호 대기 없음.

### 공유 파일 넷은 워커가 안 건드린다

`문제_가이드_목록.md` · `tools/_baseline/guide-rhythm.tsv` ·
`sandbox/algo-guide-v2/FEEDBACK.md` §5 · `KANBAN.md` 넷은 배치 전체가 공유한다.
**워커는 자기 편 디렉터리만 쓴다.** 넷은 배치를 닫을 때 한 자리에서 일괄 반영하고,
그 일괄 반영도 전진 한 단위라 커밋한다. `tools/ord004-manifest.json` 은 `KAN-034.8` 몫이다.

**워커는 `git add` 도 하지 않는다** — 한 워크트리의 인덱스를 공유해서 스테이징이 남의
커밋에 딸려 들어간다(W1 배치6 에서 실제로 났다).

## 3. 리스크

- **골격 결함이 있으면 이 배치의 편들이 함께 틀린다.** 배치를 닫기 전에 `bun run tools/ci.ts all` 이 초록이어야 한다.
- **`tools/check-guide-rhythm.ts` 를 v2 편에 부르지 않는다** — 구 헤딩 접두와 어긋나 조용히 공전하고 통과 표시가 거짓이 된다.
- **스캐너 넷이 0 이어도 `FEEDBACK.md` §3 대조를 밟는다** — W1 배치6 에서 네 편 전부 그 뒤에 결함이 나왔다(3 · 3 · 7건).
- 되돌리기는 편 단위다 — 편 하나가 커밋 하나이고 태그 하나를 받는다.

## 4. 착수 시점 판단

착수함(2026-08-31). 넷을 그대로 이 배치에서 돈다 — 다음 배치로 미루는 work 는 없다.

**이 배치는 단일 세션이 순차로 돈다.** 배치1~3 은 워커 넷을 병렬로 띄웠는데, 이 세션은
서브에이전트 사용이 꺼져 있어 오케스트레이션 관점을 못 쓴다. 산출물과 완료 기준은 그대로이고
갈리는 것은 벽시계와 **대조가 한 사람 손에 들어온다**는 것 하나다 — 배치1~3 은 워커가 자가
`FEEDBACK.md` §3 대조를 하고 오케스트레이터가 다시 대조해 매 편 1~2건을 더 잡았다. 그 두
번째 눈이 없어지므로 **편마다 §3 대조를 원고와 시간을 띄워 두 번 밟는다**(집필 직후 한 번,
스캐너·테스트를 다 통과시킨 뒤 한 번). §2 의 워커 제약 둘(공유 파일 넷을 안 건드린다 ·
`git add` 를 안 한다)은 순차 수행에서 저절로 지켜진다.

근거 넷(전부 착수 직전 실측).

- **골격이 성해 있다** — `bun run tools/ci.ts all` 이 `all 모드 통과 — 단계 16개` 로 exit 0.
  배치3 종료 뒤 마커 id 문법 교정으로 15단계가 16단계가 된 상태 그대로다.
- **넷 다 지금 잡을 수 있다** — `bun run tools/algo-wbs.ts` 가 낸 W2 claim 후보 10건 안에
  `kruskalMst`(graph-flow) · `treeDiameter`(tree) ·
  `longestIncreasingSubsequence`(array) · `gcd`(number-theory) 가 모두 있다.
  카테고리가 넷 다 달라 도구 판정으로도 동시에 선다. 전개는 `[W2] 12/41` 에서 출발한다.
- **뷰 조합 첫 편이 하나도 없다** — 후보 줄에 `[단독]` 표시가 넷 다 안 붙었다. 넷의 조합은
  전부 선례가 있다: `graph+keyValue` 는 `S2` topologicalSort, `keyValue+tree` 는 `S12` trie
  (`S13` radixTree 가 라벨 규약을 넓혔다), `array+keyValue` 는 `S6`
  bestTimeToBuyAndSellStockK, `keyValue` 단독은 `S35` fastPower(`S18` enumerateSubmasks 가
  비트 연산 규약 여섯을 더했다). `S8`·`S31` 은 **카테고리** 첫 편이지만 이 배치에 같은
  카테고리의 후속이 없다 — `graph-flow` 후속은 `S9` primMst(배치5) · `S27` isBipartite(배치8)
  이고 `tree` 는 `S31` 이 곧 마감이다. 그래서 이 배치 안에 선례 대기가 없다.
- **인바운드에 `.mdx` 상호 링크가 없다** — `bun run tools/check-links.ts refs` 를 넷에 돌린
  결과가 편마다 두 자리뿐이고 둘 다 공유 파일이다(`tools/ord004-manifest.json` 은
  `KAN-034.8` 몫, `문제_가이드_목록.md` 는 배치 종료 일괄 반영 몫). **WP1·WP4 가 예상한
  `kruskalMst`↔`primMst` · `gcd`↔`extendedEuclidean` 상호 링크는 인바운드에 없다** —
  둘 다 아직 `.mdx` 라 새로 쓰는 `.md` 에서도 걸지 않는다. 그러므로 편 커밋에 함께 넣을
  링크 교체가 이 배치에는 없다.
