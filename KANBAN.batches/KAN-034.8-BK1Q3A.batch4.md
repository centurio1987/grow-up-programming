---
card: KAN-034.8-BK1Q3A
batch: 4
created: 2026-09-04
branch: KAN-034-KSD7XR
status: 완료
steps: S8, S9, S10
---

# KAN-034.8-BK1Q3A 배치4 — v1 잔여 처분 (W3 종료 후)

카드: [KAN-034.8-BK1Q3A.md](../KANBAN.cards/KAN-034.8-BK1Q3A.md) · 범위 `S8` `S9` `S10`
선행: [배치3](./KAN-034.8-BK1Q3A.batch3.md) + **`KAN-034.7-QMZ3RE`(W3) 종료**

> **이 문서는 착수 전 계획이다.** 수행 내역은 카드 실행 문서의 「수행 내역」에 있다.

**이 배치만 W3 에 걸린다.** 셋 다 판단이 「대상이 0 이다」에 걸려 있고, 그것은 W3 이 끝나야
참이 된다(카드 전략 2항). 2026-09-04 실측으로 셋이 같은 30 을 가리킨다 — 살아 있는 알고리즘
`.mdx` 30장 · rhythm baseline 의 `src/algorithms` 행 30 · W3 잔여 30편.

## 1. 작업 패키지

### WP1 · `S8` `_deprecated/` 알고리즘 몫 처분

알고리즘 쪽 `_deprecated/` 에 `.mdx` **107장**이 있다(자료구조 쪽 70장은 ORD-006 몫이라 안
건드린다). **지울지 남길지가 처분 방식이 갈리는 자리다** — 지우면 git 이력에만 남는다.

**참조 스윕이 먼저다.** `bun run tools/check-links.ts refs src/algorithms` 로 구판을 가리키는
자리를 전수로 낸 뒤에 옮긴다. 손 `grep` 은 상대 경로 import 를 놓친다(KAN-031 메모의 실측).

**완료 기준**: `find src/algorithms -path '*_deprecated*' -type f | wc -l` 이 0 ·
`bun run tools/check-links.ts check` 통과

### WP2 · `S9` rhythm 스캐너 알고리즘 몫과 baseline 정리

`tools/check-guide-rhythm.ts` 를 **통째로 지우지 않는다** — 자료구조 트랙이 아직 쓴다.
알고리즘 몫만 걷고 baseline 의 `src/algorithms` 행을 지운다.

**대상이 0 인 스캐너는 통과 표시가 무의미해지고 다음 사람이 그것을 근거로 읽는다** —
이 work 가 있는 이유가 그것이다.

**완료 기준**: `grep -c 'src/algorithms' tools/_baseline/guide-rhythm.tsv` 가 0 ·
`bun run tools/check-guide-rhythm.ts` 가 자료구조만 보고 통과 · `ci.ts all` 통과

### WP3 · `S10` 이월 셋 처분 — 예시 교체가 아니라 spec 정리

셋이 `convexHull-guide.mdx` 를 가리키는데 그 파일은 이미 없다.

| 자리 | 무엇 |
| --- | --- |
| `.claude/authoring/paths.json:29` | `exemplars.algo-guide` |
| `tools/ord004-regen.workflow.mjs:26` | `EXEMPLAR.algo` |
| `tools/ord004-manifest.json:659` | ORD-004 매니페스트 |

**다른 살아 있는 `.mdx` 로 옮기지 않는다.** `paths.json` 자신의 `_note_algo-guide` 가 처방을
적어 두었다 — 「알고리즘 트랙이 v2 골격으로 다 넘어가면 이 spec 자체가 대상을 잃으므로, 그때
예시가 아니라 spec 을 정리한다」. W3 종료가 그 「그때」다.

**완료 기준**: 셋 중 어느 것도 없는 파일을 안 가리킴 · `authoring-write algo-guide` 경로가
없는 파일 Read 로 실패하지 않음 · `check-links check` 통과

## 2. 의존과 순서

**배치 밖 의존이 하나다 — `KAN-034.7-QMZ3RE` 가 완료여야 한다.** 그 전에는 이 배치의 세 완료
기준이 전부 거짓이므로 착수해도 닫히지 않는다.

`S8` → `S9` 순서다. `_deprecated/` 를 처분하면 rhythm 스캐너의 대상 집합이 확정된다.
`S10` 은 둘과 독립이지만 예산이 남는 자리가 아니라 순차로 둔다.

## 3. 리스크

- **`_deprecated/` 107장을 지우면 되돌리기가 git 이력뿐이다.** 참조 스윕이 「이게 전부」라고
  보고한 뒤에도 상대 경로가 남을 수 있다 — KAN-031 메모가 그 실패를 적어 두었고, 대상이
  `.md` 였다면 타입 검사도 못 잡았다. **스윕 → 이동 → `ci.ts all` → 삭제** 순서로 간다.
- `S10` 이 `algo-guide` spec 을 정리하면 **그 spec 을 쓰는 다른 프로젝트가 있는지**가 걸린다.
  `~/.claude/authoring/` 는 전역이고 여러 프로젝트가 함께 쓴다. 정리 전에 확인한다.
- 이 배치가 닫히면 카드 「검증」 절의 넷이 동시에 참이어야 한다. 하나라도 거짓이면 카드를
  검토로 옮기지 않는다.

## 4. 착수 시점 판단

**셋 다 이 배치에서 한다**(2026-09-09 착수 실측). **선행 조건이 참이 됐다** —
`KAN-034.7-QMZ3RE`(W3)가 42/42 로 완주했고 `algo-wbs` 가 **111/111 · W3 44/44**,
살아 있는 알고리즘 `.mdx` **0** 이다.

착수 직전 `bun run tools/ci.ts all` **exit 0** · `check-links check` **exit 0**(링크 733건).

### 배치 문서와 실측이 어긋나는 자리가 셋이다 — 전부 「이미 됐다」 쪽이다

이 문서는 **2026-09-04 실측**으로 쓰였고 그 뒤 W3 이 여덟 배치를 돌았다. 그 사이에 세 자리가
바뀌었다.

| 이 문서가 적은 것 | 2026-09-09 실측 | 왜 |
| --- | --- | --- |
| 「셋이 같은 30 을 가리킨다」 | 셋 다 **0** | W3 이 편마다 목록·rhythm 행을 함께 지웠다 |
| `S9` — baseline 의 `src/algorithms` 행 정리 | **이미 0** | 〃 |
| `S10` — `manifest:659` 한 자리 | **107 자리** | 문서가 `convexHull` 하나만 적었다 |

**`S9` 는 완료 기준 셋 중 둘이 이미 참이다.** `grep -c 'src/algorithms' guide-rhythm.tsv` 가
0 이고, 스캐너는 **이미 `_deprecated` 를 건너뛰고 `src/**/*-guide.mdx` 만 본다**
(`tools/check-guide-rhythm.ts:372-373`). 알고리즘 `.mdx` 가 0 이므로 **지금 그 스캐너가 보는
것은 자료구조 69편뿐**이고, 실행하면 「69편 검사 — 위반 0편」과 「래칫 baseline 69편 남음
(재집필 부채)」를 낸다.

**즉 「알고리즘 몫을 걷는다」에서 걷을 코드가 없다.** 대상 집합이 글롭 하나로 정해지고 그
글롭이 이미 비어 있다 — `S9` 에 남은 것은 **그 사실을 문서에 박는 일**이다. 이 work 가 있는
이유가 「대상이 0 인 스캐너는 통과 표시가 무의미해지고 다음 사람이 그것을 근거로 읽는다」였고,
그 위험은 **코드가 아니라 출력 문구와 헤더 주석**에 남아 있다.

### `S8` 의 참조 스윕은 깨끗하다 — 삭제해도 깨질 링크가 0 이다

`bun run tools/check-links.ts refs src/algorithms` 로 훑었고 **`_deprecated/` 를 가리키는
자리가 0 건**이다. 리스크 1번이 경고한 「스윕이 이게 전부라고 보고한 뒤에도 상대 경로가
남는다」는 KAN-031 실측인데, **그 실패 뒤에 `check-links.ts` 가 상대 경로 해석을 갖췄고**
(`tools/check-links.test.ts` 가 회귀를 막는다) 이번 스윕은 그 도구로 돌렸다.

그래도 **순서는 문서 그대로 간다** — 스윕 → 이동 → `ci.ts all` → 삭제. 107장은 전부 `.mdx`
이고 자료구조 쪽 70장은 안 건드린다.

### `S10` 의 리스크 2번은 실측으로 해소된다

「`algo-guide` spec 을 쓰는 다른 프로젝트가 있는지」가 걸렸는데, **그 spec 은 이 저장소 로컬**
이다(`.claude/authoring/specs/algo-guide/`). 전역 `~/.claude/authoring/specs/` 는 **없다** —
전역에 있는 것은 원칙(`principles/`)과 퍼소나(`voices/`)뿐이고 spec 은 프로젝트가 각자 든다
(`CLAUDE.md` 의 「집필 규칙의 정본은 플러그인이다」 표 그대로). **다른 프로젝트를 깨뜨리지
않는다.**

다만 **manifest 가 107 자리**라 이 work 의 크기가 문서보다 크다. 셋을 나눠 본다.

| 자리 | 무엇 | 처분 |
| --- | --- | --- |
| `.claude/authoring/paths.json:29` | `exemplars.algo-guide` | **spec 을 정리한다** — `_note_algo-guide` 자신의 처방 |
| `tools/ord004-regen.workflow.mjs:26` | `EXEMPLAR.algo` | 〃 |
| `tools/ord004-manifest.json` | 알고리즘 항목 **107개**가 죽은 `.mdx` 를 가리킨다 | 별도 판단 |

`check-links` 가 JSON 을 안 봐서 manifest 는 **조용히 낡아 있다.** W3 워커 여덟이 배치마다
그 자리를 보고했고 전부 이 카드 몫으로 넘겼다.

### W3 이 남긴 스캐너 구멍 서른한 건은 이 카드의 계획에 없다

W3 배치6~11 이 `KAN-034.8` 몫으로 넘긴 것이 서른한 건인데, **이 카드의 work 열둘은
2026-09-04 에 확정됐고 그 목록에 없다.** `S11`·`S12` 가 W3 배치4 의 발견으로 뒤에 붙은
선례가 있으므로 같은 방식이 성립한다 — **다만 이 배치의 셋과는 성격이 다르다.** 셋은
「대상이 0 이 됐으니 걷는다」이고 서른한 건은 「도구를 고친다」이다.

**이 배치에서 정하지 않는다.** 배치4 를 닫은 뒤 그 서른한 건을 **분류해 work 로 세울지 후속
카드로 낼지**를 유저에게 낸다 — 크기가 이 카드의 남은 셋보다 크고, `P15` 하나만 해도 오탐
조건이 셋 쌓여 있어 설계가 필요하다.

### 순서와 폭

`S8` → `S9` → `S10` 순차, 폭 1 이다(문서 그대로). 셋 다 파일 몇 개를 고치는 일이라 워커를
띄우지 않고 오케스트레이터가 직접 한다 — **워커를 띄우는 비용이 일보다 크다.**

### 끝난 뒤 — 착수 판단이 맞았는가 · 카드 종료 판정

**셋 다 닫혔다**(12/12). **카드 「검증」 절의 넷이 동시에 참이다.**

```
① 살아 있는 v1 산출        0
② rhythm 알고리즘 부채      0
③ _deprecated 알고리즘 몫   0
④ bun run tools/ci.ts all  exit 0
```

배치 종료 명령 넷도 전부 exit 0 이다 — `check-v2 --all` · `check-metaphor --all` ·
`check-proof --all` · `check-links check`.

### 착수 판단이 짚은 셋이 전부 맞았다

**「문서와 실측이 셋 어긋나고 전부 이미 됐다 쪽이다」가 맞았다.** 이 문서는 2026-09-04 에
쓰였고 그 뒤 W3 이 여덟 배치를 돌면서 세 자리를 미리 처리했다.

- **`S9` 는 코드가 이미 알고리즘을 안 봤다.** 글롭이 `src/**/*-guide.mdx` 이고 그 트랙에
  `.mdx` 가 0 이라 **걷을 코드가 없었다.** baseline 의 `src/algorithms` 행도 W3 이 편마다
  지워 0 이었다. **남은 위험은 출력 문구였다** — 「래칫 baseline 69편 남음 (재집필 부채)」가
  어느 트랙인지 안 말해 저장소 전체 부채로 읽힌다. 요약 줄이 트랙을 말하게 고치고
  회귀 시험 셋을 세웠다(`tools/check-guide-rhythm.test.ts`).
- **`S8` 의 스윕은 깨끗했다.** `check-links refs` 가 `_deprecated/` 인바운드 **0건**을 냈고,
  옮겨 두고 `check-links check`·`ci.ts all` 을 돌린 뒤 삭제했다. 107장 · 33,194줄.
- **`S10` 의 리스크 2번은 실측으로 해소됐다.** `algo-guide` spec 은 이 저장소 로컬이고
  전역 `specs/` 는 없다 — 다른 프로젝트를 안 깨뜨린다.

### 착수 판단이 예고한 대로 `S10` 이 문서보다 컸다

배치 문서는 manifest 를 **`:659` 한 자리**로 적었는데 실측은 **algo 항목 107개 전부**가 죽은
경로였다. `check-links` 가 JSON 을 안 봐서 **조용히 낡아 있었고**, W3 워커 여덟이 배치마다
그 자리를 보고하고 이 카드로 넘겼다.

**넷을 갈라 처분했다** — 자리마다 옳은 처분이 다르기 때문이다.

| 자리 | 처분 | 왜 |
| --- | --- | --- |
| `ord004-manifest.json` | algo 107 항목 제거 · `counts` 갱신 | ds 69 는 ORD-006 이 아직 쓴다 — **진행 기록째 남긴다** |
| `paths.json` `exemplars.algo-guide` | 항목을 걷고 사유 note | 그 note 자신의 처방 — 「예시가 아니라 spec 을 정리한다」 |
| `ord004-regen.workflow.mjs` | `EXEMPLAR.algo` 를 지우고 **받는 자리에서 막는다** | 없는 파일을 조용히 Read 하게 두면 워커가 거기서 실패하고 **왜 실패했는지가 어디에도 안 남는다** |
| `specs/algo-guide/` | **지우지 않고 은퇴 표시**(`spec.md` 머리 + `spec.json` 의 `retired`) | `authoring.lock.json` 이 해시로 고정하고 있고, 지우면 「왜 없어졌는가」가 사라진다 |

`authoring.py lock --update` 로 재고정했고 `validate` 통과다(갈래 위반 0).

### 워커를 안 띄운 판단이 맞았다

셋 다 파일 몇 개를 고치는 일이라 오케스트레이터가 직접 했다. **워커를 띄우는 비용이 일보다
크다**고 착수 판단이 적었고, 실제로 세 work 가 한 세션 안에서 끝났다.

### 실측 하나 — `check-proof --all` 이 10분을 넘는다

배치11 워커가 「2분 안에 안 끝난다」고 보고한 자리를 이 배치 종료에서 다시 쟀다. **exit 0
이지만 10분을 넘겼다**(111편 × 사이드카 두 번 호출). 편별 실행은 즉시 끝난다. **CI 시간
예산을 볼 자리이고 후속 몫이다.**

### 이 카드가 닫힌 뒤에 남는 것 — W3 이 넘긴 서른한 건

W3 배치6~11 이 `KAN-034.8` 몫으로 넘긴 스캐너 구멍이 서른한 건인데 **이 카드의 work 열둘에
없다**(2026-09-04 확정). 착수 판단이 「이 배치에서 정하지 않는다」고 적었고 그대로 뒀다.

**성격이 다르다** — 이 카드의 셋은 「대상이 0 이 됐으니 걷는다」이고 서른한 건은 「도구를
고친다」다. `P15` 하나만 해도 **일곱 배치 연속** 나왔고 오탐 조건이 셋 쌓여 있어(정렬 방향·
`^` 눈금 / 첫 줄이 데이터인 표 / 중첩 들여쓰기가 뜻인 그림) 설계가 필요하다. **분류해 work 로
세울지 후속 카드로 낼지는 유저가 정한다.**
