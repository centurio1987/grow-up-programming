---
card: KAN-034.6-CF6ZHE
batch: 2
created: 2026-08-30
branch: KAN-034-KSD7XR
status: 계획
steps: S15, S2, S5, S28
---

# KAN-034.6-CF6ZHE 배치2 — `keyValue+priorityQueue` 첫 편과 `graph`·`dp` 진입

카드: [KAN-034.6-CF6ZHE.md](../KANBAN.cards/KAN-034.6-CF6ZHE.md) · 범위 `S15` · `S2` · `S5` · `S28`
선행: [배치1](KAN-034.6-CF6ZHE.batch1.md)

> **이 문서는 착수 전 계획이다.** 수행 내역은 카드 실행 문서의 「수행 내역」에 있다.

## 1. 작업 패키지

**공통 완료 기준은 카드 실행 문서 「실행 계획」의 41편 공통 블록이고, 명령 전문은 그 문서
「검증」 절이다.** 여기에는 편마다 다른 것만 적는다.

### WP1 · `S15` medianFromDataStream (sorting · 뷰 keyValue+priorityQueue)

**뷰 조합 `keyValue+priorityQueue` 첫 등장이다** — 시뮬 규격에 선례가 없으므로 이 편이 선례를 만든다

**완료 기준**: `src/algorithms/sorting/medianFromDataStream/medianFromDataStream-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `build-html` exit 0 · `bun test src/algorithms/sorting/medianFromDataStream/medianFromDataStream-guide.test.ts` 통과

### WP2 · `S2` topologicalSort (graph · 뷰 graph+keyValue)


**완료 기준**: `src/algorithms/graph/topologicalSort/topologicalSort-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `build-html` exit 0 · `bun test src/algorithms/graph/topologicalSort/topologicalSort-guide.test.ts` 통과

### WP3 · `S5` editDistance (array · 뷰 matrix)


**완료 기준**: `src/algorithms/array/editDistance/editDistance-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `build-html` exit 0 · `bun test src/algorithms/array/editDistance/editDistance-guide.test.ts` 통과

### WP4 · `S28` subsetSum (dp · 뷰 keyValue+matrix)


**완료 기준**: `src/algorithms/dp/subsetSum/subsetSum-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `build-html` exit 0 · `bun test src/algorithms/dp/subsetSum/subsetSum-guide.test.ts` 통과

## 2. 의존과 순서

넷 다 카테고리가 다르다(`sorting` · `graph` · `array` · `dp`) — 도구가 카테고리마다 한 편만 후보로 내므로
이것이 병렬 폭의 상한이다. 배치 안 상호 대기 없음.

**다만 뷰 조합 첫 편이 하나 들어 있다** — `S15` medianFromDataStream(`keyValue+priorityQueue`).
그 편이 서야 같은 조합의 후속 배치가 열린다.

### 공유 파일 넷은 워커가 안 건드린다

`문제_가이드_목록.md` · `tools/_baseline/guide-rhythm.tsv` ·
`sandbox/algo-guide-v2/FEEDBACK.md` §5 · `KANBAN.md` 넷은 배치 전체가 공유한다.
**워커는 자기 편 디렉터리만 쓴다.** 넷은 배치를 닫을 때 한 자리에서 일괄 반영하고,
그 일괄 반영도 전진 한 단위라 커밋한다. `tools/ord004-manifest.json` 은 `KAN-034.8` 몫이다.

**워커는 `git add` 도 하지 않는다** — 한 워크트리의 인덱스를 공유해서 스테이징이 남의
커밋에 딸려 들어간다(W1 배치6 에서 실제로 났다).

## 3. 리스크

- **골격 결함이 있으면 이 배치의 편들이 함께 틀린다.** 배치를 닫기 전에 `bun run tools/ci.ts all` 이 초록이어야 한다.
- **뷰 조합 첫 편은 선례를 만든다.** 그 편의 `sim.ts` 가 틀리면 같은 조합의 후속 편이 전부 그것을 베낀다 — 다른 편보다 한 번 더 본다.
- **`tools/check-guide-rhythm.ts` 를 v2 편에 부르지 않는다** — 구 헤딩 접두와 어긋나 조용히 공전하고 통과 표시가 거짓이 된다.
- **스캐너 넷이 0 이어도 `FEEDBACK.md` §3 대조를 밟는다** — W1 배치6 에서 네 편 전부 그 뒤에 결함이 나왔다(3 · 3 · 7건).
- 되돌리기는 편 단위다 — 편 하나가 커밋 하나이고 태그 하나를 받는다.

## 4. 착수 시점 판단
<!-- 착수할 때 채운다 — 마지막 work 를 다음 배치로 미룰지 여기서 정한다. -->
