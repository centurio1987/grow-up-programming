---
card: KAN-034.5-0RGSQM
batch: 3
created: 2026-08-29
branch: KAN-034-KSD7XR
status: 계획
steps: S3, S5, S15, S22
---

# KAN-034.5-0RGSQM 배치3 — 후속 4편 — `binary-search` 마감 · `dp` 개시

카드: [KAN-034.5-0RGSQM.md](../KANBAN.cards/KAN-034.5-0RGSQM.md) · 범위 `S3` · `S5` · `S15` · `S22`
선행: [배치2](KAN-034.5-0RGSQM.batch2.md)

> **이 문서는 착수 전 계획이다.** 수행 내역은 카드 실행 문서의 「수행 내역」에 있다.

## 1. 작업 패키지

**공통 완료 기준은 카드 실행 문서 「실행 계획」의 24편 공통 블록이고, 명령 전문은 그 문서
「검증」 절이다.** 여기에는 편마다 다른 것만 적는다.

### WP1 · `S3` ternarySearch (binary-search · 뷰 array+keyValue)

이 편으로 `binary-search` 가 마감된다

**완료 기준**: `src/algorithms/binary-search/ternarySearch/ternarySearch-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `bun test src/algorithms/binary-search/ternarySearch/ternarySearch-guide.test.ts` 통과

### WP2 · `S5` prefixSumRangeQuery (array · 뷰 array+keyValue)

**완료 기준**: `src/algorithms/array/prefixSumRangeQuery/prefixSumRangeQuery-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `bun test src/algorithms/array/prefixSumRangeQuery/prefixSumRangeQuery-guide.test.ts` 통과

### WP3 · `S15` dfsTraversal (graph · 뷰 graph+keyValue)

인덱스 링크는 **1개**다 — 계획 시점의 「2회 등장」은 `S2` 정리로 이미 해소됐다(§4 실측)

**완료 기준**: `src/algorithms/graph/dfsTraversal/dfsTraversal-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `bun test src/algorithms/graph/dfsTraversal/dfsTraversal-guide.test.ts` 통과

### WP4 · `S22` coinChangeWays (dp · 뷰 keyValue+matrix)

`dp` 는 파일럿 `knapsack01` 이 이미 열었다. 카테고리에 이 편 하나뿐이다

**완료 기준**: `src/algorithms/dp/coinChangeWays/coinChangeWays-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `bun test src/algorithms/dp/coinChangeWays/coinChangeWays-guide.test.ts` 통과

## 2. 의존과 순서

넷 다 카테고리가 이미 열려 있다 — `S3`←`S1`, `S5`←`S4`, `S15`←`S13`,
`S22` 는 파일럿 `knapsack01` 이 연 `dp` 다. 배치 안 상호 대기 없음.

### 공유 파일 넷은 워커가 안 건드린다

편별 종료 산출 일곱 중 **넷이 배치 전체가 공유하는 파일**이다 —
`문제_가이드_목록.md` · `tools/_baseline/guide-rhythm.tsv` ·
`sandbox/algo-guide-v2/FEEDBACK.md` §5 · `KANBAN.md`.
넷을 워커가 각자 고치면 같은 줄에서 부딪히고, 부딪힌 결과가 조용히 반쯤 반영된다.

**워커는 자기 편 디렉터리만 쓴다.**
넷은 배치를 닫을 때 **한 자리에서 일괄 반영**한다. 그 일괄 반영도 전진 한 단위라 커밋한다.

## 3. 리스크

- **골격 결함이 있으면 이 배치의 4편이 함께 틀린다.** 폭을 도구가 내는 6 이 아니라 4 로
  낮춘 이유가 그것이다. 배치를 닫기 전에 `bun run tools/ci.ts all` 이 초록이어야 한다.
- **`tools/check-guide-rhythm.ts` 를 v2 편에 부르지 않는다.** 구 헤딩 접두와 어긋나 조용히
  공전하고 통과 표시가 거짓이 된다.
- 되돌리기는 편 단위다 — 편 하나가 커밋 하나이고 태그 하나를 받는다. 공유 파일 넷은 줄 단위
  추가·삭제라 편별 되돌리기가 서로를 안 깬다.

## 4. 착수 시점 판단
<!-- 착수할 때 채운다 — 마지막 work 를 다음 배치로 미룰지 여기서 정한다. -->
**착수 2026-08-29.** `bun run tools/algo-wbs.ts` 가 낸 claim 후보 6건 안에 이 배치의 넷이
전부 있다 — `ternarySearch` · `prefixSumRangeQuery` · `dfsTraversal` · `coinChangeWays`.
넷 다 카테고리·뷰 조합 선례가 이미 닫혀 있어 `[단독]` 표시가 없다.

후보에 함께 있는 `insertionSort`(`S10`) · `minMaxPair`(`S24`) 둘은 배치4·6 의 것이라 잡지
않는다. **도구가 내는 폭 6 을 그대로 쓰지 않고 4 로 유지한다** — 배치1·2 와 같은 판단이고
근거도 같다(골격 결함이 배치 전체를 함께 틀리게 한다).

**미룰 work 가 없다.** 넷이 서로를 기다리는 자리가 없고(§2), `S3` 이 밀리면 `binary-search`
카테고리가 W1 끝까지 남는다 — 이 편이 그 카테고리의 마지막이다.

**계획 정정 — 인덱스 중복 링크는 이미 없다.** 이 문서 `WP3` 과 상위 카드 실행 문서는
`dfsTraversal` · `dfsAllPaths` · `countIslands` 세 편이 `문제_가이드_목록.md` 에 두 번씩
나온다고 적었는데(계획 시점 실측 114회 등장 · 111 유니크), 착수 시점 실측은
**111 링크 전부 유니크**다. 상위 카드 `S2` 의 「인덱스 중복 정리」가 그것을 이미 닫았다.
`WP3` 의 해당 줄을 그에 맞게 고쳤다 — 워커는 링크 **한 줄**만 바꾼다.

**착수 직전 실측** — 네 대상 디렉터리에 `<name>.ts` · `<name>.test.ts` · `<name>-problem.md` ·
`<name>-guide.mdx` 가 있고 v2 산출은 하나도 없다. 넷 다 `guide-rhythm.tsv` 에 행이 **있다**.
전역으로는 살아 있는 `.mdx` **99** · `guide-rhythm.tsv` 의 `src/algorithms` 행 **93** ·
`algo-wbs` 가 `[W1] 10/26`. 배치가 닫히면 각각 95 · 89 · `[W1] 14/26` 이다.
