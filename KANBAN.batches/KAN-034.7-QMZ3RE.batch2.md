---
card: KAN-034.7-QMZ3RE
batch: 2
created: 2026-09-04
branch: KAN-034-KSD7XR
status: 계획
steps: S29, S1, S4, S9
---

# KAN-034.7-QMZ3RE 배치2 — `shortest-path`·`graph`·`graph-flow` 열기

카드: [KAN-034.7-QMZ3RE.md](../KANBAN.cards/KAN-034.7-QMZ3RE.md) · 범위 `S29` · `S1` · `S4` · `S9`
선행: [배치1](KAN-034.7-QMZ3RE.batch1.md)

> **이 문서는 착수 전 계획이다.** 수행 내역은 카드 실행 문서의 「수행 내역」에 있다.

## 1. 작업 패키지

**공통 완료 기준은 카드 실행 문서 「실행 계획」의 42편 공통 블록이고, 명령 전문은 그 문서
「검증」 절이다.** 여기에는 편마다 다른 것만 적는다.

### WP1 · `S29` segmentsIntersect (geometry · 뷰 keyValue)

**`guide-rhythm.tsv` 에 행이 없는 유일한 편이다** — 지울 행이 없는 것이 정상이고, 지금 `.mdx` 42 와 rhythm 41 의 차이 1 이 이것이다

목록 `:106` 을 `S30`(배치3)·`S31`(배치4)과 **셋이 공유한다** — 세 배치가 한 줄을 순차로 지난다. `S33` bentleyOttmann(배치8)이 이 편을 3곳에서 건다

**완료 기준**: `src/algorithms/geometry/segmentsIntersect/segmentsIntersect-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `build-html` exit 0 · `bun test src/algorithms/geometry/segmentsIntersect/segmentsIntersect-guide.test.ts` 통과

### WP2 · `S1` bellmanFord (shortest-path · 뷰 graph+keyValue)

목록 `:61` 을 `S2` spfa(배치4)와 공유한다

**완료 기준**: `src/algorithms/shortest-path/bellmanFord/bellmanFord-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `build-html` exit 0 · `bun test src/algorithms/shortest-path/bellmanFord/bellmanFord-guide.test.ts` 통과

### WP3 · `S4` zeroOneBfs (graph · 뷰 graph+keyValue)

**완료 기준**: `src/algorithms/graph/zeroOneBfs/zeroOneBfs-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `build-html` exit 0 · `bun test src/algorithms/graph/zeroOneBfs/zeroOneBfs-guide.test.ts` 통과

### WP4 · `S9` maxFlow (graph-flow · 뷰 graph+keyValue)

목록 `:67` 을 `S10` minCut(배치5)과 공유한다. `S10` 이 이 편을 2곳에서 걸므로 **이 편이 먼저다**

**완료 기준**: `src/algorithms/graph-flow/maxFlow/maxFlow-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `build-html` exit 0 · `bun test src/algorithms/graph-flow/maxFlow/maxFlow-guide.test.ts` 통과

## 2. 의존과 순서

넷 다 카테고리가 다르다(`geometry` · `shortest-path` · `graph` · `graph-flow`). 배치1 의
`convexHull` 이 닫혀야 `S29` 가 열린다 — **배치1 → 배치2 는 실제 대기다.**

**이 배치가 남기는 것 둘** — `S9` maxFlow 는 `S10` minCut(배치5)이 걸고, `S29`
segmentsIntersect 는 `S33` bentleyOttmann(배치8)이 건다. 둘 다 **거는 쪽이 뒤 배치**라
순서가 맞다.

### 공유 파일 넷은 워커가 안 건드린다

`문제_가이드_목록.md` · `tools/_baseline/guide-rhythm.tsv` ·
`sandbox/algo-guide-v2/FEEDBACK.md` §5 · `KANBAN.md` 넷은 배치 전체가 공유한다.
**워커는 자기 편 디렉터리만 쓴다.** 넷은 배치를 닫을 때 한 자리에서 일괄 반영하고,
그 일괄 반영도 전진 한 단위라 커밋한다. `tools/ord004-manifest.json` 은 `KAN-034.8` 몫이다.

**워커는 `git add` 도 하지 않는다** — 한 워크트리의 인덱스를 공유해서 스테이징이 남의
커밋에 딸려 들어간다(W1 배치6 에서 실제로 났다).

**목록 링크 교체는 언제나 `src/algorithms/` 접두사로 좁힌다** — `:81`·`:82` 는 같은 줄에
자료구조 트랙 링크를 들고 있어서 이름으로 치환하면 그 트랙까지 `.md` 가 된다.

## 3. 리스크

- **골격 결함이 있으면 이 배치의 편들이 함께 틀린다.** 배치를 닫기 전에 `bun run tools/ci.ts all` 이 초록이어야 한다.
- **`tools/check-guide-rhythm.ts` 를 v2 편에 부르지 않는다** — 구 헤딩 접두와 어긋나 조용히 공전하고 통과 표시가 거짓이 된다.
- **스캐너 넷이 0 이어도 `FEEDBACK.md` §3 대조를 밟는다** — W2 배치6~11 의 대조 건수가 65 · 94 · 116 · 166 · 79 · 63 이고 줄어드는 추세가 없다.
- **최악 입력이라고 손으로 적은 수의 성질을 실행으로 확인한다** — W2 에서 `isPrimeTrial` 의 999,999,000,001 이 소수였다.
- **경쟁 설계를 배정밀도로 재지 않는다** — `alt.ts` 의 `measure()` 가 매 실행마다 정본과 답을 대조한다(W2 `digitDp` 실패에서 굳은 규칙).
- 되돌리기는 편 단위다 — 편 하나가 커밋 하나이고 태그 하나(`kan/KAN-034.7-QMZ3RE/S<n>`)를 받는다.
- **`S29` 는 `guide-rhythm.tsv` 행이 없다** — 「행 삭제」 단계가 아무것도 안 하는 것이 정상이고, 없다고 다른 행을 지우지 않는다.

## 4. 착수 시점 판단

착수함(2026-09-04). **도구가 낸 후보 11건에 이 배치의 넷이 다 들어 있다.**

```
W3 claim 후보 11건 (카테고리별 한 편)   ← 배치1 종료 직후
  shortest-path    bellmanFord            graph+keyValue     ← 이 배치
  graph            zeroOneBfs             graph+keyValue     ← 이 배치
  graph-flow       maxFlow                graph+keyValue     ← 이 배치
  geometry         segmentsIntersect      keyValue           ← 이 배치
  tree             heavyLightDecomposition · string suffixArray · array sparseTableRangeMin ·
  number-theory millerRabin · dp tspBitmask · advanced divideAndConquerDp ·
  sorting externalMergeSort
막힌 편 27 — category 27
```

**`geometry` 가 열렸다.** 배치1 의 `convexHull` 이 서면서 막힌 편이 31 에서 27 로 내려갔고,
`segmentsIntersect` 가 후보에 처음 떴다. 이 웨이브에 카테고리 배리어는 더 없다.

**폭은 4 그대로다.** 배치1 이 넷을 한 번에 닫았고 골격 결함이 안 나왔다 — 되올릴 근거도
내릴 근거도 없다.

**인바운드 실측**(`bun run tools/check-links.ts refs`):

| 편 | 그 편 커밋 몫 | 인덱스(배치 종료 일괄) | 이월 |
| --- | --- | --- | --- |
| `segmentsIntersect` | **`bentleyOttmann-guide.mdx:34`·`:124`·`:237` 세 곳** | `문제_가이드_목록.md:106` | `ord004-manifest.json:715` |
| `bellmanFord` | 없음 | `:61` | `:1122` |
| `zeroOneBfs` | 없음 | `:63` | `:939` |
| `maxFlow` | **`minCut-guide.mdx:120`·`:164` 두 곳** | `:67` | `:771` |

**상호 링크 다섯 자리가 이 배치에 몰려 있다** — 42편 전체의 세 쌍 중 두 쌍이 여기다.
거는 쪽(`bentleyOttmann` 배치8 · `minCut` 배치5)이 둘 다 뒤 배치이므로 **아직 v1 인 그 파일의
링크만 `.md` 로 고쳐 이 배치의 편 커밋에 넣는다** — 배치1 이 `rotatingCalipersDiameter` 에 쓴
처리와 같다. 안 고치면 배치 종료에서 `check-links check` 가 다섯 자리를 깨진 것으로 낸다.

**목록 공유 줄 셋을 지난다** — `:61`(`S1`, 짝은 `S2` spfa 배치4) · `:67`(`S9`, 짝은 `S10` minCut
배치5) · `:106`(`S29`, 짝은 `S30`·`S31` 배치3·4). 셋 다 짝이 뒤 배치라 되돌릴 때는 역순이다.

**`segmentsIntersect` 는 `guide-rhythm.tsv` 에 행이 없다.** 42편 중 유일하고, 지금 살아 있는
`.mdx` 38 과 rhythm 행 37 의 차이 1 이 그것이다. 「행 삭제」 단계가 아무것도 안 하는 것이
정상이고, 없다고 다른 행을 지우지 않는다.

**배치1 이 남긴 것 둘을 이 배치가 받는다.** ① `$$` 는 한 줄에 닫는다 — 배치1 에서 둘이 걸렸고
`build-html` 만 잡는다. ② `geometry` 후속은 `convexHull` 의 `sideOf`(배정밀도 걸러내기 +
`BigInt` 되재기)를 가져간다 — `segmentsIntersect` 가 그 첫 사용처다.

**마지막 work 를 다음 배치로 미루지 않는다.** 넷 다 카테고리가 다르고 배리어가 없다.
