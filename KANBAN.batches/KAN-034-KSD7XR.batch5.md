---
card: KAN-034-KSD7XR
batch: 5
created: 2026-08-28
branch: KAN-034-KSD7XR
status: 계획
steps: S7
---

# KAN-034-KSD7XR 배치5 — W3 중요도 하 42편 전개

카드: [KAN-034-KSD7XR.md](../KANBAN.cards/KAN-034-KSD7XR.md) · 범위 `S7`
선행: [배치4](./KAN-034-KSD7XR.batch4.md)

> **이 문서는 착수 전 계획이다.** 수행 내역은 카드 실행 문서의 「수행 내역」에 있다.

**work 하나가 42편이라 이 배치는 한 세션에 안 들어간다.** 편 단위 재분해는 하위 카드
`KAN-034.7-QMZ3RE` 의 실행 문서에서 한다 — 거기서 `S<n>` 이 편 하나가 되고 배치가 다시 갈린다.
이 문서는 **웨이브 전체의 경계 조건**만 정한다.

## 1. 작업 패키지

### WP1 · `S7` W3 — 중요도 하 42편 (`KAN-034.7-QMZ3RE`)

집필 순서의 정본은 `문제_가이드_목록.md` 의 문서 순서다(등급 안에서도 위에 있을수록 자주
등장한다는 것이 그 문서의 규약이다). 카테고리 분포는 geometry 7 · advanced 5 · number-theory 5 · string 5 · tree 5 · graph 4 · graph-flow 4 · shortest-path 4 · array 2 · dp 2 · sorting 1 (11종, 파일럿 2편 포함 44).

**편마다 종료 산출 일곱**(카드 실행 문서 「편별 배치 종료 산출」 그대로):
`<name>-guide.md` + 사이드카 5종 생성 · 같은 편 `.mdx` 삭제 · `문제_가이드_목록.md` 링크 교체 ·
`tools/_baseline/guide-rhythm.tsv` 그 편 행 삭제(없는 편 8개는 없는 대로) ·
`FEEDBACK.md` §5 조건부 절 셋(`L34`·`L35`·`L36`) 판정 행 추가 · `verdicts/<name>-r<NN>.md` 커밋 ·
`KANBAN.md` 카드 메모 갱신(`manage-kanban` 경유)

**완료 기준**: 하 등급에서 살아 있는 `.mdx` **0** · `guide-rhythm.tsv` 41 → **0** · 살아 있는 `.mdx` 42 → **0**

## 2. 의존과 순서

**웨이브 경계가 배리어다.** 앞 웨이브가 안 비면 `tools/algo-wbs.ts` 가 이 웨이브의 claim
후보를 내지 않는다. 웨이브 안에서만 카테고리로 병렬화한다 — 중요도 순이 병렬화에 먹히면
유저 지시를 어기는 것이 된다.

**선례는 별도 단계가 아니다.** 카테고리가 처음 열리는 편과 뷰 조합이 처음 등장하는 편은
단독으로 돌리고, 그동안 **같은 카테고리의 후속만** 막는다(전역 정지가 아니다).
이 웨이브에서 처음 열리는 카테고리: `geometry`.
`matrix` 는 카테고리가 아니라 뷰다 — 실측 14 카테고리에 없고 `matrix` 를 쓰는 편 16개가
여러 카테고리에 흩어져 있다. 그래서 선례 판정 축이 둘이다.

## 3. 리스크

- **이해 시험 회차가 예산을 정한다.** 파일럿 실적이 `mosAlgorithm` 13회차 · `knapsack01` 3회 ·
  `quicksort` 3회다. 편당 상한 **5회차**, 초과하면 그 편을 보류하고 다음 편으로 넘어간다
  (웨이브를 막지 않는다). `exit 2`(미실행 — 외부 모델 소진)는 실패로 안 세고 배치를 멈춘다.
- **병렬 폭만큼 외부 모델 소진이 빨라진다.** 트랙 상한은 카테고리 수지만 실질 폭은 소진과
  선례 대기가 정한다.
- **`tools/check-guide-rhythm.ts` 를 v2 편에 호출하지 않는다.** 구 헤딩 접두와 어긋나 조용히
  공전하고 통과 표시가 거짓이 된다.

## 4. 착수 시점 판단
<!-- 착수할 때 채운다 — 마지막 work 를 다음 배치로 미룰지 여기서 정한다. -->
