---
card: KAN-034.5-0RGSQM
batch: 4
created: 2026-08-29
branch: KAN-034-KSD7XR
status: 계획
steps: S6, S10, S16, S24
---

# KAN-034.5-0RGSQM 배치4 — 후속 4편 — `advanced` 마감

카드: [KAN-034.5-0RGSQM.md](../KANBAN.cards/KAN-034.5-0RGSQM.md) · 범위 `S6` · `S10` · `S16` · `S24`
선행: [배치3](KAN-034.5-0RGSQM.batch3.md)

> **이 문서는 착수 전 계획이다.** 수행 내역은 카드 실행 문서의 「수행 내역」에 있다.

## 1. 작업 패키지

**공통 완료 기준은 카드 실행 문서 「실행 계획」의 24편 공통 블록이고, 명령 전문은 그 문서
「검증」 절이다.** 여기에는 편마다 다른 것만 적는다.

### WP1 · `S6` subarraySumEqualsK (array · 뷰 array+keyValue)

**완료 기준**: `src/algorithms/array/subarraySumEqualsK/subarraySumEqualsK-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `bun test src/algorithms/array/subarraySumEqualsK/subarraySumEqualsK-guide.test.ts` 통과

### WP2 · `S10` insertionSort (sorting · 뷰 array)

**완료 기준**: `src/algorithms/sorting/insertionSort/insertionSort-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `bun test src/algorithms/sorting/insertionSort/insertionSort-guide.test.ts` 통과

### WP3 · `S16` dfsAllPaths (graph · 뷰 graph+keyValue)

`문제_가이드_목록.md` 링크는 **한 줄**이다 — 계획이 적었던 중복은 상위 카드 `S2` 가 이미 닫았다(§4)

**완료 기준**: `src/algorithms/graph/dfsAllPaths/dfsAllPaths-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `bun test src/algorithms/graph/dfsAllPaths/dfsAllPaths-guide.test.ts` 통과

### WP4 · `S24` minMaxPair (advanced · 뷰 keyValue)

`S18` 이 연 카테고리의 후속. 이 편으로 `advanced` 가 마감된다

**완료 기준**: `src/algorithms/advanced/minMaxPair/minMaxPair-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `bun test src/algorithms/advanced/minMaxPair/minMaxPair-guide.test.ts` 통과

## 2. 의존과 순서

넷 다 카테고리가 열려 있다. `S24`←`S18`(배치1). 배치 안 상호 대기 없음.

### 공유 파일 넷은 워커가 안 건드린다

편별 종료 산출 일곱 중 **넷이 배치 전체가 공유하는 파일**이다 —
`문제_가이드_목록.md` · `tools/_baseline/guide-rhythm.tsv` ·
`sandbox/algo-guide-v2/FEEDBACK.md` §5 · `KANBAN.md`.
넷을 워커가 각자 고치면 같은 줄에서 부딪히고, 부딪힌 결과가 조용히 반쯤 반영된다.

**워커는 자기 편 디렉터리만 쓴다.**
넷은 배치를 닫을 때 **한 자리에서 일괄 반영**한다. 그 일괄 반영도 전진 한 단위라 커밋한다.

## 3. 리스크

- **골격 결함이 있으면 이 배치의 4편이 함께 틀린다.** 폭을 도구가 내는 6 이 아니라 4 로
  낮춘 이유가 그것이다. 배치를 닫기 전에 `bun run tools/ci.ts all` 이 초록이어야 한다.
- **`tools/check-guide-rhythm.ts` 를 v2 편에 부르지 않는다.** 구 헤딩 접두와 어긋나 조용히
  공전하고 통과 표시가 거짓이 된다.
- 되돌리기는 편 단위다 — 편 하나가 커밋 하나이고 태그 하나를 받는다. 공유 파일 넷은 줄 단위
  추가·삭제라 편별 되돌리기가 서로를 안 깬다.

## 4. 착수 시점 판단
<!-- 착수할 때 채운다 — 마지막 work 를 다음 배치로 미룰지 여기서 정한다. -->
**착수 2026-08-29.** `bun run tools/algo-wbs.ts` 가 낸 claim 후보 **4건이 이 배치의 넷과
정확히 일치한다** — `subarraySumEqualsK` · `insertionSort` · `dfsAllPaths` · `minMaxPair`.
배치1~3 은 도구가 6을 내는데 4로 낮춰 잡았지만, 이번에는 도구가 내는 폭이 이미 4다.

**`minMaxPair` 에 `[단독]` 이 붙는다 — 그래도 넷을 함께 돌린다.** `keyValue` 단독 뷰의 첫
편이라 도구가 표시한 것이고, 선례 대기는 **같은 카테고리의 후속만** 막는다(상위 카드 배치4 §2).
`advanced` 에 W1 남은 편은 이 하나뿐이라(`S18` 이 배치1 에서 카테고리를 열었다) 막을 후속이
없다. 뷰 조합 선례의 결정은 `minMaxPair-guide.sim.ts` 헤더에 박는다 — 배치1 의 `S4`·`S13`
과 같은 규약이다.

**미룰 work 가 없다.** 넷이 서로를 기다리는 자리가 없고(§2), 넷을 다 닫으면 `advanced` 와
`sorting` 의 W1 잔량이 각각 0 · 2 로 떨어진다.

**계획 정정 — `WP3` 의 인덱스 중복은 이미 없다.** 착수 시점 실측으로 네 편 모두
`문제_가이드_목록.md` 에 **한 줄씩**만 있다(`dfsAllPaths` 포함). 배치3 §4 가 같은 정정을
했고 그 근거가 여기에도 그대로 선다 — 워커는 링크 한 줄만 바꾼다. `WP3` 의 해당 줄을 고쳤다.

**착수 직전 실측** — 네 대상 디렉터리에 `<name>.ts` · `<name>.test.ts` · `<name>-problem.md` ·
`<name>-guide.mdx` 가 있고 v2 산출은 하나도 없다. 넷 다 `guide-rhythm.tsv` 에 행이 **있다**
(9 · 21 · 50 · 74 행). 네 편으로 들어오는 `.mdx` 상호 링크는 **0건**(배치3 이 남긴 인바운드
확인 절차를 착수 전에 돌렸다). 전역으로는 살아 있는 `.mdx` **95** · `guide-rhythm.tsv` 의
`src/algorithms` 행 **89** · `algo-wbs` 가 `[W1] 14/26`. 배치가 닫히면 각각 91 · 85 ·
`[W1] 18/26` 이다.
