---
card: KAN-026
batch: 4
created: 2026-09-14
branch: KAN-026
status: 계획
steps: S5
---

# KAN-026 배치4 — invariant 등급 — 이중 연결 리스트

카드: [KAN-026.md](../KANBAN.cards/KAN-026.md) · 범위 `S5`
선행: [배치3](KAN-026.batch3.md)

> **이 문서는 착수 전 계획이다.** 수행 내역은 카드 실행 문서의 「수행 내역」에 있다.
> 수행 방식: 단일 에이전트(2026-09-14 유저 결재). 착수 전 `python3 <skill>/scripts/kanban.py resume . --card KAN-026` 로 다음 work 를 확인한다.

## 1. 작업 패키지

### WP1 · `S5` `linear/doublyLinkedList` 계약 전환

걸린 것: 앞뒤 링크의 정합이 불변식(`p4-triage.md:42`). `xorLinkedList-guide.md:58` 이 이 구조의 `.mdx` 를 링크하지만 이 카드는 `.mdx` 를 안 지운다.

**완료 기준**: 공통 완료 기준은 카드 문서 「실행 계획 · 단계별 완료 기준」의 구조 하나 항목이다 — 헤더 여섯 항목 · `.contract.ts` + `.test.ts` + `_reference/<name>.ts`(`__cost` · `#region guide:core`) · vector 등록(알파벳 순) · `check-links refs` 뒤 `-problem.md` 제거 · `bun run tools/ci.ts all` · `check-contract` · `tsc` · biome 경고 0 · 종료 산출(불변 사실은 카드 대역).

## 2. 의존과 순서

- **결재 반영**: 연결 마디는 자명한 구현(결재 2026-09-15) — `S5` 는 `complexity` 가 아니다. `deque` 근거 재판정은 별도 카드다.
- `S4` 의 계약이 선 뒤 — 단방향과 계약이 갈리는 자리를 헤더에 적는다.
- 불변 사실 번호는 이 카드 대역(200–229)만 쓴다 — `KAN-027` 가 다른 워크트리에서 같은 런북을 고친다.

## 3. 리스크

- 불변식 스위트(축2)가 실제로 링크 불일치를 잡는지 결함 fixture 로 확인한다(`_fixtures/` 는 읽기만, 새 파일로).
- 세션 절반을 넘길 기미가 보이면 이 배치의 마지막 work 를 다음 배치 앞으로 미루고 「4. 착수 시점 판단」에 적는다.

## 4. 착수 시점 판단
<!-- 착수할 때 채운다 — 마지막 work 를 다음 배치로 미룰지 여기서 정한다. -->

**`S5` 하나를 이 배치에서 끝냈다 — 미룬 work 없음.** (2026-09-15, 단일 에이전트)

- 착수 시 워킹트리 깨끗 · `resume` 의 다음 work `S5` · 배치 4 확인.
- **불변 사실 대역이 바뀌었다.** §2 의 「200–229 만」은 계획 당시 값이고 직전 배치(S6)가 229 까지 썼다. 카드
  「전략」의 「넘치면 270–299」대로 270 부터 썼고 `docs/ORD-006-wbs.md` §2 표 · `tools/ord006-wbs.ts` TA 대역 ·
  런북 TA 절 제목을 「200–229 · 270–299」로 적었다(불변 사실 270–275).
- **§3 리스크 「축2가 링크 불일치를 잡는지」의 답.** 전제가 S1 에서 바뀌었다 — 「앞뒤 링크 정합」은 불변식이
  아니다(불변 사실 36). 그래도 물음은 남아 **정본 변이 넷**(이음 하나씩 빠뜨리기)으로 쟀고, 넷 다 축2의 수
  불변식과 축1 무작위에서 걸렸다. 경계 케이스는 처음에 넷 중 셋만 잡아 하나를 더했다(불변 사실 273). 결함
  fixture 는 계획의 「링크 불일치」 대신 계약이 가르는 셋으로 지었다 — 걷는 빼기(축3 `remove`) · 상자 배열(축3
  세 행) · 값 끌어오기 우회(축3 전부 통과, 축1이 잡음).
- **핸들이 계약에 들어온 첫 A군 계약이라 하네스를 고쳐야 하는지가 착수 전 불확실했다.** 번호 규칙으로
  하네스 무수정에 들어갔다(불변 사실 271). `_fixtures/` 는 새 파일 셋만 더했고 기존 파일은 안 건드렸다.
- `runContract.test.ts` 에 줄을 넣지 않았다(자기시험은 새 파일) — 불변 사실 106 의 전수 대조가 필요 없다.
