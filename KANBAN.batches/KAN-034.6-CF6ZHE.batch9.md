---
card: KAN-034.6-CF6ZHE
batch: 9
created: 2026-08-30
branch: KAN-034-KSD7XR
status: 계획
steps: S20, S24, S32, S39
---

# KAN-034.6-CF6ZHE 배치9 — `array` 마감 — 아홉째 편으로 가장 긴 사슬을 닫는다

카드: [KAN-034.6-CF6ZHE.md](../KANBAN.cards/KAN-034.6-CF6ZHE.md) · 범위 `S20` · `S24` · `S32` · `S39`
선행: [배치8](KAN-034.6-CF6ZHE.batch8.md)

> **이 문서는 착수 전 계획이다.** 수행 내역은 카드 실행 문서의 「수행 내역」에 있다.

## 1. 작업 패키지

**공통 완료 기준은 카드 실행 문서 「실행 계획」의 41편 공통 블록이고, 명령 전문은 그 문서
「검증」 절이다.** 여기에는 편마다 다른 것만 적는다.

### WP1 · `S20` lowestSetBit (bit-manipulation · 뷰 keyValue)


**완료 기준**: `src/algorithms/bit-manipulation/lowestSetBit/lowestSetBit-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `build-html` exit 0 · `bun test src/algorithms/bit-manipulation/lowestSetBit/lowestSetBit-guide.test.ts` 통과

### WP2 · `S24` slidingWindowMaximum (array · 뷰 array+keyValue)

이 편으로 `array` 가 마감된다

`tools/_baseline/guide-rhythm.tsv` 에 이 편의 행이 **없다** — 지울 행이 없는 것이 정상이다

**완료 기준**: `src/algorithms/array/slidingWindowMaximum/slidingWindowMaximum-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `build-html` exit 0 · `bun test src/algorithms/array/slidingWindowMaximum/slidingWindowMaximum-guide.test.ts` 통과

### WP3 · `S32` matrixChainMultiplication (dp · 뷰 keyValue+matrix)


**완료 기준**: `src/algorithms/dp/matrixChainMultiplication/matrixChainMultiplication-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `build-html` exit 0 · `bun test src/algorithms/dp/matrixChainMultiplication/matrixChainMultiplication-guide.test.ts` 통과

### WP4 · `S39` binomialModP (number-theory · 뷰 keyValue)


**완료 기준**: `src/algorithms/number-theory/binomialModP/binomialModP-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `build-html` exit 0 · `bun test src/algorithms/number-theory/binomialModP/binomialModP-guide.test.ts` 통과

## 2. 의존과 순서

넷 다 카테고리가 다르다(`bit-manipulation` · `array` · `dp` · `number-theory`) — 도구가 카테고리마다 한 편만 후보로 내므로
이것이 병렬 폭의 상한이다. 배치 안 상호 대기 없음.

### 공유 파일 넷은 워커가 안 건드린다

`문제_가이드_목록.md` · `tools/_baseline/guide-rhythm.tsv` ·
`sandbox/algo-guide-v2/FEEDBACK.md` §5 · `KANBAN.md` 넷은 배치 전체가 공유한다.
**워커는 자기 편 디렉터리만 쓴다.** 넷은 배치를 닫을 때 한 자리에서 일괄 반영하고,
그 일괄 반영도 전진 한 단위라 커밋한다. `tools/ord004-manifest.json` 은 `KAN-034.8` 몫이다.

**워커는 `git add` 도 하지 않는다** — 한 워크트리의 인덱스를 공유해서 스테이징이 남의
커밋에 딸려 들어간다(W1 배치6 에서 실제로 났다).

**워커의 임시 파일은 편 이름의 하위 디렉터리에 둔다** — `<스크래치패드>/<편 이름>/sweep.ts` 다.
스크래치패드 최상위에 `sweep.ts` 같은 공통 이름으로 두면 형제 워커가 서로를 덮는다. 배치8 에서
실제로 났고, 한 번은 **남의 편 증명 블록이 출력됐다.** 워커 지시에 이 문장을 그대로 넣는다.

## 3. 리스크

- **골격 결함이 있으면 이 배치의 편들이 함께 틀린다.** 배치를 닫기 전에 `bun run tools/ci.ts all` 이 초록이어야 한다.
- **`tools/check-guide-rhythm.ts` 를 v2 편에 부르지 않는다** — 구 헤딩 접두와 어긋나 조용히 공전하고 통과 표시가 거짓이 된다.
- **스캐너 넷이 0 이어도 `FEEDBACK.md` §3 대조를 밟는다** — W1 배치6 에서 네 편 전부 그 뒤에 결함이 나왔다(3 · 3 · 7건).
- 되돌리기는 편 단위다 — 편 하나가 커밋 하나이고 태그 하나를 받는다.

## 4. 착수 시점 판단

**2026-09-03 착수. 네 편을 그대로 간다 — 미루는 편이 없다.**

근거 넷을 착수 직전에 실측했다.

- **골격 기준선이 초록이다** — `bun run tools/ci.ts all` 이 **단계 16 개 통과**로 exit 0 이고
  (③ 실습 채점의 스텁 실패 313 건은 판정 제외가 정상이다), `bun run tools/check-links.ts check`
  가 **링크 478 건 전부 실재**를 냈다. 리스크 1 이 닫혔다.
- **넷 다 지금 잡을 수 있고, 후보가 정확히 넷이다** — `bun run tools/algo-wbs.ts` 의 W2 claim
  후보 **4 건**이 이 배치의 넷과 한 글자도 다르지 않다(`bit-manipulation` lowestSetBit ·
  `array` slidingWindowMaximum · `dp` matrixChainMultiplication · `number-theory` binomialModP).
  배치8 착수 때 6 건이던 것이 넷으로 준 것은 그 배치가 `graph-flow`·`binary-search` 를
  마감했기 때문이다. **이 배치가 후보 넷을 전부 가져가고 `array` 를 마감하므로 남는 후보는
  셋**이고, 배치10 의 work 가 셋인 것과 맞는다.
- **편 커밋에 함께 넣을 링크 교체가 하나 있다** — 넷에 `check-links.ts refs` 를 돌린 결과
  `slidingWindowMaximum` 에만 `.mdx` 상호 링크가 하나 붙어 있다
  (`src/data-structures/linear/monotonicQueue/monotonicQueue-guide.mdx:757`). 나머지 셋은 인바운드가
  `tools/ord004-manifest.json`(`KAN-034.8` 몫)과 `문제_가이드_목록.md`(배치 종료 일괄 반영 몫)
  두 자리뿐이다.
- **배치 크기를 바꿀 이유가 없다** — 배치1~8 이 전부 work 4 개로 닫혔다(32/41).

**남기는 경계 셋.**

- **그 링크 한 줄이 이 카드 `scope` 밖이다.** 카드 `scope` 는 `src/algorithms/**` 로 서고
  `src/data-structures/**` 는 안 들어 있다 — 이 카드가 그 트랙을 건드리는 첫 자리다. 그래도
  고치는 이유는, 안 고치면 `.mdx` 를 지우는 순간 링크 478 건 중 하나가 깨져 **웨이브 종료 기준
  「깨진 링크 0」이 이 편 때문에 무너지기** 때문이다. 「검증」 절이 `.mdx` 상호 링크는 그 편
  커밋에 함께 넣으라고 이미 정해 뒀다. **고치는 것은 그 한 줄의 확장자뿐**이고, 카드
  frontmatter 의 `scope` 에 그 파일 하나를 더해 검토가 이 사실을 보게 한다.
- **이 배치로 `array` 가 마감된다**(WP2). 배치7 이 `sorting`·`graph` 를, 배치8 이 `graph-flow`·
  `binary-search` 를 닫은 것과 같은 자리다. 남는 카테고리가 셋이 되므로 **다음 배치부터 병렬
  폭 상한이 4 아래로 내려간다** — 배치10 이 셋, 배치11 이 둘이다.
- **뷰 조합 첫 편이 없다** — 넷의 조합에 전부 선례가 있다. `keyValue` 단독은 `S18`
  enumerateSubmasks 가 비트 연산 규약 여섯을 `sim.ts` 헤더에 박아 뒀고 `lowestSetBit` 이 그것을
  그대로 물려받는다(`S18` 수행 내역이 뒤의 `lowestSetBit`·`matrixPowerFibonacci` 를 이름으로
  지목한다). `array+keyValue` 는 `S22`·`S23`, `keyValue+matrix` 는 `S28`·`S29`·`S30`,
  `number-theory` 의 `keyValue` 는 `S37`·`S38` 이 선례다. 배치8 의 `S30` 같은 선례 신설이 없어
  리스크 2 는 이번에 안 선다.

**§3 대조 방향.** 배치8 이 편마다 18~39 건을 냈고 값을 낸 방법 둘이 그대로 유효하다 —
**참조 편과의 정확 일치 줄 전수 대조**(저장소 골격 문구를 걷어내고 두 편에만 있는 문장만
고른다)와 **자기 문서 안의 모순 찾기**(본문이 적은 사실과 같은 편 `deep.math`·전개 값이
부딪히는 자리). 낫표 안 점검은 배치7·8 에서 연속으로 전수 0 건이었으므로 이번에도 **낫표 밖
다의어**로 방향을 둔다 — 배치8 에서 `METAPHORS` 정규식의 사각이 셋 더 드러났다(활용형 앞에
지시어·조사를 요구해 「서다」·「얹은」·「돌아야」를 못 본다).
