---
card: KAN-026
batch: 8
created: 2026-09-14
branch: KAN-026
status: 계획
steps: S13
---

# KAN-026 배치8 — 공간과 메모리 — 비트 배열

카드: [KAN-026.md](../KANBAN.cards/KAN-026.md) · 범위 `S13`
선행: [배치7](KAN-026.batch7.md)

> **이 문서는 착수 전 계획이다.** 수행 내역은 카드 실행 문서의 「수행 내역」에 있다.
> 수행 방식: 단일 에이전트(2026-09-14 유저 결재). 착수 전 `python3 <skill>/scripts/kanban.py resume . --card KAN-026` 로 다음 work 를 확인한다.

## 1. 작업 패키지

### WP1 · `S13` `linear/bitArray` 계약 전환

걸린 것: 존재 이유가 공간(`bitArray.ts:5`). 메모리 직접 사용 목록 편입은 `S1` 판정 — typed array `byteLength` 로 절약을 보일 수 있으면 넣지 않는다(`conventions.md:1425-1429`).

**완료 기준**: 공통 완료 기준은 카드 문서 「실행 계획 · 단계별 완료 기준」의 구조 하나 항목이다 — 헤더 여섯 항목 · `.contract.ts` + `.test.ts` + `_reference/<name>.ts`(`__cost` · `#region guide:core`) · vector 등록(알파벳 순) · `check-links refs` 뒤 `-problem.md` 제거 · `bun run tools/ci.ts all` · `check-contract` · `tsc` · biome 경고 0 · 종료 산출(불변 사실은 카드 대역).

## 2. 의존과 순서

- `S1` 처분.
- 불변 사실 번호는 이 카드 대역(200–229)만 쓴다 — `KAN-027` 가 다른 워크트리에서 같은 런북을 고친다.

## 3. 리스크

- 편입으로 판정되면 Rust 측정 경로(`rust/`)가 붙어 이 배치가 커진다 — 그때는 Rust 측정을 다음 배치 앞으로 미룬다.
- 세션 절반을 넘길 기미가 보이면 이 배치의 마지막 work 를 다음 배치 앞으로 미루고 「4. 착수 시점 판단」에 적는다.

## 4. 착수 시점 판단
<!-- 착수할 때 채운다 — 마지막 work 를 다음 배치로 미룰지 여기서 정한다. -->

**`S12` 를 건너뛰고 `S13` 을 먼저 했다(2026-09-15 착수 지시).** `resume` 은 배치7 의 `S12`(`hash/rollingHash` 알고리즘 트랙 이관)를
다음 work 로 가리켰지만, 그 work 는 배치7 에서 조사 뒤 멈췄고 선택지(`docs/ORD-006-conventions.md` 「A군 알고리즘 트랙 이관 조사 —
rollingHash」)가 **사람 결정 대기**다. 두 work 는 서로의 파일을 안 건드리므로 순서를 바꿔도 잃는 것이 없다. `S12` 는 todo 그대로다.

- **불변 사실 번호는 둘째 대역의 남은 자리 289–296 을 썼다.** 위 「2. 의존과 순서」의 「200–229 만」은 계획 시점 문장이다(그 대역은
  S6 에서 소진). 셋째 대역(350–379)은 열지 않았다 — 여덟으로 모자라지 않았다.
- **메모리 직접 사용 목록 편입은 아니다(`S1` 판정 유지)** — 위 「3. 리스크」의 Rust 측정 경로는 붙지 않았다. 판정 조건(정본이 상태를
  typed array 에 둔다)을 정본으로 지켰다.
- **판별 셋째 걸음은 뒤집히지 않았다** — 공간 제약을 무시한 구현이 네 축을 정본과 같은 값으로 통과했다.
- **`S1` 예상에서 갈린 것 하나** — 등급이 `invariant` 가 아니라 `basic` 이다. 물려받은 `count` 를 배제 없는 행으로 빼면서 불변식 후보가
  사라졌다(불변 사실 290).
- 예산은 절반을 넘지 않았다. 미룬 work 는 없다.
