---
card: KAN-034.7-QMZ3RE
batch: 9
created: 2026-09-04
branch: KAN-034-KSD7XR
status: 계획
steps: S8, S12, S17, S22
---

# KAN-034.7-QMZ3RE 배치9 — 카테고리 넷의 마지막 편

카드: [KAN-034.7-QMZ3RE.md](../KANBAN.cards/KAN-034.7-QMZ3RE.md) · 범위 `S8` · `S12` · `S17` · `S22`
선행: [배치8](KAN-034.7-QMZ3RE.batch8.md)

> **이 문서는 착수 전 계획이다.** 수행 내역은 카드 실행 문서의 「수행 내역」에 있다.

## 1. 작업 패키지

**공통 완료 기준은 카드 실행 문서 「실행 계획」의 42편 공통 블록이고, 명령 전문은 그 문서
「검증」 절이다.** 여기에는 편마다 다른 것만 적는다.

### WP1 · `S8` bridgesInGraph (graph · 뷰 graph+keyValue)

목록 `:66` 을 `S7` articulationPoints(배치6)와 공유한다

**완료 기준**: `src/algorithms/graph/bridgesInGraph/bridgesInGraph-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `build-html` exit 0 · `bun test src/algorithms/graph/bridgesInGraph/bridgesInGraph-guide.test.ts` 통과

### WP2 · `S12` minCostMaxFlow (graph-flow · 뷰 graph+keyValue)

**완료 기준**: `src/algorithms/graph-flow/minCostMaxFlow/minCostMaxFlow-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `build-html` exit 0 · `bun test src/algorithms/graph-flow/minCostMaxFlow/minCostMaxFlow-guide.test.ts` 통과

### WP3 · `S17` treeIsomorphism (tree · 뷰 keyValue+tree)

**완료 기준**: `src/algorithms/tree/treeIsomorphism/treeIsomorphism-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `build-html` exit 0 · `bun test src/algorithms/tree/treeIsomorphism/treeIsomorphism-guide.test.ts` 통과

### WP4 · `S22` longestPalindrome (string · 뷰 array+keyValue)

**완료 기준**: `src/algorithms/string/longestPalindrome/longestPalindrome-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `build-html` exit 0 · `bun test src/algorithms/string/longestPalindrome/longestPalindrome-guide.test.ts` 통과

## 2. 의존과 순서

넷 다 카테고리가 다르다(`graph` · `graph-flow` · `tree` · `string`). **네 카테고리가 이 배치로
전부 닫힌다** — `graph` 4/4 · `graph-flow` 4/4 · `tree` 5/5 · `string` 5/5.

`S8` bridgesInGraph 이 목록 `:66` 의 뒤쪽이다 — `S7`(배치6)이 이미 지났다.

### 공유 파일 넷은 워커가 안 건드린다

`문제_가이드_목록.md` · `tools/_baseline/guide-rhythm.tsv` ·
`sandbox/algo-guide-v2/FEEDBACK.md` §5 · `KANBAN.md` 넷은 배치 전체가 공유한다.
**워커는 자기 편 디렉터리만 쓴다.** 넷은 배치를 닫을 때 한 자리에서 일괄 반영하고,
그 일괄 반영도 전진 한 단위라 커밋한다. `tools/ord004-manifest.json` 은 `KAN-034.8` 몫이다.

**워커는 `git add` 도 하지 않는다** — 한 워크트리의 인덱스를 공유해서 스테이징이 남의
커밋에 딸려 들어간다(W1 배치6 에서 실제로 났다).

**목록 링크 교체는 언제나 `src/algorithms/` 접두사로 좁힌다** — `:81`·`:82` 는 같은 줄에
자료구조 트랙 링크를 들고 있어서 이름으로 치환하면 그 트랙까지 `.md` 가 된다.

## 3. 리스크

- **골격 결함이 있으면 이 배치의 편들이 함께 틀린다.** 배치를 닫기 전에 `bun run tools/ci.ts all` 이 초록이어야 한다.
- **`tools/check-guide-rhythm.ts` 를 v2 편에 부르지 않는다** — 구 헤딩 접두와 어긋나 조용히 공전하고 통과 표시가 거짓이 된다.
- **스캐너 넷이 0 이어도 `FEEDBACK.md` §3 대조를 밟는다** — W2 배치6~11 의 대조 건수가 65 · 94 · 116 · 166 · 79 · 63 이고 줄어드는 추세가 없다.
- **최악 입력이라고 손으로 적은 수의 성질을 실행으로 확인한다** — W2 에서 `isPrimeTrial` 의 999,999,000,001 이 소수였다.
- **경쟁 설계를 배정밀도로 재지 않는다** — `alt.ts` 의 `measure()` 가 매 실행마다 정본과 답을 대조한다(W2 `digitDp` 실패에서 굳은 규칙).
- 되돌리기는 편 단위다 — 편 하나가 커밋 하나이고 태그 하나(`kan/KAN-034.7-QMZ3RE/S<n>`)를 받는다.

## 4. 착수 시점 판단

**넷 다 이 배치에서 한다 — 미룰 work 없음**(2026-09-07 착수 실측).

`bun run tools/algo-wbs.ts` 의 W3 claim 후보에 넷이 전부 들어 있고, **막힌 편이 0 이 됐다.**
남은 10편이 전부 서로 다른 카테고리라 이제 **후보 10건이 곧 남은 편 전부**다 — 배치9(4) ·
배치10(4) · 배치11(2) 이 그 10편을 나눠 가진다. 2항이 「배치9 부터 카테고리 잔량이 폭을
내린다」고 예고한 자리인데, **이 배치까지는 아직 세션 예산이 상한**이고 배치10 부터 후보가
6 · 2 로 줄어든다.

착수 직전 `bun run tools/ci.ts all` 이 **exit 0** 이다. 파이프 없이 종료코드를 봤다.

### 문서에 없던 상호 링크가 하나 나왔다 — `S8` 커밋에 함께 넣는다

`bun run tools/check-links.ts refs` 가 `bridgesInGraph` 에 **`.md` 상호 링크 두 자리**를 냈다.

```
src/algorithms/graph/articulationPoints/articulationPoints-guide.md:135
src/algorithms/graph/articulationPoints/articulationPoints-guide.md:1094
```

**배치6 에서 닫은 편이 이 편의 `.mdx` 를 가리키고 있다.** 그때는 상대가 아직 `.mdx` 였으므로
맞는 링크였고, 이 배치에서 그 파일이 사라지면서 끊긴다. **카드 실행 계획의 「상호 링크 세
쌍」에도 이 배치 문서 2항에도 없던 자리다** — 배치5 가 `pollardRho` 에서 만난 것과 같은
부류이고, 그때와 같은 이유로 **`grep` 이 아니라 `refs` 가 잡았다.**

**그 두 줄은 `S8` 커밋에 함께 넣는다** — 되돌리기 단위가 편이라 `.mdx` 삭제와 링크 교체가
같이 가야 한다(카드 「검증」 절의 규약 그대로).

| 편 | 인바운드 | 누구 몫인가 |
| --- | --- | --- |
| `bridgesInGraph` | `manifest:855` · `목록:66` · **`articulationPoints-guide.md:135`·`:1094`** | 앞 둘은 `KAN-034.8`·배치 종료 · **뒤 둘은 `S8` 커밋** |
| `minCostMaxFlow` | `manifest:785` · `목록:69` | 앞은 `KAN-034.8` · 뒤는 배치 종료 일괄 반영 |
| `treeIsomorphism` | `manifest:1487` · `목록:77` | 〃 |
| `longestPalindrome` | `manifest:1361` · `목록:84` | 〃 |

`목록:66` 은 `S7` articulationPoints(배치6)가 이미 지난 줄이고 **이 편이 그 줄의 마지막
알고리즘 링크**다. `:77` 은 링크 뒤에 「(변형)」이 붙어 있으니 경로만 바꾼다.

### 자료구조 트랙으로 나가는 링크 둘은 그대로 둔다

`bridgesInGraph` → `graphAdjList-guide.mdx` · `minCostMaxFlow` → `minHeap-guide.mdx`.
**대상 둘 다 실재한다.** ORD-006 이 아직 그 트랙을 안 옮겼으므로 새 원고도 `.mdx` 를 그대로
가리킨다 — 배치8 `bentleyOttmann` 과 같은 처리다.

### 네 카테고리가 이 배치로 닫힌다

`graph` 4/4 · `graph-flow` 4/4 · `tree` 5/5 · `string` 5/5. **닫히는 카테고리의 마지막 편은
그 카테고리의 용어를 확정하는 자리**이므로, 워커에게 같은 카테고리의 선행 편들과 용어를
맞추게 하되 **문장은 옮겨 오지 않게** 한다.

### 이 배치의 중복 위험이 W3 에서 가장 크다

`S8` bridgesInGraph 와 `S7` articulationPoints(배치6)는 **같은 카테고리·같은 뷰·같은 DFS
저수 기법**이고, 그 배치6 편이 §3 대조 121건 중 **91건이 형제 편과의 문장 중복**이었다.
이번에는 그 형제가 바로 이 편이다. `S12` minCostMaxFlow 도 `maxFlow`·`minCut`·
`maxBipartiteMatching` 셋과 붙어 있고, `S17` 은 `subtreeSumQuery`·`treeRerooting` 과,
`S22` 는 `suffixArray`·`kasaiLcp` 와 뷰까지 같다. **네 편 전부 지시에 겹침 대조를 넣는다.**

배치8 이 낸 새 얼굴 — **겹침이 값 오염으로도 나타난다**(모범 편의 수치가 그대로 딸려 왔다) —
도 함께 싣는다.

### 배치8 이 인계한 것

**판정은 `bun run tools/check-v2.ts --all` 로 한다**(단일 파일 경로가 `P16` 을 안 돈다).
배치8 워커 넷이 전부 그렇게 했고 문제가 없었다.

배치6·7·8 이 낸 스캐너 구멍 열넷을 워커 지시에 그대로 싣는다. 이 배치에서 특히 걸리기 쉬운 것:

- **`P15` 의 손 펜스 사각** — 세 배치 연속 나왔고 워커 넷이 각자 검사기를 새로 짰다.
  **그래프 그림이 많은 `S8`·`S12` 에서 가장 잘 걸린다.**
- **은유 활용형 누락 넷** — 비용을 값으로 말하는 그 형용사 · 우열의 피동 동사 ·
  「돌아가다」 · 「서는데」. 넷 다 스캐너가 초록을 낸다.
- **`josa()` 가 값에 붙어 나오는 것** · **생성 블록 셀 문자열 안의 2칸 공백**.

### 사용량 한도 대비 — 폭 2 를 유지하되 재개 절차를 쓴다

배치8 에서 폭 2 로도 워커 둘이 한도에 걸렸다. **폭이 아니라 그 시각의 잔여 한도가 정한다.**
죽으면 새로 띄우지 않고 **같은 워커에 이어 붙이되 중단 시점의 실측 상태**(어느 파일이 있고
판정이 몇이고 플레이스홀더가 몇 자리인지)를 함께 넘긴다 — 배치8 이 그렇게 두 편을 한 번에
끝냈다.

### 끝난 뒤 — 착수 판단이 맞았는가
<!-- 배치를 닫을 때 채운다. -->
