---
card: KAN-034.6-CF6ZHE
batch: 3
created: 2026-08-30
branch: KAN-034-KSD7XR
status: 계획
steps: S3, S13, S6, S18
---

# KAN-034.6-CF6ZHE 배치3 — `bit-manipulation` 첫 편과 `shortest-path` 마감

카드: [KAN-034.6-CF6ZHE.md](../KANBAN.cards/KAN-034.6-CF6ZHE.md) · 범위 `S3` · `S13` · `S6` · `S18`
선행: [배치2](KAN-034.6-CF6ZHE.batch2.md)

> **이 문서는 착수 전 계획이다.** 수행 내역은 카드 실행 문서의 「수행 내역」에 있다.

## 1. 작업 패키지

**공통 완료 기준은 카드 실행 문서 「실행 계획」의 41편 공통 블록이고, 명령 전문은 그 문서
「검증」 절이다.** 여기에는 편마다 다른 것만 적는다.

### WP1 · `S3` dagShortestPath (shortest-path · 뷰 graph+keyValue)

이 편으로 `shortest-path` 가 마감된다

**완료 기준**: `src/algorithms/shortest-path/dagShortestPath/dagShortestPath-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `build-html` exit 0 · `bun test src/algorithms/shortest-path/dagShortestPath/dagShortestPath-guide.test.ts` 통과

### WP2 · `S13` radixTree (string · 뷰 keyValue+tree)

`trie`(`S12`, 배치1 — 앞 배치)와 상호 링크가 잦은 짝이다. `check-links.ts refs` 가 내는 자리를 그 편 커밋에 함께 넣는다

**완료 기준**: `src/algorithms/string/radixTree/radixTree-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `build-html` exit 0 · `bun test src/algorithms/string/radixTree/radixTree-guide.test.ts` 통과

### WP3 · `S6` bestTimeToBuyAndSellStockK (array · 뷰 array+keyValue)


**완료 기준**: `src/algorithms/array/bestTimeToBuyAndSellStockK/bestTimeToBuyAndSellStockK-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `build-html` exit 0 · `bun test src/algorithms/array/bestTimeToBuyAndSellStockK/bestTimeToBuyAndSellStockK-guide.test.ts` 통과

### WP4 · `S18` enumerateSubmasks (bit-manipulation · 뷰 keyValue)

**`bit-manipulation` 카테고리 첫 편이다** — 이 편이 서야 같은 카테고리의 후속이 열린다

`tools/_baseline/guide-rhythm.tsv` 에 이 편의 행이 **없다** — 지울 행이 없는 것이 정상이다

**완료 기준**: `src/algorithms/bit-manipulation/enumerateSubmasks/enumerateSubmasks-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `build-html` exit 0 · `bun test src/algorithms/bit-manipulation/enumerateSubmasks/enumerateSubmasks-guide.test.ts` 통과

## 2. 의존과 순서

넷 다 카테고리가 다르다(`shortest-path` · `string` · `array` · `bit-manipulation`) — 도구가 카테고리마다 한 편만 후보로 내므로
이것이 병렬 폭의 상한이다. 배치 안 상호 대기 없음.

### 공유 파일 넷은 워커가 안 건드린다

`문제_가이드_목록.md` · `tools/_baseline/guide-rhythm.tsv` ·
`sandbox/algo-guide-v2/FEEDBACK.md` §5 · `KANBAN.md` 넷은 배치 전체가 공유한다.
**워커는 자기 편 디렉터리만 쓴다.** 넷은 배치를 닫을 때 한 자리에서 일괄 반영하고,
그 일괄 반영도 전진 한 단위라 커밋한다. `tools/ord004-manifest.json` 은 `KAN-034.8` 몫이다.

**워커는 `git add` 도 하지 않는다** — 한 워크트리의 인덱스를 공유해서 스테이징이 남의
커밋에 딸려 들어간다(W1 배치6 에서 실제로 났다).

## 3. 리스크

- **골격 결함이 있으면 이 배치의 편들이 함께 틀린다.** 배치를 닫기 전에 `bun run tools/ci.ts all` 이 초록이어야 한다.
- **`tools/check-guide-rhythm.ts` 를 v2 편에 부르지 않는다** — 구 헤딩 접두와 어긋나 조용히 공전하고 통과 표시가 거짓이 된다.
- **스캐너 넷이 0 이어도 `FEEDBACK.md` §3 대조를 밟는다** — W1 배치6 에서 네 편 전부 그 뒤에 결함이 나왔다(3 · 3 · 7건).
- 되돌리기는 편 단위다 — 편 하나가 커밋 하나이고 태그 하나를 받는다.

## 4. 착수 시점 판단

착수함(2026-08-31). 넷을 그대로 이 배치에서 돈다 — 다음 배치로 미루는 work 는 없다.

근거 넷(전부 착수 직전 실측).

- **골격이 성해 있다** — `bun run tools/ci.ts all` 이 `all 모드 통과 — 단계 15개` 로 exit 0.
  배치2 종료 상태 그대로다.
- **넷 다 지금 잡을 수 있다** — `bun run tools/algo-wbs.ts` 가 낸 W2 claim 후보 11건 안에
  `dagShortestPath`(shortest-path) · `radixTree`(string) ·
  `bestTimeToBuyAndSellStockK`(array) · `enumerateSubmasks`(bit-manipulation) 이 모두 있다.
  카테고리가 넷 다 달라 도구 판정으로도 동시에 선다. 전개는 `[W2] 8/41` 에서 출발한다.
- **뷰 조합 첫 편이 하나도 없다** — 후보 줄에 `[단독]` 표시가 넷 다 안 붙었다. 넷의 조합은
  전부 선례가 있다: `graph+keyValue` 는 `S2` topologicalSort, `keyValue+tree` 는 `S12` trie,
  `array+keyValue` 는 W1 의 `bestTimeToBuyAndSellStock`, `keyValue` 단독은 `S35` fastPower.
  `S18` 은 **카테고리** 첫 편이지만(`bit-manipulation`) 이 배치에 같은 카테고리의 후속이 없다.
- **인바운드에 `.mdx` 상호 링크가 없다** — `bun run tools/check-links.ts refs` 를 넷에 돌린
  결과가 편마다 두 자리뿐이고 둘 다 공유 파일이다(`tools/ord004-manifest.json` 은
  `KAN-034.8` 몫, `문제_가이드_목록.md` 는 배치 종료 일괄 반영 몫). **WP2 가 예상한
  `trie`↔`radixTree` 상호 링크는 실제로 없다** — `trie-guide.md` 가 `radixTree-guide.mdx` 를
  가리키지 않는다. 그러므로 편 커밋에 함께 넣을 링크 교체가 이 배치에는 없다.
