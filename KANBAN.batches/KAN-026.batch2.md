---
card: KAN-026
batch: 2
created: 2026-09-14
branch: KAN-026
status: 계획
steps: S2, S3
---

# KAN-026 배치2 — trie 계약 정본과 radixTree

카드: [KAN-026.md](../KANBAN.cards/KAN-026.md) · 범위 `S2` · `S3`
선행: [배치1](KAN-026.batch1.md)

> **이 문서는 착수 전 계획이다.** 수행 내역은 카드 실행 문서의 「수행 내역」에 있다.
> 수행 방식: 단일 에이전트(2026-09-14 유저 결재). 착수 전 `python3 <skill>/scripts/kanban.py resume . --card KAN-026` 로 다음 work 를 확인한다.

## 1. 작업 패키지

### WP1 · `S2` `trie/trie` 계약 전환

걸린 것: `ternarySearchTree` 와 같은 계약(B15 확정, `docs/ORD-006-conventions.md:1776-1801`). 정본 자리는 `S1` 판정을 따른다.

**완료 기준**: 공통 완료 기준은 카드 문서 「실행 계획 · 단계별 완료 기준」의 구조 하나 항목이다 — 헤더 여섯 항목 · `.contract.ts` + `.test.ts` + `_reference/<name>.ts`(`__cost` · `#region guide:core`) · vector 등록(알파벳 순) · `check-links refs` 뒤 `-problem.md` 제거 · `bun run tools/ci.ts all` · `check-contract` · `tsc` · biome 경고 0 · 종료 산출(불변 사실은 카드 대역).

### WP2 · `S3` `trie/radixTree` 계약 전환

걸린 것: `S1` 이 `trie` 와 같은 계약으로 판정했으면 성격 전환(`{...정본Contract, name}`).

**완료 기준**: 공통 완료 기준은 카드 문서 「실행 계획 · 단계별 완료 기준」의 구조 하나 항목이다 — 헤더 여섯 항목 · `.contract.ts` + `.test.ts` + `_reference/<name>.ts`(`__cost` · `#region guide:core`) · vector 등록(알파벳 순) · `check-links refs` 뒤 `-problem.md` 제거 · `bun run tools/ci.ts all` · `check-contract` · `tsc` · biome 경고 0 · 종료 산출(불변 사실은 카드 대역).

## 2. 의존과 순서

- **결재 반영**: 정본은 `ternarySearchTree`(결재 2026-09-15) — `S2`·`S3` 은 둘 다 성격 전환이고 순서가 없다. 아래 WP 의 「정본 자리는 `S1` 판정을 따른다」는 이 결재로 닫혔다.
- `S3` 은 `S2` 의 계약이 선 뒤에만 — 성격 전환이면 정본 계약 객체를 그대로 쓰기 때문이다.
- 불변 사실 번호는 이 카드 대역(200–229)만 쓴다 — `KAN-027` 가 다른 워크트리에서 같은 런북을 고친다.

## 3. 리스크

- `radixTree` 의 존재 이유가 공간(압축)이면 계약이 담지 못한다 — `S1` 처분을 그대로 따르고 새로 판정하지 않는다.
- 세션 절반을 넘길 기미가 보이면 이 배치의 마지막 work 를 다음 배치 앞으로 미루고 「4. 착수 시점 판단」에 적는다.

## 4. 착수 시점 판단
<!-- 착수할 때 채운다 — 마지막 work 를 다음 배치로 미룰지 여기서 정한다. -->

**배치6(`S9` `S10`)이 이 배치보다 먼저 돌았다(2026-09-15).** 이 배치는 착수 시점에 정본 위치 결정을
기다렸고, 배치6 의 그래프 둘은 그 결정에 걸리지 않았기 때문이다(`KAN-026.batch6.md` 「4」). 결정이
2026-09-15 에 내려져(정본은 `trie/ternarySearchTree`, 임시) 이 배치가 열렸다. 배치6 이 먼저 돈 것이
이 배치에 남긴 것은 공유 파일 처리 방식 셋이다 — 자기시험은 `runContract.test.ts` 가 아니라 구조별
새 파일, `tools/guide-skeleton.test.ts` `SKELETON_EXEMPT` 에 구조 경로, `VERIFICATION_GRADES` 새 줄은
자기보다 뒤로 정렬되는 첫 기존 키 앞. 이 배치도 그대로 따른다.

- **마지막 work(`S3`)를 미루지 않는다.** 두 전환이 같은 정본 계약을 쓰고 공유 문서 산출(불변 사실 ·
  conventions 절 · TA-01 note)이 한 자리에 모인다. 성격 전환은 계약을 새로 적지 않으므로 배치 하나에
  둘이 묶인다(`docs/ORD-006-conventions.md:1969-1971`).
- `S3` 의 필수 실측(압축 에지를 문자열 복사로 쪼개면 `O(m)` 을 어기는가)이 정본 스위트 시나리오에
  안 걸릴 수 있다 — 스위트의 낱말 길이가 8 로 고정이라 꼬리 길이도 8 이하다. 걸리지 않으면 정본
  스위트를 고치지 않고 필요한 시나리오를 보고한다(정본 쪽 파일은 이 배치에서 고치지 않는다).
