---
card: KAN-027
batch: 6
created: 2026-09-14
branch: KAN-027
status: 계획
steps: S7, S8
---

# KAN-027 배치6 — 되돌리기 분리집합과 지연 전파 구간 트리

카드: [KAN-027.md](../KANBAN.cards/KAN-027.md) · 범위 `S7` · `S8`
선행: [배치5](KAN-027.batch5.md)

> **이 문서는 착수 전 계획이다.** 수행 내역은 카드 실행 문서의 「수행 내역」에 있다.
> 수행 방식: 단일 에이전트(2026-09-14 유저 결재). 착수 전 `python3 <skill>/scripts/kanban.py resume . --card KAN-027` 로 다음 work 를 확인한다.

## 1. 작업 패키지

### WP1 · `S7` `disjoint-set/disjointSetRollback` 계약 전환

걸린 것: 되돌리기가 경로 압축을 막아 랭크만 남아 O(log n). `unionFind` 와 「다르다」(B19, `conventions.md:2239-2242`).

**완료 기준**: 공통 완료 기준은 카드 문서 「실행 계획 · 단계별 완료 기준」의 구조 하나 항목이다 — 헤더 여섯 항목 · `.contract.ts` + `.test.ts` + `_reference/<name>.ts`(`__cost` · `#region guide:core`) · vector 등록(알파벳 순) · `check-links refs` 뒤 `-problem.md` 제거 · `bun run tools/ci.ts all` · `check-contract` · `tsc` · biome 경고 0 · 종료 산출(불변 사실은 카드 대역).

### WP2 · `S8` `range-query/segmentTreeLazy` 계약 전환

걸린 것: JSDoc 없는 스텁. 구간 갱신이 더해져 `segmentTree` 와 「다르다」(B19).

**완료 기준**: 공통 완료 기준은 카드 문서 「실행 계획 · 단계별 완료 기준」의 구조 하나 항목이다 — 헤더 여섯 항목 · `.contract.ts` + `.test.ts` + `_reference/<name>.ts`(`__cost` · `#region guide:core`) · vector 등록(알파벳 순) · `check-links refs` 뒤 `-problem.md` 제거 · `bun run tools/ci.ts all` · `check-contract` · `tsc` · biome 경고 0 · 종료 산출(불변 사실은 카드 대역).

## 2. 의존과 순서

- `S7` 은 `S6` 뒤(같은 트랙). `S8` 은 독립.
- 불변 사실 번호는 이 카드 대역(T2 120–139 · T3 140–159 · T4 160–179 · T5 90–99→230–249)만 쓴다 — `KAN-026` 가 다른 워크트리에서 같은 런북을 고친다.

## 3. 리스크

- `segmentTree` 정본(완료)의 fixture 를 공유하고 싶어진다 — 읽기만 하고 필요하면 새 파일.
- 세션 절반을 넘길 기미가 보이면 이 배치의 마지막 work 를 다음 배치 앞으로 미루고 「4. 착수 시점 판단」에 적는다.

## 4. 착수 시점 판단
<!-- 착수할 때 채운다 — 마지막 work 를 다음 배치로 미룰지 여기서 정한다. -->

- **2026-09-15 착수 · 미루지 않는다 — `S7` `S8` 둘 다 이 배치에서 닫는다.** 착수 시점 `git status` 깨끗, `resume` 의 `next_work` 가
  `S7`. `S7` 도중 API 세션 한도로 한 번 끊겼고(claim 만료) 같은 세션이 `doc-step --steal` 로 다시 잡아 이어 했다. 두 유닛은 공유
  fixture 가 없어 독립이고, `S7` 을 닫은 뒤 세션 예산이 절반에 못 미쳐 `S8` 을 이어 잡았다.
- **번호 대역** — T3 추가 대역 310–319 중 남은 317–319 를 `S7` 이 다 쓰고 모자라 **T5 몫 330–349 의 끝 열 340–349** 에서 340–342 를
  더 썼다(`docs/ORD-006-wbs.md` §2 · `tools/ord006-wbs.ts` T3 facts · 런북 「T3 추가 대역 둘째」). `S8` 은 T4 대역 160–179 의 남은
  174–179 를 다 썼고 320–329 는 열지 않았다(wbs §2 T4 행에 소진만 적었다).
- **착수 중 바뀐 판단 넷** — ① `disjointSetRollback` 의 스냅숏·되살리기·집합 수를 계약에서 뺐다(불변 사실 319 — 결재 대상) ② 되돌리는
  단위를 합치기 「호출」로 정했다 ③ `segmentTreeLazy` 의 갱신 주입에 덮는 자리 수 인자와 합성을 넣었다(불변 사실 174·175) ④ `worst`
  시나리오의 걸음 수를 32 로 고정했다(불변 사실 178 — 자기시험 시간). scope 밖 파일은 고치지 않았다.
