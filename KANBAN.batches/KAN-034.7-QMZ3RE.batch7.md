---
card: KAN-034.7-QMZ3RE
batch: 7
created: 2026-09-04
branch: KAN-034-KSD7XR
status: 계획
steps: S11, S16, S21, S26
---

# KAN-034.7-QMZ3RE 배치7 — 꼬리 진입 — 카테고리마다 마지막에서 둘째

카드: [KAN-034.7-QMZ3RE.md](../KANBAN.cards/KAN-034.7-QMZ3RE.md) · 범위 `S11` · `S16` · `S21` · `S26`
선행: [배치6](KAN-034.7-QMZ3RE.batch6.md)

> **이 문서는 착수 전 계획이다.** 수행 내역은 카드 실행 문서의 「수행 내역」에 있다.

## 1. 작업 패키지

**공통 완료 기준은 카드 실행 문서 「실행 계획」의 42편 공통 블록이고, 명령 전문은 그 문서
「검증」 절이다.** 여기에는 편마다 다른 것만 적는다.

### WP1 · `S11` maxBipartiteMatching (graph-flow · 뷰 graph+keyValue)

**완료 기준**: `src/algorithms/graph-flow/maxBipartiteMatching/maxBipartiteMatching-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `build-html` exit 0 · `bun test src/algorithms/graph-flow/maxBipartiteMatching/maxBipartiteMatching-guide.test.ts` 통과

### WP2 · `S16` subtreeSumQuery (tree · 뷰 keyValue+tree)

**완료 기준**: `src/algorithms/tree/subtreeSumQuery/subtreeSumQuery-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `build-html` exit 0 · `bun test src/algorithms/tree/subtreeSumQuery/subtreeSumQuery-guide.test.ts` 통과

### WP3 · `S21` suffixAutomaton (string · 뷰 keyValue+tree)

**완료 기준**: `src/algorithms/string/suffixAutomaton/suffixAutomaton-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `build-html` exit 0 · `bun test src/algorithms/string/suffixAutomaton/suffixAutomaton-guide.test.ts` 통과

### WP4 · `S26` fftMultiply (number-theory · 뷰 keyValue)

**완료 기준**: `src/algorithms/number-theory/fftMultiply/fftMultiply-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `build-html` exit 0 · `bun test src/algorithms/number-theory/fftMultiply/fftMultiply-guide.test.ts` 통과

## 2. 의존과 순서

넷 다 카테고리가 다르다(`graph-flow` · `tree` · `string` · `number-theory`). 배치 안 상호 대기 없음.

여기서부터 카테고리 잔량이 폭을 결정하기 시작한다 — 배치9 부터는 한 카테고리에 한 편씩만
남는다.

### 공유 파일 넷은 워커가 안 건드린다

`문제_가이드_목록.md` · `tools/_baseline/guide-rhythm.tsv` ·
`sandbox/algo-guide-v2/FEEDBACK.md` §5 · `KANBAN.md` 넷은 배치 전체가 공유한다.
**워커는 자기 편 디렉터리만 쓴다.** 넷은 배치를 닫을 때 한 자리에서 일괄 반영하고,
그 일괄 반영도 전진 한 단위라 커밋한다. `tools/ord004-manifest.json` 은 `KAN-034.8` 몫이다.

**워커는 `git add` 도 하지 않는다** — 한 워크트리의 인덱스를 공유해서 스테이징이 남의
커밋에 딸려 들어간다(W1 배치6 에서 실제로 났다).

**목록 링크 교체는 언제나 `src/algorithms/` 접두사로 좁힌다** — `:81`·`:82` 는 같은 줄에
자료구조 트랙 링크를 들고 있어서 이름으로 치환하면 그 트랙까지 `.md` 가 된다.

## 3. 리스크

- **골격 결함이 있으면 이 배치의 편들이 함께 틀린다.** 배치를 닫기 전에 `bun run tools/ci.ts all` 이 초록이어야 한다.
- **`tools/check-guide-rhythm.ts` 를 v2 편에 부르지 않는다** — 구 헤딩 접두와 어긋나 조용히 공전하고 통과 표시가 거짓이 된다.
- **스캐너 넷이 0 이어도 `FEEDBACK.md` §3 대조를 밟는다** — W2 배치6~11 의 대조 건수가 65 · 94 · 116 · 166 · 79 · 63 이고 줄어드는 추세가 없다.
- **최악 입력이라고 손으로 적은 수의 성질을 실행으로 확인한다** — W2 에서 `isPrimeTrial` 의 999,999,000,001 이 소수였다.
- **경쟁 설계를 배정밀도로 재지 않는다** — `alt.ts` 의 `measure()` 가 매 실행마다 정본과 답을 대조한다(W2 `digitDp` 실패에서 굳은 규칙).
- 되돌리기는 편 단위다 — 편 하나가 커밋 하나이고 태그 하나(`kan/KAN-034.7-QMZ3RE/S<n>`)를 받는다.

## 4. 착수 시점 판단
<!-- 착수할 때 채운다 — 마지막 work 를 다음 배치로 미룰지 여기서 정한다. -->
