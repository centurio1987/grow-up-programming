---
card: KAN-034.7-QMZ3RE
batch: 3
created: 2026-09-04
branch: KAN-034-KSD7XR
status: 계획
steps: S30, S14, S19, S24
---

# KAN-034.7-QMZ3RE 배치3 — `tree`·`string`·`number-theory` 두 번째 편

카드: [KAN-034.7-QMZ3RE.md](../KANBAN.cards/KAN-034.7-QMZ3RE.md) · 범위 `S30` · `S14` · `S19` · `S24`
선행: [배치2](KAN-034.7-QMZ3RE.batch2.md)

> **이 문서는 착수 전 계획이다.** 수행 내역은 카드 실행 문서의 「수행 내역」에 있다.

## 1. 작업 패키지

**공통 완료 기준은 카드 실행 문서 「실행 계획」의 42편 공통 블록이고, 명령 전문은 그 문서
「검증」 절이다.** 여기에는 편마다 다른 것만 적는다.

### WP1 · `S30` pointInPolygon (geometry · 뷰 keyValue)

목록 `:106` 을 `S29`(배치2)·`S31`(배치4)과 셋이 공유한다

**완료 기준**: `src/algorithms/geometry/pointInPolygon/pointInPolygon-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `build-html` exit 0 · `bun test src/algorithms/geometry/pointInPolygon/pointInPolygon-guide.test.ts` 통과

### WP2 · `S14` heavyLightDecomposition (tree · 뷰 keyValue+tree)

**완료 기준**: `src/algorithms/tree/heavyLightDecomposition/heavyLightDecomposition-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `build-html` exit 0 · `bun test src/algorithms/tree/heavyLightDecomposition/heavyLightDecomposition-guide.test.ts` 통과

### WP3 · `S19` suffixArray (string · 뷰 array+keyValue)

목록 `:82` 이 **자료구조 트랙 링크를 같은 줄에 들고 있다**(`src/data-structures/trie/suffixArray/…`) — `src/algorithms/` 접두사로 좁혀서 교체한다. 같은 줄을 `S20` kasaiLcp(배치5)와 공유한다

**완료 기준**: `src/algorithms/string/suffixArray/suffixArray-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `build-html` exit 0 · `bun test src/algorithms/string/suffixArray/suffixArray-guide.test.ts` 통과

### WP4 · `S24` millerRabin (number-theory · 뷰 keyValue)

목록 `:101` 을 `S25` pollardRho(배치5)와 공유한다

**완료 기준**: `src/algorithms/number-theory/millerRabin/millerRabin-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `build-html` exit 0 · `bun test src/algorithms/number-theory/millerRabin/millerRabin-guide.test.ts` 통과

## 2. 의존과 순서

넷 다 카테고리가 다르다(`geometry` · `tree` · `string` · `number-theory`). 배치 안 상호 대기 없음.

목록 공유 줄 셋을 지난다 — `:106`(`S30`) · `:82`(`S19`) · `:101`(`S24`). 셋 다 짝이 다른
배치라 **되돌릴 때는 나중 배치부터 역순**이다.

### 공유 파일 넷은 워커가 안 건드린다

`문제_가이드_목록.md` · `tools/_baseline/guide-rhythm.tsv` ·
`sandbox/algo-guide-v2/FEEDBACK.md` §5 · `KANBAN.md` 넷은 배치 전체가 공유한다.
**워커는 자기 편 디렉터리만 쓴다.** 넷은 배치를 닫을 때 한 자리에서 일괄 반영하고,
그 일괄 반영도 전진 한 단위라 커밋한다. `tools/ord004-manifest.json` 은 `KAN-034.8` 몫이다.

**워커는 `git add` 도 하지 않는다** — 한 워크트리의 인덱스를 공유해서 스테이징이 남의
커밋에 딸려 들어간다(W1 배치6 에서 실제로 났다).

**목록 링크 교체는 언제나 `src/algorithms/` 접두사로 좁힌다** — `:81`·`:82` 는 같은 줄에
자료구조 트랙 링크를 들고 있어서 이름으로 치환하면 그 트랙까지 `.md` 가 된다.

## 3. 리스크

- **골격 결함이 있으면 이 배치의 편들이 함께 틀린다.** 배치를 닫기 전에 `bun run tools/ci.ts all` 이 초록이어야 한다.
- **`tools/check-guide-rhythm.ts` 를 v2 편에 부르지 않는다** — 구 헤딩 접두와 어긋나 조용히 공전하고 통과 표시가 거짓이 된다.
- **스캐너 넷이 0 이어도 `FEEDBACK.md` §3 대조를 밟는다** — W2 배치6~11 의 대조 건수가 65 · 94 · 116 · 166 · 79 · 63 이고 줄어드는 추세가 없다.
- **최악 입력이라고 손으로 적은 수의 성질을 실행으로 확인한다** — W2 에서 `isPrimeTrial` 의 999,999,000,001 이 소수였다.
- **경쟁 설계를 배정밀도로 재지 않는다** — `alt.ts` 의 `measure()` 가 매 실행마다 정본과 답을 대조한다(W2 `digitDp` 실패에서 굳은 규칙).
- 되돌리기는 편 단위다 — 편 하나가 커밋 하나이고 태그 하나(`kan/KAN-034.7-QMZ3RE/S<n>`)를 받는다.

## 4. 착수 시점 판단

착수함(2026-09-04). **도구가 낸 후보 11건에 이 배치의 넷이 다 들어 있다.**

```
W3 claim 후보 11건 (카테고리별 한 편)   ← 배치2 종료 직후
  shortest-path    spfa                           graph+keyValue
  graph            stronglyConnectedComponents    graph+keyValue
  graph-flow       minCut                         graph+keyValue
  tree             heavyLightDecomposition        keyValue+tree      ← 이 배치
  string           suffixArray                    array+keyValue     ← 이 배치
  array            sparseTableRangeMin            array+keyValue
  number-theory    millerRabin                    keyValue           ← 이 배치
  geometry         pointInPolygon                 keyValue           ← 이 배치
  dp               tspBitmask                     keyValue+matrix
  advanced         divideAndConquerDp             keyValue+matrix
  sorting          externalMergeSort              keyValue+priorityQueue
막힌 편 23 — category 23
```

막힌 편이 27 에서 23 으로 내려갔다. 배치1·2 가 닫은 여덟이 각 카테고리의 다음 편을 열었고,
**이 웨이브에 남은 카테고리 배리어는 없다.**

**폭은 4 그대로다.** 배치1·2 가 넷씩 닫는 동안 골격 결함이 안 나왔다 — 되올릴 근거도 내릴
근거도 없다.

**인바운드 실측**(`bun run tools/check-links.ts refs`):

| 편 | 그 편 커밋 몫 | 인덱스(배치 종료 일괄) | 이월 |
| --- | --- | --- | --- |
| `pointInPolygon` | 없음 | `문제_가이드_목록.md:106` | `ord004-manifest.json:673` |
| `heavyLightDecomposition` | 없음 | `:74` | `:1431` |
| `suffixArray` | 없음 | `:82` — **같은 줄에 자료구조 트랙 링크가 있다** | `:1389` |
| `millerRabin` | 없음 | `:101` | `:1066` |

**이 배치는 `.mdx` 상호 링크 딸림 수정이 하나도 없다.** 배치1(한 곳)·배치2(다섯 곳)와
갈리는 자리이고, 42편 전체의 상호 링크 세 쌍 중 둘이 배치2 에서 이미 처분됐기 때문이다.
남은 한 쌍(`S34` rotatingCalipersDiameter → `S28` convexHull)은 배치1 에서 이미 고쳤다.

**목록 공유 줄 셋을 지난다** — `:106`(`S30`, 짝은 `S29` 배치2 완료 · `S31` polygonArea 배치4) ·
`:82`(`S19`, 짝은 `S20` kasaiLcp 배치5) · `:101`(`S24`, 짝은 `S25` pollardRho 배치5).
셋 다 짝이 뒤 배치이거나 이미 끝났으므로 되돌릴 때는 나중 배치부터 역순이다.

**`suffixArray` 는 `guide-rhythm.tsv` 에 행이 둘이다** — `tools/_baseline/guide-rhythm.tsv:32`
(`src/algorithms/string/…`)과 `:103`(`src/data-structures/trie/…`). **지울 것은 `:32` 하나**이고,
이름으로 지우면 자료구조 트랙 부채가 함께 사라져 그쪽 래칫이 눈이 먼다. 목록 교체와 같은
이유·같은 처방이다(경로 접두사로 좁힌다).

**배치2 가 남긴 것 둘을 이 배치가 받는다.** ① `geometry` 세 번째 편인 `S30` 은 `convexHull`
`S28` 이 세우고 `S29` 가 다듬은 `sideOf`(배정밀도 걸러내기 + `BigInt` 되재기)와 용어 삼분을
그대로 가져간다 — 좌표 ±10^9 에서 외적이 4×10^18 이라 배정밀도만으로는 부호가 틀린다.
② `tree` 두 번째 편인 `S14` 는 `S13` lowestCommonAncestor 가 세운 조상 점프 표와 그 편이
겪은 뷰 규약(뿌리 고정)을 딛는다. 전수 대조 대상이 그 편이다.

**착수 시점 실측** — 살아 있는 `.mdx` **34** · `guide-rhythm.tsv` 의 `src/algorithms` 행 **34**.
배치2 에서 `segmentsIntersect`(rhythm 행이 없는 유일한 편)가 닫히면서 두 수의 차이 1 이
사라졌고, 이 배치 뒤로는 **둘이 같은 수로 함께 줄어든다**(닫힐 때 0 · 0).

**마지막 work 를 다음 배치로 미루지 않는다.** 넷 다 카테고리가 다르고 배리어가 없으며
딸림 수정도 0 이다 — 미룰 근거가 없다.
