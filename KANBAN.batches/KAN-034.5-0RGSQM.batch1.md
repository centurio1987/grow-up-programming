---
card: KAN-034.5-0RGSQM
batch: 1
created: 2026-08-29
branch: KAN-034-KSD7XR
status: 계획
steps: S1, S4, S13, S18
---

# KAN-034.5-0RGSQM 배치1 — 선례 4편 — 카테고리 셋과 뷰 둘이 여기서 열린다

카드: [KAN-034.5-0RGSQM.md](../KANBAN.cards/KAN-034.5-0RGSQM.md) · 범위 `S1` · `S4` · `S13` · `S18`
선행: 없음 (이 카드의 첫 배치)

> **이 문서는 착수 전 계획이다.** 수행 내역은 카드 실행 문서의 「수행 내역」에 있다.

## 1. 작업 패키지

**공통 완료 기준은 카드 실행 문서 「실행 계획」의 24편 공통 블록이고, 명령 전문은 그 문서
「검증」 절이다.** 여기에는 편마다 다른 것만 적는다.

### WP1 · `S1` binarySearch (binary-search · 뷰 array)

**`binary-search` 카테고리 첫 편.** 이 편이 닫히기 전에는 `S2`·`S3` 이 claim 되지 않는다. 뷰 `array` 는 파일럿 `quicksort` 가 이미 열었다

**완료 기준**: `src/algorithms/binary-search/binarySearch/binarySearch-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `bun test src/algorithms/binary-search/binarySearch` 통과

### WP2 · `S4` longestSubarrayAtMostSum (array · 뷰 array+keyValue)

**`array+keyValue` 뷰 조합 첫 편.** W1 에서 이 뷰를 쓰는 편이 12개이고, 그중 `S8`(다른 카테고리)까지 이 편을 기다린다. 카테고리 `array` 는 파일럿 `mosAlgorithm` 이 이미 열었다

**완료 기준**: `src/algorithms/array/longestSubarrayAtMostSum/longestSubarrayAtMostSum-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `bun test src/algorithms/array/longestSubarrayAtMostSum` 통과

### WP3 · `S13` bfsShortestPath (graph · 뷰 graph+keyValue)

**`graph` 카테고리 첫 편이자 `graph+keyValue` 뷰 첫 편** — 선례 축 둘이 여기서 겹친다. `S14`~`S17` 이 이 편을 기다린다

**완료 기준**: `src/algorithms/graph/bfsShortestPath/bfsShortestPath-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `bun test src/algorithms/graph/bfsShortestPath` 통과

### WP4 · `S18` nQueens (advanced · 뷰 matrix)

**`advanced` 카테고리 첫 편.** `S24` 가 이 편을 기다린다. 뷰 `matrix` 는 파일럿 `knapsack01` 이 이미 열었다

**완료 기준**: `src/algorithms/advanced/nQueens/nQueens-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `bun test src/algorithms/advanced/nQueens` 통과

## 2. 의존과 순서

**넷이 서로를 안 기다린다.** 카테고리도 뷰도 겹치지 않게 고른 조합이라 동시에 돈다.
`S1`(binary-search) · `S4`(array+keyValue 뷰) · `S13`(graph + graph+keyValue 뷰) ·
`S18`(advanced) 넷이 이 웨이브의 **선례 전부**다.

**배치 밖 의존** — `S8`(`etc`·배치2)이 `S4` 를 기다린다. 카테고리는 다르지만 뷰가 같다.

### 공유 파일 넷은 워커가 안 건드린다

편별 종료 산출 일곱 중 **넷이 배치 전체가 공유하는 파일**이다 —
`문제_가이드_목록.md` · `tools/_baseline/guide-rhythm.tsv` ·
`sandbox/algo-guide-v2/FEEDBACK.md` §5 · `KANBAN.md`.
넷을 워커가 각자 고치면 같은 줄에서 부딪히고, 부딪힌 결과가 조용히 반쯤 반영된다.

**워커는 자기 편 디렉터리만 쓴다.**
넷은 배치를 닫을 때 **한 자리에서 일괄 반영**한다. 그 일괄 반영도 전진 한 단위라 커밋한다.

## 3. 리스크

- **골격 결함이 있으면 이 배치의 4편이 함께 틀린다.** 폭을 도구가 내는 6 이 아니라 4 로
  낮춘 이유가 그것이다. 배치를 닫기 전에 `bun run tools/ci.ts all` 이 초록이어야 한다.
- **`tools/check-guide-rhythm.ts` 를 v2 편에 부르지 않는다.** 구 헤딩 접두와 어긋나 조용히
  공전하고 통과 표시가 거짓이 된다.
- 되돌리기는 편 단위다 — 편 하나가 커밋 하나이고 태그 하나를 받는다. 공유 파일 넷은 줄 단위
  추가·삭제라 편별 되돌리기가 서로를 안 깬다.
- **이 배치가 실패하면 배치2 를 열지 않는다.** 선례 넷이 여기 다 들어 있어서, 여기서
  골격이 틀리면 남은 20편이 같은 결함을 물려받는다. 배치1 은 「4편을 냈다」가 아니라
  「골격이 24편에 쓸 만한가」를 판정하는 자리다.
- 증명 블록(`<!--proof:{id}-->` + `<name>-guide.proof.ts`)이 편마다 새로 붙는 첫 배치다.
  `check-proof.ts` 는 사이드카가 `<name>-guide.ref.ts` 를 import 하는지까지 본다 —
  값을 손으로 적으면 대조가 자기 자신과의 대조가 된다.

## 4. 착수 시점 판단

**착수 2026-08-29.** `bun run tools/algo-wbs.ts` 가 낸 claim 후보 6건 안에 이 배치의 넷이
전부 있다 — `binarySearch` · `longestSubarrayAtMostSum` · `bfsShortestPath` · `nQueens`.
후보에 함께 있는 `sortArray`·`coinChangeWays` 는 선례가 아니라 파일럿이 이미 연 카테고리라
배치2·3 으로 미뤘다. 도구가 내는 폭 6 을 그대로 쓰지 않고 4 로 유지한 판단이 이 자리다.

**미룰 work 가 없다.** 넷이 이 웨이브의 선례 전부이고, 하나를 다음 배치로 넘기면 그
카테고리(또는 뷰)의 후속이 통째로 함께 밀린다 — 배치 경계를 넘기는 비용이 편 하나가 아니다.

**착수 직전 실측** — 네 대상 디렉터리에 `<name>.ts` · `<name>.test.ts` · `<name>-problem.md` ·
`<name>-guide.mdx` 가 다 있고 v2 산출은 하나도 없다. 전역으로는 살아 있는 `.mdx` 107 ·
`guide-rhythm.tsv` 의 `src/algorithms` 행 101 · `algo-wbs` 가 `[W1] 2/26`.
