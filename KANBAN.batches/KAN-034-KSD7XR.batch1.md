---
card: KAN-034-KSD7XR
batch: 1
created: 2026-08-28
branch: KAN-034-KSD7XR
status: 계획
steps: S1
---

# KAN-034-KSD7XR 배치1 — 정합 회복 + 파일럿 4편 재판정

카드: [KAN-034-KSD7XR.md](../KANBAN.cards/KAN-034-KSD7XR.md) · 범위 `S1`
선행: 없음 (이 카드의 첫 배치)

> **이 문서는 착수 전 계획이다.** 수행 내역은 카드 실행 문서의 「수행 내역」에 있다.

**이 배치가 work 하나뿐인 이유는 끝에 유저 승인 게이트가 있기 때문이다.** `S1` 의 산출은
파일럿 4편의 최신 골격 판정이고, 그것이 유저의 편 단위 승인 근거가 된다.
게이트 뒤로 work 를 더 넣으면 승인 전에 인프라를 옮기게 된다.

> **2026-08-29 — 판정 장치가 바뀌었다.** 유저가 외부 모델 없이 가기로 정해 이해 시험을
> 걷어냈다. 이 배치가 돌 때의 판정은 이해 시험이었고 그 결과(4편 통과)는 그대로 남는다.
> 새 장치는 `tools/check-proof.ts` 이고 대응은 `SPEC.md` §0 에 있다.

## 1. 작업 패키지

### WP1 · `S1` 정합 회복 + 파일럿 4편 재판정 (`KAN-034.1-V337W5`)

넷을 순서대로 한다. 앞의 셋은 결정론이고 넷째만 외부 모델을 부른다.

1. **`KAN-033` 잔여물 봉인** — 완료(`edbee27`, 30파일). `git status --porcelain` 0줄 실측.
2. **`FEEDBACK.md` 결번 참조 교정** — `SPEC.md:677` 이 `L23`·`L26`~`L31` 을 결번으로 선언하는데
   `FEEDBACK.md` 가 16줄에서 그것을 쓴다. **교정 대상은 §1 반영표의 규칙 열뿐이다** —
   `:49`·`:87` 은 폐기 사실의 기록이라 고치면 결번 선언의 근거가 사라진다.
3. **은유 부류 표기 통일** — 문서마다 갈린 부류 수를 `check-metaphor.ts` 의 실제 부류 수인
   11 로 맞춘다.
4. **파일럿 4편 재판정** — 당시 장치인 `comprehension.sh` 를 편마다 한 번씩 돌렸다.
   `babyStepGiantStep` 은 첫 실행이었다. 기존 `verdicts/` 31개는 전부 8/20 이고 그 뒤에 파트
   2분할(`SPEC.md:75`)·항목 3종 개편·`invariant` 신설이 들어가서 최신 골격의 근거가 아니었다.
   **다음 편부터는 이 자리가 `check-proof.ts` 다.**
5. **HTML 레일 렌더 확인** — `build-html.ts` 산출을 브라우저로 연다. 사람이 보는 판정이다.

**완료 기준** (카드 「검증」 절의 `S1` 블록 그대로)

```bash
git status --short                                            # 잔여 0
for f in sandbox/algo-guide-v2/pilot/*/*-guide.md; do
  bun run sandbox/algo-guide-v2/tools/check-proof.ts "$f"   # 0 통과 · 1 위반
done
#   ↑ 이 배치가 돌 때는 comprehension.sh 였다. 그 결과는 verdicts/ 에 남아 있다
sed -n '/^## 1\./,/^## 2\./p' sandbox/algo-guide-v2/FEEDBACK.md | grep -cE 'L23|L2[6-9]|L3[01]'    # 0
```

그리고 파일럿 4편의 verdict 가 최신 골격 기준으로 서 있을 것 — `babyStepGiantStep` 포함.

## 2. 의존과 순서

배치 안: 1 → 2·3(서로 독립) → 4 → 5. 4번이 2·3 뒤인 이유는 판정이 원고를 근거로 돌기
때문이다. 원고를 고친 뒤에 판정해야 판정이 최신 원고를 가리킨다.

배치 밖: 이 배치가 닫히고 **유저 승인을 받은 뒤에** 배치2 가 돈다. 승인 없이 `S2` 로 넘어가면
미승인 골격이 본 저장소로 걷힌다 — 그것이 `KAN-033` 카드가 걸어 둔 조건이다
(「pilot 가이드가 완전히 나에게 승인 됐을 때 비로소 모든 가이드에 적용」).

## 3. 리스크

- **판정이 외부 모델 호출이라 예산을 먹었다.** 편당 수 분이 들었고 소진되면 배치가 멈췄다.
  2026-08-29 이후로는 이 리스크가 없다 — 판정이 전부 로컬 실행이다.
- `babyStepGiantStep` 은 verdict 가 0개라 첫 판정에서 떨어질 가능성이 나머지 셋보다 높다.
  카드 규정대로 5회차를 넘기면 그 편을 보류하고 나머지 셋의 승인을 먼저 받는다.
- 되돌리기: 2·3번은 문서 편집이라 revert 로 닫힌다. 4번 verdict 는 append 라 되돌릴 것이 없다.

## 4. 착수 시점 판단

착수함(2026-08-28). WP1 의 1번은 착수 전에 이미 닫혔다 — 워크트리를 만들려면 워킹트리가
깨끗해야 해서 순서가 앞당겨졌다(`edbee27`). 나머지 넷을 이 배치에서 돈다.
