---
card: KAN-034.9-WHH53W
batch: 2
created: 2026-09-10
branch: KAN-034-KSD7XR
status: 완료
steps: S3, S6, S7
---

# KAN-034.9-WHH53W 배치2 — 가장 무거운 셋

카드: [KAN-034.9-WHH53W.md](../KANBAN.cards/KAN-034.9-WHH53W.md) · 범위 `S3` `S6` `S7`
선행: [배치1](./KAN-034.9-WHH53W.batch1.md)

> **이 문서는 착수 전 계획이다.** 수행 내역은 카드 실행 문서의 「수행 내역」에 있다.

**폭 3.** 파일이 셋으로 갈린다 — `check-v2.ts` · `check-proof.ts` · `build-html.ts`.
**`S3` 이 이 카드에서 가장 크고, 이 배치의 무게는 사실상 그것이다.**

## 1. 작업 패키지

### WP1 · `S3` `P15` 를 넓히고 면제를 좁힌다 (`B1`) — **가장 크다**

**일곱 배치 연속으로 샜고 워커 여덟이 각자 검사기를 새로 짰다.** 31건이라고 셌던 것 중
일곱 번이 이 하나다.

지금 규칙이 못 보는 것 둘: **손으로 쓴 `text` 펜스**(`<!--proof:-->` 한정이라 안 본다)와
**2행 덩어리**(`run.length >= 3` 하한). 그리고 **머리줄 면제가 첫 줄이 데이터인 표의 어긋남을
가린다.**

배치11 이 정량 근거를 냈다 — **범위를 넓혀 3건, 면제를 끄고 4건 추가.**

**오탐 조건 넷을 함께 박는다. 이것이 이 work 의 어려운 부분이다.**

| # | 조건 | 근거 |
| --- | --- | --- |
| 1 | 빈 줄로 덩어리를 끊는다 | 배치9 |
| 2 | 오른쪽 정렬 열을 인정한다 | 배치9 (지금 규칙이 이미 양쪽을 인정한다) |
| 3 | `^` 눈금 칸은 제외한다 | 배치9·10 — 캐럿 도식은 행마다 칸 수가 달라 덩어리가 안 선다 |
| 4 | 중첩 들여쓰기가 뜻인 그림(의사코드·나무)은 첫 열을 어긋남으로 세지 않는다 | 배치11 |

**검사기 자체의 자기시험이 함께 선다.** `KAN-034.7` 검토 4항에서 레일을 42편 전수로 확인할 때
클릭한 앵커와 위치를 잰 헤딩이 달라 **42편 전부가 거짓 실패**로 나왔다(`a:last-of-type` 이
부모별 마지막이라 실제 마지막 링크가 아니었다). 고치니 전부 통과했다. 워커들이 `P15` 검사기를
각자 짜면서 겪은 것과 같은 부류다 — **검사기가 틀려서 난 빨강을 원고의 빨강으로 읽지 않는다.**

**완료 기준**(공통 기준에 더해): 손으로 쓴 `text` 펜스와 2행 덩어리를 본다 · 머리줄 면제가
첫 줄 데이터 표를 안 가린다 · **오탐 조건 넷마다 시험 한 벌씩** · 111편 전수를 다시 돌려
새로 잡힌 자리를 **규칙의 오탐인지 원고의 부채인지 가른 목록**을 낸다

### WP2 · `S6` 변이 자리 대조와 「같다」 세 부류 (`A2`·`C2`) — `tools/check-proof.ts`

**`A2`**: `←` 로 짚은 줄이 정본에 있으면 `mutantSiteFailures` 가 검사를 건너뛴다
(`s === "" || inRef.has(s)` → `continue`). 지운 줄을 주석형으로 보여도 `s === ""` 로 역시
건너뛴다 — **drop 변이는 자리 대조가 구조적으로 안 돈다.** (배치11 `S41`)

**`C2`**: 변이 표의 「같다」가 세 부류다.

| 부류 | 무엇 | 어떻게 가리는가 |
| --- | --- | --- |
| ① | 그 줄을 **안 지나감** | 「지나간 횟수」 열 |
| ② | 지나갔고 **답도 같음** | 「지나간 횟수」 열 |
| ③ | 지나갔고 **중간 값도 갈렸는데 뒤 걸음이 상쇄해 답만 같음** | **걸음별 대조 블록**이 있어야 보인다 |

배치9 가 ①을, 배치10 `crt` 가 ③을 찾았다. `check-proof` 가 **「지나간 횟수 ≥ 1 인데 같다」인
행을 기계로 짚어 줄 수 있다.**

**완료 기준**(공통 기준에 더해): drop 변이에서 자리 대조가 돈다 · 「지나간 횟수 ≥ 1 인데
같다」 행을 화면이 짚는다 · 세 부류를 가르는 시험 세 벌

### WP3 · `S7` 표 칸의 세로줄 (`B2`) — `tools/build-html.ts`

표 칸 안의 `|` 가 GFM 칸 구분자로 읽혀 **HTML 에서 그 칸이 사라진다.** 인라인 코드 안이어도
그렇고 **스캐너 넷이 전부 초록**이다. (배치6 `articulationPoints` 기호표)

처방이 이미 실측됐다 — **`build-html` 이 md 표 칸 수와 렌더된 `<td>` 수를 대조하면
결정론으로 잡힌다.**

**완료 기준**(공통 기준에 더해): md 칸 수 ≠ 렌더 `<td>` 수인 표를 잡는다 ·
`articulationPoints` 기호표가 그 검사에 걸린다(그리고 고친다) · 111편 전수에서 같은 자리를 훑는다

## 2. 의존과 순서

**셋이 서로 독립이다** — 파일이 갈리고 검사 대상도 겹치지 않는다. 워커 셋을 동시에 띄운다.

`S3` 만 111편 전수를 다시 돌린다. **전수 실행은 배치 종료에 한 번만** 한다 — 셋이 각자 돌리면
같은 10분을 세 번 쓴다(`check-proof --all` 이 10분을 넘는다는 것이 `KAN-034.8` 배치4 실측이다).

## 3. 리스크

- **`S3` 이 이 배치의 일정을 정한다.** 오탐 조건 넷 + 자기시험 + 전수 판정이라 다른 둘의 두세 배다.
  `S3` 이 안 끝나면 배치를 안 닫되, **`S6`·`S7` 은 먼저 끝나면 그 자리에서 커밋한다**(직렬 대기 없음).
- **`S3` 의 전수 결과가 다음 배치를 바꿀 수 있다.** 새로 잡힌 자리가 많으면 원고 교정이 별도
  work 로 서야 한다 — 그 판단을 배치 종료 문서에 남긴다.
- **워커 셋이 `FEEDBACK.md` §2 에 각자 한 줄씩 더한다.** 줄 단위 추가라 충돌하지 않지만,
  **배치 종료에 일괄 반영**한다(W3 에서 세운 규율 그대로).
- **`S7` 의 처방이 렌더러를 바꾼다.** `build-html` 은 산출 HTML 을 내는 쪽이라, 검사를 더하는
  것과 렌더를 고치는 것을 섞지 않는다 — **검사만 더하고 원고를 고친다.**

## 4. 착수 시점 판단 (2026-09-10)

**셋을 계획대로 폭 3 으로 띄운다.** 미루는 work 없음.

배치1 이 `S1`·`S2` 로 `check-v2.ts` 를 이미 만졌으므로 `S3` 은 그 위에서 시작한다 —
`continuesSentence`·`mappingSpan`·`bodyNumbers` 가 새로 서 있고, `P15` 는 그것들과
다른 함수(`generatedBlockAlignment`)라 겹치지 않는다.

**배치1 이 남긴 규율 하나를 셋 다 진다** — 「시험이 통과한다」와 「시험이 무언가를 잰다」는
다른 말이다. 배치1 에서 변이를 열한 번 걸었고 그중 셋이 「0 fail」로 나와 시험을 고쳐야 했다.
**각 work 는 자기가 세운 시험에 변이를 걸어 보고 그 결과를 보고한다.**


## 5. 끝난 뒤 (2026-09-10)

셋 다 닫혔다. **폭 3 이 값을 했다** — 파일이 갈려 있어 충돌이 한 번도 없었고, 워커 셋이
각자 자기 파일만 만졌다.

### 배치1 의 규율이 두 워커에게서 실제로 값을 했다

「자기가 세운 시험에 변이를 걸어 보라」를 셋 다 지켰고, **둘이 그 자리에서 `0 fail` 을 받았다.**

- `S3` 의 `M9b`(「나머지 열이 다 맞아야 한다」) — 그 조건을 **아무 시험도 재고 있지 않았다.**
  시험 한 벌을 더해 빨갛게 만들었다.
- `S6` 의 `M10`(칸 수가 안 맞아도 읽는다) — 표본이 **앞선 숫자 가드에 먼저 걸려** 정작 재려던
  길이 가드를 한 번도 안 탔다. 「칸이 밀렸는데 그 자리에 숫자가 오는」 표본으로 갈았다.

**둘 다 배치1 이 겪은 것과 같은 부류다.** 규율을 문서에 적어 두는 것과 워커가 그 자리에서
실행하는 것 사이에 거리가 없었다 — 프롬프트에 근거(배치1 실측)를 함께 실은 것이 이유로 보인다.

변이 합계: `S3` 17 · `S6` 12 · `S7` 7 = **36개, 전부 빨강**(고친 둘 포함).

### 세 work 가 낸 것

| work | 부류 | 전수에서 실제로 잡힌 것 |
| --- | --- | --- |
| `S3` | `B1` | 위반 **4자리**(사이드카 부채, 고침) · 경고 151자리 62편 |
| `S6` | `A2`·`C2` | 원고 **28줄(25편)**이 처음 검사에 들어옴 · `fftMultiply` 1편 교정 · 정보 46행 7편 |
| `S7` | `B2` | **2편 2줄** 교정 |

**`S6` 의 28줄이 이 배치에서 가장 큰 몫이다.** drop 변이의 자리 대조가 구조적으로 안 돌아서
25편의 그 줄들이 **한 번도 대조된 적이 없었다.** 그중 하나가 실제로 틀려 있었다.

### 심각도 사다리가 이 저장소의 관행으로 굳었다

`S3` 이 `Finding.warn` 을 신설하면서 `check-proof.ts` 의 2026-09-05 선례(「중화 대조를 못 잰
자리」)를 그대로 따랐고, `S6` 도 「같다」를 **정보**로 냈다. 셋의 판단이 같은 규칙에서 나온다 —
**한꺼번에 빨개지면 다른 편을 닫는 세션이 자기 것이 아닌 빨강을 본다.**

그래서 이 배치가 남긴 화면 문구가 셋 다 「지금은 경고이고, 고친 뒤 위반으로 올린다」 꼴이다.
**승격 경로를 화면이 스스로 적는다.**

### 다음으로 넘기는 것 셋

1. **`S3` 의 경고 151자리 62편 원고 교정.** 표본 판정으로 손 펜스 133 중 **20/22 가 부채**,
   생성 2행 18 중 5가 부채다. **이 카드의 부류가 아니다** — 스캐너 구멍이 아니라 원고 부채이고,
   `KAN-034.9` 가 W3 의 부채로 선 것과 같은 자리에 선다. 별도 카드가 맞다.
2. **`S6` 의 승격 경로 — `SPEC` 블록 id 규약.** 답 블록 `mutant-x` 에 걸음 블록 `mutant-x-step`
   을 짝으로 두게 하면, 「지나갔는데 같다」 행이 있는 블록에 짝이 있는지를 **기계가 물을 수 있다.**
   그러면 정보가 경고로, 다음에 위반으로 올라간다. **교정이 먼저이므로 이번에 안 넣었다.**
3. **`S7` 이 낸 사각지대 둘 — 실측 0건.** ① 행 끝의 세로줄(칸 수가 같아 안 걸린다) ② 머리줄의
   세로줄(**표가 아예 안 선다** — 파싱 결과 0개). 오케스트레이터가 111편을 훑어 **둘 다 0건**을
   확인했다. ②는 손상이 더 크지만 칸 수 대조로는 원리적으로 못 잡는다 — 「`| --- |` 구분줄이
   있는데 그 자리가 표가 아니다」를 봐야 하고 그것은 `check-v2` 쪽이다. **배치3 후보**이고
   스캔 코드는 여덟 줄이면 된다.

### 배치3 은 계획대로 간다

`S4` ‖ `S8` ‖ `S9`, 폭 3. `S4` 는 배치2 의 `S3` 이 `P15` 를 넓힌 뒤라야 값이 온전한데
그 의존이 이 배치로 이미 만족됐다.
