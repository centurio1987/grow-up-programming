---
card: KAN-034.7-QMZ3RE
batch: 6
created: 2026-09-04
branch: KAN-034-KSD7XR
status: 계획
steps: S32, S38, S3, S7
---

# KAN-034.7-QMZ3RE 배치6 — 중반 — 남은 넷 카테고리 한 편씩

카드: [KAN-034.7-QMZ3RE.md](../KANBAN.cards/KAN-034.7-QMZ3RE.md) · 범위 `S32` · `S38` · `S3` · `S7`
선행: [배치5](KAN-034.7-QMZ3RE.batch5.md)

> **이 문서는 착수 전 계획이다.** 수행 내역은 카드 실행 문서의 「수행 내역」에 있다.

## 1. 작업 패키지

**공통 완료 기준은 카드 실행 문서 「실행 계획」의 42편 공통 블록이고, 명령 전문은 그 문서
「검증」 절이다.** 여기에는 편마다 다른 것만 적는다.

### WP1 · `S32` closestPairOfPoints (geometry · 뷰 keyValue)

**완료 기준**: `src/algorithms/geometry/closestPairOfPoints/closestPairOfPoints-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `build-html` exit 0 · `bun test src/algorithms/geometry/closestPairOfPoints/closestPairOfPoints-guide.test.ts` 통과

### WP2 · `S38` knuthOptimization (advanced · 뷰 keyValue+matrix)

목록 `:115` 를 `S37` divideAndConquerDp(배치4)와 공유한다

**완료 기준**: `src/algorithms/advanced/knuthOptimization/knuthOptimization-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `build-html` exit 0 · `bun test src/algorithms/advanced/knuthOptimization/knuthOptimization-guide.test.ts` 통과

### WP3 · `S3` floydWarshall (shortest-path · 뷰 keyValue+matrix)

**완료 기준**: `src/algorithms/shortest-path/floydWarshall/floydWarshall-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `build-html` exit 0 · `bun test src/algorithms/shortest-path/floydWarshall/floydWarshall-guide.test.ts` 통과

### WP4 · `S7` articulationPoints (graph · 뷰 graph+keyValue)

목록 `:66` 을 `S8` bridgesInGraph(배치9)와 공유한다

**완료 기준**: `src/algorithms/graph/articulationPoints/articulationPoints-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `build-html` exit 0 · `bun test src/algorithms/graph/articulationPoints/articulationPoints-guide.test.ts` 통과

## 2. 의존과 순서

넷 다 카테고리가 다르다(`geometry` · `advanced` · `shortest-path` · `graph`). 배치 안 상호 대기 없음.

`S3` floydWarshall 이 `shortest-path` 의 셋째다 — 문서 순(`S1`→`S2`→`S3`→`S5`)을 지키느라
`S5` aStarSearch 보다 앞 배치에 온다.

### 공유 파일 넷은 워커가 안 건드린다

`문제_가이드_목록.md` · `tools/_baseline/guide-rhythm.tsv` ·
`sandbox/algo-guide-v2/FEEDBACK.md` §5 · `KANBAN.md` 넷은 배치 전체가 공유한다.
**워커는 자기 편 디렉터리만 쓴다.** 넷은 배치를 닫을 때 한 자리에서 일괄 반영하고,
그 일괄 반영도 전진 한 단위라 커밋한다. `tools/ord004-manifest.json` 은 `KAN-034.8` 몫이다.

**워커는 `git add` 도 하지 않는다** — 한 워크트리의 인덱스를 공유해서 스테이징이 남의
커밋에 딸려 들어간다(W1 배치6 에서 실제로 났다).

**목록 링크 교체는 언제나 `src/algorithms/` 접두사로 좁힌다** — `:81`·`:82` 는 같은 줄에
자료구조 트랙 링크를 들고 있어서 이름으로 치환하면 그 트랙까지 `.md` 가 된다.

## 3. 리스크

- **골격 결함이 있으면 이 배치의 편들이 함께 틀린다.** 배치를 닫기 전에 `bun run tools/ci.ts all` 이 초록이어야 한다.
- **`tools/check-guide-rhythm.ts` 를 v2 편에 부르지 않는다** — 구 헤딩 접두와 어긋나 조용히 공전하고 통과 표시가 거짓이 된다.
- **스캐너 넷이 0 이어도 `FEEDBACK.md` §3 대조를 밟는다** — W2 배치6~11 의 대조 건수가 65 · 94 · 116 · 166 · 79 · 63 이고 줄어드는 추세가 없다.
- **최악 입력이라고 손으로 적은 수의 성질을 실행으로 확인한다** — W2 에서 `isPrimeTrial` 의 999,999,000,001 이 소수였다.
- **경쟁 설계를 배정밀도로 재지 않는다** — `alt.ts` 의 `measure()` 가 매 실행마다 정본과 답을 대조한다(W2 `digitDp` 실패에서 굳은 규칙).
- 되돌리기는 편 단위다 — 편 하나가 커밋 하나이고 태그 하나(`kan/KAN-034.7-QMZ3RE/S<n>`)를 받는다.

## 4. 착수 시점 판단

**넷 다 이 배치에서 한다 — 미룰 work 없음**(2026-09-06 착수 실측).

`bun run tools/algo-wbs.ts` 의 W3 claim 후보 11건에 넷이 전부 들어 있다
(`floydWarshall` · `articulationPoints` · `closestPairOfPoints` · `knuthOptimization`).
현재 W3 은 22/44 이고 막힌 편 11 은 전부 `category` 사유다 — 배치 문서와 도구가 어긋나지
않았다.

착수 직전 `bun run tools/ci.ts all` 이 **exit 0**(단계 16개)이다. 배치3 리스크가 요구한
「골격이 성한 자리에서 시작한다」를 실측으로 확인했고, `tail` 파이프 없이 종료코드를 봤다
(배치5 가 그 자리에서 한 번 틀리게 읽었다).

### 인바운드는 넷 다 두 자리뿐이다 — 편 커밋에 딸려 넣을 링크 교체가 없다

`bun run tools/check-links.ts refs` 를 편마다 돌렸다. `.mdx` 상호 링크가 **한 건도 없다** —
배치5 가 `pollardRho` 에서 만난 것과 같은 자리는 이번에 없고, **워커는 자기 편 디렉터리만
쓰면 된다.**

| 편 | 인바운드 | 누구 몫인가 |
| --- | --- | --- |
| `closestPairOfPoints` | `ord004-manifest.json:645` · `문제_가이드_목록.md:107` | 앞은 `KAN-034.8` · 뒤는 배치 종료 일괄 반영 |
| `knuthOptimization` | `ord004-manifest.json:55` · `문제_가이드_목록.md:115` | 〃 |
| `floydWarshall` | `ord004-manifest.json:1164` · `문제_가이드_목록.md:62` | 〃 |
| `articulationPoints` | `ord004-manifest.json:827` · `문제_가이드_목록.md:66` | 〃 |

`:115` 는 `S37` divideAndConquerDp(배치4)가 이미 지난 줄이고 `knuthOptimization` 이 그 줄의
마지막 알고리즘 링크다. `:66` 은 `S8` bridgesInGraph(배치9)가 뒤에 한 번 더 지난다 —
그 줄은 이 배치에서 안 닫힌다.

### 배치5 가 인계한 워커 격리 두 자리를 지시에 넣는다

배치5 종료 판단이 격리가 샌 자리 둘을 남겼다. **문서에만 적으면 다시 샌다.**

1. **임시 파일 이름에 편 이름을 접두로 붙인다** — 스크래치패드가 워커 사이에 공유돼
   같은 이름(`fill.ts`·`render.ts`)이 서로를 덮었고, 한쪽이 **다른 편의 원고를 다시 쓰는
   일**이 실제로 났다.
2. **커밋은 `git commit -- <경로>` 로 경로를 명시한다** — 워커에게 `git add` 를 금지해도
   `.mdx` 삭제가 인덱스에 올라와 남의 커밋에 딸려갈 수 있다.

### 띄우는 폭은 2 다

배치4 가 넷을 한꺼번에 띄웠다가 사용량 한도로 워커 넷이 전부 죽었고, 배치5 가 폭 2 로
끝냈다. **배치 크기(work 4)는 그대로 두고 동시에 띄우는 수만 2 로 둔다.**

### 이 배치가 확인하려는 것

배치5 가 남긴 열린 항목 하나 — **`L37` 레일 실렌더가 배치4~11 전부 미확인**이다. 카드
「검증」이 「이 웨이브에서 한 번은 실제 창 폭으로 연다」고 적었고, 이 배치의 편 하나에서
`build-html` 산출을 실제로 연다.

### 끝난 뒤 — 착수 판단이 맞았는가
<!-- 배치를 닫을 때 채운다. -->
