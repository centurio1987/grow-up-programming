---
card: KAN-034.6-CF6ZHE
batch: 8
created: 2026-08-30
branch: KAN-034-KSD7XR
status: 계획
steps: S27, S21, S23, S30
---

# KAN-034.6-CF6ZHE 배치8 — `graph-flow`·`binary-search` 마감과 `matrix+tree` 첫 편

카드: [KAN-034.6-CF6ZHE.md](../KANBAN.cards/KAN-034.6-CF6ZHE.md) · 범위 `S27` · `S21` · `S23` · `S30`
선행: [배치7](KAN-034.6-CF6ZHE.batch7.md)

> **이 문서는 착수 전 계획이다.** 수행 내역은 카드 실행 문서의 「수행 내역」에 있다.

## 1. 작업 패키지

**공통 완료 기준은 카드 실행 문서 「실행 계획」의 41편 공통 블록이고, 명령 전문은 그 문서
「검증」 절이다.** 여기에는 편마다 다른 것만 적는다.

### WP1 · `S27` isBipartite (graph-flow · 뷰 graph+keyValue)

이 편으로 `graph-flow` 가 마감된다

**완료 기준**: `src/algorithms/graph-flow/isBipartite/isBipartite-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `build-html` exit 0 · `bun test src/algorithms/graph-flow/isBipartite/isBipartite-guide.test.ts` 통과

### WP2 · `S21` searchInRotatedSortedArray (binary-search · 뷰 array)

이 편으로 `binary-search` 가 마감된다

**완료 기준**: `src/algorithms/binary-search/searchInRotatedSortedArray/searchInRotatedSortedArray-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `build-html` exit 0 · `bun test src/algorithms/binary-search/searchInRotatedSortedArray/searchInRotatedSortedArray-guide.test.ts` 통과

### WP3 · `S23` largestRectangleInHistogram (array · 뷰 array+keyValue)


**완료 기준**: `src/algorithms/array/largestRectangleInHistogram/largestRectangleInHistogram-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `build-html` exit 0 · `bun test src/algorithms/array/largestRectangleInHistogram/largestRectangleInHistogram-guide.test.ts` 통과

### WP4 · `S30` treeMaxIndependentSet (dp · 뷰 matrix+tree)

**뷰 조합 `matrix+tree` 첫 등장이다** — 시뮬 규격에 선례가 없으므로 이 편이 선례를 만든다

**완료 기준**: `src/algorithms/dp/treeMaxIndependentSet/treeMaxIndependentSet-guide.md` 가 서고 같은 자리의 `.mdx` 가 사라진다 · 그 편에서 `check-v2`·`check-proof`·`check-metaphor`·`bench-alt --check` 가 0 · `build-html` exit 0 · `bun test src/algorithms/dp/treeMaxIndependentSet/treeMaxIndependentSet-guide.test.ts` 통과

## 2. 의존과 순서

넷 다 카테고리가 다르다(`graph-flow` · `binary-search` · `array` · `dp`) — 도구가 카테고리마다 한 편만 후보로 내므로
이것이 병렬 폭의 상한이다. 배치 안 상호 대기 없음.

**다만 뷰 조합 첫 편이 하나 들어 있다** — `S30` treeMaxIndependentSet(`matrix+tree`).
그 편이 서야 같은 조합의 후속 배치가 열린다.

### 공유 파일 넷은 워커가 안 건드린다

`문제_가이드_목록.md` · `tools/_baseline/guide-rhythm.tsv` ·
`sandbox/algo-guide-v2/FEEDBACK.md` §5 · `KANBAN.md` 넷은 배치 전체가 공유한다.
**워커는 자기 편 디렉터리만 쓴다.** 넷은 배치를 닫을 때 한 자리에서 일괄 반영하고,
그 일괄 반영도 전진 한 단위라 커밋한다. `tools/ord004-manifest.json` 은 `KAN-034.8` 몫이다.

**워커는 `git add` 도 하지 않는다** — 한 워크트리의 인덱스를 공유해서 스테이징이 남의
커밋에 딸려 들어간다(W1 배치6 에서 실제로 났다).

## 3. 리스크

- **골격 결함이 있으면 이 배치의 편들이 함께 틀린다.** 배치를 닫기 전에 `bun run tools/ci.ts all` 이 초록이어야 한다.
- **뷰 조합 첫 편은 선례를 만든다.** 그 편의 `sim.ts` 가 틀리면 같은 조합의 후속 편이 전부 그것을 베낀다 — 다른 편보다 한 번 더 본다.
- **`tools/check-guide-rhythm.ts` 를 v2 편에 부르지 않는다** — 구 헤딩 접두와 어긋나 조용히 공전하고 통과 표시가 거짓이 된다.
- **스캐너 넷이 0 이어도 `FEEDBACK.md` §3 대조를 밟는다** — W1 배치6 에서 네 편 전부 그 뒤에 결함이 나왔다(3 · 3 · 7건).
- 되돌리기는 편 단위다 — 편 하나가 커밋 하나이고 태그 하나를 받는다.

## 4. 착수 시점 판단

**2026-09-03 착수. 네 편을 그대로 간다 — `S30` 을 배치9 로 미루지 않는다.**

근거 넷을 착수 직전에 실측했다.

- **골격 기준선이 초록이다** — 배치7 을 닫으면서 `bun run tools/ci.ts all` 이 16 단계 통과했고
  `bun run tools/check-links.ts check` 가 465 건 전부 실재를 냈다(리스크 1 이 닫혔다).
- **넷 다 지금 잡을 수 있다** — `bun run tools/algo-wbs.ts` 의 W2 claim 후보 **6 건**에 넷이
  전부 들어 있다(`graph-flow` isBipartite · `binary-search` searchInRotatedSortedArray ·
  `array` largestRectangleInHistogram · `dp` treeMaxIndependentSet). 후보가 배치7 착수 때의
  8 건에서 준 것은 `sorting` 과 `graph` 가 마감됐기 때문이고, **이 배치가 후보 넷을 한꺼번에
  가져가므로 남는 것은 둘뿐**이다(`bit-manipulation` lowestSetBit · `number-theory`
  binomialModP). 배치9 착수 때 다시 잰다.
- **편 커밋에 함께 넣을 링크 교체가 없다** — 네 편에 `check-links.ts refs` 를 돌린 결과
  인바운드가 전부 `tools/ord004-manifest.json`(`KAN-034.8` 몫)과 `문제_가이드_목록.md`
  (배치 종료 일괄 반영 몫) 두 자리뿐이다. 배치5 와 같은 모양이고 배치6·7 과 다르다.
- **배치 크기를 바꿀 이유가 없다** — 배치1~7 이 전부 work 4 개로 닫혔다(28/41).

**남기는 경계 셋.**

- **`S30` treeMaxIndependentSet 이 `matrix+tree` 뷰 조합의 첫 편이라 선례를 만든다**(리스크 2).
  두 뷰의 선례는 각각 따로 서 있다 — `matrix` 는 `S4`·`S5`·`S28`·`S29`, `tree` 는
  `S12`·`S13`·`S31`. **둘을 함께 쓰는 자리가 없어서** 그 편의 `sim.ts` 가 규약을 새로 세운다.
  다른 편보다 한 번 더 보고, 세운 규약을 수행 내역에 남겨 뒤 편이 물려받게 한다.
- **이 배치로 `graph-flow` 와 `binary-search` 가 마감된다**(WP1 · WP2). 배치7 이 `sorting`·
  `graph` 를 닫은 것과 같은 자리이고, 카테고리가 닫힐수록 배치의 병렬 폭 상한이 내려간다.
- 스캐너 넷이 0 이어도 `FEEDBACK.md` §3 대조를 편마다 밟는다(리스크 4). 배치6·7 이 합쳐
  **159 건**을 냈고 값을 낸 방법 둘이 확립됐다 — **참조 편과의 정확 일치 줄 전수 대조**(저장소
  골격 문구를 걷어내고 두 편에만 있는 문장만 고른다)와 **자기 문서 안의 모순 찾기**(본문이
  적은 사실과 같은 편 `deep.math` 의 유도가 부딪히는 자리). **낫표 안 점검은 배치7 에서
  98 곳 전수 0 건이었고, 대신 낫표 밖에서 은유 목록에 없어 스캐너가 못 보는 다의어가
  잡혔다** — 이번 배치는 점검 방향을 그쪽으로 돌린다.
