---
card: KAN-048-QN5194
title: 칸반 카드 문서의 밀린 인용 31건 정정 — KAN-047 이 표시한 자리
created: 2026-09-17
branch: KAN-048-QN5194
worktree: /Users/centurio/orca/workspaces/code_test/KAN-048-QN5194
base: main
status: 검토 대기
---

# KAN-048-QN5194 검토 요청 — 칸반 카드 문서의 밀린 인용 31건 정정 — KAN-047 이 표시한 자리

카드: [KAN-048-QN5194.md](../KANBAN.cards/KAN-048-QN5194.md)

> 이 문서는 **검토를 위한 산출물**이다. 수행 내역은 카드 실행 문서에 있고, 착수 전
> 계획은 배치 문서에 있다. 여기 있는 것은 "지금 이 브랜치를 무엇으로 판정하는가" 뿐이다.

## 1. 검토 대상

| 항목 | 값 |
|---|---|
| 브랜치 | `KAN-048-QN5194` |
| 워크트리 | `/Users/centurio/orca/workspaces/code_test/KAN-048-QN5194` |
| 베이스 | `main` |
| 변경 훑기 | `git diff main...HEAD` |

**커밋 3건**

```text
0d23c7e KAN-048 배치2: 런북 예시 · 카드 자신의 좌표 표기 · 대장 갱신 (S3)
1899334 KAN-048 배치1: 표류 정정 — 좌표 12 · 경로 적기 11 (S1 · S2)
8d5f82b kanban: KAN-048 착수 — 카드 문서 · 배치 둘
```

**변경 파일 17개 (+490 −89)**

| 파일 | 상태 | 추가 | 삭제 |
|---|:--:|---:|---:|
| `.kanban/archive.jsonl` | M | 1 | 0 |
| `.kanban/log.md` | M | 1 | 1 |
| `.kanban/state.json` | M | 20 | 15 |
| `KANBAN.batches/KAN-048-QN5194.batch1.md` | M | 180 | 0 |
| `KANBAN.batches/KAN-048-QN5194.batch2.md` | M | 137 | 0 |
| `KANBAN.board.html` | M | 4 | 4 |
| `KANBAN.cards/KAN-026.md` | M | 4 | 2 |
| `KANBAN.cards/KAN-027.md` | M | 2 | 1 |
| `KANBAN.cards/KAN-033-E8KHSV.md` | M | 6 | 4 |
| `KANBAN.cards/KAN-034-KSD7XR.md` | M | 4 | 2 |
| `KANBAN.cards/KAN-034.7-QMZ3RE.md` | M | 5 | 4 |
| `KANBAN.cards/KAN-034.8-BK1Q3A.md` | M | 6 | 4 |
| `KANBAN.cards/KAN-035-31T4BY.md` | M | 6 | 4 |
| `KANBAN.cards/KAN-048-QN5194.md` | M | 69 | 0 |
| `KANBAN.md` | M | 18 | 17 |
| `docs/ORD-006-runbook.md` | M | 1 | 1 |
| `tools/_baseline/citations.tsv` | M | 26 | 30 |

**롤백 태그 5개**

```text
kan/KAN-048-QN5194/S1
kan/KAN-048-QN5194/S2
kan/KAN-048-QN5194/S3
kan/KAN-048-QN5194/batch1
kan/KAN-048-QN5194/batch2
```

## 2. 검증 — 기준과 실행 결과

<!-- 기준은 카드 실행 문서 「검증」 절의 사본이다. 정본은 KANBAN.cards/KAN-048-QN5194.md 이므로
     기준이 바뀌면 그쪽을 고치고 review-init --refresh 로 이 항만 다시 뜬다.
     결과는 착수한 쪽이 이미 돌린 것이다 — 검토자에게 다시 돌리라고 시키지 않는다.
     **다시 돌려 아래와 다르게 나오면 그 자체가 반려 사유다.** -->

**기준**

**카드 종료 조건.**

1. 대장의 `drift` 가 **대상 항목 자체가 걷혀 고칠 곳이 없는 자리만** 남고, 그 사유가 카드에 적혀 있다. ㉢ 의 기록 자리는 경로 없는 인용이라 애초에 대장 밖(보류)이다 — 조건 5 가 둘을 함께 받는다.
2. 고친 인용마다 대상 줄의 내용이 감싼 산문의 주장과 맞는다 — 전수로 확인한 기록이 배치 문서에 있다.
3. `cards bare` 래칫이 32 에서 **내려간다**(경로를 적은 만큼).
4. `bun run tools/check-citations.ts` 통과 · `bun run tools/ci.ts all` 통과 · `cd rust && cargo test` 통과 · 시험 26 pass 유지.
5. 고치지 않기로 한 자리(기록 · 대상이 걷힌 것 · 어색해지는 자리)가 무엇이고 왜인지 카드에 남는다.

**실행 결과**

```text
이 브랜치에서 2026-09-17 실행. 로그: 세션 스크래치 `ci-048-b2.log`.

- `bun run tools/check-citations.ts` — **exit 0**. 인용 1,211건 존재 통과 · 대장 **1,199행** 지문 전부 일치 · `drift` 34 → **6**
- 래칫: `bare 461`(462 → 461) · `cards bare=23`(32 → 23) · `cards unresolved=16`(17 → 16) · `detached=2` · `naked-name=0` · `naked-line 29` — **오른 칸 없음**
- `bun run tools/ci.ts all` — **all 모드 통과 — 단계 17개.** · `cd rust && cargo test` 실패 0 · `bun test tools/check-citations.test.ts tools/ci-workflow.test.ts` 26 pass / 0 fail · `bunx tsc --noEmit` 0 · `guide-core check` · `check-links check` 통과
- `--update` 재실행 **바이트 동일**(멱등, sha256 `5fb28740ee8b…`)

**정정 규모**: 31 인용 중 **23 정정**(좌표 12 · 경로 적기 11) · **8 남김**. 고친 23 인용의 대상 줄 32개를 산문과 나란히 놓고 **전수 대조**했고, 편집은 전부 `doc-edit` CAS 로 한 줄 안에서 끝나 **줄 밀림 0** 입니다.

**남긴 8(대장 6행)**: 대상 항목 자체가 걷혔고 **산문도 낡은** 자리입니다(`paths.json` 28·29 줄 · `ord004-manifest.json` 659 줄 · `treap.ts` 45 줄). 인용만 걷으면 「그 파일이 지금 무엇을 들고 있다」는 거짓 문장이 근거 없이 남습니다 — 고치려면 산문을 다시 써야 하고 그것은 이 카드 범위 밖입니다. 여섯 대상 줄을 `S3` 이 다시 열어 **여전히 밀려 있음**을 확인했습니다.

**착수 시점에 게이트가 이미 붉었습니다** — 원인은 이 카드 문서 자신이었습니다(전략·실행 계획에 좌표를 글자 그대로 적어 도구가 진짜 인용으로 읽음 · `cards bare` 32 → 38). `S3` 이 그 표기를 산문으로 바꿔 닫았고, 같은 갈래인 런북 불변 사실 25 의 예시 인용도 `NN` 관례로 되돌렸습니다.
```

## 3. 판단 항목 — 스크립트가 판정할 수 없는 것

<!-- 스크립트가 판정할 수 없는 것만 적는다 — 값의 진위, 선택지 중 하나를 고른 근거,
     범위를 그은 자리. 2항에서 이미 돌아간 검증을 여기 옮겨 적지 않는다.
     한 줄 형식: 체크박스 하나에 의견 하나 — "<주제> — <지금 고른 값과 그 근거>".
     **의견마다 「상세」 접기가 따라붙는다** — 검토자는 이 카드를 수행하지 않았으므로
     내부 기호(`L10`·`P5`·`S8`)만 던지면 판정할 재료가 없다. 상세에는 그 기호를 풀어
     쓰고 원문 경로(`파일:줄`)나 링크를 건다.
     비어 있으면 "기계가 다 판정했고 사람이 정할 것이 없다"는 뜻이다. 그 판단도
     착수한 쪽이 하는 것이지 검토자가 빈칸을 보고 추측할 일이 아니다.
     **승계 절(3-0)이 있으면 그것이 먼저 온다** — 다른 검토서에서 넘어온 의견이고,
     판정은 승계를 받은 이 문서 하나에서만 내려진다. -->

**의견마다 판정과 추가 의견이 따로 붙습니다.** 판정은 상태이고 추가 의견은 말입니다 — 승인/반려를 아직
안 정했어도 의견 하나에만 추가 의견을 달 수 있고, 반대로 의견 하나만 먼저 닫을 수도 있습니다.
`<번호>`는 의견 순서이고, 주제의 문구 일부로도 찾습니다.

```
# 판정 — 승인 · 반려 · 철회
python3 scripts/kanban.py review-judge <project-root> --card KAN-048-QN5194 --item <번호> --verdict 승인
# 추가 의견
python3 scripts/kanban.py review-note <project-root> --card KAN-048-QN5194 --item <번호> --text "<추가 의견>"
# 추가 의견을 반영하다 새 의견이 생겼으면 (맨 뒤에 붙어 앞 번호가 안 밀립니다)
python3 scripts/kanban.py review-item <project-root> --card KAN-048-QN5194 --add "<주제>
  <상세>"
```

**전체 승인은 살아있는 항목이 전부 승인일 때만 섭니다**(철회는 분모에서 빠집니다). 하나라도
반려·추가 의견·미정이면 4항의 전체 승인도 `→ 완료` 이동도 종료코드 14로 거부됩니다.


## 4. 판정

<!-- 문서 하나에 대한 판정이다. **항목별로 갈리는 말은 여기 적지 않는다** — 3항 각 의견의
     「판정」과 「추가 의견」이 그 자리다. 여기 남는 것은 그 항목들이 전부 승인으로 닫혔다는
     사실 하나뿐이다.
     아래 「판정 이력」은 **덧붙기만 하는 이력**이다. 왕복이 돌면 줄이 쌓이고, 그것이 이 문서가
     무엇을 거쳐 승인에 닿았는지의 전부다 — 지우지 않는다. **판정에는 사유 칸이 없다** —
     승인은 대체로 덧붙일 말이 없고, 있다면 그것은 문서 전체가 아니라 그 항목에 대한
     말이라 3항의 「추가 의견」이 받는다.
     `review-judge --card KAN-048-QN5194 --verdict 승인` 이 이 자리를 쓰고
     frontmatter 의 status 도 함께 고친다. 손으로 적어도 되지만, 그때는 수렴 검사를
     안 거치므로 `validate` 가 항목 판정과 어긋난 승인을 error 로 잡는다. -->

**판정**: (아직 없습니다)

**판정 이력**:

- 승인이면 → `apply --op move --id KAN-048-QN5194 --to done` 뒤에 `main` 병합과 워크트리 정리(출력의 `cleanup`)
- 반려면 → `apply --op move --id KAN-048-QN5194 --to doing` 뒤에 `doc-log --entry "<반려 사유>"`.
  요청서는 **지우지도 다시 뜨지도 않는다** — 고친 뒤 그 항목을 `review-judge --verdict 승인` 으로
  뒤집으면 같은 문서에서 수렴한다. 1·2항이 낡았으면 `review-init --refresh` 로 그 두 항만 간다.
