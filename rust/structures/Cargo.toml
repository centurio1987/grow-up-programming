[package]
name = "ds-structures"
edition.workspace = true
version.workspace = true
publish.workspace = true

[lints]
workspace = true

[dependencies]
serde_json = { workspace = true }

[target.'cfg(loom)'.dependencies]
loom = { workspace = true }

[dev-dependencies]
ds-contract = { path = "../contract" }
loom = { workspace = true }
