//! 계약 스위트의 Rust 하네스.
//!
//! 규격의 정본은 `docs/ORD-006-conventions.md` §규약2 다. 이 crate 는 규격을 다시 적지
//! 않고 **실행**한다.
//!
//! | 축 | 여기서 도는가 | 왜 |
//! |---|---|---|
//! | 축1 동작 | 돈다 | `rust/vectors/*.json` 재생. 계약의 정본은 TS 이고 vector 는 그 파생물이다 |
//! | 축2 불변식 | 돌지 않는다 | 불변식은 단일 스레드 성질이고 TS 하네스가 이미 돈다 |
//! | 축3 복잡도 | 돌지 않는다 | 성장률도 마찬가지다. Rust 로 오는 구조는 규약4 (가) 등급 — TS 로 **표현이 안 되는** 것이지 느린 것이 아니다 |
//! | 축4 동시성 | **여기서만 돈다** | 선형화·진행 보장은 단일 스레드 TS 에서 표현되지 않는다 |
//!
//! 축4 테스트는 `loom` 모델 검사기 아래에서 돈다. 실행 방법은 `rust/README.md` 에 있다.

pub mod fixtures;
pub mod linearize;
pub mod vector;
