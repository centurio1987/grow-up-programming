---
name: analyze-solution
description: 알고리즘에 대한 답을 분석합니다.
disable-model-invocation: true
argument-hint: <file-name-except-extension>
---

## 솔루션 src/$ARGUMENTS.ts에 대해 분석한다.

규칙의 정본은 `.claude/skills/analyze-solution/SKILL.md` 다.
그 파일과 같은 디렉터리의 `rereview-canvas.md`를 읽고 그대로 따라라.
이 파일에 규칙을 다시 적지 말아라 — 두 벌로 갈라진다.
