# 알고리즘 가이드 캔버스 (ORD-003 템플릿)

알고리즘 가이드의 골격이다. `##` 헤딩의 **문구와 순서는 고정**이고, `###` 헤딩은 직무가 고정이되
제목 문구는 내용에 맞춰 지을 수 있다(고정 접미·직무 주석 참조). 모범 예시(필독):
`src/algorithms/array/mosAlgorithm/mosAlgorithm-study-guide.mdx`.

**조건부 절 규칙**: "조건부"로 표시된 절은 해당 사항이 없으면 **문서에서 절 자체를 생략**한다
(문서 안에 "해당 없음" 메모를 남기지 않는다 — 독자용 문서에 집필 메타를 싣지 않는다).
대신 생략 사유 한 줄을 **품질 게이트 보고에 기록**한다. 감사자는 생략된 조건부 절의
사유 기록이 보고에 있는지 확인한다.

---

```
import { AlgorithmSimulation } from "#guide-sim";
```

# {알고리즘 이름} — {직관을 담은 부제}

<!-- 부제 직무: 이 알고리즘의 "돌파 관찰"을 한 구절로 예고한다.
     예: "질의를 정렬하면 포인터 이동이 √n배 줄어드는 이유" -->

## 한눈에 보는 컨셉

<!-- 템플릿 1단계. 3문장 이하. 아이디어의 전체 모양(무엇을, 어떤 관찰로, 얼마나 빠르게)이
     이 문단만 읽어도 잡혀야 한다. 복잡도는 코드 스팬(`O(...)`)으로 표기. -->

## 출발점: 가장 순진한 방법부터

<!-- 템플릿 2단계. 직무 세 가지:
     1. 문제를 정확히 고정한다 — 함수 시그니처 코드 블록 + 입출력 계약 + 제약 규모.
     2. naive 아이디어를 "어떤 근거와 단서로" 도출했는지 과정을 보인다
        (예: "최댓값 문제 → 후보 전부 나열이 정직한 출발"). 결과만 던지지 않는다.
     3. naive가 막히는 지점(비용 폭발)을 ascii art 수치 예시로 체감시키고,
        다음 절로 이어질 "낌새(단서)"를 한 문장으로 남긴다.
     ascii art는 ```text 블록. 작은 구체 예시(원소 3~6개)로 직접 나열해 보인다. -->

## 아이디어 자세히 들여다보기

<!-- 템플릿 3단계의 컨테이너. 아래 ### 절들이 순서대로 온다. -->

### {아이디어를 담은 제목} — 수식과 그림

<!-- 3.1. 제목 앞부분은 자유("마지막 집에서 거꾸로 생각하기" 등), "— 수식과 그림" 계열 접미 유지.
     직무: 핵심 상태/연산을 LaTeX($...$, $$...$$)로 정식 정의하고, 동시에 ascii art로
     같은 내용을 그림으로 보인다 — 이론적 이해와 개념적 이해를 함께 제공한다.
     정의가 "왜 하필 이 형태여야 하는지"(대안 정의와의 대비)를 한 문단 포함한다.
     작은 입력으로 표/그림을 직접 채워 보이고, 중간에 가벼운 확인 질문을 1개 두어
     독자가 손을 움직이게 한다(톤 규칙). -->

### 단서를 설명해 주는 이론 — {이론 이름}

<!-- 3.2 (조건부: 근거·단서를 해소하는 실마리가 이미 이론화되어 있을 때).
     직무: 앞 절에서 쓴 단서를 일반 이론의 언어로 승격한다. 이론 자체를 자세히 설명하고
     (이름만 던지면 미흡), 이 문제와의 대응을 명시한다.
     예: knapsack01의 "루프가 n-1 결과만 필요" → Markov property 상세 설명.
     예: mosAlgorithm의 "처리 순서 재배열" → 오프라인 처리 + 평방 분할(√n 균형 논증).
     뒤 최적화 절에서 이 이론을 다시 소환할 것이면 "기억해 두세요"로 예고한다. -->

### 셈을 맡는 자료구조 — {자료구조 이름}

<!-- 3.3 (조건부: 문제 해결에 보조 자료구조가 필요할 때).
     직무: 필요한 자료구조가 "이 문제에서 무슨 역할인지"만 설명한다.
     그 자료구조의 가이드가 프로젝트 안에 있으면 **직접 설명하지 않고 상대경로 링크**를 단다.
     예: [hashMapChaining 가이드](../../../data-structures/hash/hashMapChaining/hashMapChaining-guide.mdx) -->

### 왜 모순 없이 작동하는가

<!-- 3.4 (필수). 직무 세 가지:
     1. 귀납적 정당화가 중심 — 기저 → 귀납 단계. 귀납 단계에서는 (a) 경우의 완전성
        (열거한 선택지 밖의 경우가 없는 이유)과 (b) 각 경우의 최적성/정확성을 논증한다.
        결론 재진술("두 경우를 비교하면 결정된다")은 증명이 아니다.
     2. 학습자가 이해 못 하거나 오해할 포인트를 "여기서 헷갈리기 쉬운 포인트" 문형으로
        명시하고, 오해가 틀렸음을 보이는 구체 반례를 ascii art로 든다.
     3. 엣지 케이스(빈 입력, 크기 1~2, 경계값)에서 아이디어가 정상 작동함을 ```text 블록으로 점검한다. -->

## 아이디어를 코드로 옮기기

<!-- 템플릿 4단계. 아이디어를 그대로 옮긴 "기본 구현" 코드(TypeScript, 가이드 내에서
     명확한 시그니처를 정의 — 원본 .ts에 종속되지 않는다. 코드는 본문 자체로 실행·검증한다) + 설명. 설명은 결정 지점만: 초기값, 인덱스 경계, 순회 방향,
     가드 순서. "가장 실수가 잦은/중요한 줄(부분)은 ~입니다" 문형으로 함정 구현부를
     직접 포인팅한다(템플릿 4.1). 코드 직역 설명 금지. -->

## 더 빠르게 만들 단서 찾기

<!-- 템플릿 5단계. 기본 구현의 실행/자원 사용을 관찰해 최적화 가능한 "근거와 단서"를
     포착한다. ascii art로 낭비 지점을 시각화한다 (예: "이 둘만 읽는다", "통째로 후진").
     3.2 이론 절이 있었다면 그 이론이 이 단서의 근거임을 연결한다. -->

## 단서를 최적화로 연결하기

<!-- 템플릿 6단계. 포착한 단서를 최적화 아이디어로 변환한다. before/after ascii art,
     불변식(슬라이딩 약속 등)을 명시한다. -->

## 최적화 코드

<!-- 템플릿 7단계. 최적화된 코드 + 설명. 기본 구현과 달라진 부분이 좁으면 diff/해당
     부분만 보여도 된다(동일 코드 블록 전체 재게재 금지). "가장 중요한 줄" 포인팅과
     실수 시나리오(순서 바꾸면 어떻게 조용히 틀리는지), 기억법 한 줄을 포함한다.
     절 끝에서 템플릿 8단계를 수행한다: 추가 최적화 여지를 검토해
     — 있으면 5~7단계 절 묶음을 반복하고,
     — 없으면 하한 논증 등 근거와 함께 종료를 선언한다. 더 빠른 "특화 해법"이
       존재하면 존재만 언급하고 이 알고리즘의 가치(범용성 등)를 짚는다. -->

## 실행 시각화

<!-- 템플릿 9단계. 새로 만들지 않는다 — 같은 주제의 기존 가이드(*-guide.mdx)에 있는
     `export const steps`와 <AlgorithmSimulation .../> 호출부를 copy한 뒤, 본문 서사와
     프레임 값이 1:1로 맞는지 대조한다(불일치 시 서사를 steps에 맞춘다).
     기존 시뮬이 없으면 simulation-scaffold.md 규격으로 새로 작성하되 실제 실행 값으로 검증한다.
     도입 문단에 고정 입력·패널 읽는 법·실제 반환값을 명시하고,
     호출부 바로 위에 "> 대화형 시뮬레이션은 MDX 런타임에서 표시됩니다." 안내를 둔다. -->

## 흐름 한눈에 보기

<!-- 템플릿 10단계. mermaid 다이어그램. 기존 가이드의 블록을 copy하되 **반드시
     `bun run tools/guide-preview/check-mermaid.ts <이 파일>`로 파서 검증을 통과시킨다**
     — 기존 가이드의 블록에도 구문 오류가 있을 수 있다(실제 사례: stadium 노드 안 대괄호).
     특수문자([, ], (, ), <, >)가 든 라벨은 큰따옴표로 감싼다: (["return nums[0]"]), {"질의 (l,r)?"} -->

## 스스로 점검하기

<!-- 톤 규칙(확인 문제 마무리). 2~3개. ① 손으로 따라가는 계산 문제(답 병기 또는 힌트),
     ② 함정을 건드리는 문제(왜 틀리는지 추적), ③ 변형/전이 문제(원형 배열이면? 같은 틀로
     다른 질의를 풀려면?). 본문을 정독해야 풀리는 문제여야 한다. -->
