# 자료구조 가이드 캔버스 (ORD-003 템플릿)

자료구조 가이드의 골격이다. **문제에 대응하는 가이드가 아니라, 자료구조 그 자체에 대한
독립 가이드**로 작성한다(특정 문제의 입출력에 종속되지 않는다). `##`/`###` 헤딩의 문구와
순서는 고정이다. 모범 예시(필독): `src/data-structures/tree/bPlusTree/bPlusTree-study-guide.mdx`.

**조건부 절 규칙**: 알고리즘 캔버스와 동일 — 해당 없으면 문서에서 절을 생략하고,
사유 한 줄을 품질 게이트 보고에 기록한다.

**다이어그램**: 이 템플릿에는 mermaid 다이어그램 단계가 없다. 필요하다고 판단되면 사용자
확인 후에만 추가하고, 추가 시 check-mermaid.ts 파서 검증을 통과시킨다.

---

```
import { AlgorithmSimulation } from "#guide-sim";
```

# {자료구조 이름} — {직관을 담은 부제}

<!-- 부제 직무: 이 자료구조의 설계 핵심을 한 구절로. 예: "데이터는 리프에만, 리프는 한 줄로 꿰기" -->

## 한눈에 보는 컨셉

<!-- 템플릿 1단계. 3문장 이하. 무엇을 하는 구조인지 + 비결 한 가지 + 대표 효과(복잡도/실사용처). -->

## 언제, 왜 쓰는가

<!-- 템플릿 2단계의 컨테이너. -->

### 이런 단서가 보이면

<!-- 2.1. 직무: 문제에서 어떤 근거·단서가 보일 때 이 자료구조를 떠올려야 하는지
     신호 목록(불릿 2~4개)으로 제시하고, 그때의 효과(복잡도 개선)를 보인다.
     naive 대안들과의 비용 비교를 ```text 표/ascii로 체감시킨다("시소 관계를 끊는다" 등).
     비슷한 용도의 다른 자료구조가 있으면 선택 기준(언제 이것, 언제 저것)을 한 문장 포함한다. -->

### 문제 사례와 적용 예시

<!-- 2.2. 직무: 실제 적용 사례 2~4개(실무 시스템 + 코딩 테스트 맥락). 각 한두 문장. -->

## 자료구조 자세히 들여다보기

<!-- 템플릿 3단계의 컨테이너. -->

### 전체 스펙: 속성과 액션

<!-- 3.1. 직무: 스펙을 빠짐없이 설명한다.
     - 속성(내부 상태): 노드/배열 구조를 코드 블록으로, 지켜야 할 불변식을 번호 목록으로.
       핵심 불변식은 LaTeX로 정식 서술하고, 구조 전체를 ascii art로 그린다.
     - 액션(연산): 이 자료구조가 제공하는 **모든 연산**을 표(연산/하는 일/비용)로 나열하고,
       핵심 연산 1~3개는 경로/변형 과정을 LaTeX + ascii art(before/after)로 자세히 보인다.
     가이드는 **일관된 인터페이스(시그니처·인덱싱 관례·기본값)를 스스로 정의**하고 서술과 일치시킨다
     (sibling `.ts`에 종속되지 않는다). -->

### 왜 모순 없이 작동하는가

<!-- 3.2 (필수). 직무:
     1. 각 액션이 불변식을 보존함을 귀납적으로 논증한다(핵심 불변식 중심).
     2. "여기서 헷갈리기 쉬운 포인트" 문형으로 오해 지점을 명시하고 반례/그림으로 해소한다.
     3. 엣지 케이스(빈 구조, 크기 1, 경계 인덱스, 중복/음수 입력 등)를 ```text 블록으로 점검한다.
     중간에 가벼운 확인 질문 1개(즉답 병기)를 두어 독자가 손을 움직이게 한다. -->

## 아이디어를 코드로 옮기기

<!-- 템플릿 4단계. 가이드가 정의한 인터페이스에 맞춘 TypeScript 구현 + 설명(본문 자체로 실행·검증).
     모든 액션을 코드로 싣는 것이 기본이되, 분량이 과도한 보조 연산은 흐름 서술로
     대체할 수 있다(그 사실을 본문에 명시: "분량상 코드 대신 흐름만").
     "가장 중요한 부분은 ~입니다" 포인팅 + 놓치기 쉬운 구현부(순서가 생명인 두 줄,
     경계 비교 연산자, 인덱스 변환 등)를 불릿으로 짚는다. 기억법 한 줄 권장.
     코드 직역 설명 금지. -->

## 실행 시각화

<!-- 템플릿 5단계. 알고리즘 캔버스의 "실행 시각화" 절과 동일 규칙:
     기존 가이드의 steps copy → 서사와 1:1 대조, #guide-sim 위탁, 정적 뷰어 안내 한 줄. -->

## 스스로 점검하기

<!-- 톤 규칙. 2~3개: 손 계산/그리기 문제, 오해를 건드리는 문제(왜 틀리는지), 구현 순서 문제. -->
