## 성능 목표 예측

## 목표 함수

## 핵심 아이디어

<!-- [MUST-B5] 직관 선행: 수식·코드 없이 "왜 이 접근법이 작동하는가"를 인간 친화적으로 1~2문장으로 먼저 작성 -->

### 원형 아이디어와 naive 접근

### 어떤 관찰이 돌파구가 되는가

### 관찰을 형식화: 상태/구조 정의

### 점화식 또는 핵심 연산

### 정당성 — 왜 이것이 옳은가

### 단계별 코드 진화
<!-- [MUST-B6] 원형(brute force) + 최소 1단계 개선 + 최종 구현의 코드(또는 핵심 스니펫) + 각 전환의 이유.
     진화 단계가 자연스럽지 않은 문제면 기술적 근거를 포함한 생략 메모 작성. 단순 "해당 없음"은 미흡. -->

### 구현 디테일과 최적화
<!-- [IF-APPLICABLE-F3] 공간/시간 최적화가 있으면: 현재 상태가 이전 상태의 어느 범위에만 의존하는지
     의존성 범위를 명시해 최적화가 왜 유효한지 설명. 최적화 없는 문제면 이 절 생략 가능. -->

### 함정과 오해
<!-- [MUST-D6] 잘못된 코드 스니펫 + 그 코드의 구체적 잘못된 결과 예시 최소 1개.
     함정이 없는 문제면 기술적 근거를 포함한 생략 메모 작성. 단순 "해당 없음"은 미흡. -->

## 시뮬레이션

import 한 줄(`import { AlgorithmSimulation } from "#guide-sim";`) 후, 알고리즘 전개를
단계별로 보여주는 선언형 `steps`와 프리셋 `view`로 `<AlgorithmSimulation … />`를 렌더한다.
프레임 스키마와 작성 규칙은 `simulation-scaffold.md`를 따른다. (JSX/Hook 직접 작성 금지)

> 정적 뷰어 대비: 시뮬레이션 호출부 바로 위에 한 줄 안내
> "대화형 시뮬레이션은 MDX 런타임에서 표시됩니다." 를 둔다.

## 수도 코드와 Activity Diagram
