# 이해 시험 V1~V7

- 대상: `sandbox/algo-guide-v2/pilot/quicksort/quicksort-guide.md`
- 회차: 1
- 절제: purpose.alt
- 응답 모델: haiku (결합 = AND)
- 판정: 미통과 — V2
- **잠정** — haiku 단독 판정이다. codex 복구 후 재판정 대상에 등록한다.

| # | 판정 | haiku |
| --- | --- | --- |
| V1 | 통과 | PASS |
| V2 | 미통과 | 근거 없음 |
| V3 | 통과 | PASS |
| V4 | 통과 | PASS |
| V5 | 통과 | PASS |
| V6 | 통과 | PASS |
| V7 | 통과 | PASS |

---

## 모델 응답 원문

### codex — 무응답

```
codex 실행 실패 — 사용량 한도이거나 인증 만료일 수 있습니다.
ERROR: You've hit your usage limit. Upgrade to Plus to continue using Codex (https://chatgpt.com/explore/plus), or try again at Sep 14th, 2026 4:36 PM.
```

### agy — 무응답

```
Error: Individual quota reached. Please upgrade your subscription to increase your limits. Resets in 38h33m31s.
```

### haiku

```
각 질문을 순서대로 검토하겠습니다.

[V1] PASS
> 인용: "가장 단순한 방법은 '가장 작은 것을 찾아 맨 앞에 놓기'를 되풀이하는 것이다... n = 50,000 → 1,249,975,000 번 ... 12억 번이다. 단순 연산을 초당 1억 번으로 잡아도 **10초가 넘는다.**"

[V2] 근거 없음
> "다른 절차"의 실명이 부재합니다. 본문에는 selection sort와 quicksort의 복잡도 패턴(O(n²) vs O(n log n))만 있고, 같은 입력 크기(예: n=50,000)에서 구체적 비교 횟수(selection sort 1,249,975,000번 vs quicksort ?번)가 없습니다.

[V3] PASS
> 인용: "**주장.** 분할 루프가 끝나면 `arr[lo..i-1]`의 모든 값이 `pivot`보다 작고, `arr[i..hi-1]`의 모든 값이 `pivot` 이상이다. **논증.** 1. **루프 시작 전** — ... 공허하게 참이다. 2. **한 바퀴 돌 때** — ... 둘 다 유지된다. 3. **루프가 끝났을 때** — ... 전체에 대해 참이다." 엣지 케이스(빈 배열, 원소 하나, 전부 같음)의 처리도 표로 제시됨.

[V4] PASS
> 인용: "**오해: '이미 정렬된 배열이 퀵 정렬의 최악이다.'** ... 정렬된 [0 1 2 3 4 5 6 7] 13번 ... 최악 [0 3 5 7 1 4 2 6] 28번 ... **두 배 넘게 차이가 난다.**"

[V5] PASS
> 인용: "분할 루프의 `i++` **한 줄만** 지워 보자... `quickSort([5, 2, 3, 1]) → [2, 3, 5, 1] (옳은 답 [1, 2, 3, 5])`"

[V6] PASS
> 인용: "| 단계 | 조건 판정 | 배열 | | T1 | `hi-lo = 3 ≥ 1` 이라 계속 | `[5 2 3 1]` | | T3 | `5 < 2` 가 **거짓** → ② | `[5 1 3 2]` | | T4 | `1 < 2` 가 **참** → ①, `i` 0→1 | `[1 5 3 2]` | ... `①`은 T4에서, `②`는 T3·T5·T10에서 밟았다. 코드의 두 분기가 모두 나왔다."

[V7] PASS
> 인용: "| 최악의 경우 | `O(n²)` | `Θ(n²)` — 실제로 도달한다 | | 평균적인 경우 | `O(n log n)` | `Θ(n log n)` | ... 최악에서 `Θ(n²)`라고 쓸 수 있는 것은 그 값을 **실제로 내는 입력이 있기** 때문이다... **'평균'이 무엇에 대한 평균인지가 중요하다.**... 여기서 말하는 평균은 **입력 순열이 고르게 분포한다는 가정 아래의 평균**이지, 구현이 만드는 무작위성에 대한 기댓값이 아니다."

```
VERDICT: FAIL
```

**V2가 충족되지 않아 전체 판정이 FAIL입니다.** V2는 "같은 입력의 수치로" quicksort가 selection sort를 이기는 것을 보여야 하는데, 본문에는 같은 입력 크기에서의 구체적 비교 횟수가 없습니다.
```

