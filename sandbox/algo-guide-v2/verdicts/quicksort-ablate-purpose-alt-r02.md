# 이해 시험 V1~V7

- 대상: `sandbox/algo-guide-v2/pilot/quicksort/quicksort-guide.md`
- 회차: 02
- 절제: purpose.alt
- 응답 모델: sonnet (결합 = AND)
- 판정: 미통과 — V2
- **잠정** — sonnet 단독 판정이다. codex 복구 후 재판정 대상에 등록한다.

| # | 판정 | sonnet |
| --- | --- | --- |
| V1 | 통과 | PASS |
| V2 | 미통과 | 근거 없음 |
| V3 | 통과 | PASS |
| V4 | 통과 | PASS |
| V5 | 통과 | PASS |
| V6 | 통과 | PASS |
| V7 | 통과 | PASS |

---

## 모델 응답 원문

### codex — 무응답

```
codex 실행 실패 — 사용량 한도이거나 인증 만료일 수 있습니다.
ERROR: You've hit your usage limit. Upgrade to Plus to continue using Codex (https://chatgpt.com/explore/plus), or try again at Sep 14th, 2026 4:36 PM.
```

### agy — 무응답

```
Error: Individual quota reached. Please upgrade your subscription to increase your limits. Resets in 25h26m17s.
```

### sonnet

```
**종합 결론: FAIL (V2가 `근거 없음`이라 전부 PASS 아님)**

[V1] PASS
> 인용: "가장 단순한 방법은 "가장 작은 것을 찾아 맨 앞에 놓기"를 되풀이하는 것이다."
> 인용: "n = 50,000 → 1,249,975,000 번"
> 인용: "12억 번이다. 단순 연산을 초당 1억 번으로 잡아도 **10초가 넘는다.**"

[V2] 근거 없음
> 인용: 없음 — 본문 전체에서 "다른 절차 실명 + 같은 입력의 수치 대조"가 없다. 유일하게 다른 알고리즘을 실명으로 부른 대목은 "병합 정렬이 비교를 더 적게 하는데도 이 문제에서 퀵을 고르는 근거는 무엇이었는가?"(「스스로 점검하기」)인데, 이건 독자에게 던지는 미답 질문이지 본문이 수치로 대조해 답한 것이 아니다. "힙 정렬로 갈아탄다"(Go pdqsort), "TimSort"(V8 sort) 언급도 이름만 있고 수치 비교가 없다.

[V3] PASS
> 인용: "분할 루프가 끝나면 `arr[lo..i-1]`의 모든 값이 `pivot`보다 작고, `arr[i..hi-1]`의 모든 값이 `pivot` 이상이다."
> 인용: "`arr[j] < pivot`이면 `arr[i]`와 맞바꾸고 `i`를 하나 늘린다. 새로 작은 구역에 들어간 값은 `arr[j]`이고 조건상 `pivot`보다 작다. 밀려난 `arr[i]`는 큰 구역의 첫 자리로 가는데, 그 값은 직전까지 큰 구역에 있었으므로 `pivot` 이상이다."
> 인용: "빈 배열 `[]` | `hi = -1`, `lo = 0` → `hi - lo = -1 < 1` | 즉시 반환"

[V4] PASS
> 인용: "오해: "이미 정렬된 배열이 퀵 정렬의 최악이다.""
> 인용: "정렬된 입력은 13번인데 진짜 최악은 28번이다. **두 배 넘게 차이가 난다.**"

[V5] PASS
> 인용: "quickSort([5, 2, 3, 1])           → [2, 3, 5, 1]      (옳은 답 [1, 2, 3, 5])"

[V6] PASS
> 인용: "T4 | `j=1` | `1 < 2` 가 **참** → ①, `i` 0→1 | `[1 5 3 2]`"
> 인용: "`①`은 T4에서, `②`는 T3·T5·T10에서 밟았다. 코드의 두 분기가 모두 나왔다."

[V7] PASS
> 인용: "최악에서 `Θ(n²)`라고 쓸 수 있는 것은 그 값을 **실제로 내는 입력이 있기** 때문이다. n=8에서 `[0 3 5 7 1 4 2 6]`이 정확히 `28 = 8·7/2`번 비교한다."
> 인용: "그러니 여기서 말하는 평균은 **입력 순열이 고르게 분포한다는 가정 아래의 평균**이지, 구현이 만드는 무작위성에 대한 기댓값이 **아니다.**"

```
VERDICT: FAIL
```
```

