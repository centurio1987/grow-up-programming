# 이해 시험 V1~V7

- 대상: `sandbox/algo-guide-v2/pilot/mosAlgorithm/mosAlgorithm-guide.md`
- 회차: 1
- 절제: purpose.alt
- 응답 모델: haiku (결합 = AND)
- 판정: 통과
- **잠정** — haiku 단독 판정이다. codex 복구 후 재판정 대상에 등록한다.

| # | 판정 | haiku |
| --- | --- | --- |
| V1 | 통과 | PASS |
| V2 | 통과 | PASS |
| V3 | 통과 | PASS |
| V4 | 통과 | PASS |
| V5 | 통과 | PASS |
| V6 | 통과 | PASS |
| V7 | 통과 | PASS |

---

## 모델 응답 원문

### codex — 무응답

```
codex 실행 실패 — 사용량 한도이거나 인증 만료일 수 있습니다.
ERROR: You've hit your usage limit. Upgrade to Plus to continue using Codex (https://chatgpt.com/explore/plus), or try again at Sep 14th, 2026 4:36 PM.
```

### agy — 무응답

```
Error: Individual quota reached. Please upgrade your subscription to increase your limits. Resets in 38h13m37s.
```

### haiku

```
각 질문을 순서대로 검토합니다.

---

[V1] PASS
> 인용: "질의마다 그 구간을 처음부터 다시 훑어 집합에 넣는다. 구간 하나가 최대 `n` 칸이니 이렇게 쌓인다. 질의 하나에 최대 n 칸  →  q × n = 10^4 × 10^4 = 100,000,000 칸"

---

[V2] PASS
> 인용: "같은 입력에서 **43,409칸**이다. 블록+`r` 정렬(6,895칸)의 **6.3배**다" 및 "같은 입력(`n = 400`, `q = 200`)에서 포인터 이동 칸 수는 **`l` 만 정렬 18,609칸, 블록+`r` 정렬 6,895칸**이다."

---

[V3] PASS
> 인용: "창이 비어 있을 때(`curL = 0`, `curR = -1`) 맵도 비어 있고 `distinct = 0` 이라 성립한다. 그 뒤 창은 **한 칸씩만** 바뀌고, 한 칸이 바뀔 때 `add` 나 `remove` 가 정확히 한 번 불린다. ... 그러니 매 칸 이동 뒤에도 두 문장이 유지된다." 및 엣지 케이스 표 제시 (질의 0개, l=r, 모든 값이 같음, n=1)

---

[V4] PASS
> 인용: "오해: '왼쪽 끝으로 정렬하면 충분하다.' ... 반례: `l 만 정렬  [0,4] [0,1] [1,3] [1,0]…   r: 4 → 1 → 3 → …   왕복한다`" 및 "같은 입력(`n = 400`, `q = 200`)에서 포인터 이동 칸 수는 **`l` 만 정렬 18,609칸, 블록+`r` 정렬 6,895칸**이다."

---

[V5] PASS
> 인용: "`if (next === 1)` 을 지웠다" 후 결과 "바른 코드   [3, 2, 3, 2] / 조건 지움   [6, 3, 6, 4]      ← 전부 부풀었다"

---

[V6] PASS
> 인용: 입력 `[1, 1, 2, 1, 3], [[0,4], [0,2], [2,4], [1,3]]` 에서 상세 전개 표 (T1~T13), 각 단계의 조건 판정 (`curR = -1 < 2` 참 → ①, `curL = 1 > 0` 참 → ②, 등), 창의 상태값, 그리고 모든 분기 (①: 6회, ②: 3회) 실행 확인

---

[V7] PASS
> 인용: "총식 = O(q log q)  +  O((n + q)√n)  +  O(이동 칸 수 × 맵 1회)" 및 "| | 상한 `O` | 타이트 `Θ` |" 표 (경계 종류 구분) 및 "창 이동 상한은 **최악 보장**이지만, 셈 갱신에 쓰는 해시 맵의 한 번 두드리기는 **평균 O(1)** 이다" (보장 종류 구분)

---

```
VERDICT: PASS
```
```

