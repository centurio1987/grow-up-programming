# 이해 시험 V1~V7

- 대상: `sandbox/algo-guide-v2/pilot/quicksort/quicksort-guide.md`
- 회차: 02
- 절제: 없음
- 응답 모델: sonnet (결합 = AND)
- 판정: 통과
- **잠정** — sonnet 단독 판정이다. codex 복구 후 재판정 대상에 등록한다.

| # | 판정 | sonnet |
| --- | --- | --- |
| V1 | 통과 | PASS |
| V2 | 통과 | PASS |
| V3 | 통과 | PASS |
| V4 | 통과 | PASS |
| V5 | 통과 | PASS |
| V6 | 통과 | PASS |
| V7 | 통과 | PASS |

---

## 모델 응답 원문

### codex — 무응답

```
codex 실행 실패 — 사용량 한도이거나 인증 만료일 수 있습니다.
ERROR: You've hit your usage limit. Upgrade to Plus to continue using Codex (https://chatgpt.com/explore/plus), or try again at Sep 14th, 2026 4:36 PM.
```

### agy — 무응답

```
Error: Individual quota reached. Please upgrade your subscription to increase your limits. Resets in 25h28m6s.
```

### sonnet

```
[V1] PASS
> 인용: "가장 단순한 방법은 "가장 작은 것을 찾아 맨 앞에 놓기"를 되풀이하는 것이다. 매번 남은 구간을 전부 훑어야 하므로 비교 횟수가 이렇게 쌓인다." / "n = 50,000 → 1,249,975,000 번" / "12억 번이다. 단순 연산을 초당 1억 번으로 잡아도 **10초가 넘는다.**"

[V2] PASS
> 인용: "같은 목표를 노리는 다른 절차로 **병합 정렬**이 있다." / "같은 입력 `[23 4 91 8 15 42 7 66 30 1 58 12 77 25 3 49]`(n=16)에 둘을 걸고 센 값이다." / "**비교는 병합이 이긴다.** 49 대 52다. 퀵이 이기는 자리는 그 옆 칸이다 — 병합은 합치는 단계마다 새 배열을 잡아 **64칸**을 쓰고, 퀵은 **0칸**으로 끝낸다."

[V3] PASS
> 인용: "1. **루프 시작 전** — `i = lo`이고 `j = lo`다. 두 구역이 모두 비어 있으니 "모든 원소가 …"는 공허하게 참이다." / "빈 배열 `[]` | `hi = -1`, `lo = 0` → `hi - lo = -1 < 1` | 즉시 반환" / "전부 같음 `[2 2 2 2 2]` | `arr[j] < pivot`이 한 번도 참이 아님 | **정답이나 가장 느림**"

[V4] PASS
> 인용: "오해: "이미 정렬된 배열이 퀵 정렬의 최악이다."" / "정렬된 [0 1 2 3 4 5 6 7]        13\n역순   [7 6 5 4 3 2 1 0]        15\n최악   [0 3 5 7 1 4 2 6]        28   ← n(n-1)/2 상한" / "정렬된 입력은 13번인데 진짜 최악은 28번이다. **두 배 넘게 차이가 난다.**"

[V5] PASS
> 인용: "quickSort([5, 2, 3, 1])           → [2, 3, 5, 1]      (옳은 답 [1, 2, 3, 5])"

[V6] PASS
> 인용: "T4  [1 5 3 2]  j=1  1<2 참   → ①  i=0→1" / "`①`은 T4에서, `②`는 T3·T5·T10에서 밟았다. 코드의 두 분기가 모두 나왔다."

[V7] PASS
> 인용: "최악의 경우 | `O(n²)` | `Θ(n²)` — 실제로 도달한다" / "그러니 여기서 말하는 평균은 **입력 순열이 고르게 분포한다는 가정 아래의 평균**이지, 구현이 만드는 무작위성에 대한 기댓값이 **아니다.**"

```
VERDICT: PASS
```
```

