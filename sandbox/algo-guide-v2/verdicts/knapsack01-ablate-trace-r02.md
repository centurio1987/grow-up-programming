# 이해 시험 V1~V7

- 대상: `sandbox/algo-guide-v2/pilot/knapsack01/knapsack01-guide.md`
- 회차: 02
- 절제: trace
- 응답 모델: sonnet (결합 = AND)
- 판정: 통과
- **잠정** — sonnet 단독 판정이다. codex 복구 후 재판정 대상에 등록한다.

| # | 판정 | sonnet |
| --- | --- | --- |
| V1 | 통과 | PASS |
| V2 | 통과 | PASS |
| V3 | 통과 | PASS |
| V4 | 통과 | PASS |
| V5 | 통과 | PASS |
| V6 | 통과 | PASS |
| V7 | 통과 | PASS |

---

## 모델 응답 원문

### codex — 무응답

```
codex 실행 실패 — 사용량 한도이거나 인증 만료일 수 있습니다.
ERROR: You've hit your usage limit. Upgrade to Plus to continue using Codex (https://chatgpt.com/explore/plus), or try again at Sep 14th, 2026 4:36 PM.
```

### agy — 무응답

```
Error: Individual quota reached. Please upgrade your subscription to increase your limits. Resets in 17h41m7s.
```

### sonnet

```
일곱 항목 모두 PASS이며 종합 판정도 PASS입니다.

[V1] PASS
> 인용: "곧바로 떠오르는 방법은 모든 조합을 만들어 보는 것이다. 넷이면 16가지라 손으로도 세지만, 백이면 `2^100` 이라 사람이 셀 수 있는 크기가 아니다."
> 인용: "물건 100개 → 2^100 ≈ 1.27 × 10^30 가지 / 1초에 10^9 가지를 본다 해도 / 1.27 × 10^21 초 ≈ 4 × 10^13 년 ← 우주 나이보다 길다"

[V2] PASS
> 인용: "같은 답을 내는 완전히 다른 표가 있다 — 가치 축 DP 다."
> 인용: "무게 축 DP (이 가이드) 51,051 / 가치 축 DP 617,552 / 무게 축이 이긴다. 12.1배 적다."

[V3] PASS
> 인용: "기저는 $dp[0][c] = 0$ 이다 — 물건이 하나도 없으면 고를 것이 없고, $c$ 가 무엇이든 가치 합은 0이다. ... 최적해는 두 값 중 하나 이하이므로 $dp[i][c] \le \max(\cdot,\cdot)$ 이고, 반대로 두 값은 각각 실제로 만들 수 있는 해의 가치이므로 $dp[i][c] \ge \max(\cdot,\cdot)$ 다. 따라서 같다."
> 인용: "`weights=[]`, `values=[]`, `W=10` | 0번째 줄만 있고 루프가 안 돈다 | `0`"

[V4] PASS
> 인용: "오해: '가치/무게 비율이 높은 것부터 담으면 된다.'"
> 인용: "그리디 답 = 4 ✗ ... 정답 = 5 ✓"

[V5] PASS
> 인용: "바른 코드 13 / 한 줄 바꿈 14 ← 있지도 않은 가치가 1 더 나왔다"

[V6] PASS
> 인용: "두고 간다 → dp[2][7] = 5 / 담는다 → dp[2][7-4] + 5 = dp[2][3] + 5 = 4 + 5 = 9 ... 큰 쪽 9 를 적는다" (분기③)
> 인용: "3번째 물건은 무게 4, 용량은 2 → 2 < 4 라 못 담는다 / dp[3][2] = dp[2][2] = 1" (분기①)
> 인용: "본문 에서 skip 과 take 가 둘 다 5 였다." (분기②, 동점에서 skip 채택)

[V7] PASS
> 인용: "이 알고리즘은 입력을 보고 일찍 끝내는 자리가 없어 표 `(n+1)(W+1)` 칸을 언제나 한 번씩 채운다."
> 인용: "보장의 종류는 최악 보장이다. 기대값이 아니다 — 코드에 무작위성이 없고 입력 분포를 가정하지도 않는다."

```
VERDICT: PASS
```

가장 약한 고리는 V6입니다. 분기② 사례(`dp[3][4]`, skip=take=5)가 본문에서 직접 유도되지 않고 "스스로 점검하기"의 자가확인 문항이 그 값을 사실로 되짚는 방식으로만 등장합니다. 표 자체에는 `dp[3][4]=5`가 있고 `skip = dp[2][4] = 5`, `take = dp[2][0]+5 = 5`도 앞서 나온 표 값들로 계산 가능하지만, 그 계산 과정을 명시적으로 써 놓은 문장은 없습니다. 이 정도면 근거로 인정할 만하다고 판단해 PASS로 매겼지만, 더 엄격한 기준을 적용한다면 이 항목만 `결론만 있음`으로 내릴 수 있는 자리입니다.
```

