# 유저 지적 → 규칙 → 강제 지점

**이 파일이 반영 상태의 정본이다.** 지적 원문은 편별 `pilot/<이름>/feedback.md` 에 그대로
두고, 여기에는 **그 지적이 어느 규칙이 되었고 무엇이 그것을 강제하는가**만 적는다.

> **왜 따로 두는가.** 지적 원문과 회차별 서술은 `pilot/mosAlgorithm/feedback.md` 에 288줄로
> 쌓여 있었는데, ① 파일럿 한 편 안에 있어서 다른 편을 쓸 때 읽히지 않았고 ② 시간순 서술이라
> "이 지적이 규칙으로 올라갔나" 를 확인하려면 전부 읽어야 했다. 실제로 **R9 반영이 `SPEC.md`
> 에 안 올라간 채로 하루가 지났다** — 2026-08-27 유저가 "기록을 충분히 하고 있냐" 고 물어서
> 확인하다 발견했다.

## 1. 반영 상태

`강제`가 **P**·**도구**면 기계가 잡는다. **검토**면 사람이 봐야 하고, 그 목록이 §3 이다.

| # | 지적 요지 | 규칙 | 강제 |
| --- | --- | --- | --- |
| R2-1 | `B` 의 정의와 정렬 과정이 없다 | `L21`·`L22` | P7 + 검토 |
| R2-2 | 은유 '서다' 를 쓰지 마라 | `L24` | **P2** |
| R2-3·4 | `trace`·`code` 배치 | `SPEC` §1 order | 구조 |
| R3-1 | 정의와 그림이 어긋난다 | `L21` | 검토 |
| R3-2 | `q` 가 정의 없이 나온다 | `L21` (기호표) | 검토 |
| R3-3 | 정렬을 과정으로 보여라 | `L22` | 검토 |
| R3-4 | 정렬 후 포인터 동작 묘사 | `L22` | 검토 |
| R3-5 | 전개에 ascii 와 코드를 충분히 | `L22` | **P7** |
| R3-6 | 놓치기 쉬운 지점에서 멈춰라 | `L5` | **P3** (멈춤 ≥ 1) |
| R3-7 | 절이 끊기지 않고 이어져야 | 규칙 폐기 (R19-1) | **게이트 제거** |
| R3-9 | 수식에 매몰되지 마라 | 규칙 폐기 (R19-1) · `L22` 가 일부 | 검토 |
| R4 | 굴려보기·코드·오해 항목 제거 | `SPEC` §1 | **P3** + `section.ts` |
| R5 | 제거는 이름까지다 | `SPEC` §1 | **P3** (폐기 이름) |
| R6 | 은유 부류를 전부 | `L24` | **P2** |
| R7 | 활용형과 검사 범위 | `L24` | **P2** + `check-metaphor` |
| R8 | 지적 전에 부류를 전수로 | `L24` | **P2** (11부류) |
| R9 | 개념은 `deep.build`, 상태 유지 동작을 값으로 | 규칙 폐기 (R19-1) · `deep.build` 직무 | 구조 |
| R10 | 한 대상을 두 표기로 적지 마라 | `L25` | **P2** |
| R11 | 기록이 충분한가 | `FEEDBACK.md` 신설 | 이 파일 |
| R12 | 아이디어 상세가 끊기고 수식으로 넘어간다 | 규칙 폐기 (R19-1) · `deep.build` ⑦ | 구조 |
| R13 | 수식 절은 아이디어 상세와 **별개**로, 수식에만 집중 | 규칙 폐기 (R19-1) · `deep.math` `##` 승격 | 구조 |
| R14 | 정렬 키가 뒤에서 다르게 나타난다 | `L25` (라벨 좌표) | **P2** |
| R15 | 「왜 항상 옳은가」 이름을 바꿔라 | `SPEC` §1 — `## 정확성 논증` | 구조 |
| R16-1·2 | 정렬의 기준 없이 정렬 결과부터 제시한다 | 규칙 폐기 (R19-1) · `deep.build` ⑤ | **게이트 제거** + 구조 |
| R16-3 | 개념 그림에 실제 입력이 없어 좌표축이 데이터로 읽힌다 | 규칙 폐기 (R19-1) · `deep.build` ⑥ | 구조 |
| R16-4 | 포인터 성질만 있고 기준을 적용하는 과정이 없다 | 규칙 폐기 (R19-1) · `deep.build` ⑥ | 구조 |
| R16-5 | 묶으면 같은 구역 안 `l` 변화가 무시되는 것으로 읽힌다 | 규칙 폐기 (R19-1) | **게이트 제거** |
| R17-1 | 절 제목이 핵심 개념이 아닌 **수단**을 가리킨다 | 규칙 폐기 (R19-1) · `deep.build` 제목 규격 | **게이트 제거** + 구조 |
| R17-2 | 준비 걸음이 절의 대부분을 차지해 핵심이 뒤로 밀린다 | 규칙 폐기 (R19-1) · 직무 재구성(일곱 → 여섯) | **게이트 제거** + 구조 |
| R17-3 | 파생 값 `(blk(l), r)` 이 새 질의로 읽힌다 | 규칙 폐기 (R19-1) · `deep.build` ⑤ | **게이트 제거** + 구조 |
| R18 | 반영이 재작성이 아니라 **재배치**였다 | 재작성률 기준 | **도구** (`check-rework`) |
| R19-1 | 지적에 **대답하듯** 쓴 문장이 본문에 들어와 읽기 어려워졌다 | `L28`~`L31` 폐기 | **게이트 제거** |
| R19-2 | 내용·의미·설득력 게이트를 걷어내고 **결정론적인 것만** 남겨라 | `SPEC` §6 재정의 | **게이트 제거** |
| R19-3 | 아이디어 상세 재작성. 분량·항목 간 연계는 고려 대상이 아니고 **기호만 일치** | `L21`·`L25` | **P2** + 계산 |
| R19-4 | 파트 부제가 **의미를 알 수 없는 표현**이다 | **`L32`** (헤딩 존댓말 종결 금지) | **P2** (P3d) |
| R19-5 | **단의적 표현 우선 · 자주 쓰는 표현 우선** | **`L33`** + `METAPHORS` 다의어 편입 | **P2** 일부 + 검토 |
| R20 | 경쟁 설계 대조는 **저울질할 수 있는 상대가 있을 때만** | **`L34`** + `purpose.alt` 조건부화 | **P10** + 검토 |
| R21 | 알면 좋은 개념을 **있을 때만** 파트 1 마지막에 | **`L35`** + `related` 신설 | **P7**·**P8** + 검토 |
| R22 | 수식이 **이해에 기여하지 않으면** 싣지 않는다 | **`L36`** + `deep.math` 조건 둘 | 검토 |
| R23 | HTML 에 **항목 레일을 우측에** | **`L37`** + `build-html.ts` 레일 | **빌드 스모크** |

**`R19-4`·`R19-5` 는 2026-08-28 자체 점검에서 표에 빠진 것을 찾아 넣었다.** 규칙은 그날
`SPEC` 에 이미 올라가 있었는데(`SPEC.md:88`·`SPEC.md:224` 에 지적 원문이 있다) 이 표에만
없었다 — R11 이 이 파일을 만든 이유가 바로 그 상태다. **회차 안에서의 정확한 위치는
이력에 없어서** 인용이 실린 날짜로 R19 뒤에 붙였다.

**규칙 열이 없는 번호를 가리키던 것을 고쳤다(2026-08-28).** `SPEC.md` 가 `L23`·`L26`~`L31` 을
결번으로 선언했는데 이 표의 열넷이 그 번호를 계속 가리켰고, `R16`·`R17` 넉 줄은 이미 걷어낸
게이트(`criterionOrder`·`coreConcept`)를 강제 지점으로 적고 있었다. 지금은 **살아 있는 것만**
적는다 — `deep.build` 직무 번호와 `deep.math` 승격은 `SPEC` §1 구조가 지고, 그 밖은 폐기다.
**폐기 사실 자체는 `R19-1` 행과 §2 「걷어낸 것」이 진다** — 그 두 자리에서 번호를 지우면
결번 선언의 근거가 사라지므로 손대지 않는다. 그래서 §1 에서 결번 번호를 찾으면 `R19-1` 한 줄과
이 문단에만 남는 것이 정상이다.

## 2. 기계가 잡는 것

**게이트는 결정론적인 것만 둔다**(R19). 판정이 「이런 문장이 있는가」에 걸리면 글은 그 문장을
끼워 넣는 쪽으로 간다 — 실제로 그렇게 됐고, 그것이 R19-1 이다.

| 강제 지점 | 무엇을 잡는가 | 회귀 시험 |
| --- | --- | --- |
| `check-v2.ts` P1 | 절 하나의 연속 산문 문단 ≤ 2 | 2 |
| `check-v2.ts` P2 | 은유 **11부류**(활용형 포함 · 다의어 「돌다」·「닿다」) · 표기 혼용 · **라벨 좌표 정합**. 은유만 **인용 구간(`"…"`·`「…」`)을 뺀 문장**을 본다 | **21** + 2 + **3** |
| `check-v2.ts` P3 | 전개 단계 ≥ 3 · 멈춤 ≥ 1 · 전체 코드 = 1 · 폐기된 항목 이름 · `T#` ≥ 6 | 4 |
| `check-v2.ts` P4 | 전체 코드의 분기 라벨 ⊆ 전개가 실행한 라벨 | 1 |
| `check-v2.ts` P5 | `perf.derive`·`selfcheck` 가 실재하는 `T#` 를 인용한다 | 2 |
| `check-v2.ts` P6·P9 | 마커 id ↔ `.sim.ts` export · `result` 값 일치 | 4 |
| `check-v2.ts` P7 | 그림 의무 · `deep.math` 의 그림 ≥ 1 **그리고** 코드 ≥ 1 | 3 |
| `check-v2.ts` P7c | `related` 가 **있으면** 그림 ≥ 1 (코드는 요구하지 않는다) | 1 |
| `check-v2.ts` P8 | `concept` 이 뒤 절 헤딩·앵커를 참조하지 않는다 | 2 |
| `check-v2.ts` P8b | `related` 의 자리 — `deep.walk` 뒤이고 `part2` 앞인가 | 2 |
| `check-v2.ts` P10 | `purpose.alt` 의 수치 = `bench-alt.ts` 실측값. **절을 생략했는데 실측값만 남은 상태**도 잡는다 | 1 + **2** |
| `check-v2.ts` P11 | 기호 개수 선언(「기호는 여섯」)과 기호표 행 수 일치. 선언이 없으면 미실행 | **4** |
| `check-v2.ts` P12 | 「코드에서는 `block`」 이라 밝힌 이름이 **코드 펜스에 실재**한다. `text` 펜스는 증언으로 안 센다 | **3** |
| `check-v2.ts` P13 | 디스플레이 수식으로 세운 이름을 뒤에서 **정의식으로 되풀어 쓰지** 않는다 | **2** |
| `check-rework.ts` | 구성 지적을 받은 절이 **다시 쓰였는가**. 옛 판에 그대로 있던 줄과 새로 쓴 줄을 갈라 세고 재작성률이 기준 미만이면 실패 | **5** |
| `build-html.ts` 레일 | 파트·항목만 담기 · 라벨 규칙 · **앵커가 절 id 에서 온다** · 반복 절 앵커 분리 · 모르는 헤딩에도 앵커 · JS-off 정적 산출 · 링크가 실재하는 id 를 가리킨다 · **숨김 기준점이 본문·레일 폭에서 유도된다**(리터럴 금지) · 빈 레일 | **9** |
| `check-metaphor.ts` | 은유 — **문서 전체**(`.md`). 인용 블록·인용 문서·**낫표 구간**은 제외. 패턴과 인용 제거는 `check-v2.ts` 것을 그대로 쓴다 | **3** |
| `section.ts` | 폐기된 항목이 헤딩으로 잡히지 않는다(SEC 에러) | 2 |
| `check-proof.ts` | 본문이 내미는 값이 **실행 결과와 글자 그대로** 같은가. 변이는 정본 소스에서 **기계로** 만들고(한 줄에만 맞아야 한다), 사이드카가 정본을 import 하지 않으면 판정력이 없다고 본다 | **10** |

**리터럴로 박은 수가 오답을 지킨 자리 하나**(2026-08-28). 레일 숨김 기준점을 시험이
`78rem` 이라고 그대로 적어 두어서, 그 값이 틀렸는데도 초록으로 통과했다 — 1440x900 화면에서
창을 최대로 켜도 `innerWidth` 가 1232px 이라 레일이 한 번도 안 떴다. 지금은 본문 폭·레일 폭·
가장자리 여백 셋에서 기준점을 **계산해서** 대조한다(`4·edge + 2·railW + mainW`). 셋 중
하나가 바뀌면 기준점이 따라오지 않는 한 시험이 실패한다.

**걷어낸 것**(2026-08-28). `criterionOrder`(L28·L29) · `coreConcept`(L30·L31) 와 회귀 시험
10벌. 넷 다 통과 조건이 **특정 꼴의 문장이 있는가**였다 — 「묶는 것은 … 정확 …」 ·
「이 값은 … 대신하는 것이 아니다」. 검사를 지나려고 그 문장을 넣은 자리가 실제로 본문에
남아 있었고, 유저가 그것을 읽기 어렵다고 지적했다.

## 3. 사람이 봐야 하는 것

기계가 못 잡는 것 중 **판정이 갈리지 않는 것만** 남긴다. 확인 질문의 답이 사람마다 다르면
그 줄은 여기 두지 않는다 — 그런 줄이 열둘이었고 R19 로 전부 걷어냈다.

**2026-08-29 로 셋이 기계로 내려갔다**(`L38`~`L40` · `P11`~`P13`). **2026-08-31 에 `L19` 가 통째로 내려갔다** — `ci.ts` 의 「v2 마커 규약」 단계가 `build-html.ts --all` 로 전수를 빌드한다. 줄을 지우지 않고 **내려간 몫만 뺐다** — 같은 줄에 남은 몫이 있고, 지우면 그것이 아무 데도 안 남는다.

**「옛 `V…`」 로 시작하는 넷은 이해 시험이 걷히면서 내려온 것이다**(2026-08-29 유저 지시,
`SPEC` §0). 걷어낸 규칙을 되살린 것이 아니라, 외부 모델이 보던 몫 중 **실행으로 못 내리는
것**이 여기로 온 것이다. 값을 보던 몫은 `check-proof.ts` 가 진다.

| 규칙 | 확인 질문 |
| --- | --- |
| `L21` | 그 절이 쓰는 기호가 **전부** 앞에서 정의됐는가. 정의와 그림·표가 어긋나는 자리가 있는가. **개수 선언과 표가 맞는지는 `P11` 이 본다** — 표 밖에서 정의한 기호가 실제로 그 자리에서 정의됐는지는 여기 남는다 |
| `L21` | **항목을 가로질러 쓰는 변수·기호가 한 벌인가.** `n`·`q`·`l`·`r`·`curL`·`curR`·`B`·`blk` 가 모든 절에서 같은 것을 가리키는가 |
| `L25` | 정의한 이름(`blk`)을 뒤 절이 그대로 쓰는가, 정의식(`⌊l/B⌋`)으로 풀어 다시 적지 않았는가. **디스플레이 수식으로 세운 정의는 `P13` 이 본다** — 산문으로 세운 정의는 여기 남는다 |
| `L25` | 같은 값의 **코드 식별자가 한 벌인가** — 식 `B` ↔ 코드 `block` 의 대응을 한 자리에서 **밝혔는가**. 밝힌 이름이 코드에 실재하는지는 `P12` 가 본다. 밝힐 자리가 있는데 안 밝힌 것은 여기 남는다 |
| 수치 | 순서에 붙인 이름(*"`l` 오름차순"*)이 그 줄의 실제 순서와 맞는가. **계수가 실행 결과와 같은지는 `check-proof.ts` 가 본다**(2026-08-29 부로 값 대조는 사람 몫이 아니다) |
| `L5` | 멈춤이 **걸리는 그 자리**에 있는가, 절 하나에 몰려 있는가 |
| `L33` | **목록에 없는 다의어가 남아 있는가.** `SPEC` §6 이 이름을 댄 넷 중 `짚다`(확인) · `잡히다`(이해)는 원고 반복 횟수가 1·0 이라 목록에 넣지 않았다 — 그 둘은 사람이 본다 |
| `L32` | **부제가 무엇을 말하는지 알아볼 수 있는가.** 존댓말 종결은 `P3d` 가 잡지만 부제의 뜻은 못 본다 |
| `L34` | 경쟁 설계가 **저울질 대상인가** — 입력·제약을 바꿨을 때 우열이 실제로 뒤집히는 자리를 값이나 조건으로 댔는가. 못 대면 절을 뺀다 |
| `L35` | `related` 가 **본문이 이미 값으로 보인 것**에 이름을 붙이는가. 그 개념이 이 편 밖에서도 쓰이는가. 분류만 하는 문장이 아닌가 |
| `L36` | `deep.math` 가 **본문의 값만으로는 안 나오는 것**을 주는가 — 일반화 · 최적값 풀이 · 닫힌 형태. 기호로 옮겨 적기만 했으면 절을 뺀다 |
| 옛 `V1` | 본문이 세운 「가장 단순한 방법」이 **실제로 가장 단순한가.** 더 단순한 것이 있으면 수치 반박이 헛돈다 |
| 옛 `V3` | 정확성 논증의 각 단계가 **이어지는가.** 값은 기계가 대조하지만 단계와 단계 사이의 연결은 못 본다 |
| 옛 `V4` | 제시한 오해를 **독자가 실제로 하는가.** 아무도 안 하는 오해를 반박하면 멈춤이 자리를 낭비한다 |
| 옛 `V7` | 경계의 종류(**상한인가 타이트한 값인가**)와 보장의 종류(**최악 보장인가 기대값인가**)가 구분돼 있는가 |
| `L37` | 레일이 **실제로 우측에 뜨고 본문과 안 겹치는가.** 마크업·링크·CSS 규칙과 **숨김 기준점**은 기계가 잡지만 **렌더 결과는 못 본다** — 브라우저로 한 번 열어 확인한다. 확인은 **실제 창 폭**에서 한다(2026-08-28: 기준점 1248px 이 최대화한 창 1232px 보다 커서 레일이 한 번도 안 떴다) |
| ~~`L19`~~ | **2026-08-31 로 기계에 내려갔다 — `ci.ts` 의 「v2 마커 규약」 단계(`build-html.ts --all`)가 v2 가이드 전수를 빌드해 판정한다.** 사람이 볼 몫이 남지 않아 이 줄은 그어 둔다. 내려가기 전 상태의 기록: `ci.ts` 가 이 도구를 안 불러 아무것도 강제하지 않았고, 마커 규약 위반은 원고를 빌드해야만 드러나서 **9편에서 `selfcheck` 의 답이 `<details>` 로 안 접힌 채 웹에 그대로 보였다**(2026-08-29 에 기록됐고 2026-08-31 브라우저 실측으로 확인됨) |

## 4. 재발 계보 — 같은 지적이 몇 번 왔는가

| 계보 | 회차 | 매번 무엇이 부족했나 |
| --- | --- | --- |
| 은유 | R2 → R6 → R7 → R8 → **자체 점검(2026-08-28)** | 단어 하나만 → 부류 일부만 → 활용형 누락·범위 좁음 → 부류 넷 더 → **활용형 누락 재발(어미를 하나씩 적은 패턴이 「되짚으면」을 놓쳐 4편에 남아 있었다)** |
| 아이디어 상세 | R2 → R3 → R9 → R12 → R16 → **R17** | 사례 하나만 → 절 신설 안 함 → 개념이 수식 절에 남음 → 절이 끊김 → 개념의 성질만 적고 적용 과정을 뺌 → **과정은 넣었는데 절의 뒤쪽이라 분량이 준비에 쏠림** |
| 반영의 성실성 | R5 → **R18** | 소절로 옮기기만 함(이름이 남음) → **문단을 옮기고 번호만 다시 붙임** |
| 항목 제거 | R4 → R5 | 소절로 옮기기만 함 → 이름이 그대로 남음 |

**R18 이 가리키는 것은 반영의 방식이다.** 위 계보 여섯 회차 중 넷이 같은 절을 다뤘는데, 매번
「그 자리 하나」를 고쳤다. `check-rework.ts` 가 그것을 재작성률로 잰다 — 구성 지적을 받은
절은 문단 이동만으로 반영을 마칠 수 없다.

**R17 의 전수 검색 결과 — 2026-08-28 배치로 해소.** `coreConcept` 를 파일럿 셋에 돌렸을 때
`mosAlgorithm` 만 통과하고 나머지 둘이 걸렸다. 둘 다 옛 직무(여섯 걸음 이전)로 쓴 편이라
걸음 ④⑤⑥ 의 **측정값을 새로 만들어야** 했고, 기계적 일괄 수정이 불가능해 재집필 대상으로
남겨 두었던 것이다.

| 편 | 걸렸던 것 | 무엇으로 해소했나 | `deep.build` 재작성률 |
| --- | --- | --- | --- |
| `knapsack01` | 핵심 걸음(④⑤⑥)이 없다 | ④ 조합 31개 대 무게 합 합침 20개 · ⑤ 상태를 `(i, c)` 두 수로(한 축만 쓰면 용량 10에서 14) · ⑥ 도달 가능 36,610 대 전부 깔기 51,051 | **74%** |
| `quicksort` | 핵심 걸음(④⑤⑥)이 없다 | ④ 정렬된 입력에 첫 원소 28번 대 중앙 13번 · ⑤ 갈림의 균형 · ⑥ 기준값 자리 네 가지 × 입력 다섯 개 | **76%** |
| `babyStepGiantStep` | 부분 원고(세 절)뿐이었다 | 전편 신규 집필. ④ `n=1` 59번 대 `n=58` 59번 · ⑤ 조각 크기 · ⑥ 일곱 값에서 `n=8` 이 16번 | — (신규) |

기준은 60%(`check-rework.ts`)이고 옛 헤딩(`## 아이디어 상세`)과 새 헤딩(`### 아이디어 상세`)을
직접 대조해 잰 값이다.

## 5. 조건부 절 판정 — 편마다 실제로 확인한 결과

`L34`·`L35`·`L36`(2026-08-28 지시 R20~R22)은 **절을 실을지 말지**를 정하는 규칙이라, 편마다
판정을 남긴다. 판정을 안 적으면 다음 편에서 「그냥 넣던 대로」 넣게 된다.

| 편 | `purpose.alt` (`L34`) | `related` (`L35`) | `deep.math` (`L36`) |
| --- | --- | --- | --- |
| `mosAlgorithm` | **싣는다** — 펜윅과 `n=5` 에서 11 대 25, `n=400` 에서 6,895 대 4,850 으로 **순서가 뒤집힌다** | **싣는다** — 제곱근 분할 | **싣는다** — `qB = n²/B` 를 풀어 `B = √n` 을 낸다 |
| `knapsack01` | **싣는다** — 가치 축과 용량·가치 합의 크기에 따라 갈린다(용량 `10^9`·가치 합 100 이면 뒤집힌다) | **싣는다** — 마르코프 성질 | **싣는다** — 집합 정의로 전이식을 유도하고 `n` 을 일반화해 칸 수 대 `2^n` 을 낸다 |
| `quicksort` | **싣는다** — 병합 정렬과 비교 49 대 52 · 추가 칸 64 대 0 으로 **축마다 갈린다** | **싣는다** — 선택 알고리즘 | **싣는다** — `T(n)` 을 두 극단에서 닫힌 형태로 푼다 |
| `babyStepGiantStep` | **싣는다** — rho 와 곱셈 155 대 408 · 저장 101 대 6. `m` 이 커지면 이쪽이 실행 불가 | **싣는다** — 중간에서 만나기 | **싣는다** — `n = m/n` 을 풀어 `n = √m` 을 낸다 |
| `binarySearch` | **싣는다** — 보간 탐색과 균등 입력에서 읽은 칸 1,024 대 9,228, 편향 입력에서 10,026 대 9,228 로 **순서가 뒤집힌다** | **싣는다** — 단조 술어와 이분법 | **싣는다** — `R(n,k) = max(k-1, n-k)` 를 최소화해 `C(n) = ⌊log₂ n⌋ + 1` 을 낸다 |
| `longestSubarrayAtMostSum` | **생략** — 경쟁 설계(접두사 합 + 이분 탐색 · 답 길이 이분)가 시간·메모리 **두 축 모두에서** 밀리고 입력·제약을 바꿔도 안 뒤집힌다 | **싣는다** — 분할 상환 분석 | **싣는다** — `lo(r)` 의 비감소를 유도하고 망원 합으로 `n + (lo(n−1) − lo(0)) ≤ 2n` 을 닫는다 |
| `bfsShortestPath` | **생략** — 다익스트라는 무가중치에서 모든 축이 열세이고, 가중치가 붙는 것은 우열이 뒤집히는 것이 아니라 BFS 가 **그 문제를 못 푸는 것**이다 | **싣는다** — 거리 층 | **싣는다** — 악수 보조정리로 `V+E` 를 임의의 `V`·`E` 로 일반화한다 |
| `nQueens` | **싣는다** — 앞을 내다보는 검사와 `n=8` 에서 방문 노드 2,057 대 1,165 · 새로 잡는 칸 0 대 13,792 로 **축마다 갈린다** | **싣는다** — 제약 충족 문제(CSP) | **싣는다** — `S(n) = ⌊e·n!⌋` 을 닫고 `n=12` 에서 1,521배를 낸다 |
| `maxCounters` | **생략** — 경쟁 후보인 구간 갱신 자료구조(지연 전파)가 모든 축에서 열세다. 전체를 덮는 연산 하나에 `O(log N)` 과 추가 칸을 쓰는데 변수 하나가 `O(1)` 로 끝난다 | **싣는다** — 지연 갱신 | **싣는다** — 두 방식의 최악 비용을 닫아 `N·M` 대 `M+N−2` 를 낸다 |
| `parametricBinarySearch` | **싣는다** — 동적 계획법과 작은 입력에서 기본 단계 46 대 132, 큰 입력에서 5,100 대 1,228,060 으로 **순서가 뒤집힌다**. 경계는 `NK` 와 `log(ΣA)` 의 대소다 | **싣는다** — 탐욕이 앞선다 논법 | **싣는다** — 판정 함수의 단조성을 유도하고 `P(s) = ⌊log₂ s⌋ + 1` 을 닫아 `T(N,S) = Θ(N log S)` 를 낸다 |
| `connectedComponents` | **싣는다** — 서로소 집합과 오프라인에서 배열 접근 16,100 대 19,200, 매번 성분 수를 묻는 온라인에서 10,003,000 대 10,300 으로 **순서가 뒤집힌다** | **싣는다** — 탐색이 만드는 숲 | **싣는다** — 도달 가능 관계를 동치 관계로 정의하고 `E ≥ V − k` 를 유도해 `k ≥ V − E` 를 낸다 |
| `sortArray` | **싣는다** — 퀵 정렬과 뒤섞인 입력에서 견주기 311 대 318, 이미 정렬된 입력에서 2,016 대 192 로 **순서가 뒤집힌다** | **싣는다** — 안정 정렬 | **싣는다** — 합치기 비교 `M(p,q) = p+q−1` 에서 점화식을 세워 `C(n) = n⌈log₂ n⌉ − 2^⌈log₂ n⌉ + 1` 을 닫는다 |
| `ternarySearch` | **생략** — 황금 분할 탐색이 같은 입력(`f=(x-2)²`·`[0,9]`·`ε=1e-6`)에서 반복 34 대 40 · `f` 호출 36 대 80 으로 **두 축 모두** 앞서고 `L`·`ε` 를 바꿔도 안 뒤집힌다(`L=20·ε=1e-9` 52 대 118 · `L=2×10^6` 76 대 174). 계측 오차에 강할 것이라는 후보 축도 실측으로 부정됐다(둘 다 7.07e-4) | **싣는다** — 선형 수렴 | **싣는다** — `L_{k+1}=(1−t)L_k` 를 펴 `L_k = r^k·L_0` 를 얻고 로그를 씌워 `K(t)=⌈ln(L/ε)/ln(1/(1−t))⌉` 를 닫는다 |
| `prefixSumRangeQuery` | **싣는다** — 펜윅 트리와 같은 작업 목록에서 배열 접근이 갱신 0 회에 5,121 대 23,548 로 앞서다가 갱신 7 회에 26,646 대 23,662 로 **순서가 뒤집히고** 갱신 1,024 회에는 3,153,921 대 37,884 가 된다(저장 칸은 1,025 로 동일) | **싣는다** — 역원이 있는 연산 | **싣는다** — 재귀 정의에서 귀납으로 닫힌 형태를 내고 구간 분할로 `Σ_{k=l..r}A[k] = P[r+1]−P[l]` 를 임의의 `l ≤ r` 에서 유도한 뒤 `q·n` 100억 대 `n+q` 20만을 낸다 |
| `dfsTraversal` | **생략** — 출력이 「이웃 중 작은 번호 먼저」로 고정돼 **같은 답을 내는 다른 절차가 정의상 없다.** 세울 수 있는 후보는 재귀 구현(같은 절차의 구현 변형)과 인접 행렬(`V=10^5` 에서 `10^10` 칸이라 모든 축 열세)뿐이라 우열이 뒤집히는 자리를 못 댄다 | **싣는다** — 되돌리기(백트래킹) | **싣는다** — 스택 삽입 총수 `Q(s)=1+Σ deg(v)` 를 `1+2E` 로 닫고 정렬 항을 `2E·log₂Δ` 로 닫아, 「작은 번호 먼저」 규칙 하나가 `V+E` 짜리 절차에 `log` 를 붙인다는 것을 낸다 |
| `coinChangeWays` | **싣는다** — 하향식 메모이제이션과 액면가 `[1,2,5]`·금액 `10^4` 에서 값을 정한 칸 40,004 대 32,001 로 밀리는데, 액면가만 `[3000,5000,7000]` 으로 키우면 40,004 대 21 로 **순서가 뒤집힌다**(1,905배). 경계는 도달 가능한 `(i, a)` 의 비율이다 | **싣는다** — 생성 함수 | **싣는다** — 상태를 개수 목록의 집합으로 정의하고 `k_i = 0`/`k_i ≥ 1` 로 갈라 전이식을 유도한 뒤, 동전이 `[1, c]` 둘일 때 텔레스코핑으로 `dp[2][a] = ⌊a/c⌋ + 1` 을 닫는다 |
| `subarraySumEqualsK` | **생략** — 경쟁 후보 셋을 같은 입력에서 재 보니 뒤집히는 축이 없다. `전부 1 · k=5`(`n=1,000`)에서 자료 접근이 정본 4,001 대 분할 정복 짝 세기 27,913 대 정렬·좌표압축 배열 49,006(비교 19,983 대 0 · 칸 4,004 대 1,001)이고 `n=10,000` 과 입력 모양 여섯으로 바꿔도 순서가 그대로다. 유일하게 비용이 작은 후보인 슬라이딩 윈도우(1,995)는 가이드 입력에서 답이 **2 대 4 로 틀린다** — 음수를 허용하는 이 문제를 못 푸는 것이라 대조가 아니라 적용 범위다. 느린 풀이는 `deep.build` 가 방식 A·B·C 로 이미 값으로 대조했다 | **싣는다** — 여집합 조회. `prefix − k` 를 T3·T9 의 값으로 보인 뒤 이름을 붙이고 `twoSum`·배수 구간 세기로 편 밖 쓰임을 댄다 | **싣는다** — 구간의 집합과 칸 짝의 집합을 정의해 전단사로 잇고, `|B| ≤ C(n+1,2) = n(n+1)/2` 를 등호 성립 조건과 함께 닫아 `n=100,000` 에서 답 50억 대 맵 연산 30만을 낸다 |
| `insertionSort` | **싣는다** — 선택 정렬과 이미 정렬된 64 칸에서 견주기 63 대 2,016 인데 쓰기는 63 대 0 이고 역순 입력에서는 쓰기가 2,079 대 64 로 **축마다 갈린다**. 쓰기 한 번을 견주기 `w` 번으로 환산해 두 축을 합치면 뒤집히는 경계가 값으로 나온다 — 이미 정렬된 입력은 `w = 31` 에서 2,016 대 2,016 으로 같고 `w = 32` 부터 선택이 앞서며, 거의 정렬된 입력은 `w = 33`(2,277 대 2,280)과 `w = 34`(2,344 대 2,288) 사이가 경계다 | **싣는다** — 적응형 정렬 | **싣는다** — 역순쌍 `I(A)` 를 정의해 `s_i` 를 입력만으로 적고 `S = I(A)` 를 유도한 뒤, `s_i = i` 인 바퀴 수 `z` 를 세워 `C = I(A) + (N−1) − z` 를 닫는다. 거기서 `C_max = N(N−1)/2` 와 평균 `E[I] = N(N−1)/4` · `E[z] = H_N − 1` 을 낸다 |
| `dfsAllPaths` | **싣는다** — 도달 가능성 가지치기와 5×5 격자(정점 36 · 간선 60)에서 이웃 검사 922 대 26,456 으로 스물여덟 배 앞서다가, 간선 수를 맞춘 덫 그래프(정점 10 · 간선 58)에서 진입한 노드 13,702 대 2 · 이웃 검사 95,902 대 3 으로 **순서가 뒤집힌다**(새로 잡는 칸은 0 대 24,156 로 이쪽이 앞선다). 경계는 진입한 노드 중 경로를 하나도 못 만드는 막다른 가지의 비율이다 — 격자 0% · 덫 99.98% | **싣는다** — 출력 민감 알고리즘 | **싣는다** — `N`·`L`·`R` 을 집합으로 정의해 전개 입력에서 3 · 10 · 9 로 검산하고, 완전 그래프에서 `N(n)=Σ(n−2)!/(n−2−k)!` 의 꼬리를 등비급수로 눌러 `N(n)=⌊e·(n−2)!⌋` 을 닫은 뒤, 격자에서 `C(2m,m)` 과 스털링 근사로 `√V` 에 대한 지수를 내고 제약 10⁴ 이 정점 아홉·격자 한 변 여덟에서 걸리는 것을 낸다 |
| `minMaxPair` | **생략** — 세울 수 있는 후보 둘이 다 저울질 대상이 아니다. 「원소마다 두 번 대조」는 경쟁 설계가 아니라 같은 문제의 느린 풀이이고(`maxCounters` 와 같은 부류) 그 대조는 `deep.build` 가 이미 값으로 한다. 분할 정복은 2 의 거듭제곱 길이에서만 비교가 같고(`n=16` 에서 22 대 22) 그 밖에서는 더 많은데(`n=100,000` 에서 165,534 대 149,998) 재귀 스택 `O(log n)` 까지 더 쓴다 — 두 축 모두 열세라 뒤집히는 자리가 없다 | **싣는다** — 토너먼트 방식 | **싣는다** — 홀·짝 두 갈래 식을 `⌈3n/2⌉−2` 로 닫고, 표시 논증으로 하한 `2n−2−⌊n/2⌋` 를 유도해 이 절차가 하한을 정확히 달성함을 낸다(`n=10^5` 에서 하한과의 차이 0) |
| `diffArrayRangeUpdate` | **싣는다** — 펜윅 트리(구간 갱신·점 조회)와 같은 작업 목록에서 배열 접근이 점 조회 0 회에 7,169 대 30,214 로 앞서다가 45 회에 30,743 대 30,438 로 **순서가 뒤집힌다** | **싣는다** — 이벤트 스위핑 | **싣는다** — 임의의 갱신 목록에서 `A[i] = Σ_{l≤i≤r} v` 를 유도하고 접근 수 닫힌 형태 `3N+4Q+1` 대 `N+2NQ` 를 낸다 |
| `countingSort` | **싣는다** — 병합 정렬과 같은 생성식 입력에서 배열 접근이 20 칸 2,082 대 508 로 밀리다가 200 칸 2,802 대 9,080 으로 **순서가 뒤집히고** 10 만 칸에서 402,002 대 9,927,060 이 된다. 뒤집히는 첫 칸 수 **65** 를 스윕으로 실측했다 | **싣는다** — 직접 주소 지정 | **싣는다** — `T(N,K) = 2K + 4N` 을 유도하고 비교 정렬 하한 `log₂(N!)` 과 견주어 경계 **547 칸**을 낸다 |
| `countIslands` | **싣는다** — 서로소 집합과 오프라인에서 칸 읽기 49,600 대 29,800 · 보조 배열 접근 59,600 대 198,389 로 **축마다 갈리다가**, 갱신 200 회에서 2,075,490 대 770 으로 **순서가 뒤집힌다** | **싣는다** — 암묵적 그래프 | **싣는다** — 도미노 덮기로 `⌈R·C/2⌉` 를 닫고 체커보드가 그 상한을 달성함을 보여 1000×1000 에서 최대 500,000 섬을 낸다 |
| `kadane` | **싣는다** — 세그먼트 트리와 같은 작업 목록에서 기본 연산이 질의 0 회에 3,069 대 90,104 로 앞서다가 29 회에 92,070 대 90,104 로 **순서가 뒤집힌다**(저장 칸은 2 대 8,192) | **싣는다** — 온라인 알고리즘. 걸음마다 `best` 가 그 앞부분만의 답이라는 것을 T1·T3·T5·T7·T9 값으로 보인다 | **싣는다** — 접두사 합 정의에서 `dp[i] = P[i+1] − min P[l]` 를 거쳐 두 갈래 점화식을 유도하고 `N(N+1)` 대 `3N−3` 을 제약 규모에서 낸다 |
| `radixSort` | **싣는다** — 병합 정렬과 같은 생성식 입력(`A[i] = (i × 999,983) mod (10^9+1)`)에서 배열 접근이 200 칸 10,484 대 8,400 으로 밀리다가 **310 칸에서 순서가 뒤집히고** 10 만 칸에서 3,204,084 대 9,455,258 이 된다. 견주기는 0 대 1,239,773 으로 늘 이쪽이 앞서고 새로 잡는 칸은 201,024 대 200,000 이다. 내주는 축은 적용 범위 — 키가 고정 폭 정수가 아니면 못 쓴다 | **싣는다** — 다중 키 정렬(마지막 키부터 안정 정렬) | **싣는다** — `B = ⌈(M+1)^(1/d)⌉` 를 총식에 넣어 `T*(d)` 를 세우고, 최적 바퀴 수가 바뀌는 경계를 **74 · 225 · 1,310 · 34,425 칸**으로 닫는다 |
| `maximumProductSubarray` | **싣는다** — 세그먼트 트리와 같은 작업 목록에서 기본 연산이 질의 0 회에 7,161 대 247,786 으로 앞서다가 **34 회에 250,635 대 247,786 으로 순서가 뒤집힌다**(33 회는 243,474 대 247,786 으로 아직 이쪽). 저장 칸은 3 대 12,288 이고, 경계 34 는 `247,786 ÷ 7,161 ≈ 34.6` 과 맞는다 | **싣는다** — 구간 산술. `curMin`·`curMax` 쌍이 곧 구간이고 한 걸음이 「구간 × 점 + 점의 볼록 껍질」이다 | **싣는다** — `max(cS)` 의 부호별 성질로 후보가 정확히 셋인 이유를 임의의 `N`·임의의 배열에서 닫고, 값이 정확한 한계 **22 칸** 과 표현되는 한계 **308 칸**(309 칸부터 `Infinity`)을 실측으로 낸다 |
| `houseRobber` | **싣는다** — 세그먼트 트리(마디마다 네 값, max-plus 합치기)와 같은 작업 목록에서 기본 연산이 질의 0 회에 2,046 대 225,263 으로 앞서다가 **110 회에 227,106 대 225,593 으로 순서가 뒤집히는데**(109 회는 225,060 대 225,590), 저장 칸은 2 대 8,192 로 끝까지 이쪽이 적어 **축마다 갈린다** | **싣는다** — 가중치가 있는 최대 독립 집합. 본문이 값으로 보인 집 0·2·5 에 이름을 붙이고 나무·구간 겹침으로 편 밖 쓰임을 댄다 | **싣는다** — 집합 갈림으로 전이식을 유도하고 후보 개수를 `F(N+2)` 로 닫아, 제약 규모에서 20,899 자리 대 모든 부분집합 30,103 자리를 낸다 |
| `bestTimeToBuyAndSellStock` | **싣는다** — 세그먼트 트리(마디마다 최저가·최고가·구간 최대 이익 셋)와 같은 작업 목록에서 기본 연산이 질의 0 회에 3,069 대 56,315 로 앞서다가 **18 회에 58,311 대 56,315 로 순서가 뒤집히고**(17 회는 55,242 대 56,315), 1,024 회에는 3,145,725 대 56,315 가 된다. 저장 칸 2 대 6,144 는 안 갈린다 | **싣는다** — 접두사 최솟값. 본문이 `minP` 로 이미 값을 보인 것에 이름을 붙이고 `pandas.Series.cummin`·최대 낙폭·최대 부분합으로 편 밖 쓰임을 댄다 | **싣는다** — 후보를 `i=j` 와 `i<j` 로 가른 일반식 `max(0, max_j(P[j] − m_(j−1)))` 을 유도하고, 코드의 시작값 `0` 이 특례가 아니라 식의 첫 항임을 보인 뒤 `N(N+1)` 대 `3N−3` 을 제약 규모에서 낸다 |
| `dijkstra` | **싣는다** — 플로이드-워셜과 `V=200`·`E=39,604` 밀집 그래프에서 출발점 수로 갈린다. 1개면 46,812 대 8,079,804 로 172배 앞서다가 **173개에서 8,143,378 대 8,114,204 로 순서가 뒤집히고** 200개면 9,410,944 대 8,119,604 다. 저장 칸 696 대 40,000 은 안 갈린다. 전개 입력(정점 여섯)으로는 출발점을 여섯까지밖에 못 늘려 별도 입력을 쓴 사유를 본문에 적었다 | **싣는다** — 최적 부분 구조. `T4` 의 `dist[3] = dist[1] + 1` 이 그 자리이고 플로이드-워셜·벨만-포드·배낭으로 편 밖 쓰임을 댄다 | **싣는다** — 큐 항목 수 상한 `E+1` 에서 닫힌 형태 `4(E+1)⌈log₂(E+1)⌉` 를 유도하고, 배열 방식 `V²` 와 뒤집히는 간선 수 **92,592,592**(제약 상한 20만의 463배)를 낸다 |
| `trie` | **싣는다** — 정렬 배열 + 이분 탐색과 견주면 연산 수는 안 갈리는데(삽입 149,091 대 417,921 · 조회 61,000 대 590,398) **저장 칸이 뒤집힌다**. 길이 합 100,000 고정에서 공통 접두사 47 글자면 102,177 대 101,000 으로 배열이 적고, **48 글자에서 100,179 대 101,000 으로 트리가 적어진다** | **싣는다** — 자리별 분해. `T2`~`T4` 에서 값으로 보인 「글자 하나로 후보를 좁힘」에 이름을 붙이고 기수 정렬·이진 트라이·IP 라우팅 표로 편 밖 쓰임을 댄다 | **싣는다** — `Pref(W)` 를 정의해 전개 입력에서 원소 7 = 노드 7 로 검산하고 상·하한과 등호 성립 조건을 닫는다. 본문 값만으로는 안 나오는 것 — 자식을 26칸 배열로 잡는 구현과 맵 구현이 **2,600,026 대 100,000 으로 26배** 갈린다 |
| `longestCommonSubsequence` | **싣는다** — 일치 쌍 따라가기(Hunt–Szymanski)와 길이 200·같은 생성식에서 알파벳 3 이면 표 채우기가 80,000 대 95,193 으로 앞서다가 **알파벳 4 에서 73,051 로 순서가 뒤집힌다**. 경계는 일치 칸 수 `r ≈ 11,000`(실측 두 점 73,051@r=10,000 · 95,193@r=13,333 사이). 전부 같은 글자면 291,018 대 80,000 으로 다시 표가 앞서고, 저장 칸 401 대 40,401 은 안 갈려 그것이 내주는 축이다 | **싣는다** — 중복 부분 문제. 같은 입력에서 111 번 호출 대 24 상태로 값을 보인 뒤 이름을 붙이고 0/1 배낭·동전 조합·편집 거리로 편 밖 쓰임을 댄다 | **싣는다** — 호출 수 점화식 `K(i,j)=1+K(i-1,j)+K(i,j-1)` 을 파스칼 규칙으로 `2·binom(i+j,i)−1` 로 닫고 `i=j=1,000` 에서 **601 자리 대 1,002,001** 을 낸다 |
| `fastPower` | **싣는다** — 5비트 슬라이딩 창(홀수 거듭제곱 표 16칸)과 같은 밑 `3`·법 `10^9+7` 에서 모듈러 곱셈이 전개 입력 26 에 8 대 17, 40비트에 58 대 58, **41비트에서 68 대 59 로 순서가 뒤집히고** 1,024비트에 1,503 대 1,206 이다. 저장 칸 3 대 19 는 끝까지 이쪽이 적어 **축마다 갈린다** | **싣는다** — 결합법칙만 요구하는 거듭제곱(모노이드). 본문이 값으로 보인 `(x·y)·z` 에 이름을 붙이고 `matrixPowerFibonacci`·`babyStepGiantStep` 으로 편 밖 쓰임을 댄다 | **싣는다** — `M(exp) = n + s` 와 경계 `n+1 ≤ M ≤ 2n` 을 유도하고, 임의의 `k` 로 접었을 때 `(k−1)/ln k` 가 최소인 **`k = 2`** 를 낸다 |
| `medianFromDataStream` | **싣는다** — 매번 선택 알고리즘으로 가운데 자리 골라내기와 같은 생성식 입력 4,096 개에서 물어본 횟수 `q`=0 이면 404,830 대 4,096 으로 골라내기가 98.9 % 적다가, **`q`=72 에서 404,830 대 413,848 로 순서가 뒤집히고** `q`=4,096 에 41배가 된다. `q`=0…300 스윕에서 골라내기가 마지막으로 적은 자리가 **71** 이다. 저장 칸 4,096 대 4,096 은 안 갈려 **축마다 갈린다** | **싣는다** — 순서통계량. `T8` 의 「작은 쪽 꼭대기 5 = 3 번째로 작은 값」에 이름을 붙이고, 크기를 `⌈N/2⌉` 대신 `⌈0.95N⌉` 로 두면 같은 절차가 95 백분위수를 답한다는 것과 선택 알고리즘·백분위수 감시·슬라이딩 창 중앙값으로 편 밖 쓰임을 댄다 | **싣는다** — 본문이 값으로 보인 것은 `N`=1…5 뿐인데 `|L_N| = ⌈N/2⌉` 를 귀납으로 임의의 `N` 에 닫고 비용 상한 `17·Σ⌈log₂ i⌉ + 5N` 을 유도해, `N`=50,000 에서 **12,735,905 대 36,723,984,465(2,884배)** 를 실행 없이 낸다 |
| `topologicalSort` | **생략** — 경쟁 설계 「끝난 순서 뒤집기」(DFS 역후위순회)가 **어느 축에서도 안 뒤집힌다.** 배열 칸 접근이 전개 입력 78 대 126 · 성긴 `V=1,000`·`E=1,997` 에서 18,982 대 28,976 · 촘촘한 `E=19,790` 에서 125,740 대 171,320 이고 추가로 잡는 칸도 2,000 대 3,000 이다. 간선 밀도를 `K`=1~60(`E` 최대 58,170)까지 스윕해 뒤집히는 첫 `K` 를 찾았고 결과는 **0**. 재귀 구현은 칸 접근이 더 적지만 제약 상한인 정점 10만 사슬에서 `RangeError` 라 대조가 아니라 적용 범위다 — 생략 사유 ①(모든 축에서 한쪽이 낫다)과 ②(적용 범위)가 한 편에서 함께 나온 자리로, `minMaxPair` 의 ①+③ 과 같은 모양이다 | **싣는다** — 선형 확장. 720 개 나열 중 유효한 것 **13 개**와 후보 통 셋이 낸 서로 다른 세 나열이 전부 유효하다는 것을 값으로 보인 뒤 이름을 붙이고, 작업 스케줄링의 실행 순서·조합론의 선형 확장 수 세기로 편 밖 쓰임을 댄다 | **싣는다** — 「사이클이 없으면 진입차수 0 인 정점이 반드시 있다」를 비둘기집으로 임의의 `V` 에 일반화해 `order.length === n` 이 사이클 없음과 동치임을 내고, 칸 접근의 닫힌 형태 **`7V + 6E`** 를 실측 다섯(6/6→78 · 100,000/99,999→1,299,994)과 대조한 뒤 제약 규모의 130만을 순열 전수 검사의 **456,574 자리**와 견준다 |
| `editDistance` | **싣는다** — 띠 계산과 문턱 배가(Ukkonen)와 길이 200·같은 생성식에서 채운 칸이 답 1 에 601 대 40,401 로 띠가 67.2배 적다가, **답 33 에서 46,873 대 40,401 로 순서가 뒤집힌다**(문턱이 32→64 로 배가되는 자리를 스윕으로 실측). 답 92 에서는 82,018 대 40,401 로 표가 2.0배 적다. 잡는 칸 40,401 대 402 는 안 갈려 그것이 내주는 축이다 | **싣는다** — 격자 위의 최단 경로. 오른쪽 아래에서 거슬러 확인한 걸음 다섯의 누적 1·1·2·2·3 이 각 칸 값과 일치하는 것에 이름을 붙이고 격자 경로 세기·전역 정렬로 편 밖 쓰임을 댄다 | **싣는다** — 편집 목록 뼈대(격자 경로) 수를 `Σ_p (i+j−p)!/(p!(i−p)!(j−p)!)` 로 닫아 실측 3·13·63·321·1,683·8,989 와 대조하고 제약 규모에서 **764 자리 대 1,002,001** 을 낸 뒤, 하한 `dp ≥ |i−j|` 에서 「비용이 문턱 `k` 이하인 경로는 대각선에서 `k` 칸 안쪽」을 유도해 경쟁 설계가 서는 근거를 세운다 |
| `subsetSum` | **싣는다** — 중간에서 만나기와 원소 36·목표 1,000 에서 기본 연산 37,037 대 7,201,496 으로 표가 194배 적다가, 원소 20·목표 500,000 에서 10,500,021 대 20,604 로 **순서가 뒤집힌다**(510배). 목표 1,000 고정 스윕에서 원소 19 개는 20,020 대 15,632 로 중간에서 만나기가 앞서고 **20 개에서 21,021 대 22,008 로 표가 앞선다** | **싣는다** — 집합의 덧셈(sumset). `i=4` 줄 `{0,3,4,7}` 에서 `i=5` 줄이 `R ∪ (a+R)` 로 나온 것에 이름을 붙이고 다항식 곱셈의 지수 집합·민코프스키 합·동전 조합으로 편 밖 쓰임을 댄다 | **싣는다** — 상한 `|R_i| ≤ min(2^i, T+1)` 와 포화 지수 `⌈log₂(T+1)⌉ = 14` 를 닫고(원소가 2의 거듭제곱일 때 등호가 서는 것을 실측), 열거와 표가 뒤집히는 원소 수 **18**(`n`=17 은 131,072 < 180,018 · `n`=18 은 262,144 > 190,019)을 낸다 |

**넷 다 세 절을 유지한다.** `L34` 의 판정 질문(우열이 실제로 뒤집히는가)에 넷 다 값이나
조건으로 답이 있었고, `L36` 의 질문(본문 값만으로는 안 나오는 것을 주는가)에도 넷 다 답이
있었다 — 앞의 셋은 최적값을 풀거나 닫힌 형태를 내고, `knapsack01` 은 임의의 `n` 으로 일반화한
칸 수를 낸다.

**`knapsack01` 의 `deep.math` 가 가장 아슬아슬했다.** 전이식 자체는 파트 1 이 이미 값으로 다
보였으므로, 그 절이 주는 것은 ① 집합 정의(본문에 없다) ② `S` 를 두 무리로 가르는 유도
③ `n = 4·20·100` 에서 칸 수와 `2^n` 을 나란히 놓은 표(본문에 없다) 셋이다. 이 셋이 없어지면
절을 뺐어야 한다.

**W1 배치1 에서 `purpose.alt` 가 처음 생략됐다(2026-08-29).** `longestSubarrayAtMostSum` 과
`bfsShortestPath` 둘이고 **사유가 서로 다르다** — 앞은 SPEC 표의 「모든 축에서 한쪽이 낫다」이고,
뒤는 대조가 아니라 **적용 범위**의 이야기다(가중치가 붙으면 BFS 가 그 문제를 못 푼다).
둘 다 `.alt.ts`·`.bench.json` 을 만들지 않아 `P10` 이 잡을 잔여물이 없다. 파일럿 넷이 전부 세 절을
유지했으므로 생략 경로가 실제로 돈 것은 여기가 처음이다.

**배치1 의 판정 4행이 §4 재작성률 표에 잘못 붙어 있던 것을 배치2 에서 이 표로 옮겼다
(2026-08-29).** 열 구성이 다른 표라 그대로 두면 §4 가 깨진 채로 서고, 다음 배치가 그 자리를
보고 또 §4 에 넣는다. 행의 내용은 한 글자도 고치지 않았다.

**W1 배치2 에서 `purpose.alt` 생략이 세 번째로 돌았다(2026-08-29).** `maxCounters` 하나이고
사유는 `longestSubarrayAtMostSum` 과 같은 부류다 — SPEC 표의 「모든 축에서 한쪽이 낫다」.
다만 **여기서는 경쟁 후보를 세우는 일 자체가 갈렸다**: 이 문제의 느린 풀이(즉시 채우기)는
경쟁 설계가 아니라 같은 설계의 최악이고 그 대조는 `deep.math` 가 이미 값으로 하고 있다.
경쟁 설계로 세울 수 있는 것은 구간 갱신 자료구조뿐인데 그것이 모든 축에서 밀린다.
**「대조할 것이 없다」와 「대조했더니 한쪽이 다 낫다」는 다른 판정이고, 둘 다 생략으로 간다.**

**W1 배치3 에서 `purpose.alt` 생략이 넷째·다섯째로 돌았고, 그중 하나가 새 부류다(2026-08-29).**
`ternarySearch` 는 앞선 셋과 같은 부류다 — SPEC 표의 「모든 축에서 한쪽이 낫다」이고, 여기서는
경쟁 설계(황금 분할)가 **더 나은** 쪽이라 실을 이유가 없다. `dfsTraversal` 이 새 부류다:
**경쟁 설계가 정의상 없다.** 출력이 「작은 번호 먼저」로 고정된 문제라 같은 답을 내는 다른
절차를 셀 수 없는 것이 아니라 **세울 수 없고**, 후보로 남는 것은 같은 절차의 구현 변형뿐이다.
생략 사유가 이제 넷이다 — ① 모든 축에서 한쪽이 낫다 ② 대조가 아니라 적용 범위다
③ 경쟁 후보를 세우는 일 자체가 갈린다 ④ 절차가 유일하다. 넷 다 「없으면 빼라」로 간다.

**`.mdx` 를 지우면 아직 v1 인 편의 본문에서 들어오는 상호 링크가 깨진다(2026-08-29, §3 감).**
배치3 에서 둘이 터졌다 — `knuthOptimization-guide.mdx:130`(→`prefixSumRangeQuery`) 과
`dfsAllPaths-guide.mdx:34`(→`dfsTraversal`). 워커는 자기 편 디렉터리만 쓰므로 잡을 수 없고,
`tools/check-links.ts check` 가 배치 종료에서 잡아 오케스트레이터가 고쳤다. **강제 지점은 이미
있다**(`ci.ts gates` 안의 `check-links`) — 빠진 것은 편을 잡을 때 인바운드를 먼저 확인하는 절차이고,
그것을 다음 배치 워커 지시에 넣는다. 남은 W1 편 중 같은 모양이 아홉 군데 더 있다.

**공통 원인은 최소 변경이다.** 지적을 인스턴스로 처리하고 부류로 처리하지 않았다. §3 목록을
편마다 대조하는 것이 그 대책이고, 기계로 옮길 수 있는 것은 옮긴다.

**W1 배치4 에서 `purpose.alt` 생략이 여섯째·일곱째로 돌았고, 하나가 새 조합이다(2026-08-29).**
`subarraySumEqualsK` 는 부류 ①(모든 축에서 한쪽이 낫다)이다 — 후보 셋을 같은 입력에서 재
보니 뒤집히는 축이 없었고, 유일하게 비용이 작은 슬라이딩 윈도우는 **가이드 입력에서 답이 틀린다**(음수를
허용하는 문제라 못 푸는 것이다). `minMaxPair` 가 새 조합이다: **①과 ③이 한 편에서 함께
나왔다.** 후보 둘 중 「원소마다 두 번 대조」는 ③(느린 풀이는 경쟁 설계가 아니다)이고 분할 정복은
①(두 축 모두 열세)이라, 생략 사유를 한 부류로 적을 수 없다. **생략 사유는 여전히 넷이고
다섯째가 생긴 것이 아니다** — 한 편이 둘을 함께 쓸 수 있다는 것이 새로 확인된 사실이다.

**§3 대조가 스캐너 통과 원고에서 결함 셋을 잡았다(2026-08-29, 배치4 `subarraySumEqualsK`).**
`L21` 둘과 그림 정렬 하나다 — ① `불변식` 절이 `deep.math` 가 *"칸 번호와 갈라 둔다"* 고 명시한
합 첨자 `t` 를 130줄 뒤에서 칸 번호로 되썼고(`j` 로 통일), ② 「전체 컨셉」 첫 그림의 구간 괄호
둘이 인덱스 열을 벗어났으며(하나는 **배열 밖**을 가리켰다), ③ 조회 결과 화살표가 「찾은 개수」가
아니라 옆 열 아래 있었다. 셋 다 `check-v2`·`check-proof`·`check-metaphor` 가 통과시킨
자리다. **§3 을 읽기만 하고 넘어가지 않고 항목마다 원고를 열어 대조하는 것**이 이 셋을 잡았고,
그것을 배치4 워커 지시에 절차로 넣은 것이 차이였다.

**ASCII 구간 괄호의 열 정렬은 어떤 스캐너도 안 본다(2026-08-29, §3 감).** 위 ②가 그 부류이고,
눈으로 읽으면 통과한다 — 열을 세야 잡힌다. `└─…─┘` 로 데이터 열을 덮는 v2 편은 지금 셋이고,
`binarySearch-guide.md:523` 은 `┘` 가 32열·첫 「거짓」이 34열이라 **경계선상**이라 한 번 봐야 한다.

**`<!--check:{id}-->` 의 id 에 하이픈을 쓰면 `build-html` 이 실패한다 — v2 편 9개가 그 상태다
(2026-08-29, 배치4 `minMaxPair` 발견).** `tools/build-html.ts:157` 의
`CHECK_OPEN = /^<!--check:([A-Za-z_$][\w$]*)-->$/` 가 하이픈을 안 받아 여는 마커가 안 잡히고
닫는 `<!--/check-->` 만 남아 `짝 없는 <!--/check-->` 로 exit 1 이 된다. 그러면 `selfcheck` 의
답이 `<details>` 로 안 접혀 **`L19`(웹 동등성)가 인정하는 md↔web 차이 하나가 조용히 성립하지
않는다.** `sortArray` 로 재현 확인했다.

**제약이 실수로 붙었다는 근거**: `data-check` 는 `build-html.ts:258` 에서 **HTML 속성 값으로만**
쓰이고 아무 데서도 식별자로 안 읽힌다. 같은 파일의 `VIZ_OPEN`(`:156`)은 같은 정규식을 쓰지만
그쪽은 `mount.ts:47-48` 이 레지스트리 키로 되읽으므로 식별자 제약이 **필요**하다. `SPEC.md` §3 의
예시가 `<!--check:c1-->` 라 하이픈을 금지한 적은 없다.

**강제 지점이 없는 것이 이 9건이 지금까지 안 잡힌 이유다.** `tools/ci.ts` 가 `build-html` 을
부르지 않는다(`grep 'build-html' tools/ci.ts` 가 0건). 상위 카드 `S2` 가 `ci.ts` 에 편입한 것은
`check-v2`·`check-proof` 둘뿐이다. 고치는 길은 둘이고 **아직 정해지지 않았다** —
ⓐ 9편의 id 를 하이픈 없이 고친다 ⓑ `CHECK_OPEN` 을 `[A-Za-z_$][\w$-]*` 로 넓힌다.
어느 쪽이든 `ci.ts gates` 에 `build-html` 을 넣지 않으면 재발한다. **배치4 범위 밖이라
여기 기록만 하고 손대지 않았다** — `tools/**` 는 이 카드(`KAN-034.5-0RGSQM`)의 `scope` 가
아니고, 9편 중 여덟은 배치1~3 에서 이미 닫힌 편이다.

| 편 | 지금 id |
| --- | --- |
| `src/algorithms/array/longestSubarrayAtMostSum/longestSubarrayAtMostSum-guide.md` | `zero-extends` |
| `src/algorithms/array/prefixSumRangeQuery/prefixSumRangeQuery-guide.md` | `single-cell` |
| `src/algorithms/array/subarraySumEqualsK/subarraySumEqualsK-guide.md` | `found-two` |
| `src/algorithms/binary-search/binarySearch/binarySearch-guide.md` | `hi-mid` |
| `src/algorithms/binary-search/parametricBinarySearch/parametricBinarySearch-guide.md` | `hi-mid` |
| `src/algorithms/binary-search/ternarySearch/ternarySearch-guide.md` | `lo-m2` |
| `src/algorithms/etc/maxCounters/maxCounters-guide.md` | `base-carries` |
| `src/algorithms/sorting/insertionSort/insertionSort-guide.md` | `skip-self-write` |
| `src/algorithms/sorting/sortArray/sortArray-guide.md` | `drop-tail` |
| `dagShortestPath` | **싣는다** — 벨만-포드와 정점 1,000 · 간선 1,996 인 그래프에서 간선 목록을 몇 조각으로 갈라 **뒤 조각부터** 적었을 때 기본 연산이 조각 1 개에 8,988 대 4,992 로 밀리다가 **조각 4 개에서 8,988 대 10,980 으로 순서가 뒤집힌다**(조각 3 개는 8,984 로 아직 밀린다). 조각 1,000 개에는 805,388 로 89.6 배가 되고, 경계는 벨만-포드의 라운드 수가 5 가 되는 자리다. 내주는 축은 저장 칸 4,996 대 1,000 과 적용 범위 — 벨만-포드는 사이클이 있는 그래프에도 쓰이고 음수 사이클까지 판정한다 | **싣는다** — 상태 의존이 사이클을 이루지 않을 때의 계산 순서. `T7`~`T9` 가 `dist[1]`→`dist[2]`→`dist[3]` 으로 값을 물려준 것에 이름을 붙이고 0/1 배낭·편집 거리의 반복문 순서로 편 밖 쓰임을 댄다 | **싣는다** — 경로 집합을 마지막 간선으로 갈라 `opt(v) = min(opt(u)+w)` 를 유도하고, 경로 수 점화식 `N(k)=2N(k−1)` 을 `2^k` 로 닫아 정점 상한 100,000(다이아몬드 33,333)에서 **10,035 자리 대 간선 133,332 번**을 낸다 |
| `radixTree` | **싣는다** — 글자마다 노드를 두는 트리와 단어 1,000 개 · 길이 100 · 길이 합 100,000 을 고정하고 공통 접두사만 늘렸을 때 저장 칸이 공유 96 글자에 5,264 대 6,412 로 앞서다가 **97 글자에서 4,265 대 3,415 로 순서가 뒤집히고**, 삽입 기본 연산도 같은 자리에서 104,222 대 105,274 에서 105,221 대 103,276 으로 함께 뒤집힌다. 경계는 접기 전 노드 수가 접은 뒤 노드 수 1,043 에 가까워지는 자리다(공유 96 에 2,138 · 97 에 1,139). 내주는 축은 조회 기본 연산 — 공유 길이 0~97 **전수 스윕**에서 접은 쪽 62,000~65,000 대 접지 않은 쪽 61,000 으로 한 번도 안 갈린다 | **싣는다** — 경로 압축. 갈림도 단어 끝도 아닌 노드 아홉을 접어 13 이 5 가 된 것에 이름을 붙이고 서로소 집합의 `find`·접미사 트리·IP 라우팅 표로 편 밖 쓰임을 댄다 | **싣는다** — 노드 집합을 `{ε} ∪ W ∪ Branch(W)` 로 정의해 전개 입력에서 원소 5 = 노드 5 · 라벨 글자 합 12 = `\|Pref(W)\| − 1` 로 검산하고, 잎 ≤ `N` 과 갈림 ≤ 잎 − 1 로 `V ≤ 2N` 을 닫는다. 본문 값만으로는 안 나오는 것 — 노드 수 상한이 길이 합과 무관해져 제약 규모에서 **2,000 대 100,001 로 50 배** 갈린다 |
| `bestTimeToBuyAndSellStockK` | **싣는다** — 거래마다 벌금을 매기고 그 벌금을 이분 탐색하는 방식(라그랑주 완화)과 `N=1,000`·`P[j]=(j×617) mod 1,001` 에서 기본 연산이 `k`=1 에 4,000 대 70,014 로 앞서다가 **`k`=18 에서 72,000 대 70,014 로 순서가 뒤집히고**(`k`=17 은 68,000 으로 아직 이쪽) `k`=100 에 400,000 대 70,014 가 된다. 경계 18 은 `70,014 ÷ 4,000 = 17.5` 와 맞고 제약 상한 `k ≤ 100` **안쪽**이다. 저장 칸은 `k`=1 에서 4 대 4 로 같고 `k`=100 에서 202 대 4 라 그것이 내주는 축이다 | **싣는다** — 상태 기계 동적 계획법. `T5` 의 `free[0]`=0 · `hold[1]`=−2 · `free[1]`=7 · `hold[2]`=1 · `free[2]`=10 을 꼭짓점과 화살표로 그린 뒤 이름을 붙이고, 수수료·냉각 기간·상한 없음 세 변형이 그 그림에서 어떻게 갈리는지와 문자열 매칭·비터비 알고리즘으로 편 밖 쓰임을 댄다 | **싣는다** — 거래 집합과 두 상태를 정의해 전이식을 유도하고, 이익이 나는 거래 `r` 개가 `b_1<s_1<⋯<b_r<s_r` 로 날 `2r` 개를 쓴다는 것에서 **`r ≤ ⌊N/2⌋`** 를 닫는다(가격이 0 과 10,000 을 번갈아 오가는 입력에서 등호 성립을 `N`=4~12 로 실측). 제약 규모에서 표 400,000 대 거래 집합 **216 자리**를 낸다 |
| `enumerateSubmasks` | **생략** — `minMaxPair` 와 같은 **①+③ 조합**이다. 「정수를 하나씩 검사」는 경쟁 설계가 아니라 같은 문제의 느린 풀이이고 그 대조는 `deep.build` 가 이미 값으로 한다(전개 입력 11 대 7 바퀴 · `2^20` 에서 1,048,576 대 1). 진짜 경쟁 설계인 「1 비트 자리 표 + 부분집합 열거」와 「재귀 분할」은 기본 연산이 전개 입력에서 22 대 79 대 33, `2^20−1` 에서 3,145,726 대 53,477,436 대 3,145,786 이고 **`0 ≤ mask < 2^16` 전수 65,536 개를 스윕해 정본보다 적은 마스크가 0 개**다. 새로 잡는 칸도 0 대 `k` 대 `2k+1` 로 두 축 모두 열세라 뒤집히는 자리가 없다 | **싣는다** — 자리내림 전파(borrow propagation). `T5` 의 `8 (1000) → 7 (0111)` 에 이름을 붙이고 `x & (x−1)` · `x & -x` · `x \| (x+1)` 과 `lowestSetBit` 으로 편 밖 쓰임을 댄다 | **싣는다** — `\|S(mask)\| = Π(mask_i + 1) = 2^k` 를 임의의 마스크로 일반화하고, 이항정리로 `Σ_{mask<2^n} \|S(mask)\| = 3^n` 을 닫아(`n`=1…5 실측 3·9·27·81·243 과 대조) `n = 20` 에서 3,486,784,401 대 마스크 이하만 검사 549,755,289,600(157.7 배)을 낸다 |
| `kruskalMst` | **싣는다** — 프림과 정점 200 고정·간선 199~19,900 스윕에서 기본 연산이 `E`=199 에 4,296 대 201,195 로 47 배 앞서다가 **`E`=9,903 에서 240,861 대 240,839 로 순서가 뒤집히고**(`E`=9,902 는 240,674 대 240,834 로 아직 이쪽) `E`=19,900 에 521,027 대 280,955 가 된다. 저장 칸은 `E`=13,334 에서 뒤집혀 **축마다 자리가 다르다**(997 대 40,400 → 60,100 대 40,400). 내주는 축은 밀집 그래프의 연산 수와 저장 칸 둘이고, 얻는 축은 간선 목록을 그대로 받는 입력 형식과 이미 정렬된 간선에서 가장 큰 항이 없어지는 것이다 | **싣는다** — 매트로이드. 파트 1 이 값으로 보인 「값 순으로 보되 조건을 깨뜨리면 건너뛴다」에 이름을 붙이고 최대 신장 트리·작업 스케줄링·선형 독립 집합으로 편 밖 쓰임을 댄다. 공리 셋 중 셋째(교환)는 파트 1 이 값으로 **안 보였고** 그 사실을 표의 그 칸에 적었다 | **싣는다** — `\|S(x)\| ≥ 2^r(x)` 를 귀납으로 닫아 `r ≤ log₂ V` 를 내고, 제약 상한에서 대표 찾기 총 상한 **6,800,000** 과 정렬 비교 상한 **3,600,000** 을 낸다. 본문 값만으로는 안 나오는 것 — 별 모양 입력은 `r`=1 인데 상한은 16 이라 값으로는 상한이 안 얻어진다(둘씩 짝지어 올리는 입력에서 등호 성립을 `V`=2~65,536 으로 실측) |
| `treeDiameter` | **생략** — `topologicalSort` 와 같은 **①+② 조합**이다. 경쟁 설계 「한 번 순회하며 자식 방향 최댓값 둘을 더하는 DP」가 정점 20,000 짜리 네 모양(한 줄·별·균형 이진·애벌레)에서 기본 연산 279,998~319,990 대 339,997~399,986 으로 **네 모양 전부 열세**이고 저장 칸도 `2n` 대 `5n` 이다. 앞서는 축은 음수 가중치 허용 하나인데 제약이 `w ≥ 0` 이라 대조가 아니라 적용 범위다 | **싣는다** — 이심률과 트리의 중심. 「아이디어 상세」의 시작점 전수 표(10·12·9·16·13·14·16)에 이름을 붙이고 반지름 9 와 중심 정점 2 를 낸 뒤 응급 시설 배치·트리 정규화·지연 상한으로 편 밖 쓰임을 댄다. 중심이 언제나 지름 경로 한가운데라는 것은 이 글이 증명하지 않는다고 적었다 | **싣는다** — 세 정점의 중앙점 보조정리로 **지름 끝점 정리**를 유도한다. `k = m(s,x,y)` 와 `j = m(s,a,k)` 를 잡아 `d(a,x) ≥ D + 2d(j,k)` 를 내고, `D` 가 최댓값이라는 것에서 `d(j,k)=0` 과 `d(a,x)=D` 를 동시에 얻는다(트리 둘 · 시작점 열둘에서 `d(j,k)=0` 실측). `a = x` 인 경우를 떼어 내는 자리까지 그림으로 세웠고, 부산물로 임의 정점의 이심률이 `max(d(v,x), d(v,y))` 라는 것이 나온다 |
| `longestIncreasingSubsequence` | **생략** — 부류 **①+②**. 경쟁 설계 둘(자리마다 앞을 보는 `N²` 표 · 좌표 압축 + 펜윅 트리)을 네 모양 × 여섯 규모(4~20,000) **24 칸 전부**에서 재 보니 이 편의 절차가 적다(`n`=20,000 무작위 312,458 대 600,634,406 대 759,962 · 증가 514,496 대 800,000,000 대 931,100). 저장 칸도 `N` 으로 가장 적다. 펜윅 판이 앞서는 축은 「차이가 `K` 이하인 증가」 같은 변형으로의 확장 하나인데 그것은 값이 아니라 적용 범위다 | **싣는다** — 인내심 정렬. 끝난 목록 `[2, 3, 7, 18]` 이 더미 넷의 맨 위 카드라는 대응표를 놓고, 더미 개수가 답인 근거를 양방향(같은 더미는 증가 못 함 · 왼쪽 더미 맨 위를 거슬러 따라가면 길이 `L`)으로 댄 뒤 `git diff --patience` · 솔리테어 · 정렬로 편 밖 쓰임을 댄다 | **싣는다** — 늘 오른쪽으로 가는 탐색의 점화식 `b(k)=1+b(⌈k/2⌉−1)` 에서 `b(k)=⌊log₂(k+1)⌋` 를 얻고, `Σ⌊log₂ m⌋` 을 2 의 거듭제곱 구간으로 묶어 **`(N+1)⌊log₂ N⌋ − 2^(⌊log₂ N⌋+1) + 2`** 로 닫는다. 여덟 규모에서 한 항씩 더한 값과 차이 0, 증가 수열 실측과도 네 규모에서 차이 0. 제약 상한 **1,468,946** 이고 헐거운 상한 1,700,000 과 15 % 차이다 |
| `gcd` | **싣는다** — 이진 GCD(스타인)와 **축을 둘로 갈랐다**. 기본 연산은 네 입력 다 호제법이 적지만(29·357·5·17 대 35·410·158·143) 나눗셈 횟수는 이진이 **언제나 0** 이다. `bigint` 나눗셈은 자릿수에 비례하는 일이라 두 계수를 같은 값으로 셀 수 없어, 손익분기를 「나눗셈 한 번을 기본 연산 몇 번으로 치면 두 설계가 같아지는가」로 내고 **1.60 배(피보나치 이웃)에서 154 배(2 의 거듭제곱)까지** 갈리는 것을 보였다. 나눗셈 한 번을 열 번쯤으로 치는 기계를 놓으면 **입력에 따라 우열이 뒤집힌다** | **싣는다** — 연분수. 전개가 낸 몫 `0 1 1 1 1 1 2` 가 곧 `273/441` 의 연분수 전개라는 것에 이름을 붙이고, `355/113 = [3; 7, 16]` 을 되돌려 계산해 3.141593 을 낸 뒤 유리수 근사·펠 방정식·확장 유클리드로 편 밖 쓰임을 댄다 | **싣는다** — **라메 상한**. 나눗셈이 `n` 번이면 작은 쪽이 `F(n+1)` 이상임을 점화식 `x_i ≥ x_{i+1} + x_{i+2}` 에서 얻고, `F(k) ≥ φ^(k−2)` 를 귀납으로 세워 **`n < d / log₁₀ φ + 1 = 4.785d + 1`** 을 낸다. 자릿수 19 에서 상한 91.9 · 실측 89 이고, 상수 4.785 가 `1 / log₁₀ φ` 라는 것까지 값으로 적었다 |
| `findAllOccurrences` | **싣는다** — 호스풀 방식(Boyer–Moore–Horspool)과 같은 생성식(`T[i] = 알파벳[(i × 7919) mod σ]` · 길이 100,000 · 패턴은 30,000 자리에서 자른 여덟 글자)에서 **글자 가짓수 `σ` 하나만** 바꾸면 글자 견주기가 `σ`=1 에 200,012 대 799,944 로 4.0 배 앞서다가 `σ`=3 에 200,010 대 266,648 로 좁혀지고 **`σ`=4 에서 200,009 대 199,992 로 순서가 뒤집혀** `σ`=26 에 126,929 대 42,306 이 된다. 저장 칸은 8 대 26 으로 안 갈리고, 내주는 축은 큰 문자집합의 견주기 횟수이며 얻는 축은 최악 보장이다 — 호스풀은 `σ`=1 에서 `(n−m+1)m` 과 같은 799,944 까지 간다 | **싣는다** — 유한 상태 기계. 전개의 `T3` 이 상태 2 에서 `a` 를 받아 3 이 되고 `T4` 가 상태 3 에서 그 밖의 글자를 받아 0 이 된 것에 이름을 붙인 뒤 패턴 `abab` 의 전이표 넉 줄을 놓고, 정규식 엔진·어휘 분석기·`ahoCorasick` 으로 편 밖 쓰임을 댄다. 실패 함수가 그 표를 `m × 26` 칸 대신 `m` 칸으로 대신한다는 것까지 값으로 적었다 | **싣는다** — 경계 집합 `B(s)` 를 정의해 세 패턴에서 검산하고, **경계의 경계도 경계**라는 것에서 `B(P[0..i])` 가 `fail` 사슬이라는 것을 유도해 대조 루프의 `while` 이 후보를 빠짐없이 차례로 읽는 근거를 세운다. 이어 늘리기 판정 · 줄이기 성공 · 줄이기 실패 세 항으로 **견주기 ≤ 3n** 을 닫고, `(n−m+1)m` 을 `m` 에 대해 최대화해 **`⌊(n+1)²/4⌋`** 와 `m = (n+1)/2` 를 낸다. 본문 값만으로는 안 나오는 것 — 제약 규모에서 **2,500,050,000 대 실측 299,996(8,334 배)** 이 그 자리다 |
| `segmentTreeRangeMin` | **싣는다** — 스파스 테이블과 같은 작업 목록(`N=1,024`·`A[i]=(37i) mod 101`)에서 **두 축이 각각 뒤집힌다.** 갱신 0 회로 고정하고 질의를 늘리면 배열 접근이 질의 3,515 개에 33,685 대 33,690 으로 앞서다가 **3,516 개에서 33,696 대 33,692 로 순서가 뒤집히고** 10,000 개에는 86,424 대 46,660 이 된다. 반대로 질의 4,096 개를 고정하고 갱신을 넣으면 0 회에 38,421 대 34,852 로 밀리다가 **1 회에서 38,453 대 61,513 으로 다시 뒤집히고** 1,024 회에 71,189 대 27,335,716(384 배)이 된다. 저장 칸 4,096 대 9,228 은 안 갈려 그것이 얻는 축이고, 내주는 축은 질의 하나의 비용이다(`2⌈log₂N⌉` 대 2) | **싣는다** — 모노이드(항등원이 있는 결합 연산). `T5`·`T12` 의 `INF` 와 노드1 = min(노드2, 노드3) 으로 이미 값을 보인 것에 이름을 붙이고, 거듭제곱(`fastPower`)·접두 표(역원까지 요구)·스파스 테이블(멱등성까지 요구)이 각각 무엇을 더 요구하는지로 편 밖 쓰임을 댄다 | **싣는다** — 같은 깊이의 노드가 배열을 겹치지 않게 나눈다는 것에서 「깊이마다 걸쳐 있는 노드 ≤ 2」를 유도해 진입 노드 `V ≤ 1+4D` 와 읽은 노드 `R ≤ 2D` 를 닫고, 모든 `(l,r)` 짝 실측과 대조한다(`N=1,024` 에서 41 대 39 · 20 대 18). 제약 규모에서 100억 대 3,400,000(≈2,941 배)을 내고, 이어 노드 번호 상한 `2^(D+1) < 4N` 을 유도해 트리 배열 `4N` 의 근거를 낸 뒤 `2N` 으로 모자란 `N` 이 1~200 중 **180 개**임을 실측으로 보인다 |
| `unboundedKnapsack` | **싣는다** — 분기 한정 탐색(액면가 큰 것부터 · `count + ⌈rest/c⌉ ≥ best` 가지치기)과 **액면가 `[4, 6]` 을 고정하고 금액만 1 바꿔** 재니, 9,996 에서 기본 연산 29,991 대 3,335 로 분기 한정이 9.0 배 앞서다가 **9,997 에서 29,994 대 4,171,669 로 순서가 뒤집힌다**(139 배). 같은 경계를 금액 1,000/999 에서 다시 재 3,003 대 339 · 3,000 대 42,335 로 확인했다. 경계는 「답이 있는가」 — 가지치기가 기댈 첫 답을 못 찾으면 `best` 가 끝까지 무한이라 아무것도 안 잘린다. 저장 칸은 29,991 대 4 로 끝까지 분기 한정이 적어 **축마다 갈린다** | **싣는다** — 단위 가중 최단 경로로의 환원. 마지막 줄 `dp[3][a] = 0 1 2 1 1 2 2` 를 걸음 수 0·1·2 로 갈라 `{0}` · `{1,3,4}` · `{2,5,6}` 로 대응시킨 뒤 이름을 붙이고 `bfsShortestPath` · `dijkstra` 로 편 밖 쓰임을 댄다. 너비 우선 탐색이 같은 값을 **다른 순서로** 정하는 절차라 대조가 아니라 환원이라는 것을 그 자리에 적어, `purpose.alt` 가 그쪽을 상대로 세우지 않는 근거로 뒀다 | **싣는다** — 개수 벡터 집합 `K(a)` 와 `M(a)` 를 정의해 `K(6)` 의 네 원소로 검산하고, 최소 해에서 동전 하나를 떼는 논법으로 전이식을 유도한다. 본문 값만으로는 안 나오는 것 — 액면가가 둘이고 서로소일 때 못 만드는 가장 큰 금액 `g(p,q) = pq − p − q` 를 양방향으로 닫고(다섯 쌍 전수 검산 `2,3`→1 · `3,4`→5 · `3,5`→7 · `4,7`→17 · `5,6`→19), 제약 상한 `p=9,999` · `q=10,000` 에 넣어 **99,970,001** 과 못 만드는 금액 **49,985,001 개**를 내 이 문제의 금액 상한 10,000 이 통째로 그 안쪽이라는 것을 낸다 |
| `primMst` | **싣는다** — 크러스컬과 정점 200 고정·간선 199~19,900 스윕에서 기본 연산의 **순서가 두 번 뒤집힌다.** `E`=199 에 1,195 대 4,296 으로 3.6 배 앞서다가 **`E`=281 에서 6,362 대 6,175 로 뒤집히고**(`E`=280 은 5,952 대 6,149 로 아직 이쪽), **`E`=4,323 에서 100,676 대 103,511 로 다시 뒤집혀**(`E`=4,322 는 107,298 대 104,339) `E`=19,900 에 173,889 대 521,027 이 된다. 갈리는 것은 프림이 큐에서 **꺼낸 항목 수**다 — `E`=199 에 200 · 4,322 에 1,624 · 19,900 에 253 으로 중간 밀도에서 가장 많다. 저장 칸은 998 대 997 · 119,146 대 60,100 으로 **어느 밀도에서도 안 뒤집혀** 그것이 내주는 축이다. `kruskalMst` 편은 경쟁 설계로 **인접 행렬 판 프림**을 세웠고 이 편이 가르치는 것은 **최소 힙 판**이라 같은 생성식의 그래프에서도 계수가 다르고, 그 사유를 본문과 `.alt.ts` 헤더에 적었다 | **싣는다** — 경계에서 하나씩 꺼내는 탐색 틀(최우선 탐색). 파트 1 이 `T2`~`T8` 로 값을 보인 「확정한 것에 붙은 후보 중 하나를 꺼내 확정한다」에 이름을 붙이고, 너비·깊이 우선 탐색·다익스트라와 나란히 놓아 **갈리는 자리가 큐에 넣는 키 한 줄**(`w + ew` 대 `ew`)뿐임을 낸다. 편 밖 쓰임은 A\* 탐색·이벤트 스위핑·허프만 부호이고, Boost 문서가 그 관계를 「적절한 비교·결합 함자를 골라 `dijkstra_shortest_paths()` 를 부르는 것뿐」이라고 코드로 적어 둔 것을 근거로 인용했다 | **싣는다** — 큐에 남은 항목 중 트리 밖을 가리키는 것의 모임이 **정확히 경계 간선 집합 `C(S)`** 라는 것을 양방향 포함으로 유도해 「힙이 내주는 최소가 곧 경계 최소」를 임의의 그래프에서 내고, 간선 하나가 항목이 되는 횟수가 최대 1 이라는 것에서 큐에 들어간 항목 수 `≤ E+1` 과 **등호 조건**(자기 루프가 없고 시작 정점의 덩어리가 전체일 것)을 닫는다. 본문 값만으로는 안 나오는 것 — 파트 1 이 보인 것은 8 = 7+1 한 점뿐인데, 총식 `4E + 3(E+1)⌈log₂(E+1)⌉` 를 제약 상한 `V`=10,000·`E`=49,995,000 에 넣어 **41 억 대 경계 재검사 4,999 억(9,999 배)** 을 낸다 |
| `topKFrequent` | **싣는다** — 크기 `k` 최소 힙과 같은 입력(`N=100,000` · 값 `j` 의 등장 횟수 `1+(j mod 3)` · 자리 `(7919 i) mod N` · 난수 없음)에서 기본 연산이 `k`=1 에 600,001 대 350,007 로 힙이 1.7배 앞서고 `k`=4,299 까지 힙인데 **`k`=4,300 에서 608,599 대 608,644 로 순서가 뒤집히고** `k`=50,000 에 700,003 대 4,295,705(6.1배)가 된다. 저장 칸 100,001 대 50,000 은 어느 `k` 에서도 안 갈려 그것이 내주는 축이다 | **싣는다** — 역색인. 파트 1 이 값으로 보인 두 표(T1 의 「값 → 등장 횟수」, T7 의 「등장 횟수 → 그 횟수인 값들」)가 방향만 뒤집힌 것임에 이름을 붙이고 검색 엔진·`GROUP BY`·해시 조인의 빌드 단계로 편 밖 쓰임을 댄다. 이웃 `countingSort` 의 「직접 주소 지정」과 겹치지 않게 골랐다 | **싣는다** — 값이 든 자리의 개수 `B` 를 `B(B+1)/2 ≤ Σc(v) = N` 에서 **`B ≤ (√(8N+1)−1)/2`** 로 닫고 `N`=100,000 에서 **446**(잡은 자리 100,001 의 0.45 %)을 낸다. 파트 1 은 입력 다섯에서 1·1·3·3·446 을 셌을 뿐이라 다섯 번 재서는 446 이 상한임을 알 수 없다. 상한이 실측 최대와 여덟 규모에서 전부 같고 등호는 `N` 이 삼각수일 때 선다 |
| `directedCycleDetection` | **싣는다** — 진입차수 세기(Kahn)와 **순서가 뒤집힌다.** 사이클이 정점 0 에서 두 걸음이면 배열 칸 접근 500,025 대 700,006 으로 삼색이 앞서고, 사이클이 진입차수 0 인 정점을 전부 막으면 1,300,001 대 700,000, 사이클이 없는 사슬이면 1,899,992 대 1,299,994 로 진입차수가 앞선다. 두 계수가 되돌아가는 간선의 자리 `p` 에 대해 기울기 8·6 인 직선이라 경계가 하나뿐이고, 이분으로 좁힌 **`p` = 99,992** 를 값으로 실었다(99,992 걸음 1,299,945 대 1,299,946 → 99,993 걸음 1,299,953 대 1,299,952). 내주는 축은 새로 잡는 칸 499,999 대 399,999(`cursor` 배열 한 벌). 전개 입력(정점 여섯)도 첫 계수로 함께 재 110 대 50 을 실었다 | **싣는다** — 깊이 우선 탐색의 간선 분류(나무·역방향·순방향·교차). 전개 간선 여덟 개의 도착 정점 색(흰 5 · 검 2 · 회 1)이 그대로 분류가 되는 것에 이름을 붙이고, 실측 표가 `0→4` 순방향 · `2→3` 교차 · `5→0` 역방향을 낸다. 편 밖 쓰임은 강한 연결 요소 · 단절점 · 다리 | **싣는다** — ① 「회색으로 가는 간선이 있다 ⟺ 사이클이 있다」를 가장 먼저 회색이 된 정점 논법으로 임의의 그래프에서 양방향 유도하고 ② 칸 접근 닫힌 형태 **`11V + 8E`** 를 실측 다섯(76 · 1,051 · 18,992 · 18,992 · 1,899,992)과 대조한 뒤 ③ 제약 규모 1,900,000 을 경로 전수 탐색 `2^25,000`(**7,526 자리**)과 견준다 |
| `fenwickRangeSum` | **싣는다** — 제곱근 분할(고정 길이 묶음 `B`=32)과 **두 축이 각각 뒤집힌다.** 같은 배열(`N`=1,024 · `A[i]=(41i) mod 97`)에서 갱신 0 회로 두고 질의를 늘리면 배열 접근이 질의 136 개에 6,465 대 6,458 로 밀리다가 **137 개에서 6,474 대 6,517 로 순서가 뒤집히고** 4,096 개에 46,049 대 165,628 이 된다. 반대로 질의 1,024 개를 고정하고 갱신을 넣으면 0 회에 15,350 대 42,199 로 앞서다가 **2,685 회에서 52,944 대 52,939 로 다시 뒤집혀** 16,384 회에 244,726 대 107,735 가 된다. 두 경계 다 산술과 맞는다(4,061 ÷ 29.8 = 136.2 → 137, 26,849 ÷ 10.0 = 2,684.9 → 2,685). 추가 칸 1,025 대 32 는 안 갈려 그것이 내주는 축이다 | **싣는다** — 이진 표현으로 분해하기. 본문이 T5·T6(앞 5 개 = 칸 5 + 칸 4, `5 = 101₂`)와 T12·T13(`4 = 100₂` · `2 = 10₂`)으로 이미 값을 보인 것에 이름을 붙이고 거듭제곱(`fastPower`)·이진 들어올리기로 편 밖 쓰임을 댄다 | **싣는다** — ① `pre(r) = tree[r] + pre(r − LSB(r))` 를 세워 읽는 칸 수가 **`r` 의 1 비트 개수**임을 임의의 `r` 에서 내고(제약 규모의 최댓값 16) ② 나이브 채우기 총 쓰기를 `Σ LSB(j) = (N/2)(log₂N + 2)` 로 닫아 `N` = 65,536 에서 **589,824 대 65,535(9.0배)** 를 낸다. 파트 1 은 다섯 칸에서 넘긴 횟수 셋만 보였다 |
| `singleNumberXor` | **생략** — 부류 **①+③**(`minMaxPair`·`enumerateSubmasks` 와 같은 모양). ③ 쪽: 「원소마다 다시 세기」·「맵으로 횟수 세기」·「정렬하고 이웃 견주기」는 경쟁 설계가 아니라 같은 문제의 느린 풀이이고 그 대조는 `deep.build` 가 이미 값으로 한다(`N`=1,001 에서 칸 접근 1,003,002 · 3,504 · 28,415 대 정본 1,001, 잡는 칸 3 · 501 · 1,003 대 2). ① 쪽: 진짜 경쟁 설계로 세울 수 있는 「집합 토글」과 「자리마다 1 의 개수를 세어 2 로 나눈 나머지」는 칸 접근 `32N` 대 `N` · 잡는 칸 32 대 2 로 **두 축 모두 열세**라 어떤 입력·제약에서도 안 뒤집힌다. 정렬된 입력에서 `2⌈log₂N⌉` 칸만 읽는 이분 탐색은 입력 계약이 다른 문제라 대조가 아니라 적용 범위다 | **싣는다** — 자기 자신이 역원인 연산(대합). T3 이 켠 자리 0 을 T5 가 그대로 끄고 그 사이 T4 는 다른 자리만 바꾼 것에 이름을 붙이고, 값 1 을 0 에 0~4 번 겹친 표(0·1·0·1·0)가 그것을 자리로 펼친다. 편 밖 쓰임은 RAID 패리티 복원 · 위치 해시 키의 증분 갱신 · 스트림 암호 · 집합의 대칭차 | **싣는다** — ① 자리별 홀짝을 `acc_j = (Σ c_v·v_j) mod 2 = u_j` 로 닫아 **임의의 `N`·임의의 배열**에 일반화하고(본문은 `N`=5 한 점) ② 약속을 하나 풀면 `acc = u ⊕ w` 라는 것을 실측 셋(`[1,1,3,5]`→6 · `[4,1,2,1,2,7]`→3 · `[9,9,9,6,6,6]`→15)으로 보이며 ③ 닫힌 형태 `5N+2` 대 `N(N+1)` 을 제약 상한에 넣어 **5,000,002 대 1,000,001,000,000(20만 배)** 을 낸다 |
| `kthSmallest` | **싣는다** — 중앙값의 중앙값과 **입력 모양 하나로 순서가 뒤집힌다.** `n`=1,024 고정에서 뒤섞인 입력(`A[i]=(i×7919) mod 10,007` · `k`=512)은 견주기 2,979 대 4,649 로 이쪽이 1.56배 적은데, 이 편의 기준값 규칙을 겨냥해 역산한 입력(`k`=1,024)에서는 **523,776 대 8,445 로 62배 많다.** 뒤집히는 첫 칸 수도 값으로 냈다 — `n`=11 은 55 대 60, **`n`=12 는 66 대 52**. 새로 잡는 칸은 0 대 317·624 로 끝까지 이쪽이 적어 **축마다 갈린다** | **싣는다** — 감소 정복. 파트 1 이 값으로 보인 「구간 6→4→3→2, 버린 쪽은 다시 안 본다」에 이름을 붙이고 이분 탐색·유클리드 호제법·퀵 정렬을 한 표에 놓아 갈리는 자리가 「이어서 보는 것이 하나인가 전부인가」임을 낸다. 이웃 `quicksort` 의 「선택 알고리즘」·`medianFromDataStream` 의 「순서통계량」과 겹치지 않게 골랐다 | **싣는다** — 점화식 `C(n) = (n−1) + C(s)` 를 세워 `n=2^t` 에서 **`C(2^t) = 2^(t+1) − t − 2`** 를 귀납으로 닫고 임의의 `n` 에 **`C(n) ≤ 2n−2`** 를 세운 뒤 제약 상한에서 **199,977 대 4,999,950,000(25,002.6배)** 을 낸다. 파트 1 은 여섯 칸에서 11 번 한 점만 보였다 |
| `undirectedCycleDetection` | **싣는다** — 서로소 집합과 **순서가 뒤집힌다.** 정점 100,000·간선 99,999 를 고정하고 **삼각형(정점 0·1·2) 간선 셋을 간선 목록의 어느 자리에 끼우는가**만 바꾸면, 맨 앞에서 배열 칸 접근 600,018 대 200,026 으로 서로소 집합이 3.0배 앞서다가 **33,334 번째에서 600,018 대 600,018 로 정확히 같고 33,335 번째부터 600,018 대 600,030 으로 뒤집혀** 맨 뒤에서 600,018 대 1,399,974 가 된다(이분으로 좁혔다). 내주는 축 셋 — 사이클이 정점 번호 뒤쪽일 때 1,599,985 대 200,026 · 사이클이 없을 때 1,599,993 대 1,399,984 · 새로 잡는 칸 400,000 대 200,000. 짝 편 `directedCycleDetection` 은 Kahn 을 상대로 「사이클이 정점 0 에서 몇 걸음인가」를 축으로 삼았고 이쪽은 **그래프를 그대로 두고 입력 순서만 바꾼다** | **싣는다** — 여분 간선의 개수(회로 랭크). 전개의 나무 간선 넷과 T9 가 만난 `[4,5]` 위에 이름을 붙이고 `E − V + k` 와 실측 여분 간선 수가 여섯 입력에서 일치함을 표로 보인 뒤, 맥케이브 순환 복잡도(`E − V + 2k`)와 전기 회로망의 독립 고리 방정식 수로 편 밖 쓰임을 댄다. 이웃 `connectedComponents` 의 「탐색이 만드는 숲」과 겹치지 않게 골랐다 | **싣는다** — ① `|F| = V − k` 를 세워 **여분 간선 ≥ 1 ⟺ 사이클**을 양방향 유도하고 ② 거기서 **`E ≥ V` 면 순회 전에 답이 정해진다**를 내며(제약 상한 둘을 동시에 못 채운다는 결론이 여기서 나온다) ③ 칸 접근 닫힌 형태 **`10V + 6E − k`** 를 실측 여섯(82 · 45 · 15,993 · 15,993 · 14,250 · 1,599,993)과 대조한 뒤 제약 규모 1,599,993 을 간선 지우기 방식의 9,999,800,001 과 견준다 |
| `nextGreaterElement` | **싣는다** — 구간 최댓값 트리와 **두 축이 각각 뒤집힌다.** 같은 배열(`N`=1,024 · `nums[i]=(7919i) mod 1009`)에서 갱신 0 회로 두고 묻는 자리를 늘리면 배열 접근이 53 개에 8,199 대 8,194 로 밀리다가 **54 개에서 8,200 대 8,211 로 뒤집히고** 4,096 개에 12,242 대 88,129(7.2배)가 된다. 반대로 질의 1,024 개를 고정하고 갱신을 넣으면 0 회에 9,170 대 27,406 으로 앞서다가 **3 회에서 33,611 대 27,502 로 다시 뒤집혀** 1,024 회에 8,355,655 대 59,569(140배)다. 두 경계 다 산술과 맞는다(981 ÷ 18.4 = 53.3 → 54, 18,236 ÷ 8,115 = 2.25 → 3). 추가 칸 1,039 대 2,048 은 안 갈려 그것이 얻는 축이다 | **싣는다** — 괄호 구조. 전개 T1~T9 가 낸 「기다리는 구간」 다섯(`[T1,T6]`·`[T2,T3]`·`[T4,T5]`·`[T7,T9]`·`[T8,T9]`)이 포개지거나 떨어지고 **절반만 걸치지 않는** 것에 이름을 붙인다. 편 밖 쓰임은 괄호 짝 맞추기·식 파싱·`dfsTraversal` 의 괄호 정리. 이웃 편의 「분할 상환 분석」·「접두사 최솟값」과 겹치지 않게 골랐다 | **싣는다** — 파트 1 은 다섯 칸에서 `S`=2 **한 점**만 보였는데, 기록 집합을 정의해 「끝난 시점의 스택 = 기록」을 양방향으로 유도하고 `P = N − S` 를 닫은 뒤 무작위 순열에서 **`E[S] = H_N` · `E[B] = N − H_N`** 을 낸다(길이 1~8 순열 전수 평균과 차이 0). 거기서 평균 접근 `8N − 5H_N` = 799,939.5 와 최악 `8N − 7` = 799,993 이 **53.5 번** 차이라는 것이 나오는데 본문 값으로는 안 얻어진다 |
| `extendedEuclidean` | **싣는다** — 최소 절댓값 나머지 판(몫을 내림 대신 반올림)과 **순서가 뒤집힌다.** 전개 입력(510·183) 걸음 6 대 4 · 기본 연산 85 대 78 로 중심 판이 앞서고 피보나치 이웃 89 대 45 · 1,247 대 943 으로도 중심 판이 앞서는데, **펠 수열 이웃(P(48)·P(47)) 49 대 49 · 687 대 835** 와 **큰 소수 쌍 9 대 8 · 127 대 142** 에서 나머지 판이 앞선다. 경계를 닫힌 형태로 냈다 — 나머지 판 `14×걸음+1`, 중심 판 `17×걸음+4×옮긴 걸음+2` 라 **걸음이 정본의 2/3 아래로 줄어야** 중심 판의 연산도 적다(비율 0.51·0.67 대 1.00·0.89). 계수의 절댓값은 넷 다 같아 그 축은 안 갈린다. `gcd` 편은 이진 GCD 와 「나눗셈 한 번의 값」을 축으로 삼았고 이쪽은 몫의 분포와 걸음당 연산 수라 안 겹친다 | **싣는다** — 일차 디오판토스 방정식. 전개 T7 이 값으로 보인 **버린 계수 쌍 `(-61, 170)`** 에 이름을 붙인다 — `|s1| = B/g = 61` · `|t1| = A/g = 170` 이고 `510×(-61) + 183×170 = 0`. 편 밖 쓰임은 CRT·동전 조합·격자점 세기. `gcd` 편의 「연분수」(몫 수열)와도 `fastPower` 의 「결합법칙만 요구하는 거듭제곱」과도 겹치지 않는다 | **싣는다** — `A·s_i + B·t_i = r_i` 를 귀납으로 임의의 `i` 에 닫고 행렬식 `D_i = (-1)^i` 를 유도해 `|s_{n+1}| = B/g` · `|t_{n+1}| = A/g` 를 낸 뒤, **마지막 몫이 2 이상**이라는 사실로 **`|x| ≤ B/(2g)` · `|y| ≤ A/(2g)`** 를 닫는다. 제약 규모 계수는 **617 자리(2,048비트급) 입력에서 `|x|` 616 자리** — 계수가 입력보다 커지지 않는다. `A = B` 가 상한 밖인 것까지 적었다 |
| `isBipartite` | **싣는다** — 쪽 관계 서로소 집합(정점마다 대표까지의 홀짝을 함께 드는 판)과 정점 100,000 고정·간선 0~200,000 스윕에서 **두 축이 반대 방향으로 각각 뒤집힌다.** 배열 칸 접근은 간선 100,005 개에 1,800,050 대 1,800,049 로 밀리다가 **100,006 개에서 1,800,060 대 1,800,061 로 뒤집혀** 상한 200,000 개에 2,800,000 대 2,999,985 가 되고, 새로 잡는 칸은 반대로 49,999 개에 299,999 대 300,000 으로 앞서다가 **50,000 개에서 300,001 대 300,000 으로 뒤집힌다**(경계가 `200,000 + 2E + 1 > 300,000` 산술과 일치). 이웃 `undirectedCycleDetection` 은 평범한 서로소 집합을 상대로 간선 목록에서의 자리를 축으로 삼았고 이쪽은 **간선 수 대 정점 수의 비**다. 상대를 2n 판이 아니라 홀짝을 드는 3n 판으로 고른 사유(정점 축에서 미리 불리한 상대를 세우지 않으려고)를 `.alt.ts` 헤더에 적었다 | **싣는다** — 그래프 채색. 본문이 값으로 보인 `side` 배열에 이름을 붙이고 필요한 값의 가짓수를 일곱 그래프에서 실측(1·2·2·2·3·3·4)해 **「2 이하 ⟺ 이분」**을 표로 보인다. 편 밖 쓰임은 레지스터 할당·시험 시간표·주파수 배정. 이웃들의 「여분 간선」·「간선 분류」·「선형 확장」·「매트로이드」·「최우선 탐색」과 겹치지 않는다 | **싣는다** — ① `c(v) = d(v) mod 2` 를 세워 **이분 ⟺ 홀수 길이 고리 없음**을 임의의 그래프에서 양방향 유도하고(역방향은 두 나무 경로가 갈라지는 정점 `w` 를 잡아 길이 `d(u)+d(v)+1−2d(w)` 인 홀수 고리를 실제로 구성) ② 칸 접근 닫힌 형태 **`8V + 10E`** 를 덩어리 수 `k` 가 상쇄되는 것까지 유도해 실측 여섯과 대조한 뒤 ③ 제약 규모 2,800,000 을 나눔 전수 검사 `2^100000`(**30,103 자리**)과 견준다 |
| `searchInRotatedSortedArray` | **싣는다** — 「최솟값이 있는 자리를 먼저 찾는 **두 번 통과**」와 **전개 입력 그대로**(21칸) 두 축이 각각 뒤집힌다. 배열을 고정하고 질의를 늘리면 배열 칸 접근이 질의 1회에 9 대 15 · 2회에 16 대 18 로 앞서다가 **3회에서 25 대 22 로 뒤집히고**, 질의 21회를 고정하고 갱신(왼쪽 한 칸 순환 이동)을 섞으면 0회에 166 대 89 로 밀리다가 7회에 151 대 151 로 같아지고 **8회에서 149 대 161 로 다시 뒤집힌다**. 내주는 축은 배열이 안 바뀔 때의 질의당 비용, 얻는 축은 전처리로 들고 있는 칸 0 대 1 과 배열이 바뀌어도 다음 호출이 그대로 옳은 것이다 | **싣는다** — 순환 이동. 파트 1 이 값으로 보인 `A[(j+p) mod 21]` 대응에 이름을 붙이고 문자열 회전 판정(`s+s`)·링 버퍼의 `head`·제자리 회전의 세 번 뒤집기로 편 밖 쓰임을 댄다. `binarySearch` 의 「단조 술어와 이분법」은 **「무엇을 물을 것인가」**이고 이쪽은 **「배열이 어떤 구조인가」**라 층이 다르다 | **싣는다** — 순환 이동 정의에서 **「`A[ℓ] ≤ A[m]` ⟺ 끊긴 자리가 `[ℓ, m)` 밖」**을 임의의 `N`·임의의 `p` 에서 양방향 유도하고, 거기서 **등호가 왜 필요한지**(`ℓ = m` 이면 `[ℓ,m)` 가 빈 구간)를 낸다 — 파트 1 은 `N=21, p=4` 한 점만 보인다. 이어 읽는 칸 상한 `3(⌊log₂N⌋+1)` 을 닫아 `N=10^6` 에서 **60 대 선형 1,000,000** 을 낸다 |
| `largestRectangleInHistogram` | **생략** — 부류 **①**(모든 축에서 한쪽이 낫다). 경쟁 설계 둘을 `N`=1,024 에서 실측했는데 뒤집히는 축이 없다 — 분할 정복 + 희소 표는 자료 접근 47,164 대 9,214 이고 추가 칸도 9,228 대 1,025 로 열세, 높이 오름차순 + 서로소 집합은 23,566~27,334 대 9,214 이고 추가 칸 `3,284+K` 로 열세다. `N` 을 4~16,384 로 높이 범위 `K` 를 2~10,001 로 스윕해도 **뒤집힘 0**(희소 표는 갱신이 불가능하고 서로소 집합은 세는 정렬을 써도 못 따라온다). 이웃 `nextGreaterElement` 가 쓴 축(질의 수·갱신 수)은 이 문제에서 두 설계 다 전면 재계산이라 갈리지 않는다 | **싣는다** — 기여도 분해. 본문이 값으로 보인 것(구간 21개를 최솟값 자리로 나눈 무리 1·10·2·1·6·1)에 이름을 붙이고 부분배열 최솟값의 합·기여 횟수 세기로 편 밖 쓰임을 댄다. 이웃 `nextGreaterElement` 의 「괄호 구조」는 **스택의 시간 구조**이고 이쪽은 **구간을 대표 자리로 나누는 세는 방법**이라 겹치지 않는다 | **싣는다** — 두 경계를 `L(j) = max{k<j : h[k] ≤ h[j]}` · `R(j) = min{k>j : h[k] < h[j]}` 로 정의하고 **`A* = max h[j](R−L−1)` 가 구간 `N(N+1)/2` 개의 최댓값과 같음을 양방향으로 유도**한다(⊇ 방향에서 「가장 왼쪽 최솟값」을 잡는 것이 등호의 비대칭을 정당화한다). 제약 상한에서 5,000,050,000 → 100,000 으로 **50,000.5 배** 감축 |
| `treeMaxIndependentSet` | **생략** — 부류 **①+②**(`treeDiameter`·`topologicalSort` 와 같은 조합). 경쟁 설계 **둘을 실제로 구현해** 재고 뺐다. 일반 그래프 분기 한정은 `n`=2,001 에서 별 14,003 대 정본 10,000 · 애벌레 `s`=16 12,310,621 · `s`=32 와 경로는 4천만 회 상한에서 미완으로 **모든 모양에서 열세**이고, 앞서는 축은 저장 칸 6,003 대 12,004 뿐인데 같은 `Θ(n)` 안의 2배라 제약(`n ≤ 10^5` · 256MB)에서 문제가 안 된다. 이분 그래프 최소 컷(Dinic)은 별 20,009 · 애벌레 28,055 · 완전 이진 35,672 · 경로 31,008 대 10,000 이고 저장 칸도 16,014 대 12,004 이며 `n`=3~257 스윕에서 뒤집히는 첫 자리가 없다 — 앞서는 축 「사이클이 있는 이분 그래프에도 쓸 수 있다」는 값이 아니라 **적용 범위**이고 이 문제 제약이 「연결된 무방향 트리」로 고정이라 발동하지 않는다 | **싣는다** — 최소 가중 정점 덮개. 전개에서 고른 쪽 `{0,3,4,5}` 의 여집합 `{1,2,6}` 이 여섯 간선을 전부 덮는 것을 표로 보이고 `W − 답` 이 곧 최소 가중 덮개임을 네 입력에서 실측(32/22/10 · 22/20/2 · 14/10/4 · 16/10/6). 가장 가까운 `houseRobber` 가 「가중치가 있는 최대 독립 집합」을 이미 썼으므로 그 이름을 피하고 **여집합 쪽**을 골랐고 쾨니그 정리·2배 근사로 편 밖 쓰임을 댔다 | **싣는다** — ① 집합족 `I(v)` 를 `v` 포함/미포함으로 갈라 전이식을 **유도**하고(본문은 값으로만 보였다) ② **트리가 2색으로 칠해진다는 것에서 답의 범위**를 `⌈n/2⌉ ≤ 답 ≤ n−1` 로 닫는다(가중치가 전부 1). `n`=100,000 에서 경로 50,000(하한 달성)·별 99,999(상한 달성)·애벌레 50,000, 완전 이진(높이 15) `n`=65,535 에서 43,690 |
| `lowestSetBit` | **생략** — 부류 **①+③**(`minMaxPair`·`enumerateSubmasks`·`singleNumberXor` 와 같은 조합). ③ 쪽: 「자리 0 부터 차례로 검사」는 경쟁 설계가 아니라 같은 문제의 느린 풀이이고 그 대조는 `deep.build` 가 이미 값으로 한다(자리 31 에서 161 대 2 · 1~10^6 순차 10,999,965 대 2,000,000). ① 쪽: 진짜 경쟁 설계 셋을 **실제로 구현해** 쟀다 — `x ^ (x & (x-1))` 3 대 2 · de Bruijn 표 조회 6~7 대 2(잡는 칸 32 대 0) · 이분 탐색 17 대 2. `0 ≤ x < 2^16` 전수 65,536 개와 자리 0~31 대표값 32 개 스윕에서 **정본보다 연산이 적거나 칸이 적은 자리가 0 개**다 | **싣는다** — 법 `2^32` 에서의 덧셈 역원. 본문이 값으로 보인 「`-x` 를 부호 없는 값으로 읽으면 `2^32 − x`」에 이름을 붙이고 여섯 입력 전수 일치를 보였다. 편 밖 쓰임은 기계의 뺄셈 `a + (2^32 − b)` · 해시 혼합의 32 비트 자르기 · 2 의 거듭제곱 크기 순환 버퍼의 `(i − 1) & (n − 1)` 이고 이웃의 자리내림 전파·대합·이진 표현 분해·모노이드와 안 겹친다 | **싣는다** — 르장드르 합 `Σ_{x=1..N} ν(x) = N − s₂(N)` 을 합 순서 바꾸기 두 번과 등비수열로 닫는다. 본문은 자리 일곱 점만 쟀는데 식이 **자리 수의 평균이 1 로 수렴**한다는 것을 주고, 거기서 1~N 을 한 번씩 넣으면 5.5 배·최악 입력만 넣으면 80.5 배라는 두 수의 거리가 나온다(`N`=8·10·100·1,000 에서 `Σν`=7·8·97·994 로 `N − s₂(N)` 과 전수 일치) |
| `slidingWindowMaximum` | **싣는다** — 조각마다 접두·접미 최댓값 표를 미리 적어 두는 판(van Herk 1992 · Gil–Werman 1993)과 **두 축이 각각 뒤집힌다.** 창 크기 축: 감소 수열 1,024 칸 고정에 `k`=2 는 9,208 대 8,189 로 조각 표가 앞서다 `k`=44 에 9,040 대 9,039 로 좁혀지고 **`k`=45 에서 9,036 대 9,038 로 뒤집혀** `k`=1,024 에 5,120 대 6,145 가 된다. 입력 모양 축: `k`=45 고정에 감소 수열은 9,036 대 9,038(이쪽)인데 곱셈 나머지는 10,999 대 9,038(조각 표)로 갈린다 — 조각 표의 접근 수가 **배열의 값에 전혀 안 매인다.** 경계를 닫힌 형태로 냈다 — 이쪽 `9N − 4k`, 조각 표 `9N − 3k + 3 − 2⌈N/k⌉` 라 `2⌈N/k⌉ < k + 3` 일 때 이쪽이 적고 대략 `k > √(2N)` 이며 `N`=1,024 에서 √2048 = 45.25 다 | **싣는다** — 형태학의 팽창과 침식. 본문이 낸 답 `[3 3 5 5 6 7]` 이 곧 평평한 구조 요소의 **팽창**이고 부등호를 뒤집은 `[-1 -3 -3 -3 3 3]` 이 **침식**임을 값으로 대응시켰다. 편 밖 쓰임은 최대·최소 필터 · 열림 · **2 차원 사각형 구조 요소가 1 차원 두 번으로 갈라진다**는 것이고 이웃 넷의 이름(괄호 구조·기여도 분해·분할 상환·지배 관계)과 안 겹친다 | **싣는다** — 후보 집합 `C(i)` 를 정의해 「걸음 `i` 를 끝낸 시점의 통 = `C(i)`」를 양방향으로 유도하고 `|C(i)| ≤ min(k, i+1)` 과 `P + F = N − |C(N−1)|` 을 얻은 뒤 총식 `T(N,k) = 3N − 1 + F + 3P + 2B + 3(N−k+1)` 을 닫는다. 본문 값만으로는 안 나오는 것이 상한 `11N − 3k − 5` 이고 두 경우(`B = N−1` → `11N−3k−6`, `B = N−2` → `11N−3k−5`)를 갈라 유도했다. 제약 상한에서 1,096,995 대 창마다 다시 세기 99,100,001 로 90.3 배다. **상한이 타이트하지 않다는 것을 절 안에 적었다** — 여섯 모양 전수 스윕의 실측 최대 11,040 과 그 `k` 의 상한 11,145 가 105 차이다 |
| `matrixChainMultiplication` | **생략** — 부류 **①+③**. ③ 쪽: 전수 괄호 배치와 가지치기를 붙인 변형은 경쟁 설계가 아니라 같은 문제의 느린 풀이이고 `deep.build` ②가 이미 값으로 대조한다(호출 수 `3^(n-1)`, 제약 상한에서 1.71 × 10^47). 분기 한정도 `n`=12 에서 88,573 대 286(310 배)이고 `n`≥14 는 4 천만 회 상한 안에서 안 끝난다. ① 쪽: 진짜 경쟁 설계인 **하향식 메모이제이션**을 구현해 재니 `n`=100 에서 166,650 대 499,951 로 3.0 배 열세이고 **`n`=2~200 × 차원 모양 4 = 796 칸 전수 스윕에서 기본 연산이 뒤집힌 자리가 0** 이다. 앞서는 축은 저장 칸 하나뿐인데 10,201 대 5,050 으로 같은 `Θ(n²)` 안의 2 배이고 제약 상한에서 82 KB 라 256 MB 제한에서 발동하지 않는다(`treeMaxIndependentSet` 과 같은 모양) | **싣는다** — 볼록 다각형의 삼각분할. 본문이 값으로 보인 합치는 비용 `p[i-1]·p[k]·p[j]` 에 이름을 붙인다 — 차원 배열을 꼭짓점 `v0`~`v4` 로 읽으면 그 식이 그대로 삼각형 `(v_(i-1), v_k, v_j)` 의 무게이고, 표가 고른 `k` 셋의 무게 합 1,500 + 3,000 + 500 = 5,000 이 답과 같다. 편 밖 쓰임은 최적 다각형 삼각분할 · 삼각분할 개수가 배치 개수와 같은 수열이라는 것 · `O(n log n)` 해법이 이 대응 위에 선다는 것이고 이웃 dp 편 다섯의 이름과 안 겹친다 | **싣는다** — 닫힌 형태 둘. ① 배치 수 점화식 `P(n) = Σ_q P(q)P(n−q)` 를 `C(2n−2, n−1)/n` 으로 닫고 ② 후보 총수 `Σ_L (n−L+1)(L−1)` 를 `(n³−n)/6` 으로 닫는다. 점화식·닫힌 형태·전수 세기 셋이 `n`=2~10 에서 일치하고, `n`=100 에서 배치 2.27 × 10^56(57 자리) 대 후보 166,650 이라 **그 배수가 52 자리**다. 파트 1 은 행렬 넷에서 배치 5 개와 후보 10 개만 세었을 뿐이라 두 수가 갈라지는 속도를 값으로는 알 수 없다 |
| `binomialModP` | **싣는다** — 파스칼 삼각형을 한 줄만 들고 채워 가는 판(덧셈만 쓰고 역원도 소수 법도 필요 없다)과 **순서가 뒤집힌다.** `k = ⌊n/2⌋` 고정에서 법 `10^9+7` 은 `n`=12 에 파스칼 114 대 116 으로 앞서다 **`n`=13 에서 126 대 116 으로 뒤집힌다.** 그 자리를 정하는 것이 법이다 — 101 로 낮추면 역원 상수 항이 92 에서 24 로 줄어 `n`=7 에 36 대 36 으로 **같아지고** `n`=8(40 대 52)부터 뒤집힌다. 두 계수를 식으로 닫아 경계의 출처를 냈다 — 정본 `4·min(k,n−k) + 2(비트 수 + 1 인 비트 수) + 2`, 파스칼 `2·(min 까지의 합 + 나머지 줄 × min)` 이라 **상수 항이 이차 항에 따라잡히는 자리**가 곧 경계이고 네 점이 전부 식과 맞는다. `n`=100,000 에서 200,092 대 7,500,050,000 이고 **저장 칸 7 대 50,001 은 어느 `n` 에서도 안 갈려** 그것이 끝까지 앞서는 축이다. 내주는 축은 작은 `n` 의 연산 수와 적용 범위(파스칼은 합성수 법도 된다) | **싣는다** — 자리올림 세기(쿠머 정리). 본문이 값으로 보인 「자리 하나라도 `k_i` 가 `n_i` 보다 크면 곱이 0」에 이름을 붙이고, `k` 와 `n − k` 를 `p` 진법으로 더한 자리올림 횟수와 대응시킨 표를 실측으로 냈다(자리올림 0 번인 두 줄에서만 답이 0 이 아니다). **자리올림이 둘 이상일 때 지수가 정확히 그만큼이라는 것은 이 글이 값으로 안 보였다고 절 안에 적었다.** 편 밖 쓰임은 르장드르 공식 · 파스칼 삼각형 mod 2 의 시에르핀스키 삼각형 · `n!` 끝의 0 개수 | **싣는다** — ① `i·C(p,i) = p·C(p−1,i−1)` 에서 프로베니우스 `(1+x)^p ≡ 1 + x^p` 를 거쳐 **뤼카 정리를 임의의 `n`·`k` 로 유도**하고 ② 답이 0 이 아닌 `k` 의 개수를 `∏(n_i + 1)` 로 닫는다. `n`=100,000·법 2 에서 후보 100,001 개 중 **64 개**(자릿수 열일곱 중 1 인 자리 여섯 → `2^6`)이고, 같은 `n` 에 법 `10^9+7` 이면 100,001 개 전부다. 다섯 입력 전수 대조로 닫힌 형태와 실제 개수가 일치한다 |
| `palindromePartitioningMinCut` | **생략** — 부류 **①**(모든 축에서 한쪽이 낫다). 중심 확장 + 즉시 컷 갱신(`O(n²)` 시간 · `O(n)` 칸)을 구현해 다섯 입력 계열 × 다섯 규모 **25칸 전수**로 쟀는데 뒤집히는 자리가 **0** 이다 — `n`=2,000 에서 글자가 전부 같으면 12,000,001 대 10,003,001 · 두 글자 번갈아면 13,501,501 대 5,008,999 · 세 글자 되풀이면 14,002,999 대 19,993 이고 저장 칸은 4,002,000 대 2,000 이다. 산술로도 안 뒤집힌다 — 회문 부분문자열이 최대 `n(n+1)/2` 개라 중심 확장의 계수 상한이 `2.5n²` 인데 표 방식의 하한이 `3n²` 다. `n`=4~2,000 스윕에서 표가 앞선 자리가 없다 | **싣는다** — 회문의 중심. 판정표의 참인 칸 여덟을 `i+j` 로 묶으면 사슬이 되는 것을 값으로 보인 뒤 이름을 붙이고 최장 회문 부분문자열·회문 개수 세기·Manacher 로 편 밖 쓰임을 댄다 | **싣는다** — `|P(s)|` 의 상·하한을 **등호 조건과 함께** 닫고, 답이 `n−1` 이 되는 조건을 「길이 2 이상 회문은 길이 2·3 회문을 중심에 품는다」로 유도해 이웃 두 자리 검사로 환원한 뒤 알파벳 3종·길이 7 전수 2,187 개에서 어긋남 0 을 확인한다. `2^(n−1)` 과 `n²` 의 배수가 제약 상한에서 **596 자리**다 |
| `matrixPowerFibonacci` | **생략** — 부류 **①+②+③** 이 한 편에 함께 나온 첫 자리다. 배가 공식(fast doubling)을 반복판으로 구현해 `n`=1~8,192 **전수 스윕**으로 쟀는데 행렬 쪽이 적은 자리가 곱셈 **0 개** · 자릿수 곱 **0 개** 이고 동률도 0 개이며 저장 칸은 9 대 4 로 어느 `n` 에서도 안 갈린다(`n`=10⁶ 에 곱셈 216 대 60 · 자릿수 곱 합 238,937,202,301 대 43,676,102,223). **②** 이 편이 앞서는 유일한 축은 「계수만 바꾸면 임의의 `k` 차 선형 점화식으로 그대로 확장된다」인데 값이 아니라 **적용 범위**이고 이 문제의 계약(피보나치 하나)에서는 발동하지 않는다. **③** 「한 걸음씩 더하기」는 경쟁 설계가 아니라 같은 문제의 느린 풀이이고 `deep.build` ②·④ 가 이미 값으로 대조한다(뒤집히는 첫 `n`=128 · 아홉 자리 뒤 160 부터 굳음) | **싣는다** — 동반 행렬. 본문이 값으로 세운 `M = [[1,1],[1,0]]` 에 이름을 붙이고 계수만 바꿔 루카스·펠·트리보나치를 같은 절차로 실행해 정의로 센 값과 일치함을 보인다(스칼라 곱셈 48 대 162 = `k³` 배). 편 밖 쓰임은 다항식 근 찾기(특성다항식 = 동반 행렬의 고윳값)와 제어 이론의 상태 공간 | **싣는다** — 임의의 `n` 의 **자릿수**다. 특성다항식에서 고윳값·비네를 거쳐 `D(n) = ⌊n·log₁₀φ − ½·log₁₀5⌋ + 1` 을 닫고 `n`=2~2,000 전수 대조 어긋남 0 을 확인한 뒤 제약 상한에서 **208,987,640,249,978,734 자리**를 낸다. 파트 1 이 실제로 실행한 최대가 `n`≈10⁶ 이라 이 값은 실행으로 못 얻는다(사이드카는 `bigint` 고정소수점 로그로 계산 — `number` 로는 자리가 어긋난다) |
| `sieveOfEratosthenes` | **싣는다** — 조각으로 갈라 채우는 판(조각 길이 1,024 고정)과 **잡는 칸 축에서 순서가 뒤집힌다.** `n`=1,067 에 1,068 대 1,068 로 **정확히 같다가** `n`=1,068 에서 1,069 대 1,068 로 갈린다 — 조각 쪽이 `⌊√n⌋+1 + π(⌊√n⌋) + 1,024` 인데 `n`=1,068 은 제곱근이 그대로 32 라 조각 쪽만 안 늘어난다. 배열 칸 접근은 어느 상한에서도 안 뒤집혀(100 만에 4,123,047 대 4,242,487 · 차이 119,440) 그것이 얻는 축이고, 내주는 축은 큰 상한의 잡는 칸이다(제약 최댓값에서 456 배). `.alt.ts` 의 `flipPoint()` 가 2~4,000 을 재서 첫 자리가 1,068 임을 실행 시점에 검증하고 상수와 어긋나면 던진다 | **싣는다** — 약수의 짝 대응. 본문이 값으로 이미 보인 것(`i` 의 제곱 시작점 · `√n` 상한)에 이름을 붙이고 36·30·25·97 의 약수 짝 표로 그림 의무를 진다. 편 밖 쓰임은 약수 전수 열거·소수 판정·인수분해 | **싣는다** — 파트 1 은 `n`=30 의 24 회 **한 점만** 보이는데, 닫힌 형태 `W(n) = Σ_{p≤√n}(⌊n/p⌋−p+1)` 을 일곱 상한에서 실측과 **정확히 일치** 확인하고 메르텐스 제2정리로 `Θ(n log log n)` 을 닫아 제약 규모의 **2,122,048 대 시행 나눗셈 67,740,404** 를 낸다 |
| `digitDp` | **싣는다** — 별과 막대 + 포함배제 닫힌 형태와 **순서가 뒤집힌다.** `N` 을 아홉 열다섯 개로 고정하고 `K` 만 115 → 116 으로 하나 올리면 기본 연산이 19,899 대 18,879 로 밀리다가 **19,088 대 19,128 로 뒤집힌다.** 두 비용이 `K` 에 반대로 움직이는 것이 경계의 출처다 — 닫힌 형태는 포함배제 항이 `⌊t/10⌋+1` 개라 `K` 에 비례해 늘고, 자릿수 DP 는 상태 수가 `K` = 67 에서 가장 크고 그 뒤로 갈래 ①(남은 자리로 `K` 에 못 미친다)이 늘어 줄어든다. 경계를 정하는 것은 `N` 의 자릿수 합이라 `10^15`(합이 1)로 바꾸면 35,649 대 201 로 어느 `K` 에서도 안 뒤집힌다. 내주는 축은 저장 칸 1,740 대 0 과 `K` 가 116 아래일 때의 연산 수이고, **앞서는 축은 정확도다** — 닫힌 형태는 중간 항 `C(88,14)` 가 `2^53` 의 0.71 배라 배정밀도로 `K` = 74 부터 어긋나고 `K` ≤ 135 에서 그런 `K` 가 60 개다 | **싣는다** — 사전식 순서로 세기(랭킹). 전개가 `[0,194]` 를 맨 왼쪽 자리 0/1 두 무리로 가른 것에 이름을 붙이고, 갈라지는 자리별 개수 100·90·4·1 의 합이 `N+1` 임을 값으로 낸 뒤 `k` 번째 순열(팩토리얼 진법)·트라이의 사전순 `k` 번째로 편 밖 쓰임을 댄다. 이웃 dp 편 여섯(마르코프 성질·집합의 덧셈·생성 함수·단위 가중 최단 경로·볼록 다각형 삼각분할·회문의 중심)의 이름과 안 겹친다 | **싣는다** — 생성 함수 `((1−z^10)/(1−z))^m` 에서 포함배제로 `W(m,t) = Σ_j (−1)^j C(m,j) C(t−10j+m−1, m−1)` 을 닫고, 답 전체를 「처음 작아지는 자리」로 갈라 `A(N,K) = Σ_p Σ_x W(L−p−1, K−S_p−x) + [S_L=K] − [K=0]` 을 유도해 전개 입력에서 9+9+1 = 19 로 검산한다. 본문 값만으로는 안 나오는 것 — 제약 상한에서 식이 `W(15,K) + [K=1]` 한 줄로 줄고, `K`=1 의 답 16 · 최댓값 `K`=67 의 35,460,394,945,125 · `Σ_t W(15,t) = 10^15` 이 그 닫힌 형태에서 나온다 |
| `isPrimeTrial` | **싣는다** — 결정론적 밀러-라빈(상한마다 알려진 밑 목록)과 각 규모의 가장 큰 소수에서 기본 연산이 `n`=997 에 23 대 36 · `n`=9,973 에 67 대 88 로 이쪽이 앞서다가, **소수 24,571(105 대 108)을 마지막으로 그다음 소수 24,593 에서 105 대 76 으로 순서가 뒤집히고** `n`=999,999,999,989 에서 666,667 대 600 이 된다. **「뒤집히는 첫 자리」 대신 「마지막으로 앞선 자리」를 낸 첫 편이다** — 밀러-라빈의 계수가 지수의 비트 패턴 때문에 `n` 에 대해 단조롭지 않아 이웃한 두 소수의 값이 오르내리고, 그래서 첫 자리는 요동에 흔들린다. `.alt.ts` 의 `lastAhead()` 가 5 부터 600,000 까지 소수를 전부 재서 24,571 뒤로 다시 앞서는 자리가 없음을 실행 시점에 확인한다. 저장 칸 3 대 12 는 어느 규모에서도 안 갈려 그것이 끝까지 앞서는 축이고, 내주는 축은 큰 `n` 의 연산 수다. 같은 「기본 연산 한 번」이 두 설계에서 다른 일(`n % d` 대 `10^24` 규모의 모듈러 곱셈)이라는 것도 절 안에 적었다 — 그 차이는 밀러-라빈 쪽에 불리한데도 큰 `n` 에서 밀러-라빈이 앞선다 | **싣는다** — 바퀴 인수분해. 전개가 값으로 보인 「9 를 건너뛴다」와 후보 밀도 1 → 1/2 → 1/3 → 4/15 에 이름을 붙이고 바퀴 `M` 별 후보 나머지 표로 그림 의무를 진다. 편 밖 쓰임은 에라토스테네스의 체의 2·3 바퀴 변형 · 인수분해 전 사전 나눗셈 · 소수 생성기다. **`√n` 경계의 근거인 「약수의 짝 대응」은 `sieveOfEratosthenes` 가 이미 썼으므로** 이 편은 그 이름을 피해 후보 생성 규칙 쪽을 골랐고, `gcd` 의 연분수 · `fastPower` 의 모노이드 거듭제곱 · `extendedEuclidean` 의 일차 디오판토스 방정식 · `binomialModP` 의 자리올림 세기와도 겹치지 않는다 | **싣는다** — 후보 집합 `D(x)` 를 정의해 나머지 두 무리로 갈라 `C(x) = ⌊(x+1)/6⌋ + ⌊(x−1)/6⌋` 을 유도하고 열 점에서 실측과 대조한 뒤, 임의의 바퀴로 넓혀 `C_M(x) = (φ(M)/M)·x + O(1)` 을 세우고 **오차 항이 여섯 바퀴에서 정확히 1 임**(식이 후보로 안 쓰는 1 을 함께 센다)을 실측으로 낸다. 파트 1 이 값으로 못 낸 것 둘이 여기서 나온다 — 임의의 `x` 에 대한 닫힌 형태와, 밀도의 곱 `∏(1 − 1/p_i)` 로 본 「바퀴를 키워 얻는 몫이 매번 절반 아래로 준다」(0.5000 → 0.1667 → 0.0667 → 0.0381 → 0.0208)다 |
| `convexHull` | **싣는다** — 선물 포장과 점 4,096 개 고정에서 껍질 크기 `h` 로 갈린다. `h`=13 에 61,626 대 64,450 으로 포장이 앞서다가 **`h`=14 에서 65,570 대 64,385 로 순서가 뒤집힌다.** 내주는 축은 잡는 칸 — 점 100,000 개·`h`=350 에서 300,350 대 200,350 으로 포장이 10만 칸 적다 | **싣는다** — 단조 스택. 본문이 값으로 보인 「담기 총수 − 걷어내기 총수 = 남은 개수」에 이름을 붙이고 히스토그램 최대 직사각형·다음 큰 원소·`convexHullTrick` 으로 편 밖 쓰임을 댄다 | **싣는다** — 상환 논증으로 방향 판정 상한 `4m − h − 6` 을 임의의 `m`·`h` 에 닫고(실측 다섯과 대조), 배정밀도 오차 한계 `err(C) = C²/2^49` 를 유도해 `SAFE = 2048` 의 근거와 **걸러내기가 불필요해지는 좌표 상한 23,726,566** 을 낸다 |
| `lowestCommonAncestor` | **싣는다** — 오일러 투어 + 희소 표와 정점 100,000 완전 이진 트리에서 질의 수로 갈린다. 질의 0 회에 배열 칸 7,999,991 대 21,889,258 로 조상 표가 2.7배 적다가 **질의 430,000 회에서 25,152,035 대 24,899,258 로 순서가 뒤집힌다.** 저장 칸 1,800,000 대 4,199,980 은 안 갈려 그것이 얻는 축이고, 내주는 축은 질의 하나의 비용(55 대 7) | **싣는다** — 함수 그래프와 그 거듭제곱. 본문이 값으로 보인 `anc[·][k]` 에 이름을 붙이고 순열 반복 적용·`k` 번 이동 후 위치·사이클 찾기·`fastPower` 로 편 밖 쓰임을 댄다 | **싣는다** — `up(v,a+b) = up(up(v,a),b)` 로 점화식을 유도하고, `A(u,k) ≠ A(v,k) ⟺ 2^k < h` 동치와 불변식 `h_k ≤ 2^(k+1)` 로 「함께 올리기가 답 바로 아래에서 멈춘다」를 임의의 트리에서 닫아 `L`=17 · 표 칸 1,700,000 · 질의당 표 참조 **최대 51** 을 낸다 |
| `ahoCorasick` | **싣는다** — 패턴마다 KMP 를 따로 돌리는 설계와 텍스트 10만·길이 합 10만 고정에서 패턴 개수로 갈린다. `k`=1 에 8,600,053 대 507,644 로 KMP 가 16.9배 적다가 **`k`=40 에서 8,598,553 대 8,607,822 로 순서가 뒤집힌다**(39개는 아직 KMP). 저장 칸 2,900,030 대 100,000 은 안 갈려 그것이 내주는 축이다 | **싣는다** — 위상 순서. 큐에서 꺼내는 차례와 「부모의 실패 링크 줄을 본다」는 의존에 이름을 붙이고 위상 정렬·표 채우기·빌드 시스템으로 편 밖 쓰임을 댄다. `findAllOccurrences` 가 이미 쓴 유한 상태 기계는 피했다 | **싣는다** — 한 끝 자리에서 함께 끝나는 서로 다른 패턴은 길이가 다르다는 성질로 `z ≤ m(n+1) − m(m+1)/2` 를 유도하고 검산 네 벌로 확인한 뒤 제약에서 `m` = 446 · `z ≤ 44,500,765` 를 닫는다. 같은 패턴을 여러 벌 담을 수 있으면 상한이 100억으로 뛴다 |
| `convexHullTrick` | **싣는다** — 리 차오 트리와 직선 1,024 · 질의 16,384 고정에서 좌표 범위 `C` 로 갈린다. `C`=2^30 에 1,897,453 대 4,106,474 로 2.16배 앞서다가 **`C`=2^10 에서 1,897,453 대 1,837,310 으로 순서가 뒤집힌다**(뒤집히는 첫 범위 2^11 을 지수 스윕으로 실측). 이쪽 비용이 `C` 와 무관하고 상대는 `⌈log₂ 2C⌉` 에 붙는 것이 갈림의 근거다. 내주는 축은 좁은 범위의 연산 수와 적용 범위(기울기 비감소 요구)이고 잡는 칸 1,024 대 1,024 는 안 갈린다 | **싣는다** — 점과 직선의 쌍대성. 본문이 값으로 보인 버려진 `(-1, 5)` 에 이름을 붙이고, 살아남은 직선 목록이 점 `(m, b)` 들의 하한 볼록 껍질과 같음을 다섯 입력에서 실행 대조한 뒤 모노톤 체인·CGAL 예제로 편 밖 쓰임을 댄다 | **싣는다** — 나눗셈 부등식을 정수 곱 부등식으로 바꾸는 부호 유도, 포텐셜 `Φ` = 껍질 길이로 임의의 `n` 에서 상환 비용 `ĉ_i` = 2 와 `Σc_i ≤ 2n` 을 닫음(파트 1 은 `2n` 을 주장만 했다), 판정식이 정확한 범위 `2^53` 과 제약 상한의 곱 `4×10^27` 의 비 4,440억 배 |

**W2 가 배치11 로 닫혔고 생략이 하나도 없다(2026-09-03).** 편 둘이 조건부 절 셋을 다 실었고,
`L34` 의 판정 질문에 둘 다 **뒤집히는 자리를 값으로** 댔다 — `digitDp` 는 `N` 을 고정하고 `K` 만
115 → 116 으로 하나 올린 자리이고, `isPrimeTrial` 은 소수 24,571 다. **`isPrimeTrial` 이 이 표에서
「뒤집히는 첫 자리」 대신 「마지막으로 앞선 자리」를 낸 첫 편이다** — 경쟁 설계의 계수가 `n` 에
대해 단조롭지 않으면 첫 자리가 요동에 흔들리므로, 그 편의 `.alt.ts` 는 `lastAhead()` 와 스윕
범위 상수를 두고 본문이 그 값을 그대로 적게 했다. 관례에서 이름이 갈리는 자리이므로 뒤 웨이브가
같은 모양을 만나면 이 줄을 근거로 쓴다.

**경쟁 설계를 배정밀도로 재면 생략 판정이 거짓 근거 위에 선다(2026-09-03, `S34` 실측).**
`digitDp` 는 처음 쟀을 때 닫힌 형태가 모든 `K` 에서 앞서 부류 ②(앞서는 축이 값이 아니라 적용
범위)로 생략할 뻔했는데, 그 계산이 배정밀도라 값 자체가 틀린 상태였다 — 중간 항 `C(88,14)` 가
`2^53` 의 0.71 배다. `BigInt` 로 고쳐 다시 재니 `K` = 116 에서 실제로 뒤집혔다. **경쟁 설계의
`measure()` 가 매 실행마다 정본과 답을 대조하게 두는 것이 규칙이다** — 답이 다른 구현으로 잰
계수는 저울질이 아니라 다른 문제의 값이고, 그것으로 낸 생략은 근거가 없다.

**스캐너 0 뒤의 §3 대조가 배치11 에서 63 건을 냈다(2026-09-03).** 편별로 `digitDp` 38 ·
`isPrimeTrial` 25 이고 둘 다 `check-v2` · `check-proof` · `check-metaphor` · `bench-alt` 가 0 을
낸 뒤에 나온 것이다. 배치6 65 · 7 94 · 8 116 · 9 166 · 10 79 에 이어 **여섯 배치 합계 583 건**이다.
부류별로는 ③(손으로 적은 그림·표의 사실 오류)이 여전히 가장 많고(21 + 16), 이 배치가 새로 보여
준 것 둘이다 — ① **최악 입력이라고 손으로 적은 수가 그 성질을 실제로 안 갖는다.**
`isPrimeTrial` 이 「큰 소수의 제곱」 자리에 적은 999,999,000,001 이 소수였다(999,983² 로 교체).
② **ASCII 열 정렬이 배치10 에 이어 또 나왔다** — `isPrimeTrial` 네 블록이 CJK 폭 2 기준으로 최대
3칸 어긋나 있었고, 편마다 손으로 검사기를 짜는 대신 도구로 승격할 후보라는 배치10 의 판단이
그대로 유효하다(`KAN-034.8` 이나 후속 카드 몫).

**W2 배치10 에서 `purpose.alt` 생략이 둘이고, 그중 하나가 부류 조합의 새 자리다(2026-09-03).**
`matrixPowerFibonacci` 가 **①+②+③** 을 한 편에서 함께 냈다 — 지금까지 표에 선 조합은 ①+②
(`topologicalSort`·`treeDiameter` 등)와 ①+③(`minMaxPair`·`lowestSetBit` 등) 둘뿐이었다.
셋이 겹친 것은 경쟁 설계가 모든 축에서 앞서면서, 이 편이 앞서는 유일한 축이 값이 아니라 적용
범위이고, 남은 후보가 경쟁 설계가 아니라 같은 문제의 느린 풀이였기 때문이다. **생략이 둘이라는
것을 규칙 완화로 읽지 않는다** — 둘 다 경쟁 설계를 실제로 구현해 전수 스윕으로 잰 뒤에 뺐고
(`n`=1~8,192 · 25칸 전수) 그 계수를 위 표에 남겼다. `.alt.ts` · `.bench.json` 은 만들지 않아
`P10` 이 잡을 잔여물이 없다.

**스캐너 0 뒤의 §3 대조가 배치10 에서 79 건을 냈다(2026-09-03).** 편별로 `sieveOfEratosthenes`
29 · `palindromePartitioningMinCut` 14 · `matrixPowerFibonacci` 36 이고, 셋 다 `check-v2` ·
`check-proof` · `check-metaphor` 가 **0 을 낸 뒤에** 나온 것이다. 이 배치가 새로 보여 준 것 셋이다.

- **어휘가 절차와 반대인 채로 전 문서에 굳는다.** `sieveOfEratosthenes` 는 산문이 시종
  「적는다」인데 표·그림이 전부 「지우기」였다. 이 절차는 아무것도 지우지 않고 `false → true` 로
  적는다 — v1 구판 어휘가 남은 것이다. 고치는 자리가 원고 한 곳이 아니라 `.proof.ts` 17 자리였고,
  원고의 증명 펜스 21 개를 **사이드카 출력으로 재생성**해야 닫혔다.
- **손으로 적은 전수 열거가 원소를 빠뜨린다.** `palindromePartitioningMinCut` 의 `D(4)` 가 유효
  원소 넷 중 셋만 적고 `C(4)=min(2,3,5)−1` 이라 썼다. 실행 블록으로 대체해 열거가 기계에서
  나오게 하는 것이 유일한 해법이었다 — 사람이 다시 세는 것으로는 같은 실패가 반복된다.
- **ASCII 열 정렬은 눈으로 안 잡힌다.** `matrixPowerFibonacci` 에서 폭을 CJK 2 칸으로 세고 열
  시작 위치를 견주는 검사기를 만들어 10 곳을 잡았고 최대 3 칸까지 어긋나 있었다. `sieveOfEratosthenes`
  도 같은 방식으로 수 격자 넷을 자리↔적힘↔담김으로 대조했다(어긋남 0). **이 검사는 편마다
  손으로 짜는 것보다 도구로 승격할 값이 있다** — `KAN-034.8` 이나 후속 카드의 후보다.

**`얹히-` 활용형이 은유 규칙을 통과한다(2026-09-03, `S33` 실측).** `tools/check-v2.ts:428` 의
`얹[는어을]` 이 리터럴 나열이라 `얹히는` 이 `P2` 와 `check-metaphor` 를 **둘 다 통과했다.**
`되돌리-` 때와 같은 **활용형 누락**이고, 같은 부류가 이것으로 네 번째다. 원고 쪽은 고쳤고
규칙 수정은 `tools/**` 라 이 카드 범위 밖이다.

**W2 배치4 에서 생략이 둘이다(2026-08-31).** 편 넷 중 `treeDiameter` 와
`longestIncreasingSubsequence` 가 `purpose.alt` 를 뺐고 둘 다 **①+② 조합**이다 — 경쟁 설계를
세워 실측했는데 모든 축에서 열세이고, 앞서는 축이 하나 있지만 그것이 이 문제의 제약 밖이거나
값이 아니라 적용 범위였다. **생략이 늘어난 것을 규칙 완화로 읽지 않는다** — 둘 다 경쟁 설계를
실제로 구현해 네 모양 × 여러 규모로 재 본 뒤에 뺐고, 그 계수를 위 표에 남겼다. `.alt.ts` ·
`.bench.json` 은 만들지 않아 `P10` 이 잡을 잔여물이 없다.

**W2 배치5 는 생략이 하나도 없다(2026-09-02).** 편 넷이 조건부 절 셋을 다 실었고, `L34` 의 판정
질문(우열이 실제로 뒤집히는가)에 넷 다 **뒤집히는 자리를 값으로** 댔다 — `findAllOccurrences` 는
글자 가짓수 `σ`=4, `segmentTreeRangeMin` 은 질의 3,516 개와 갱신 1 회 **두 축에서 각각**,
`unboundedKnapsack` 은 금액 9,996→9,997 한 칸, `primMst` 는 간선 281 과 4,323 **두 번**이다.
배치4 가 생략 둘이었던 것과 갈리는데, 그것을 규칙이 느슨해졌다고 읽지 않는다 — 넷 다 경쟁
설계를 실제로 구현해 스윕으로 재고 그 계수를 위 표에 남겼다.

**스캐너 0 뒤의 §3 대조가 배치5 에서 40 건을 냈다(2026-09-02).** 편별로 `findAllOccurrences` 6 ·
`unboundedKnapsack` 7 · `segmentTreeRangeMin` 9 · `primMst` 18 이고, 넷 다 `check-v2` ·
`check-proof` · `check-metaphor` · `bench-alt` 가 **0 을 낸 뒤에** 나온 것이다. 부류별로는
①(기호 충돌)이 셋 — 「마디」와 「노드」를 섞어 쓴 것 · `deep.math` 의 자유 변수가 기호표의 것과
같은 글자였던 것 · 큐 항목 표기가 간선 표기와 글자가 겹친 것 — 이고, ③(손으로 적은 그림·표의
사실 오류)이 가장 많으며, ④(다른 편에서 옮겨 온 문구)는 이웃이 많은 편일수록 늘었다
(`unboundedKnapsack` 은 `knapsack01`·`subsetSum`·`coinChangeWays` 셋과 붙어 있어 완전 일치
문장이 81 개였고 40 개로 줄였다). **`selfcheck` 의 답이 틀린 채로 스캐너 넷을 통과한 사례가
`primMst` 에서 나왔다** — 답이 `T3` 이라 적혔는데 실제로는 `T4` 다. 기계가 볼 수 있는 종류가
아니므로 편마다 §3 대조를 거친다는 규칙이 이 배치에서 다시 값을 했다.

**뒤 웨이브 주의 — 도구가 내는 건수를 실제 건수로 읽지 않는다(2026-08-29, 배치4 실측).**
`check-proof` 는 블록마다 **첫 번째로 다른 줄만** 내므로 참값은 `grep -rn PLACEHOLDER` 다
(`dfsAllPaths` 는 2건으로 알고 들어갔는데 16건이었다). `check-v2` P1 은 절마다 **최악 하나만**
내므로 run=4 를 고치면 숨어 있던 run=3 이 그때 확인된다 — 고치기 전에 절 전체의 run 을
줄번호로 펼쳐 한 번에 끊는다(`dfsAllPaths` 는 신고 2자리가 실제로 4자리였다).

**`되돌리-` 활용형은 통과하지 않는다(2026-08-29 정정).** 배치3 이 남긴 「`되돌아간다`·`되돌리기`
는 통과」는 맞지만, 규칙(`tools/check-v2.ts:409`)이 그 어간의 활용형 셋을 **리터럴로** 나열하면서
`되-` 를 빼는 lookbehind 를 안 달았다. 그래서 `되돌리-` 에 `-면`·`-ㄴ다`·`-ㅂ니다` 가 붙은 꼴은
**전부 걸리고** 빠져나가는 것은 `되돌아-` 와 `되돌리기` 뿐이다. **이 문서에도 그 활용형을 적을 수
없다** — `check-metaphor --all` 의 대상 집합에 이 파일이 들어 있어서, 규칙을 예시로 적는 순간
그 줄이 위반이 된다.
