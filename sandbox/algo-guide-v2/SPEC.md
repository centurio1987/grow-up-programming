# `algo-learn-guide` — 알고리즘 학습 가이드 명세

> **이 파일이 명세의 정본이다.** `.claude/authoring/specs/` 에 등록하지 않는다 —
> `authoring-kit` 에서 쓰는 것은 voice 하나이고, 판정 장치는 전부 이 샌드박스에 짓는다.
> 근거는 카드 실행 문서 `KANBAN.cards/KAN-033-E8KHSV.md` 의 「전략」 절.
>
> 적용 voice: `ppangtolab-teacher`
> (`~/.claude/authoring/voices/ppangtolab-teacher/{voice.md,voice.json}` 을 집필 프롬프트에 싣는다)

## 0. 이 명세가 서 있는 자리

카드가 요구한 것은 **분량을 신경 쓰지 않고 독자가 이해하는 것만을 기준으로 삼는 가이드**다.
그래서 이 명세의 원칙 대부분은 "무엇을 적어라"가 아니라 **"무엇이 없으면 미흡인가"** 로 쓰였다.

판정은 셋이 나눠 진다.

| 누가 | 무엇을 | 어디에 |
| --- | --- | --- |
| **이해 시험 V1~V7** | 외부 모델이 본문만 읽고 이해했는가 | `tools/comprehension.sh` |
| **스캐너 P1~P10** | 기계로 셀 수 있는 것 | `tools/check-v2.ts` |
| **유저** | 최종 승인. 편 단위, 항목 id 로 반려 | — |

**항목을 늘려서 결손을 메우지 않는다.** `docs/comprehension-gate-audit.md:53-54` 가
*"측정 없이 항목만 늘면 문서는 체크박스의 합집합이 되고, 집필은 채우기가 된다"* 라 적었고,
`:57-58` 이 *"처방도 항목 추가가 아니라 기존 절 **직무의 재정의**로 간다"* 로 못 박았다.
그래서 이 명세에서 **신설은 `mistake` 하나**이고, 나머지는 카드 항목의 배치이거나 복원이다.

> **항목 수 정정.** 플랜 5판 본문이 "24항목"이라 적었는데 표의 행은 **25개**다(컨테이너 3개 포함).
> 이 파일의 25가 맞다.

## 1. 항목 — id · 헤딩 · 순서

`fixed` 는 헤딩 문구를 바꿀 수 없다는 뜻이다. `false` 면 직무만 고정이고 문구는 내용에 맞춰 짓는다.

| order | id | 헤딩 | 필수 | fixed | parent |
| --- | --- | --- | --- | --- | --- |
| 5 | `title` | `# {알고리즘 이름} — {돌파 관찰 한 구절}` | ✓ | false | — |
| 10 | `prereq` | `## 시작하기 전에 — 이미 알고 있어야 하는 것` | ✓ | true | — |
| 20 | `concept` | `## 전체 컨셉` | ✓ | true | — |
| 30 | `deep` | `## 아이디어 상세` | ✓ | true | — |
| 31 | `deep.build` | `### {아이디어 이름} — 생각이 닿는 경로` | ✓ | false | `deep` |
| 32 | `deep.math` | `### 수식 정의와 유도` | 조건부 | true | `deep` |
| 33 | `deep.proof` | `### 왜 항상 옳은가` | ✓ | true | `deep` |
| 34 | `deep.trap` | `### 흔한 오해와 그것이 거짓인 이유` | ✓ | true | `deep` |
| 35 | `deep.check` | `### 이해 점검` | ✓ | true | `deep` |
| 40 | `purpose` | `## 이 알고리즘이 최적인 자리` | ✓ | true | — |
| 41 | `purpose.fit` | `### 최적인 문제의 모양` | ✓ | true | `purpose` |
| 42 | `purpose.cue` | `### 문제에서 이것을 떠올리게 하는 단서` | ✓ | true | `purpose` |
| 43 | `purpose.real` | `### 실제로 쓰이는 곳` | ✓ | true | `purpose` |
| 44 | `purpose.alt` | `### 경쟁 설계와의 대조` | ✓ | true | `purpose` |
| 50 | `code` | `## 코드로 옮기기` | ✓ | true | — |
| 51 | `code.pseudo` | `### 수도 코드로 본 전체` | ✓ | true | `code` |
| 52 | `code.step` | `### {단계 이름}` | ✓ **repeat** | false | `code` |
| 53 | `code.final` | `### 완성 코드` | ✓ | true | `code` |
| 60 | `trace` | `## 한 입력으로 끝까지 굴려 보기` | ✓ | true | — |
| 70 | `perf` | `## 비용 계산` | ✓ | true | — |
| 71 | `perf.derive` | `### 비용을 세는 과정` | ✓ | true | `perf` |
| 72 | `perf.bounds` | `### 케이스별 비용과 그 경계` | ✓ | true | `perf` |
| 73 | `perf.worst` | `### 최악을 만드는 입력` | ✓ | true | `perf` |
| 75 | `mistake` | `## 한 곳을 바꿔 보면` | ✓ | true | — |
| 80 | `selfcheck` | `## 스스로 점검하기` | ✓ | true | — |

`deep`·`purpose`·`code` 는 **컨테이너**다. 자기 본문을 길게 쓰지 않고 하위 절을 담는다.

**순서 하나가 카드 원문과 다르다.** 카드는 성능(`KANBAN.md:60`)을 코드(`:65`) **앞**에 놓았는데
여기서는 뒤다. `perf.derive` 가 `trace` 의 `T#` 를 인용해야 결속(P5)이 서기 때문이고,
카드의 면제 문구(`:45`)는 **이름만** 면제하므로 이 변경은 **유저 결재 항목**이다(B5).

## 2. id ↔ 헤딩 매핑 (`check-v2.ts` 가 읽는다)

구 스캐너의 절 식별은 **고정 헤딩 접두 일치 + 첫 매치 하나**였다. `fixed:false` 이거나
`repeat` 인 절을 못 찾고, 찾아도 첫 벌만 본다. 그래서 매핑을 명세가 준다.

```
title        ^# .+$
prereq       ^## 시작하기 전에 — 이미 알고 있어야 하는 것$
concept      ^## 전체 컨셉$
deep         ^## 아이디어 상세$
deep.build   ^### .+ — 생각이 닿는 경로$
deep.math    ^### 수식 정의와 유도$
deep.proof   ^### 왜 항상 옳은가$
deep.trap    ^### 흔한 오해와 그것이 거짓인 이유$
deep.check   ^### 이해 점검$
purpose      ^## 이 알고리즘이 최적인 자리$
purpose.fit  ^### 최적인 문제의 모양$
purpose.cue  ^### 문제에서 이것을 떠올리게 하는 단서$
purpose.real ^### 실제로 쓰이는 곳$
purpose.alt  ^### 경쟁 설계와의 대조$
code         ^## 코드로 옮기기$
code.pseudo  ^### 수도 코드로 본 전체$
code.final   ^### 완성 코드$
code.step    (아래 규칙)
trace        ^## 한 입력으로 끝까지 굴려 보기$
perf         ^## 비용 계산$
perf.derive  ^### 비용을 세는 과정$
perf.bounds  ^### 케이스별 비용과 그 경계$
perf.worst   ^### 최악을 만드는 입력$
mistake      ^## 한 곳을 바꿔 보면$
selfcheck    ^## 스스로 점검하기$
```

**`code.step` 은 잔여로 정한다.** `## 코드로 옮기기` 컨테이너 안의 `###` 절 중
`code.pseudo`·`code.final` 로 해소되지 **않은 전부**가 `code.step` 이고, 순서대로 수집한다.
정규식으로 이름을 강제하면 단계 이름을 알고리즘에 맞춰 짓지 못한다.

**해소 순서**: 고정 헤딩 → `fixed:false` 패턴 → 잔여(`code.step`). 어느 것으로도 안 잡히는
`##`·`###` 헤딩이 있으면 **에러**다(조용히 넘기면 그 절이 어느 판정에도 안 걸린다).

## 3. 항목별 작성 방법

### `title`

`# {알고리즘 이름} — {돌파 관찰 한 구절}`. 부제는 **이 알고리즘의 결정적 관찰을 한 구절로
예고**한다. 예: *"질의를 정렬하면 포인터 이동이 √n배 줄어드는 이유"*.

### `prereq` — 시작하기 전에

**출발점을 고정한다.** Study Mode 의 첫 동작(학습자 수준 파악 → 기존 지식에 연결)이 근거다.
독자가 이 글을 읽기 위해 이미 알고 있어야 하는 것을 **목록으로** 적고, 모르면 갈 곳을
상대경로 링크로 단다. 프로젝트 안에 그 가이드가 있으면 링크하고, 없으면 그렇게 적는다.

여기서 "알고 있어야 하는 것"은 **개념**이지 문법이 아니다. "TypeScript 를 안다"는 쓸모가 없고
"배열의 부분합이 무엇인지 안다"는 쓸모가 있다.

### `concept` — 전체 컨셉

**이 절만 읽고도 전체가 잡혀야 한다.** 분량 상한이 없다 — 카드의 제1 요구다.

자립한다는 것은 둘이다(L2). ① 뒤 절의 헤딩·앵커를 참조하지 않는다("아래 `## 비용 계산` 에서
보겠지만" 금지). ② 이 절에서 처음 등장한 용어 중 정의 없이 쓰인 것이 0.

복잡도를 여기서 **언급할 수는 있다.** 다만 그 자리에서 "최악"·"평균"·"타이트 경계" 같은 말을
쓰려면 정의를 함께 줘야 하고(②), 그러면 `perf` 와 내용이 겹친다. **겹치면 `perf` 의 직무를
침범한 것이다** — 결과값 한 줄(`O(n log n)`)로 그치고 유도는 `perf` 에 넘긴다.

### `deep.build` — 생각이 닿는 경로 (직무 셋)

**① 문제를 고정한다.** 함수 시그니처 코드 블록 + 입출력 계약 + **제약 규모**. 이 셋이 있어야
독자가 "무엇을 얼마나 빠르게 해야 하는가"를 알고 읽는다.

**② 가장 단순한 방법을 세우고 수치로 무너뜨린다**(V1). 아무 기법 없이 푸는 법을 실제로 적고,
제약 규모를 넣었을 때 몇 번의 연산이 되는지 **숫자로** 보인다. "느리다"는 미흡이고
"n=10^5 이면 10^10 번이라 초 단위로 안 끝난다"가 충족이다.

**③ 거기서 아이디어까지 생략 없이 간다.** 걸음마다 **직전 걸음의 결과를 구체 값으로
이어받는다**(L1). 정의가 연달아 둘 이상 나오고 그 사이에 값 계산이 없으면 위반이다.
원소 3~6개짜리 작은 입력을 하나 잡고 그 위에서 굴린다.

### `deep.math` — 수식 정의와 유도 (조건부)

**조건**: `deep.build` 또는 `perf.derive` 에서 재귀 관계 $T(n)=\dots$ 또는 상태 전이식이
**실제로 세워질 때만** 쓴다. "수식으로 표현 가능한가"로 판단하지 않는다 — 모든 알고리즘이
그렇다.

LaTeX(`$...$` · `$$...$$`)로 정식 정의하고, **정의 하나마다 계산 하나를 붙인다.**
수식은 문단을 끊지 못한다 — P1 이 "그림"으로 세는 것은 펜스와 표뿐이다.

### `deep.proof` — 왜 항상 옳은가 (구 `idea.proof` 복원)

논증의 **각 단계와 그것이 성립하는 이유**를 적는다. "자명하다"·"쉽게 알 수 있다"로 넘기지
않는다(voice 금지 문형이기도 하다).

**엣지 케이스 처리를 함께 적는다** — 빈 입력 · 크기 1~2 · 경계값(최솟값·최댓값·중복 전부 같음).
이 직무는 구 명세에 있다가 개정 과정에서 한 번 잃었던 것이라 여기 명시한다.

이 절은 **그림 의무(L10)에서 제외**된다. 논증에 억지로 도식을 넣으면 빈 상자가 된다.

### `deep.trap` — 흔한 오해와 그것이 거짓인 이유

**오해를 명시하고 → 그 오해대로 전개하고 → 반례로 거짓임을 보인다.** 셋이 다 있어야 한다.
"주의하세요"·"헷갈리기 쉽습니다"는 미흡이다.

반례는 **구체 입력**이어야 하고, 그 입력에서 오해가 낸 답과 옳은 답이 **둘 다** 나와야 한다.

### `deep.check` — 이해 점검

예측 문제를 던지고 **바로 답을 이어 보인다**(voice 규칙 9). md 에서는 답이 그대로 보이고,
**web 에서만** 빌더가 접는다.

```
<!--check:c1-->
답: …
<!--/check-->
```

마커는 **블록 자리에만** 둔다. 문장 중간(`… 답은 <!--check:c1--> 3 입니다`)은 `html` 노드가
되지 않아 빌더가 못 잡는다.

### `purpose.fit` · `purpose.cue` · `purpose.real`

- **fit** — 어떤 모양의 문제에 최적인가. 입력의 성질(정렬 여부·범위·질의가 오프라인인가)로 적는다.
- **cue** — 문제 지문에서 이 알고리즘을 떠올리게 하는 **신호**를 목록으로. 독자가 다음에
  비슷한 문제를 만났을 때 쓸 것이라 구체적이어야 한다.
- **real** — **실명 + 1차 출처(공식 문서·논문·소스코드 링크) + 조회일**(L12). 2차 자료(블로그·
  요약 기사)만으로는 미흡이다. 실제 도입 사례를 못 찾으면 **없다고 적는다** — 지어내지 않는다.

### `purpose.alt` — 경쟁 설계와의 대조 (V2)

**감사가 실패 111편 중 50편의 원인으로 지목한 자리다**(`audit:23`·`:37-40`).

같은 목표를 노리는 **다른 절차를 실명으로** 세우고, **같은 입력에서 같은 계수로 나란히 잰다.**
넷이 다 있어야 충족이다 — 실명 · 같은 입력 · 수치 · **대가**.

**이 가이드가 가르치는 절차가 그 수치에서 져도 된다.** 지면 지는 대로 적고, 그럼에도 이 절차를
쓰는 축(일반성 · 최악 보장 · 메모리 · 구현 난이도)을 같은 절에 적는다. 어느 쪽이 우월한지를
판정하는 절이 **아니다** — 어느 축에서 무엇을 내주고 무엇을 얻는지를 적는 절이다. 한쪽 이점만
적고 대가를 빼면 V2 가 `한쪽만 있음` 으로 떨어뜨린다.

**뒤집히는 조건을 적는다.** 어떤 입력·제약에서 우열이 반대로 가는지 한 대목으로. 못 적겠으면
그것은 대조가 덜 된 것이지 뒤집힐 자리가 없는 것이 아니다.

**수치는 `tools/bench-alt.ts` 가 낸 결정론적 계수**여야 한다(L13). 비교 횟수 · 포인터 이동
칸 수 · 방문 노드 수처럼 실행마다 같은 값이 나오는 것. 벽시계·처리량은 쓰지 않는다 —
"일치"를 정의할 수 없다.

**입력을 결과에 맞춰 고르지 않는다**(L20). 대조가 불리하게 나온다는 이유로 입력을 갈아 끼우면
그 절은 대조가 아니라 연출이고, 그때 나온 수치는 아무것도 재지 않는다. 셋을 지킨다.

- bench 입력은 `deep.build`·`trace` 가 쓰는 입력과 **같은 것**을 쓴다. 규모가 달라야 하면
  (전개용 입력이 너무 작아 계수가 안 갈리는 경우) **왜 다른지를 본문 한 문장으로** 적는다.
- **시드 · 크기 · 생성식**을 `.alt.ts` 에 상수로 두고 본문 대조 문단에도 적는다. 독자가 그
  값으로 다시 돌려 같은 수를 얻을 수 있어야 한다.
- 한 번 적은 입력은 **수치가 마음에 안 든다는 이유로 바꾸지 않는다.** 바꿔야 할 사정이 생기면
  바꾼 사실과 사유를 커밋 메시지에 남긴다. 진 결과를 지우려고 바꾼 것이 아님이 이력에 남아야 한다.

`deep.build` 직무 ②(가장 단순한 방법)의 답을 여기 재사용하지 않는다. 그쪽은 "왜 이 목표가
필요한가"이고 이쪽은 "왜 하필 이 절차로 그 목표에 닿는가"다.

경쟁 설계가 정의상 없으면(절차가 유일하면) **왜 없는지를 한 문장으로** 적는다.

### `code.pseudo` · `code.step` · `code.final`

- **pseudo** — 전체 절차를 한 장으로. 언어 문법이 아니라 절차를 적는다.
- **step**(반복, 최소 3벌) — 구현 단계마다 한 벌. **이유 + 코드 스니펫**이 짝이다.
  분기에는 **원문자 라벨**(①②③)을 달아 `trace` 가 그것을 밟게 한다(P4).
  코드만 나열하고 이유가 없으면 미흡이다.
- **final** — 실제 작동하는 전체 코드. `<name>-guide.ref.ts` 에서 옮긴다.

### `trace` — 한 입력으로 끝까지 굴려 보기 (V6)

**고정 입력 하나**를 잡고 끝까지 간다. 셋이 다 있어야 한다(L17).

1. 단계별 상태값
2. **분기 조건의 참/거짓을 실제 값으로** — "조건을 만족하므로"가 아니라 "`l=2, r=5` 라
   `l < r` 이 참"
3. **모든 분기 최소 1회 피복** — `code.step` 의 원문자 라벨 전부가 여기 나와야 한다(P4)

단계에 `T1`·`T2` … 를 단다. `perf.derive` 와 `selfcheck` 가 이것을 인용한다(P5).

**선언형 `steps` 배열은 전개로 인정하지 않는다.** `.sim.ts` 에 값이 있는 것과 본문이 그 값을
설명하는 것은 다른 일이다.

### `perf.derive` · `perf.bounds` · `perf.worst`

- **derive** — 비용을 세는 **과정**. 결론만 던지지 않는다. **`trace` 의 `T#` 를 인용해** 센다.
- **bounds** — **케이스**(최악·평균)와 **경계**(상한 $O$ · 타이트 $\Theta$)를 **두 축으로 갈라**
  적는다(L7). "최악=$O$, 평균=$\Theta$" 로 짝지으면 **위반**이다 — $O$·$\Theta$ 는 케이스가
  아니라 경계의 종류다. 평균이 정의되지 않으면 **왜** 정의되지 않는지 적는다.
  **총식을 한 번은 정확히 쓴다**(L15) — 앞뒤로 붙는 항(정렬·전처리·좌표압축)이 있으면 그것까지.
  **보장의 종류를 밝힌다**(L16) — 최악 보장인지 기대값인지, 기대값이면 **무엇에 대한 기댓값인지**
  (입력 분포인가, 구현이 만드는 무작위성인가).
- **worst** — 최악을 만드는 입력을 **실제로 구성한다**. "정렬된 배열이면 느리다" 같은 물려받은
  통념을 검증 없이 쓰지 않는다.

### `mistake` — 한 곳을 바꿔 보면 (V5, 신설)

**코드의 순서·조건 한 곳을 바꾸고, 그 결과 나오는 잘못된 결과값을 보인다.**
"틀린다"·"버그가 생긴다"로 끝내면 미흡이다 — **실제 값**이 나와야 한다.

Study Mode 의 *"DO NOT DO THE USER'S WORK FOR THEM"* 을 정적 문서로 옮긴 자리다.
독자가 왜 그 자리가 그 모양이어야 하는지를 스스로 확인한다.

**`T#` 와 원문자 라벨을 쓰지 않는다.** P4·P5 가 `trace`·`code.step` 에서 토큰을 뽑으므로
여기서 같은 토큰을 쓰면 절 경계가 오염된다.

### `selfcheck` — 스스로 점검하기

**이름을 바꾸지 않는다** — `voice.md:87` 이 문서 끝을 이 이름으로 마무리하라고 지정한다.
회수(핵심 재진술) + 문제 2~3개. 문제 중 최소 하나는 `trace` 의 `T#` 를 인용한다(P5).

## 4. 시각화 — 횡단 규칙

카드는 *"시각화를 적극적으로 이용해서 **설명한다**"* 라고 했다. 절 하나로 가두면 나머지가
산문 덩어리가 되므로 **절마다** 진다.

**그림 의무(L10)** — 아래 아홉 절이 각각 그림 ≥ 1.

```
concept · deep.build · deep.trap · code.step · trace · mistake · perf.derive · perf.worst
```

- `code.step` 은 **절 전체 기준 ≥ 1**(벌마다가 아니다).
- `deep.proof` 는 **제외**(논증 절).
- **예외는 없다.** 4판까지 "그림이 성립하지 않는 부류" 를 위한 예외 조항을 열어 뒀는데,
  S8 이 그 가설을 실물로 시험해 **거짓**임을 확인했다(`SURVEY.md` 뒷부분). 후보 1순위였던
  `babyStepGiantStep` 에서 세 절 다 그림이 억지가 아니라 설명의 중심이었다.
  전개 중에 정말 못 지키는 편이 나오면 **그때 왜 못 지키는지를 적고** 다시 논의한다 —
  없는 예외를 미리 만들어 두면 집필이 그 문으로 나간다.

**"그림"의 정의**(P1·P7 이 같은 정의를 쓴다): ```` ``` ```` 펜스와 `|` 표. **그것뿐이다.**
`<` 로 시작하는 줄은 세지 않는다 — 세면 `<!--check:c1-->` 마커가 그림이 된다.
`code.step` 의 코드 스니펫도 그림으로 세지 않는다(repeat 라 절마다 있어 P1 이 무력해진다).

**md ↔ web**

```
<!--viz:{id}-->
```text
(ascii art — md 는 이것을 보여주고, web 은 이 자리를 대화형 패널로 덮는다)
```
```

마커 id ↔ `<name>-guide.sim.ts` 의 export 키 ↔ ascii 펜스가 **3자로 맞아야 한다**(L11 · P6).

## 5. `.sim.ts` 계약

```ts
export const {id} = {
  view,     // ViewName | ViewName[]  — src/_guide-sim/index.tsx 의 프리셋 6종
  steps: [ /* 인라인 배열 리터럴만 */ ],
  title,
  result,   // 마지막 프레임이 뜻하는 실제 반환값 (P9)
  extraViews?, overrides?,   // 샌드박스 전용
}
```

**`steps` 는 인라인 배열 리터럴이어야 한다.** 변수 참조 · 함수 호출 · **spread 금지**.
실측: `steps: [...base, {t}]` 는 실제 3인데 `ts-morph` 가 **2로 센다** — 과소 계수는 P3 을
지나 **얇은 trace 를 통과시킨다.** 위반 시 P3 은 통과가 아니라 **에러**로 죽는다.

**`result` 를 계약에 넣는다.** `Frame` 에 반환값 필드가 없고 결과가 뷰마다 다른 자리에 있어
(배열 최종값 · `entries` · `matrix` 셀 · 산문) 기계 대조가 불가능했다. 본문에는
`<!--result:{id}-->` 마커로 같은 값을 적고 P9 가 대조한다.

**새 뷰의 프레임 필드는 `Frame.extra` 아래에 둔다.** `Frame` 이 `Partial<...>` 교집합이라
최상위에 필드를 더하면 이름이 겹칠 때 `never` 가 된다.

## 6. 범위 원칙 L1~L19

| code | 내용 | 판정 |
| --- | --- | --- |
| `L1` | 분량을 이유로 단계를 접지 않는다. `deep.build` 의 각 걸음이 직전 걸음의 결과를 구체 값으로 이어받고, 정의가 연달아 둘 이상 나오며 그 사이에 값 계산이 없는 자리가 0 | P1·P2 |
| `L2` | `concept` 은 자립한다 — 뒤 절 헤딩·앵커 참조 0(P8), 처음 등장한 용어 중 정의 없이 쓰인 것 0(V) | P8 + V |
| `L3` | `deep.build` 는 문제를 고정(시그니처·입출력 계약·제약 규모)하고, 가장 단순한 방법을 세워 **수치로** 무너뜨린다 | V1 |
| `L4` | `deep.proof` 는 논증의 각 단계와 성립 이유 + **엣지 케이스** 처리를 적는다 | V3 |
| `L5` | `deep.trap` 은 오해를 실제로 전개해 **반례로** 거짓임을 보인다 | V4 |
| `L6` | `mistake` 는 코드의 한 곳을 바꿔 **실제 잘못된 결과값**을 보인다 | V5 |
| `L7` | `perf.bounds` 는 **케이스**와 **경계**를 두 축으로 갈라 적는다. "최악=$O$, 평균=$\Theta$" 짝짓기는 위반 | V7 |
| `L8` | `perf.worst` 는 최악을 만드는 입력을 구성해 제시한다 | V7 |
| `L9` | `code.final` 이 기존 `*.test.ts` 의 **입출력 케이스**를 통과한다(학습자 스텁은 안 건드린다) | `bun test` |
| `L10` | 그림 의무 — 아홉 절 각각 ≥ 1. `code.step` 은 절 전체 기준. `deep.proof` 제외. **예외 없음**(S8 판정) | P7 |
| `L11` | 마커 id ↔ `.sim.ts` export 키 일치, 각 `viz` 마커 아래 ascii 펜스가 비어 있지 않음 | P6 |
| `L12` | `purpose.real` 은 실명 + **1차 출처** + 조회일 | V |
| `L13` | `purpose.alt` 가 실명 + 같은 입력 + **결정론적 실측 계수** + **대가**(어느 축에서 무엇을 내주는가) | P10 + V2 |
| `L14` | `.sim.ts` 의 `result` 와 본문 `<!--result:{id}-->` 가 일치 | P9 |
| `L15` | 복잡도 진술의 완전성 — 어느 비용만 센 값인지 밝히고 총식을 한 번은 정확히 쓴다 | V7 |
| `L16` | 보장의 종류 — 최악 보장인지 기대값인지, 기대값이면 무엇에 대한 기댓값인지 | V7 |
| `L17` | `trace` 가 (a) 단계별 상태값 (b) 분기 조건 참/거짓을 실제 값으로 (c) 모든 분기 최소 1회 피복 | P3·P4 + V6 |
| `L18` | 결속 — `perf.derive` 와 `selfcheck` 가 각각 `T#` 를 1개 이상 인용(**형식만**). 실질은 V7 | P5 + V7 |
| `L19` | 웹 동등성 — md 의 모든 절이 html 에 있고 표·수식·코드·**ascii 그림**이 안 깨진다. 차이는 `deep.check` 의 답 접힘 하나뿐 | 빌드 스모크 |
| `L20` | bench 입력을 **결과에 맞춰 고르지 않는다** — `trace` 와 같은 입력(다르면 사유), 시드·크기·생성식 본문 명시, 사후 교체는 사유를 이력에 남긴다 | V2 + 검토 |

## 7. 벤치마크 대조

카드는 *"openai 의 learning 스킬과 claude desktop `/learn` 로부터 벤치마킹 해라"* 를
**실행 지시**로 줬다. 항목에 매핑한다.

| 벤치마크 원칙 | 출처 | 반영 항목 |
| --- | --- | --- |
| 학습자 수준 파악 → 기존 지식에 연결 | Study Mode | `prereq` |
| 한 번에 한 걸음, 건너뛰지 않는다 | Study Mode | `deep.build` 직무 ③ + L1 |
| 이해를 점검한다(예측 → 확인) | Study Mode | `deep.check` · `selfcheck` |
| *"DO NOT DO THE USER'S WORK FOR THEM"* | Study Mode | `deep.check` · `mistake` |
| 결정 지점을 독자에게 넘긴다 | `learning-output-style` | `purpose.alt` |
| 선택의 트레이드오프를 별도 블록으로 | `learning-output-style`(`★ Insight`) | `purpose.alt` · `perf.bounds` |
| 분량 상한을 두지 않는다 | 카드 제1 요구 | L1 |

**충돌 하나.** Study Mode 의 *"독자의 일을 대신하지 말라"* 와 카드의 *"실제 작동하는 완성 코드를
마지막에 제공한다"* 는 방향이 반대다. **카드가 이긴다** — `code.final` 은 필수로 두고,
"대신하지 말라"는 `deep.check`·`mistake` 두 절이 진다.

**Claude 데스크톱 `/learn` 은 원문을 확인하지 못했다.** 위 표는 확인된 두 출처에서만 뽑았고,
이 사실을 B5 결재에 올린다.

## 8. 조건부 절 규약

해당 사항이 없으면 **절 자체를 생략한다.** 문서 안에 "해당 없음"·"이 절은 생략한다" 같은
집필 메타를 남기지 않는다(voice 금지). 생략 사유는 `JOURNAL.md` 에만 적는다.
