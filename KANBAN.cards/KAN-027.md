---
card: KAN-027
title: [P4·27] P4-B군 — complexity 등급(축3 필요, 에스컬레이션 불필요)
created: 2026-09-14
scope: src/data-structures/disjoint-set/unionFind/**, src/data-structures/disjoint-set/disjointSetRollback/**, src/data-structures/hash/lruCache/**, src/data-structures/heap/pairingHeap/**, src/data-structures/heap/fibonacciHeap/**, src/data-structures/heap/vanEmdeBoasTree/**, src/data-structures/linear/pieceTable/**, src/data-structures/probabilistic/skipList/**, src/data-structures/range-query/segmentTreeLazy/**, src/data-structures/range-query/persistentSegmentTree/**, src/data-structures/range-query/sparseTable/**, src/data-structures/spatial/kdTree/**, src/data-structures/spatial/quadtree/**, src/data-structures/tree/huffmanTree/**, src/data-structures/tree/merkleTree/**, src/data-structures/trie/ahoCorasick/**, docs/ORD-006-conventions.md, docs/ORD-006-runbook.md, docs/ORD-006-inventory.tsv, docs/ORD-006-wbs.md, src/data-structures/_contract/**, tools/emit-vectors.ts, tools/ord006-inventory.ts, rust/vectors/**, tools/ord006-wbs.ts, tools/guide-skeleton.test.ts, src/algorithms/string/radixTree/radixTree-guide.md
---

# KAN-027 — [P4·27] P4-B군 — complexity 등급(축3 필요, 에스컬레이션 불필요)

## 전략
### 범위 — 남은 16종의 계약 전환만 (2026-09-14 유저 결재·재개)

**`cuckooFilter`(T5-05)는 `KAN-026` 으로 이관됐다**(2026-09-14 유저 결재). `bloomFilter`(A군)와 한
판정 자리에서 보라는 서술(`docs/ORD-006-conventions.md:2285`)이 워크트리를 넘는 의존을 만들었기
때문이다. 그래서 `S15` 는 수행 없이 이관으로 닫고, `S1` 이 `UNITS` 에서 그 유닛을 뺀다.
아래 실측 표의 17 은 이관 전 값이다.

2026-09-13 정지(`KAN-035-31T4BY` S1) 이후 **계약 층만 들고 재개한다.** 산출은 구조마다
`<name>.ts` 계약 헤더 여섯 항목 · `<name>.contract.ts` · `<name>.test.ts`(runContract) ·
`_reference/<name>.ts`(`__cost` 계측, `#region guide:core`) · `-problem.md` 제거다.
**v2 가이드는 쓰지 않는다** — `KAN-036` 이 이 카드와 `KAN-026` 완료 뒤에 66편을 한꺼번에
쓰도록 직렬 중재돼 있다. 옛 `-guide.mdx` 와 `_deprecated/` 는 손대지 않는다.

`docs/ORD-006-wbs.md` 는 「계약 + 8단계 가이드」를 한 유닛으로 묶었다는 이유로 무효 선언됐지만
(`KAN-035` 전략 「버리는 것 · 잇는 것」), **트랙 · 번호 대역 · 공유 파일 규약 · claim 도구는
가이드와 무관하게 그대로 쓸 수 있다.** 무효인 것은 유닛의 산출 정의뿐이다. 그래서 재개는
유닛에서 가이드 산출을 떼어 내는 것으로 시작한다(`S1`).

### 착수 전 실측 (2026-09-14)

| 항목 | 값 | 근거 |
| --- | --- | --- |
| B군 계약이 선 것 | 25 / 42 | `.contract.ts` 36 = B군 25 + B군 밖 11. WBS 분모 37 은 선행 배치분 5종(avl·redBlack·twoThree·bTree·bPlusTree)을 뺀 값(`tools/ord006-wbs.ts:56`) |
| 남은 17종 | `.contract.ts`·`_reference/` 0 · `-problem.md` 17 | 디렉터리 실측. `unionFind`·`segmentTreeLazy` 는 JSDoc 없는 스텁 |
| WBS 도구 | 32 유닛 / 37 종, 완료 20종. T1 완료 · T2 2/5 · T3 2/5 · T4 2/7 · T5 2/8 | `bun run tools/ord006-wbs.ts` 출력 |
| 도구가 내는 다음 claim | T2-03 `pairingHeap` · T3-03 `lruCache` · T4-03 `segmentTreeLazy` · T5-03 `pieceTable` | 같은 출력 |
| 마지막 배치 | T2-02·T3-02·T4-02·T5-02 (2026-08-15, `d914556`) — 4유닛 5종 병렬, 60파일 +16,349 | `docs/ORD-006-runbook.md:731-734` |
| 판정으로 접힐 쌍 | **없다** — 17유닛 전부 `transitions: []`. B18·B19 가 남은 쌍을 전부 「다르다」로 닫았다 | `ord006-wbs.ts:140-327` · `docs/ORD-006-conventions.md:2192-2193,2239-2249` |

### 유닛마다 걸린 판정 — 착수 전에 읽을 것

| 유닛 | 걸린 것 | 근거 |
| --- | --- | --- |
| T2-03 `pairingHeap` · T2-04 `fibonacciHeap` | 계약 C(넣기·합치기 O(1), 빼기 amortized O(log n)) · D(decreaseKey amortized O(1)). B↔C 는 상한이 아니라 한정자로만 갈려 적대 시나리오 「넣기 n 번 뒤 빼기 한 번」이 필요하다 | `conventions.md:2157-2184` |
| T2-05 `vanEmdeBoasTree` | 상한 O(log log u) — n 이 아닌 것이 상한에 들어오는 첫 사례. Bound 는 지금 다섯(O(1)·O(log n)·O(sqrt n)·O(n)·O(n log n)). **미결**이고 판정 결과를 §규약2 에 적는다 | `runbook.md:1920` · `ord006-wbs.ts:161` |
| T3-04 `unionFind` | 역아커만을 Bound 에 넣지 않기로 **확정** — O(1) 구간과 겹쳐 배제하는 것이 없다 | `conventions.md:2264-2271` |
| T3-03 `lruCache` · T4-04 `persistentSegmentTree` | 불변 사실 67 적용 | `ord006-wbs.ts:187,237` |
| T4-05 `sparseTable` · T5-07 `merkleTree` | **불변 구조** — B13 네 원칙(불변식 절 판별 · 인자 없는 질의는 구성과 묶어 상한 · 계약이 구성 시점 · 스위트 껍데기). 뒤에 도는 쪽이 앞의 선례를 쓴다 | `conventions.md:323-378,2541-2562,2578-2579` |
| T5-03 `pieceTable` | `gapBuffer`(완료)와 같은 계약인지 함께 본다 · 공간 | `ord006-wbs.ts:287` |
| T5-04 `skipList` | `treap`(완료)과 같은 계약일 수 있다 · `concurrentSkipList` 와 연산 집합이 갈렸다는 사실이 입력 | `wbs.md:83-85` · `conventions.md:1634-1635` |
| T5-05 `cuckooFilter` | **`bloomFilter`(A군 · `KAN-026`)와 함께 판정.** 메모리 직접 사용 목록 편입 판정도 걸린다 | `conventions.md:2283-2285,1425-1429` · `runbook.md:1922` |
| T5-06 `huffmanTree` | 「자료구조인지부터」 — 알고리즘 산출물이면 계약이 서지 않는 것 자체가 산출 | `ord006-wbs.ts:309-311` |

### `KAN-026` 과 병렬 — 워크트리를 넘는 의존 하나

유저 지시로 `KAN-026` 과 병렬이다(`dep-waive` 기록). 겹치는 공유 파일은 `wbs.md:101-124` §4
규약을 그대로 따르고 둘을 더한다.

- **불변 사실 번호 대역**: T2 120–139(소진) → 250–269 · T3 140–159 · T4 160–179. T5 는 180–199 를
  소진했으므로 90–99 를 쓰고, 모자라면 **230–249**. 이 카드가 더 열어야 하면 **300–349**. `KAN-026` 은 200–229 와 넘칠 때 270–299 를 쓴다. 대역을 미리
  갈라야 병합 뒤 「불변 사실 N」 인용이 안 어긋난다(`wbs.md:64-70` 추천안 ①).
- **`tools/ord006-wbs.ts` 는 두 카드가 함께 고친다** — 이 카드는 기존 T2~T5 블록 안만,
  `KAN-026` 은 파일 끝에 새 트랙 블록만.

**`cuckooFilter` 는 워크트리를 넘는 의존이었다.** `bloomFilter` 판정이 `KAN-026` 에서 나오는데
두 카드는 병합 전까지 서로의 산출을 못 본다. 루트 카드끼리의 의존은 구조 오류이므로 유저 결재로
`KAN-026` 에 이관했다 — 두 카드 사이에 남은 의존은 없다.

### 재개 전 정리할 미결 셋 (`S1`)

1. **§규약2 「크게 키웠다 줄인 뒤 재는 시나리오」** — 넣으면 계약 20종의 스위트를 다시 봐야
   한다(`runbook.md:390-391,2472-2473`). 이 카드는 **넣지 않고 넘긴다** — 17종 전환과 무관하게
   20종을 다시 여는 일이라 범위가 다르다. 결정은 `S1` 에서 기록만 한다.
2. **T5 번호 대역 소진** — 위 대역 배정으로 닫는다.
3. **stack 가이드 옛 문형** — 가이드 층이라 `KAN-036` 으로 넘긴다.

### 배치안 — 단일 에이전트 수행 (2026-09-14 유저 결재)

`KAN-026` 과 카드 단위로 이미 병렬이라 동시에 도는 흐름이 둘이다. `wbs.md` 권장 동시 트랙 수(2~3)의
상한은 공유 파일이 정하므로 카드 안에서 다시 병렬로 돌리지 않는다. 에이전트 하나가 이 워크트리에서
배치를 순서대로 돌고, 메인 세션이 배치마다 검증·커밋한다. 트랙 도구의 claim 순서는 병렬용이라
여기서는 **트랙을 섞어 무게로 묶는다** — 트랙 안의 선후(`sparseTable` → `merkleTree`)만 지킨다.

**수치는 추정이다** — 과거 실적은 가이드 포함(세션 ≈ 1~2종), 계약만 떼어 낸 실측은 B23 계약
커밋 하나(2종 +1,220줄, `50cd614`)다.

| 배치 | work | 묶은 이유 |
| --- | --- | --- |
| 1 | `S1` `S15` | 재개 정비 + `cuckooFilter` 이관 닫기(수행 없음) |
| 2 | `S2` | 합치기 계약 C — 한정자로만 갈리는 적대 시나리오가 필요해 무겁다 |
| 3 | `S3` | 키 낮추기 계약 D |
| 4 | `S4` | Bound 판정(O(log log u))을 동반한다 |
| 5 | `S5` `S6` | 사전·분리집합 — 역아커만은 확정 처분이라 가볍다 |
| 6 | `S7` `S8` | 되돌리기 분리집합 + 지연 전파 구간 트리 |
| 7 | `S9` `S10` | 영속 구간 트리 + 불변 구조 선례(`sparseTable`) |
| 8 | `S11` `S12` | 공간 분할 둘 — B19 가 다른 계약으로 닫았다 |
| 9 | `S13` `S14` | `pieceTable`(gapBuffer 대조) + `skipList`(treap 대조) |
| 10 | `S16` `S18` | 「자료구조인가」(`huffmanTree`) + `ahoCorasick` |
| 11 | `S17` `S19` | 불변 구조 둘째(`merkleTree`, `sparseTable` 선례 인용) + 마감 |

오케스트레이션안(8 배치, 폭 3)은 채택하지 않았다 — 두 카드를 합쳐 최대 여섯이 같은 런북과 등록
파일을 동시에 고치게 되고, 등록이 섞인 선례(B20, `wbs.md:103-105`)가 있다.

### 버린 대안

1. **WBS 도구와 트랙을 버리고 새로 뽑기** — 무효인 것은 유닛의 가이드 산출뿐이다. 트랙·대역·
   claim·주의사항(`!`)은 17유닛에 그대로 맞고, 새로 뽑으면 판정 이력 인용이 끊긴다.
2. **§규약2 시나리오 규칙을 이 카드에서 도입** — 완료된 20종을 다시 연다. 별도 카드 몫이다.
3. **`cuckooFilter` 를 `bloomFilter` 판정 없이 먼저 전환** — `conventions.md:2285` 가 함께 보라고
   적었다. 따로 판정하면 한쪽 계약을 다시 쓴다.

## 실행 계획
- [x] `S1` 재개 정비 — WBS 유닛에서 가이드 산출 제거 · 번호 대역 배정 · 미결 셋 처분 기록
- [x] `S2` T2-03 heap/pairingHeap 계약 전환
- [x] `S3` T2-04 heap/fibonacciHeap 계약 전환
- [x] `S4` T2-05 heap/vanEmdeBoasTree 계약 전환 — O(log log u) 판정
- [x] `S5` T3-03 hash/lruCache 계약 전환
- [x] `S6` T3-04 disjoint-set/unionFind 계약 전환
- [x] `S7` T3-05 disjoint-set/disjointSetRollback 계약 전환
- [x] `S8` T4-03 range-query/segmentTreeLazy 계약 전환
- [x] `S20` range-query/segmentTreeLazy 스위트에 자리 수가 값을 바꾸는 주입 한 벌 추가 — act 의 count 오전달을 잡기
- [x] `S9` T4-04 range-query/persistentSegmentTree 계약 전환
- [x] `S10` T4-05 range-query/sparseTable 계약 전환 — 불변 구조
- [x] `S11` T4-06 spatial/kdTree 계약 전환
- [x] `S12` T4-07 spatial/quadtree 계약 전환
- [ ] `S13` T5-03 linear/pieceTable 계약 전환
- [ ] `S14` T5-04 probabilistic/skipList 계약 전환
- [x] `S15` T5-05 probabilistic/cuckooFilter — KAN-026 으로 이관(수행 없음)
- [ ] `S16` T5-06 tree/huffmanTree 처분 실행
- [ ] `S17` T5-07 tree/merkleTree 계약 전환 — 불변 구조
- [ ] `S18` T5-08 trie/ahoCorasick 계약 전환
- [ ] `S19` 마감 — B군 계약 확인(cuckooFilter 는 KAN-026) + 검토서

### 단계별 완료 기준

**`S1` — 재개 정비.** 산출 넷: ① `tools/ord006-wbs.ts` 남은 유닛의 산출 정의에서 가이드를 떼어
「계약만」으로(주의사항 `!` 은 보존). T5-05 `cuckooFilter` 는 `UNITS` 에서 빼고 `KAN-026` 이관을 주석으로 남긴다 ② `docs/ORD-006-wbs.md` 머리에 재개 사실 · 무효 범위(가이드
산출뿐) · §5 종료 산출에서 가이드 항목 제거 ③ 번호 대역 표에 T5 → 90–99 · 230–249, 그리고
`KAN-026` 200–229 를 사실로 기록 ④ 미결 셋 처분(§규약2 시나리오 규칙 넘김 · 대역 · stack 가이드
→ `KAN-036`)을 런북 배치 지도에. 판정: `bun run tools/ord006-wbs.ts` 가 남은 16유닛을 내고
`bun run tools/ci.ts gates` 통과.

**`S2`~`S18`(`S15` 제외) — 유닛 하나.** 공통 완료 기준(`docs/ORD-006-wbs.md` §3·§5 에서 가이드를 뺀 것):
- 착수 전 `bun run tools/ord006-wbs.ts --all` 로 그 유닛의 `!` 주의사항을 읽는다.
- 헤더 여섯 항목(`docs/ORD-006-conventions.md:42`). 물려받은 상한 주장은 판정 입력으로 쓰지 않는다(불변 사실 65).
- `.contract.ts` + `.test.ts`(runContract) + `_reference/<name>.ts`(`__cost` · `#region guide:core`).
  불변 구조는 스위트 껍데기(`conventions.md:372`).
- `tools/emit-vectors.ts` · `VERIFICATION_GRADES` 등록(알파벳 순), `rust/vectors/` 생성.
- 지우기 전 `bun run tools/check-links.ts refs <problem.md>` → 참조 교체 → `-problem.md` 제거.
- `bun run tools/ci.ts all` · `bun run tools/check-contract.ts` · `bunx tsc --noEmit` ·
  `bunx --bun @biomejs/biome check <바꾼 경로>` 경고 0.
- 종료 산출: 칸반 메모 유닛 id 로 시작 · conventions 절 추가(규약이 바뀐 경우) · 런북 불변 사실(트랙 대역) · `UNITS` 완료 반영.

추가 기준:
- `S4` `vanEmdeBoasTree` — O(log log u) 를 Bound 에 넣을지 판정하고 결과를 §규약2 에 절 추가로 적는다. 넣지 않으면 계약이 그 상한을 어떻게 적는지까지.
- `S6` `unionFind` — 역아커만은 O(1) 로 접는다(확정, `conventions.md:2264-2271`). 다시 열지 않는다.
- `S10` `sparseTable` · `S17` `merkleTree` — B13 네 원칙 적용 사실을 헤더·스위트에서 확인. `S17` 은 `S10` 선례를 인용.
- `S14` `skipList` — `treap` 과 같은 계약이면 성격 전환(`{...정본Contract, name}`).
- `S15` `cuckooFilter` — 유저 결재(2026-09-14)로 `KAN-026` `S19` 에 이관. 배치1 에서 `doc-step --state done --note "KAN-026 S19 로 이관 — 수행 없음"` 으로 닫는다.
- `S16` `huffmanTree` — 알고리즘 산출물로 판정되면 처분(이관·제거)을 실행하고 inventory 에 사유를 남긴다.

**`S20` — segmentTreeLazy 스위트 구멍 막기(2026-09-15 추가).** `S8` 실측: 정본 스위트가 덮기 주입(`coverAct` —
`segmentTreeLazy.contract.ts:55-56`, 자리 수를 안 읽는다)만 써서, `act` 에 자리 수를 잘못 넘기는 구현이 축1 을 통과한다
(불변 사실 175 — 지금은 `.test.ts` 주입 정책 시험이 정본과 스텁만 본다). 완료 기준:
- 자리 수가 값을 바꾸는 주입(예: 합 + 더하기 갱신, `act(u, v, c) = v + u·c`)으로 같은 연산 집합을 한 벌 더 돌린다 — 계약
  표·헤더·정본은 바꾸지 않는다.
- 자리 수를 잘못 넘기는 결함 fixture(새 파일)가 새 벌에서 축1 로 걸리고, 정본은 두 벌 모두 통과한다.
- `emit-vectors --check` 가 바뀌면 vector 를 다시 뽑고 그 사실을 적는다. `runContract.test.ts` 에 줄을 넣으면 인용 전수 대조.
- `bun run tools/ci.ts all` · `check-contract` · `check-citations`. 불변 사실은 T4 추가 대역 320–329, conventions 는 절 추가.

**`S19` — 마감.** `bun run tools/ord006-wbs.ts` 가 T2~T5 완료 · B군 `-problem.md` 0 ·
`bun run tools/ci.ts all` · 검토서(`review-init`) 발행.

## 검증
### 카드 종료 조건

1. 대상 구조 전부에 `.contract.ts` · `_reference/<name>.ts` 가 서 있고 `-problem.md` 가 0 이다
   (「자료구조가 아니다」로 처분된 구조는 inventory 에 사유와 함께 제외).
2. `bun run tools/ci.ts all` 이 통과한다 — 3모드(self · reference · practice) + 게이트.
3. `bun run tools/ord006-wbs.ts` 가 이 카드의 트랙을 완료로 낸다.
4. 검토서의 살아 있는 항목이 전부 승인이다.

**옛 `-guide.mdx` 는 이 카드의 종료 조건이 아니다** — `KAN-036` 이 걷는다.
그래서 `tools/check-guide-rhythm.ts` 기준선이 이 카드에서 바뀌면 안 된다.

### 명령

```bash
bun run tools/ci.ts all                          # 배치 종료마다
bun run tools/check-contract.ts                  # 명세↔스텁↔정본↔스위트 정합
bun run tools/emit-vectors.ts --check            # 언어 중립 vector 가 계약과 어긋났는가
bun run tools/check-links.ts refs <지울 경로>     # 지우기 전에
bun run tools/check-links.ts check
bun run tools/check-citations.ts                 # runContract.test.ts 줄을 넣었으면 특히
bun run tools/guide-core.ts check                # 새 정본에 guide:core 구간이 있는가
bun run tools/ord006-wbs.ts                      # 트랙 진행
bunx tsc --noEmit
bunx --bun @biomejs/biome check <바꾼 경로>        # bunx biome 은 다른 패키지다
cd rust && cargo test                            # vector 재생
```

### 함정 (실측)

- **축3 은 벽시계를 쓰지 않는다** — `__cost` 누적 카운터만. `check-contract.ts` 가 `performance.now` 를 센다.
- **`bunx biome` 금지** — 아무것도 검사하지 않고 exit 0.
- **`_contract/_fixtures/` 는 읽기만** — 두 계약이 공유하는 fixture 를 고치면 다른 계약의 자기시험 기대값이 말없이 바뀐다(`docs/ORD-006-wbs.md` §4).
- **`runContract.test.ts` 에 줄을 넣으면 다른 문서의 `경로:줄` 인용이 밀린다** — 넣은 배치가 `grep -rn "runContract.test.ts:" src docs` 로 전수 대조(불변 사실 106).
- **병렬 카드(`KAN-026` ↔ `KAN-027`)가 같은 공유 파일을 고친다** — 불변 사실 번호는 카드별 대역만 쓴다. 병합 충돌은 줄 단위로 양쪽을 살린다.

## 수행 내역
<!-- KANBAN:LOG append-only — 아래로만 덧붙인다. 위를 고치지 않는다. -->
- 2026-09-14T20:44 · s:f2fd90c7 — `전략` 섹션 교체
- 2026-09-14T20:44 · s:f2fd90c7 — `실행 계획` 섹션 교체
- 2026-09-14T20:44 · s:f2fd90c7 — `검증` 섹션 교체
- 2026-09-14T21:11 · s:f2fd90c7 — `전략` 섹션 교체
- 2026-09-14T21:11 · s:f2fd90c7 — `실행 계획` 섹션 교체
- 2026-09-14T21:11 · s:f2fd90c7 · S15 done — 유저 결재(2026-09-14)로 KAN-026 S19 에 이관 — 이 카드에서는 수행 없음. bloomFilter 와 한 판정 자리에서 보라는 docs/ORD-006-conventions.md:2285 가 워크트리를 넘는 의존을 만들었다
- 2026-09-14T21:18 · s:f2fd90c7 · S1 doing — 착수
- 2026-09-14T21:30 · s:f2fd90c7 · S1 done — 재개 정비 — WBS 도구 T5-05 cuckooFilter 제거(KAN-026 이관 주석 8줄로 줄 인용 보존)·31 유닛/36종·남은 16·note 전부 보존, wbs.md 머리에 재개·무효 범위(가이드 산출뿐)·T5 대역 90–99→230–249(KAN-026 200–229), 런북 배치 지도에 재개 행+미결 셋 처분. ci gates 통과
- 2026-09-14T21:40 · s:f2fd90c7 · S2 doing — 착수
- 2026-09-15T01:24 · s:f2fd90c7 · S2 done — T2-03 pairingHeap 계약 C(넣기·합치기 worst O(1) · 빼기 amortized O(log n)) — 축1 객체를 leftistHeap 과 공유, 한 호출 적대 시나리오는 B 스위트+정본 교차로 받음, 결함 fixture 2 신설, 불변 사실 250–256(T2 대역 소진으로 250–269 개설), guide-skeleton 면제 1줄. ci.ts all 16단계 통과
- 2026-09-15T01:29 · s:f2fd90c7 · S3 doing — 착수
- 2026-09-15T02:19 · s:f2fd90c7 · S3 done — T2-04 fibonacciHeap 계약 D(C 일곱 행 + decreaseKey amortized O(1)) — 핸들 모형(가리키는 원소만 정함·빠진 원소 거절), D⊃C·D∦B 수치(B18 의 B⊂D 는 연산 집합에서만), 경계 케이스 B 여덟·시나리오 C 넷 같은 객체, fixture 2 신설(siftingLinkingHeap·rescanningTopHeap), 불변 사실 257–267, guide-skeleton 면제 1줄. ci.ts all 16단계 통과
- 2026-09-15T02:23 · s:f2fd90c7 · S4 doing — 착수
- 2026-09-15T03:07 · s:f2fd90c7 · S4 done — T2-05 vanEmdeBoasTree 계약(정수 우주 위의 정렬 집합 · 생성자 worst O(u) · 갱신·찾기·이웃 worst O(log log u) · 양 끝 O(1)) — Bound 늘리지 않음(loglog 비율 1.079·1.062 가 O(1)·O(log n) 구간 안, 새 구간에 O(log u) 트라이 1.19·1.16 이 듦, 안전 정수 전체 폭 1.72배), 사다리를 원소 수·우주로 갈라 둘 다 O(1) 판정, fixture 4 신설, 불변 사실 268–269·300–306(T2 300–309 개설), pairingHeap 헤더 상각 문장 교체, guide-skeleton 면제 1줄. ci.ts all 16단계 통과
- 2026-09-15T03:24 · s:f2fd90c7 · S5 doing — 착수
- 2026-09-15T03:46 · s:f2fd90c7 · S5 done — T3-03 lruCache 계약(용량이 정해진 사전 · 넘치면 가장 오래 안 쓴 키 밀어내기) — 생성자 worst O(n)(n=용량) · get·put expected O(1)(hashMapChaining 의 시퀀스 기대 정의), 「쓴다」=put·맞은 get, 표면 넷 고침(임의 K,V+펴기 주입 · -1→null · 용량 RangeError · 처방 제거), 불변식 없음(읽는 연산이 전부 상태를 바꿈), 사전과 동시에 만족하는 구현 없음(181 모양), fixture 5 신설(축3 셋 · 축1 둘), 불변 사실 155–159(T3 대역 소진 → 310–319 개설), guide-skeleton 면제 1줄
- 2026-09-15T03:46 · s:f2fd90c7 · S6 doing — 착수
- 2026-09-15T04:12 · s:f2fd90c7 · S6 done — T3-04 unionFind 계약(원소 [0,n) 분할 · 합치기 · 이름표 · 같은 집합 묻기) — 생성자 worst O(n) · 세 행 amortized O(alpha(n)) 을 축3이 O(1) 로 판정(B19 확정 처분을 헤더·수치로), find 를 「가장 작은 원소」로 정함(결재 대상), 전제 둘 RangeError 전면화, 불변식 없음, 거울상 두 계열을 한 호출열에서 겨눔, fixture 2(방식 5) 신설 · 로그만큼 어기는 둘 통과 고정, 불변 사실 310–316(T3 추가 대역 개설), radixTree 가이드 링크 1줄(scope 밖), guide-skeleton 면제 1줄. ci.ts all 16단계 통과
- 2026-09-15T04:17 · s:f2fd90c7 · S7 doing — 착수
- 2026-09-15T08:34 · s:f2fd90c7 · S7 doing — 이어 하기(세션 한도 뒤)
- 2026-09-15T08:43 · s:f2fd90c7 · S7 done — T3-05 disjointSetRollback 계약(원소 [0,n)·이름표=가장 작은 원소 unionFind 그대로 · 되돌리는 단위=합치기 호출) — find·union·connected amortized O(log n) · rollback amortized O(1), snapshot·restore·groupCount 제거(물려받은 restore 구멍 자기시험 고정), 되돌리기는 한정자를 깨지 않고 상각 설계(줄이는 숲·작은 쪽 다시 적기·다시 합치기)를 깨뜨림 — unionFind 스위트 통과·되돌린 뒤 거듭하기에서만 걸림, unionFind 와 서로 담지 않음(정본 6·7·8 로그 인수), B19 「압축이 이전 상태를 지운다」 반례(크기+기록 줄이기 통과), fixture 3 신설, 불변 사실 317–319·340–342(T3 대역 소진 → T5 몫 끝 열 340–349), guide-skeleton 면제 1줄. T3 트랙 완료
- 2026-09-15T08:43 · s:f2fd90c7 · S8 doing — 착수
- 2026-09-15T09:01 · s:f2fd90c7 · S8 done — T4-03 segmentTreeLazy 계약(구간의 자리마다 갱신 적용 · 임의 구간 접기) — 생성자 worst O(n) · apply·query worst O(log n)(세 법칙에 조건부), 주입 넷(combine·identity·act(update,value,count)·compose(later,earlier)) · 주입자 의무 결합법칙·분배·합성(깨지면 정본 16/값 배열 10), 표면 넷 고침(주입 열기 · values 받는 생성자 · 반열린 구간 · rangeAdd/rangeSum→apply/query), segmentTree 와 연산 집합으로 서로 담지 않음(pointwise 27,652·126,982·573,448), worst 근거 buffered(단일 최대 50,650… / 평균 53.70…), worst 시나리오 32 걸음 고정, fixture 4 신설, 불변 사실 174–179(T4 대역 소진 → 320–329), guide-skeleton 면제 1줄
- 2026-09-15T09:11 · s:f2fd90c7 — `실행 계획` 섹션 교체
- 2026-09-15T09:18 · s:f2fd90c7 · S20 doing — 착수
- 2026-09-15T09:25 · s:f2fd90c7 · S20 done — segmentTreeLazy 스위트 둘째 벌(합·더하기·자리 13 · scenarios 같은 객체) — fixture miscountedLazyRangeFold(one·parent)가 첫째 벌 통과·둘째 벌 첫 경계 케이스에서 걸림(2/22 · 41/25), 축3 계측 정본과 같음, 채운 잎 세기 변이는 결함 아님(2만 회 무차이), vector SegmentTreeLazyCounted.json 신설(기존 불변), 불변 사실 320–321(T4 추가 대역 320–329 개설)
- 2026-09-15T09:29 · s:f2fd90c7 · S9 doing — 착수
- 2026-09-15T09:43 · s:f2fd90c7 · S9 done — T4-04 persistentSegmentTree 계약(어느 버전에서든 자리 하나 바꾼 새 버전 짓기 · 지은 어느 버전이든 구간 접기) — 생성자 worst O(n) · update·query worst O(log n), 버전 수는 상한에 없음, 번호=지은 차례·던진 update 는 버전 안 지음, 불변 사실 67 세 걸음 → 「잊지 말라」가 관측돼 계약이 갈림(latestOnly 7/0), segmentTree 를 담음(결합에 기대지 않는 껍데기 · 스위트 전부 통과), 시나리오 둘(사슬 n + 첫 버전·사슬 끝 번갈아 32 걸음)이 replaying·rerooting·buffered 를 잡음, worst 근거 buffered(22,583…/39·47·55), versionCount 제거, 무작위 버전 인자 나눈 나머지, fixture 4 신설, 불변 사실 322–325, guide-skeleton 면제 1줄
- 2026-09-15T09:48 · s:f2fd90c7 · S10 doing — 착수
- 2026-09-15T09:57 · s:f2fd90c7 · S10 done — T4-05 sparseTable 계약(한 번 지은 수열의 임의 구간을 멱등 결합으로 접기) — 생성자 worst O(n log n)(구성 시점 조항) · query worst O(1), B13 네 원칙 적용(불변식 없음 · 인자 없는 질의 없음 · 구성 시점 · reindex 껍데기), 멱등=상수 질의의 충분조건·필요조건 아님(nonOverlappingFoldTable 이 합에서 같은 계급 21,504…/4) → 겹쳐 덮는 계열을 들이는 주입자 의무로 적음(합에서 정본 8/값 배열 6), 불변 구조 worst 셋째 계열(구성을 첫 질의로 미루기 17,435… 실패/20.02·24.01·28 통과 — T1-06 절 보강 필요), segmentTree 정본이 이 스위트를 로그 인수만큼 어기며 통과, fixture 3 신설, 불변 사실 326–329(T4 추가 대역 소진), guide-skeleton 면제 1줄
- 2026-09-15T10:38 · s:f2fd90c7 · S11 doing — 착수
- 2026-09-15T13:57 · s:f2fd90c7 · S11 done — T4-06 kdTree 계약(평면 정수 점 · 여러 벌 · 넣기 amortized O(log² n)→O(log n) 판정 · 범위 worst O(√n+k) · 최근접 worst O(n)) — 차원 2 고정(열면 n^{1-1/d} 가 Bound 밖), 좌표 폭은 상한에 없음(n 과 같은 배율로 사다리), k 양 끝(가는 띠 0 · 전부 n), 최근접 행은 비용으로 아무것도 배제 안 함(원 위 입력에서만 정본 O(n)), quadtree 와 서로 담지 않음(정의역 정수 · 최근접 연산), size·생성자 점 목록 제거, 불변식 1, fixture 6 신설(scanning·unbalanced·sortedOutput·buffered·xSortedRuns·regionQuadtree), guide-skeleton 면제 1줄
- 2026-09-15T13:58 · s:f2fd90c7 · S12 doing — 착수
- 2026-09-15T14:17 · s:f2fd90c7 · S12 done — T4-07 quadtree 계약(평면 유한 좌표 점 · 여러 벌 · 넣기 amortized O(log² n) · 범위 worst O(√n+k)) — 경계 사각형·용량·size 제거(배제하는 구현 없음), query(Rect)→rangeSearch(min,max)(덧셈 반올림 경계), 불변식 없음, 정본=점 순위 네 갈래 정적 나무의 이진 올림, kdTree 가 지은 경계 케이스 셋·시나리오 셋 같은 객체, 칸 가운데 네 등분 설계(regionQuadtree)가 띠 양옆 몰린 점에서 246·728·2,632 로 걸림, kdTree 와 서로 담지 않음(같은 호출 다른 답·최근접 행은 비용으로 배제 없음), 불변 사실 330–339(T4 추가 대역 둘째), conventions 절 넷, guide-skeleton 면제 1줄. T4 트랙 완료. ci.ts all 16단계 통과
