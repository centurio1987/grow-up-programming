---
card: KAN-044-VZXM29
title: cartesianTree 구성 상한의 셋째 계열 실측 — 헤더 「배제되는 계열은 둘」 보강
created: 2026-09-17
scope: src/data-structures/tree/cartesianTree/**, src/data-structures/_contract/_fixtures/**, src/data-structures/_contract/runContract.cartesianTree.test.ts, src/data-structures/trie/suffixArray/**, src/data-structures/trie/suffixTree/**, docs/ORD-006-conventions.md, docs/ORD-006-runbook.md, tools/_baseline/citations.tsv
---

# KAN-044-VZXM29 — cartesianTree 구성 상한의 셋째 계열 실측 — 헤더 「배제되는 계열은 둘」 보강

## 전략
**무엇이 낡았나.** `tree/cartesianTree` 헤더의 「여섯 연산이 모두 `worst` 인 근거」 문단은 **배제되는 계열이 둘**이라 적고 둘 다 이 계약에 없다고 맺는다 — ① 비용을 뒤 호출로 미루는 계열(상태를 바꾸는 연산이 있어야 선다) ② 무작위성으로 상한을 얻는 계열(답이 입력의 함수로 유일하다). 그런데 앞 카드가 **셋째 계열**을 실물로 냈다 — 공개 연산이 상태를 바꾸지 않아도 구현이 안에서 **짓기를 첫 질의로 미룰 수 있다.** 그것이 런북 불변 사실 328 이고, 규약의 한정자 근거 절도 셋째 계열을 받았으며, `range-query/sparseTable` 헤더는 이미 셋을 적고 수치까지 들고 있다. `tree/cartesianTree` 만 그 정정을 못 받았다 — 앞 두 카드의 scope 밖이었기 때문이다.

**왜 산문 고치기로 끝내지 않는가.** 이 문단이 주장하는 것은 「배제해도 잃는 것이 없다」이고, 그 근거는 **그 계열이 실제로 걸리는 자리를 보인 수치**다. `sparseTable` 이 그렇게 섰다 — 지연 구성 계열의 단일 호출 최대가 걸리고 호출 평균은 통과한다는 실측이 있고, 그래서 「축3의 단일 호출 최대가 그 강함을 잰다」가 근거 문장이 된다. 수치 없이 계열 이름만 셋으로 늘리면 이 계약에서는 그 계열이 **어디서 걸리는지**를 아무도 모르는 채 문단만 길어진다. 그리고 걸리는 자리가 없다면 그것은 더 중요한 발견이다 — 그때는 「배제해도 잃는 것이 없다」가 아니라 **「배제한다고 적었는데 스위트가 안 본다」**가 되어 검사 공백이다.

**이 계약의 모양이 `sparseTable` 과 다른 점.** 둘 다 불변 구조이고 구성 상한이 있지만, `cartesianTree` 의 질의는 `size` · `value` · `left` · `right` 넷이 `O(1)` 이고 `inOrder` 가 `O(n)` 이다. 지연 구성 계열은 생성자를 베끼기로만 끝내므로 구성 시나리오(`O(n)`)는 **더 빠르게** 통과하고, 첫 질의 한 번이 트리를 통째로 지어 상수 상한을 어긴다. 그러니 걸리는 자리는 질의 시나리오의 단일 호출 최대다. 이것이 예측이고, 이 카드가 할 일은 **그 예측을 짓고 재는 것**이다.

**범위를 여기서 긋는다.** 같은 문장을 쓰는 불변 구조가 이 계약 하나가 아니다. 카드의 목표가 `trie/suffixArray` · `trie/suffixTree` 를 지목하고 있지만, 지목이 맞는지부터 확인한다 — 두 구조의 헤더에는 「배제되는 계열」이라는 말이 안 보인다(전수 검색). **이름으로 찾지 않고 성질로 찾는다**: 공개 연산이 상태를 바꾸지 않으면서 구성 상한을 가진 계약을 전부 훑고, 그 가운데 한정자 근거를 「배제될 계열이 없다」 꼴로 적은 것을 목록으로 낸다. 고치는 것과 두는 것을 그 목록 위에서 가른다.

**하지 않는 것.** 지연 구성 계열을 구조마다 하나씩 짓지 않는다. 이 카드가 짓는 실물은 `cartesianTree` 용 하나이고, 나머지 구조는 **문단이 필요한지 판정하고 그 판정 근거를 적는 데까지**다. 실물이 더 필요하다고 판정되면 그 사실과 대상을 다음 카드로 남긴다 — 검사 공백을 여는 판정이므로 근거 없이 「없다」로 닫지 않는다.

## 실행 계획
- [x] `S1` 지연 구성 계열을 짓고 축3으로 잰다 — 걸리는 자리가 어디인지 수치로 낸다
- [x] `S2` 헤더 근거 문단을 보강하고, 같은 문단이 필요한 구조를 성질로 전수 판정한다
- [x] `S3` 검토 반려 ① — 약한 한정자 둘을 다 재고 마무리 문장을 실측에 맞춘다
- [x] `S4` 검토 반려 ②·③ — 넘긴 자리에 받는 곳을 만들고 낡는 문장 목록을 채운다

### 단계별 완료 기준

- **`S1`**: `_contract/_fixtures/` 에 새 파일로 지연 구성 `cartesianTree`(생성자는 받은 수열을 베껴 두기만 하고 첫 질의가 트리를 짓는다. 안쪽은 정본을 그대로 쓴다 — `deferredSparseTable` 이 선례다). 기존 fixture 는 고치지 않는다. **축1·축2 를 통과해야 한다** — 답이 전부 옳으면서 축3만 어기는 계열이어야 배제 근거의 증거가 된다. 자기시험(`runContract.cartesianTree.test.ts`)에 등록하고 **어느 시나리오에서 걸리는지와 어느 시나리오를 통과하는지를 둘 다 수치로 고정한다.** 예측은 「구성 시나리오는 통과(베끼기라 더 싸다) · 질의 시나리오의 단일 호출 최대에서 실패」이고, **예측이 틀리면 실측을 적고 그 사실이 이 카드의 결론이 된다.** 호출 평균이 어떻게 나오는지도 함께 잰다(`sparseTable` 선례는 평균으로는 안 걸렸다).
- **`S2`**: `cartesianTree.ts` 헤더의 「여섯 연산이 모두 `worst` 인 근거」 문단을 **셋째 계열을 포함하도록** 다시 쓰고 `S1` 의 수치를 근거로 건다. 그다음 **성질로 전수 조사한다** — 공개 연산이 상태를 바꾸지 않으면서 구성 상한을 가진 계약을 전부 훑어, 한정자 근거를 「배제될 계열이 없다」 꼴로 적은 자리를 목록으로 낸다(카드가 지목한 `trie/suffixArray` · `trie/suffixTree` 가 그 목록에 드는지도 여기서 판정된다). 각 자리를 **고친다 · 둔다 · 다음 카드로 넘긴다** 셋 중 하나로 처분하고 근거를 적는다. 규약의 한정자 근거 절과 런북에 결과를 남긴다(기존 절을 고쳐야 하면 `docs/ORD-006-wbs.md` §4 를 따른다). 문서를 고치면 **줄 수를 유지한 제자리 정정**을 우선하고, 인용 게이트로 표류를 확인한 뒤 대장을 갱신한다.

- **`S3`**(검토 반려 ① — 항목 3): 헤더 마무리 문장을 고치며 `amortized` 는 재고 `expected` 는 **재지 않은 채** 「흐리기가 참인 것은 `expected` 쪽뿐」이라 단정했다. 검토자가 재니 `expected` 로도 통과한다 — 무작위 8.17 · 8.16 · 8.15 · 사슬 7.50 · 7.50 · 7.50(정본 3.19 · 3.16 · 3.15 / 3.50 · 3.50 · 3.50, 수열만 드는 계열은 실패). **직접 재현하고** 마무리 문장을 「약한 한정자 둘이 다 셋째 계열을 들이고 막는 축은 `worst` 하나」로 다시 쓴다. `expected` 수치를 자기시험에 `amortized` 와 나란히 박는다. 규약 새 절의 마무리 문장 소절과 「구성이 $n\log n$ 이상이면 선례 문장이 그대로 선다」는 좁힘 문단, 런북 불변 사실 404, 낡는 문장 표의 가이드 행, 카드 수행 내역의 같은 주장을 **함께** 고친다 — 좁힘 문단은 선례 절이 그 계약에 대해 적은 것과도 어긋난다(선형·로그 갈림이 가르는 것은 판정 통계의 모양뿐이다).
- **`S4`**(검토 반려 ②·③ — 항목 6 · 7): 넘긴 자리에 **받는 곳**을 만든다. `trie/suffixArray` · `trie/suffixTree` 는 메인 세션이 백로그 카드 `KAN-050-0B1N1Z` 를 세웠으므로 규약 전수 판정 표의 두 행과 런북 불변 사실 405 에 그 이름을 적고, 두 헤더에 **검사 공백 한 줄**을 지금 넣는다(근거 문단이 없고 그 계열을 재지 않았다는 사실). 낡는 문장 표에 **T1-06 절의 마무리 문장** 행을 더한다 — 그 절은 다른 두 헤더가 인용하는 규격이라 다음 집필자가 그대로 베낀다. 카드 메모에 「규약 기존 절 넷의 담당은 미정 · 사람 결정 · 추천은 `KAN-049`」를 적어 작업 분해 문서 4절의 메모 요건을 채운다(다른 카드의 브리프는 고치지 않는다 — 결재 나면 그때 명시한다).

## 검증
- **축1·축2**: 새 fixture 가 계약 동작과 불변식을 전부 통과한다. 통과하지 못하면 그것은 「축3만 어기는 계열」이 아니므로 근거가 되지 않는다.
- **축3**: 걸리는 시나리오와 통과하는 시나리오가 **각각 무엇인지** 자기시험에 수치로 고정된다. 걸리는 자리가 하나도 없으면 그 사실을 검사 공백으로 적고 헤더 문단의 주장을 그에 맞춰 고친다(「배제해도 잃는 것이 없다」를 근거 없이 적지 않는다).
- **전수 조사**: 성질로 훑은 결과가 목록으로 남고, 목록의 각 행이 처분(고침 · 둠 · 넘김)과 근거를 갖는다. 목록에 없는 구조가 뒤에 나오면 조사 기준이 틀린 것이므로 기준을 적어 둔다.
- **게이트**: `bun run tools/ci.ts all` 17단계 · `bun run tools/check-contract.ts` · `bun run tools/check-citations.ts`(대장 지문 일치 · 래칫 세 칸 유지) · `bunx tsc --noEmit` · 고친 파일의 `bunx --bun @biomejs/biome check` 경고 0 · `cd rust && cargo test --workspace`.
- **문서 정정**: 줄 수를 유지했는지 `git diff --numstat` 으로 확인하고, 표류가 고친 자리에서만 났는지 목록을 눈으로 본 뒤 대장을 갱신한다.

## 수행 내역
<!-- KANBAN:LOG append-only — 아래로만 덧붙인다. 위를 고치지 않는다. -->
- 2026-09-17T05:12 · s:f2fd90c7 — `전략` 섹션 교체
- 2026-09-17T05:12 · s:f2fd90c7 — `실행 계획` 섹션 교체
- 2026-09-17T05:12 · s:f2fd90c7 — `검증` 섹션 교체
- 2026-09-17T05:18 · s:f2fd90c7 · S1 doing — 착수
- 2026-09-17T05:22 · s:f2fd90c7 · S1 done — 완료
- 2026-09-17T05:22 · s:f2fd90c7 · S2 doing — 착수
- 2026-09-17T05:36 · s:f2fd90c7 · S1 — `S1` 지연 구성 계열을 짓고 축3으로 쟀다 — 배치1 /  / **지은 것 둘(새 파일).** `src/data-structures/_contract/_fixtures/deferredCartesianTree.ts` 는 생성자가 받은 수열을 자리마다 1 씩 / 세며 베끼기만 하고, 다섯 질의 중 처음 불린 것이 안쪽 정본을 세운다. 계측은 베낀 몫 + 안쪽 정본의 `__cost` 이고 부분트리 객체를 따로 / 감싸지 않는다 — 정본의 부분트리가 뿌리의 칸을 올리므로 걸음 비용이 그대로 잡힌다. 자기시험은 / `src/data-structures/_contract/runContract.cartesianTree.test.ts` 로 **새 파일에 두었다**(`runContract.test.ts` 에 줄을 넣으면 이 카드 / 범위 밖 가이드 인용이 밀린다 — 불변 사실 106·255. `runContract.sparseTable.test.ts` 와 같은 길). /  / **축1 통과.** 경계 케이스 여덟과 무작위 시퀀스 500 회에서 참조 모델과 갈리는 자리가 없다(정본도 같이 돌려 대조군으로 뒀다). / **축2는 돌 것이 없다** — 이 계약의 불변식 절이 「없다」이고 스위트의 불변식 목록이 빈 배열이다(불변 사실 52 ①). /  / **축3 실측(정본 대비).** /  / | 시나리오 | 지연 구성 | 정본 | / |---|---|---| / | 구성(무작위) `O(n)` | 통과 1,024 · 4,096 · 16,384 | 통과 5,109 · 20,466 · 81,899 | / | 구성(오름차순 · 적대적) `O(n)` | 통과 1,024 · 4,096 · 16,384 | 통과 4,095 · 16,383 · 65,535 | / | 훑기(무작위) `O(1)` | **실패** 5,112 · 20,469 · 81,902(r = 4.00 · 4.00) | 통과 4 · 4 · 4 | / | 훑기(사슬 · 적대적) `O(1)` | **실패** 4,098 · 16,386 · 65,538(r = 4.00 · 4.00) | 통과 4 · 4 · 4 | / | `inOrder`(적대적) `O(n)` | 통과 6,143 · 24,575 · 98,303 | 통과 2,048 · 8,192 · 32,768 | /  / **호출 평균(같은 실행을 한정자만 바꿔 읽음).** 지연 구성 8.19 · 8.16 · 8.13(무작위) · 7.51 · 7.50 · 7.50(사슬) — **둘 다 통과.** / 정본은 3.20 · 3.16 · 3.14 / 3.51 · 3.50 · 3.50. 수열만 드는 계열(`rescanningCartesianView`)은 579.67 · 1,552.29 · 5,109.18 / / 1,286.89 · 5,130.45 · 20,496.46 으로 **평균으로도 걸린다.** /  / **예측과 어긋난 것 둘.** 카드 전략의 방향(구성 통과 · 질의의 단일 호출 최대에서 실패)은 맞았다. 다른 것은 ① 걸리는 질의 시나리오가 / 하나가 아니라 **둘**이다(훑기를 겨눈 시나리오가 둘이고 둘 다 첫 걸음이 트리를 짓는다) ② 호출 평균이 선례처럼 로그를 따라 자라 / 「해상도 아래라 통과」하는 것이 아니라 **진짜 상수**다(구성이 선형인데 나눠 갚을 걸음이 n 개다). ②가 근거 문장을 선례보다 강하게 / 만든다 — 이 계열은 상각 읽기를 실제로 지키므로 그것을 배제하는 축이 `worst` 뿐이다. /  / **통과·실패 모양만으로는 `rescanningCartesianView` 와 구별되지 않는다**(둘 다 구성 둘 통과 · 훑기 둘 실패). 갈리는 자리가 호출 / 평균이고, 그래서 자기시험이 두 통계를 나란히 박았다.
- 2026-09-17T05:37 · s:f2fd90c7 · S2 — `S2` 헤더 근거 문단 보강 + 성질로 한 전수 판정 — 배치1 /  / **헤더 문단을 다시 썼다**(`src/data-structures/tree/cartesianTree/cartesianTree.ts` · 다섯 줄 → 열아홉 줄). 바뀐 요지 셋. /  / 1. 「배제되는 계열은 **둘**이고 이 계약에는 둘 다 없다」 → 「**셋**이고 앞 둘은 없으며 셋째(구성을 첫 질의로 미루기)는 **있다**」. / 2. 셋째 계열을 배제해도 잃는 것이 없다는 근거를 `S1` 수치로 걸었다 — 구성 둘 통과(정본보다 싸다) · 훑기 둘 실패 · 정본은 같은 /    자리에서 4 · 4 · 4. / 3. **마무리 문장을 정정했다.** 옛 문장은 「상태가 하나뿐인 구조에서 `amortized` 나 `expected` 를 적는 것은 아무것도 다르게 배제하지 /    않으면서 상한을 흐리는 것」이었는데, 실측이 그것을 `amortized` 에 대해 **뒤집었다** — 호출 평균이 진짜 상수라 `amortized` 를 적으면 /    셋째 계열이 통째로 들어온다. 흐리기가 참인 것은 `expected` 쪽뿐이다(답이 입력의 함수로 유일하면 seed 별 중앙값이 최대와 같은 값을 /    본다). /  / **전수 조사 — 이름이 아니라 성질로.** 기준 둘: ① 스텁의 공개 메서드가 전부 질의다(상태를 바꾸는 연산이 하나도 없다) ② 연산 계약 / 표에 생성자 행이 있고 상한이 적혀 있다. 계약 스위트가 선 **67 계약 전부**를 훑었다(스텁 헤더의 표와 공개 메서드를 기계로 뽑아 / 대조). 다섯이 남았다. /  / | 계약 | 한정자 근거 문단 | 처분 | / |---|---|---| / | `tree/cartesianTree` | 있었고 「둘」이라 적었다 | **고침** — 이 배치 | / | `range-query/sparseTable` | 있고 셋째 계열 · 수치를 든다(T4-05) | 둠 | / | `trie/ahoCorasick` | 있고 셋째 계열 · 수치를 든다(T5-08) | 둠 | / | `trie/suffixArray` | **없다** — 「구성 시점을 계약이 정한다」 절만 있다(B13) | **넘김** | / | `trie/suffixTree` | **없다** — 같다(B13) | **넘김** | /  / **카드가 지목한 둘의 판정.** `trie/suffixArray` · `trie/suffixTree` 는 기준 둘을 만족하므로 **목록에 든다.** 다만 「배제되는 계열은 / 둘」이라는 문장을 쓴 것이 아니라 **근거 문단이 아예 없다**(KAN-027 `S19` 가 낸 결론 그대로). 두 계약의 스위트는 그 계열을 잡을 / 모양이다 — 질의 시나리오가 색인을 계측 밖에서 짓고 그 뒤 n 번을 재므로 첫 걸음이 구성을 진다. **다만 재지 않았다**(지연 구성 계열을 / 구조마다 하나씩 짓지 않는 것이 이 카드의 범위다). 그래서 적은 것은 「걸린다」가 아니라 **검사 공백**이고, 넘기는 일은 fixture 둘을 / 짓고 재서 두 헤더에 근거 문단을 새로 쓰는 것이다. /  / 목록 밖 이웃 둘도 근거를 적었다 — `tree/merkleTree`(`update` 있음 · 불변 사실 343 이 같은 판정) · / `range-query/persistentSegmentTree`(`update` 가 새 버전을 짓는다). /  / **문서.** 규약에 절을 **덧붙였다**(기존 절 수정 없음). 런북 끝에 불변 사실 404 · 405 를 붙였다. `git diff --numstat` 으로 확인 — / 규약 98 추가 · 0 삭제, 런북 25 추가 · 0 삭제. 인용 게이트가 표류 한 건(고친 헤더 문단)을 냈고 목록을 눈으로 본 뒤 대장을 갱신했다 / (대장 1,199 행 그대로 · 바뀐 줄 하나 · 래칫 세 칸 461/73/286/120 과 카드 문서 23/3/2/18 그대로). /  / **고치지 않고 남긴 낡은 문장 넷.** 규약 문서 기존 절 셋(정정 목록의 `tree/cartesianTree` 행 · 정정 담당 표 8 행과 그 범례 · / T4-05 보강 절 끝의 「별도 카드다」)은 `docs/ORD-006-wbs.md` §4 가 막아 담당 미정으로 넘긴다 — 규약 문서 정정을 이미 받은 `KAN-049` / 가 자연스럽다(사람 결정). 넷째는 `tree/cartesianTree` 가이드 5단계의 「배제되는 계열은 둘입니다」와 그 뒤 두 문단이고, 가이드는 이 / 트랙의 종료 산출이 아니라 `KAN-036` 몫이다. 넷 다 규약의 「낡는 문장」 절에 표로 적었다.
- 2026-09-17T05:37 · s:f2fd90c7 · S2 done — 완료
- 2026-09-17T06:09 · s:f2fd90c7 — `실행 계획` 섹션 교체
- 2026-09-17T06:12 · s:f2fd90c7 · S3 doing — 착수
- 2026-09-17T06:28 · s:f2fd90c7 · S3 — `S3` 약한 한정자 둘을 다 재고 마무리 문장을 실측에 맞췄다 — 배치2(검토 반려 항목 3) /  / **먼저 쟀다 — 검토자 값을 옮겨 적지 않고 직접 재현했다.** 검토자 스크립트를 베끼지 않고 새로 써서(스크래치패드) 훑기 시나리오 둘을 / 한정자 셋으로 각각 읽었다. 시나리오 본문 · 입력 · 걸음은 그대로 두고 `qualifier` 만 바꿔 판정기를 부르는 방식이다. 판정기의 통계 / 정의(`_contract/judge.ts` 의 `statistic`)도 함께 확인했다 — `worst` 단일 호출 최대 · `amortized` seed 1 의 시퀀스 평균 · / `expected` seed 다섯의 시퀀스 평균 중 **중앙값**이고, seed 목록이 한정자마다 다르다(`expected` 만 다섯). /  / | 시나리오 | 한정자 | 지연 구성 | 정본 | 수열만 드는 계열 | / |---|---|---|---|---| / | 훑기(무작위) `O(1)` | `worst` | **실패** 5,112 · 20,469 · 81,902 | 통과 4 · 4 · 4 | **실패** 2,049 · 8,193 · 32,769 | / | 훑기(무작위) `O(1)` | `amortized` | 통과 8.19 · 8.16 · 8.13 | 통과 3.20 · 3.16 · 3.14 | **실패** 579.67 · 1,552.29 · 5,109.18 | / | 훑기(무작위) `O(1)` | `expected` | 통과 8.17 · 8.16 · 8.15 | 통과 3.19 · 3.16 · 3.15 | **실패** 472.86 · 1,563.69 · 5,786.64 | / | 훑기(사슬 · 적대적) `O(1)` | `worst` | **실패** 4,098 · 16,386 · 65,538 | 통과 4 · 4 · 4 | **실패** 3,070 · 12,286 · 49,150 | / | 훑기(사슬 · 적대적) `O(1)` | `amortized` | 통과 7.51 · 7.50 · 7.50 | 통과 3.51 · 3.50 · 3.50 | **실패** 1,286.89 · 5,130.45 · 20,496.46 | / | 훑기(사슬 · 적대적) `O(1)` | `expected` | 통과 7.50 · 7.50 · 7.50 | 통과 3.50 · 3.50 · 3.50 | **실패** 1,278.07 · 5,123.87 · 20,479.39 | /  / **검토자가 낸 `expected` 값 여섯 묶음과 내가 잰 값이 전부 같다** — 지연 구성 8.17 · 8.16 · 8.15 와 7.50 · 7.50 · 7.50, 정본 3.19 · / 3.16 · 3.15 와 3.50 · 3.50 · 3.50, 수열만 드는 계열 472.86 · 1,563.69 · 5,786.64 와 1,278.07 · 5,123.87 · 20,479.39. 다른 자리는 / 없다. 그래서 문서에 쓴 수치는 「검토자가 잰 값」이 아니라 **이 배치가 잰 값**이고, 대조 결과가 일치라는 사실을 함께 적는다. /  / **결론.** 배치1 이 「흐리기가 참인 것은 `expected` 쪽뿐 — 답이 입력의 함수로 유일하면 seed 별 중앙값이 단일 호출 최대와 같은 값을 / 본다」고 적은 것은 틀렸다. `expected` 의 통계는 **최대가 아니라 평균의 중앙값**이고, 평균이 상수로 고정되는 이 계열은 그 읽기도 / 지킨다. 그러니 이 구조에서 약한 한정자 **둘이 다** 셋째 계열을 들이고 **막는 축은 `worst` 하나**다. /  / **마무리 문장 — 전후.** 전: 「그러므로 이 구조에서 `amortized` 는 …아니라 셋째 계열을 통째로 들이는 것이고, 그 흐리기가 참인 것은 / `expected` 쪽뿐이다 — 답이 입력의 함수로 유일한 자리에서 seed 별 중앙값은 최대와 같은 값을 본다.」 후: 「**그 계열을 막는 축이 / `worst` 하나다.** …`amortized` 와 `expected` 는 상한을 흐리기만 하는 것이 아니라 **둘 다 계열을 들인다.**」 **줄 수를 그대로 / 유지해 일곱 줄을 일곱 줄로 고쳤다**(`git diff --numstat` 7 / 7). /  / **선례와의 대비도 함께 고쳤다.** 옛 문장은 「저쪽은 해상도 아래라 통과였지만 여기서는 상각 읽기를 실제로 지킨다」로 적어 T4-05 와 / 어긋났다 — 그 절은 `range-query/sparseTable` 에 대해 「생성자 호출을 호출열에 넣어 읽는 상각에서는 이 계열이 약속을 실제로 / 지킨다」고 이미 적어 두었다. 지금 문장은 **판정 통계의 모양**만 대비한다 — 저쪽에서는 지킴이 해상도 아래에 숨어 있었고 여기서는 / 판정이 그 지킴을 상수로 보여 준다. /  / **고친 자리 전부(여섯).** ① `tree/cartesianTree` 스텁 헤더의 마무리 문장(줄 수 유지) ② 규약 새 절의 「선례 문장을 그대로 옮기면 / 틀리는 자리」 둘째 불변 항목 ③ 같은 절의 **마무리 문장 소절**(제목도 함께 — 「`amortized` 는 여기서 실제로 다르게 배제한다」 → / 「약한 한정자 **둘 다** 계열을 들인다」)과 새로 붙인 「한쪽을 재고 다른 쪽을 미루어 적지 않는다」 문단 ④ 같은 절의 **좁힘 문단** / ⑤ 같은 절의 「통과·실패 모양이 같은 이웃」 소절(제목 포함 — 갈리는 자리를 「호출 평균」에서 「약한 읽기」로) ⑥ 런북 불변 사실 404. / 여기에 지연 구성 fixture 헤더의 같은 대비 문장과 자기시험이 더 붙는다. /  / **좁힘 문단은 이렇게 말한다.** 구성을 첫 질의로 미루는 계열은 **구성 계급과 무관하게** 약한 한정자 둘을 들인다 — 규약의 상각이 / 호출열 전체로 적히므로 생성자 호출을 호출열에 넣어 읽으면 지은 몫이 걸음 수로 나뉘고, T4-05 가 선례 계약에 대해 이미 그렇게 적었다. / 그러니 T1-06 의 마무리 문장은 **어느 불변 구조에서도 그대로 쓰지 않는다.** 선형이냐 로그냐가 가르는 것은 **판정 통계의 모양** 하나다 / — 선형이면 평균이 상수로 고정되어 통과가 눈에 보이고, $n\log n$ 이상이면 평균이 로그를 따라 자라 축3 해상도 아래로 통과한다 / (불변 사실 53). 갈리는 것은 「계열이 약속을 지키느냐」가 아니라 「판정이 그 지킴을 값으로 보여 주느냐」다. /  / **자기시험이 지금 고정하는 것 — 셋이다.** 훑기 두 시험이 각각 `worst` · `amortized` · `expected` 를 나란히 잡는다. 사슬 쪽은 / 지연 구성 `worst` 4,098 · 16,386 · 65,538 실패 · `amortized` 7.51 · 7.50 · 7.50 통과 · `expected` 7.50 · 7.50 · 7.50 통과, / 수열만 드는 계열 `amortized` · `expected` 둘 다 실패, 정본 둘 다 통과. 무작위 쪽은 `worst` 5,112 · 20,469 · 81,902 실패 · / `amortized` 8.19 · 8.16 · 8.13 · `expected` 8.17 · 8.16 · 8.15 통과, 수열만 드는 계열 둘 다 실패, 정본 둘 다 통과. `readAs` 의 / 한정자 매개변수에 `expected` 를 더했고 시험 이름도 바꿨다(「worst 로만 걸리고 amortized · expected 로는 통과한다」). / `expected` 는 seed 다섯을 돌므로 제한 시간을 300 초로 올렸다. **4 건 전부 통과**(3.34 초). /  / **전수 검색으로 남은 것이 없음을 확인했다.** 「`expected` 쪽뿐」 · 「`expected` 에만 참」 · 「seed 별 중앙값이 단일 호출 최대」 · / 「상한을 흐리는 것」 · 「상각 읽기」를 `src` · `docs` · `tools` 전체에 걸어 훑었다. 소스와 문서에 남은 것은 **정정 문단이 옛 주장을 / 인용부호 안에서 되짚는 두 줄뿐**이고, 그 밖의 hit 는 T1-06 규격 원문(고칠 수 없는 기존 절 — 낡는 문장 표에 올렸다)과 검토서 · / 배치 문서 · 카드의 지난 기록뿐이다. 굵게 표시가 낀 문구는 grep 이 놓치므로 표시를 뺀 짧은 패턴으로 한 번 더 훑어 지연 구성 fixture / 헤더의 「상각 읽기를 실제로 지킨다」를 찾아 고쳤다 — 이것이 첫 훑기에서 빠졌던 자리다. /  / **게이트.** `bunx tsc --noEmit` 통과 · 고친 다섯 파일의 `@biomejs/biome check` 경고 10 은 **전부 학습자 스텁의 미사용 매개변수**로 / 이 배치가 건드리지 않은 자리다(새로 넣은 것은 주석 줄뿐이다) · `check-contract` 67 종 일치 · `check-citations` 표류 **0 건** / (대장 1,199 행 그대로 · 래칫 461/73/286/120 과 카드 문서 23/3/2/18 그대로).
- 2026-09-17T06:28 · s:f2fd90c7 · S3 done — 완료
- 2026-09-17T06:28 · s:f2fd90c7 · S4 doing — 착수
- 2026-09-17T06:29 · s:f2fd90c7 · S4 — `S4` 넘긴 자리에 받는 곳을 만들고 낡는 문장 목록을 채웠다 — 배치2(검토 반려 항목 6 · 7) /  / **받는 카드 이름을 세 자리에 박았다.** `trie/suffixArray` · `trie/suffixTree` 의 한정자 근거 문단 신설은 **`KAN-050-0B1N1Z`**(백로그 / · `KAN-036` 선행 · 메인 세션이 세웠다)가 받는다. ① 규약 새 절 **전수 판정 표의 「넘긴다」 두 행**의 처분 칸을 「넘긴다 — / `KAN-050-0B1N1Z`」로 바꿨다 ② 같은 절에 **「받는 자리를 만들어 두었다」 문단**을 붙여 두 헤더의 검사 공백이 무엇을 드는지와 「받는 / 자리 없는 넘김은 넘김이 아니라 놓침이다」(`KAN-048` 검토 · `KAN-042` `S5` 가 같은 자리를 이미 두 번 잡았다)를 적었다 ③ **런북 / 불변 사실 405** 의 표제를 「둘은 문단 자체가 없어 다음 카드다」에서 「…`KAN-050-0B1N1Z` 가 받는다」로 바꾸고 본문에 헤더 검사 공백 / 사실을 넣었다. **다른 카드의 브리프는 건드리지 않았다** — 이름을 목록 쪽에 적어 목록과 카드를 이은 것이다. /  / **두 헤더에 검사 공백 문단을 한 줄짜리 요지로 하나씩 넣었다.** 자리는 둘 다 「구성 시점을 계약이 정한다」 문단 **바로 뒤**다 — 미루는 / 계열을 막는 것이 그 문단이므로 공백도 거기 붙어야 읽힌다. 제목 줄은 둘 다 같다: **「검사 공백 — 한정자 근거 문단이 없고, 구성을 첫 / 질의로 미루는 계열을 재지 않았다」**(`KAN-044` `S2`·`S4` 가 열었다 · 받는 카드 `KAN-050-0B1N1Z`). 본문이 드는 것은 셋 — 표가 / 여섯 행을 전부 `worst` 로 적지만 그 한정자가 무엇을 배제하는지는 헤더에 없다는 것, T1-06 의 셋째 계열(생성자가 문자열을 베끼기만 / 하고 첫 질의가 색인을 통째로 짓는 구현)이 이 계약에도 선다는 것, 스위트는 그 계열을 잡을 **모양이지만**(질의 시나리오가 `reindex` / 를 걸음 밖에서 부르고 그 뒤 n 번을 잰다) **실물을 지어 재지는 않았으므로 「걸린다」가 아니라 공백으로 적는다**는 것. 두 스위트의 / 모양은 직접 열어 확인했다 — 질의 시나리오 넷이 다 걸음 밖에서 색인을 짓는다. 계약 문장 · 표 · 스텁 표면은 건드리지 않았고 더한 / 것은 주석 아홉 줄씩뿐이다. /  / **낡는 문장 표에 T1-06 절 마무리 문장 행을 더했다**(표는 고치지 않고 행만 더했다 — 기존 절은 그대로다). 행이 드는 것: 위치는 / §「`worst` 만 있는 계약의 한정자 근거는 「배제될 계열이 없다」로 적는다」(T1-06) 절의 **마무리 문장** — 「불변 구조에서 / `amortized`·`expected` 를 적는 것은 약하게 적는 것이 아니라 아무것도 다르게 배제하지 않으면서 상한을 흐리는 것이다」. 무엇이 / 바뀌었나: **두 절반이 다 낡았다** — 구성을 첫 질의로 미루는 계열이 약한 한정자 **둘 다** 들인다(이 배치 실측 · 불변 사실 404). / 앞 카드가 그 절에 셋째 계열을 3 항으로 넣었지만 마무리 문장은 그대로 남았고 그것은 **새 낡음**이라 정정 담당 표 7 행이 덮지 않는다. / 그리고 이 절은 `range-query/sparseTable` · `trie/ahoCorasick` · `tree/merkleTree` 헤더가 인용하는 **규격**이라 다음 집필자가 / 그대로 베낀다(세 헤더가 그 절을 인용하는 것은 전수 검색으로 확인했다). 담당은 앞 셋과 같다 — 미정. /  / **표가 셋에서 넷이 되면서 함께 고친 자리 셋.** 절 제목(「규약 문서 셋 · 가이드 하나」 → 「규약 문서 넷 · 가이드 하나」) · 그 아래 / 도입 문장(「낡은 문장 넷 … 넷 다 고치지 않았다 … 앞 셋은 … 넷째는 가이드라」 → 다섯 · 앞 넷 · 다섯째) · 런북 불변 사실 405 의 / 같은 셈(「낡은 문장 넷」 → 「다섯」, 넷째가 T1-06 마무리 문장이라는 것과 담당 미정 · 사람 결정 · 추천 `KAN-049` · 카드 메모가 그 / 사실을 든다는 것). **가이드 행의 「마무리 문장은 `expected` 에만 참이다」도 여기서 함께 정정했다** — 「약한 한정자 둘 다에 거짓이고 / 막는 축은 `worst` 하나다」. /  / **카드 메모를 고쳤다.** 적은 문장은 「**규약 기존 절 넷의 담당은 미정이다 — 사람 결정이고 추천은 `KAN-049` 다**(검토 반려 / 2026-09-17 항목 7 · 작업 분해 문서 4절이 요구하는 메모)」이고, 넷이 무엇인지(정정 목록의 이 계약 행 · 정정 담당 표 8 행과 범례 · / T4-05 절 끝 · T1-06 마무리 문장)와 「**후보는 담당이 아니므로 다른 카드의 브리프는 건드리지 않았다 — 결재가 나면 그때 그 카드 / 브리프에 명시로 넣고 정정 담당 표의 담당 칸을 갱신한다**」를 함께 적었다. 메모 끝에 suffix 둘의 받는 카드가 `KAN-050-0B1N1Z` 라는 / 것과 그 이름이 어디에 적혀 있는지도 남겼다. /  / **규약 절의 「이 카드가 고친 자리」도 갱신했다** — 자기시험에 `expected` 읽기가 붙은 것, 헤더 마무리 문장을 줄 수 그대로 다시 쓴 것, / 두 suffix 헤더에 검사 공백 문단이 하나씩 붙은 것. 규약 문서는 여전히 **이 카드의 절 안에서만** 고쳤고 기존 절은 건드리지 않았다.
- 2026-09-17T06:29 · s:f2fd90c7 · S4 done — 완료
- 2026-09-17T06:42 · s:f2fd90c7 · S4 — `S4` 덧붙임 — 승인된 항목 5 의 권고도 넣었다(배치2) /  / 검토 항목 5 는 **승인**이면서 권고를 하나 달았다 — 「생성자 상한이 있으면서 갱신 연산도 있는 계약에서도 구성을 첫 질의로 미루는 / 구현은 지을 수 있지만 이 문단의 대상이 아니다」를 규약의 **「조사 기준을 남겨 두는 이유」** 문단에 한 줄 적어 두면 다음 사람이 같은 / 것을 다시 따지지 않는다는 것이다(승인의 조건은 아니다). 넣었다 — 다만 **권고 문장을 옮겨 적지 않고 직접 확인했다.** /  / 확인한 것 둘. ① 검토자가 든 여섯(`range-query/segmentTree` · `range-query/fenwickTree` · `tree/linkCutTree` · / `disjoint-set/unionFind` · `linear/bitArray` · `graph-repr/graphAdjMatrix`)이 정말 **생성자 행과 갱신 행을 둘 다 든다** — 여섯 다 / 헤더 표에 생성자 행이 있고 갱신 연산 행(`update` · `link`/`cut` · `union` · `set` · `addEdge`/`removeEdge`)이 있다. ② / **여섯 중 T1-06 의 불변 구조 문단을 인용하는 헤더가 하나도 없다** — 그 절을 인용하는 헤더는 전수 검색으로 `range-query/sparseTable` / · `trie/ahoCorasick` · `tree/merkleTree` 셋뿐이다. 그래서 적은 문장은 「지을 수 있다 · 그래도 대상이 아닌 이유는 둘(첫째 계열이 / 이미 선다 · 그 절을 인용하지 않는다)」이고, 근거를 문장 안에 걸었다. /  / 게이트를 다시 돌렸다 — `check-citations` 표류 0 건(대장 1,199 행 · 래칫 그대로) · `check-links` 870 건 · `ci.ts gates` 11 단계 / 통과. 그 앞에 `ci.ts all` 17 단계와 `cargo test --workspace` 를 통과시켰고, 이 덧붙임은 규약 문서 한 곳의 문단 추가뿐이라 시험 / 네 모드에 걸리는 것이 없다.
