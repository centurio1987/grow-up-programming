---
card: KAN-035-31T4BY
title: 진행 중인 자료구조 재집필 트랙을 취소하고, 다시 설계
created: 2026-09-11
scope: docs/**, sandbox/ds-guide-v2/**, tools/section.ts, tools/guide-v2-targets.ts, tools/check-v2.ts, tools/check-proof.ts, tools/guide-core.ts, src/data-structures/linear/deque/**, src/data-structures/tree/redBlackTree/**, src/data-structures/linear/xorLinkedList/**
request_fp: ca3479cddf345085c3bffa16a6ae558d3cd3e5a4
---

# KAN-035-31T4BY — 진행 중인 자료구조 재집필 트랙을 취소하고, 다시 설계

## 전략
<!-- 왜 이 접근인가 · 제약 · 버린 대안. 사람이 자유롭게 편집한다. -->
<!-- doc-init --from-request KANBAN.requests/backlog/KAN-035-31T4BY.request.md — 요청서 본문은 리포트가 별도 블록으로 싣는다. 여기서는 중복을 피해 반영 결과만 적는다 -->

**요청서 반영.** 원문은 `KANBAN.requests/backlog/KAN-035-31T4BY.request.md` 이고 지문이
frontmatter 의 `request_fp` 에 박혀 있다. 정한 것은 셋이다 — `KAN-027` 정지 · 재집필 계획
재수립 · **새 알고리즘 집필 전략 계승**, 그리고 편당 분량 면제. 아래가 그 셋을 실행 가능한
형태로 푼 것이다.

### 유저 결재 둘 (2026-09-11)

요청서가 정하지 않은 두 갈래를 유저가 정했다. 카드 메모가 「첫 물음」으로 지목했던 자리다.

1. **취소는 가이드 층까지다.** 계약 헤더 · `_reference/` 정본 · 계약 스위트 4축 · CI 3모드 ·
   언어 중립 vector 27종 · Rust 축4 하네스는 **존치**한다. 69편 가이드를 v2 골격으로 다시 쓴다.
2. **이 카드는 규격 수립 + 파일럿 승인까지다.** 69편 전개는 후속 카드로 낸다.
   알고리즘 트랙이 `KAN-033`(규격) → `KAN-034`(111편 전개)로 갈린 것과 같은 분할이다.

### 계승 대상의 정본은 `sandbox/algo-guide-v2/SPEC.md` 다

요청서의 「새로 집필한 알고리즘 집필 전략」이 그 파일(958줄)이다.
`.claude/authoring/specs/algo-guide/spec.md:3-14` 가 스스로 *"이 명세는 대상을 잃었다"* 며
v2 를 정본으로 가리킨다 — v1 10단계 산출(`*-guide.mdx`)이 알고리즘 트랙에 0 편이기 때문이다.
v2 는 `KAN-034` 에서 111편에 전부 적용돼 종료됐다(커밋 `39ab3b5`).

### 두 층은 겹치지 않고 포갠다 — 이것이 설계의 축이다

| 층 | 무엇을 판정하는가 | 장치 |
| --- | --- | --- |
| **계약·구현** (ORD-006, 존치) | 계약이 옳은가 · 정본이 그것을 지키는가 | 계약 스위트 4축 · `check-contract` · `emit-vectors` · Rust 축4 |
| **원고** (algo-guide-v2, 새로 얹는다) | 본문 값이 실행에서 나왔는가 · 골격을 지켰는가 | `check-v2` · `check-proof` · `check-metaphor` · `bench-alt` |

algo-guide-v2 에 계약 층이 없는 것은 알고리즘이 함수 하나이기 때문이다. 자료구조는 연산이
복수이고 상한·한정자가 연산마다 갈리므로, v2 의 `invariant`·`perf` 절이 근거로 삼을 정본이
계약 헤더(`<name>.ts` JSDoc 여섯 항목)다. **그 층을 버리면 v2 절들이 근거를 잃는다.**

### 명세는 델타로 쓴다 — 같은 규칙을 두 곳에 적지 않는다

`sandbox/ds-guide-v2/SPEC.md` 를 새로 두되 **algo SPEC 을 base 로 삼고 갈리는 것만 적는다.**
공통 조항(`L24` 은유 · `L32` 헤딩 존댓말 · `L25` 표기 일관 · `L1` 분량 면제)은 중복 기재하지
않고 절 번호로 건다. *"한 사실이 두 곳에 적히면 둘이 갈라진다"* 가 ORD-006 과 v2 가 함께 쓰는
규약이고, 갈라진 자리에서 가이드가 코드보다 낙관적인 복잡도를 주장한 것이 이 트랙을 한 번
재설계하게 만든 결함이다.

**예외는 id↔헤딩 매핑 하나다** — `tools/section.ts` 가 기계로 읽으므로 완전해야 한다.

### 골격 이식 — 손대는 자리는 넷

원칙은 v2 자신의 것이다: *"항목을 늘려서 결손을 메우지 않는다. 처방도 항목 추가가 아니라
기존 절 직무의 재정의로 간다"*(`sandbox/algo-guide-v2/SPEC.md:76-79`).

| v2 항목 | 자료구조에서 | 처분 |
| --- | --- | --- |
| `concept`·`prereq`·`deep.build`·`deep.walk.pause`·`purpose.real`·`purpose.alt`·`related`·`deep.math`·`selfcheck` | 그대로 | 계승 |
| `deep.walk.step` | **연산 묶음마다 반복.** 요청서의 분량 면제가 겨눈 자리 | `repeat` 하한을 연산 묶음 수에 연동 |
| `purpose.fit`·`purpose.cue` | **직무 재정의.** ds 는 문제를 다루지 않는다 — 단서는 문제 문장이 아니라 *"어떤 연산이 어떤 복잡도로 동시에 필요한가"* 에서 나온다 | 헤딩 문구·직무 교체. 옛 문구는 `RETIRED_HEADING_WORDS`(`tools/check-v2.ts:534-548`)로 막는다 |
| `deep.walk.final` | `_reference/<name>.ts` 의 `#region guide:core` 를 **추출**한다 — 옮겨 적지 않는다 | `tools/guide-core.ts` 규약에 결속. **P16 과 충돌하므로 `S3` 에서 함께 처리한다** |
| `invariant`·`perf.*` | 계약 헤더가 정본. 절은 조건이 아니라 **조건의 이유**를 쓰고 표를 복사하지 않는다. 상한은 축3 성장률 $r = C(4n)/C(n)$ 실측을 인용하고 벽시계를 쓰지 않는다 | 계약 스위트와 결속 |
| **에스컬레이션** | **신설이 아니다.** `docs/ORD-006-conventions.md:1171`(8단계 표 6번 행)과 `:1252-1275`(발동 조건 · 첫 사례 `xorLinkedList` · 내용 셋)가 이미 확정해 뒀다 | ORD-006 §규약3 6단계 · §규약4 를 v2 절 체계로 **이관**. `perf` 아래 조건부 하위 절. `S2` 는 절 번호로만 인용하고 규격을 다시 쓰지 않는다 |

### 버리는 것 · 잇는 것

- **버린다**: ds-guide 8단계 골격(`.claude/authoring/specs/ds-guide/`) · `-guide.mdx` 69편 ·
  `.mdx` 안의 `export const steps` · **`tools/check-guide-rhythm.ts` 와
  `tools/_baseline/guide-rhythm.tsv`**. algo v1 이 그랬듯 **이름까지** 버린다 —
  이름이 남으면 직무도 남는다.
  - 그 도구는 `:34` 가 *"대상 집합은 `src/**/*-guide.mdx` 이고, 그것은 이제 자료구조
    트랙뿐이다"* 라고 적고 `:381` 글롭이 비면 `:387-390` 이 `process.exit(2)` 한다.
    `tools/ci.ts` GATES 에 등록돼 있으므로 **`.mdx` 가 0 이 되는 순간 CI 가 실패한다.**
    이 카드는 3편만 빼므로 66편이 남고, 도구 제거는 전개 카드 몫이다.
- **잇는다**: 계약 헤더 · `_reference/` · `.contract.ts` · 계약 스위트 · CI 3모드 ·
  `check-contract`·`emit-vectors`·`check-citations`·`check-links` · Rust 축4 ·
  `docs/ORD-006-conventions.md` 절 110개 · `docs/ORD-006-runbook.md` 불변 사실 146건.
- **`docs/ORD-006-wbs.md` 는 무효가 된다** — 유닛 32개가 「계약 + 8단계 가이드」를 한 단위로
  묶었기 때문이다. 계약만 남은 단위로 다시 뽑는 것은 전개 카드의 몫이다.

### 파일럿 3편 — 선정 근거는 실측이다

| 편 | 등급 | 무엇을 시험하는가 | rhythm 베이스라인 |
| --- | --- | --- | --- |
| `linear/deque` | `complexity` | 연산 8개 · 불변식 **없음** · 8단계 모범 예시라 **두 골격의 직접 대조**가 된다 | 25행 (3/0) |
| `tree/redBlackTree` | `complexity` | `worst` 한정자 계약의 정본(B17) · 연산 다수 + 불변식 있음 — `invariant` 절이 실제로 성립하는가 | 62행 (5/0) |
| `linear/xorLinkedList` | `invariant` | **규약4 (나) 첫 사례**(`docs/ORD-006-conventions.md:1259`) — 에스컬레이션 절이 실제로 켜지는 확정된 자리. 검증 등급의 셋째 값도 함께 덮는다 | 36행 (4/0) |

**`tree/treap` 을 쓰지 않는다.** `src/data-structures/tree/treap/treap.ts:45` 가 *"처분은
규약4 (나)가 아니라 **가이드**다 — 막는 것이 언어가 아니라 판정 규격이라 Rust 에서도 똑같이
안 보인다"* 라고 적어 두었다. 즉 `(-)` 이고, `docs/ORD-006-conventions.md:1254-1256` 규격상
`(-)` 면 절이 생략된다 — 그 편으로는 에스컬레이션 절이 한 번도 안 켜진다.

축4(`probabilistic/concurrentSkipList`, TS 정본 없음)는 파일럿 밖에 둔다. 규격에 **자리만**
만들고 전개 카드가 다룬다 — `deep.walk.final` 이 추출할 TS 정본이 없는 유일한 자리라 셋과
성격이 다르고, 넣으면 승인 게이트가 그 하나에 묶인다.

### 착수 전 실측 (2026-09-11)

| 항목 | 값 | 근거 |
| --- | --- | --- |
| 자료구조 종 수 | 69 | `src/data-structures/*/*` |
| 계약이 선 것 | 36 (`.contract.ts`) / TS 정본 35 (`_reference/`) | `probabilistic/concurrentSkipList` 는 축4라 TS 정본 없음 |
| ds-guide 8단계 재집필분 | 35편 | `-guide.mdx` 중 「이 구조가 필요한 이유」 보유 |
| ds-guide 구판(문제 기반) | 34편 | 69 − 35 |
| `<name>-problem.md` 잔존 | 33 | ORD-006 규약상 제거 대상 |
| ds 쪽 v2 사이드카 | **0** | `.sim.ts`·`.ref.ts`·`.proof.ts` 없음 |
| ds `.mdx` 중 시뮬 사용 | **69/69** | `AlgorithmSimulation` 전수 |
| `KAN-027` 워크트리·브랜치 | **없다** | `git worktree list`·`git branch` 에 부재 — main 에서 직접 돌았다 |

`KAN-027` 에 걷을 워크트리가 없으므로 **정지는 보드 이동으로 끝난다.**

### 버린 대안 셋

1. **계약 층까지 걷어내고 v2 사이드카 구조로 통일** — 규약 110개와 불변 사실 146건이 함께
   무효가 되고, v2 의 `invariant`·`perf` 가 근거로 삼을 정본이 사라진다. 유저가 (1)을 결재해
   배제됐다.
2. **이미 8단계로 쓴 35편을 두고 구판 34편만 v2 로** — 한 저장소에 두 골격이 영구히 남는다.
   판정 장치도 둘을 계속 병행해야 한다.
3. **규격과 69편 전개를 한 카드로** — 파일럿 승인 전에 전개를 시작하면 골격이 흔들릴 때 전부
   다시 쓴다. `KAN-033` 이 파일럿을 순차로 돌린 이유가 그것이다.

### 외부 검토 1회 (`plan-reviewer`, 2026-09-11)

지적 10건을 전부 반영했다 — 파일럿 ③ 교체(`treap` → `xorLinkedList`) · `tools/section.ts`
누락 · P16 의 조용한 미실행 · 에스컬레이션의 「신설 → 이관」 · `check-guide-rhythm` 처분 ·
`v2Guides()` 반환형 유지 · `check-proof` 의 `__cost` 혼입 · `.mdx`/`.md` 공존 처분 ·
`S2` 기계 판정 신설 · 「규약 110개 → 절 110개」. 플랜 원본은
`~/.claude/plans/kan-035-31t4by-purrfect-abelson.md`.

## 실행 계획
<!-- `S<n>`은 고정 id — 이름을 바꾸지 않는다. 체크 상태는 doc-step 이 갱신한다. -->
- [x] `S1` `KAN-027` 정지 + 딸린 카드 넷 재정의
- [x] `S2` `sandbox/ds-guide-v2/SPEC.md` 작성 — 델타 명세
- [x] `S3` 판정 장치 확장 — 파일 다섯 + 확인 둘
- [x] `S4` 시뮬 이관 규격 — `export const steps` → `.sim.ts` + `<!--viz:-->`
- [>] `S5` 파일럿 ① `linear/deque` <!-- claim:s=5c978e18 t=2026-09-13T18:11 -->
- [ ] `S6` 파일럿 ② `tree/redBlackTree` · ③ `linear/xorLinkedList`
- [ ] `S7` 종합 승인 요청 — 유저 게이트

### 단계별 완료 기준

**`S1` — `KAN-027` 정지 + 딸린 카드 넷 재정의**

보드 이동과 메모뿐이다. 워크트리가 없으므로 걷을 것이 없다.

- `KAN-027` 이 `doing → backlog`. 메모에 정지 시점 · 인계 상태(계약 36종 존치 · B군 20/37) ·
  이 카드 참조.
- `KAN-026`·`KAN-028` 메모에 「가이드 골격은 ds-guide-v2 를 따른다」.
- `KAN-031` 착수 조건(69종 계약이 전부 확정된 뒤)은 **유지**한다 — 계약 층을 존치하므로
  그 조건이 그대로 성립한다.
- `KAN-001` 봉인 조건에 새 카드 수를 반영한다(전개 카드가 뒤에 하나 더 붙는다).
- 판정: `python3 <skill>/scripts/kanban.py validate .` 가 error 0.

**`S2` — `sandbox/ds-guide-v2/SPEC.md` 작성**

항목 표 · id↔헤딩 매핑 · 갈리는 절의 작성법 · 조건부 절 규약 · L 조항.
계승 조항은 algo SPEC 절 번호로 인용하고, ds 신설분은 `L41` 부터 붙인다(번호를 재부여하지
않는다). 에스컬레이션은 `docs/ORD-006-conventions.md` §규약3 6단계 · §규약4 를 **인용**하고
규격을 옮겨 적지 않는다.

- 판정: **`bun test tools` 에 회귀 하나 신설** — ds SPEC 의 항목 전부가 `tools/section.ts`
  매핑을 갖는가. 사람 대조가 아니라 종료코드로 판정한다.
- 계승 조항의 중복 서술 0.

**`S3` — 판정 장치 확장**

파일 다섯을 고치고 둘을 확인한다. **`tools/section.ts` 가 먼저다** — 나머지의 입력이다.

1. `tools/section.ts` — `FIXED`·`PATTERNED`(`:34-68`)가 알고리즘 문구로 하드코딩돼 있고
   `parseSections(text)` 에 `kind` 인자가 없다. 시그니처를 넓히고 호출부 둘
   (`tools/check-v2.ts:2211,2293`)을 함께 옮긴다. **`:141` 이 미해소 헤딩을 `unresolved` 로
   올려 판정을 통째로 중단시키므로**, 이 파일을 안 고치고 ds 문구를 넣으면 그 편이 그 자리에서
   멈춘다.
2. `tools/guide-v2-targets.ts` — 대상 집합에 `src/data-structures/**/*-guide.md` 추가.
   **`v2Guides()` 반환형은 `string[]` 그대로 두고 `kindOf(path)` 를 별도 export 한다** —
   객체 배열로 바꾸면 호출부 넷(`check-v2.ts:2294` · `check-proof.ts:925` ·
   `check-metaphor.ts:385` · `build-html.ts:708-709`)이 한 커밋에서 같이 움직여야 하고,
   `tools/ci.ts` GATES 가 그 넷을 전부 `--all` 로 돌린다. 글롭은 여전히 **이 파일 한 곳**에만
   적는다(`sandbox/algo-guide-v2/README.md` 규약).
3. `tools/check-v2.ts` — `kind` 로 매핑을 가르고 ds 전용 조항 둘을 신설한다:
   ① 계약 헤더의 연산 표 ↔ `deep.walk.step` 이 다룬 연산의 **피복** ② 계약 표 복사 금지.
   **P16(`:1179-1231`, 호출 `:2157`, 로드 `:2226`)을 함께 고친다** — 지금은 `${stem}.ref.ts`
   **파일 전체**를 정규화 비교하는데 ds 편에는 그 파일이 없어 `:2282` 의 「미실행」으로 조용히
   빠진다. ds 에서는 `guide-core` 경로(`#region guide:core` + `stripInstrumentation`)로
   돌리고, 정본 부재는 **위반으로 올린다.**
4. `tools/check-proof.ts` — ds 사이드카가 import 해야 하는 정본을 `_reference/<name>.ts` 로
   가른다(algo 는 `.ref.ts`). **`mutantSiteFailures`(`:652-672`)가 `inRef` 를 구성하기 전에
   `stripInstrumentation` 을 통과시킨다** — ds `_reference/*.ts` 35편은 전부 `__cost` 를
   갖는데 그 줄은 추출 때 걷히므로 원고가 절대 보여 주지 않는다. 안 걷으면 대조 기준선이
   원고가 볼 수 없는 줄을 포함한다.
5. `tools/guide-core.ts` — 추출 대상을 `deep.walk.final` 에 결속하고, 시뮬 검사(`:357`)의
   대상을 `-guide.mdx` 한정에서 `.md` 로 넓힌다.
6. `tools/check-citations.ts` — `SCAN_GLOBS`(`:30`)에 `sandbox` 를 더한다. **`S2` 에서
   실측으로 드러난 구멍이다** — 새 `sandbox/ds-guide-v2/SPEC.md` 가 건 `경로:줄번호` 인용
   넷을 아무도 안 본다(손으로 대조했다). `algo SPEC` 도 같은 처지이고, algo 승격 조건이
   같은 이유로 `src/algorithms` 를 더한 선례가 있다
   (`sandbox/algo-guide-v2/README.md` 의 승격 조건 3번).

- **확인만**: `tools/check-metaphor.ts` · `tools/build-html.ts` — 2 의 `string[]` 유지 방침이
  지켜지면 변경이 없다. `bun run tools/ci.ts gates` 로 실제 무변경을 확인한다.
- 판정: `bun test tools` 통과 + 고친 다섯 각각에 회귀 시험 신설.

**`S4` — 시뮬 이관 규격**

`.mdx` 의 `export const steps` → `<name>-guide.sim.ts` + `<!--viz:{id}-->` 마커
(`tools/build-html.ts:178` 이 읽는 형식). 69/69 가 시뮬을 쓰므로 규격 없이는 전개가 막힌다.

- 판정: **규격이 `sandbox/ds-guide-v2/SPEC.md` §9 로 서고** `bun test src/_guide-sim`
  (`tools/ci.ts` SELF)이 통과한다.
- **실물 실증은 `S5` 의 첫 편이 진다**(2026-09-13 조정). 이관할 대상(`.mdx`)이 파일럿 편에만
  있으므로 이 work 안에서 닫히지 않는다 — 착수 시점 계획이 실증을 여기 걸어 둔 것이
  구조와 어긋났다. 마커 id ↔ `.sim.ts` export 키 ↔ ascii 펜스의 3자 일치(`L11`·`P6`)와
  `result` 대조(`L14`·`P9`)는 `S5` 완료 기준에 있다.

**`S5` — 파일럿 ① `linear/deque`** · **`S6` — 파일럿 ② `tree/redBlackTree` · ③ `linear/xorLinkedList`**

편 단위로 제출한다. 편마다 판정 다섯 + 처분 둘이다.

- 판정: `check-v2` 위반 0 · `check-proof` 통과 · `check-metaphor` 0 · `build-html` 0 ·
  `bun run tools/ci.ts all` 통과.
- 처분: `bun run tools/check-links.ts refs <경로>` 로 참조를 갱신한 뒤 `-guide.mdx` 제거,
  `tools/_baseline/guide-rhythm.tsv` 해당 행 제거.
- `S5` 를 단독 배치로 두는 이유는 **첫 편이 규격의 실물 시험**이기 때문이다 — 여기서 SPEC 이
  흔들리면 `S2` 로 돌아간다.

**`S7` — 종합 승인 요청**

검토서(`review-init`) + 계획 리포트. 판단 항목마다 상세를 달고 내부 기호를 그 자리에서 푼다.

- 판정: 검토서의 살아 있는 항목이 전부 승인. 승인 뒤에야 전개 카드를 신설한다.

## 검증
<!-- 무엇을 실행해 무엇이 나오면 이 카드가 끝난 것인가. -->

### 카드 종료 조건

셋이 동시에 성립할 때 이 카드가 끝난다.

1. `sandbox/ds-guide-v2/SPEC.md` 가 서 있고, 그 항목 전부가 `tools/section.ts` 매핑을 갖는
   것이 `bun test tools` 로 판정된다.
2. 파일럿 3편(`linear/deque` · `tree/redBlackTree` · `linear/xorLinkedList`)이 `-guide.md`
   + 사이드카로 서고, 아래 게이트를 전부 통과한다.
3. 검토서(`KANBAN.reviews/KAN-035-31T4BY.review.*`)의 살아 있는 항목이 전부 승인이다.

### 명령

```bash
bun run tools/ci.ts all                      # 3모드 + 게이트. 배치 종료마다
bun run tools/check-v2.ts --all              # 알고리즘 111편 + ds 파일럿 3편
bun run tools/check-proof.ts --all --require # 증명 0개도 위반
bun run tools/check-metaphor.ts --all
bun run tools/guide-core.ts check            # deep.walk.final 이 _reference/ 추출본과 같은가
bun run tools/check-citations.ts             # `경로:줄번호` 인용
bun run tools/check-links.ts check
bun run tools/check-contract.ts              # 명세↔스텁↔정본↔스위트 정합 (계약 층, 존치)
bunx tsc --noEmit
bunx --bun @biomejs/biome check <바꾼 경로>    # bunx biome 은 다른 패키지다
```

편별 빌드와 참조 스윕은 대상을 받는다.

```bash
bun run tools/build-html.ts src/data-structures/linear/deque/deque-guide.md
bun run tools/check-links.ts refs src/data-structures/linear/deque/deque-guide.mdx
```

### 단계별 판정

```bash
# S1 — KAN-027 정지
python3 <skill>/scripts/kanban.py validate .      # error 0
#   ↑ KAN-027 이 backlog, KAN-026·KAN-028·KAN-031·KAN-001 메모가 갱신돼 있다

# S2 — 델타 명세
bun test tools                                     # 신설 회귀: ds SPEC 항목 ↔ section.ts 매핑 전수 일치
#   ↑ 매핑에 빠진 항목 0. 계승 조항의 중복 서술 0

# S3 — 판정 장치
bun test tools                                     # 고친 다섯 각각의 회귀
bun run tools/check-v2.ts --all
#   ↑ guide-v2-targets 가 ds 3편을 대상으로 내고 kindOf 가 algo/ds 를 가른다
#   ↑ ds 편의 P16 이 「미실행」이 아니라 **실제 판정**으로 나온다 — 이것이 이 단계의 핵심이다
bun run tools/ci.ts gates
#   ↑ check-metaphor·build-html 이 무변경인 것을 실제로 확인한다(string[] 유지 방침)

# S4 — 시뮬 이관
bun test src/_guide-sim                            # 182편이 함께 쓰는 모듈의 회귀
#   ↑ 파일럿 3편의 <!--viz:--> 마커 ↔ .sim.ts export 키가 일치

# S5·S6 — 편 하나마다
bun run tools/check-v2.ts <편>                      # 위반 0
bun run tools/check-proof.ts <편> --require
bun run tools/check-metaphor.ts <편>
bun run tools/build-html.ts <편>                    # L19 웹 동등성 · L37 항목 레일
bun run tools/ci.ts all
#   그리고 처분 둘 — check-links refs 로 참조 갱신 뒤 .mdx 제거, guide-rhythm.tsv 행 제거

# 파일럿 종료
find src/data-structures -name "*-guide.md" | wc -l    # 3
find src/data-structures -name "*-guide.mdx" -not -path "*_deprecated*" | wc -l   # 69 → 66
wc -l < tools/_baseline/guide-rhythm.tsv               # 69 → 66 (+ 헤더)
```

### 함정 (전부 실측)

- **`.mdx` 를 0 으로 만들지 않는다.** `tools/check-guide-rhythm.ts:381` 글롭이 비면
  `:387-390` 이 `process.exit(2)` 하고, 그 도구는 `tools/ci.ts` GATES 에 있다. 이 카드는
  3편만 빼므로 66편이 남아 안전하다. 도구 제거는 전개 카드 몫이다.
- **`bunx biome` 을 쓰지 않는다.** npm 의 `biome` 은 이 저장소가 설정한 `@biomejs/biome` 와
  다른 패키지이고, 아무것도 검사하지 않고 exit 0 을 준다.
- **`bun test <경로>` 와 `biome check <디렉터리>` 는 대상 파일이 0개면 exit 1** 이다.
- **파일을 지우거나 옮기기 전에 `check-links.ts refs` 를 돌린다.** 손 grep 은 패턴을
  빠뜨리고, 빠뜨린 것을 본인이 알 수 없다.
- **축3 은 벽시계를 쓰지 않는다.** `__cost` 누적 카운터로만 잰다 —
  `tools/check-contract.ts` 가 `performance.now` 호출을 구문 트리로 센다.

## 수행 내역
<!-- KANBAN:LOG append-only — 아래로만 덧붙인다. 위를 고치지 않는다. -->
- 2026-09-11T11:44 · s:5c978e18 — `전략` 섹션 교체
- 2026-09-11T11:45 · s:5c978e18 — `실행 계획` 섹션 교체
- 2026-09-11T11:45 · s:5c978e18 — `검증` 섹션 교체
- 2026-09-11T11:52 · s:5c978e18 — `전략` 섹션 교체
- 2026-09-13T16:52 · s:5c978e18 · S1 doing — 착수
- 2026-09-13T16:54 · s:5c978e18 · S1 done — KAN-027 정지(doing→backlog, 메모 앞에 인계 문단) · KAN-026·KAN-028 에 ds-guide-v2 골격 전환 · KAN-001 봉인 조건 31장 예고 · KAN-031 착수 조건 유지(계약 층 존치라 그대로 성립) · KAN-035 브리프 넷 채우고 메모 축약. validate error 0
- 2026-09-13T16:54 · s:5c978e18 · S2 doing — 착수
- 2026-09-13T17:07 · s:5c978e18 — `실행 계획` 섹션 교체
- 2026-09-13T17:07 · s:5c978e18 · S2 done — sandbox/ds-guide-v2/SPEC.md 384줄(algo SPEC 958줄의 델타). 항목 24개(필수 20·조건부 4) — perf.escalation 이 ORD-006 §규약3 6단계에서 이관돼 하나 늘었다. 회귀 tools/ds-spec.test.ts 4벌 신설, 변이 4종으로 실제 판정 확인. bun test tools 434 pass · ci.ts gates 11단계 통과 · 은유 0 · 인용 4건 손 대조(SCAN_GLOBS 밖이라 S3 에 추가 항목 넣음)
- 2026-09-13T17:42 · s:5c978e18 · S3 doing — 착수
- 2026-09-13T18:06 · s:5c978e18 · S3 done — 판정 장치 확장 — 계획의 파일 여섯 중 다섯을 고치고 둘을 확인했으며, 실측으로 셋이 늘었다. section.ts(kind 분기·공통분 한 벌) · guide-v2-targets(ds 대상 + kindOf/kindOfOr) · check-v2(CheckInput.kind · ds 정본 경로 · P16 부재를 위반으로 · P17/P18/P19 신설) · check-proof(_reference 경로 · inRef 계측 제거) · guide-core(.md 확장) · check-citations(sandbox). 늘어난 셋: build-html 이 parseSections 호출부였고(확인만 대상이 아니었다), contract-header.ts·ord006-escalation.ts 를 순수 모듈로 뽑았다(두 CLI 에 import.meta.main 가드가 없어 import 만으로 stdout 이 오염됐다). 회귀 tools/ds-guide-v2.test.ts 15벌 신설, 변이 3종 확인. ci.ts all 16단계 통과 · 449 pass · 인용 268건 통과(sandbox 를 넣자 낡은 인용 4건이 드러나 함께 고쳤다)
- 2026-09-13T18:07 · s:5c978e18 · S4 doing — 착수
- 2026-09-13T18:09 · s:5c978e18 — `실행 계획` 섹션 교체
- 2026-09-13T18:09 · s:5c978e18 · S4 done — ds SPEC §9 시뮬 이관 절차 확정 — 무엇이 어디로 가는가(표) · 자료구조는 시뮬이 여럿일 수 있다(연산 묶음마다) · 절차 다섯 · 판정 셋. 이관의 실제 비용은 result 와 ascii 펜스 둘이다(.mdx 에 없던 것 — 웹 전용이라 md 대응물이 없었다). bun test src/_guide-sim 통과. **실물 실증은 S5 의 첫 편이 진다** — 완료 기준을 그렇게 조정했고 사유를 실행 계획에 적었다
- 2026-09-13T18:11 · s:5c978e18 · S5 doing — 착수
- 2026-09-13T18:13 · s:5c978e18 — S5 착수 — 재료 확인까지 진행. deque 는 정본 113줄(#region guide:core 18-113) · 기존 8단계 mdx 318줄 · 계약 연산 8개. 재사용할 수치: 배열 하나(unshift) r=4.00 대 계약 r=1.00 · 경쟁 설계는 배열 두 개(등 맞대기)로 큐 패턴에서는 옳고 앞뒤 교대에서 무너진다 · 벽시계를 안 쓰는 근거(엔진의 shift 최적화). 집필은 다음 세션이 이어받는다 — 파일럿 한 편이 v2 골격 24항목 + 사이드카 셋이라 한 세션 예산 안에 안 들어간다
