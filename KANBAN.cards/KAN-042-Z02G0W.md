---
card: KAN-042-Z02G0W
title: vanEmdeBoasTree 검사 공백 보완 — 우주 비트 · 원소 비트 사다리
created: 2026-09-17
scope: src/data-structures/heap/vanEmdeBoasTree/**, src/data-structures/_contract/_fixtures/**, src/data-structures/_contract/runContract.vanEmdeBoasTree.test.ts, docs/ORD-006-conventions.md, docs/ORD-006-runbook.md, docs/ORD-006-inventory.tsv, tools/_baseline/citations.tsv
---

# KAN-042-Z02G0W — vanEmdeBoasTree 검사 공백 보완 — 우주 비트 · 원소 비트 사다리

## 전략
**푸는 것.** `heap/vanEmdeBoasTree` 의 **검사 공백**을 스위트로 닫는다. 계약은 연산마다 $O(\log\log u)$ 인데 축3 은 `O(1)` 계급으로 판정한다 — 판정이 배제하는 것은 「u · n 의 거듭제곱으로 자라는 구현」이고 계약이 배제하는 것은 그보다 넓어서, **로그 인수만큼 어기는 구현이 지나간다**(KAN-027 `S24` 가 헤더에 적어 둔 공백).

**지나가는 계열 둘(지금 스위트 다섯 시나리오를 전부 통과한다).**

| 공백 | 지나가는 구현 | 지금 비율 |
|---|---|---|
| u 쪽 — 이웃 찾기·갱신이 $O(\log u)$ | 비트 하나씩 내려가는 트라이(`_contract/_fixtures/bitTrieIntegerSet.ts`, **저장소에 있다**) | 1.19 · 1.16 |
| n 쪽 — 모든 연산이 $O(\log n)$(비교로만 견주는 균형 탐색 트리) | 결정론 트립(KAN-027 이 스크래치에만 두었다 — **이 카드가 fixture 로 세운다**) | 1.13 · 1.26 |

**공백의 출처는 판정 규격이 아니라 시나리오가 사다리를 읽는 방식이다.** 지금 시나리오는 사다리의 n 을 원소 수나 우주 크기로 그대로 읽어 세 점이 4 배 간격이고, 로그 인수는 비율을 1.2 만 바꾼다. **파라미터를 비트 수가 두 배씩 오르게 읽으면** 로그가 비율 2.0 으로 보인다.

**채택하는 것 — 후보 (가)와 (나) 둘 다.** KAN-027 이 실측해 둔 값이 근거다.

- **(가) 우주를 비트 수로 올리는 사다리** — 비트 수 5 · 10 · 20, bound `O(log n)`. 트라이 1.92 · 1.96 **실패**, 정본 1.12 · 1.11 통과. 비용: 시나리오 하나가 정본 계측 142 만(지금 다섯 시나리오 합 243 만).
- **(나) 담긴 수를 비트 수로 올리는 사다리** — 우주 $2^{22}$ 고정, 담긴 수 비트 5 · 10 · 20, bound `O(1)`. 트립 1.80 · 2.22 **실패**, 정본 통과. 비용: 시나리오 하나가 정본 계측 3,736 만(**지금 합의 15 배**).

**버리는 것.** (다) 허용치 좁히기 — 하네스 규격 변경이고 상수 몫이 크면 로그 계열이 도로 통과한다(산술). (라) `O(log log n)` 계급 신설 — 더 거르는 것이 없다(KAN-027 산술).

**대가를 그대로 적는다.** (나)를 넣으면 이 구조의 축3 계측이 지금 합의 15 배가 된다. CI 가 그만큼 무거워지므로, **비용 수치를 카드와 규약에 적고** 무거운 시나리오를 어느 모드에서 돌릴지는 이 카드가 정한다(후보: 늘 돌린다 · `trials` 처럼 모드를 가른다 · 크기를 줄여 같은 성질만 남긴다).

**하지 않는 것.** 판정 규격(`judge.ts` 의 bound 목록·허용치)을 고치는 일 — 69 종에 걸린다. $\sqrt{\log u}$ 처럼 비율 1.41 인 계열은 (가)로도 안 걸린다는 사실은 **적고 남긴다**(이 카드가 닫는 것은 로그 인수 계열이다).

## 실행 계획
- [x] `S1` 결정론 트립 fixture 를 저장소에 세운다(n 쪽 공백을 지나가는 계열)
- [x] `S2` 사다리 둘을 계약 스위트에 넣는다 — (가) 우주 비트 · (나) 담긴 수 비트
- [x] `S3` 비용 판단과 마감 — 무거운 시나리오의 자리 · 헤더·규약 갱신 · 통합 검증
- [ ] `S4` 검토 반려 ① — 갱신 쪽 로그 인수를 재는 셋째 비트 사다리를 붙인다
- [ ] `S5` 검토 반려 ② — 낡는 문장의 런북 셋을 제자리 정정하고 나머지에 담당을 박는다

### 단계별 완료 기준

- **`S1`**: `_contract/_fixtures/` 에 새 파일로 결정론 트립(비교로만 견주는 균형 탐색 트리 — 키의 곱셈 해시를 우선순위로). 기존 fixture 는 고치지 않는다. **축1·축2 를 통과해야 한다**(계약을 지키면서 축3 만 어기는 계열이어야 공백의 증거가 된다) — KAN-027 실측은 무작위 3,000 걸음에서 모델과 어긋남 0 이었다. 자기시험(`runContract.vanEmdeBoasTree.test.ts`)에 등록해 **지금 사다리에서는 통과**하는 것을 먼저 고정한다(그것이 공백의 재현이다).
- **`S2`**: `vanEmdeBoasTree.contract.ts` 축3 시나리오에 둘을 더한다. (가) 비트 수 5 · 10 · 20 · bound `O(log n)` · (나) 우주 $2^{22}$ 고정 · 담긴 수 비트 5 · 10 · 20 · bound `O(1)`. **정본이 통과하고 트라이와 트립이 각각 걸리는 것**을 수치로 보인다(KAN-027 실측값과 대조해 어긋나면 그 사실을 적는다). `bun test` 자기시험으로 고정한다.
- **`S3`**: (나)의 계측 비용(예상 3,736 만)을 실측하고, 무거운 시나리오를 **어디서 돌릴지 정한다** — 늘 돌린다 · 모드를 가른다(`trials` 선례) · 크기를 줄여 같은 성질만 남긴다 중에서 고르고 근거를 적는다. `vanEmdeBoasTree.ts` 헤더의 「검사 공백」 블록을 **닫힌 것과 남은 것**으로 다시 쓰고(로그 인수 계열은 닫혔고 $\sqrt{\log u}$ 계열은 남는다), 규약에 결과를 새 절로 적는다. `bun run tools/ci.ts all` · `cargo test` · 게이트 통과.
- **`S4`**(검토 반려 ① — 항목 4): 비트 사다리 둘의 `covers` 에 넣기·지우기가 없어 **갱신 쪽 로그 인수는 어떤 비트 사다리도 재지 않는데** 헤더가 그것까지 닫혔다고 적었다. 검토자 실측: 요약과 클러스터 양쪽으로 재귀하는 넣기 계열은 일곱 시나리오를 전부 지나간다. 채택한 길은 **시나리오를 붙이는 쪽**이다 — 4 배 갱신 시나리오의 본문(키 0 을 둔 채 우주 마지막 키를 넣었다 지우기 세 번)을 우주 비트 사다리로 옮겨 여덟째 시나리오로 세운다(`covers` 넣기·지우기 · `worst` · bound `O(log n)` · 적대 참). 정본이 통과하고 **트라이가 걸리는 것**을 자기시험에 수치로 고정한다(검토자 실측: 정본 15 · 17 · 19, 트라이 14 · 24 · 44). 함께 맞출 자리 넷 — 헤더 「검사 공백」의 닫힌 것 표, 계약 파일 머리말의 가르는 표, 규약 새 절의 「닫힌 것은 계열 둘」 문장과 「이 카드가 고친 자리」, 런북 불변 사실 400 의 트라이 비율 문장. **n 쪽은 서술만 한정한다** — 조회는 상수인데 갱신만 담긴 수의 로그인 계열은 이 계약에서 자연스럽지 않다는 판단을 그 자리에 적는다.
- **`S5`**(검토 반려 ② — 항목 7): 낡는 문장 일곱을 「고치지 않았다」로 넘긴 근거가 실물보다 셌다. 기존 절을 고치지 않는다는 규칙은 규약 문서 한 파일에만 걸리고, 제자리 정정은 줄 수를 그대로 두면 아무 줄도 밀지 않으며, 인용 대장은 이 카드 scope 안이다. **런북 셋**(불변 사실 63 · B17 카드 줄 · T2-05 카드 줄)은 줄 수를 유지한 채 제자리에서 고치고 인용 게이트로 표류를 확인한 뒤 대장을 갱신한다. **규약 둘**(B13 절 · T2-05 절 4)은 규칙대로 두되 `KAN-049` 의 브리프에 명시로 넣어 소유자를 만든다. 새 절의 「낡는 문장」 표를 **고친 것 · KAN-049 담당 · 앞 카드 담당 그대로** 셋으로 갈라 적고, 규약 정정 담당 표에서 이 카드로 배정된 행들이 어떻게 처리됐는지 새 절에 남긴다. 승인 항목에 검토자가 남긴 메모 셋도 같은 배치에서 반영한다 — 로그 로그 계급이 상한 구간에 서려면 밑 비트 수가 **4 이상**이어야 한다는 하한(밑 2 · 3 은 첫 비율이 상한을 넘는다), 하네스 시험 이름이 행·한정자·상한·적대 여부로만 지어져 **같은 이름이 둘 나올 수 있다**는 사실, 비트 수를 세는 도우미가 이 계약 파일 안에만 있어 재사용자가 복사해야 한다는 사실.

## 검증
**카드 종료 조건.**

1. 결정론 트립 fixture 가 저장소에 있고 **축1·축2 를 통과**한다(계약을 지키면서 축3 만 어기는 계열).
2. 사다리 둘이 계약 스위트에 있고, **정본은 통과 · 트라이는 (가)에서 · 트립은 (나)에서 걸린다** — 수치가 자기시험으로 고정된다.
3. (나)의 계측 비용이 실측으로 적히고, 무거운 시나리오를 어디서 돌릴지 정해져 있다.
4. 헤더의 「검사 공백」 블록이 **닫힌 것과 남은 것**으로 다시 쓰였다($\sqrt{\log u}$ 계열이 남는다는 사실 포함).
5. `bun run tools/ci.ts all` · `cd rust && cargo test` · `bun run tools/check-citations.ts` 통과. 하네스(`runContract.ts` · `judge.ts`)는 고치지 않았다.

## 수행 내역
<!-- KANBAN:LOG append-only — 아래로만 덧붙인다. 위를 고치지 않는다. -->
- 2026-09-17T02:27 · s:f2fd90c7 — `전략` 섹션 교체
- 2026-09-17T02:28 · s:f2fd90c7 — `실행 계획` 섹션 교체
- 2026-09-17T02:28 · s:f2fd90c7 — `검증` 섹션 교체
- 2026-09-17T02:33 · s:f2fd90c7 · S1 doing — 착수
- 2026-09-17T02:39 · s:f2fd90c7 · S1 done — 완료
- 2026-09-17T02:39 · s:f2fd90c7 · S1 — `S1` — 결정론 트립 fixture `_contract/_fixtures/hashPriorityTreapIntegerSet.ts` 를 세웠다(키의 곱셈 해시를 우선순위로 쓰는 트립 · 비교로만 견준다). 축1·축2 를 `runContract` 등록으로 통과시키고, 지금 다섯 시나리오를 전부 통과하는 것(공백의 재현)을 자기시험에 수치로 고정했다 — 넣기 8·9·11 · 지우기 14·16·20 · 조회 63·66·81 · 우주 둘 13·14·13 / 7·9·7.
- 2026-09-17T02:39 · s:f2fd90c7 · S2 doing — 착수
- 2026-09-17T02:51 · s:f2fd90c7 · S2 done — 완료
- 2026-09-17T02:51 · s:f2fd90c7 · S2 — `S2` — 계약 스위트 끝에 사다리 둘을 붙였다. (가) 우주 비트 5·10·20 · bound `O(log n)` — 정본 17·19·21(1.12·1.11) 통과 · 트라이 38·73·143(1.92·1.96) 실패로 KAN-027 실측 그대로 재현. (나) 우주 2^22 고정 · 담긴 수 비트 사다리 · bound `O(1)` — 트립 15·39·66(2.60·1.69) 실패 · 정본 33·34·34 · 트라이 75·78·78 통과. (나)의 끝점을 2^20 에서 2^12 로 낮춰 달았다(끝점 2^20 도 판정이 같고 계측 3,736 만인데, 크기 순 배열 fixture 가 원소 2^20 을 채우는 데 원소 수의 제곱이 들어 자기시험이 그 계열을 못 태운다). 달아 둔 (나)의 계측은 1,714 만이고 그중 1,703 만이 우주 세우기 준비다. 수치는 배치1 문서 4절.
- 2026-09-17T03:02 · s:f2fd90c7 · S3 doing — 착수
- 2026-09-17T03:23 · s:f2fd90c7 · S3 done — `S3` — 무거운 시나리오의 자리를 ⓑ(고정 파라미터를 낮춘다)로 정했다. (나)의 우주를 2^22 에서 2^16 으로 내리니 정본 계측이 1,713 만에서 374,329 로 45.8 배 줄고 여섯 대상의 판정이 그대로다(우주 2^14·2^16·2^18·2^20·2^22 전부 같았다 — 트립만 실패, 비율 2.60·1.69 는 우주와 무관). 2^16 은 4 배 사다리 조회 시나리오와 같은 값이라 비트 사다리의 끝점이 그 시나리오의 가운데 점과 같은 입력이 되고 여섯 대상의 걸음이 전부 맞아떨어진다(30·66·60·14·12,280·75,392) — 자기시험에 그 일치를 고정했다. 버린 것: ⓐ 7.05 배를 내고 사는 것 없음 · ⓒ 15.4 배 + 크기 순 배열을 못 태움 · ⓓ 모드 가름은 닫으려던 「기본 경로가 못 보는 자리」를 다시 기본 경로 밖으로 뺀다. 일곱 합 4,229,262(1.74 배)라 늘 돈다. adversarial 을 true 에서 false 로 되돌렸다 — 입력이 조회 시나리오와 같고 끝점 걸음이 오히려 작으며(트립 66 대 81), 이름을 가르는 자리는 규약2 가 정해 둔 차례(#2)다. 헤더 「검사 공백」을 닫힌 것(로그 인수 계열 둘 · (가) bound 가 O(log n) 인 까닭 = 사다리 파라미터가 비트 수 b 라 log log u 가 log b 다)과 남은 것(√(log u) 는 비율 1.41 이라 O(log n) 구간 안이라 통과)으로 다시 썼다. 규약에 일반화한 새 절(조건 셋 · 비용은 준비에서 온다 · bound 를 새 파라미터의 말로 · 적대 여부는 이름을 가르는 자리가 아니다 · 낡는 문장 표), 런북 끝에 불변 사실 400–402. 검증: ci.ts all 17 단계 통과 · cargo test 13 통과 0 실패 · 인용 1,211 건 대장 1,199 행 지문 일치 · 링크 863 · check-contract 67 종 · vector 56 종 · guide-core · tsc · biome(고친 두 파일 경고 0, 스텁의 기존 경고 6 은 그대로).
- 2026-09-17T03:23 · s:f2fd90c7 — S24 가 「채택하면 후속 카드가 정정한다」고 적은 낡는 문장 일곱 줄은 정정하지 않았다 — 기존 절을 고치는 일이고 문서 가운데를 고치면 인용 래칫이 밀린다. 새 규약 절의 「낡는 문장 — 고치지 않았다」 표에 적었고 정정 자리는 KAN-049 가 후보다(사람 결정).
- 2026-09-17T04:01 · s:f2fd90c7 — `실행 계획` 섹션 교체
